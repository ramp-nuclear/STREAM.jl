---
phase: quick
plan: 260322-l7z
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/lof_transient.jl
autonomous: true
requirements: [QUICK]
must_haves:
  truths:
    - "Script runs standalone with `julia --project examples/lof_transient.jl` and exits cleanly"
    - "Script produces 8+ plot files in examples/output/lof_transient/"
    - "Script prints key metrics (mdot_ss, mdot_nc, T_max_ss, T_max_nc, flapper fire time, NC time, energy balance) to stdout"
    - "Time-series plots show pump trip, flapper open, and NC establishment annotations"
    - "Spatial profile snapshots show temperature/Re/HTC at 3+ time points"
  artifacts:
    - path: "examples/lof_transient.jl"
      provides: "Standalone LOF transient visualization script"
      min_lines: 200
  key_links:
    - from: "examples/lof_transient.jl"
      to: "src/examples.jl"
      via: "build_loop_lof_bypass()"
      pattern: "build_loop_lof_bypass"
    - from: "examples/lof_transient.jl"
      to: "src/solvers.jl"
      via: "solve_steady, solve_transient"
      pattern: "solve_transient"
---

<objective>
Create a standalone LOF transient example script with comprehensive plots and analysis.

Purpose: Visual validation tool for developers to verify physical correctness of the bypass LOF simulation — mass flow reversal, flapper firing, natural circulation establishment, energy balance.
Output: `examples/lof_transient.jl` script + plots saved to `examples/output/lof_transient/`
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/examples.jl (build_loop_lof_bypass — builder function for bypass topology)
@src/solvers.jl (solve_steady, solve_transient — solver API)
@test/test_loss_of_flow.jl (_lof_bypass_ic — IC strategy, constants, callback construction)
@src/components/flapper.jl (Flapper — xi, T_open variables)
@src/components/thermal_channel.jl (ChannelHeatFlux — T[n], Re[n], Nu[n], h_tc[n], v[n], q_wall[n], dP, T_out)
@src/components/channel.jl (Channel — T[n], Re[n], Nu[n], h_tc[n], v[n], q_wall[n], dP, T_out)
@src/components/misc.jl (Inertia, HeatExchanger)

<interfaces>
<!-- Key constants and IC strategy from test_loss_of_flow.jl -->
Constants (reuse same values):
  BYPASS_N=10, BYPASS_L_CH=1.0, BYPASS_D_CH=0.01, BYPASS_T_WALL=373.15,
  BYPASS_T_INLET=313.15, BYPASS_G_ACC=9.80665, BYPASS_L_OVER_A=1.75e5,
  BYPASS_R_EXT=1.0e6, BYPASS_THRESHOLD=0.01, BYPASS_DT_RAMP=5.0, BYPASS_DP_REF=1.5e4

IC strategy (_lof_bypass_ic pattern):
  1. Build reference loop: Pump(DP_REF) -> HX(T_inlet) -> ChannelHeatFlux(g=-G_ACC) -> Pump
  2. solve_steady to get mdot_ss, T_ss[1:n]
  3. Build bypass system via build_loop_lof_bypass(...)
  4. Set ICs: ine.inlet.mdot=>mdot_ss, ret.inlet.mdot=>mdot_ss,
     Dt(ret.inlet.mdot)=>0.0, flapper.T_open=>1e30, ch.T[i]=>T_ss[i], ret.T[i]=>T_inlet
  5. Build native ContinuousCallback monitoring ine.inlet.mdot crossing threshold

Solver call: solve_transient(ssys, op, t_arr; callbacks=cb)

Accessible observables on compiled system (ssys):
  ssys.ch.T[i], ssys.ch.Re[i], ssys.ch.h_tc[i], ssys.ch.q_wall[i], ssys.ch.v[i], ssys.ch.dP
  ssys.ret.T[i], ssys.ret.Re[i], ssys.ret.h_tc[i], ssys.ret.q_wall[i], ssys.ret.v[i], ssys.ret.dP
  ssys.ch.inlet.mdot, ssys.ret.inlet.mdot
  ssys.flapper.inlet.mdot, ssys.flapper.xi, ssys.flapper.T_open
  ssys.ine.inlet.mdot
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create examples/lof_transient.jl standalone script</name>
  <files>examples/lof_transient.jl</files>
  <action>
Create `examples/` directory and `examples/lof_transient.jl`. The script must be self-contained and well-commented. Structure:

**Header and imports:**
- `using STREAM, ModelingToolkit, DifferentialEquations, Plots, Statistics`
- `using ModelingToolkit: t_nounits as t`
- Set `gr()` backend

**Section 1: Parameters** (copy from test constants, clearly labeled):
- n=10, L_ch=1.0, D_ch=0.01, T_wall=373.15, T_inlet=313.15, g_acc=9.80665
- L_over_A=1.75e5, R_ext=1.0e6, threshold=0.01, dt_ramp=5.0, dP_ref=1.5e4

**Section 2: Steady-state reference** (replicate _lof_bypass_ic logic):
- Build reference loop: @named pump_ref = Pump(dP_ref), hx_ref = HeatExchanger(T_bc=T_inlet), ch_ref = ChannelHeatFlux(n=n, geometry=PipeGeometry_circular(L_ch, D_ch), g=-g_acc, T_wall=T_wall)
- Connect, compose, mtkcompile, solve_steady
- Extract mdot_ss, T_ss[1:n]
- Print: "Steady-state mdot = $mdot_ss kg/s", "Steady-state T_max = $(maximum(T_ss)) K"

**Section 3: Build bypass system and ICs** (replicate _lof_bypass_ic):
- build_loop_lof_bypass(; n, L_ch, D_ch, T_wall, T_inlet, L_over_A, g_acc, R_ext, threshold, dt_ramp)
- Set op with ine.inlet.mdot=>mdot_ss, ret.inlet.mdot=>mdot_ss, Dt(ret.inlet.mdot)=>0.0, flapper.T_open=>1e30, ch.T[i]=>T_ss[i], ret.T[i]=>T_inlet
- Build ContinuousCallback: variable_index for T_open and ine_mdot, condition u[i_ine_mdot]-threshold, affect_neg sets u[i_T_open]=integrator.t

**Section 4: Solve transient:**
- t_arr = range(0.0, 300.0; length=3001)
- sol = solve_transient(ssys, op, t_arr; callbacks=cb)
- Check sol.retcode == ReturnCode.Success, error if not

**Section 5: Extract time series** into plain arrays for plotting:
- t_vec = collect(t_arr)
- mdot_ch = sol[ssys.ch.inlet.mdot, :], mdot_ret = sol[ssys.ret.inlet.mdot, :]
- mdot_flap = sol[ssys.flapper.inlet.mdot, :], mdot_ine = sol[ssys.ine.inlet.mdot, :]
- T_ch[i,:] for i in 1:n, T_ret[i,:] for i in 1:n
- q_wall_ch[i,:], Re_ch[i,:], htc_ch[i,:] for i in 1:n
- xi = sol[ssys.flapper.xi, :], T_open = sol[ssys.flapper.T_open, :]
- dP_ch = sol[ssys.ch.dP, :], dP_ret = sol[ssys.ret.dP, :]

**Section 6: Compute key metrics and print:**
- mdot_nc = abs(mdot_ch[end]) (NC mass flow)
- T_max_nc = maximum(T_ch[:, end]) (max T at NC)
- T_max_ss = maximum(T_ss) (max T at SS)
- flapper_fire_time = T_open[end] < 1e10 ? T_open[end] : NaN (when flapper fired)
- NC establishment time: find first index after flapper_fire_time+dt_ramp where abs(diff(mdot_ch))/dt < small tolerance; use approximate criterion: first time after flapper fully open where |d(mdot_ch)/dt| < 1e-5
- Energy balance at final time: Q_wall = sum(q_wall_ch[:,end]), Q_advect = abs(mdot_ch[end])*cp_water(T_inlet)*abs(T_ch_max_end - T_inlet_to_ch), print ratio
- Print all metrics in a formatted block

**Section 7: Create output directory:**
- `mkpath("examples/output/lof_transient")`
- `outdir = "examples/output/lof_transient"`

**Section 8: Time-series plots** (one plot per figure, saved as PNG):

Plot 1 — Mass flow rates:
- 4 lines: ch, ret, flapper, ine mdot vs time
- Add vline at flapper_fire_time (label "Flapper fires"), vline at flapper_fire_time+dt_ramp (label "Flapper open")
- xlabel="Time [s]", ylabel="Mass flow [kg/s]", title="LOF Bypass: Mass Flow Rates"
- savefig("$outdir/01_mass_flow_rates.png")

Plot 2 — Channel temperatures T[1..n]:
- n lines for ch.T[i] vs time, use a colormap (palette=:viridis or manual gradient)
- Label each line "Cell $i"
- Add event markers (vlines)
- savefig("$outdir/02_channel_temperatures.png")

Plot 3 — Return channel temperatures T[1..n]:
- Same as Plot 2 but for ret.T[i]
- savefig("$outdir/03_return_temperatures.png")

Plot 4 — Heat flux q_wall per cell:
- n lines for ch.q_wall[i] vs time
- savefig("$outdir/04_heat_flux.png")

Plot 5 — Reynolds numbers Re per cell:
- n lines for ch.Re[i] vs time
- savefig("$outdir/05_reynolds.png")

Plot 6 — HTC h_tc per cell:
- n lines for ch.h_tc[i] vs time
- savefig("$outdir/06_htc.png")

Plot 7 — Flapper state:
- 2 subplots (layout=(2,1)): top=xi vs time, bottom=T_open vs time (clamp display to sensible range)
- savefig("$outdir/07_flapper_state.png")

Plot 8 — Pressure drops:
- 2 lines: ch.dP, ret.dP vs time
- savefig("$outdir/08_pressure_drops.png")

**Section 9: Spatial profile snapshots:**

Pick 3 time indices: t_snap = [1, idx_at_flapper_fire, end] (before LOF, at transition, NC established).
For robustness, find idx closest to flapper_fire_time.

Plot 9 — Temperature profiles at snapshots:
- x-axis = cell index (1:n), y-axis = T[K]
- 3 lines (one per snapshot time), labeled "t=0s", "t=$(round(t_fire))s", "t=300s"
- savefig("$outdir/09_temperature_profiles.png")

Plot 10 — Reynolds profiles at snapshots:
- Same structure as Plot 9 but for Re
- savefig("$outdir/10_reynolds_profiles.png")

Plot 11 — HTC profiles at snapshots:
- Same for h_tc
- savefig("$outdir/11_htc_profiles.png")

**Comments:** Each section should have a block comment explaining what physical behavior to expect. For example:
- "After pump trips (t=0), inertia carries momentum. mdot decays exponentially."
- "When mdot drops below threshold, flapper fires. xi ramps from 0 to 1 over dt_ramp seconds."
- "After flapper opens, buoyancy drives reversed flow through heated channel (mdot_ch < 0)."
- "NC equilibrium: ch flow reversed (upward), ret flow forward (downward). T_max at cell 1 (top of heated channel)."

**Important implementation notes:**
- Use `Plots.jl` with `gr()` backend (lightweight, no display needed for headless)
- All plot sizes: `size=(900, 600)` for readability
- Use `dpi=150` for decent quality without huge files
- Add `legend=:topright` or `:outerright` as appropriate
- For the `ret` channel: note that `ret` is a `Channel` (not `ChannelHeatFlux`), so `Re[i]`, `h_tc[i]`, `q_wall[i]` are available but `q_wall[i]` is thermal.Q_flow/n (heat from thermal port pinned to T_inlet, so small)
- The `v[i]` observable is signed velocity (inlet.mdot / (rho*A))
- Ensure `.gitignore` already has patterns to ignore PNG output, or add `examples/output/` to `.gitignore`
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM && julia --project -e 'include("examples/lof_transient.jl")' 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Script runs to completion without error
    - 11 PNG files exist in examples/output/lof_transient/
    - Stdout contains: mdot_ss, mdot_nc, T_max_ss, T_max_nc, flapper fire time, energy balance ratio
    - Plots show physical behavior: mdot crossing zero, temperature rise, flapper transition
  </done>
</task>

<task type="auto">
  <name>Task 2: Add examples/output/ to .gitignore and verify</name>
  <files>.gitignore</files>
  <action>
Append `examples/output/` to `.gitignore` if not already present. This prevents committing generated PNG files.

After updating .gitignore, do a quick verification that the script output directory is properly ignored:
- `git status` should not show examples/output/ files as untracked after the script has been run.
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM && grep -q "examples/output" .gitignore && echo "PASS: gitignore updated"</automated>
  </verify>
  <done>.gitignore contains examples/output/ pattern; generated PNGs are not tracked</done>
</task>

</tasks>

<verification>
1. `julia --project examples/lof_transient.jl` completes without error
2. 11 PNG files exist in `examples/output/lof_transient/`
3. Stdout metrics are physically reasonable: mdot_ss > 0, mdot_nc > 0 and < mdot_ss, T_max_nc > T_inlet
4. `examples/output/` is in `.gitignore`
</verification>

<success_criteria>
- Standalone script runs and produces all plots and metrics
- Plots visually capture the full LOF transient: pump coastdown, flapper firing, flow reversal, NC establishment
- Key metrics printed to stdout enable quick numerical validation
- Output directory is gitignored
</success_criteria>

<output>
After completion, create `.planning/quick/260322-l7z-create-lof-transient-example-script-with/260322-l7z-SUMMARY.md`
</output>
