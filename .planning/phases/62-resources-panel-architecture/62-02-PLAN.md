---
phase: 62
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gui/src/store/useStore.ts
  - gui/src/store/__tests__/resources.slice.test.ts
  - gui/src/store/__tests__/modelOptions.test.ts
  - gui/src/store/__tests__/activeLeftTab.test.ts
  - gui/src/store/__tests__/selection.test.ts
autonomous: true
requirements_addressed:
  - D-04
  - D-05
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-26
  - CD-03
  - CD-04
tags:
  - gui
  - zustand
  - state
  - foundation

must_haves:
  truths:
    - "Working branch is gui-redesign; no new branches created."
    - "Components never carry inline geometry/power-shape values; only _ref UUIDs."
    - "Renaming a Resource never breaks consumer references (lookup-at-render)."
    - "Per-kind name uniqueness enforced on add and rename; a Geometry and a Power Shape may share a name; two Geometries may not."
    - "Every Resource CRUD action pushes a snapshot BEFORE mutation (undo works across canvas + resource boundaries)."
    - "The unset Power Shape sentinel UUID is the constant '00000000-0000-0000-0000-000000000000', baked into the store's initial state."
  artifacts:
    - path: "gui/src/store/useStore.ts"
      provides: "Resources slice, ModelOptions slice, activeLeftTab state, selection-kind discriminator, snapshot extension"
      contains: "SENTINEL_UNSET_POWER_SHAPE"
    - path: "gui/src/store/__tests__/resources.slice.test.ts"
      provides: "CRUD + uniqueness + undo/redo + sentinel coverage"
  key_links:
    - from: "addGeometry / addPowerShape / renameResource / removeResource / updateResource"
      to: "_pushSnapshot"
      via: "first call in each action body"
      pattern: "_pushSnapshot\\(\\)"
    - from: "selectNode"
      to: "clears selectedResourceId"
      via: "set() in action body"
      pattern: "selectedResourceId: null"
    - from: "selectResource"
      to: "clears selectedNodeId"
      via: "set() in action body"
      pattern: "selectedNodeId: null"
  references:
    - "D-04: Project tab body IS the Model Options form (Name / Description / Default fluid / Default g / Solver defaults)."
    - "D-05: Right Properties panel routes by selection kind; canvas / resource / project scopes are exclusive."
    - "D-08: Active left tab persists in .scp layout.active_left_tab; defaults to 'Components'."
    - "D-09: Components reference Resources by stable UUID via geometry_ref / power_shape_ref."
    - "D-10: Resource store is a zustand slice keyed by UUID with per-kind name uniqueness."
    - "D-11: UUIDs minted client-side via crypto.randomUUID(); never reused."
    - "D-12: Rename propagation automatic via UUID lookup at render time; no broken refs."
    - "D-13: Copy-paste of a component preserves the FK; does NOT duplicate the Resource."
    - "D-26: Unset Power Shape sentinel UUID baked into store initial state; not user-editable."
    - "CD-03: UUID generation via crypto.randomUUID() (planner choice; no uuid/nanoid dependency)."
    - "CD-04: Solver defaults expose abstol, reltol, dtmax."
---

<objective>
Extend `gui/src/store/useStore.ts` with the four new state slices Phase 62 introduces: **Resources** (geometries, powerShapes, fluids — UUID-keyed records per D-10), **ModelOptions** (D-04 — name, description, default fluid, default g, solver defaults per CD-04), **activeLeftTab** (D-08), and a **selection-kind router** (D-05 — `selectionKind: "none" | "component" | "resource" | "project"` discriminator with mutual exclusivity). Extend the `_pushSnapshot` / undo / redo machinery so every Resource and ModelOptions mutation is undoable (RESEARCH Pitfall 2). Bake the `unset` Power Shape sentinel UUID constant into the store's initial state per D-26 + RESEARCH "Alternatives Considered".

Purpose: this is the foundation Wave 2 surfaces ride on. No UI changes here; this is a pure state-layer expansion.

Output: a meaningfully larger `useStore.ts` with new types + actions + extended snapshot shape, plus four Vitest test files validating the slice invariants (D-09..D-13, D-26, INV-01..05, INV-08, INV-09, INV-17, CONS-only-irrelevant).
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-resources-panel-architecture/62-CONTEXT.md
@.planning/phases/62-resources-panel-architecture/62-RESEARCH.md
@.planning/phases/62-resources-panel-architecture/62-VALIDATION.md
@.planning/phases/62-resources-panel-architecture/62-UI-SPEC.md
@gui/src/store/useStore.ts
@gui/src/store/__tests__/useStore.test.ts
@CLAUDE.md

<interfaces>
<!-- Extracted from gui/src/store/useStore.ts (read once; do not re-explore) -->

Existing AppState (lines 35-83) currently contains:
- `nodes: Node[]`, `edges: Edge[]`, `selectedNodeId: string | null`, `bcs: BCEntry[]`
- Panel state (`bottomPanelOpen`, `bottomPanelHeight`, `toolboxCollapsed`, `sidebarCollapsed`)
- Validation (`errorNodeIds`, `validationResult`, `validateAndGate`, `clearValidation`)
- Layer view (`activeLayer: LayerView`)
- Persistence (`isDirty`, `currentFilePath`, `recentFiles`)
- Undo/redo (`_undoPast: CanvasSnapshot[]`, `_undoFuture: CanvasSnapshot[]`, `_pushSnapshot`, `undo`, `redo`)
- Canvas/BC actions
- File I/O (saveProject, saveProjectAs, loadProject, loadProjectFromPath, newProject)

Existing CanvasSnapshot (lines 25-29):
```
interface CanvasSnapshot { nodes: Node[]; edges: Edge[]; bcs: BCEntry[]; }
```

Existing _pushSnapshot (lines 211-217):
```
_pushSnapshot: () => {
  const { nodes, edges, bcs, _undoPast } = get();
  set({ _undoPast: [..._undoPast, { nodes, edges, bcs }].slice(-50), _undoFuture: [] });
},
```
Plus `undo` (218-232) and `redo` (234-249) which both read/write `nodes`, `edges`, `bcs`.

Existing UUID minting (line 304): `crypto.randomUUID()` is already used for node IDs. Per CD-03 + RESEARCH "UUID generation", use `crypto.randomUUID()` for Resource UUIDs too — no new dependency.

Existing instance-counter helper (top of file): `instanceCounters: Record<string, number>` + `getNextInstanceName(componentId)` and `clearInstanceCounters()`. The §3.5 smart-name-increment rule lives here. Phase 62 needs a parallel kind-scoped helper for Resource names (see Task 1 below).

Existing reconstructInstanceCounters import from "../lib/projectIO" (line 17). Don't break it.
</interfaces>

<sentinel_decision>
<!-- Per D-26 + RESEARCH "Alternatives Considered" recommendation -->

`SENTINEL_UNSET_POWER_SHAPE = "00000000-0000-0000-0000-000000000000"` (module-level const, exported from useStore.ts for downstream import).

Initial Resources state baked into the store includes a single PowerShape:
- `uuid: SENTINEL_UNSET_POWER_SHAPE`
- `name: "(leave unset — fill in code)"` (literal copy per UI-SPEC "Power Shape picker — extra fixed top entry")
- `kind: "unset"`
- `params: {}`

This sentinel:
- IS the value `power_shape_ref` carries when the user hasn't picked a real Power Shape
- Is NOT serialized into `.scp` (the deserialize path re-injects it on load)
- Is NOT shown in the Resources tab Power Shapes group (it lives only as the fixed top entry in the picker dropdown)
- Is NOT renameable, deletable, or duplicable
</sentinel_decision>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Resources slice + ModelOptions slice + activeLeftTab + selection discriminator types and actions to useStore.ts; extend CanvasSnapshot + _pushSnapshot/undo/redo</name>
  <files>gui/src/store/useStore.ts</files>
  <read_first>
    - .planning/phases/62-resources-panel-architecture/62-CONTEXT.md (D-04 model options fields; D-05 selection-kind exclusivity; D-08 activeLeftTab persistence; D-09 FK shape; D-10 Resources record shape; D-11 UUID minting; D-12 rename propagation; D-13 copy-paste preserves FK; D-26 unset sentinel)
    - .planning/phases/62-resources-panel-architecture/62-RESEARCH.md (Pattern 1 — Resource store slice with snapshot integration; Pattern 4 — Selection-kind router; Pitfall 2 — snapshot push omitted on Resource rename; "Alternatives Considered" — sentinel UUID rationale)
    - .planning/phases/62-resources-panel-architecture/62-UI-SPEC.md (Copywriting Contract — "Power Shape picker — extra fixed top entry" literal sentinel name; Solver defaults exposure — abstol/reltol/dtmax field set)
    - gui/src/store/useStore.ts (existing AppState interface; CanvasSnapshot; _pushSnapshot/undo/redo; crypto.randomUUID() usage at line 304)
    - CLAUDE.md (TypeScript / GUI conventions — no project-specific style rule, but follow the existing pattern in the file)
  </read_first>
  <behavior>
    Test contract (verified across Tasks 2-5):
    - Adding a GeometryResource mints a UUID and returns it; the new resource appears in `state.resources.geometries[uuid]`
    - Adding a PowerShapeResource with `kind="unset"` is forbidden (sentinel is the only unset; user-created PowerShapes pick from `uniform | z_cosine | file_loaded`)
    - Renaming to a name already used by another Geometry of the same kind throws `Error("A geometry named X already exists.")` (or returns a `Result.err` per implementer choice; UI-SPEC mandates the user-visible string but the store can surface it as a thrown error consumed by the picker)
    - A Geometry named `mtr_channel` and a PowerShape named `mtr_channel` may coexist (per-kind uniqueness only — D-10)
    - `_pushSnapshot()` is called BEFORE every Resource mutation (addGeometry, addPowerShape, renameResource, updateResource, removeResource)
    - Removing the sentinel PowerShape is a no-op (or throws — implementer picks; either is acceptable)
    - `state.activeLeftTab` defaults to `"Components"`; `setActiveLeftTab("Resources")` changes it; the value is NOT included in `_pushSnapshot` (UI state, not content state — same exclusion as `selectedNodeId`)
    - `state.selectionKind` is derived from `selectedNodeId | selectedResourceId`: `"component"` if `selectedNodeId != null`, `"resource"` if `selectedResourceId != null`, otherwise `"none"`. `selectNode(id)` clears `selectedResourceId`; `selectResource(uuid, kind)` clears `selectedNodeId`; `clearSelection()` clears both
    - `modelOptions` initial state: `{ name: "", description: "", default_fluid: "water", g_default: 9.80665, solver: { abstol: 1e-8, reltol: 1e-6, dtmax: null } }`; `setModelOptions(patch)` does a shallow merge + pushSnapshot + `isDirty: true`
  </behavior>
  <action>
    Edit `gui/src/store/useStore.ts` (single file, multi-section edit). Concrete additions:

    **1. Module-level constants (top of file, near `RECENT_FILE_NAME`):**
    - `export const SENTINEL_UNSET_POWER_SHAPE = "00000000-0000-0000-0000-000000000000";`
    - `const DEFAULT_FLUID = "water";` (string, read-only display per D-04)
    - `const DEFAULT_G = 9.80665;` (m/s^2 per D-04)
    - `const DEFAULT_SOLVER = { abstol: 1e-8, reltol: 1e-6, dtmax: null as number | null };` per CD-04 + UI-SPEC "Solver defaults exposure"
    - `const SENTINEL_POWER_SHAPE_NAME = "(leave unset — fill in code)";` (verbatim from UI-SPEC; note: contains U+2014 em-dash — this string is user-facing copy, NOT a Julia identifier, so Unicode is allowed here per UI-SPEC tab labels rule)

    **2. New type declarations (above the AppState interface):**
    - `interface GeometryResource { uuid: string; name: string; kind: "rectangular" | "circular"; params: { L: number; W?: number; H?: number; D?: number; }; }`
    - `interface PowerShapeResource { uuid: string; name: string; kind: "uniform" | "z_cosine" | "file_loaded" | "unset"; params: { amplitude?: number; path?: string; }; }`
    - `interface FluidResource { uuid: string; name: string; }` (placeholder only — single non-editable "light_water" row per D-03 + UI-SPEC; no kind, no params)
    - `type SelectionKind = "none" | "component" | "resource" | "project";`
    - `type SelectedResourceKind = "geometry" | "powerShape" | "fluid" | null;`
    - `interface ResourcesSliceState { geometries: Record<string, GeometryResource>; powerShapes: Record<string, PowerShapeResource>; fluids: Record<string, FluidResource>; }`
    - `interface ModelOptionsSliceState { name: string; description: string; default_fluid: string; g_default: number; solver: { abstol: number; reltol: number; dtmax: number | null; }; }`
    - `type ActiveLeftTab = "Components" | "Resources" | "Project";` (the three literal tab IDs from D-01 + UI-SPEC)

    **3. Extend CanvasSnapshot:**
    ```
    interface CanvasSnapshot {
      nodes: Node[];
      edges: Edge[];
      bcs: BCEntry[];
      resources: ResourcesSliceState;        // NEW per Phase 62
      modelOptions: ModelOptionsSliceState;  // NEW per Phase 62
    }
    ```
    Per UI-SPEC §"Cross-Cutting Invariants" and RESEARCH §"_pushSnapshot": `activeLeftTab` is NOT included in the snapshot (UI state, not content state — mirrors the existing exclusion of `selectedNodeId`).

    **4. Extend AppState interface with new fields and actions:**
    - State: `resources: ResourcesSliceState; modelOptions: ModelOptionsSliceState; activeLeftTab: ActiveLeftTab; selectedResourceId: string | null; selectedResourceKind: SelectedResourceKind; selectionKind: SelectionKind;`
    - Resources actions: `addGeometry: (g: Omit<GeometryResource, "uuid">) => string;` (returns new uuid); `addPowerShape: (p: Omit<PowerShapeResource, "uuid">) => string;` (rejects `kind: "unset"` — only the sentinel is unset); `renameResource: (kind: "geometry" | "powerShape", uuid: string, newName: string) => void;`; `updateResource: (kind: "geometry" | "powerShape", uuid: string, patch: Partial<GeometryResource | PowerShapeResource>) => void;`; `removeResource: (kind: "geometry" | "powerShape", uuid: string) => void;` (no-op if uuid === SENTINEL_UNSET_POWER_SHAPE); `duplicateResource: (kind: "geometry" | "powerShape", uuid: string) => string;` (smart-name-increment per kind per D-19; returns new uuid)
    - Project actions: `setActiveLeftTab: (tab: ActiveLeftTab) => void;`; `setModelOptions: (patch: Partial<ModelOptionsSliceState>) => void;`
    - Selection actions: `selectResource: (uuid: string, kind: "geometry" | "powerShape" | "fluid") => void;` (clears `selectedNodeId`); `clearSelection: () => void;` (clears both `selectedNodeId` and `selectedResourceId`). MODIFY the existing `selectNode` to also clear `selectedResourceId` per D-05 mutual exclusivity.
    - Helper for D-19 smart-name-increment: a module-level function `nextResourceName(kind: "geometry" | "powerShape", existingNames: Set<string>): string` returning `geometry_<lowest-free-positive-integer>` or `power_shape_<lowest-free-positive-integer>`. Must skip integers already used. ASCII-only by construction (per CLAUDE.md / feedback_ascii_variable_names.md). Same algorithmic shape as `getNextInstanceName` but lowest-free instead of next-after-highest.

    **5. Validation rule in name handling:**
    Before accepting a user-edited name (either from `addGeometry`/`addPowerShape` with a caller-supplied name OR from `renameResource`):
    - Check uniqueness against the same-kind record (case-sensitive; `Object.values(s.resources.geometries).some(g => g.name === newName)`)
    - Check Julia identifier validity: regex `^[A-Za-z_][A-Za-z0-9_]*$` (ASCII letters, digits, underscores; does not start with a digit — same rule §3.5 + UI-SPEC popover validation messages)
    - On collision: throw `Error("A geometry named " + newName + " already exists.")` (or "power shape" — UI-SPEC verbatim copy). On invalid identifier: throw `Error("Use ASCII letters, digits, and underscores; must not start with a digit.")` (UI-SPEC verbatim).

    The popover form (62-08) will catch these errors and render the user-facing message. The store throws to signal the failure; it does NOT silently no-op.

    **6. Bake initial state:**
    - `resources: { geometries: {}, powerShapes: { [SENTINEL_UNSET_POWER_SHAPE]: { uuid: SENTINEL_UNSET_POWER_SHAPE, name: SENTINEL_POWER_SHAPE_NAME, kind: "unset", params: {} } }, fluids: { /* a single non-editable light_water row — generate a deterministic uuid (planner: use crypto.randomUUID() once at module load and bake the value as a const, OR use a second sentinel UUID like "00000000-0000-0000-0000-000000000001" — pick one and document) */ } }`
    - `modelOptions: { name: "", description: "", default_fluid: DEFAULT_FLUID, g_default: DEFAULT_G, solver: DEFAULT_SOLVER }`
    - `activeLeftTab: "Components"` (default per D-01 + D-08)
    - `selectedResourceId: null; selectedResourceKind: null;`
    - `selectionKind: "none"` (initial; getter/derivation pattern: implement as a derived value in selectors used by consumers, OR maintain it as explicit state synced inside `selectNode`/`selectResource`/`clearSelection`/`setActiveLeftTab` — pick one and document. RESEARCH Pattern 4 shows the explicit-state shape; that is the recommended path because zustand selectors do not auto-recompute on dependent state changes.)

    **7. Extend _pushSnapshot / undo / redo:**
    - `_pushSnapshot` body changes from `{ nodes, edges, bcs, _undoPast }` to `{ nodes, edges, bcs, resources, modelOptions, _undoPast }` and pushes the full extended snapshot
    - `undo`/`redo` similarly read/write the full extended snapshot; restoring also sets `isDirty: true` and clears validation (matches current `undo` body at lines 218-232)

    **8. Every Resource action MUST call `get()._pushSnapshot()` BEFORE the `set(...)` and MUST set `isDirty: true`.** This is the Pitfall 2 hazard — code-review checklist applies. Do NOT skip on rename or update.

    **9. Update existing `selectNode` (around line 295 — exact line varies) to additionally clear `selectedResourceId` and recompute `selectionKind`.** Update existing `addNode`, `removeNode`, `addEdge`, `removeEdge`, `updateNodeParams`, `addBC`, `removeBC` — they should NOT need changes; they don't touch resources directly. BUT confirm by reading the action bodies once and noting any that should also clear `selectedResourceId` (defensive; D-05 says single-source-of-truth on selection).

    DO NOT introduce zundo middleware (RESEARCH "Anti-Patterns" — the explicit `_pushSnapshot` discipline is intentional). DO NOT add a `validation` rule for "resource of wrong kind" or "shape mismatch" (Phase 71 owns validation framework per CONTEXT.md OOS list).
  </action>
  <verify>
    <automated>cd gui &amp;&amp; npx tsc --noEmit 2>&amp;1 | tee /tmp/62-02-task1-tsc.log; ! grep -E "error TS[0-9]+:" /tmp/62-02-task1-tsc.log | grep -v "^src/(?:.*\\.test\\.tsx?|.*\\.spec\\.tsx?)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SENTINEL_UNSET_POWER_SHAPE" gui/src/store/useStore.ts` returns at least 3 (const declaration, initial state injection, at least one guard in `removeResource`)
    - `grep -c "export const SENTINEL_UNSET_POWER_SHAPE" gui/src/store/useStore.ts` returns 1
    - `grep -cE "_pushSnapshot\\(\\)" gui/src/store/useStore.ts` increases by at least 5 vs baseline (5 new Resource actions: addGeometry, addPowerShape, renameResource, updateResource, removeResource — plus duplicateResource may or may not be counted depending on implementation; >= 5 is the minimum)
    - `grep -c "interface ResourcesSliceState" gui/src/store/useStore.ts` returns 1
    - `grep -c "interface ModelOptionsSliceState" gui/src/store/useStore.ts` returns 1
    - `grep -c "type ActiveLeftTab" gui/src/store/useStore.ts` returns 1
    - `grep -c "type SelectionKind" gui/src/store/useStore.ts` returns 1
    - TypeScript compiles cleanly above the documented 7-error baseline (from `.planning/phases/61-registry-audit-rewrite-for-v1-1/deferred-items.md`); the automated check passes (no NEW errors introduced; the 7 baseline errors are outside `useStore.ts`)
    - `grep -c "crypto.randomUUID()" gui/src/store/useStore.ts` increased by at least 1 (Resource UUID minting)
    - No `import.*from "uuid"` or `import.*from "nanoid"` lines (per CD-03 + RESEARCH "UUID generation" — crypto.randomUUID() only)
    - Implements D-04 (Model Options state), D-05 (selection-kind mutual exclusivity), D-08 (activeLeftTab default + state), D-09 (FK shape), D-10 (Resources record shape + per-kind name uniqueness), D-11 (UUID strategy via crypto.randomUUID), D-12 (rename propagation by construction), D-13 (copy-paste preserves FK — by NOT cloning the resource record in duplicateResource), D-26 (sentinel UUID for unset Power Shape), CD-03 (UUID lib choice), CD-04 (solver-defaults field set)
  </acceptance_criteria>
  <done>useStore.ts compiles cleanly; new types/actions present; sentinel constant exported; snapshot machinery extended; no zundo middleware introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resources slice tests — CRUD + uniqueness + sentinel + snapshot/undo (resources.slice.test.ts)</name>
  <files>gui/src/store/__tests__/resources.slice.test.ts</files>
  <read_first>
    - gui/src/store/useStore.ts (just-written Task 1 — types, actions, sentinel constant)
    - gui/src/store/__tests__/useStore.test.ts (existing test patterns — vitest + happy-dom setup; how `useStore.getState()` is used in tests)
    - .planning/phases/62-resources-panel-architecture/62-VALIDATION.md (INV-01..05, INV-08 spec)
    - .planning/phases/62-resources-panel-architecture/62-RESEARCH.md (Pitfall 2 — undo regression)
  </read_first>
  <behavior>
    Test file must cover (each as one or more `it()` blocks under a `describe("resources slice", ...)` root):
    - **INV-01 UUID uniqueness:** add 100 geometries; assert 100 unique UUIDs (`new Set(uuids).size === 100`)
    - **INV-04 / D-10 per-kind name uniqueness:** add `{ name: "mtr_ch", ... }` twice — second throws with the exact UI-SPEC string `"A geometry named mtr_ch already exists."`; adding a PowerShape named `"mtr_ch"` succeeds (kinds are independent namespaces)
    - **Julia-identifier validation on add:** adding a geometry with name `"3channel"` throws the UI-SPEC string `"Use ASCII letters, digits, and underscores; must not start with a digit."`; adding `"my-channel"` throws the same string; adding `"my_channel"` succeeds; adding `"my_channel_2"` succeeds
    - **INV-02 rename propagation:** after `renameResource("geometry", uuid, "renamed")`, `state.resources.geometries[uuid].name === "renamed"`; the UUID itself is unchanged
    - **INV-04 rename uniqueness:** renaming a geometry to a name already used by another geometry throws; renaming to a name used by a power shape succeeds
    - **INV-05 UUID never reused on delete:** add A; remove A; add B; assert `B.uuid !== A.uuid`
    - **INV-08 undo/redo over Resources:** add geometry G; `useStore.getState().undo()`; assert G is gone; `redo()`; assert G is back
    - **INV-08 undo rename:** add G; rename G; undo; assert G has its original name; redo; assert renamed
    - **D-26 sentinel exists in initial state:** `state.resources.powerShapes[SENTINEL_UNSET_POWER_SHAPE]` exists, has `kind === "unset"`, has `name === "(leave unset — fill in code)"` (verbatim from UI-SPEC)
    - **D-26 sentinel is uneditable:** `removeResource("powerShape", SENTINEL_UNSET_POWER_SHAPE)` is a no-op (sentinel still present in state after the call); `renameResource("powerShape", SENTINEL_UNSET_POWER_SHAPE, "anything")` is a no-op OR throws — either is acceptable, document the choice in the test description and assert accordingly
    - **D-26 user cannot create another "unset" PowerShape:** `addPowerShape({ name: "myx", kind: "unset", params: {} })` throws (the kind is reserved for the sentinel)
    - **D-13 copy-paste preserves FK (cross-check; full test lives in Wave 2-3 component-level):** lookup function works — given a geometry uuid, `state.resources.geometries[uuid]` returns the record; deleting it leaves `state.resources.geometries[uuid] === undefined` (dangling-ref UX is Phase 71 per INV-05 of VALIDATION.md)
  </behavior>
  <action>
    Create `gui/src/store/__tests__/resources.slice.test.ts` using the existing `useStore.test.ts` pattern (vitest + `useStore.getState()`/`setState()` direct mutation). Reset the store between tests via `useStore.setState(useStore.getInitialState ? useStore.getInitialState() : initialStateLiteral)` — adapt to whatever reset pattern `useStore.test.ts` uses. If no reset exists, manually clear `state.resources.geometries`, `state.resources.powerShapes` (preserving the sentinel), `_undoPast`, `_undoFuture` in a `beforeEach`.

    Each test MUST reference the invariant ID (INV-NN or D-NN) in its `it("INV-04: per-kind name uniqueness — geometries", ...)` title format so the per-task verification map can correlate.

    DO NOT test cross-component behaviors (rename of a geometry causes Channel re-render with new label) — that is a component-level test owned by 62-08 (ReferencePicker test). The store test asserts the record changes; the visual side is downstream.
  </action>
  <verify>
    <automated>cd gui &amp;&amp; npx vitest run src/store/__tests__/resources.slice.test.ts 2>&amp;1 | tee /tmp/62-02-task2.log; grep -qE "Tests +[0-9]+ passed" /tmp/62-02-task2.log &amp;&amp; ! grep -qE "[0-9]+ failed" /tmp/62-02-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - `gui/src/store/__tests__/resources.slice.test.ts` exists
    - All tests pass: `cd gui && npx vitest run src/store/__tests__/resources.slice.test.ts` exits 0
    - `grep -cE "INV-0[1-5]|INV-08" gui/src/store/__tests__/resources.slice.test.ts` returns >= 5
    - `grep -c "SENTINEL_UNSET_POWER_SHAPE" gui/src/store/__tests__/resources.slice.test.ts` returns >= 2
    - At least one `it("...undo...")` block; at least one `it("...redo...")` block
    - Implements INV-01, INV-02, INV-04, INV-05, INV-08 (test layer for D-09..D-13, D-26)
  </acceptance_criteria>
  <done>resources.slice.test.ts present, vitest green, all listed invariants covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ModelOptions + activeLeftTab + selection tests (modelOptions.test.ts, activeLeftTab.test.ts, selection.test.ts)</name>
  <files>gui/src/store/__tests__/modelOptions.test.ts, gui/src/store/__tests__/activeLeftTab.test.ts, gui/src/store/__tests__/selection.test.ts</files>
  <read_first>
    - gui/src/store/useStore.ts (Task 1 — new state + actions)
    - gui/src/store/__tests__/useStore.test.ts (existing test patterns)
    - .planning/phases/62-resources-panel-architecture/62-VALIDATION.md (INV-06, INV-09, INV-11, INV-17 — though INV-06/INV-11 partly belong to projectIO; this task covers the in-memory state side)
    - .planning/phases/62-resources-panel-architecture/62-UI-SPEC.md (Solver defaults exposure section; Tab switch focus behavior; Esc cascade)
  </read_first>
  <behavior>
    **modelOptions.test.ts:**
    - Initial state: `name === ""`, `description === ""`, `default_fluid === "water"`, `g_default === 9.80665`, `solver.abstol === 1e-8`, `solver.reltol === 1e-6`, `solver.dtmax === null`
    - `setModelOptions({ name: "demo" })` shallow-merges (only `name` changes; other fields preserved)
    - `setModelOptions({ solver: { abstol: 1e-10, reltol: 1e-6, dtmax: 0.1 } })` replaces the whole solver subobject
    - `setModelOptions(...)` calls `_pushSnapshot()` BEFORE mutation (assert by checking `_undoPast.length` increases by 1)
    - `setModelOptions(...)` sets `isDirty: true`
    - `undo()` after a `setModelOptions` reverts the change

    **activeLeftTab.test.ts:**
    - Initial state: `state.activeLeftTab === "Components"` (D-01 + D-08 default)
    - `setActiveLeftTab("Resources")` updates the value
    - `setActiveLeftTab(...)` does NOT call `_pushSnapshot()` (UI state, not content state — assert `_undoPast.length` unchanged after a tab switch)
    - `setActiveLeftTab(...)` does NOT set `isDirty: true` UNLESS the tab is being set during a load operation — actually per D-29 + RESEARCH §"Cross-Cutting Invariants", layout edits do not dirty the simulation-relevant diff. But the user changing tabs DOES dirty the project (the saved active tab differs from what's on disk). Resolution: `setActiveLeftTab` DOES set `isDirty: true` because the change is part of the `layout` block that gets serialized. Assert this.

    **selection.test.ts:**
    - Initial state: `selectedNodeId === null`, `selectedResourceId === null`, `selectionKind === "none"`
    - **INV-09 mutual exclusivity:** Add a node, `selectNode("node-1")`; assert `selectedNodeId === "node-1"`, `selectedResourceId === null`, `selectionKind === "component"`. Then `selectResource(geomUuid, "geometry")`; assert `selectedResourceId === geomUuid`, `selectedNodeId === null` (cleared per D-05), `selectionKind === "resource"`. Then `selectNode("node-2")`; assert `selectedResourceId === null` again
    - **INV-17 Edit… jump shape (state level):** `selectResource(geomUuid, "geometry")` also implies the parent component is about to call `setActiveLeftTab("Resources")`. In the store this is two separate actions; the Edit… handler in the picker (62-08) chains them. Assert here that the two actions compose correctly: after `selectResource(...) + setActiveLeftTab("Resources")`, `state.activeLeftTab === "Resources"` AND `state.selectedResourceId === geomUuid` AND `state.selectedNodeId === null`
    - **clearSelection clears both:** after `selectNode(...) + clearSelection()`, both ids are null and `selectionKind === "none"`
    - **Esc cascade interactions are component-layer; here we only test the underlying state primitives:** `clearSelection()` is the store-level primitive. The popover-close / rename-cancel / context-menu-close higher-precedence cases (UI-SPEC §"Esc precedence cascade") are component-level concerns owned by 62-08 / 62-06 / 62-09.
  </behavior>
  <action>
    Create three sibling test files. Each uses the same `useStore.getState()` direct-mutation pattern as Task 2. Each test references the invariant ID or decision ID in its title. Use `beforeEach` to reset relevant slice state to the initial values.

    The three files are intentionally split (vs one mega-file) so per-task verification points to a small, fast-running test file per concern. Each file should run in well under 5 seconds.

    Per UI-SPEC "Solver defaults exposure" section, the solver field set is exactly `{ abstol, reltol, dtmax }` — no `alg` or `progress_callback` or other extras. Assert that any unknown field passed in a `setModelOptions({ solver: { unknown_field: 42 } as any })` patch is just merged in without throwing (TypeScript catches the bad shape at compile time; runtime is permissive).
  </action>
  <verify>
    <automated>cd gui &amp;&amp; npx vitest run src/store/__tests__/modelOptions.test.ts src/store/__tests__/activeLeftTab.test.ts src/store/__tests__/selection.test.ts 2>&amp;1 | tee /tmp/62-02-task3.log; grep -qE "Tests +[0-9]+ passed" /tmp/62-02-task3.log &amp;&amp; ! grep -qE "[0-9]+ failed" /tmp/62-02-task3.log</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist; `npx vitest run` exits 0
    - `grep -c "INV-09\\|INV-17" gui/src/store/__tests__/selection.test.ts` returns >= 2
    - `grep -c "_pushSnapshot\\|_undoPast" gui/src/store/__tests__/modelOptions.test.ts` returns >= 2 (verifies snapshot integration assertions exist)
    - `grep -c "abstol\\|reltol\\|dtmax" gui/src/store/__tests__/modelOptions.test.ts` returns >= 3 (CD-04 solver-defaults coverage)
    - Implements INV-06 (state-layer only; serialization in 62-04), INV-09 (selection exclusivity), INV-11 (active tab in-memory side; serialization in 62-04), INV-17 (Edit… composes correctly), D-04, D-08
  </acceptance_criteria>
  <done>Three test files green; ModelOptions / activeLeftTab / selection invariants asserted in vitest.</done>
</task>

</tasks>

<verification>
- `cd gui && npx vitest run src/store/__tests__/` — green
- `cd gui && npx tsc --noEmit` — no new errors above the 7-error documented baseline
- `grep -c "SENTINEL_UNSET_POWER_SHAPE" gui/src/store/useStore.ts` >= 3
- `grep -c "_pushSnapshot" gui/src/store/useStore.ts` increased by >= 5 over the pre-Phase-62 baseline
- Each Resource CRUD action grepped: `addGeometry`, `addPowerShape`, `renameResource`, `updateResource`, `removeResource`, `duplicateResource` — each has `_pushSnapshot` as the first line of the action body (verify by reading; the grep gate above is the structural floor)
</verification>

<success_criteria>
- Resources slice + ModelOptions slice + activeLeftTab + selection-kind discriminator all present in useStore.ts
- Sentinel UUID baked into initial state and exported as a constant
- Every Resource mutation pushes a snapshot (Pitfall 2 mitigated structurally)
- Per-kind name uniqueness + Julia-identifier validation enforced
- Selection exclusivity invariant (D-05) holds at the store layer
- Four vitest files cover INV-01..05, INV-08, INV-09, INV-17, D-26 sentinel, CD-04 solver defaults
</success_criteria>

<output>
After completion, create `.planning/phases/62-resources-panel-architecture/62-02-SUMMARY.md` capturing:
- Final shape of the new types (post-implementation; any deviation from the action plan)
- Choice for sentinel-PowerShape remove/rename (no-op vs throw — documented)
- Fluids placeholder UUID strategy (deterministic constant vs runtime-minted)
- Any TypeScript friction encountered (e.g., zustand generic narrowing on the snapshot extension)
</output>
