---
phase: 19-docstrings-claude-md-and-final-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - Project.toml
  - test/test_channel.jl
autonomous: true
requirements: [QOL-03, QOL-04, QOL-05]

must_haves:
  truths:
    - "CLAUDE.md has a `Why:` rationale sentence after each existing rule"
    - "CLAUDE.md has an MTK Patterns section with at least 5 documented patterns"
    - "ChannelHeatFlux has a dedicated standalone testset in test_channel.jl"
    - "Project.toml reads version 0.5.0"
    - "ConstantTemperature is confirmed exported and tested (no new tests needed)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Expanded project instructions with rationale and MTK patterns"
      contains: "## MTK Patterns"
    - path: "Project.toml"
      provides: "Version bump"
      contains: 'version = "0.5.0"'
    - path: "test/test_channel.jl"
      provides: "Dedicated ChannelHeatFlux testset"
      contains: "ChannelHeatFlux"
  key_links:
    - from: "CLAUDE.md"
      to: "Every rule in the file"
      via: "Why: rationale sentence"
      pattern: "Why:"
    - from: "test/test_channel.jl"
      to: "src/components/thermal_channel.jl"
      via: "ChannelHeatFlux constructor call in testset"
      pattern: "ChannelHeatFlux"
---

<objective>
Expand CLAUDE.md with rationale and MTK patterns, add a dedicated ChannelHeatFlux test, and bump version to 0.5.0.

Purpose: QOL-03 makes CLAUDE.md useful for a returning developer by explaining *why* each rule exists. QOL-05 ensures ChannelHeatFlux has standalone test coverage. QOL-04 marks the v0.5 milestone complete.

Output: Updated CLAUDE.md, new testset in test_channel.jl, version bump in Project.toml.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-docstrings-claude-md-and-final-polish/19-CONTEXT.md
@.planning/phases/19-docstrings-claude-md-and-final-polish/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CLAUDE.md with rationale and MTK Patterns section (QOL-03)</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md,
    src/fluids.jl (for @register_symbolic pattern),
    src/components/misc.jl (for vars=[] and Inertia pattern),
    src/components/channel.jl (for ifelse flow reversal pattern)
  </read_first>
  <action>
Edit CLAUDE.md in-place. Preserve ALL existing content. Add rationale and a new section.

**Step 1: Add `Why:` rationale after each existing rule.**

The existing rules and their rationale to add:

In "File Structure Standard" section, after "This is the canonical layout. **Always follow it when editing or adding files.**":
- No change needed here (the layout table and "Where new code goes" bullets are descriptive, not rules needing rationale)

In "Component authoring conventions" section, after each bullet:

1. After "All component constructor arguments are **keyword-only** (no positional args). Matches MTK convention everywhere.":
   Add: `Why: Keyword-only prevents argument-order bugs and makes call sites self-documenting. MTK's own `@mtkmodel` macro generates keyword-only constructors.`

2. After "Factory functions (`PipeGeometry_rectangular`, `PipeGeometry_circular`) are also keyword-only.":
   Add: `Why: Consistency with component constructors. Mixing positional and keyword styles across the API creates confusion.`

3. After "Internal helpers are prefixed with `_` and not exported.":
   Add: `Why: Keeps the public API surface small and signals that these functions may change without notice. The `_` prefix is a Julia community convention.`

4. After "Every exported name has a docstring with at minimum: description, `# Arguments`, `# Returns`.":
   Add: `Why: The REPL `?name` lookup is the primary discovery mechanism for Julia packages. Structured sections make docstrings scannable.`

In "Exports" section, after "All public exports are declared in `STREAM.jl`. Never add `export` statements inside component files.":
   Add: `Why: A single export list in the module entry point makes the public API auditable at a glance. Scattered exports are easy to lose track of.`

Format each rationale as an indented line below the rule:
```
- Rule text here.
  *Why: Rationale text here.*
```

**Step 2: Add new "## MTK Patterns" section at the end of the file (before any trailing newlines).**

Content for the MTK Patterns section (verbatim -- copy this exactly):

```markdown
## MTK Patterns

Non-obvious ModelingToolkit conventions used throughout the codebase.

### `@register_symbolic` for fluid properties

Plain Julia functions cannot accept MTK's `Num` type (symbolic variables). `@register_symbolic` wraps them as opaque nodes in the symbolic expression graph, allowing them to appear in MTK equations without being traced or differentiated symbolically by Symbolics.jl.

### `ifelse()` for flow reversal and regime switching

Julia's `if`/`else` on a symbolic `Num` expression would evaluate the branch condition at trace time (equation construction), collapsing to a single branch permanently. `ifelse()` emits a symbolic conditional node that the solver evaluates at each timestep, enabling correct flow-reversal and laminar/turbulent regime switching.

### `vars=[]` for Inertia

When `Dt(port_in.mdot)` appears in an equation, MTK automatically promotes `port_in.mdot` to a differential state variable. Explicitly listing it in `vars` would be redundant. Passing `vars=[]` makes clear that the component introduces no *additional* state variables beyond what MTK infers.

### `@observed` vs plain unknowns

`@observed` variables are computed post-solve from the solution trajectory -- they are not part of the DAE system. Use `@observed` for diagnostic quantities (e.g., `Re`, `Nu`, `htc`) that are never referenced on the RHS of another equation. If a variable appears in another equation, it must be a plain unknown so the solver can see it.

### `mtkcompile` before solve

MTK's symbolic IR requires structural analysis (index reduction from DAE to ODE), Jacobian sparsity detection, and code generation before a numerical solver can use it. Always call `mtkcompile(sys)` to produce a compiled system. Passing an uncompiled `System` to `solve` will error or produce wrong results.
```
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM && grep -c "Why:" CLAUDE.md && grep -c "## MTK Patterns" CLAUDE.md && grep -c "@register_symbolic" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains at least 5 lines matching the pattern `*Why:` (one per rule)
    - CLAUDE.md contains the header `## MTK Patterns`
    - CLAUDE.md contains subsections `### \`@register_symbolic\` for fluid properties`, `### \`ifelse()\` for flow reversal`, `### \`vars=[]\` for Inertia`, `### \`@observed\` vs plain unknowns`, `### \`mtkcompile\` before solve`
    - All original file structure tables, test tables, and rules are preserved (no deletions)
    - CLAUDE.md "Exports" section still contains "All public exports are declared in `STREAM.jl`"
  </acceptance_criteria>
  <done>CLAUDE.md has a `Why:` rationale after every rule and a new MTK Patterns section with 5 documented patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Add ChannelHeatFlux dedicated test and bump version to 0.5.0 (QOL-04, QOL-05)</name>
  <files>test/test_channel.jl, Project.toml</files>
  <read_first>
    test/test_channel.jl (see existing test patterns and THERM-03 CHF reference),
    src/components/thermal_channel.jl (ChannelHeatFlux constructor signature),
    Project.toml
  </read_first>
  <action>
**Part A: ChannelHeatFlux dedicated testset (QOL-05)**

Add a new `@testset "ChannelHeatFlux: standalone"` block to `test/test_channel.jl`. Place it after the existing THERM-03 testset (which is the last testset in the file). The test follows the same topology pattern as the CHF side of THERM-03.

Insert this testset at the end of the file (before the final `end` of the top-level testset, if there is one, or at the end of the file):

```julia
@testset "ChannelHeatFlux: standalone" begin
    n = 10; T_inlet = 313.15; T_wall = 373.15
    L_ch = 0.6; D_ch = 0.01; dP_pump = 3.0e4

    @named pump = Pump(dP_pump=dP_pump)
    @named chf  = ChannelHeatFlux(n=n, geometry=PipeGeometry_circular(L_ch, D_ch), T_wall=T_wall)
    @named bc   = HeatExchanger(T_bc=T_inlet)
    conns = [
        connect(pump.port_out, bc.port_in),
        connect(bc.port_out, chf.port_in),
        connect(chf.port_out, pump.port_in),
        pump.port_in.P ~ 1.0e5,
        chf.port_in.T  ~ T_inlet,
    ]
    @named sys = compose(System(conns, t; name=:sys), pump, bc, chf)
    ssys = mtkcompile(sys)
    T_guess = steady_state_guess(T_inlet=T_inlet, Q_wall=1e4, mdot_guess=0.490, n=n)
    op = [ssys.chf.T[i] => T_guess[i] for i in 1:n]
    push!(op, ssys.chf.port_in.mdot => 0.490)
    sol = solve_steady(ssys, op)

    @test sol.retcode == ReturnCode.Success
    @test sol[ssys.chf.T_out] > T_inlet   # outlet must be warmer than inlet
end
```

Note: `PipeGeometry_circular` takes positional arguments `(L, D)`, not keyword arguments. The retcode check uses `sol.retcode == ReturnCode.Success` to match the established pattern in this file (line 60).

Also verify that `ConstantTemperature` is exported (it is -- confirmed in STREAM.jl exports line) and tested (it is -- used in 5+ test files). No new tests needed for ConstantTemperature per CONTEXT.md decision.

**Part B: Version bump (QOL-04)**

In `Project.toml`, change line 4 from:
```
version = "0.1.0"
```
to:
```
version = "0.5.0"
```
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM && julia --project -e 'include("test/test_channel.jl")' && grep 'version = "0.5.0"' Project.toml</automated>
  </verify>
  <acceptance_criteria>
    - `test/test_channel.jl` contains `@testset "ChannelHeatFlux` (a dedicated testset, not just the THERM-03 reference)
    - The ChannelHeatFlux standalone test asserts `sol[ssys.chf.T_out] > T_inlet`
    - The ChannelHeatFlux standalone test asserts successful retcode via `sol.retcode == ReturnCode.Success`
    - `Project.toml` contains exactly `version = "0.5.0"` on line 4
    - `julia --project -e 'include("test/test_channel.jl")'` exits 0 (all channel tests pass including new one)
    - `grep 'version = "0.5.0"' Project.toml` returns a match
  </acceptance_criteria>
  <done>ChannelHeatFlux has a dedicated standalone testset confirming it builds, solves, and produces heated output. Project.toml version is 0.5.0. ConstantTemperature confirmed exported and tested (no new tests needed).</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. CLAUDE.md has rationale and MTK patterns: `grep -c "Why:" CLAUDE.md` returns >= 5; `grep "## MTK Patterns" CLAUDE.md` matches
2. ChannelHeatFlux test passes: `julia --project -e 'include("test/test_channel.jl")'` exits 0
3. Version bumped: `grep 'version = "0.5.0"' Project.toml` matches
4. Full test suite passes: `julia --project test/runtests.jl` exits 0
</verification>

<success_criteria>
- QOL-03: CLAUDE.md has `Why:` rationale after each rule and an MTK Patterns section with 5 patterns
- QOL-04: Project.toml reads `version = "0.5.0"`
- QOL-05: ChannelHeatFlux has a dedicated `@testset` in test_channel.jl; ConstantTemperature confirmed exported and tested
</success_criteria>

<output>
After completion, create `.planning/phases/19-docstrings-claude-md-and-final-polish/19-02-SUMMARY.md`
</output>
