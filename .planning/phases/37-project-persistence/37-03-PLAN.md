---
phase: 37-project-persistence
plan: 03
type: execute
wave: 3
depends_on: ["37-02"]
files_modified: []
autonomous: false
requirements: [PERS-01, PERS-02, PERS-03, PERS-04]
must_haves:
  truths:
    - "Save creates a .streamgui file that can be reopened to restore the full canvas"
    - "Unsaved changes dialog appears on window close with dirty state"
    - "Recent Projects list shows in the welcome overlay after saving a file"
    - "Window title shows filename and asterisk correctly"
---

<objective>
Human verification of the complete project persistence feature.

Purpose: Verify all file I/O, dirty state tracking, and UI interactions work correctly in the real Tauri runtime.
Output: Confirmed working save/load/resume workflow.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-project-persistence/37-CONTEXT.md
@.planning/phases/37-project-persistence/37-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete project persistence: File menu (New/Open/Save/Save As), keyboard shortcuts (Ctrl+S/O/N/Shift+S), .streamgui save/load, unsaved-changes guard, recent projects welcome overlay, window title dirty indicator.</what-built>
  <how-to-verify>
    Run `cd gui && npm run tauri dev` and test these scenarios:

    1. **File Menu exists**: Verify a "File" dropdown button appears at the left of the toolbar (before Code). Click it — should show New, Open..., Save, Save As... with shortcut labels.

    2. **Save flow (PERS-01)**:
       a. Drag a Pump and a Channel onto the canvas.
       b. Press Ctrl+S — a native file save dialog should appear (first save = Save As behavior per D-02).
       c. Save as "test_project.streamgui". Verify the window title shows "test_project.streamgui - STREAM Composer" (no asterisk).
       d. Edit a parameter — verify asterisk appears: "test_project.streamgui* - STREAM Composer".
       e. Press Ctrl+S again — should save silently (no dialog) and asterisk disappears.

    3. **Load flow (PERS-02)**:
       a. Press Ctrl+N to create a new project (accept discard or save if prompted).
       b. Press Ctrl+O — native file open dialog appears.
       c. Open the "test_project.streamgui" file — verify the Pump and Channel are restored with correct positions and parameters.

    4. **Unsaved changes guard (PERS-03)**:
       a. Add a new node to make the canvas dirty.
       b. Click the window X button — verify a "Save changes?" dialog appears with Save / Don't Save / Cancel.
       c. Click Cancel — window stays open.
       d. Click X again, click "Don't Save" — window closes.
       e. Relaunch, add a node, click X, click "Save" — save dialog appears, save, then window closes.

    5. **Recent Projects (PERS-04)**:
       a. Create a new project (Ctrl+N).
       b. The canvas should be empty — verify the welcome overlay appears showing "STREAM Composer" heading, "Recent Projects" with the previously saved file listed.
       c. Click the recent file entry — verify the project loads.

    6. **Save As (Ctrl+Shift+S)**: With a project open, press Ctrl+Shift+S — native save dialog appears; save to a new name; verify title updates to the new filename.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 6 scenarios pass in the Tauri desktop app.
</verification>

<success_criteria>
- All manual test scenarios pass without errors
- File round-trip preserves all nodes, edges, positions, and parameters
- Dirty indicator works correctly
- Recent projects persist across app restarts
</success_criteria>

<output>
After completion, create `.planning/phases/37-project-persistence/37-03-SUMMARY.md`
</output>
