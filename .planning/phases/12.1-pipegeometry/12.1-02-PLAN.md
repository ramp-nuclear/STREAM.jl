---
phase: 12.1-pipegeometry
plan: 02
type: execute
wave: 2
depends_on: ["12.1-01"]
files_modified:
  - test/runtests.jl
  - .planning/STATE.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "Python reference script runs successfully and produces T_outlet_l, T_outlet_r, mdot_l, mdot_r, T_plate_center for all three VAL scenarios"
    - "VAL-01, VAL-02, VAL-03 each have hardcoded reference constants with 1% quantitative assertions using isapprox(...; rtol=0.01)"
    - "Physics-only assertions (T_out > T_in, symmetry, energy balance) are kept alongside the new quantitative assertions"
    - "STATE.md stale entries referencing π*Dh/2 are updated; 'Python STREAM refs incompatible' entry is removed"
    - "Full test suite (including new 1% assertions) is green"
  artifacts:
    - path: "test/runtests.jl"
      provides: "VAL-01/02/03 with hardcoded reference constants and 1% isapprox assertions"
      contains: "isapprox.*rtol=0.01"
    - path: ".planning/STATE.md"
      provides: "Updated key decisions — no stale π*Dh/2 references"
  key_links:
    - from: "generate_mtr_reference.py output (stdout)"
      to: "runtests.jl VAL-01/02/03 hardcoded constants"
      via: "manual checkpoint — human reads script output, executor pastes values"
      pattern: "val0[123]_.*_ref\\s*="
    - from: "VAL-01/02/03 tests"
      to: "isapprox assertions"
      via: "rtol=0.01 tolerance"
      pattern: "isapprox.*rtol=0\\.01"
---

<objective>
Run the Python reference script to obtain correct rectangular MTR geometry reference constants, then hardcode them into VAL-01/02/03 with 1% quantitative assertions. Clean up stale STATE.md entries.

Purpose: Plan 01 fixed the geometry. This plan closes the loop: the Python script now uses the same rectangular MTR geometry as Julia, so its output is the authoritative reference. Adding 1% isapprox assertions to VAL-01/02/03 upgrades them from physics-only sanity checks to full quantitative validation against Python STREAM.

Output: VAL-01/02/03 with hardcoded reference constants (from Python script stdout) and 1% assertions. STATE.md stale entries removed. Full suite green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.1-pipegeometry/12.1-CONTEXT.md
@.planning/phases/12.1-pipegeometry/12.1-01-SUMMARY.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Run Python reference script and capture output</name>
  <files>test/generate_mtr_reference.py</files>
  <action>
Run the Python reference script (already updated by Plan 01 to use rectangular MTR geometry) and capture all numeric output. The executor cannot run Python scripts — this requires the user.

Command to run:
```bash
cd /home/itay/projects/Julia-STREAM/test && python generate_mtr_reference.py
```

The script prints reference values for VAL-01 (symmetric), VAL-02 (asymmetric), and VAL-03 (one-sided). Capture: T_outlet_l, T_outlet_r, mdot_l, mdot_r, T_plate_center (or equivalent plate temperature) for each scenario.
  </action>
  <verify>Script produces numeric output for all three VAL scenarios without errors.</verify>
  <done>Complete stdout of generate_mtr_reference.py with all reference constants provided in resume signal.</done>
  <what-built>Plan 01 updated generate_mtr_reference.py to use rectangular MTR geometry (heated_parts=(0.07, 0.07)). The script must be executed now to produce the new reference constants.</what-built>
  <how-to-verify>
Run the following command and copy the full stdout output:

```bash
cd /home/itay/projects/Julia-STREAM/test && python generate_mtr_reference.py
```

The script should print reference values for VAL-01 (symmetric), VAL-02 (asymmetric), and VAL-03 (one-sided). You need:
- VAL-01: T_outlet_l, T_outlet_r, mdot_l, mdot_r, T_plate_center (or equivalent plate temperature)
- VAL-02: T_outlet_l, T_outlet_r and/or T_plate at center — confirms asymmetric profile
- VAL-03: T_outlet (one-sided), mdot, T_plate

Paste the complete script output as your resume signal.
  </how-to-verify>
  <resume-signal>Paste the complete stdout of `python generate_mtr_reference.py` — include all numeric values printed by the script.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add hardcoded reference constants and 1% assertions to VAL-01/02/03 in test/runtests.jl</name>
  <files>test/runtests.jl</files>
  <action>
Using the reference constants from the Python script output (provided in the checkpoint resume signal), add hardcoded reference constants and 1% `isapprox` assertions to each VAL test.

**Pattern to follow (for each VAL test):**

Insert the reference constant block immediately after the solve call (before the existing `@test` assertions). Use this exact naming convention:

For VAL-01 (symmetric MTR):
```julia
# Reference constants from generate_mtr_reference.py (rectangular MTR geometry, y=0.07 m)
val01_T_outlet_l_ref = XXXX.XXXX   # K — left channel outlet temperature
val01_T_outlet_r_ref = XXXX.XXXX   # K — right channel outlet temperature
val01_mdot_l_ref     = X.XXXXXX    # kg/s — left channel mass flow
val01_mdot_r_ref     = X.XXXXXX    # kg/s — right channel mass flow
val01_T_plate_center = XXXX.XXXX   # K — plate center temperature (mid-axial, mid-lateral)
@test isapprox(T_out_l,  val01_T_outlet_l_ref; rtol=0.01)
@test isapprox(T_out_r,  val01_T_outlet_r_ref; rtol=0.01)
@test isapprox(mdot_l,   val01_mdot_l_ref;     rtol=0.01)
@test isapprox(mdot_r,   val01_mdot_r_ref;     rtol=0.01)
@test isapprox(T_center, val01_T_plate_center;  rtol=0.01)
```

For VAL-02 (asymmetric): add whatever scalar outputs the Python script produces for this scenario (T_outlet per channel — confirms asymmetry is in correct direction).

For VAL-03 (one-sided): add T_outlet and mdot quantitative assertions.

**KEEP all existing physics-only assertions** (T_out > T_in, symmetry or asymmetry checks, energy balance). The new 1% assertions supplement, not replace, the physics guards.

**Variable names:** Match the existing variable names already in runtests.jl for each VAL test (e.g., `T_out_l`, `T_out_r`, `mdot_l`, `mdot_r`, `T_center`). Read the current test code to confirm the exact names before inserting.

**If Python script output is missing a quantity** (e.g., does not print T_plate_center), skip that assertion — only add assertions for quantities the script actually printed. Do not fabricate constants.

**Remove this comment from VAL-01/02/03** (if present): "quantitative Python reference comparison is not feasible" — this is the problem we just fixed.
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM && julia --project=. -e "using Pkg; Pkg.test()" 2>&1 | tail -30</automated>
  </verify>
  <done>VAL-01/02/03 each contain at least 2 new `isapprox(...; rtol=0.01)` assertions with hardcoded reference constants from the Python script. Full test suite (all previously passing tests + new assertions) is green.</done>
</task>

<task type="auto">
  <name>Task 2: Update stale STATE.md Key Decisions entries</name>
  <files>.planning/STATE.md</files>
  <action>
In `/home/itay/projects/Julia-STREAM/.planning/STATE.md`, find the Key Decisions table and make these three targeted changes:

1. **Update** the entry:
   - Old: `q_wall[i] ~ thermal_left[i].Q_flow + thermal_right[i].Q_flow | Two-sided heating: each side uses pi*Dh/2; sum recovers pi*Dh — Phase 10 interface contract`
   - New: `q_wall[i] ~ thermal_left[i].Q_flow + thermal_right[i].Q_flow | Two-sided heating: each side uses geometry.heated_parts[i]; for circular: π*D/2 each; for rectangular MTR: y=0.07 m each — Phase 12.1 PipeGeometry contract`

2. **Update** the entry:
   - Old: `ChannelAndContacts requires explicit thermal.Q_flow equations | Acausal MTK: port Q_flow must be defined via h_tc*(pi*Dh/2)*dz*deltaT; missing eqs leave system underdetermined`
   - New: `ChannelAndContacts requires explicit thermal.Q_flow equations | Acausal MTK: port Q_flow must be defined via h_tc*geometry.heated_parts[face]*dz*deltaT; missing eqs leave system underdetermined`

3. **Remove** the entire row for:
   - `Python STREAM refs incompatible with Julia MTR geometry | EffectivePipe.circular = left-face-only; Julia CAC = two-sided; 1% tolerance replaced by physics-based energy balance assertions`
   - This entry described the problem that Phase 12.1 fixed. Remove it entirely.

Also update `last_activity`, `stopped_at`, and the Current Position section to reflect Phase 12.1 Plan 02 completion.
  </action>
  <verify>
    <automated>grep -n "pi.Dh.2\|Python STREAM refs incompatible" /home/itay/projects/Julia-STREAM/.planning/STATE.md; echo "exit:$?"</automated>
  </verify>
  <done>STATE.md no longer contains any of: "pi*Dh/2", "π*Dh/2", or "Python STREAM refs incompatible". The three targeted entries have been updated or removed as specified. Current Position reflects Phase 12.1 complete.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `julia --project=. -e "using Pkg; Pkg.test()"` — all tests green including new 1% VAL assertions
2. `grep -n "isapprox.*rtol=0.01" test/runtests.jl` — should return at least 6 lines (2+ per VAL-01/02/03)
3. `grep -n "Python STREAM refs incompatible" .planning/STATE.md` — no matches
4. `grep -n "val0[123]_.*_ref" test/runtests.jl` — shows hardcoded reference constants for each VAL scenario
</verification>

<success_criteria>
- Python reference script ran successfully with rectangular MTR geometry and produced numeric constants
- VAL-01, VAL-02, VAL-03 each have isapprox assertions at rtol=0.01 with hardcoded Python reference values
- Full Julia test suite green (all physics-only assertions kept + new quantitative assertions)
- STATE.md stale entries (π*Dh/2 references) updated; "Python STREAM refs incompatible" entry removed
- Phase 12.1 is complete: the 4.46× geometry error is fixed, quantitative MTR validation is restored
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-pipegeometry/12.1-02-SUMMARY.md`
</output>
