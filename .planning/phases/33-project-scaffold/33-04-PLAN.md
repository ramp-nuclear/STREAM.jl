---
phase: 33-project-scaffold
plan: 04
type: execute
wave: 3
depends_on: ["33-01", "33-03"]
files_modified: []
autonomous: false
requirements:
  - SCAF-02

must_haves:
  truths:
    - "Running `npm run tauri build` from gui/ produces a native installer in src-tauri/target/release/bundle/"
    - "The bundle directory contains at least one distributable file (e.g., .deb or .AppImage on Linux)"
  artifacts: []
  key_links: []
---

<objective>
Verify the Tauri 2 app can be built into a native desktop installer (SCAF-02).

Purpose: SCAF-02 requires the app to produce a native installer via `npm run tauri build`. This checkpoint runs the build and verifies output exists.

Output: Human-verified confirmation that the installer build succeeds.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-project-scaffold/33-01-SUMMARY.md
@.planning/phases/33-project-scaffold/33-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Tauri build and verify installer output</name>
  <files></files>
  <action>
    Run the Tauri production build from the gui/ directory:
    ```bash
    cd /home/itay/projects/Julia-STREAM/gui && npm run tauri build 2>&1 | tail -30
    ```

    After the build completes, verify the bundle directory exists and contains at least one installer file:
    ```bash
    ls -la /home/itay/projects/Julia-STREAM/gui/src-tauri/target/release/bundle/
    ```

    On Linux, expect to find subdirectories like `deb/` and/or `appimage/` containing distributable files.

    If the build fails due to missing system dependencies (e.g., missing libraries for bundling), document what is needed and create a checkpoint for the user to install them.
  </action>
  <verify>
    <automated>ls /home/itay/projects/Julia-STREAM/gui/src-tauri/target/release/bundle/ 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run tauri build` exits successfully (exit code 0)
    - gui/src-tauri/target/release/bundle/ directory exists
    - At least one installer file exists in the bundle subdirectories
  </acceptance_criteria>
  <done>Tauri build produces a native installer. SCAF-02 (native desktop installer) verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm installer is valid</name>
  <files></files>
  <action>
    Human verification checkpoint. Task 1 ran the build.

    **What was built:**
    - `npm run tauri build` was run from gui/
    - Output should be in gui/src-tauri/target/release/bundle/

    **How to verify:**
    1. Check the terminal output from Task 1 for build success
    2. List contents: `ls -la gui/src-tauri/target/release/bundle/deb/` (or `appimage/`)
    3. Optionally install/run the built package to verify it launches

    Expected: At least one installer file (.deb, .AppImage, or platform equivalent) exists in the bundle directory.
  </action>
  <verify>
    <automated>test -d /home/itay/projects/Julia-STREAM/gui/src-tauri/target/release/bundle && echo "bundle dir exists" || echo "NO bundle dir"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms bundle directory contains installer file(s)
    - SCAF-02 acceptance criterion met: native desktop installer produced
  </acceptance_criteria>
  <done>User confirmed native installer build works. SCAF-02 fully verified.</done>
</task>

</tasks>

<verification>
1. `gui/src-tauri/target/release/bundle/` contains at least one installer file
2. Build process completed without errors
</verification>

<success_criteria>
- `npm run tauri build` succeeds from gui/
- gui/src-tauri/target/release/bundle/ contains at least one distributable installer
- SCAF-02 (native desktop installer) is verified
</success_criteria>

<output>
After completion, create `.planning/phases/33-project-scaffold/33-04-SUMMARY.md`
</output>
