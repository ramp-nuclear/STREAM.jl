---
phase: 33-project-scaffold
plan: 03
type: execute
wave: 2
depends_on: ["33-01"]
files_modified: []
autonomous: false
requirements:
  - SCAF-01
  - SCAF-02

must_haves:
  truths:
    - "Desktop app window opens when running npm run tauri dev"
    - "Three-panel layout is visible: left toolbox, center canvas, right sidebar"
    - "ReactFlow canvas renders with zoom/pan/minimap controls"
    - "No console errors in DevTools"
  artifacts: []
  key_links: []
---

<objective>
Verify the Tauri 2 desktop app launches and renders correctly.

Purpose: SCAF-01 requires the app to start with hot-reload. SCAF-02 requires it to be installable. This checkpoint verifies the visual result of Plan 01 before building the registry on top.

Output: Human-verified confirmation that the app launches and renders the three-panel layout.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-project-scaffold/33-01-SUMMARY.md
@.planning/phases/33-project-scaffold/33-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Tauri desktop app launches with three-panel layout</name>
  <files>gui/src/App.tsx</files>
  <action>
    Human verification checkpoint. No automated work -- Plan 01 already built everything.

    **What was built (Plan 01):**
    - Tauri 2 + React + ReactFlow desktop application at gui/
    - Three-panel layout: left toolbox (240px, "Components" heading), center ReactFlow canvas (zoom/pan/minimap/dot grid), right sidebar (320px, "Properties" heading)
    - Zustand store for nodes/edges/selectedNodeId
    - Vitest configured with jsdom
    - shadcn initialized (but no components installed)

    **How to verify:**

    Option A: Full Tauri desktop app (requires Rust + WebKitGTK)
    1. Open terminal in /home/itay/projects/Julia-STREAM/gui
    2. Run `npm run tauri dev`
    3. Wait for Rust compilation (first run takes 2-5 minutes)
    4. Verify a "STREAM Composer" desktop window appears
    5. Verify three-panel layout: left panel says "Components", right panel says "Properties"
    6. Verify center panel shows ReactFlow canvas with dot grid background
    7. Verify zoom (scroll wheel), pan (click-drag), minimap (bottom-right), controls (bottom-left)
    8. Open DevTools (right-click > Inspect or Ctrl+Shift+I) and check Console for errors

    Option B: Browser-only (if Rust/WebKitGTK not available)
    1. Open terminal in /home/itay/projects/Julia-STREAM/gui
    2. Run `npm run dev`
    3. Open http://localhost:1420 in a browser
    4. Perform steps 5-8 from Option A

    Expected: Window title "STREAM Composer", left panel 240px with "Components", center ReactFlow canvas with dot grid/zoom/pan/minimap/controls, right panel 320px with "Properties" and "Select a component on the canvas to view its properties." No console errors.
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - User confirms "STREAM Composer" window opens
    - User confirms three-panel layout visible with "Components" and "Properties" headings
    - User confirms ReactFlow canvas renders with zoom, pan, minimap
    - User confirms no blocking console errors
  </acceptance_criteria>
  <done>User approved the Tauri desktop app launch and three-panel layout rendering. SCAF-01 (dev mode) visually verified.</done>
</task>

</tasks>

<verification>
Human confirms the app launches and renders the three-panel layout correctly.
</verification>

<success_criteria>
- User confirms STREAM Composer window opens (via Tauri or browser)
- Three-panel layout visible with correct headings
- ReactFlow canvas functional (zoom, pan, minimap)
- No blocking console errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-project-scaffold/33-03-SUMMARY.md`
</output>
