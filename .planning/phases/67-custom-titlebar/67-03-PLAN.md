---
phase: 67-custom-titlebar
plan: 03
type: execute
wave: 3
depends_on:
  - 67-01
  - 67-02
files_modified:
  - gui/src/components/EditMenu.tsx
  - gui/src/components/ViewMenu.tsx
  - gui/src/components/HelpMenu.tsx
  - gui/src/components/CustomTitlebar.tsx
  - gui/src/components/SecondaryToolbar.tsx
  - gui/src/App.tsx
  - gui/src/components/Toolbar.tsx
  - gui/src/components/ThemeMenu.tsx
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-16
  - D-17
  - D-18
  - D-19
  - D-21
  - D-22
  - D-26

must_haves:
  truths:
    - "User sees a 36px custom titlebar at the top of the app: app icon, project name (basename without extension), dirty dot when isDirty, four menus (File/Edit/View/Help), drag region, window controls"
    - "User sees a 32px secondary strip immediately below the titlebar: Layer toggle, Code button, Export button. ThemeMenu is gone from here"
    - "OS chrome (GTK title bar on Linux/WSLg) is gone"
    - "Clicking the Min/Max/Close buttons performs the corresponding OS window action"
    - "Double-clicking the drag region toggles maximize"
    - "Dragging the empty center of the titlebar moves the window"
    - "Edit menu items fire useStore actions: undo, redo, cutSelection, copySelection, pasteFromClipboard, duplicateSelection (PER D-22 — NOT pasteClipboard)"
    - "View → Toggle Code Preview toggles BottomPanel and shows a check mark when open"
    - "View → Layer submenu radio group syncs bidirectionally with the secondary strip's Layer toggle (both read/write the same `activeLayer` store slice)"
    - "View → Theme submenu radio group is array-driven from `THEMES` (D-21) and changes theme on selection"
    - "Help → About STREAM Composer opens the AboutDialog showing the app version"
    - "Help → Keyboard Shortcuts is disabled (Phase 72 stub)"
    - "Edit → Preferences is disabled (Phase 72 stub)"
    - "`Toolbar.tsx` and `ThemeMenu.tsx` are deleted"
    - "Existing vitest suite stays green relative to the documented pre-existing baseline (1 pre-existing failure, 11 pre-existing tsc errors per STATE.md — no NEW regressions)"
  artifacts:
    - path: gui/src/components/EditMenu.tsx
      provides: "Edit menu with 7 actions + 2 separators + Preferences stub; inline D-19 comment documenting always-fire vs Phase 65 keyboard guard asymmetry"
      contains: "pasteFromClipboard"
    - path: gui/src/components/ViewMenu.tsx
      provides: "View menu with Toggle Code Preview + Layer submenu (radio) + Theme submenu (radio, array-driven from THEMES)"
      contains: "DropdownMenuSub"
    - path: gui/src/components/HelpMenu.tsx
      provides: "Help menu with About (opens AboutDialog) + Keyboard Shortcuts stub"
    - path: gui/src/components/CustomTitlebar.tsx
      provides: "36px full-width titlebar shell composing icon + name + dirty dot + 4 menus + drag region (SIBLING per D-26) + WindowControls"
      contains: "data-tauri-drag-region"
    - path: gui/src/components/SecondaryToolbar.tsx
      provides: "32px full-width strip with Layer toggle + Code button + Export button (NO ThemeMenu per D-03/D-16)"
    - path: gui/src/App.tsx
      provides: "Root JSX restructured: CustomTitlebar + SecondaryToolbar OUTSIDE the flex flex-1 row; old <Toolbar> removed from center column"
  key_links:
    - from: gui/src/components/CustomTitlebar.tsx
      to: gui/src/components/WindowControls.tsx
      via: "Component composition in titlebar right cluster"
    - from: gui/src/components/CustomTitlebar.tsx
      to: "@tauri-apps/api/window::getCurrentWindow"
      via: "onDoubleClick handler on data-tauri-drag-region sibling div"
      pattern: "onDoubleClick"
    - from: gui/src/components/ViewMenu.tsx
      to: gui/src/hooks/useTheme.ts
      via: "THEMES constant imported and mapped over (D-21 array-driven theme list)"
      pattern: "THEMES"
    - from: gui/src/components/ViewMenu.tsx
      to: gui/src/store/useStore.ts
      via: "Layer submenu and Toggle Code Preview bound to activeLayer / setActiveLayer / toggleBottomPanel / bottomPanelOpen"
    - from: gui/src/components/EditMenu.tsx
      to: gui/src/store/useStore.ts
      via: "All 6 actions invoke useStore.getState().<actionName>() at click time"
      pattern: "useStore\\.getState\\(\\)\\.(undo|redo|cutSelection|copySelection|pasteFromClipboard|duplicateSelection)"
    - from: gui/src/App.tsx
      to: gui/src/components/CustomTitlebar.tsx
      via: "Rendered as first child of root flex-col, OUTSIDE the flex flex-1 min-h-0 row"
---

<objective>
Compose the leaf primitives from Plan 02 into the actual titlebar shell, build the three new menus (Edit/View/Help) following the FileMenu pattern, refactor App.tsx to host the new two-strip layout above the panel row, and delete the now-superseded `Toolbar.tsx` and `ThemeMenu.tsx`. End with a manual UAT checkpoint covering the surfaces that cannot be vitested: drag, double-click maximize, window controls, edge-resize (D-18 known-risk surface on WSLg).

Purpose: Ship the user-visible titlebar. Every action, every visual decision (D-01..D-26) lands here.
Output: A running Tauri app whose OS window has no chrome and whose custom titlebar covers File/Edit/View/Help + identity + window controls, with a 32px secondary strip beneath it.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/67-custom-titlebar/67-CONTEXT.md
@.planning/phases/67-custom-titlebar/67-RESEARCH.md
@.planning/phases/67-custom-titlebar/67-PATTERNS.md
@.planning/phases/67-custom-titlebar/67-UI-SPEC.md
@.planning/phases/67-custom-titlebar/67-01-SUMMARY.md
@.planning/phases/67-custom-titlebar/67-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing project surfaces this plan consumes. Extracted directly from useStore.ts and Toolbar.tsx on 2026-05-16. -->
<!-- VERIFIED LINE NUMBERS — do NOT re-explore. -->

From gui/src/store/useStore.ts (verified action names per D-22):
- s.undo: () => void                                  // line 233 (decl), 945 (impl)
- s.redo: () => void                                  // line 234 (decl), 986 (impl)
- s.cutSelection: () => Promise<void>                 // line 340 (decl), 1917 (impl)
- s.copySelection: () => Promise<void>                // line 339 (decl), 1889 (impl)
- s.pasteFromClipboard: () => Promise<void>           // line 341 (decl), 1964 (impl) — NOT pasteClipboard
- s.duplicateSelection: () => void                    // line 342 (decl), 2053 (impl)
- s.toggleBottomPanel: () => void                     // line 251 (decl), 1807 (impl)
- s.bottomPanelOpen: boolean                          // (state)
- s.activeLayer: LayerView                            // line 213 (decl)
- s.setActiveLayer: (layer: LayerView) => void        // line 214 (decl), 1031 (impl)
- s.isDirty: boolean                                  // line 223
- s.currentFilePath: string | null                    // line 224

From gui/src/components/FileMenu.tsx (existing — unchanged, just relocated):
- Props: { onUnsavedCheck: () => Promise<"save" | "discard" | "cancel"> }
- Uses shadcn DropdownMenu + DropdownMenuTrigger + DropdownMenuContent + DropdownMenuItem
- Trigger: <Button variant="outline" size="sm">File <ChevronDown /></Button>

From gui/src/components/ui/dropdown-menu.tsx — exports include: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuSub, DropdownMenuSubTrigger, DropdownMenuSubContent, DropdownMenuPortal, DropdownMenuRadioGroup, DropdownMenuRadioItem.

From gui/src/hooks/useTheme.ts (after Plan 02):
- export const THEMES = ["light", "dark", "system"] as const
- export type Theme = (typeof THEMES)[number]
- export function useTheme(): { theme: Theme; resolvedTheme: "light"|"dark"; setTheme: (t: Theme) => void }

From gui/src/components/WindowControls.tsx (created in Plan 02): default export, no required props.
From gui/src/components/AboutDialog.tsx (created in Plan 02): default export with props { open: boolean; onOpenChange: (open: boolean) => void }.

From gui/src/App.tsx (current layout — lines 387-477):
- Root: <div className="flex flex-col h-screen w-screen overflow-hidden">
- Inside: <div className="flex flex-1 min-h-0"> ← outer panel row
- Inside that: ToolboxPanel | CenterColumn (containing <Toolbar> + <CanvasPanel>) | SidebarPanel
- Outside: <BottomPanel />
- AutoRecover render gate at lines 372-385 — leave alone
- showUnsavedDialog callback at App.tsx — must flow to CustomTitlebar → FileMenu instead of to Toolbar

Existing Toolbar.tsx export shape (the SecondaryToolbar replacement consumes the same store slices but drops FileMenu/ThemeMenu/identity blocks):
- Renders: identity block (icon? + name + dirty marker), FileMenu, layer ToggleGroup, Code button, Export button, ThemeMenu
- Phase 67 split: identity + FileMenu → CustomTitlebar; layer ToggleGroup + Code button + Export → SecondaryToolbar; ThemeMenu → deleted (folded into ViewMenu)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EditMenu.tsx, ViewMenu.tsx, HelpMenu.tsx (the three new menus)</name>
  <files>gui/src/components/EditMenu.tsx, gui/src/components/ViewMenu.tsx, gui/src/components/HelpMenu.tsx</files>
  <read_first>
    - gui/src/components/FileMenu.tsx (full file — the analog for trigger shape, accelerator-label pattern, and store-action wiring)
    - gui/src/components/ThemeMenu.tsx (full file — analog for the radio-group portion of ViewMenu)
    - gui/src/components/canvasMenus/AddComponentSubmenu.tsx (analog for shadcn `DropdownMenuSub` + `DropdownMenuPortal` viewport-collision pattern — see 67-PATTERNS.md §Pattern S6)
    - gui/src/components/ui/dropdown-menu.tsx (verify export surface — confirm DropdownMenuSub/SubTrigger/SubContent/Portal/RadioGroup/RadioItem are all present)
    - .planning/phases/67-custom-titlebar/67-CONTEXT.md D-08, D-10, D-11, D-12, D-19, D-21, D-22 (locked menu content + accelerator-display rule + always-fire rule + array-driven THEMES + `pasteFromClipboard` name)
    - .planning/phases/67-custom-titlebar/67-RESEARCH.md §"Pattern 5: View menu — submenus + radio groups (shadcn)" and §"Pitfall 6" (always-fire vs Phase 65 keyboard guard)
    - .planning/phases/67-custom-titlebar/67-PATTERNS.md §"gui/src/components/menus/EditMenu.tsx", §"ViewMenu.tsx", §"HelpMenu.tsx" (composite analog assignments)
    - .planning/phases/67-custom-titlebar/67-UI-SPEC.md §"Menu Content Contract" (full item list and state per menu)
  </read_first>
  <action>
    Create the three menu components in `gui/src/components/` (NOT in a nested `menus/` subdirectory — match the existing flat layout where FileMenu.tsx and ThemeMenu.tsx live at the top of `gui/src/components/`). Each component is a self-contained DropdownMenu using the FileMenu trigger pattern (`<Button variant="outline" size="sm">{label}<ChevronDown /></Button>`).

    `EditMenu.tsx` (per D-10, D-22):
    - No props.
    - Subscribe to store actions via narrow selectors (Pattern S1 — 67-PATTERNS.md): `const undo = useStore((s) => s.undo)`, same for `redo`, `cutSelection`, `copySelection`, `pasteFromClipboard`, `duplicateSelection`. Use exact names per D-22 (`pasteFromClipboard`, NOT `pasteClipboard`).
    - Items in order: Undo (Ctrl+Z) → Redo (Ctrl+Y) → `<DropdownMenuSeparator />` → Cut (Ctrl+X) → Copy (Ctrl+C) → Paste (Ctrl+V) → Duplicate (Ctrl+D) → `<DropdownMenuSeparator />` → Preferences... (no shortcut, `disabled`).
    - Each item uses the FileMenu accelerator-label shape: `<span className="flex justify-between w-full items-center gap-4"><span>{label}</span><span className="text-muted-foreground text-xs">{shortcut}</span></span>`.
    - `onClick` for each: simply invoke the store action — e.g., `onClick={() => void cutSelection()}` for async actions, `onClick={() => undo()}` for sync.
    - Per D-19, add an inline comment ABOVE the action-binding block: "Edit menu items fire unconditionally — no input-focus guard. Phase 65 keyboard listeners in CanvasPanel.tsx:222-247 own the guard; menu path is explicit user intent."
    - Accelerator labels are display-only — do NOT add `window.addEventListener("keydown", ...)`.

    `ViewMenu.tsx` (per D-11, D-21):
    - Props: `{ theme: Theme; setTheme: (t: Theme) => void }`. (Pass-through from App.tsx; do not subscribe to a separate `useTheme()` hook here — avoids duplicate localStorage reads.)
    - Import `THEMES` from `../hooks/useTheme`.
    - Subscribe to store slices: `bottomPanelOpen`, `toggleBottomPanel`, `activeLayer`, `setActiveLayer`.
    - Item 1 (Toggle Code Preview): `<DropdownMenuItem onClick={toggleBottomPanel}>{bottomPanelOpen && <Check className="h-4 w-4 mr-2" />}Toggle Code Preview</DropdownMenuItem>`.
    - Item 2 (Layer submenu): `<DropdownMenuSub><DropdownMenuSubTrigger>Layer</DropdownMenuSubTrigger><DropdownMenuPortal><DropdownMenuSubContent><DropdownMenuRadioGroup value={activeLayer} onValueChange={(v) => setActiveLayer(v as LayerView)}>` with three `<DropdownMenuRadioItem>` for "Hydraulic", "Both", "Thermal".
    - Item 3 (Theme submenu): same shape, mapping over `THEMES`. Capitalize the label (e.g., `"light" → "Light"`); the value stays lowercase to match the existing `Theme` union. Per D-21, the mapping is required — do not unroll the three items as hardcoded JSX.
    - The `LayerView` type import is `import type { LayerView } from "../store/useStore";` (verify against the existing import at Toolbar.tsx line ~8).

    `HelpMenu.tsx` (per D-12):
    - No props.
    - Local state: `const [aboutOpen, setAboutOpen] = useState(false)`.
    - Items: `About STREAM Composer` (`onClick={() => setAboutOpen(true)}`) and `Keyboard Shortcuts` (disabled stub).
    - Render `<AboutDialog open={aboutOpen} onOpenChange={setAboutOpen} />` AFTER the closing `</DropdownMenu>` so the dialog portal is not nested inside the menu (Radix Portal handles this either way, but keeping the JSX flat avoids reader confusion).

    No tests required — Plan 03's manual UAT checkpoint covers behavior. Existing vitest suite must still pass; if `useStore` selector subscription patterns trigger a tsc error, fix the import (do not silence with `any`).
  </action>
  <verify>
    <automated>cd gui && grep -q 'pasteFromClipboard' src/components/EditMenu.tsx && (! grep -q 'pasteClipboard' src/components/EditMenu.tsx) && grep -q 'THEMES' src/components/ViewMenu.tsx && grep -q 'DropdownMenuSub' src/components/ViewMenu.tsx && grep -q 'DropdownMenuPortal' src/components/ViewMenu.tsx && grep -q 'AboutDialog' src/components/HelpMenu.tsx && grep -q 'disabled' src/components/EditMenu.tsx && grep -q 'disabled' src/components/HelpMenu.tsx && npx tsc --noEmit -p . 2>&1 | grep -E '(EditMenu|ViewMenu|HelpMenu)' | (! grep -E 'error TS')</automated>
  </verify>
  <done>Three menu files exist, use exact action names per D-22, use array-driven THEMES per D-21, include D-19 comment, render Preferences/Keyboard Shortcuts as disabled stubs, no NEW tsc errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create CustomTitlebar.tsx + SecondaryToolbar.tsx</name>
  <files>gui/src/components/CustomTitlebar.tsx, gui/src/components/SecondaryToolbar.tsx</files>
  <read_first>
    - gui/src/components/Toolbar.tsx (full file — 130 lines — every block of this file moves into one of the two new components; nothing is invented)
    - .planning/phases/67-custom-titlebar/67-PATTERNS.md §"gui/src/components/CustomTitlebar.tsx (NEW)" (project-name + dirty-marker shape adjusted for D-06; root container classes)
    - .planning/phases/67-custom-titlebar/67-PATTERNS.md §"gui/src/components/SecondaryToolbar.tsx (NEW)" (verbatim Layer toggle + Code button + Export pattern)
    - .planning/phases/67-custom-titlebar/67-RESEARCH.md §"Pattern 4: Drag region with double-click toggle" (drag region is a SIBLING flex-1 h-full sandwiched between the menu cluster and WindowControls — D-26)
    - .planning/phases/67-custom-titlebar/67-UI-SPEC.md §"Layout Contract" §"Strip 1 — CustomTitlebar" and §"Strip 2 — SecondaryToolbar" (full slot-by-slot table)
    - .planning/phases/67-custom-titlebar/67-CONTEXT.md D-01, D-05, D-06, D-07, D-13, D-16, D-17, D-26
  </read_first>
  <action>
    Create `gui/src/components/CustomTitlebar.tsx`. Props: `{ onUnsavedCheck: () => Promise<"save" | "discard" | "cancel">; theme: Theme; setTheme: (t: Theme) => void }`.

    Store subscriptions (Pattern S1): `isDirty`, `currentFilePath`. Compute project name inline (per D-06):
    - if `currentFilePath` is truthy: `basename = currentFilePath.split(/[/\\\\]/).pop() ?? ""`, then strip last extension via `basename.replace(/\\.[^.]+$/, "")`.
    - else if `isDirty`: `"Untitled"`.
    - else: `""`.

    Root JSX shape (per UI-SPEC §"Strip 1" + D-01 + D-26):
    - Outer: `<div className="flex items-center h-9 bg-muted border-b w-full">`.
    - App icon (D-05, D-25): `<img src="/32x32.png" alt="" className="w-5 h-5 ml-2 shrink-0" />`.
    - Project name span: `<span className="text-xs text-muted-foreground ml-1 select-none truncate max-w-[120px]">{projectName}</span>`.
    - Dirty dot: `{isDirty && <span className="text-xs text-muted-foreground ml-0.5 select-none">●</span>}` (literal Unicode bullet character — NOT a Lucide icon, per UI-SPEC §Copywriting + D-06).
    - Four menus, in order, as SIBLINGS: `<FileMenu onUnsavedCheck={onUnsavedCheck} />`, `<EditMenu />`, `<ViewMenu theme={theme} setTheme={setTheme} />`, `<HelpMenu />`.
    - Drag region (per D-26 — MUST be a sibling, never a wrapper): `<div data-tauri-drag-region className="flex-1 h-full" onDoubleClick={() => void getCurrentWindow().toggleMaximize()} />`. Import `getCurrentWindow` from `@tauri-apps/api/window`. Per Pitfall 2 (RESEARCH.md), wrapping menus inside the drag region breaks their click handlers.
    - Window controls: `<WindowControls />`.

    Do NOT add `px-2` to the outer container (icon already carries `ml-2`). Do NOT add `justify-between` (drag region's `flex-1` handles the spacing). Do NOT change `h-9` — UI-SPEC §"Inherited Exceptions" forbids it.

    Create `gui/src/components/SecondaryToolbar.tsx`. No props.

    Store subscriptions (Pattern S1): `hasNodes` (derive via `useStore((s) => s.nodes.length > 0)`), `bottomPanelOpen`, `toggleBottomPanel`, `activeLayer`, `setActiveLayer`. Plus the `handleExport` async function that lives in current Toolbar.tsx (verbatim — copy lines 45-56 including imports of `generateCode`, `exportCode`, `getComponent`).

    Root JSX (per UI-SPEC §"Strip 2" + D-16):
    - Outer: `<div className="flex items-center justify-between h-8 px-2 bg-muted border-b w-full">`.
    - Left cluster: Layer toggle (verbatim copy of the existing `<ToggleGroup>` block at Toolbar.tsx lines 81-113, including the colored `data-[state=on]` variant classes for Hydraulic/Both/Thermal).
    - Right cluster: Code button (Toolbar.tsx lines 70-77 verbatim) + Export button (Toolbar.tsx lines 117-126 verbatim).
    - NO ThemeMenu (per D-03/D-16).
    - NO FileMenu (lives in CustomTitlebar).

    Per D-17, both strips are full-width and live at the root flex-col, NOT inside the center column. App.tsx restructure in Task 3 handles the placement.
  </action>
  <verify>
    <automated>cd gui && grep -q 'data-tauri-drag-region' src/components/CustomTitlebar.tsx && grep -q 'flex-1 h-full' src/components/CustomTitlebar.tsx && grep -q 'toggleMaximize' src/components/CustomTitlebar.tsx && grep -q '"/32x32.png"' src/components/CustomTitlebar.tsx && grep -q '●' src/components/CustomTitlebar.tsx && grep -q 'h-9' src/components/CustomTitlebar.tsx && grep -q 'h-8' src/components/SecondaryToolbar.tsx && (! grep -q 'ThemeMenu' src/components/SecondaryToolbar.tsx) && (! grep -q 'FileMenu' src/components/SecondaryToolbar.tsx) && npx tsc --noEmit -p . 2>&1 | grep -E '(CustomTitlebar|SecondaryToolbar)' | (! grep -E 'error TS')</automated>
  </verify>
  <done>CustomTitlebar shell exists with drag region as a SIBLING of menus and controls (D-26), 36px height, all five visual elements present; SecondaryToolbar is 32px with Layer + Code + Export only; no NEW tsc errors.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure App.tsx + delete Toolbar.tsx and ThemeMenu.tsx</name>
  <files>gui/src/App.tsx, gui/src/components/Toolbar.tsx, gui/src/components/ThemeMenu.tsx</files>
  <read_first>
    - gui/src/App.tsx (full file — focus on lines 1-50 imports, lines 251-365 keyboard + Tauri effects, lines 372-385 AutoRecover gate, lines 387-477 return JSX which is the target of the restructure)
    - .planning/phases/67-custom-titlebar/67-RESEARCH.md §"Integration site in App.tsx" (current vs target layout side-by-side at lines ~683-714)
    - .planning/phases/67-custom-titlebar/67-PATTERNS.md §"gui/src/App.tsx (MODIFIED — root-JSX restructure)" (preserve invariants list — AutoRecover gate, useEffect blocks, useShowCodeFor call)
    - .planning/phases/67-custom-titlebar/67-CONTEXT.md D-02, D-03, D-17 (Toolbar and ThemeMenu are deleted; both strips OUTSIDE the flex flex-1 row)
    - CLAUDE.md §"No back-compat shims during heavy dev" (delete freely; no compatibility exports needed)
  </read_first>
  <action>
    Modify `gui/src/App.tsx`:

    Imports:
    - Remove the `Toolbar` import.
    - Add `import CustomTitlebar from "./components/CustomTitlebar";` and `import SecondaryToolbar from "./components/SecondaryToolbar";`.
    - Keep the `useTheme` import — App.tsx is still the owner of `theme`/`setTheme` state passed down to ViewMenu via CustomTitlebar.

    JSX restructure (target shape per RESEARCH.md §"Integration site"):
    ```
    <div className="flex flex-col h-screen w-screen overflow-hidden">
      <CustomTitlebar onUnsavedCheck={showUnsavedDialog} theme={theme} setTheme={setTheme} />
      <SecondaryToolbar />
      <div className="flex flex-1 min-h-0">
        {!toolboxCollapsed && <ToolboxPanel … />}
        <div className="flex flex-col flex-1 min-w-0">
          <CanvasPanel resolvedTheme={resolvedTheme} />
        </div>
        {!sidebarCollapsed && <SidebarPanel … />}
      </div>
      <BottomPanel />
    </div>
    ```

    Removed: the inline `<Toolbar onUnsavedCheck={showUnsavedDialog} theme={theme} resolvedTheme={resolvedTheme} setTheme={setTheme} />` from inside the center column.

    Preserved invariants (per 67-PATTERNS.md):
    - AutoRecover render gate at lines 372-385 — the new `<CustomTitlebar>` and `<SecondaryToolbar>` render only AFTER the gate resolves (i.e., they sit INSIDE the existing post-gate return path; the gate JSX wrapper is unchanged).
    - All four `useEffect` blocks (lines 251, 274, 298, 305, 332) are untouched.
    - `useShowCodeFor()` call at line 36 is untouched.
    - `resolvedTheme` continues flowing to `<CanvasPanel resolvedTheme={resolvedTheme} />` — it is no longer propagated to a Toolbar (since Toolbar is deleted).

    Delete `gui/src/components/Toolbar.tsx` entirely (per D-02 + CLAUDE.md no-back-compat rule). Use `git rm` or `rm` — no leftover compatibility export file.

    Delete `gui/src/components/ThemeMenu.tsx` entirely (per D-03). Its three radio items already live in `ViewMenu.tsx` per Task 1.

    Verify there are no remaining imports of `Toolbar` or `ThemeMenu` anywhere in the codebase: `grep -rE "from .+/Toolbar\\\"|from .+/ThemeMenu\\\"" gui/src/` should return zero matches. If matches exist, update those import sites (likely test files) — either delete the test if it tests only the deleted component, or rewrite to import from `CustomTitlebar` / `SecondaryToolbar` / `ViewMenu` as appropriate. Document any test deletions in the SUMMARY for Plan 03.
  </action>
  <verify>
    <automated>cd gui && (! test -f src/components/Toolbar.tsx) && (! test -f src/components/ThemeMenu.tsx) && grep -q 'CustomTitlebar' src/App.tsx && grep -q 'SecondaryToolbar' src/App.tsx && (! grep -E "from ['\"].*Toolbar['\"]" src/App.tsx) && ! grep -rE "from ['\"].*\\/(Toolbar|ThemeMenu)['\"]" src/ 2>/dev/null && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>Toolbar.tsx and ThemeMenu.tsx are deleted from disk; no remaining imports reference them; App.tsx renders the new two-strip layout above the panel row; `npm run build` exits 0 (tsc errors do not exceed the pre-existing baseline of 11 per STATE.md; vitest 1 pre-existing failure is the only acceptable failure).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT — run npm run tauri dev on WSLg and exercise the titlebar surfaces</name>
  <what-built>
    The full Phase 67 custom titlebar: 36px strip with app icon + project name + dirty dot + File/Edit/View/Help menus + draggable center + Min/Max/Close window controls; 32px secondary strip below with Layer toggle + Code button + Export button. OS chrome removed. Old Toolbar.tsx and ThemeMenu.tsx deleted.
  </what-built>
  <how-to-verify>
    Run `cd gui && npm run tauri dev` on WSLg and walk through this checklist. Some items have known-risk surfaces flagged by RESEARCH.md — note any deviations in your response.

    Visual / layout:
    1. There is NO GTK window title bar above the custom titlebar (D-04). The first thing you see at the top of the window is the custom 36px strip.
    2. The titlebar shows: leftmost app icon (~20px placeholder, per D-05) → project name (basename of current file, no extension, or "Untitled" if dirty+unsaved, or empty if clean+unsaved per D-06) → dirty dot `●` immediately after the name when isDirty is true.
    3. Below the titlebar is the 32px secondary strip with Layer toggle, Code button, Export button — and NO ThemeMenu (D-03/D-16).
    4. The two strips occupy full screen width — not just the center column (D-17).

    Drag region (D-07/D-26 + Pitfall 2):
    5. Click-and-drag in the empty center area between the menus and the window controls — the OS window moves.
    6. Double-click the same empty center — the window maximizes (or restores if already maximized).
    7. Verify menu triggers (File/Edit/View/Help) still open when clicked — i.e., clicks do NOT bubble into the drag region (Tauri issue #9901 regression check).

    Window controls (D-13/D-14/D-15):
    8. On WSLg you should see the Windows/Linux variant — three Lucide icons on the right: Minus, Maximize2 (or Minimize2 when maximized), X. Click Minimize → window minimizes. Restore from taskbar. Click Maximize → toggles. Click Close → triggers the existing unsaved-changes guard.
    9. Maximize/Restore icon swap: after maximizing, the middle button should switch from Maximize2 to Minimize2 (the useWindowMaximized hook driving this swap).

    Menus (D-09/D-10/D-11/D-12 + D-22):
    10. File menu: New/Open/Save/Save As all behave as before (regression check — FileMenu.tsx is unchanged, only relocated).
    11. Edit menu items fire correctly: Undo/Redo/Cut/Copy/Paste/Duplicate. Paste in particular must wire to `pasteFromClipboard` (D-22) — verify by selecting nodes, choosing Copy from the Edit menu, then Paste from the Edit menu, and confirming nodes are duplicated.
    12. Edit menu: Preferences... is visibly disabled (grayed out).
    13. View menu: Toggle Code Preview shows a check mark when the BottomPanel is open.
    14. View → Layer submenu: selecting Hydraulic/Both/Thermal updates the secondary strip's Layer ToggleGroup (and vice versa — change the ToggleGroup and the radio selection updates).
    15. View → Theme submenu: selecting Light/Dark/System changes the theme immediately.
    16. Help menu: About STREAM Composer opens a dialog with title "STREAM Composer", a version line (matches `gui/src-tauri/tauri.conf.json` `version` field), and a "View on GitHub" link pointing to https://github.com/ramp-nuclear/STREAM.jl (D-20).
    17. Help menu: Keyboard Shortcuts is visibly disabled.

    Known-risk surfaces (D-18, Pitfall 3 in RESEARCH.md):
    18. **Edge-resize on WSLg.** Hover the cursor over each window edge (top, bottom, left, right) and each corner. The cursor SHOULD change to a resize cursor, and dragging SHOULD resize the window.
        - If resize works on all 4 edges + 4 corners: report PASS for edge-resize.
        - If resize is broken on some edges: report FAIL with which edges, but DO NOT block the phase. Per D-18, edge-resize regressions on WSLg are deferred to a follow-up phase — the window can still be resized via maximize toggle.
    19. **WSLg window-snap behavior with maximize.** Drag the window to a screen edge (Windows Snap gesture) or use Win+Left/Win+Right. Note whether snapping behaves correctly. Not a blocker either way; deferred.

    Build hygiene:
    20. `Toolbar.tsx` and `ThemeMenu.tsx` files are deleted from disk.
    21. `npm run build` exits 0 (pre-existing baseline: 11 tsc errors, 1 vitest failure per STATE.md — no NEW regressions).
  </how-to-verify>
  <resume-signal>Type "approved" if items 1-17 + 20-21 pass; report any FAILs on items 18-19 as known-risk surfaces (deferrable per D-18). Block on any FAIL in items 1-17 or 20-21 and describe the issue.</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
- Phase 67 custom titlebar visible and functional on WSLg
- All 26 locked decisions implemented (D-01..D-26)
- Toolbar.tsx and ThemeMenu.tsx deleted
- Manual UAT items 1-17 and 20-21 pass; items 18-19 (edge-resize, snapping) may regress per D-18 known-risk
</verification>

<success_criteria>
- `npm run tauri dev` launches a window with NO OS chrome and the custom titlebar visible
- Items 1-17 + 20-21 of the manual UAT checklist all PASS
- `cd gui && npm run build` exits 0; tsc + vitest baseline matches STATE.md (11 pre-existing tsc errors, 1 pre-existing vitest failure — no regressions)
- `grep -rE "from ['\"].*\\/(Toolbar|ThemeMenu)['\"]" gui/src/` returns zero matches
- Items 18-19 (WSLg edge-resize and snapping) documented in the SUMMARY whether they pass or fail; failures auto-deferred per D-18
</success_criteria>

<output>
Create `.planning/phases/67-custom-titlebar/67-03-SUMMARY.md` when done. Summarize: which UAT items passed/failed, any WSLg-specific behavior observations, whether the dialog rendering on first About-open had any flicker or layout shift, and which (if any) tests had to be deleted or rewritten because they imported the now-deleted Toolbar/ThemeMenu. List all 26 D-decisions and confirm each is implemented (or note specifically which decision was deferred and why).
</output>

## Truths — Decision Coverage

This plan claims the following CONTEXT.md decisions (colon-suffixed form for the GSD coverage gate):

- D-01: Two-strip layout — 36px `CustomTitlebar` + 32px `SecondaryToolbar`, both full-width, with `border-b` between them.
- D-02: Refactor `Toolbar.tsx` — split content between `CustomTitlebar` and `SecondaryToolbar`; restructure `App.tsx` so the titlebar lives outside the `flex flex-1 min-h-0` row.
- D-03: Delete `ThemeMenu.tsx` outright; its three theme items fold into View → Theme submenu inside `ViewMenu.tsx`.
- D-05: App icon — `<img src="/32x32.png">` rendered at ~20px (`w-5 h-5`) on the left of the titlebar.
- D-06: Project name display logic — `basename(currentFilePath)` without extension, falling back to `"Untitled"` when dirty, nothing when clean-and-unsaved; dirty dot `●` follows.
- D-07: Drag region carries `data-tauri-drag-region`, occupies center via `flex-1`, dbl-click calls `getCurrentWindow().toggleMaximize()`.
- D-08: Menu items display Ctrl+* accelerator labels (display only; Phase 65 already registered keydown listeners).
- D-09: File menu items preserved unchanged — New / Open… / Save / Save As… with existing accelerators; menu relocated into titlebar.
- D-10: Edit menu items — Undo, Redo, separator, Cut, Copy, Paste, Duplicate, separator, Preferences… (disabled stub); wired to store actions.
- D-11: View menu items — Toggle Code Preview (check on `bottomPanelOpen`), Layer submenu (radio: Hydraulic/Both/Thermal), Theme submenu (radio over `THEMES`).
- D-12: Help menu items — About STREAM Composer (opens `AboutDialog`), Keyboard Shortcuts (disabled stub).
- D-13: Window controls always on the right side of the titlebar; no platform-conditional positioning.
- D-16: Secondary strip contents (left to right) — Layer toggle, Code preview toggle, Export. ThemeMenu removed from this strip.
- D-17: Secondary strip is full-width (`w-full`) and lives in the root `flex flex-col`, not scoped to the center column.
- D-18: WSLg edge-resize contingency deferred — Task 4 UAT items 18-19 capture status; no CSS resize-gutter added.
- D-19: Edit menu items always-fire (no input-focus guard inside menu handlers); inline comment documents the asymmetry.
- D-21: ViewMenu Theme submenu maps over `THEMES` exported from `useTheme.ts` (Plan 02 added the export).
- D-22: Edit menu Paste uses store action `pasteFromClipboard` (NOT `pasteClipboard`); grep gate enforces.
- D-26: `data-tauri-drag-region` rendered as a sibling between the menu cluster and `<WindowControls />`, never as a wrapper around them.
