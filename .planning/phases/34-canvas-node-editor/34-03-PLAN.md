---
phase: 34-canvas-node-editor
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified: []
autonomous: false
requirements: [CANV-01, CANV-02, CANV-03, CANV-04, CANV-05, CANV-06, CANV-07]

must_haves:
  truths:
    - "User visually confirms drag-drop, edge drawing, deletion, and undo/redo work in the live app"
  artifacts: []
  key_links: []
---

<objective>
Human verification that the full canvas and node editor works correctly in the live Tauri app.

Purpose: Visual and interactive behaviors (drag-drop feel, edge drawing, handle snapping, node positioning) cannot be fully validated by unit tests. This checkpoint confirms the end-to-end experience matches CANV-01 through CANV-07.
Output: User approval or list of issues to fix.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-canvas-node-editor/34-CONTEXT.md
@.planning/phases/34-canvas-node-editor/34-01-SUMMARY.md
@.planning/phases/34-canvas-node-editor/34-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify canvas and node editor in live app</name>
  <what-built>
    Complete canvas and node editor: toolbox with 12 draggable components (Hydraulic + Thermal categories), custom node rendering with type label + instance name + FlowPort handles, edge drawing with directionality validation, node/edge deletion via Delete/Backspace, undo/redo via Ctrl+Z / Ctrl+Shift+Z.
  </what-built>
  <how-to-verify>
    Start the app: `cd gui && npm run tauri dev`

    1. **CANV-01 — Canvas controls**: Verify canvas has zoom (scroll wheel), pan (drag background), minimap (bottom-right), and fit-to-view button (Controls panel).

    2. **CANV-02 — Drag from toolbox**: Drag "Pump" from the left toolbox onto the canvas. Verify a node appears at the drop position with label "Pump" and instance name "pump_1". Drag another Pump — verify it's named "pump_2". Drag "Channel" — verify "channel_1".

    3. **CANV-03 — Edge connection**: Drag from Pump's port_out handle (right side) to Channel's port_in handle (left side). Verify an edge (line) connects them. Try dragging from port_in to port_in — verify it is rejected (no edge created).

    4. **CANV-04 — Delete**: Click a node to select it (should show ring highlight). Press Delete or Backspace — verify node and its connected edges disappear. Select an edge and delete it the same way.

    5. **CANV-05 — Reposition**: Drag a node to a new position. Verify its connected edges follow (no orphaned edges).

    6. **CANV-06 — Node display**: Verify each node card shows the component type (small gray text) and instance name (bold text). Handles visible on left/right sides.

    7. **CANV-07 — Undo/redo**: Add 3 nodes and 2 edges. Press Ctrl+Z five times — verify all operations are undone (canvas empty). Press Ctrl+Shift+Z five times — verify all operations are redone.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
User confirms all 7 CANV requirements work in the live app.
</verification>

<success_criteria>
- All 7 CANV requirements visually verified by user
- No console errors during interaction
- User types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/34-canvas-node-editor/34-03-SUMMARY.md`
</output>
