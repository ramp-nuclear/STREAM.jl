---
phase: 35.1-correlation-picker
verified: 2026-04-02T14:02:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
gaps: []
---

# Phase 35.1: Correlation Picker Verification Report

**Phase Goal:** Implement factory correlation picker UI — users can select and configure factory HTC correlations (regime_dependent, elenbaas_htc, maximal_htc) with their sub-parameters in the sidebar.
**Verified:** 2026-04-02T14:02:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                 | Status     | Evidence                                                                                             |
|----|-------------------------------------------------------------------------------------------------------|------------|------------------------------------------------------------------------------------------------------|
| 1  | `FunctionOption.sub_parameters` field exists in `gui/src/registry/types.ts`                          | ✓ VERIFIED | Line 15: `sub_parameters?: Parameter[]` with comment `// only present when kind === "factory"`       |
| 2  | `FactoryCorrelationValue` interface exists in `gui/src/registry/types.ts`                             | ✓ VERIFIED | Lines 19–23: `{ kind: "factory"; value: string; subParams: Record<string, unknown> }`               |
| 3  | Factory options in `components.json` have `sub_parameters` populated for all three channel components | ✓ VERIFIED | Channel, ChannelAndContacts, ChannelHeatFlux each have 3 factory options with 2–4 sub-params each    |
| 4  | `FunctionSelect.tsx` renders factory sub-fields inline (no longer disabled)                           | ✓ VERIFIED | Lines 124–151: `{isFactory && subParams.length > 0 && <div ...>}` renders sub-param fields          |
| 5  | `FunctionSelect.tsx` emits `FactoryCorrelationValue` for factory, plain string for simple             | ✓ VERIFIED | `handleSelectChange`: factory → `{ kind: "factory", value, subParams: {} }`; simple → plain string  |
| 6  | Info icons (ⓘ) added to `NumericField`, `PipeGeometryPicker`, `InstanceNameField` labels              | ✓ VERIFIED | All three files import `Info` from lucide-react and render `<Info>` inside `<TooltipProvider>`       |
| 7  | `FunctionSelect.test.tsx` exists with ≥8 tests                                                       | ✓ VERIFIED | 8 tests in `describe("FunctionSelect", ...)` — all 8 pass                                           |
| 8  | All 77 tests pass (no regressions)                                                                    | ✓ VERIFIED | `Tests  77 passed | 17 todo (94)` — `Test Files  9 passed | 1 skipped (10)`                          |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact                                                              | Expected                                              | Status     | Details                                                                     |
|-----------------------------------------------------------------------|-------------------------------------------------------|------------|-----------------------------------------------------------------------------|
| `gui/src/registry/types.ts`                                           | `FunctionOption.sub_parameters` + `FactoryCorrelationValue` | ✓ VERIFIED | Both present, well-typed                                                |
| `gui/src/registry/components.json`                                    | Factory options with sub_parameters for 3 components  | ✓ VERIFIED | Channel/ChannelAndContacts/ChannelHeatFlux each have regime_dependent (3 sub-params), elenbaas_htc (4), maximal_htc (2) |
| `gui/src/components/sidebar/FunctionSelect.tsx`                       | Renders sub-fields inline, emits FactoryCorrelationValue | ✓ VERIFIED | Fully implemented with recursive sub-FunctionSelect for nested factories |
| `gui/src/components/sidebar/NumericField.tsx`                         | Info icon on label                                    | ✓ VERIFIED | `<Info className="h-3 w-3 ...">` inside TooltipProvider                     |
| `gui/src/components/sidebar/PipeGeometryPicker.tsx`                   | Info icon on label                                    | ✓ VERIFIED | `<Info className="h-3 w-3 ...">` inside TooltipProvider                     |
| `gui/src/components/sidebar/InstanceNameField.tsx`                    | Info icon on label                                    | ✓ VERIFIED | `<Info className="h-3 w-3 ...">` inside TooltipProvider                     |
| `gui/src/components/sidebar/__tests__/FunctionSelect.test.tsx`        | ≥8 tests                                              | ✓ VERIFIED | 8 tests, all passing                                                         |

### Key Link Verification

| From                    | To                          | Via                                    | Status     | Details                                                              |
|-------------------------|-----------------------------|----------------------------------------|------------|----------------------------------------------------------------------|
| `ParameterForm.tsx`     | `FunctionSelect.tsx`        | import + `case "Function":` render     | ✓ WIRED    | Line 4 import; line 80–86 render with value and onChange passthrough |
| `FunctionSelect.tsx`    | `FactoryCorrelationValue`   | import from `@/registry/types`         | ✓ WIRED    | Line 17 import; used in handler and prop types                       |
| `FunctionSelect.tsx`    | `NumericField.tsx`          | import + sub-param `type === "Real"`   | ✓ WIRED    | Line 16 import; lines 138–147 render for Real/Int sub-params         |
| `FunctionSelect.tsx`    | Recursive self               | `type === "Function"` sub-param branch | ✓ WIRED    | Lines 129–136 — nested FunctionSelect for function-typed sub-params  |
| `components.json`       | `FunctionOption.sub_parameters` | schema matches TypeScript interface | ✓ WIRED    | sub_parameters arrays in registry match Parameter[] type shape       |

### Data-Flow Trace (Level 4)

Not applicable — this phase delivers UI components (no async data fetching, no DB layer). The data source is `components.json` (static registry), which is fully populated with factory sub-parameters.

### Behavioral Spot-Checks

| Behavior                                    | Command                                     | Result           | Status  |
|---------------------------------------------|---------------------------------------------|------------------|---------|
| TypeScript compiles with zero errors        | `npx tsc --noEmit`                          | No output (clean) | ✓ PASS |
| FunctionSelect 8 tests pass                 | `npx vitest run`                            | 8/8 passing       | ✓ PASS |
| Full suite 77 tests pass                    | `npx vitest run --reporter=verbose`         | 77 passed, 0 fail | ✓ PASS |

### Requirements Coverage

No PARA- requirement IDs were assigned to this phase (it emerged from Phase 35 discuss-phase). Coverage is assessed against the phase goal directly — all 8 must-haves pass.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | — | — | No anti-patterns found |

Specific checks run:
- No `TODO/FIXME/HACK` comments in any phase files
- `FunctionSelect.tsx`: no `return null` or empty handler stubs (sub-field section conditionally renders)
- `handleSelectChange` and `handleSubParamChange` both emit real values (not `console.log`-only)
- `isFactory && subParams.length > 0` guard is correct — prevents rendering an empty container

### Human Verification Required

#### 1. Factory picker visual rendering in running app

**Test:** Open the app, drag a Channel node onto the canvas, select it, open the sidebar. Change the `htc_correlation` dropdown to "Regime Dependent". Verify that sub-fields (`htc_forced`, `htc_natural`, `threshold`) appear inline below the dropdown, indented with a left border.
**Expected:** Sub-fields appear immediately with correct labels, types (dropdowns for Function sub-params, numeric inputs for Real), and default values.
**Why human:** Visual layout and interaction timing cannot be verified with grep or TypeScript checks.

#### 2. Nested factory sub-picker (elenbaas within regime_dependent.htc_forced)

**Test:** Select "Regime Dependent", then in the `htc_forced` sub-dropdown select "Elenbaas". Verify that b, L, Dh, g numeric inputs appear nested inside the htc_forced sub-section.
**Expected:** Nested sub-fields render correctly (recursive FunctionSelect with numeric inputs for b, L, Dh, g).
**Why human:** Recursive rendering depth requires visual confirmation.

### Gaps Summary

No gaps. All 8 must-haves are verified. The phase goal is fully achieved.

---

_Verified: 2026-04-02T14:02:00Z_
_Verifier: Claude (gsd-verifier)_
