---
phase: 38-ui-design-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gui/src/registry/icons.ts
  - gui/src/components/StreamNode.tsx
  - gui/src/components/ToolboxItem.tsx
  - gui/src/registry/__tests__/icons.test.ts
  - gui/src/components/__tests__/StreamNode.test.tsx
autonomous: true
requirements: [DSGN-03, DSGN-04]

must_haves:
  truths:
    - "Every canvas node shows a Lucide icon matching its component type"
    - "Every canvas node has a colored left-border stripe (blue for Hydraulic, amber for Thermal)"
    - "Every toolbox item shows the same icon as its corresponding canvas node"
    - "All 12 STREAM components have an icon mapping"
  artifacts:
    - path: "gui/src/registry/icons.ts"
      provides: "COMPONENT_ICONS map, CATEGORY_BORDER_CLASSES map, getComponentIcon, getCategoryBorderClass"
      exports: ["COMPONENT_ICONS", "FALLBACK_ICON", "getComponentIcon", "CATEGORY_BORDER_CLASSES", "getCategoryBorderClass"]
    - path: "gui/src/components/StreamNode.tsx"
      provides: "Redesigned node with icon + category border stripe"
      contains: "border-l-"
    - path: "gui/src/components/ToolboxItem.tsx"
      provides: "Toolbox item with Lucide icon"
      contains: "getComponentIcon"
    - path: "gui/src/registry/__tests__/icons.test.ts"
      provides: "Icon map coverage test"
      contains: "COMPONENT_ICONS"
  key_links:
    - from: "gui/src/registry/icons.ts"
      to: "gui/src/components/StreamNode.tsx"
      via: "import getComponentIcon, getCategoryBorderClass"
      pattern: "getComponentIcon|getCategoryBorderClass"
    - from: "gui/src/registry/icons.ts"
      to: "gui/src/components/ToolboxItem.tsx"
      via: "import getComponentIcon"
      pattern: "getComponentIcon"
---

<objective>
Add component icons and category color differentiation to canvas nodes and toolbox items.

Purpose: Implements DSGN-03 (toolbox icons per component type) and DSGN-04 (canvas nodes visually differentiated by color coding and icons). Creates the single-source-of-truth icon map used throughout the app.
Output: `icons.ts` map, redesigned `StreamNode.tsx` with left-border stripe + icon, updated `ToolboxItem.tsx` with icon, tests.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ui-design-pass/38-UI-SPEC.md
@.planning/phases/38-ui-design-pass/38-CONTEXT.md
@.planning/phases/38-ui-design-pass/38-RESEARCH.md

@gui/src/components/StreamNode.tsx
@gui/src/components/ToolboxItem.tsx
@gui/src/components/ToolboxPanel.tsx
@gui/src/registry/__tests__/registry.test.ts
@gui/src/components/__tests__/StreamNode.test.tsx

<interfaces>
<!-- Existing interfaces the executor needs -->

From gui/src/registry/index.ts:
```typescript
export function getComponent(id: string): ComponentDef | undefined;
export function getComponentsByCategory(category: string): ComponentDef[];
```

From gui/src/store/useStore.ts:
```typescript
export interface StreamNodeData {
  componentId: string;
  instanceName: string;
  parameters: Record<string, unknown>;
  constructorMode?: string;
}
```

From registry components.json, each component has:
```json
{ "id": "Channel", "label": "Channel", "category": "Hydraulic", ... }
```
Category is always "Hydraulic" or "Thermal".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create icon map and update StreamNode + ToolboxItem</name>
  <files>gui/src/registry/icons.ts, gui/src/registry/__tests__/icons.test.ts, gui/src/components/StreamNode.tsx, gui/src/components/ToolboxItem.tsx, gui/src/components/__tests__/StreamNode.test.tsx</files>
  <behavior>
    - Test: COMPONENT_ICONS has entries for all 12 component IDs (Channel, ChannelAndContacts, ChannelHeatFlux, Pump, Flapper, Friction, Gravity, Resistor, Inertia, HeatExchanger, ConstantTemperature, HeatDiffusion)
    - Test: getComponentIcon("Channel") returns RectangleHorizontal, getComponentIcon("unknown") returns FALLBACK_ICON (Box)
    - Test: getCategoryBorderClass("Hydraulic") returns "border-l-blue-500", getCategoryBorderClass("Thermal") returns "border-l-amber-500", getCategoryBorderClass("unknown") returns ""
    - Test: StreamNode renders with the icon (SVG element present in container)
    - Test: StreamNode renders with category border class (container has "border-l-blue-500" for Pump which is Hydraulic)
    - Test: StreamNode still renders component label and instance name
  </behavior>
  <action>
    1. Create `gui/src/registry/icons.ts` per UI-SPEC.md Component Icon Map (per D-01, D-02, D-09):
       - Import all 12 Lucide icons: RectangleHorizontal, Layers, Flame, Gauge, ToggleRight, Slash, ArrowDown, Minus, Weight, Thermometer, ThermometerSun, Grid3x3, Box
       - Export `COMPONENT_ICONS: Record<string, LucideIcon>` mapping all 12 component IDs to their icons
       - Export `FALLBACK_ICON = Box`
       - Export `getComponentIcon(componentId: string): LucideIcon` helper
       - Export `CATEGORY_BORDER_CLASSES: Record<string, string>` with `Hydraulic: "border-l-blue-500"` and `Thermal: "border-l-amber-500"` (per D-03, D-04) — MUST be full literal strings, never dynamic construction (Tailwind JIT pitfall)
       - Export `getCategoryBorderClass(category: string): string` helper

    2. Create `gui/src/registry/__tests__/icons.test.ts`:
       - Test that COMPONENT_ICONS has exactly the 12 expected keys
       - Test getComponentIcon for known and unknown IDs
       - Test getCategoryBorderClass for both categories and unknown

    3. Update `gui/src/components/StreamNode.tsx` (per D-04, D-05):
       - Import `getComponentIcon`, `getCategoryBorderClass` from `@/registry/icons`
       - Import `getComponent` to look up `category` field
       - Add 3px left border stripe: `border-l-[3px] ${getCategoryBorderClass(component.category)}`
       - Add icon on first line next to component type label: `<Icon className="w-3.5 h-3.5" />`
       - First line: `flex items-center gap-1 text-[11px] text-muted-foreground` with icon + component.label
       - Second line: `font-semibold text-sm` with instanceName (unchanged content)
       - Keep existing `ring-2 ring-[var(--ring)]` selection ring (per UI-SPEC: selection ring is unchanged)
       - Keep existing Handle rendering (unchanged)

    4. Update `gui/src/components/ToolboxItem.tsx` (per D-08, D-09):
       - Import `getComponentIcon` from `@/registry/icons`
       - Add `flex items-center gap-2` to the outer div
       - Render `<Icon className="w-4 h-4 text-muted-foreground" />` before the label text
       - Keep existing `draggable`, `onDragStart`, and styling classes

    5. Update `gui/src/components/__tests__/StreamNode.test.tsx`:
       - Add test: node container includes `border-l-blue-500` for Pump (Hydraulic category)
       - Add test: node container includes an SVG element (the Lucide icon)
       - Existing tests (renders label, instance name, handles) must still pass
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui && npx vitest run src/registry/__tests__/icons.test.ts src/components/__tests__/StreamNode.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 12 components have icon mappings; StreamNode shows category border stripe + icon; ToolboxItem shows icon; all tests pass</done>
</task>

</tasks>

<verification>
cd /home/itay/projects/Julia-STREAM/gui && npx vitest run --passWithNoTests 2>&1 | tail -10
</verification>

<success_criteria>
- All 12 STREAM components have a Lucide icon mapping in icons.ts
- Canvas nodes display component icon + colored left border (blue for Hydraulic, amber for Thermal)
- Toolbox items display the same icon as corresponding canvas nodes
- All existing tests still pass; new icon/border tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/38-ui-design-pass/38-01-SUMMARY.md`
</output>
