---
phase: 61-registry-audit-rewrite-for-v1-1
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - gui/src/registry/components.json
autonomous: true
requirements: []
tags: [gui, registry, channels, heat-diffusion]
must_haves:
  truths:
    - "components.json `Channel` entry has no `thermal` port and no `htc_correlation` kwarg, but has `h_left`/`h_right` polymorphic kwargs and an `external_inputs[]` block with `T_wall_left` and `T_wall_right`"
    - "components.json `ChannelHeatFlux` entry has no `thermal` port and an `external_inputs[]` block with `q_left` and `q_right`"
    - "components.json `ChannelAndContacts` entry keeps `htc_correlation`/`friction_correlation`; every factory option in the recursive tree drops `Dh`/`L`/`b`/`aspect_ratio`; geometry-bearing factories carry `geom_source: \"parent\"`; thermal port pair encoded with `array_size: \"n\"`, `default_axis: \"vertical\"`, `pair_with`"
    - "components.json `HeatDiffusion` thermal port pair encoded with `array_size: \"nz\"`, `default_axis: \"horizontal\"`, `pair_with`"
  artifacts:
    - path: "gui/src/registry/components.json"
      provides: "Rewritten Channel, ChannelHeatFlux, ChannelAndContacts, HeatDiffusion entries"
      contains: '"external_inputs"'
  key_links:
    - from: "gui/src/registry/components.json Channel.external_inputs[].source_component"
      to: "gui/src/registry/components.json WallTemperature (added in Plan 03)"
      via: "FK string match"
      pattern: '"source_component": "WallTemperature"'
    - from: "gui/src/registry/components.json ChannelHeatFlux.external_inputs[].source_component"
      to: "gui/src/registry/components.json HeatFluxSource (added in Plan 03)"
      via: "FK string match"
      pattern: '"source_component": "HeatFluxSource"'
---

<objective>
Rewrite the four channel-family entries in `gui/src/registry/components.json`: `Channel`, `ChannelHeatFlux`, `ChannelAndContacts`, and `HeatDiffusion`. This is the core port/parameter delta for v1.1 — Channel and CHF lose their `thermal` port (they no longer expose MTK thermal connectors; T_wall/q are external-input @variables now), Channel gains `h_left`/`h_right`, CAC keeps its correlation knobs but with collapsed factory sub-trees per Phase 59 geom-first, and HD picks up the new `array_size`/`default_axis`/`pair_with` shape.

Purpose: The four entries are the most-changed by v1.1. Bundling them in one plan with ordered tasks (one task per entry) avoids parallel write-contention on the same JSON file and lets each task see the cumulative state.

Output: Rewritten Channel/CHF/CAC/HD entries. New components (WallTemperature, HeatFluxSource, PK, RC) and unchanged-component re-audit are deferred to Plans 03/04.

Note on FK to-be-added: Channel.external_inputs[].source_component points to "WallTemperature" and CHF.external_inputs[].source_component points to "HeatFluxSource". Neither source component exists in components.json yet — they are added in Plan 03. This dangling reference is intentional and resolved by Plan 05's cross-validation test.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-registry-audit-rewrite-for-v1-1/61-CONTEXT.md
@.planning/phases/61-registry-audit-rewrite-for-v1-1/61-01-SUMMARY.md
@.planning/notes/correlation-geom-first-api.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the Channel entry</name>
  <files>gui/src/registry/components.json</files>
  <read_first>
    - gui/src/registry/components.json (lines 5-172 — the current Channel entry, end-to-end; the executor MUST see what is being replaced)
    - src/components/channels.jl (lines 199-300 — the authoritative `Channel(; name, n, geometry, g=0.0, h_left, h_right, friction_correlation)` signature plus the `T_wall_left[1:n]` / `T_wall_right[1:n]` @variables block at lines 256-272 — this is the source of truth for parameter list AND external_inputs shape)
    - .planning/phases/61-registry-audit-rewrite-for-v1-1/61-CONTEXT.md sections <decisions> D-03/D-04/D-05/D-10/D-15/D-18 and <specifics> (the explicit JSON sketch for Channel)
    - .planning/notes/correlation-geom-first-api.md (the allowed options under Channel.friction_correlation: blasius_friction, turbulent_friction, laminar_friction — and per CD-03, regime_dependent is NOT a legal option here)
  </read_first>
  <action>
    Replace the entire `Channel` entry (currently at lines 5-172) in `gui/src/registry/components.json` with a v1.1-compliant entry. Required key/value shape:

    - Top-level: `id: "Channel"`, `label: "Channel"`, `category: "Hydraulic"`, `description` (update wording to reflect v1.1: per-cell h_left/h_right Properties-tab kwargs; T_wall_left/T_wall_right external-input BCs; no thermal MTK port).
    - `parameters`: in this exact order, matching the Julia kwarg list at `src/components/channels.jl:199-207`:
      1. `n` — Int, required, positional false, description "Number of axial finite-volume cells".
      2. `geometry` — PipeGeometry, required, positional false, description (carry over from current entry).
      3. `g` — Real, default 0.0, unit "m/s^2", required false, positional false.
      4. `h_left` — uses D-10 polymorphic pattern: `type_union: ["Real","Vector","Function"]`, `input_modes: ["scalar","vector","callable"]`, default 0.0, unit "W/(m^2 K)", required false, positional false. (Source: `channels.jl:204`.)
      5. `h_right` — same shape as `h_left`. (Source: `channels.jl:205`.)
      6. `friction_correlation` — Function, default "blasius_friction", required false, positional false. Its `options` array contains ONLY: `blasius_friction` (kind: "stateless", no sub_parameters), `turbulent_friction` (kind: "stateless", sub_parameters: [{name: "epsilon", type: "Real", default: 0.0, required: false}]), `laminar_friction` (kind: "factory", geom_source: "parent", no sub_parameters). Per CD-03, do NOT include `regime_dependent` (Channel has no htc consumer).
    - `external_inputs` (NEW top-level array per D-03): two entries — `T_wall_left` and `T_wall_right`. Each has fields per D-05: `name`, `shape: "[1:n]"`, `unit: "K"`, `description`, `bc_modes: ["Value","Profile","Function","Mark","Source"]`, `source_component: "WallTemperature"`, `source_port: "T_wall_out"`. The exact `<specifics>` sketch in CONTEXT.md shows this verbatim — follow it.
    - `ports`: per D-18, exactly two entries — `{name: "port_in", type: "FlowPort", side: "left"}` and `{name: "port_out", type: "FlowPort", side: "right"}`. The OLD `thermal` port (current line 13) MUST be removed.
    - `constructorModes`: keep a single mode entry — `{mode: "default", signature: "Channel(; n, geometry, g=0.0, h_left=0.0, h_right=0.0, friction_correlation=blasius_friction)", parameters: ["n","geometry","g","h_left","h_right","friction_correlation"]}` — derive from the current Julia signature at `channels.jl:199-207`.

    Do NOT inline factory sub-trees that have been collapsed. Do NOT carry the old `htc_correlation` kwarg (per D-18, Channel has no `htc_correlation` under v1.1 — only CAC does).

    Preserve JSON validity. Keep indentation consistent with the rest of `components.json`.
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui &amp;&amp; jq -e '(.components[] | select(.id=="Channel")) as $c | ($c.ports | map(.type) | contains(["ThermalPort"]) | not) and ($c.parameters | map(.name) | contains(["h_left","h_right"])) and ($c.parameters | map(.name) | contains(["htc_correlation"]) | not) and ($c.external_inputs | map(.name) == ["T_wall_left","T_wall_right"]) and ($c.external_inputs[0].source_component=="WallTemperature") and ($c.external_inputs[0].source_port=="T_wall_out") and ($c.external_inputs[0].bc_modes == ["Value","Profile","Function","Mark","Source"]) and (($c.parameters[] | select(.name=="friction_correlation").options | map(.value)) == ["blasius_friction","turbulent_friction","laminar_friction"])' src/registry/components.json &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.components[] | select(.id=="Channel") | .ports | map(.type) | index("ThermalPort")' components.json` returns `null` (thermal port dropped).
    - `jq '.components[] | select(.id=="Channel") | .parameters | map(.name) | index("htc_correlation")' components.json` returns `null` (htc dropped from Channel per D-18).
    - `jq '.components[] | select(.id=="Channel") | .parameters | map(.name) | index("h_left")' components.json` returns a non-null integer; same for `h_right`.
    - `jq '.components[] | select(.id=="Channel") | .external_inputs | length' components.json` returns `2`.
    - `jq '.components[] | select(.id=="Channel") | .external_inputs[] | .source_component' components.json` returns `"WallTemperature"` twice.
    - `jq '.components[] | select(.id=="Channel") | .parameters[] | select(.name=="friction_correlation") | .options | map(.value)' components.json` does NOT contain `"regime_dependent"`.
    - `jq '.components[] | select(.id=="Channel") | .parameters[] | select(.name=="h_left") | .type_union' components.json` outputs `["Real","Vector","Function"]`.
    - `npm --prefix gui run build` exits 0.
  </acceptance_criteria>
  <done>
    Channel entry mirrors `src/components/channels.jl:199-207` exactly: same kwargs in same order, same defaults, polymorphic `h_left`/`h_right`, no thermal port, `external_inputs[]` declares T_wall_{left,right}.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the ChannelHeatFlux entry</name>
  <files>gui/src/registry/components.json</files>
  <read_first>
    - gui/src/registry/components.json (the current `ChannelHeatFlux` entry, line ~349 — the executor MUST see what is being replaced)
    - src/components/channels.jl (lines 357-460 — the authoritative `ChannelHeatFlux(; name, n, geometry, g=0.0, friction_correlation)` signature plus the `q_left[1:n]`/`q_right[1:n]` @variables block)
    - .planning/phases/61-registry-audit-rewrite-for-v1-1/61-CONTEXT.md sections D-03/D-05/D-19 (CHF port list identical to Channel)
    - The just-rewritten Channel entry in components.json (Task 1 output) — CHF mirrors Channel's friction_correlation shape exactly; copy that subtree verbatim for consistency.
  </read_first>
  <action>
    Replace the entire `ChannelHeatFlux` entry in `gui/src/registry/components.json` with a v1.1-compliant entry:

    - Top-level: `id: "ChannelHeatFlux"`, `label: "Channel (Heat Flux BC)"`, `category: "Hydraulic"`, `description` updated to reflect the v1.1 design (per-cell `q_left`/`q_right` external-input flux BCs; no thermal MTK port; no Properties-tab h_left/h_right since CHF imposes flux directly).
    - `parameters` in this exact order, matching `src/components/channels.jl:357-365`:
      1. `n` — Int, required, positional false.
      2. `geometry` — PipeGeometry, required, positional false.
      3. `g` — Real, default 0.0, unit "m/s^2", required false, positional false.
      4. `friction_correlation` — same shape as Channel's `friction_correlation` (Task 1): options = blasius_friction (stateless), turbulent_friction (stateless, sub_parameters: [epsilon]), laminar_friction (factory, geom_source: parent). Per CD-03, no `regime_dependent`.
    - `external_inputs` per D-03 / D-05: two entries — `q_left` and `q_right`. Each: `shape: "[1:n]"`, `unit: "W/m^2"`, `description` (per-cell heat flux BC), `bc_modes: ["Value","Profile","Function","Mark","Source"]`, `source_component: "HeatFluxSource"`, `source_port: "q_out"`.
    - `ports` per D-19: identical to Channel — `port_in` (FlowPort, left), `port_out` (FlowPort, right). NO thermal port.
    - `constructorModes`: single mode `{mode: "default", signature: "ChannelHeatFlux(; n, geometry, g=0.0, friction_correlation=blasius_friction)", parameters: ["n","geometry","g","friction_correlation"]}`.

    DROP the legacy `T_wall` scalar parameter that the current entry has (under v1.1, CHF imposes flux, not wall temperature; the legacy test `chf!.parameters.find(p => p.name === 'T_wall')` will be updated in Plan 05 to reflect this).
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui &amp;&amp; jq -e '(.components[] | select(.id=="ChannelHeatFlux")) as $c | ($c.ports | map(.type) | contains(["ThermalPort"]) | not) and ($c.parameters | map(.name) | contains(["T_wall"]) | not) and ($c.parameters | map(.name) | contains(["htc_correlation"]) | not) and ($c.external_inputs | map(.name) == ["q_left","q_right"]) and ($c.external_inputs[0].source_component=="HeatFluxSource") and ($c.external_inputs[0].source_port=="q_out") and ($c.external_inputs[0].unit=="W/m^2") and (($c.parameters[] | select(.name=="friction_correlation").options | map(.value)) == ["blasius_friction","turbulent_friction","laminar_friction"])' src/registry/components.json &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.components[] | select(.id=="ChannelHeatFlux") | .ports | map(.type) | index("ThermalPort")'` returns `null`.
    - `jq '.components[] | select(.id=="ChannelHeatFlux") | .parameters | map(.name) | index("T_wall")'` returns `null` (legacy scalar T_wall dropped).
    - `jq '.components[] | select(.id=="ChannelHeatFlux") | .external_inputs | length'` returns `2`.
    - `jq '.components[] | select(.id=="ChannelHeatFlux") | .external_inputs[] | .source_component'` returns `"HeatFluxSource"` twice.
    - `jq '.components[] | select(.id=="ChannelHeatFlux") | .external_inputs[0].unit'` returns `"W/m^2"`.
    - `npm --prefix gui run build` exits 0.
  </acceptance_criteria>
  <done>
    CHF entry mirrors `src/components/channels.jl:357-365`: no thermal port, no T_wall scalar, `external_inputs[]` declares q_{left,right} with HeatFluxSource as source.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the ChannelAndContacts entry — collapse factory sub-trees, reshape thermal ports</name>
  <files>gui/src/registry/components.json</files>
  <read_first>
    - gui/src/registry/components.json (the current `ChannelAndContacts` entry, line ~173 — heaviest single component, has the deepest recursive factory tree)
    - src/components/channels.jl (lines 467-620 — the authoritative `ChannelAndContacts(; name, n, geometry, g=0.0, htc_correlation, friction_correlation)` signature plus the `thermal_left[1:n]`/`thermal_right[1:n]` array-of-scalar ThermalPort declarations)
    - src/physical_models/correlations.jl OR src/physical_models/htc/correlations.jl + src/physical_models/friction/correlations.jl — current factory signatures: `laminar_friction(geom)`, `elenbaas_htc(geom; g=9.81)`, `fully_developed_laminar_h_spl(geom)`, `developing_laminar_h_spl(geom; develop_length)`, `regime_dependent(geom; htc_laminar, htc_turbulent, friction_laminar, friction_turbulent, htc_natural=nothing, g=9.81, Re_transition=2300)`. The executor must read these to confirm the remaining sub_parameters per D-08.
    - .planning/notes/correlation-geom-first-api.md — Phase 59 handoff doc spelling out remaining kwargs per factory.
    - .planning/phases/61-registry-audit-rewrite-for-v1-1/61-CONTEXT.md sections D-06/D-07/D-08/D-09/D-16/D-17/D-20 and the <specifics> Factory tree example.
  </read_first>
  <action>
    Replace the entire `ChannelAndContacts` entry in `gui/src/registry/components.json` with a v1.1-compliant entry. Key/value shape:

    - Top-level: `id: "ChannelAndContacts"`, `label: "Channel and Contacts"`, `category: "Hydraulic"`, `description` updated for v1.1.
    - `parameters` in this exact order, matching `src/components/channels.jl:467` constructor signature:
      1. `n` — Int, required, positional false.
      2. `geometry` — PipeGeometry, required, positional false.
      3. `g` — Real, default 0.0, unit "m/s^2", required false, positional false.
      4. `htc_correlation` — Function, default "dittus_boelter", required false, positional false. `options` array with these entries per D-08 (do NOT include `Dh`/`L`/`b`/`aspect_ratio` in any factory's sub_parameters — Phase 59 dropped them):
         - `{value: "dittus_boelter", kind: "stateless"}` — no sub_parameters.
         - `{value: "constant_Nusselt", kind: "stateless", sub_parameters: [{name: "Nu", type: "Real", default: 8.235}]}`.
         - `{value: "elenbaas_htc", kind: "factory", geom_source: "parent", sub_parameters: [{name: "g", type: "Real", default: 9.81, unit: "m/s^2"}]}`.
         - `{value: "developing_laminar_h_spl", kind: "factory", geom_source: "parent", sub_parameters: [{name: "develop_length", type: "Real", required: true, no_default: true, unit: "m"}]}`.
         - `{value: "fully_developed_laminar_h_spl", kind: "factory", geom_source: "parent"}` — no sub_parameters per CD-01 (omit the empty array).
         - `{value: "regime_dependent", kind: "factory", geom_source: "parent", produces: ["htc","friction"], sub_parameters: [...]}` — sub_parameters per D-08 exact list: htc_laminar (Function, required: true, options: only the HTC stateless leaves — dittus_boelter, constant_Nusselt, elenbaas_htc, developing_laminar_h_spl, fully_developed_laminar_h_spl, but NOT regime_dependent itself — no infinite recursion); htc_turbulent (same shape as htc_laminar); friction_laminar (Function, required: true, options: blasius_friction, turbulent_friction, laminar_friction); friction_turbulent (same shape as friction_laminar); htc_natural (Function, required: false, options: same as htc_laminar); g (Real, required_if: "htc_natural", default omitted because conditional, unit: "m/s^2"); Re_transition (Real, default: 2300).
      5. `friction_correlation` — Function, default "blasius_friction", required false. `options`: blasius_friction (stateless), turbulent_friction (stateless, sub_parameters: [epsilon]), laminar_friction (factory, geom_source: "parent"), regime_dependent (factory, geom_source: "parent", produces: ["htc","friction"], same sub_parameters tree as in htc_correlation.options — per D-09 it is independently selectable on both fields; the codegen-side dedupe is Phase 66's concern).
    - `external_inputs`: OMIT the field entirely (per D-03 — CAC has no external inputs; its thermal boundary is set via the `thermal_left`/`thermal_right` MTK ports).
    - `ports` per D-20:
      - `{name: "port_in", type: "FlowPort", side: "left"}`
      - `{name: "port_out", type: "FlowPort", side: "right"}`
      - `{name: "thermal_left", type: "ThermalPort", array_size: "n", default_axis: "vertical", pair_with: "thermal_right"}`
      - `{name: "thermal_right", type: "ThermalPort", array_size: "n", default_axis: "vertical", pair_with: "thermal_left"}`
      - The legacy `side: "top"`/`side: "bottom"` AND legacy `array: true`/`arrayParam: "n"` keys must NOT appear on the rewritten thermal ports (Plan 05 sunsets the legacy keys; for new entries, do not emit them).
    - `constructorModes`: single mode `{mode: "default", signature: "ChannelAndContacts(; n, geometry, g=0.0, htc_correlation=dittus_boelter, friction_correlation=blasius_friction)", parameters: ["n","geometry","g","htc_correlation","friction_correlation"]}`.

    Reasoning encoded directly: every collapsed factory sub_parameters drop maps to a one-line Julia signature reduction in `src/physical_models/`. The executor verifies by grepping each factory in `src/physical_models/` and confirming the remaining kwargs match the registry one-to-one.
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui &amp;&amp; jq -e '(.components[] | select(.id=="ChannelAndContacts")) as $c | ($c.ports | length == 4) and (($c.ports[] | select(.name=="thermal_left")).array_size == "n") and (($c.ports[] | select(.name=="thermal_left")).default_axis == "vertical") and (($c.ports[] | select(.name=="thermal_left")).pair_with == "thermal_right") and (($c.ports[] | select(.name=="thermal_right")).pair_with == "thermal_left") and (($c.parameters[] | select(.name=="htc_correlation").options | map(.value)) | contains(["regime_dependent","dittus_boelter","constant_Nusselt","elenbaas_htc","developing_laminar_h_spl","fully_developed_laminar_h_spl"])) and (([$c.parameters[] | select(.name=="htc_correlation").options[] | select(.value=="elenbaas_htc").sub_parameters[].name] | contains(["b","L","Dh","aspect_ratio"]) | not)) and (($c.parameters[] | select(.name=="htc_correlation").options[] | select(.value=="elenbaas_htc").geom_source) == "parent") and (($c.parameters[] | select(.name=="htc_correlation").options[] | select(.value=="regime_dependent").produces) == ["htc","friction"]) and ($c.external_inputs == null or $c.external_inputs == [])' src/registry/components.json &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.components[] | select(.id=="ChannelAndContacts") | .ports | length'` returns `4`.
    - `jq '.components[] | select(.id=="ChannelAndContacts") | .ports[] | select(.name=="thermal_left") | .array_size'` returns `"n"`.
    - `jq '.components[] | select(.id=="ChannelAndContacts") | .ports[] | select(.name=="thermal_left") | .default_axis'` returns `"vertical"`.
    - `jq '.components[] | select(.id=="ChannelAndContacts") | .ports[] | select(.name=="thermal_left") | .pair_with'` returns `"thermal_right"`.
    - Every factory option in both `htc_correlation.options` and `friction_correlation.options` (recursively) — no `sub_parameters` entry has `name` equal to `Dh`, `L`, `b`, or `aspect_ratio`. Use this jq: `jq '[.components[] | select(.id=="ChannelAndContacts") | .. | objects | select(has("sub_parameters")) | .sub_parameters[]?.name] | map(select(. == "Dh" or . == "L" or . == "b" or . == "aspect_ratio")) | length'` and confirm result is `0`.
    - `jq '.components[] | select(.id=="ChannelAndContacts") | .parameters[] | select(.name=="htc_correlation") | .options[] | select(.value=="elenbaas_htc") | .geom_source'` returns `"parent"`.
    - `jq '.components[] | select(.id=="ChannelAndContacts") | .parameters[] | select(.name=="htc_correlation") | .options[] | select(.value=="regime_dependent") | .produces'` returns `["htc","friction"]`.
    - `jq '.components[] | select(.id=="ChannelAndContacts") | .external_inputs'` returns `null` (or the key is absent).
    - `npm --prefix gui run build` exits 0.
  </acceptance_criteria>
  <done>
    CAC entry reflects v1.1 source exactly: collapsed factory sub-trees, geom_source tags on factories, thermal port pair with array_size/default_axis/pair_with, no external_inputs.
  </done>
</task>

<task type="auto">
  <name>Task 4: Reshape the HeatDiffusion thermal port pair</name>
  <files>gui/src/registry/components.json</files>
  <read_first>
    - gui/src/registry/components.json (the current `HeatDiffusion` entry, line ~804 — last entry in the file)
    - src/components/heat_diffusion.jl (full file — confirm port names `thermal_left[1:nz]` / `thermal_right[1:nz]`, parameter list, defaults; the executor should treat this as authoritative)
    - .planning/phases/61-registry-audit-rewrite-for-v1-1/61-CONTEXT.md section D-21 (HD default_axis is horizontal, array_size references nz not n)
  </read_first>
  <action>
    Update the `HeatDiffusion` entry in `gui/src/registry/components.json`:

    - DO NOT touch `parameters` unless the audit against `src/components/heat_diffusion.jl` reveals drift (this task is primarily ports + cross-ref). Plan 04 owns the full unchanged-components re-audit; here we touch HD because its ports need the new schema.
    - `ports`: replace the current two thermal entries with:
      - `{name: "thermal_left", type: "ThermalPort", array_size: "nz", default_axis: "horizontal", pair_with: "thermal_right"}`
      - `{name: "thermal_right", type: "ThermalPort", array_size: "nz", default_axis: "horizontal", pair_with: "thermal_left"}`
    - Remove the legacy `array: true` / `arrayParam: "nz"` / `side: "left"|"right"` keys from these thermal ports (Plan 05 sunsets these globally; here we proactively drop them from the rewritten entries).
    - `external_inputs`: OMIT (HD has no BC variables — its `T[1:nz, 1:nx]` is internal state, not an external input).

    Note for executor: HD's `array_size: "nz"` is intentional (not `"n"`) — HD's array parameter is `nz`, not `n`. This is the one place in the registry where `array_size` references a non-`n` parameter; cross-validation in Plan 05's tests will check that `array_size` referents exist on the same entry.
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui &amp;&amp; jq -e '(.components[] | select(.id=="HeatDiffusion")) as $c | (($c.ports[] | select(.name=="thermal_left")).array_size == "nz") and (($c.ports[] | select(.name=="thermal_left")).default_axis == "horizontal") and (($c.ports[] | select(.name=="thermal_left")).pair_with == "thermal_right") and (($c.ports[] | select(.name=="thermal_right")).array_size == "nz") and (($c.ports[] | select(.name=="thermal_right")).default_axis == "horizontal") and (($c.ports[] | select(.name=="thermal_right")).pair_with == "thermal_left") and ($c.parameters | map(.name) | contains(["nz","nx"]))' src/registry/components.json &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.components[] | select(.id=="HeatDiffusion") | .ports[] | select(.name=="thermal_left") | .array_size'` returns `"nz"`.
    - `jq '.components[] | select(.id=="HeatDiffusion") | .ports[] | select(.name=="thermal_left") | .default_axis'` returns `"horizontal"`.
    - `jq '.components[] | select(.id=="HeatDiffusion") | .ports[] | select(.name=="thermal_left") | .pair_with'` returns `"thermal_right"`.
    - `jq '.components[] | select(.id=="HeatDiffusion") | .ports[] | select(.name=="thermal_right") | .pair_with'` returns `"thermal_left"`.
    - `jq '.components[] | select(.id=="HeatDiffusion") | .ports[] | select(.name=="thermal_left") | has("array")'` returns `false` (legacy `array` key dropped from this entry).
    - `jq '.components[] | select(.id=="HeatDiffusion") | .external_inputs'` returns `null`.
    - `npm --prefix gui run build` exits 0.
  </acceptance_criteria>
  <done>
    HD thermal port pair carries the v1.1 array_size/default_axis/pair_with triple with horizontal default_axis; legacy `array`/`arrayParam`/`side` keys removed from these specific ports.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON file ↔ TS loader | Same as Plan 01 — `components.json` is the contract; tsc + tests enforce shape. |
| `external_inputs[].source_component` FK | A string FK that resolves at runtime when the BCs-tab UI opens the dropdown. Dangling FKs (referenced component absent) yield empty dropdowns, not crashes — but Plan 05 adds a build-time test that catches them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04 | Tampering | `components.json` Channel/CHF entries | mitigate | `jq` assertions in `<verify>` confirm exact shape; build runs after each task. |
| T-61-05 | Information Disclosure | `external_inputs` FK strings | accept | Registry is non-sensitive metadata; FKs are component IDs already exposed elsewhere. |
| T-61-06 | Denial of Service | GUI startup if FKs dangle | mitigate (Plan 05) | Plan 05 adds a cross-validation test that every `source_component` resolves to a registered id and every `array_size` referent exists as a sibling parameter. |
</threat_model>

<verification>
- `cd gui && npm run build` exits 0 after each task.
- `jq '.components | length' gui/src/registry/components.json` still returns `12` (no components added or removed in this plan — only four rewritten in place).
- Channel/CHF: no `ThermalPort` in `.ports`; both have `external_inputs` of length 2.
- CAC: 4 ports; thermal pair carries array_size/default_axis/pair_with; no `Dh`/`L`/`b`/`aspect_ratio` anywhere in its factory sub-trees.
- HD: thermal pair carries array_size=`"nz"`, default_axis=`"horizontal"`, pair_with cross-references.
</verification>

<success_criteria>
- All four channel-family entries reflect v1.1 source-of-truth signatures and port shapes.
- All factory sub-trees in CAC are collapsed per Phase 59 geom-first.
- Polymorphic kwargs (Channel.h_left, Channel.h_right) carry the `type_union`/`input_modes` shape from D-10.
- `external_inputs[]` populated where applicable (Channel, CHF) and absent where not (CAC, HD).
- `tsc + vite build` is clean at the end of every task.
</success_criteria>

<output>
After completion, create `.planning/phases/61-registry-audit-rewrite-for-v1-1/61-02-SUMMARY.md` documenting: the four entries rewritten, key shape decisions taken under Claude's discretion (e.g., how `regime_dependent.htc_laminar.options` was bounded to prevent infinite recursion), and any source-vs-registry discrepancies surfaced during the rewrite.
</output>
