---
phase: 61-registry-audit-rewrite-for-v1-1
plan: 05
type: execute
wave: 5
depends_on: ["61-01", "61-02", "61-03", "61-04"]
files_modified:
  - gui/src/registry/__tests__/registry.test.ts
  - gui/src/lib/validation.ts
  - gui/src/lib/codeGenerator.ts
  - gui/src/store/useStore.ts
autonomous: false
requirements: []
tags: [gui, registry, tests, consumer-reconciliation, verification]
must_haves:
  truths:
    - "`registry.test.ts` reflects the v1.1 component set (16 entries, includes the 4 new IDs) and the v1.1 invariants (no thermal port on Channel/CHF, BCPort recognized, external_inputs present where required)"
    - "Cross-validation assertions exist for: every `external_inputs[].source_component` resolves to a registered component id; every `array_size` referent exists as a sibling parameter; every `pair_with` resolves to a sibling port"
    - "Downstream consumers (validation.ts, codeGenerator.ts, useStore.ts) continue to type-check and pass their existing tests under the new schema — no full behavior rework, but minimal compatibility edits where the relaxed schema unblocks compilation"
    - "Final acceptance: `npm --prefix gui run build && npm --prefix gui test` exits 0"
  artifacts:
    - path: "gui/src/registry/__tests__/registry.test.ts"
      provides: "v1.1-aligned registry test suite (component count, IDs, port shapes, FK resolution)"
      contains: "WallTemperature"
  key_links:
    - from: "gui/src/registry/__tests__/registry.test.ts"
      to: "gui/src/registry/components.json"
      via: "import + jest-style assertion"
      pattern: "getAllComponents"
---

<objective>
Update the test suite and downstream registry consumers so the v1.1 registry passes `npm run build && npm test`. This plan is the verification + reconciliation pass: it (a) updates `registry.test.ts` to reflect the v1.1 invariants, (b) adds new cross-validation tests (FK resolution, `array_size` referent resolution, `pair_with` resolution), and (c) touches downstream consumers (`validation.ts`, `codeGenerator.ts`, `useStore.ts`) only to the minimum extent needed to keep them compiling under the v1.1 schema — full behavior rework is out of scope for Phase 61 (Phase 63/66/71 own those reworks per CONTEXT §3.2 / 3.10 / 3.11).

This plan ends with a checkpoint task: human verifies the GUI starts (`npm run dev`) and the toolbox renders all 16 components (including the 4 new ones) without console errors.

Output: A test suite that locks in the v1.1 invariants; downstream consumers compiling under the new types; a human-verified GUI smoke test.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-registry-audit-rewrite-for-v1-1/61-CONTEXT.md
@.planning/phases/61-registry-audit-rewrite-for-v1-1/61-01-SUMMARY.md
@.planning/phases/61-registry-audit-rewrite-for-v1-1/61-02-SUMMARY.md
@.planning/phases/61-registry-audit-rewrite-for-v1-1/61-03-SUMMARY.md
@.planning/phases/61-registry-audit-rewrite-for-v1-1/61-04-SUMMARY.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update registry.test.ts for the v1.1 shape + add cross-validation tests</name>
  <files>gui/src/registry/__tests__/registry.test.ts</files>
  <read_first>
    - gui/src/registry/__tests__/registry.test.ts (full file — 137 lines; every assertion needs review)
    - gui/src/registry/components.json (now in v1.1 shape after Plans 01-04 — confirm the exact ID set, port shapes, external_inputs presence)
    - gui/src/registry/types.ts (the extended schema from Plan 01)
    - gui/src/registry/index.ts (the public API the tests use)
  </read_first>
  <action>
    Rewrite assertions in `registry.test.ts` to lock in the v1.1 invariants. Specific edits:

    1. `'loads all 12 STREAM.jl components'` → `'loads all 16 STREAM.jl components (v1.1)'`. Change `toHaveLength(12)` to `toHaveLength(16)`.

    2. `stream_version` / `schema_version` assertions are already updated by Plan 01 Task 2 — confirm they read `"1.1.0"` and `"2.0"` and leave alone.

    3. `'contains all expected component IDs'` — update the `expected` array to include the 4 new IDs:
       ```ts
       const expected = [
         'Channel', 'ChannelAndContacts', 'ChannelHeatFlux',
         'Pump', 'Flapper', 'Friction', 'Gravity', 'Resistor',
         'Inertia', 'HeatExchanger', 'ConstantTemperature', 'HeatDiffusion',
         'WallTemperature', 'HeatFluxSource', 'PointKinetics', 'ReactivityController'
       ];
       ```

    4. `'every port has required fields'` — the `port.type` regex currently matches `^(FlowPort|ThermalPort)$`. Extend to `^(FlowPort|ThermalPort|BCPort)$`. The `port.side` regex currently requires `^(left|right|top|bottom)$`; loosen to allow `side` to be optional (some array ports under v1.1 may omit `side` in favor of `default_axis`).

    5. `'ChannelAndContacts has ThermalPort array ports'` — currently asserts `array: true` and `arrayParam: 'n'`. Update to assert `array_size === 'n'`, `default_axis === 'vertical'`, `pair_with === 'thermal_right'` for `thermal_left` and the mirror for `thermal_right`.

    6. `'HeatDiffusion has ThermalPort array ports'` — same update: assert `array_size === 'nz'`, `default_axis === 'horizontal'`, `pair_with`.

    7. `'ChannelHeatFlux has no ThermalPort (T_wall is scalar BC)'` — UPDATE the test body: the assertion that `thermalPorts.length === 0` is correct and stays. But the second part — asserting that `T_wall` is a scalar param — is WRONG under v1.1 (T_wall was renamed to per-cell q via external_inputs). Replace with: assert no `T_wall` param exists, and assert `external_inputs` contains entries with names `q_left` and `q_right`.

    8. `'Channel has no ThermalPort (T_wall is external input)'` — ADD this new test. Assert `Channel.ports` has no `ThermalPort` and no `htc_correlation` param; assert `external_inputs` includes `T_wall_left` and `T_wall_right`.

    9. `'getComponentsByCategory filters correctly'` — currently expects 10 Hydraulic + 2 Thermal. Update to reflect v1.1: Hydraulic = 10 (Channel, CHF, CAC, Pump, Flapper, Friction, Gravity, Resistor, Inertia, HeatExchanger), Thermal = 2 (ConstantTemperature, HeatDiffusion), Sources = 2 (WallTemperature, HeatFluxSource), Reactor Physics = 1 (PointKinetics), Resources = 1 (ReactivityController). Total still 16.

    10. ADD cross-validation tests (NEW assertions, encoded D-03/D-05/D-16/D-17):
        - `'every external_inputs[].source_component resolves to a registered component id'` — for each component with `external_inputs`, every `source_component` must appear in the master id set.
        - `'every external_inputs[].source_port resolves to a port on its source_component'` — for each FK, the named source component must have a port whose `name` matches `source_port`.
        - `'every array_size references a sibling parameter on the same component'` — for each port with `array_size`, the value must appear in the same component's `parameters[].name` set (Channel.thermal_*.array_size="n" → Channel.parameters must include `n`; HD.thermal_*.array_size="nz" → HD.parameters must include `nz`).
        - `'every pair_with port reference resolves to a sibling port'` — for each port with `pair_with`, the referenced port name must exist on the same component, and its `pair_with` must point back symmetrically.
        - `'BCPort is only used by Sources category components'` — value-source-only invariant per D-14/D-15.
        - `'ReactivityController has resource_kind and no ports'` — D-13 invariant.

    11. KEEP `'Pump has two constructor modes'` and `'ConstantTemperature is Thermal category'` — they were re-audited in Plan 04 and still hold.

    12. KEEP the SCAF-04 architecture-check test (`'adding a component requires only JSON'`) but consider updating its variable IDs to include `WallTemperature` so the test reflects the new components.
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui &amp;&amp; npm test -- src/registry/__tests__/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix gui test -- src/registry/__tests__/registry.test.ts` exits 0 (all assertions pass).
    - `grep -c "toHaveLength(16)" gui/src/registry/__tests__/registry.test.ts` ≥ 1.
    - `grep -c "WallTemperature\|HeatFluxSource\|PointKinetics\|ReactivityController" gui/src/registry/__tests__/registry.test.ts` ≥ 4.
    - `grep -c "external_inputs" gui/src/registry/__tests__/registry.test.ts` ≥ 2 (cross-validation tests added).
    - `grep -c "pair_with" gui/src/registry/__tests__/registry.test.ts` ≥ 1.
    - `grep -c "array_size" gui/src/registry/__tests__/registry.test.ts` ≥ 1.
    - `grep -c "BCPort" gui/src/registry/__tests__/registry.test.ts` ≥ 1.
  </acceptance_criteria>
  <done>
    Registry test suite reflects v1.1 invariants; cross-validation assertions detect FK/array_size/pair_with breakage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile downstream consumers (validation.ts, codeGenerator.ts, useStore.ts)</name>
  <files>gui/src/lib/validation.ts, gui/src/lib/codeGenerator.ts, gui/src/store/useStore.ts</files>
  <read_first>
    - gui/src/lib/validation.ts (full file — imports `ComponentDefinition` from registry/types; the executor must see what it does with ports/parameters)
    - gui/src/lib/codeGenerator.ts (full file or at least the registry imports and the per-component-handler dispatch — what does it read from each entry?)
    - gui/src/store/useStore.ts (lines 13, 151, 305-320, 383-410, 450 — every `getComponent`/`constructorModes` call site flagged in the earlier grep)
    - gui/src/components/sidebar/ParameterForm.tsx (line 26 — `component.constructorModes.find(...)` — confirm what happens when a Resources entry has no constructorModes; since Plan 01 made constructorModes optional, this call site may need a guard)
    - gui/src/lib/codeGenerator.test.ts and gui/src/lib/validation.test.ts (their existing assertions — what behaviors are pinned?)
  </read_first>
  <action>
    The full rework of these files belongs to Phase 63/66/71. This task makes ONLY the minimum edits needed to keep the build and existing tests green under the v1.1 schema.

    For each file:

    1. `gui/src/lib/validation.ts`:
       - If it references `port.type === 'ThermalPort'` exhaustively (e.g., a discriminated-union narrowing), confirm it handles `'BCPort'` as well. If a `default:` branch / exhaustive check would throw, add a no-op handler for `BCPort` (the actual BCPort validation rules are Phase 71's concern; here we just keep compilation green).
       - If it iterates over `component.parameters` and the new `type_union` / `input_modes` / `Mark` / `Symbol` types cause type-narrowing failures, widen the cast or add a guard. Do NOT implement the new mode-picker logic — Phase 63 does that.

    2. `gui/src/lib/codeGenerator.ts`:
       - The registry imports (line 12) pull `Parameter`, `FunctionOption`, etc. With the relaxed schema (Plan 01), existing code should compile. If tsc surfaces issues (e.g., `Parameter.type` now admits `"Vector" | "Symbol"`), narrow at the call site or add type-guards.
       - Do NOT implement the `external_inputs[]` codegen path here (Phase 66 owns it). For now, an Entry with `external_inputs[]` simply produces a TODO comment in the emitted Julia. Add a minimal stub: when generating code for `Channel`/`ChannelHeatFlux`, emit a `# TODO: Phase 66 — wire external_inputs into MTK equations` comment block.
       - The `regime_dependent.produces` field is informational only at this phase — codegen does not need to honor it yet.

    3. `gui/src/store/useStore.ts`:
       - Line 315: `component?.constructorModes[0]?.mode ?? "default"`. Since `constructorModes` is now optional, this needs a fallback when `constructorModes` is undefined: `component?.constructorModes?.[0]?.mode ?? "default"`. Tweak the optional chaining.
       - Other call sites: confirm they handle the case where `getComponent(id)` returns an entry without `ports` (Resources have no ports). Where the code iterates `comp.ports`, default to `[]`.

    4. `gui/src/components/sidebar/ParameterForm.tsx`:
       - Line 26: `component.constructorModes.find(...)`. With `constructorModes` now optional, this errors. Guard: `component.constructorModes?.find(...)` and handle the undefined case (skip rendering mode picker for Resources). Resources never reach ParameterForm via the toolbox-drag path; they reach it via the navigator (Phase 62 territory). For Phase 61, an `if (!component.constructorModes || component.constructorModes.length === 0) return ...` early-return is acceptable.

    Document every line touched in the SUMMARY with the rationale "compile-only fix; full behavior rework deferred to Phase X".
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui &amp;&amp; npm run build &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd gui &amp;&amp; npm run build` exits 0 (tsc clean, vite build clean).
    - `cd gui &amp;&amp; npm test` exits 0 (full vitest suite green, including registry, codeGenerator, validation, icons, and any other existing tests).
    - `grep -c "TODO: Phase 66" gui/src/lib/codeGenerator.ts` ≥ 1 OR the deferred-codegen note appears as a SUMMARY entry (executor may choose: comment-in-code OR doc-in-summary; either is acceptable).
    - No file outside the declared `files_modified` list has been touched (executor verifies with `git status` and reports any drift in the SUMMARY).
  </acceptance_criteria>
  <done>
    All downstream consumers compile and pass their existing tests under the v1.1 schema; deferred rework explicitly marked.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify GUI startup + toolbox rendering</name>
  <what-built>The v1.1 registry (16 components, new schema fields) and its TypeScript schema have been rewritten and validated by automated tests. This checkpoint confirms the running GUI handles the new schema visually — no crashes, no console errors, all 16 components visible in the toolbox.</what-built>
  <how-to-verify>
    1. From the project root, in a terminal: `cd gui && npm run dev`. Wait for Vite to report "Local: http://localhost:5173/" (or similar).
    2. Open the URL in a browser (or trigger the Tauri dev window if running via `npm run tauri dev` — pick whichever the project normally uses).
    3. Open the browser DevTools console (F12 → Console tab). Reload the page. Expect: ZERO red console errors. Yellow warnings are acceptable but should be noted.
    4. Confirm the toolbox panel renders. Count the components visible. Expected: 16 entries grouped by category. Specifically:
       - Hydraulic category: Channel, ChannelAndContacts, ChannelHeatFlux, Pump, Flapper, Friction, Gravity, Resistor, Inertia, HeatExchanger (10).
       - Thermal: ConstantTemperature, HeatDiffusion (2).
       - Sources: WallTemperature, HeatFluxSource (2).
       - Reactor Physics: PointKinetics (1).
       - Resources: ReactivityController (1). NOTE: Resources may not appear in the toolbox (navigator-tree UI is Phase 62) — it is acceptable for ReactivityController to be missing from the toolbox; if so, confirm only via `getAllComponents().length === 16` in the browser console.
    5. Drag a `Channel` block onto the canvas. Click it to open the sidebar/property panel. Confirm: `n`, `geometry`, `g`, `h_left`, `h_right`, `friction_correlation` fields render. NO `htc_correlation` field. NO thermal port handle on the block icon (only the two FlowPort handles).
    6. Drag a `WallTemperature` block onto the canvas. Confirm: a single output-side handle renders (this is the BCPort). The block's property panel shows `n` and `T_wall` with the `T_wall` field offering a mode picker (scalar/vector/callable) per D-10. If the mode picker is not yet implemented (Phase 63 owns it), that is acceptable — confirm only that the field exists.
    7. Stop the dev server (Ctrl-C).
  </how-to-verify>
  <resume-signal>Type "approved" (or describe issues — e.g., console errors, missing components, broken UI). If the GUI crashes or shows console errors, do NOT approve; instead describe the failure and the orchestrator will spawn a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite ↔ JSON | Tests are the executable contract; they catch FK/array_size/pair_with breakage at CI time. |
| GUI runtime ↔ JSON | The browser parses `components.json` at app load; malformed JSON would crash startup. Checkpoint Task 3 is the human-in-the-loop smoke for this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-12 | Tampering | registry FK drift | mitigate | New cross-validation tests in registry.test.ts catch dangling source_component / array_size / pair_with at CI time. |
| T-61-13 | Denial of Service | GUI startup with malformed registry | mitigate | Checkpoint Task 3 — human confirms zero console errors at startup. |
| T-61-14 | Tampering | downstream consumer drift | mitigate | `npm run build` (tsc) is the structural gate; this plan keeps it green. Behavior rework (BCs tab, codegen for external_inputs) is explicitly deferred. |
| T-61-15 | Repudiation | "looks fine to me" handwaves | mitigate | Task 3's `how-to-verify` lists 7 numbered steps with measurable outcomes; subjective approvals are rejected. |
</threat_model>

<verification>
- `cd gui && npm run build` exits 0.
- `cd gui && npm test` exits 0.
- New cross-validation tests in `registry.test.ts` execute and pass.
- Human-verified: GUI starts, toolbox renders 16 components, no console errors, Channel and WallTemperature blocks render correctly.
</verification>

<success_criteria>
- Registry test suite locks in v1.1 invariants.
- Downstream consumers compile and pass existing tests.
- GUI runs with the new registry without errors.
- All four must_haves (count = 16, cross-validation tests present, consumers green, build+test exit 0) are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/61-registry-audit-rewrite-for-v1-1/61-05-SUMMARY.md` documenting: test updates with rationale, every downstream-consumer file touched with rationale (and deferred rework explicitly labeled), checkpoint approval status from the human, and any open follow-ups discovered during smoke testing (these become candidate inputs to Phase 62/63/66/71).
</output>
