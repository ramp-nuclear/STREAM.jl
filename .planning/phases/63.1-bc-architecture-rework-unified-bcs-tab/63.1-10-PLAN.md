---
phase: 63.1
plan: 10
type: execute
wave: 6
depends_on: ["63.1-08"]
files_modified:
  - gui/src/store/useStore.ts
autonomous: true
requirements:
  - D-20
  - D-21
  - D-22
  - D-23
requirements_addressed:
  - D-20
  - D-21
  - D-22
  - D-23
must_haves:
  truths:
    - "CR-01: addEdge BCPort branch writes a bcMode entry (mode:'source') for the dragged BC edge and (when symmetric) for the sibling port (D-20)"
    - "CR-02: setBCSymmetric(true) with leftEntry undefined + rightEntry defined deletes the right entry (and reconciles the dangling BC edge) (D-21)"
    - "CR-03: setBCSymmetric(true) with leftEntry defined + leftEntry !== rightEntry mirrors left → right AND reconciles BC edges for the right side (D-21)"
    - "Click-vs-drag: onNodesChange syncs selectedNodeId when a select NodeChange with selected:true arrives (D-22)"
    - "N-mismatch ring auto-clears when the underlying mismatch is corrected (D-23 — verified empirically by selectNodeErrors recompute behaviour; subsumed by Plan 02)"
    - "Edge reconciliation logic in setBCSymmetric reuses a private helper _reconcileEdgesForBCMode extracted from setBCMode (no duplicate edge-add code)"
  artifacts:
    - path: "gui/src/store/useStore.ts"
      provides: "CR-01 addEdge BCPort fix; CR-02/CR-03 setBCSymmetric fix; click-vs-drag onNodesChange fix; _reconcileEdgesForBCMode private helper"
      contains: "CR-01"
  key_links:
    - from: "addEdge BCPort branch"
      to: "bcMode slice + sibling-port write under symmetric mode"
      via: "bcModeKey + siblingExternalInputName + stripSideSuffix helpers"
      pattern: "bcModeKey"
    - from: "onNodesChange select sync"
      to: "selectedNodeId via selectNode action"
      via: "NodeChange.type==='select' && c.selected"
      pattern: "selectNode"
---

<objective>
Wave 6 — close all four named bug fixes (D-20, D-21, D-22) and confirm the auto-clearing behavior (D-23 — already structurally true after Plan 02). Each fix is a tight surgical change to useStore.ts. Extract a private helper `_reconcileEdgesForBCMode` so setBCSymmetric and setBCMode share the edge-add/remove logic instead of duplicating it.

Purpose: Patch the named regressions from Phase 63 (CR-01 / CR-02 / CR-03 / click-vs-drag) in a single coherent plan now that the underlying state shape is settled. Plan 01's CR-01 / CR-02 / CR-03 / click-vs-drag RED tests flip GREEN here. After this plan, vitest is green across the whole GUI suite.

Output: useStore.ts patched in three branches (addEdge, setBCSymmetric, onNodesChange) plus a new private helper; all four Plan 01 RED tests flip to GREEN.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-CONTEXT.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-08-SUMMARY.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-09-SUMMARY.md
@gui/src/store/useStore.ts
@gui/src/lib/bcMode.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — addEdge BCPort branch writes bcMode entry on canvas-drag (D-20)</name>
  <files>gui/src/store/useStore.ts</files>
  <behavior>
    - When a user drags a BC edge from a WT/HFS source port to a consumer BCPort target, addEdge writes `bcMode[consumer::targetHandle] = {mode:"source", sourceNodeId: source}`
    - When the consumer pair is symmetric (bcSymmetric[consumer::baseField] is true or default), the same entry is written to the sibling key (e.g. T_wall_left also seeds T_wall_right)
    - Existing `_checkBCNMismatch(...)` call at this branch is gone (deleted in Plan 02 already; verify nothing references it)
    - Plan 01's useStore.addEdge.bcport.test.ts flips to GREEN
  </behavior>
  <read_first>
    gui/src/store/useStore.ts (lines 1047-1062 current BCPort branch in addEdge — the body to replace),
    gui/src/lib/bcMode.ts (exports bcModeKey + helpers stripSideSuffix + siblingExternalInputName — verify by grep that these exist; PATTERNS notes they're already used by setBCMode at L1120-1124),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"CR-01 fix" lines 624-654 — drop-in code; §"CR-01" in PATTERNS lines 305-353),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"useStore.ts — CR-01 fix" lines 305-353),
    gui/src/store/__tests__/useStore.addEdge.bcport.test.ts (Plan-01 RED — this task flips it to GREEN)
  </read_first>
  <action>Locate the BCPort branch inside addEdge near line 1047-1062. After the existing `set({ edges: finalEdges, isDirty: true })` line, before the `return`, insert the materialize-bcMode block from RESEARCH lines 631-649 verbatim. Specifically: when `connection.targetHandle` is truthy, derive `key = bcModeKey(connection.target, connection.targetHandle)`, `baseField = stripSideSuffix(connection.targetHandle)`, `symKey = ${connection.target}::${baseField}`, `symmetric = get().bcSymmetric[symKey] ?? true` (default true matches existing convention), `siblingName = symmetric ? siblingExternalInputName(connection.targetHandle) : null`. Construct `entry: BCModeEntry = {mode:"source", sourceNodeId: connection.source}`. Build `nextBCMode = {...get().bcMode, [key]: entry}`; if siblingName is truthy, also write `nextBCMode[bcModeKey(connection.target, siblingName)] = entry`. `set({bcMode: nextBCMode})`. After D-15 the legacy `get()._checkBCNMismatch(connection.source, connection.target)` call is already gone (Plan 02 removed it); verify no stale reference remains in this branch. Add an inline comment `// CR-01 (D-20): materialize bcMode on canvas-drag` for grep/traceability.</action>
  <verify>
    <automated>cd gui && grep -c "CR-01" src/store/useStore.ts; grep -c "bcModeKey(connection.target, connection.targetHandle)" src/store/useStore.ts; grep -c "_checkBCNMismatch" src/store/useStore.ts; npx vitest run src/store/__tests__/useStore.addEdge.bcport.test.ts 2>&1 | tail -15; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "CR-01" gui/src/store/useStore.ts` returns 1 (the explanatory comment).
    `grep -c "bcModeKey(connection.target, connection.targetHandle)" gui/src/store/useStore.ts` returns 1.
    `grep -c "_checkBCNMismatch" gui/src/store/useStore.ts` returns 0 (already gone per Plan 02; this task confirms no resurrection).
    `cd gui && npx vitest run src/store/__tests__/useStore.addEdge.bcport.test.ts` reports all GREEN.
    `cd gui && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>CR-01 fix in place; the addEdge BCPort branch writes bcMode (and sibling under symmetric mode); test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-02 / CR-03 — setBCSymmetric handles both edge-holes; extract _reconcileEdgesForBCMode helper (D-21)</name>
  <files>gui/src/store/useStore.ts</files>
  <behavior>
    - A new private function `_reconcileEdgesForBCMode(edges, nodes, consumerNodeId, externalInputName, prevEntry, newEntry, symmetric): Edge[]` exists, extracted from setBCMode's edge-materialization branch (lines ~1137-1208). It is pure: returns a new edges array; does NOT call set() or _pushSnapshot
    - setBCMode is refactored to call _reconcileEdgesForBCMode instead of its inline edge-materialization block
    - setBCSymmetric(true) handles two cases:
      (i) leftEntry === undefined && rightEntry !== undefined → delete rightKey from bcMode AND call _reconcileEdgesForBCMode(..., rightEntry, undefined, false) to remove the dangling right BC edge (CR-02)
      (ii) leftEntry !== undefined && leftEntry !== rightEntry → write nextBCMode[rightKey] = leftEntry AND call _reconcileEdgesForBCMode(..., rightEntry, leftEntry, false) to add/remove the right BC edge to match the new mode (CR-03)
    - Plan 01's useStore.setBCSymmetric.test.ts (both CR-02 and CR-03 cases) flips to GREEN
  </behavior>
  <read_first>
    gui/src/store/useStore.ts (lines 1116-1218 current setBCMode body including its edge-materialization branch at ~1137-1208; lines 1283-1303 current setBCSymmetric body; lines 1187-1204 the edge-add idiom that needs extraction),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"CR-02/CR-03 fix" lines 658-714 — extracted helper + refactored setBCSymmetric body),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"useStore.ts — CR-02 / CR-03 fix" lines 357-407),
    gui/src/store/__tests__/useStore.setBCSymmetric.test.ts (Plan-01 RED tests — both CR-02 and CR-03 cases),
    gui/src/lib/bcMode.ts (BCEdgeData shape if referenced; cycleBCEdgeTargetSide if needed)
  </read_first>
  <action>Step A — extract the helper. Inside useStore.ts, just above the BC actions section, declare a non-exported function: `function _reconcileEdgesForBCMode(edges: Edge[], nodes: Node[], consumerNodeId: string, externalInputName: string, prevEntry: BCModeEntry | undefined, newEntry: BCModeEntry | undefined, symmetric: boolean): Edge[] { ... }`. Body: when newEntry is `{mode:"source", sourceNodeId}` and prevEntry is anything else (or undefined), add an `bce-...` edge mirroring the lines 1187-1204 idiom (id with crypto.randomUUID().slice(0,8), source, sourceHandle (find the source-port name via the source component's BCPort port), target=consumerNodeId, targetHandle=externalInputName, type="bcEdge", data={componentId: consumerNodeId, externalInputName, targetSide: "both"} cast through `as unknown as Record<string, unknown>`). When newEntry is undefined (or mode≠"source") and prevEntry was `{mode:"source", sourceNodeId}`, remove the matching edge: filter edges where `e.type==="bcEdge" && e.target===consumerNodeId && e.targetHandle===externalInputName`. The `symmetric` arg is reserved for future use (sibling-edge reconciliation if we ever want to do both sides in one helper call); set it false at all current call-sites — the callers handle siblings explicitly. Return the new edges array. Step B — refactor setBCMode (lines ~1116-1218) to use the helper. Replace its inline edge-materialization block (~lines 1137-1208) with a call: `nextEdges = _reconcileEdgesForBCMode(state.edges, state.nodes, componentId, externalInputName, state.bcMode[key], entry, state.bcSymmetric[symKey] ?? true)`. If setBCMode also reconciles the sibling under symmetric mode, call the helper twice (once per side). Step C — refactor setBCSymmetric (~lines 1283-1303) per RESEARCH lines 679-713. Read prev state, compute symKey, leftKey, rightKey, leftEntry, rightEntry. When symmetric === true: branch on the four sub-cases. For (leftEntry === undefined && rightEntry !== undefined): build nextBCMode by spread + delete rightKey; call _reconcileEdgesForBCMode for the right side with newEntry=undefined to remove the dangling edge — this is CR-02. For (leftEntry !== undefined && leftEntry !== rightEntry): nextBCMode = {...state.bcMode, [rightKey]: leftEntry}; call _reconcileEdgesForBCMode for the right side with newEntry=leftEntry to add/remove the right edge — this is CR-03. For all other cases: no mode/edge change beyond setting bcSymmetric. Then `set({bcMode: nextBCMode, bcSymmetric: nextBCSymmetric, edges: nextEdges, isDirty: true})`. Add inline comments `// CR-02 (D-21)` and `// CR-03 (D-21)` for traceability. Step D — verify _checkBCNMismatch is NOT re-introduced; the n-mismatch surface is now derived through selectNodeErrors per Plan 02.</action>
  <verify>
    <automated>cd gui && grep -c "_reconcileEdgesForBCMode" src/store/useStore.ts; grep -c "CR-02" src/store/useStore.ts; grep -c "CR-03" src/store/useStore.ts; grep -c "_checkBCNMismatch" src/store/useStore.ts; npx vitest run src/store/__tests__/useStore.setBCSymmetric.test.ts 2>&1 | tail -25; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "_reconcileEdgesForBCMode" gui/src/store/useStore.ts` returns 3 or more (declaration + 2+ call sites: setBCMode, setBCSymmetric).
    `grep -c "CR-02" gui/src/store/useStore.ts` returns 1 or more.
    `grep -c "CR-03" gui/src/store/useStore.ts` returns 1 or more.
    `grep -c "_checkBCNMismatch" gui/src/store/useStore.ts` returns 0.
    `cd gui && npx vitest run src/store/__tests__/useStore.setBCSymmetric.test.ts` reports all GREEN (both CR-02 and CR-03 cases).
    `cd gui && npx vitest run src/store/__tests__/useStore.bc.test.ts` reports all GREEN (no regression to setBCMode behavior).
    `cd gui && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>setBCSymmetric handles both edge-holes through the shared helper; setBCMode refactored to use the same helper; tests green; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Click-vs-drag selection sync in onNodesChange (D-22); D-23 verification</name>
  <files>gui/src/store/useStore.ts</files>
  <behavior>
    - onNodesChange iterates over incoming NodeChange[] and, for every change where type === "select" && selected === true, calls get().selectNode(c.id) (no-op if the id is already the selectedNodeId)
    - Contentless-batch handling (the existing isContentless branch) is preserved; the select-sync loop runs BEFORE the early return so click-and-drag interactions get the selection update even though their batch is technically "contentless"
    - Plan 01's useStore.onNodesChange.select.test.ts flips to GREEN
    - D-23 auto-clearing of the n-mismatch red ring is already structurally satisfied by Plan 02 (selector recomputes on every render). Verify empirically by adding a quick assertion in the selector test or by manual smoke — no code change needed beyond confirming Plan 02 is in place
  </behavior>
  <read_first>
    gui/src/store/useStore.ts (lines 886-903 current onNodesChange; line ~935-941 existing selectNode action),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Click-vs-drag selection fix (D-22)" lines 716-744; Pitfall 6 lines 489-498 — root-cause documentation),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"useStore.ts — Click-vs-drag selection fix" lines 409-458),
    gui/src/store/__tests__/useStore.onNodesChange.select.test.ts (Plan-01 RED — this task flips it to GREEN)
  </read_first>
  <action>In onNodesChange (lines ~886-903), insert the select-sync block from RESEARCH lines 720-737 BEFORE the existing `if (isContentless) { set({...}); return }` branch. Body: `for (const c of changes) { if (c.type !== "select") continue; if (c.selected && get().selectedNodeId !== c.id) { get().selectNode(c.id); } }`. Add inline comment `// D-22: sync selectedNodeId from ReactFlow select events (fixes click-vs-drag stale Properties)`. Preserve the rest of the onNodesChange body verbatim — contentless branch, remove-handling snapshot push, and the final applyNodeChanges set. Do NOT touch selectNode action — it already handles resource selection clearing and selectionKind. Step B — D-23 verification: no code change. Add a short comment at the top of the selectNodeErrors selector module (gui/src/lib/selectors/nodeErrors.ts already exists from Plan 02): `// D-23: ring/error state auto-clears because this selector recomputes on every render after the underlying mismatch is corrected. No event handler needed.` Skip this if the comment was already added in Plan 02.</action>
  <verify>
    <automated>cd gui && grep -c "D-22" src/store/useStore.ts; grep -c "if (c.type !== \"select\")" src/store/useStore.ts; npx vitest run src/store/__tests__/useStore.onNodesChange.select.test.ts 2>&1 | tail -15; npx vitest run 2>&1 | tail -30; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "D-22" gui/src/store/useStore.ts` returns 1.
    `grep -c "c.type !== \"select\"" gui/src/store/useStore.ts` returns 1.
    `cd gui && npx vitest run src/store/__tests__/useStore.onNodesChange.select.test.ts` reports all GREEN.
    `cd gui && npx vitest run` (FULL SUITE) reports ZERO failures — every Plan 01 RED stub is now GREEN; no test is RED.
    `cd gui && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Click-vs-drag sync added; full vitest suite green for the first time since Plan 01; tsc green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1.10-01 | Tampering | addEdge BCPort write | accept | bcMode key derived from typed registry portField + caller-supplied target — no external input flows here. ASVS V5 N/A (in-process state). |
| T-63.1.10-02 | DoS | setBCSymmetric edge reconciliation | accept | Edge array filtered in-place; O(|edges|) per call; bounded by canvas size |
</threat_model>

<verification>
- All four bug fixes (CR-01, CR-02, CR-03, click-vs-drag) implemented and tested.
- _reconcileEdgesForBCMode helper deduplicates edge-add/remove logic across setBCMode and setBCSymmetric.
- Plan 01 RED stubs are ALL GREEN.
- Full GUI vitest suite is green for the first time end-to-end.
- tsc + vitest green.
</verification>

<success_criteria>
- D-20, D-21, D-22 all satisfied.
- D-23 verified structurally (Plan 02 already in place; this plan confirms no event-driven cleanup re-introduction).
- Phase 63.1 implementation complete; the cross-AI review and human UAT can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-10-SUMMARY.md`
</output>
