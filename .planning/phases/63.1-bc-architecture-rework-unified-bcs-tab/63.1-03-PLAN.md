---
phase: 63.1
plan: 03
type: execute
wave: 2
depends_on: ["63.1-02"]
files_modified:
  - gui/src/lib/anchors.ts
  - gui/src/store/useStore.ts
  - gui/src/lib/projectIO.ts
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04
  - D-14
requirements_addressed:
  - D-02
  - D-03
  - D-04
  - D-14
must_haves:
  truths:
    - "anchors: Record<nodeId, AnchorEntry> slice exists on the store, snapshot-undoable, with setAnchor / clearAnchor actions"
    - "bcs: BCEntry[] slice + addBC + removeBC are physically removed from useStore.ts"
    - "removeNode purges anchors[nodeId] (replaces the legacy bcs.filter cleanup — no orphan anchors after node delete)"
    - "Project save/load emits an anchors Record field and does NOT read legacy bcs[] (D-14 no-fallback)"
    - "AnchorEntry shape matches D-02 verbatim: { portField: 'port_in.P' | 'port_out.P', value: number }"
    - "D-01: AnchorEntry models a pressure anchor ONLY — portField is restricted to 'port_in.P' | 'port_out.P' so no other anchor type (gravity reference, transient initial condition) can be represented or persisted in this phase; gravity stays a per-component property via Gravity(H) (unchanged), transient ICs continue to source from steady_state_guess (unchanged)"
    - "D-03: the legacy bcs: BCEntry[] slice is physically removed (not emptied) from useStore.ts + CanvasSnapshot + projectIO + saveProject prep destructure; this plan plus Plan 05 (which deletes BCPanel.tsx / BCRow.tsx) jointly satisfy 'single source of truth, single rendering site'"
  artifacts:
    - path: "gui/src/lib/anchors.ts"
      provides: "AnchorEntry + CodegenAnchorsState types (pure module)"
      exports: ["AnchorEntry", "CodegenAnchorsState"]
    - path: "gui/src/store/useStore.ts"
      provides: "anchors slice + setAnchor / clearAnchor + CanvasSnapshot.anchors + undo plumbing + removeNode anchors cleanup"
      contains: "setAnchor:"
    - path: "gui/src/lib/projectIO.ts"
      provides: "StreamProject.anchors field; serializer + deserializer field swap"
      contains: "anchors"
  key_links:
    - from: "gui/src/store/useStore.ts setAnchor"
      to: "CanvasSnapshot.anchors via _pushSnapshot"
      via: "snapshot-before-mutate discipline (Pattern A)"
      pattern: "_pushSnapshot"
    - from: "gui/src/store/useStore.ts removeNode"
      to: "anchors[nodeId] deletion via spread+delete (Pattern B)"
      via: "co-located with node delete so anchors never outlive their host node"
      pattern: "removeNode"
    - from: "gui/src/lib/projectIO.ts serializeProject"
      to: "JSON anchors Record field on disk"
      via: "direct Record write (no Array conversion)"
      pattern: "anchors"
---

<objective>
Wave 2 part A — anchors slice + persistence. Introduce the new anchors: Record<nodeId, AnchorEntry> slice, its setAnchor / clearAnchor actions with snapshot-undoable discipline, and the .scp schema field swap. Physically delete the legacy bcs: BCEntry[] slice, addBC, removeBC, and remove bcs from CanvasSnapshot + projectIO. Per D-14, no legacy fallback — old .streamgui files break cleanly. Note: removeNode currently uses bcs.filter to drop entries when the host node is deleted; that responsibility migrates to an explicit anchors[nodeId] purge in this plan (replaces, not just removes, the cleanup).

Purpose: Replace the global anchor list with a per-node Record (D-02 at-most-one-per-node semantics). Decouple anchor storage from the legacy bottom-panel BCs surface so Plan 05 can delete BCPanel/BCRow cleanly. Decouple codegen/validation in Plan 04 from the bcs Array shape.

Output: New gui/src/lib/anchors.ts types module; useStore.ts with the new slice and the legacy slice removed; projectIO.ts schema-swapped.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-CONTEXT.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-02-SUMMARY.md
@gui/src/store/useStore.ts
@gui/src/lib/bcMode.ts
@gui/src/lib/projectIO.ts
@gui/src/lib/codeGenerator.ts

<interfaces>
New types module (per RESEARCH §"Anchors slice shape" lines 511-525 and PATTERNS.md §"gui/src/lib/anchors.ts (NEW)" lines 56-107).

Required exports from `gui/src/lib/anchors.ts`:
- `interface AnchorEntry { portField: "port_in.P" | "port_out.P"; value: number }`
- `interface CodegenAnchorsState { anchors: Record<string, AnchorEntry> }`

Required store changes (signatures locked):
- ADD on AppState: `anchors: Record<string, AnchorEntry>`, `setAnchor(nodeId, entry): void`, `clearAnchor(nodeId): void`
- DELETE from AppState: `bcs: BCEntry[]`, `addBC`, `removeBC`
- CanvasSnapshot: DELETE `bcs`, ADD `anchors`
- removeNode: REPLACE `bcs: bcs.filter((bc) => bc.nodeId !== nodeId)` with `anchors: <spread+delete of anchors[nodeId]>` (so anchors are cleaned up on node delete, matching the cleanup discipline the legacy bcs slice had)
- projectIO.StreamProject: DELETE `bcs`, ADD `anchors: Record<string, AnchorEntry>`

`promoteToSharedSource` is NOT in this plan — Plan 08 owns it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create gui/src/lib/anchors.ts pure module</name>
  <files>gui/src/lib/anchors.ts</files>
  <behavior>
    - AnchorEntry type has exactly two fields: portField (union of two string literals) and value (number)
    - CodegenAnchorsState has one field: anchors: Record<string, AnchorEntry>
    - Module is pure: no React, no zustand, no @xyflow runtime imports — only export interface declarations
  </behavior>
  <read_first>
    gui/src/lib/bcMode.ts (lines 1-13 module-header purity comment; lines 19-23 sentinel-by-absence comment; lines 29-43 BCMode/BCModeEntry discriminated-union shape — copy structural idiom),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Anchors slice shape" lines 511-525 — exact type body),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"gui/src/lib/anchors.ts (NEW)" lines 56-107)
  </read_first>
  <action>Create gui/src/lib/anchors.ts. Header comment mirrors bcMode.ts:1-13 listing consumers: useStore.ts (anchors slice), codeGenerator.ts (Plan 04 — anchors emission), projectIO.ts (this plan — schema), AnchorsSection.tsx (Plan 06 — UI), StreamNode.tsx (Plan 08 — canvas indicator). Add a Sentinel-by-absence paragraph: anchors[nodeId] === undefined means no anchor on that component; do NOT introduce a {kind:"unset"} sentinel. Declare the two interfaces verbatim from RESEARCH lines 514-524. No runtime code, no other exports.</action>
  <verify>
    <automated>cd gui && grep -c "export interface AnchorEntry" src/lib/anchors.ts; grep -c "export interface CodegenAnchorsState" src/lib/anchors.ts; npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    File gui/src/lib/anchors.ts exists.
    `grep -c "export interface AnchorEntry" gui/src/lib/anchors.ts` returns 1.
    `grep -c "export interface CodegenAnchorsState" gui/src/lib/anchors.ts` returns 1.
    `grep -c "portField" gui/src/lib/anchors.ts` returns 1 or more and the line contains both "port_in.P" and "port_out.P" literals.
    `grep -E "^import [^t]" gui/src/lib/anchors.ts | grep -v "^import type"` returns nothing (purity check — only type imports allowed; the file ideally has zero imports).
    `cd gui && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>anchors.ts created with AnchorEntry + CodegenAnchorsState; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add anchors slice + setAnchor/clearAnchor to useStore.ts; delete bcs/addBC/removeBC; migrate removeNode cleanup</name>
  <files>gui/src/store/useStore.ts</files>
  <behavior>
    - useStore.getState().anchors exists and defaults to {}
    - setAnchor(nodeId, entry) writes anchors[nodeId] = entry and pushes a snapshot
    - setAnchor called twice on the same nodeId overwrites (D-02 at-most-one)
    - clearAnchor(nodeId) deletes the entry via spread + delete (Pattern B)
    - removeNode in useStore.ts deletes anchors[nodeId] when present (no orphan anchors after node delete); verifiable by `setAnchor(id, …)` → `removeNode(id)` → `useStore.getState().anchors[id] === undefined`
    - bcs, addBC, removeBC no longer exist on AppState
    - BCEntry import from codeGenerator is no longer used by the store
    - CanvasSnapshot has no bcs field and has an anchors field
    - _pushSnapshot, undo, redo all copy anchors along the same lines they previously copied bcs
  </behavior>
  <read_first>
    gui/src/store/useStore.ts (lines 1-30 imports; lines 132-143 CanvasSnapshot; lines 656 initial bcs: [] line; lines 797-845 _pushSnapshot + undo + redo handling; lines 998-1012 removeNode — note the `const { ..., bcs, ... } = get()` destructure on line 1000 and the `bcs: bcs.filter((bc) => bc.nodeId !== nodeId)` line at 1007 — both must go, replaced by anchors cleanup; lines 1098-1106 current addBC/removeBC; lines 1640 and 1965 for the other `bcs` destructure / initial-state-clear references uncovered by grep),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Anchors slice shape" lines 526-567; Pitfall 4 lines 475-481 undo discipline; Pitfall 5 lines 483-487 snapshot stack handling),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"useStore.ts — anchors slice + actions" lines 168-258; Pattern A lines 950-968; Pattern B lines 972-989),
    gui/src/store/__tests__/useStore.anchors.test.ts (Plan-01 RED tests — this task flips them to GREEN)
  </read_first>
  <action>Step A — imports near the @/lib/bcMode import, add: import type { AnchorEntry } from "@/lib/anchors". Remove the BCEntry import from ../lib/codeGenerator (codeGenerator.ts still exports BCEntry until Plan 04 retires it; useStore.ts simply no longer imports it). Step B — AppState interface: ADD anchors: Record<string, AnchorEntry>, setAnchor and clearAnchor with the signatures above. REMOVE bcs, addBC, removeBC. Step C — CanvasSnapshot: REMOVE bcs field. ADD anchors: Record<string, AnchorEntry>. Update the snapshot header comment at lines 132-138 to drop the legacy bcs reference and note anchors as the new undoable slice per D-02. Step D — initial state: REMOVE the `bcs: []` line at ~656 AND the `bcs: []` line at ~1965 (the reset-on-newProject site). ADD `anchors: {}` in both regions. Step E — action bodies: DELETE the addBC and removeBC bodies. ADD setAnchor and clearAnchor per PATTERNS lines 244-258. setAnchor calls get()._pushSnapshot() then set({ anchors: { ...get().anchors, [nodeId]: entry }, isDirty: true }). clearAnchor does the immutable-delete idiom (const next = { ...get().anchors }; delete next[nodeId]; set({ anchors: next, isDirty: true })). Step F — snapshot plumbing: in _pushSnapshot (line ~799) REMOVE the bcs: prev.bcs line and ADD anchors: prev.anchors. In undo and redo (line ~843) REMOVE the bcs read and ADD anchors: next.anchors. Step G — removeNode (lines 998-1012): in the destructure on line 1000, remove `bcs` from the destructured names and ADD `anchors`. Remove the `bcs: bcs.filter((bc) => bc.nodeId !== nodeId)` line at 1007 and REPLACE it with an immutable spread+delete that purges the host node's anchor: `const nextAnchors = { ...anchors }; delete nextAnchors[nodeId];` declared just before the set({...}) call, and pass `anchors: nextAnchors` inside the set. Rationale: the legacy bcs cleanup ensured no orphan BC entries survived after node deletion; the anchors slice owns the same per-node lifetime now, so the cleanup migrates here. Mirror the clearAnchor idiom (Pattern B) rather than inventing a new pattern. Step H — final grep sweep inside useStore.ts: zero remaining references to the legacy bcs identifier anywhere in the file. The bcs destructure at line 1640 (used by saveProject prep) must be replaced by `anchors` from get() — search by `const { nodes, edges, bcs } = get()` and rewrite. Sanity-check by running the Step-I grep below and confirming zero matches before moving on.</action>
  <verify>
    <automated>cd gui && grep -nE "\\bbcs\\b" src/store/useStore.ts; grep -cE "anchors:|setAnchor|clearAnchor" src/store/useStore.ts; npx vitest run src/store/__tests__/useStore.anchors.test.ts 2>&1 | tail -15; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    `grep -cE "\\b(addBC|removeBC)\\b" gui/src/store/useStore.ts` returns 0.
    `grep -cE "\\bbcs\\b" gui/src/store/useStore.ts` returns 0 (PRIMARY enumerable check — this single grep is the primary backstop; do NOT substitute narrower patterns like `bcs: \[\]` or `bcs: prev\.` which silently miss destructures such as `const { ..., bcs, ... } = get();` and the `bcs.filter(...)` call inside `removeNode`). Chosen comment-rule for this plan: NO explanatory comment may contain the bare token `bcs`. If a comment is desired, write `// Phase 63.1: legacy boundary-conditions slice removed` (note the long-form `boundary-conditions` — no `bcs` substring), keeping the grep at exactly 0. The tsc gate is a second line of defence but cannot replace this grep — tsc passes even when stray `bcs` identifiers appear inside untyped destructuring patterns or comments.
    `grep -c "setAnchor:" gui/src/store/useStore.ts` returns 1.
    `grep -c "clearAnchor:" gui/src/store/useStore.ts` returns 1.
    `grep -c "anchors: {}" gui/src/store/useStore.ts` returns 2 or more (initial state at ~656 and reset-on-newProject at ~1965).
    `grep -A 12 "removeNode:" gui/src/store/useStore.ts | grep -c "anchors"` returns 1 or more (removeNode purges anchors[nodeId]).
    `removeNode` in `gui/src/store/useStore.ts` MUST contain an immutable spread+delete (or equivalent omission) that removes the deleted node's key from the `anchors` Record inside the same `set({...})` call. Deterministic vitest gate: `gui/src/store/__tests__/useStore.anchors.test.ts` MUST contain (add it as part of this task if Plan 01 missed it) a test that does: `useStore.getState().setAnchor("n1", { portField: "port_in.P", value: 1.5 })` then `useStore.getState().removeNode("n1")` then asserts `useStore.getState().anchors["n1"] === undefined`. This replaces the legacy behavior in which `removeNode` previously called `bcs.filter((bc) => bc.nodeId !== nodeId)` to drop entries for the deleted node — that line is gone, the equivalent cleanup MUST exist for `anchors[nodeId]` or anchor entries will accumulate as silent orphan data on every node delete.
    `cd gui && npx vitest run src/store/__tests__/useStore.anchors.test.ts` reports all GREEN. The Plan-01 RED suite must include a `setAnchor → removeNode → anchors[id] === undefined` assertion; if Plan 01 missed it, add the assertion to useStore.anchors.test.ts as part of this task (RED-then-GREEN in one commit is acceptable here since the test is a single line and the implementation is in the same task).
    `cd gui && npx tsc --noEmit` exits 0 — though if codeGenerator.ts's bcs param is still typed BCEntry[] AND no caller still passes a BCEntry[] from the store, there should be no error here. (Plan 04 changes the signature.)
  </acceptance_criteria>
  <done>anchors slice live on the store; setAnchor/clearAnchor working under undo discipline; removeNode purges anchors[nodeId]; bcs/addBC/removeBC physically removed; Plan-01 anchors tests GREEN; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: projectIO.ts schema swap (bcs Array → anchors Record); D-14 no-fallback</name>
  <files>gui/src/lib/projectIO.ts</files>
  <behavior>
    - StreamProject.anchors: Record<string, AnchorEntry> replaces StreamProject.bcs: BCEntry[]
    - serializeProject writes the anchors Record verbatim (no Array conversion)
    - deserializeProject reads anchors and defaults to {} if absent
    - deserializeProject does NOT read a legacy bcs field (D-14) — if both are present in input JSON, anchors wins and bcs is silently ignored
    - All test fixtures referencing bcs: [] across the gui/src tree are updated to anchors: {} (full file set enumerated by Step F grep)
  </behavior>
  <read_first>
    gui/src/lib/projectIO.ts (lines 44-59 StreamProject type; lines 86-98 SerializeProjectArgs; lines 115-138 serializer body; lines 162-208 deserializer body; PATTERNS lines 547-575 for the swap shape),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"projectIO.ts — schema field swap" lines 545-575; A1 in §Assumptions Log lines 967-968 — persistence is correct path; D-14 no-fallback rule),
    gui/src/lib/__tests__/projectIO.anchors.test.ts (Plan-01 RED test for D-14 — this task flips it to GREEN),
    gui/src/lib/__tests__/projectIO.scp.test.ts (existing roundtrip test; fixture updates required)
  </read_first>
  <action>Step A — StreamProject type at lines 44-59: REMOVE bcs: BCEntry[]. ADD anchors: Record<string, AnchorEntry>. Add import: import type { AnchorEntry } from "./anchors". Remove BCEntry import from ./codeGenerator if no longer used. Step B — SerializeProjectArgs (line 86-98): REMOVE bcs. ADD anchors: Record<string, AnchorEntry>. Step C — serializer body: REMOVE the bcs Array emission. ADD a direct anchors Record write — assignment level on the output object (no transformation; keys are nodeIds, value is AnchorEntry). Step D — deserializer body (line 197 area): REMOVE the `const bcs = (parsed.bcs as BCEntry[]) ?? []` line. ADD `const anchors = (parsed.anchors as Record<string, AnchorEntry>) ?? {}`. Per D-14, do NOT add a fallback that reads parsed.bcs and converts it; if a legacy file has bcs[] and no anchors, the result is anchors = {} (the user's pressure anchors are dropped — they re-set them). Document this in a short comment block above the deserializer: `// D-14: no legacy bcs[] fallback. Old .streamgui files lose their anchor data on load — accepted breakage.` Step E — update all callers of serialize/deserialize: in useStore.ts saveProject / loadProject (search for SerializeProjectArgs construction and the deserialize side), pass anchors: get().anchors / state.anchors and remove bcs: get().bcs / state.bcs. Step F — fixture sweep across the full gui/src tree: run `grep -lrE "bcs:\\s*\\[\\]" gui/src` BEFORE starting and capture the file list; the expected set at the time this plan was written is the 21 paths returned by that grep (20 test files + useStore.ts itself, which Task 2 already handled). For each remaining test file, replace `bcs: []` with `anchors: {}` in every fake-StreamProject and useStore.setState fixture. Do NOT just delete the line — the field must be present (matching the new StreamProject shape) so the fixture validates against tsc. Do not touch the Plan-01 projectIO.anchors.test.ts beyond verifying it now passes.</action>
  <verify>
    <automated>cd gui && grep -nE "\\bbcs\\b" src/lib/projectIO.ts; grep -c "anchors" src/lib/projectIO.ts; grep -lrE "bcs:\\s*\\[\\]" src 2>/dev/null; npx vitest run src/lib/__tests__/projectIO.anchors.test.ts src/lib/__tests__/projectIO.scp.test.ts 2>&1 | tail -20; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    `grep -cE "\\bbcs\\b" gui/src/lib/projectIO.ts` returns 0 OR returns 1 if the only match is the D-14 explanatory comment.
    `grep -c "anchors" gui/src/lib/projectIO.ts` returns 4 or more (type, serialize, deserialize, comment).
    `grep -c "BCEntry" gui/src/lib/projectIO.ts` returns 0.
    `grep -lrE "bcs:\\s*\\[\\]" gui/src` returns ZERO files (Option A — primary enumerable check for full test-tree coverage; this single grep replaces any hardcoded list of test files). The plan does NOT hardcode a fixture-file enumeration because the actual set is determined by running the grep at task start, not by a list captured at planning time (a hardcoded list goes stale). Implementor MUST run `grep -lrE "bcs:\\s*\\[\\]" gui/src` BEFORE starting Step F, capture the file list as the work queue, and re-run after the sweep to confirm zero output. Per a planning-time grep there are 16+ candidate test files, but the implementor MUST trust the live grep, not this prose count.
    `cd gui && npx vitest run src/lib/__tests__/projectIO.anchors.test.ts` reports all GREEN.
    `cd gui && npx vitest run src/lib/__tests__/projectIO.scp.test.ts` reports all GREEN.
    `cd gui && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>projectIO.ts schema-swapped; D-14 no-fallback enforced (explicit comment); fixture sweep across gui/src removes all `bcs: []` occurrences; roundtrip test passes; tsc green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1.03-01 | Tampering | .scp file deserialization | accept | parsed.anchors is type-asserted via `as Record<string, AnchorEntry>` without validation; consistent with existing parsed.bcs handling. Pressure-value validation is enforced at the editor (validateReal in NumericField, Plan 06) not at parse time. ASVS V5 applies trivially (input from local filesystem under user control). |
| T-63.1.03-02 | Information Disclosure | undo snapshot of anchors | N/A — in-memory only; not persisted beyond .scp save, which the user controls |
</threat_model>

<verification>
- anchors.ts pure module exists.
- useStore.ts has the anchors slice live, with snapshot discipline, and bcs/addBC/removeBC are physically gone.
- removeNode purges anchors[nodeId] (replaces the legacy bcs.filter cleanup at line 1007 of the pre-change file).
- projectIO.ts schema-swapped; D-14 comment present.
- Plan-01 RED tests (useStore.anchors.test.ts, projectIO.anchors.test.ts) flip to GREEN.
- Existing tests touching the bcs/anchors region (projectIO.scp.test.ts and any store-level fixture tests) are updated; full vitest suite (except still-RED Plan-01 stubs awaiting Plans 04+) is GREEN.
- tsc --noEmit exits 0.
</verification>

<success_criteria>
- AnchorEntry / CodegenAnchorsState types declared in the pure anchors.ts module.
- Store exposes setAnchor / clearAnchor with at-most-one-per-node semantics and full undo discipline.
- removeNode no longer leaves orphan anchors[nodeId] entries when its host node is deleted.
- Persistence emits the anchors Record on save and reads it on load; no legacy fallback (D-14).
- Codegen and validation modules will be updated in Plan 04 — they currently still take a bcs param; after this plan, no one passes bcs to them, but their signature still exists. Plan 04 retires the signature.
</success_criteria>

<output>
After completion, create `.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-03-SUMMARY.md`
</output>
