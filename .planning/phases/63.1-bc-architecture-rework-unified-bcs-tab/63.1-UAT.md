---
status: partial
phase: 63.1-bc-architecture-rework-unified-bcs-tab
source: 63.1-01..10-SUMMARY.md (all 10 plans)
started: 2026-05-14T00:00:00Z
updated: 2026-05-14T00:30:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing paused — 1 blocked item outstanding (test 6, gated on WT/HFS drag-connect fix)]

## Tests

### 1. Surface shape — bottom panel + toolbox + BCs-tab visibility
expected: |
  (a) Bottom panel has a single "Code" tab (no "BCs" tab).
  (b) Toolbox has NO "Sources" category, no WallTemperature/HeatFluxSource drag rows.
  (c) Pump → BCs tab visible in right sidebar; WallTemperature → BCs tab absent.
result: pass
note: "WallTemperature is no longer reachable from the toolbox — confirmed by (b); negative case in (c) verified by absence of the source from the palette."

### 2. Anchors — empty → populated → edit → canvas dot → clear
expected: |
  Select the Pump. Open BCs tab. "Anchors" section shows "No anchor set" + outline "+ Add anchor" button.
  Click "+ Add anchor" → form appears (Port dropdown with port_in.P/port_out.P, Pressure numeric input default 0 Pa, "Clear anchor" ghost button).
  Set Pressure = 100000, leave port_in.P → small dark dot (~6px) appears next to Pump's inlet handle on canvas.
  Switch Port to port_out.P → dot moves to outlet handle.
  Click "Clear anchor" → section returns to empty state, dot disappears.
result: issue
reported: "The dot exists and all of that works, But I don't like the dot icon. Can we have an actual anchor icon there instead?"
severity: cosmetic

### 3. Codegen + .scp round-trip (anchors as Record, no legacy bcs)
expected: |
  Set Pump anchor: port_in.P = 100000. Open bottom Code tab — Julia output contains a line like
  `{pumpName}.port_in.P ~ 100000.0,` (no `bcs` array, no legacy form).
  Save the project (.scp). Inspect the saved JSON — top-level has `"anchors": { "<id>": { "portField": "port_in.P", "value": 100000 } }` and NO `"bcs"` key.
  Reload the saved file → Pump is still anchored, value preserved, canvas indicator present.
result: pass

### 4. Channel BCs tab — Anchors + External Inputs, mode dropdown, segmented control
expected: |
  Drop a Channel. Open its BCs tab. Two sections stacked:
    "Anchors" section (empty state OK).
    "External Inputs" section below a separator.
  Inside External Inputs, the BC mode picker is a single inline dropdown (NOT 5 pills). Click it → five options: Value, Profile, Function, Mark, Source.
  Above the mode rows, the Symmetric/Asymmetric toggle is a labeled segmented control with two pills: "Symmetric" (active) and "Asymmetric". Click Asymmetric → row expands into two stacked dropdowns (left + right).
  For an unset mode dropdown, the destructive hint "BC required — select a mode" renders below it.
result: issue
reported: "Source should be disabled unless a source exists in the project no?"
severity: minor

### 5. Promote-to-shared-source — spawn + bind + hide-after
expected: |
  On the Channel's External Inputs (Symmetric ON, T_wall row mode = unset or Value), a ghost "↗ Promote to shared source" button sits next to the mode dropdown.
  Click it → a new WallTemperature node spawns to the upper-left of the Channel (~160 left, ~40 up); a dashed BC edge connects WT → Channel's T_wall handle; the T_wall mode dropdown flips to "Source" (the new WT selected); the new WT's `n` parameter equals the Channel's `n`.
  After promotion, the Promote button is HIDDEN (not disabled) on that row.
result: issue
reported: "That all works, but the button itself is kind of ugly, and I just now notice that Wall Temperature (and probably the heatflux one too) is broken. Its properties only contain 'n', no other. And connecting it doesn't actually work. You can't drag it onto anything. If we plan for them to exist at some point in the canvas, they have to work properly. They also alert that T_wall is unset."
severity: blocker
note: "Locked-in Promote flow (spawn + bind + hide-after) works. Surfaced four separate defects in the spawned source nodes themselves — see Gaps."

### 6. Canvas-drag BCPort persistence (CR-01)
expected: |
  You need a WallTemperature node on the canvas; since it's hidden from the toolbox, spawn one via Promote on a Channel (then unbind / drop a second Channel). Drag manually from the WT's BCPort to a different Channel's T_wall handle.
  Dashed BC edge appears AND that Channel's T_wall BC mode in the BCs-tab dropdown shows "Source" with the WT selected. State stays consistent across re-selecting the Channel.
result: blocked
blocked_by: prior-phase
reason: "WallTemperature BCPort drag-connect is broken (test 5 finding) — cannot verify CR-01's manual canvas-drag path until WT/HFS drag-connect is fixed. Note: programmatic edge creation via Promote did work in test 5, so the CR-01 store branch is partially exercised, but the user-visible drag test is unreachable."

### 7. setBCSymmetric edge holes — CR-02 + CR-03
expected: |
  On a Channel, toggle T_wall to Asymmetric. Two sub-rows appear (left, right).
  CR-02: Leave T_wall_left mode UNDEFINED; set T_wall_right to Source mode pointing to some WT. Toggle Symmetric back ON → the right-side source binding is cleared AND the dashed edge to T_wall_right is removed.
  CR-03: Re-do Asymmetric. Set T_wall_left to Source mode (some WT). Set T_wall_right different (Value or undefined). Toggle Symmetric ON → T_wall_right mirrors left (Source pointing to same WT), and a dashed edge to T_wall_right is materialized.
result: pass
note: "CR-02 + CR-03 edge-hole logic works as specified. Underlying WT defects (test 5 gaps) persist but user flagged as 'not important' for this gate."

### 8. Click-vs-drag selection sync + n-mismatch auto-clear
expected: |
  (D-22) Click a node → Properties panel updates. Drag-select a different node (marquee or click-and-drag) → Properties panel updates to the newly selected node (no stale Properties for the old node).
  (D-23) Promote a WT from a Channel with n=10 (so n matches). Change the Channel's n to 12 → red error indicator (bc-n-mismatch ring) appears on Channel and/or WT. Set the WT's n to 12 → red ring disappears immediately, no refresh needed.
result: pass

## Summary

total: 8
passed: 4
issues: 3
pending: 0
skipped: 0
blocked: 1

## Gaps

- truth: "Canvas anchor indicator uses a recognizable anchor glyph (not a generic dot)"
  status: failed
  reason: "User reported: The dot exists and all of that works, But I don't like the dot icon. Can we have an actual anchor icon there instead?"
  severity: cosmetic
  test: 2
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "BC mode dropdown disables the 'Source' option when no source nodes exist in the project"
  status: failed
  reason: "User reported: Source should be disabled unless a source exists in the project no?"
  severity: minor
  test: 4
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Promote-to-shared-source button has acceptable visual design"
  status: failed
  reason: "User reported: the button itself is kind of ugly"
  severity: cosmetic
  test: 5
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "WallTemperature node Properties expose all editable parameters (T value, etc.), not only 'n'"
  status: failed
  reason: "User reported: Wall Temperature is broken. Its properties only contain 'n', no other."
  severity: blocker
  test: 5
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "WallTemperature node BCPort can be dragged onto consumer BCPort handles to materialize a BC edge"
  status: failed
  reason: "User reported: connecting it doesn't actually work. You can't drag it onto anything."
  severity: blocker
  test: 5
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Channel does not show a spurious 'T_wall unset' alert when T_wall is already bound to a WallTemperature via Promote/source mode"
  status: failed
  reason: "User reported: They also alert that T_wall is unset."
  severity: major
  test: 5
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "HeatFluxSource node works equivalently to WallTemperature (Properties + drag-connect)"
  status: failed
  reason: "User reported: (and probably the heatflux one too) is broken"
  severity: blocker
  test: 5
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""
