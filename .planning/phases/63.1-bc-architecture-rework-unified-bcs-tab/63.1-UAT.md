---
status: diagnosed
phase: 63.1-bc-architecture-rework-unified-bcs-tab
source: 63.1-01..10-SUMMARY.md (all 10 plans)
started: 2026-05-14T00:00:00Z
updated: 2026-05-14T00:30:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing paused — 1 blocked item outstanding (test 6, gated on WT/HFS drag-connect fix)]

## Tests

### 1. Surface shape — bottom panel + toolbox + BCs-tab visibility
expected: |
  (a) Bottom panel has a single "Code" tab (no "BCs" tab).
  (b) Toolbox has NO "Sources" category, no WallTemperature/HeatFluxSource drag rows.
  (c) Pump → BCs tab visible in right sidebar; WallTemperature → BCs tab absent.
result: pass
note: "WallTemperature is no longer reachable from the toolbox — confirmed by (b); negative case in (c) verified by absence of the source from the palette."

### 2. Anchors — empty → populated → edit → canvas dot → clear
expected: |
  Select the Pump. Open BCs tab. "Anchors" section shows "No anchor set" + outline "+ Add anchor" button.
  Click "+ Add anchor" → form appears (Port dropdown with port_in.P/port_out.P, Pressure numeric input default 0 Pa, "Clear anchor" ghost button).
  Set Pressure = 100000, leave port_in.P → small dark dot (~6px) appears next to Pump's inlet handle on canvas.
  Switch Port to port_out.P → dot moves to outlet handle.
  Click "Clear anchor" → section returns to empty state, dot disappears.
result: issue
reported: "The dot exists and all of that works, But I don't like the dot icon. Can we have an actual anchor icon there instead?"
severity: cosmetic

### 3. Codegen + .scp round-trip (anchors as Record, no legacy bcs)
expected: |
  Set Pump anchor: port_in.P = 100000. Open bottom Code tab — Julia output contains a line like
  `{pumpName}.port_in.P ~ 100000.0,` (no `bcs` array, no legacy form).
  Save the project (.scp). Inspect the saved JSON — top-level has `"anchors": { "<id>": { "portField": "port_in.P", "value": 100000 } }` and NO `"bcs"` key.
  Reload the saved file → Pump is still anchored, value preserved, canvas indicator present.
result: pass

### 4. Channel BCs tab — Anchors + External Inputs, mode dropdown, segmented control
expected: |
  Drop a Channel. Open its BCs tab. Two sections stacked:
    "Anchors" section (empty state OK).
    "External Inputs" section below a separator.
  Inside External Inputs, the BC mode picker is a single inline dropdown (NOT 5 pills). Click it → five options: Value, Profile, Function, Mark, Source.
  Above the mode rows, the Symmetric/Asymmetric toggle is a labeled segmented control with two pills: "Symmetric" (active) and "Asymmetric". Click Asymmetric → row expands into two stacked dropdowns (left + right).
  For an unset mode dropdown, the destructive hint "BC required — select a mode" renders below it.
result: issue
reported: "Source should be disabled unless a source exists in the project no?"
severity: minor

### 5. Promote-to-shared-source — spawn + bind + hide-after
expected: |
  On the Channel's External Inputs (Symmetric ON, T_wall row mode = unset or Value), a ghost "↗ Promote to shared source" button sits next to the mode dropdown.
  Click it → a new WallTemperature node spawns to the upper-left of the Channel (~160 left, ~40 up); a dashed BC edge connects WT → Channel's T_wall handle; the T_wall mode dropdown flips to "Source" (the new WT selected); the new WT's `n` parameter equals the Channel's `n`.
  After promotion, the Promote button is HIDDEN (not disabled) on that row.
result: issue
reported: "That all works, but the button itself is kind of ugly, and I just now notice that Wall Temperature (and probably the heatflux one too) is broken. Its properties only contain 'n', no other. And connecting it doesn't actually work. You can't drag it onto anything. If we plan for them to exist at some point in the canvas, they have to work properly. They also alert that T_wall is unset."
severity: blocker
note: "Locked-in Promote flow (spawn + bind + hide-after) works. Surfaced four separate defects in the spawned source nodes themselves — see Gaps."

### 6. Canvas-drag BCPort persistence (CR-01)
expected: |
  You need a WallTemperature node on the canvas; since it's hidden from the toolbox, spawn one via Promote on a Channel (then unbind / drop a second Channel). Drag manually from the WT's BCPort to a different Channel's T_wall handle.
  Dashed BC edge appears AND that Channel's T_wall BC mode in the BCs-tab dropdown shows "Source" with the WT selected. State stays consistent across re-selecting the Channel.
result: blocked
blocked_by: prior-phase
reason: "WallTemperature BCPort drag-connect is broken (test 5 finding) — cannot verify CR-01's manual canvas-drag path until WT/HFS drag-connect is fixed. Note: programmatic edge creation via Promote did work in test 5, so the CR-01 store branch is partially exercised, but the user-visible drag test is unreachable."

### 7. setBCSymmetric edge holes — CR-02 + CR-03
expected: |
  On a Channel, toggle T_wall to Asymmetric. Two sub-rows appear (left, right).
  CR-02: Leave T_wall_left mode UNDEFINED; set T_wall_right to Source mode pointing to some WT. Toggle Symmetric back ON → the right-side source binding is cleared AND the dashed edge to T_wall_right is removed.
  CR-03: Re-do Asymmetric. Set T_wall_left to Source mode (some WT). Set T_wall_right different (Value or undefined). Toggle Symmetric ON → T_wall_right mirrors left (Source pointing to same WT), and a dashed edge to T_wall_right is materialized.
result: pass
note: "CR-02 + CR-03 edge-hole logic works as specified. Underlying WT defects (test 5 gaps) persist but user flagged as 'not important' for this gate."

### 8. Click-vs-drag selection sync + n-mismatch auto-clear
expected: |
  (D-22) Click a node → Properties panel updates. Drag-select a different node (marquee or click-and-drag) → Properties panel updates to the newly selected node (no stale Properties for the old node).
  (D-23) Promote a WT from a Channel with n=10 (so n matches). Change the Channel's n to 12 → red error indicator (bc-n-mismatch ring) appears on Channel and/or WT. Set the WT's n to 12 → red ring disappears immediately, no refresh needed.
result: pass

## Summary

total: 8
passed: 4
issues: 3
pending: 0
skipped: 0
blocked: 1

## Gaps

- truth: "Canvas anchor indicator uses a recognizable anchor glyph (not a generic dot)"
  status: failed
  reason: "User reported: The dot exists and all of that works, But I don't like the dot icon. Can we have an actual anchor icon there instead?"
  severity: cosmetic
  test: 2
  root_cause: "Design choice — Plan 09 ships a 6×6 px filled circle via Tailwind classes (w-1.5 h-1.5 rounded-full bg-foreground). No bug; user wants a recognizable anchor glyph instead."
  artifacts:
    - path: "gui/src/components/StreamNode.tsx"
      issue: "FlowPortHandle renders a generic dot when anchors[nodeId]?.portField === portFieldKey"
  missing:
    - "Replace the rounded-full bg-foreground span with a lucide-react Anchor icon (or equivalent) at the same absolute-positioned offset; keep the data-testid='anchor-indicator' and aria-label='Pressure anchor'"
  debug_session: ""

- truth: "BC mode dropdown disables the 'Source' option when no source nodes exist in the project"
  status: failed
  reason: "User reported: Source should be disabled unless a source exists in the project no?"
  severity: minor
  test: 4
  root_cause: "Design choice — BCsTabForm.tsx renders all five SelectItems unconditionally; Plan 07 documented selecting Source with no sources existing as an 'acceptable rare path' that lands users in an empty source-list dropdown. User correctly identifies this as a dead end."
  artifacts:
    - path: "gui/src/components/sidebar/BCsTabForm.tsx"
      issue: "Mode SelectItem 'source' rendered unconditionally — no gate on registry source-component-count or canvas source-node-count"
  missing:
    - "Derive `hasAnySourceNode` selector: `state.nodes.some(n => isSourceCategoryComponent(n.data.componentId))` (or similar)"
    - "Pass disabled={!hasAnySourceNode} to the Source SelectItem so users see it greyed out when the project has no source nodes; tooltip 'Add a source first (use Promote ↗ to spawn one)' is optional but nice"
  debug_session: ""

- truth: "Promote-to-shared-source button has acceptable visual design"
  status: failed
  reason: "User reported: the button itself is kind of ugly"
  severity: cosmetic
  test: 5
  root_cause: "Design choice — Plan 08 implemented Promote as a shadcn ghost variant Button with the literal '↗ Promote to shared source'. The arrow + long label + ghost styling read as ugly in context next to the larger Mode Select trigger."
  artifacts:
    - path: "gui/src/components/sidebar/BCsTabForm.tsx"
      issue: "Promote button rendered as <Button variant='ghost' size='sm'>↗ Promote to shared source</Button>"
  missing:
    - "Redesign options for the planner to consider: (a) icon-only button with tooltip ('Promote', up-right-arrow lucide icon), (b) split-icon-plus-text variant aligned to the Mode Select's height, (c) move into the Mode Select as a separate 'Promote new source...' SelectItem footer entry"
    - "Locked-in behavior preserved: visible when entry.mode !== 'source' && source_component defined; hidden after promotion"
  debug_session: ".planning/debug/wt-hfs-source-component-defects.md"

- truth: "WallTemperature node Properties expose all editable parameters (T_wall value, not only 'n')"
  status: failed
  reason: "User reported: Wall Temperature is broken. Its properties only contain 'n', no other."
  severity: blocker
  test: 5
  root_cause: "RC-1: gui/src/components/sidebar/ParameterForm.tsx lines 36-44 group visibleParams by param.type only (Int|Real|Bool / PipeGeometry / Function / Matrix). renderField at line 46 switches on param.type exclusively. WT's T_wall is declared with type_union: ['Real','Vector','Function'] and no `type`, so it falls into none of the four groups and is silently dropped. Only `n` (type: 'Int') renders. Channel's h_left/h_right are silently broken the same way."
  artifacts:
    - path: "gui/src/components/sidebar/ParameterForm.tsx"
      issue: "Lines 36-44 group-by-type filter drops type_union params; lines 46-136 renderField switch has no type_union case"
    - path: "gui/src/registry/components.json"
      issue: "WT T_wall (lines ~1023-1040), HFS q (lines ~1050-1082), Channel h_left/h_right (lines 39-57) all use type_union — all silently dropped from Properties"
    - path: "gui/src/registry/types.ts"
      issue: "Lines 119-145 declare the Parameter contract as type XOR type_union + input_modes — only the type branch is implemented"
  missing:
    - "Add a type_union branch to ParameterForm: render an input_modes mode picker (scalar / vector / callable) and a per-mode editor (NumericField for scalar, vector input for vector, FunctionSelect/signature picker for callable)"
    - "Group type_union params alongside scalarParams in the existing layout (they read as scalar-input-with-mode-toggle)"
    - "Repair carries Channel.h_left/h_right and HFS.q automatically — same root cause, no per-symbol work"
  debug_session: ".planning/debug/wt-hfs-source-component-defects.md"

- truth: "WallTemperature node BCPort can be dragged onto consumer BCPort handles to materialize a BC edge"
  status: failed
  reason: "User reported: connecting it doesn't actually work. You can't drag it onto anything."
  severity: blocker
  test: 5
  root_cause: "RC-2: Channel and ChannelHeatFlux declare no BCPort in registry ports[] — only port_in/port_out FlowPorts. external_inputs[] is BCs-tab metadata, NOT a ReactFlow handle. WT renders T_wall_out as a BCPort source handle (type='source') but there is no matching type='target' BCPort handle on Channel/CHF for ReactFlow to land the drag on. The whole-body 'Connect BC' overlay at StreamNode.tsx:253-262 is decoy — pointer-events-none. Programmatic edge creation via promoteToSharedSource works because it bypasses ReactFlow entirely."
  artifacts:
    - path: "gui/src/registry/components.json"
      issue: "Channel.ports[] (~lines 10-13) and ChannelHeatFlux.ports[] (~lines 569-572) have no BCPort target handle"
    - path: "gui/src/components/StreamNode.tsx"
      issue: "Lines 253-262 render a decoy 'Connect BC' overlay with pointer-events-none; lines 302-317 bcPorts.map renders only type='source' handles"
    - path: "gui/src/lib/bcMode.ts"
      issue: "Lines 94-110 isAllowedBCConnection would already accept the connection — gate is purely the missing target handle"
  missing:
    - "Option (a, recommended): add a BCPort target handle entry to Channel.ports[] and ChannelHeatFlux.ports[] in the registry (one per external_input — e.g. T_wall_target, q_target) and render them as type='target' in StreamNode's bcPorts.map; align with the symmetric pair semantics by emitting *_left + *_right handles when external_input.symmetric"
    - "Pick a position (likely top/bottom edge or a small dedicated side) and an offset that doesn't collide with FlowPort handles"
    - "addEdge BCPort branch (CR-01 fix, Plan 10) already writes bcMode entries when targetHandle is truthy — once target handles exist, the drag path automatically lights up"
    - "Option (b, fallback): drop pointer-events-none on the body overlay and intercept drop event there → setBCMode dispatch. Smaller surface but breaks ReactFlow handle-to-handle model"
  debug_session: ".planning/debug/wt-hfs-source-component-defects.md"

- truth: "Promoted source nodes do not show a destructive '(unset)' label on their body before the user has had a chance to edit the value"
  status: failed
  reason: "User reported: They also alert that T_wall is unset."
  severity: major
  test: 5
  root_cause: "RC-3: The 'alert' is the WT node body's own destructive-red label rendered by sourceLabelLine at StreamNode.tsx:50-72 — it returns {text:'T_wall = (unset)', muted:true} when parameters.T_wall is undefined/null/'', and the muted state styles the label text-destructive/80 (line 270). promoteToSharedSource at useStore.ts:1335-1379 seeds only `n` (lines 1367-1369), leaving T_wall undefined. Combined with Gap A (user cannot reach T_wall in Properties), the destructive label is permanent. NOT a Channel-side check — the UAT-time interpretation was wrong; the user's literal 'they' meant the source nodes themselves."
  artifacts:
    - path: "gui/src/components/StreamNode.tsx"
      issue: "Lines 50-77 sourceLabelLine returns '(unset)' when value undefined; lines 268-275 render it in text-destructive/80"
    - path: "gui/src/store/useStore.ts"
      issue: "Lines 1335-1379 promoteToSharedSource seeds only n; never seeds T_wall (or HFS.q)"
  missing:
    - "Primary fix: RC-1 (Gap A) makes T_wall reachable in Properties, so once the user edits it the destructive label dissipates — most of the spurious-alert pain is gone"
    - "Optional: seed a default T_wall (e.g. 300.0 K or null-but-treat-as-placeholder) inside promoteToSharedSource when the spawned component has a type_union scalar 'value' param; same for HFS.q — so a fresh promoted source doesn't read '(unset)' before any editing"
    - "Alternative: de-emphasize the destructive styling on freshly-spawned sources (e.g. only red after user has interacted with the node) — lower priority"
  debug_session: ".planning/debug/wt-hfs-source-component-defects.md"

- truth: "HeatFluxSource node works equivalently to WallTemperature (Properties + drag-connect + no spurious unset)"
  status: failed
  reason: "User reported: (and probably the heatflux one too) is broken"
  severity: blocker
  test: 5
  root_cause: "Confirmed — HFS shares RC-1/RC-2/RC-3 with WT. HFS registry at components.json:1050-1082 is structurally identical (single BCPort source handle q_out, parameters n + type_union q, no external_inputs). RC-1 hides q from ParameterForm; RC-2 affects HFS→ChannelHeatFlux drag (CHF has no BCPort target handle either); RC-3 renders 'q = (unset)' destructive label (StreamNode.tsx:74-77 SOURCE_LABEL_FIELD['HeatFluxSource']='q')."
  artifacts:
    - path: "gui/src/registry/components.json"
      issue: "Lines 1050-1082 HFS registry — same structural shape as WT"
    - path: "gui/src/components/StreamNode.tsx"
      issue: "Lines 74-77 SOURCE_LABEL_FIELD HFS->q binding"
  missing:
    - "No HFS-specific work — the RC-1/RC-2/RC-3 fixes for WT carry HFS along automatically as long as the ParameterForm type_union branch is generic and BCPort target handles are added to BOTH Channel and ChannelHeatFlux"
    - "Add explicit Vitest coverage on HFS that mirrors the WT cases (Properties shows q; HFS BCPort drag-connect to CHF q_left/q_right works; no destructive q=(unset) after user types a value)"
  debug_session: ".planning/debug/wt-hfs-source-component-defects.md"
