---
phase: 63.1
plan: 02
type: execute
wave: 1
depends_on: ["63.1-01"]
files_modified:
  - gui/src/lib/selectors/nodeErrors.ts
  - gui/src/store/useStore.ts
  - gui/src/components/StreamNode.tsx
  - gui/src/store/__tests__/useStore.bc.test.ts
autonomous: true
requirements:
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19
  - D-23
requirements_addressed:
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19
  - D-23
must_haves:
  truths:
    - "selectNodeErrors(state, nodeId) is a pure function returning string[] for bc-n-mismatch tag (Phase 71 foundation)"
    - "errorTagsByNodeId slice no longer exists in useStore.ts (deleted, not just emptied)"
    - "_checkBCNMismatch action no longer exists in useStore.ts"
    - "StreamNode's hasBCError selector reads through selectNodeErrors and returns a primitive boolean (Pitfall 1)"
    - "n-mismatch red ring auto-clears when the underlying mismatch is fixed (no event handler needed)"
    - "D-16: stored-derived-state bug class is eliminated by construction (selector-derived replaces stored errorTagsByNodeId, so CR-03 residual ring + click-vs-drag stale Properties cannot recur)"
    - "D-17: selectNodeErrors is memoized at the consumer site via useStore(useCallback(s => selectNodeErrors(s, id).length > 0, [id])) so re-render cost stays trivial on tens-of-components graphs"
    - "D-18: ALL prior consumers of errorTagsByNodeId (StreamNode.tsx ring; test fixtures) are migrated to read selectNodeErrors — no parallel storage of derived state remains anywhere in gui/src"
    - "D-19: selectNodeErrors(state, nodeId): string[] signature is the Phase 71 validator-as-selector foundation — additional validators will follow the same shape (each validator = a pure selector)"
  artifacts:
    - path: "gui/src/lib/selectors/nodeErrors.ts"
      provides: "selectNodeErrors pure function and NodeErrorsInput type"
      exports: ["selectNodeErrors", "NodeErrorsInput"]
    - path: "gui/src/store/useStore.ts"
      provides: "errorTagsByNodeId slice + _checkBCNMismatch action removed; CanvasSnapshot field removed"
      contains: "// errorTagsByNodeId removed in Phase 63.1 per D-15"
    - path: "gui/src/components/StreamNode.tsx"
      provides: "hasBCError now reads via selectNodeErrors"
      contains: "selectNodeErrors"
  key_links:
    - from: "gui/src/components/StreamNode.tsx"
      to: "gui/src/lib/selectors/nodeErrors.ts"
      via: "useStore(useCallback(s => selectNodeErrors(s, id).length > 0, [id]))"
      pattern: "selectNodeErrors"
---

<objective>
Wave 1 — selector-derived validator foundation (D-15..D-19, D-23). Replace the stored `errorTagsByNodeId` slice + inline `_checkBCNMismatch` mutator with a pure memoized selector `selectNodeErrors(state, nodeId): string[]`. Migrate the only non-test consumer (`StreamNode.tsx:86-87`) to read through the selector. Delete the slice, its actions, its `CanvasSnapshot` field, and all undo plumbing for it. This eliminates the entire stale-derived-state bug class (D-16) that produced CR-03's residual n-mismatch ring and the click-vs-drag stale-Properties bug.

Purpose: Set the Phase 71 validator-as-selector foundation (D-19). After this, no stored derived BC state exists; ring/error state recomputes on demand on every render.

Output: New `gui/src/lib/selectors/nodeErrors.ts`; useStore.ts free of `errorTagsByNodeId` and `_checkBCNMismatch`; StreamNode.tsx migrated; existing `useStore.bc.test.ts` rewritten to assert via the selector instead of the deleted slice.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-CONTEXT.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-01-SUMMARY.md
@gui/src/store/useStore.ts
@gui/src/components/StreamNode.tsx
@gui/src/lib/bcMode.ts
@gui/src/lib/validation.ts

<interfaces>
Selector signature (NEW — per RESEARCH §"Selector-derived validator" lines 256-313):

```ts
// gui/src/lib/selectors/nodeErrors.ts (NEW)
import type { Node, Edge } from "@xyflow/react";
import type { BCModeEntry } from "../bcMode";

export interface NodeErrorsInput {
  nodes: Node[];
  edges: Edge[];
  bcMode: Record<string, BCModeEntry>;
  bcSymmetric: Record<string, boolean>;
  anchors: Record<string, { portField: "port_in.P" | "port_out.P"; value: number }>;
}

export function selectNodeErrors(state: NodeErrorsInput, nodeId: string): string[];
```

Note: `anchors` field is declared here even though Plan 03 lands the slice — selector accepts it as input shape so this plan and Plan 03 don't need ordering beyond depends_on chain. For Plan 02, the slice doesn't exist yet on the store; the selector receives `state.anchors ?? {}` via the consumer wrapper in StreamNode (use `s.anchors ?? {}` in the useCallback closure as a transient adaptor until Plan 03 adds the slice).

Existing consumer pattern to preserve (StreamNode.tsx:79-90 — DO NOT CHANGE the primitive-return discipline):
```ts
const hasBCError = useStore(useCallback(s => (s.errorTagsByNodeId[id]?.length ?? 0) > 0, [id]));
```
Replace with:
```ts
const hasBCError = useStore(useCallback(s => selectNodeErrors(s, id).length > 0, [id]));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create selectNodeErrors pure module + flip its RED tests to GREEN</name>
  <files>gui/src/lib/selectors/nodeErrors.ts</files>
  <behavior>
    - selectNodeErrors returns ['bc-n-mismatch'] when a source-mode bcMode entry connects two nodes whose parameters.n values differ (either consumer-side or source-side iteration finds the mismatch)
    - selectNodeErrors returns [] when n values match
    - selectNodeErrors returns [] when the bcMode entry's mode is not 'source'
    - selectNodeErrors recomputes correctly when caller mutates n (D-23 auto-clear)
    - Pure: no React, no zustand, no @xyflow runtime imports — types only
  </behavior>
  <read_first>
    gui/src/lib/selectors/__tests__/nodeErrors.test.ts (the RED tests from Plan 01 Task 2 — they define the contract this implementation must satisfy),
    gui/src/lib/bcMode.ts (lines 1-13 module-purity header; lines 29-43 BCMode/BCModeEntry types — copy purity-comment idiom),
    gui/src/lib/validation.ts (lines 73-84 — purity contract template),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Selector-derived validator" lines 256-313 — full draft body to translate; §"Pitfall 1" and §"Pitfall 2" for what consumers must do — but THIS file is the pure module, consumers come in Task 2),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"net-new patterns" lines 41-50 — selector library convention; Pattern C purity rule lines 988-994),
    gui/src/store/useStore.ts (lines 1364-1385 for current `_checkBCNMismatch` — the per-event mutator whose logic we are inverting into the pull-based selector)
  </read_first>
  <action>Create the directory `gui/src/lib/selectors/` and the file `nodeErrors.ts`. Header comment must mirror bcMode.ts:1-13 stating "Zero React dependencies, zero zustand dependencies, zero ReactFlow imports" — only type imports allowed. Export interface `NodeErrorsInput` matching the contract above. Export function `selectNodeErrors(state: NodeErrorsInput, nodeId: string): string[]`. Body translates RESEARCH §"Selector-derived validator" lines 270-313 verbatim: find the node by id, read its `parameters.n`, iterate `Object.entries(state.bcMode)` twice — once for consumer-side (`key.startsWith(nodeId+"::")` + entry.mode==='source') and once for source-side (`entry.mode==='source' && entry.sourceNodeId===nodeId`). Push `'bc-n-mismatch'` (literal string — keep the existing tag name from useStore.ts line ~1370 for grep-compatibility with downstream Phase 71 work) when typeof both n's are number and they differ. Use `break` after first push to deduplicate. Do NOT use `anchors` field in the body yet — declare it on the interface for future-use; if linter complains about unused field, mark with `// reserved: anchors used by future validators (Phase 71)` and leave the field. Read `parameters.n` via `(node.data as { parameters?: Record<string, unknown> })?.parameters?.["n"]` (no `unknown` widening; mirrors useStore.ts:1370 idiom).</action>
  <verify>
    <automated>cd gui && npx vitest run src/lib/selectors/__tests__/nodeErrors.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    File `gui/src/lib/selectors/nodeErrors.ts` exists.
    `grep -c "import.*from.*zustand\|import.*from.*react\|import.*from.*@xyflow/react'" gui/src/lib/selectors/nodeErrors.ts` returns 0 (purity check — type-only `import type` is permitted; the grep checks for value-imports). Acceptable refinement: `grep -E "^import [^t]" gui/src/lib/selectors/nodeErrors.ts | grep -v "^import type"` returns nothing.
    `grep -c "export function selectNodeErrors" gui/src/lib/selectors/nodeErrors.ts` equals 1.
    `cd gui && npx vitest run src/lib/selectors/__tests__/nodeErrors.test.ts` reports all tests passing (GREEN).
  </acceptance_criteria>
  <done>nodeErrors.ts pure module exists, purity grep passes, and the Plan 01 RED tests for selectNodeErrors flip to GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate StreamNode.tsx to selectNodeErrors and delete errorTagsByNodeId from useStore.ts</name>
  <files>gui/src/components/StreamNode.tsx, gui/src/store/useStore.ts, gui/src/store/__tests__/useStore.bc.test.ts</files>
  <behavior>
    - StreamNode's `hasBCError` selector reads via `selectNodeErrors(s, id).length > 0` and returns a primitive boolean (Pitfall 1 — never a fresh array)
    - useStore.ts has no remaining references to `errorTagsByNodeId`, `_checkBCNMismatch`, `addTagInPlace`, `removeTagInPlace` (delete entirely — D-18 no-parallel-storage rule)
    - useStore.bc.test.ts's `beforeEach` reset no longer references `errorTagsByNodeId`; assertions about the n-mismatch tag now read through `selectNodeErrors`
    - n-mismatch red ring on canvas auto-clears when the underlying mismatch is corrected (verified by unit test calling selectNodeErrors after a state mutation)
  </behavior>
  <read_first>
    gui/src/components/StreamNode.tsx (lines 77-91 — the three selector hooks; the comment block at lines 80-83 documents Pitfall 1 — PRESERVE that comment, just update the body),
    gui/src/store/useStore.ts (use grep to find ALL `errorTagsByNodeId` references — RESEARCH §"errorTagsByNodeId consumer audit" lists exact line numbers: 132, 142, 249, 266, 737, 760, 774, 790, 804, 815, 834, 848, 859, 1258, 1278, 1341, 1361, 1371, 1384, 1925, 2010; also `_checkBCNMismatch` at 1364-1385 and the helper `addTagInPlace`/`removeTagInPlace` inline functions),
    gui/src/store/__tests__/useStore.bc.test.ts (the reset boilerplate at lines 14-31; assertion sites referencing `errorTagsByNodeId` — ~10 sites per RESEARCH audit),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"errorTagsByNodeId consumer audit" line range; Pitfall 5 lines 483-487 about snapshot stack handling),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"useStore.ts — anchors slice" Pattern A snapshot discipline; Pattern D primitive-return selector lines 996-1010)
  </read_first>
  <action>Step A — StreamNode.tsx: add `import { selectNodeErrors } from "@/lib/selectors/nodeErrors";` near the existing `@/lib/*` imports. Replace lines 84-90 (the `hasBCError` selector body) so the useStore hook calls `useCallback((s) => selectNodeErrors(s as unknown as NodeErrorsInput, id).length > 0, [id])`. Preserve the Pitfall-1 comment at lines 80-83 verbatim (it documents why a primitive must be returned). Keep `hasError` (the legacy `errorNodeIds: Set<string>`-based selector at line 78) untouched — it's a separate Phase-39 surface, not affected by D-15. Step B — useStore.ts: delete every line referencing `errorTagsByNodeId`, `_checkBCNMismatch`, `addTagInPlace`, `removeTagInPlace`. This includes (i) the `AppState` field declaration, (ii) the initial-state `errorTagsByNodeId: {}` line, (iii) the `CanvasSnapshot` field at line 142, (iv) every `_pushSnapshot`/`undo`/`redo` site that copies the field (lines 249/266/737/760/774/790/804/815/834/848/859), (v) every call to `_checkBCNMismatch` (lines ~1058 in `addEdge`, others in `setBCMode`/`clearBCMode`), (vi) the `_checkBCNMismatch` definition at 1364-1385 itself, (vii) the `addTagInPlace`/`removeTagInPlace` inline helpers and their imports (search by name; RESEARCH says they live as locally-scoped helpers near the action). Add a single explanatory comment block at the top of the deleted-region replacement (e.g. just below the existing Phase 63 BC slice header at ~line 1108): `// Phase 63.1 D-15: errorTagsByNodeId slice + _checkBCNMismatch action removed.  // Ring state now derives from selectNodeErrors (gui/src/lib/selectors/nodeErrors.ts).` Step C — useStore.bc.test.ts: drop `errorTagsByNodeId: {}` from the `beforeEach` reset; for every assertion that previously read `useStore.getState().errorTagsByNodeId[nodeId]`, rewrite to `selectNodeErrors(useStore.getState(), nodeId)` (import selectNodeErrors at the top of the test file). Drop any test that asserts ABOUT the stored tag's existence as a side-effect of an event (these were testing the bug class we just eliminated); leave the BEHAVIORAL tests (n-mismatch detection) but reroute them through the selector. The CR-01/CR-02/CR-03 regression tests are in Plan 01 Wave 0 (separate files) — do not duplicate here.</action>
  <verify>
    <automated>cd gui && grep -c "errorTagsByNodeId\|_checkBCNMismatch\|addTagInPlace\|removeTagInPlace" src/store/useStore.ts 2>/dev/null; npx vitest run src/components/__tests__/StreamNode.test.tsx src/store/__tests__/useStore.bc.test.ts src/lib/selectors/__tests__/nodeErrors.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "errorTagsByNodeId" gui/src/store/useStore.ts` returns 1 or fewer (the explanatory comment block is the only permitted match).
    `grep -c "_checkBCNMismatch\|addTagInPlace\|removeTagInPlace" gui/src/store/useStore.ts` returns 0.
    `grep -c "selectNodeErrors" gui/src/components/StreamNode.tsx` returns 1 or more.
    `grep -c "// Select a primitive" gui/src/components/StreamNode.tsx` returns 1 (the Pitfall-1 comment is preserved).
    `grep -c "errorTagsByNodeId" gui/src/store/__tests__/useStore.bc.test.ts` returns 0.
    `cd gui && npx vitest run src/store/__tests__/useStore.bc.test.ts src/lib/selectors/__tests__/nodeErrors.test.ts src/components/__tests__/StreamNode.test.tsx` reports all GREEN.
    `cd gui && npx tsc --noEmit` exits 0 (no TS errors introduced).
  </acceptance_criteria>
  <done>The errorTagsByNodeId slice and its mutator are physically removed from useStore.ts; StreamNode reads via selectNodeErrors; bc.test.ts rewritten; tsc and vitest both green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wave 1 gate — full vitest suite green</name>
  <files>(no edits — verification only)</files>
  <read_first>
    gui/src/store/__tests__ (whole directory — there are 8 store tests that may have referenced errorTagsByNodeId in fixtures),
    gui/src/lib/__tests__,
    gui/src/components/__tests__
  </read_first>
  <action>Run the full GUI test suite. If any test outside the rewritten files still fails because it referenced `errorTagsByNodeId` in a `beforeEach` reset or fixture (RESEARCH §Wave 0 Gaps lists candidates: `saveAndOpenErrors.test.ts`, `saveProjectAs.test.ts`, `modelOptions.test.ts`, `activeLeftTab.test.ts`, `resources.slice.test.ts`, `selection.test.ts`, `useStore.test.ts`), drop that one line from the offending test's `useStore.setState(...)` fixture — no other change. Do NOT touch `bcs: []` references in those fixtures yet (Plan 03 owns the anchors/bcs swap for the same set of test fixtures). After fixture cleanup, vitest must be green (other than the Wave 0 RED stubs from Plan 01 that target Plans 03/04/05/06/07/08/09/10 — those remain RED until their plans land; this gate accepts that).</action>
  <verify>
    <automated>cd gui && npx vitest run 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    `grep -rE "\\berrorTagsByNodeId\\b" gui/src` returns EXACTLY 0 OR exactly 1 match. If 1, the single permitted match MUST be the explanatory comment block inserted in Task 2 Step B at the top of the deleted-region replacement in `gui/src/store/useStore.ts` (the line beginning `// Phase 63.1 D-15: errorTagsByNodeId slice + _checkBCNMismatch action removed.`). No other file may contain the token, and no other line in useStore.ts may contain it. This is a deterministic grep gate — it does NOT depend on judging whether test failures are "attributable to" the removal; it directly enumerates remaining call sites. If the implementor chose to omit the explanatory comment entirely (also acceptable), the grep returns exactly 0; document the choice in the task SUMMARY.
    `cd gui && npx tsc --noEmit` exits 0 (mechanical proof that no consumer still references the removed slice as a value — the type system catches what grep cannot).
    `cd gui && npx vitest run` reports the full suite GREEN except for the documented Plan-01 RED stubs awaiting later plans. The permitted-RED file set is exactly: `useStore.anchors.test.ts`, `useStore.onNodesChange.select.test.ts`, `useStore.addEdge.bcport.test.ts`, `useStore.setBCSymmetric.test.ts`, `promoteToSharedSource.test.ts`, `codeGenerator.anchors.test.ts`, `projectIO.anchors.test.ts`, `StreamNode.anchor.test.tsx`. Confirm by running the suite and listing failures: every failure file MUST be in that 8-item set. If any file outside that set fails, this gate fails.
    All OTHER tests are GREEN.
  </acceptance_criteria>
  <done>Full vitest run shows only the documented Wave-0-pending-implementation RED stubs (8-file allowlist); grep proves the errorTagsByNodeId identifier is gone except for the one permitted comment; tsc green; Wave 1 done.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1.02-01 | Information Disclosure | selectNodeErrors | N/A — local in-process selector, no logging, no network |
| T-63.1.02-02 | Tampering | useStore slice deletion | N/A — undo stack is in-memory only, no persistence of errorTagsByNodeId (Pitfall 5 confirms in-memory ephemeral) |
</threat_model>

<verification>
- `gui/src/lib/selectors/nodeErrors.ts` exists and is purity-clean (only `import type` imports from other modules).
- `gui/src/store/useStore.ts` no longer contains `errorTagsByNodeId` declarations (only a comment block referencing the deletion is permitted).
- `gui/src/components/StreamNode.tsx` reads through `selectNodeErrors` and the Pitfall-1 comment is preserved.
- Full GUI vitest suite GREEN except for the documented Plan-01 RED stubs awaiting Plans 03+.
- tsc --noEmit exits 0.
</verification>

<success_criteria>
- nodeErrors.ts pure module exists, GREEN-tested for D-15 / D-23 behaviors.
- useStore.ts free of the deleted slice and mutator; CanvasSnapshot smaller by one field.
- StreamNode.tsx red-ring rendering still works for n-mismatch (verified by an existing test that previously asserted the stored tag, now rewritten to assert the selector).
- Wave 1 unblocks Wave 2 (anchors slice + codegen swap can proceed without touching the n-mismatch surface).
</success_criteria>

<output>
After completion, create `.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-02-SUMMARY.md`
</output>
