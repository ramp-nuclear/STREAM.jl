---
phase: 63.1
plan: 08
type: execute
wave: 5
depends_on: ["63.1-06", "63.1-07"]
files_modified:
  - gui/src/store/useStore.ts
  - gui/src/components/sidebar/BCsTabForm.tsx
autonomous: true
requirements:
  - D-07
  - D-08
requirements_addressed:
  - D-07
  - D-08
must_haves:
  truths:
    - "useStore exposes a promoteToSharedSource(consumerNodeId, externalInputName) action under snapshot-undo discipline"
    - "promoteToSharedSource spawns the corresponding WallTemperature / HeatFluxSource node at (consumer.x - 160, consumer.y - 40), seeds n from the consumer, writes a source-mode bcMode entry, and materializes the dashed BC edge"
    - "BCsTabForm.tsx renders a `↗ Promote to shared source` ghost button next to the Mode Select on every External Inputs row where externalInput.source_component is defined AND entry?.mode !== 'source' (D-07 visibility rule)"
    - "Existing handleNewSource flow in BCsTabForm collapses into a single dispatch of the new store action"
  artifacts:
    - path: "gui/src/store/useStore.ts"
      provides: "promoteToSharedSource action"
      contains: "promoteToSharedSource:"
    - path: "gui/src/components/sidebar/BCsTabForm.tsx"
      provides: "Promote button inline next to the Mode dropdown"
      contains: "↗ Promote to shared source"
  key_links:
    - from: "BCsTabForm Promote button onClick"
      to: "useStore.promoteToSharedSource(nodeId, externalInputName)"
      via: "store action dispatch"
      pattern: "promoteToSharedSource"
    - from: "promoteToSharedSource"
      to: "WallTemperature / HeatFluxSource node + dashed BC edge on canvas"
      via: "addNode + setBCMode (mode=source) inside a single transaction"
      pattern: "addNode.*setBCMode"
---

<objective>
Wave 5 — Promote-to-shared-source flow (D-07, D-08). Hoist the existing inline `BCsTabForm.handleNewSource` into a store action `promoteToSharedSource(consumerNodeId, externalInputName)` and surface it as a `↗ Promote to shared source` ghost button next to the Mode Select. The button is visible only when the input has an associated source_component in the registry AND the current entry is not already in source mode.

Purpose: Default flow keeps inline BCs on the BCs tab (Sources canvas category is hidden in Plan 09); when the user wants the shared-source model, they explicitly elevate via this single button. The store action makes the operation atomic (single snapshot, single subscribe-driven re-render).

Output: New store action; BCsTabForm.tsx renders the Promote button on each eligible row.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-CONTEXT.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-07-SUMMARY.md
@gui/src/store/useStore.ts
@gui/src/components/sidebar/BCsTabForm.tsx

<interfaces>
New store action (signature locked):
```ts
promoteToSharedSource: (consumerNodeId: string, externalInputName: string) => void;
```

Side effects per D-08 + UI-SPEC §"Promote-to-Shared-Source Button — Click behavior":
1. Push undo snapshot.
2. Resolve sourceCompId via registry: getComponent(consumerComponentId)?.external_inputs?.find(e => e.name === externalInputName)?.source_component (returns "WallTemperature" or "HeatFluxSource").
3. addNode(sourceCompId, {x: consumer.x - 160, y: consumer.y - 40}) — RESEARCH A6 default position.
4. Identify newly spawned node by diffing pre/post id sets (existing pattern from BCsTabForm.handleNewSource at lines 685-689). Seed n on the new node by calling updateNodeParams with parameters.n equal to the consumer's parameters.n (so the n-mismatch selector does not flag the brand-new pair).
5. setBCMode(consumerNodeId, externalInputName, { mode: "source", sourceNodeId: newNode.id }) — this is the existing setBCMode action; its edge-materialization branch creates the dashed BC edge automatically.

Visibility rule (UI-SPEC §"Promote-to-Shared-Source Button — Visibility rules"):
- externalInput.source_component is defined
- entry?.mode !== "source"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add promoteToSharedSource action to useStore.ts</name>
  <files>gui/src/store/useStore.ts</files>
  <behavior>
    - Action exists at the same level as other BC actions (near setBCMode / clearBCMode)
    - Action pushes a single snapshot before any mutations (Pattern A discipline)
    - Action handles missing-component gracefully: if consumer node not found OR sourceCompId not resolvable from registry, the action is a no-op (no snapshot, no state change) — the Promote button shouldn't render in those cases anyway
    - n-seeding happens BEFORE the setBCMode call so the n-mismatch selector evaluates against a matching pair
    - Plan 01's promoteToSharedSource.test.ts flips from RED to GREEN
  </behavior>
  <read_first>
    gui/src/store/useStore.ts (find setBCMode body to confirm edge-materialization branch exists; find addNode action body to confirm it returns void; find updateNodeParams action signature; find existing _pushSnapshot usage),
    gui/src/components/sidebar/BCsTabForm.tsx (lines 673-703 — current handleNewSource that we are lifting verbatim; this is the proven shape of the operation),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Promote to shared source" lines 788-820; A2 in Assumptions Log lines 968-969 — addNode currently returns void; do NOT change its signature in this plan, use the pre/post id-diff trick from handleNewSource verbatim),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"promoteToSharedSource action" lines 262-303 — exact lift code),
    gui/src/registry (for getComponent + external_inputs[].source_component schema — already used by handleNewSource so the field is known to exist),
    gui/src/store/__tests__/promoteToSharedSource.test.ts (the Plan-01 RED test — this task flips it to GREEN)
  </read_first>
  <action>Step A — locate the BC slice actions in useStore.ts (near setBCMode / clearBCMode, line ~1116). Add a new action `promoteToSharedSource: (consumerNodeId, externalInputName) => { ... }` following PATTERNS lines 262-303 verbatim. The body resolves the consumer node, reads its componentId via `(consumer.data as unknown as StreamNodeData).componentId`, calls `getComponent(componentId)` and finds the matching `external_inputs[].source_component` for `externalInputName`. If sourceCompId is undefined, return without mutating. Otherwise call `get()._pushSnapshot()`, then `get().addNode(sourceCompId, {x: consumer.position.x - 160, y: consumer.position.y - 40})`. After addNode, diff the pre/post id sets (RESEARCH A2 pattern — addNode returns void in current API; the diff is the safe way to find the new node): `const beforeIds = new Set(state.nodes.map(n => n.id))` BEFORE addNode, `const afterNodes = get().nodes` after, `const newNode = afterNodes.find(n => !beforeIds.has(n.id))`. If newNode is undefined, abort early (don't call setBCMode without a target). Step B — n-seeding: read `consumerN = (consumer.data as unknown as StreamNodeData).parameters?.n ?? 1`. Call `get().updateNodeParams(newNode.id, { parameters: { n: consumerN as number } })` matching the existing handleNewSource shape at BCsTabForm.tsx:691-693. Step C — dispatch setBCMode in source mode: `get().setBCMode(consumerNodeId, externalInputName, { mode: "source", sourceNodeId: newNode.id })`. Note: setBCMode internally pushes another snapshot — that's fine; the user gets two snapshots for one Promote op, which still behaves correctly under undo (one Ctrl+Z reverts the BC mode, a second reverts the spawn+n-seed). If undo-granularity becomes a UX concern, future refactor can suppress the inner snapshot — out of scope here. Step D — type the action on AppState interface near the other BC actions.</action>
  <verify>
    <automated>cd gui && grep -c "promoteToSharedSource:" src/store/useStore.ts; grep -c "promoteToSharedSource(" src/store/useStore.ts; npx vitest run src/store/__tests__/promoteToSharedSource.test.ts 2>&1 | tail -20; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "promoteToSharedSource:" gui/src/store/useStore.ts` returns 1 (action body).
    AppState interface declares promoteToSharedSource — verify by `grep -c "promoteToSharedSource: (consumerNodeId" gui/src/store/useStore.ts` returns 2 or more (type + body).
    `grep -c "consumer.position.x - 160" gui/src/store/useStore.ts` returns 1 (spawn position per A6).
    `grep -c "consumer.position.y - 40" gui/src/store/useStore.ts` returns 1.
    `cd gui && npx vitest run src/store/__tests__/promoteToSharedSource.test.ts` reports all GREEN.
    `cd gui && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>promoteToSharedSource action live, snapshot-disciplined, n-seeded before BC mode set; Plan 01 promoteToSharedSource.test.ts GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `↗ Promote to shared source` ghost button next to Mode Select in BCsTabForm.tsx</name>
  <files>gui/src/components/sidebar/BCsTabForm.tsx</files>
  <behavior>
    - In every External Inputs FieldRow whose externalInput.source_component is defined AND entry?.mode !== "source", a `<Button variant="ghost" size="sm">` renders next to the Mode Select with the literal text "↗ Promote to shared source"
    - Button click dispatches useStore.promoteToSharedSource(nodeId, externalInputName)
    - The button is hidden (not rendered) when entry?.mode === "source" — no disabled state
    - The button aligns inline with the Mode Select via flex container: `<div className="flex items-center gap-[8px]"><Select.../><Button.../></div>`
    - The button uses `flex-shrink-0` so it does not push the Select narrower; if row width is < 240px it wraps below (UI-SPEC §"Trigger placement")
    - The existing handleNewSource local function in BCsTabForm collapses to a single store-action dispatch (kept as a thin wrapper OR deleted entirely if its only call site is now redundant)
  </behavior>
  <read_first>
    gui/src/components/sidebar/BCsTabForm.tsx (lines 673-703 current handleNewSource function — this becomes a simple `() => useStore.getState().promoteToSharedSource(nodeId, externalInput.name)` or its call site directly dispatches the store action and the helper is deleted; lines 707-710 existing `+ New ${sourceCompId}` Button as the analog for inline action button shape — its `variant="outline"` becomes `variant="ghost"` for Promote per UI-SPEC),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md (§"Promote-to-Shared-Source Button" full section — trigger placement, visibility rules, exact copy "↗ Promote to shared source" with the Unicode U+2197, ghost variant + sm size, click behavior; §"Mode Dropdown — Layout" diagram showing the Select + Promote in the same row),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Promote to shared source" lines 788-820; §"Pattern 3" lines 388-393 — exact JSX snippet),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"Promote button" lines 706-712)
  </read_first>
  <action>Step A — wrap the Mode Select (added in Plan 07) inside a flex container: `<div className="flex items-center gap-[8px]"><div className="flex-1 min-w-0"> ... existing Select + required-unset hint ... </div> {promoteButton} </div>`. Step B — define `promoteButton` as a conditional fragment: when `externalInput.source_component` is truthy AND `entry?.mode !== "source"`, render `<Button variant="ghost" size="sm" className="flex-shrink-0" onClick={() => useStore.getState().promoteToSharedSource(nodeId, externalInput.name)}>↗ Promote to shared source</Button>`. Note: the literal `↗` is the Unicode arrow U+2197 — paste it directly in the JSX, not as an HTML entity. Step C — clean up handleNewSource: if the only caller of handleNewSource is now this button click, replace the body with `useStore.getState().promoteToSharedSource(nodeId, externalInput.name)` OR delete the local function and inline the dispatch. The "+ New ${sourceCompId}" Button at lines 707-710 — UI-SPEC does not call for it to remain. It was the predecessor of the Promote button. Per D-07 + UI-SPEC §"Promote button" the Promote button supersedes "+ New". DELETE the "+ New ${sourceCompId}" Button rendering and its surrounding empty-state branch if it was the only render path (the inline value editor for value-mode + the source-list Select for source-mode remain — only the "+ New" affordance goes). Step D — confirm copy verbatim: `grep -c "↗ Promote to shared source"` returns 1.</action>
  <verify>
    <automated>cd gui && grep -c "↗ Promote to shared source" src/components/sidebar/BCsTabForm.tsx; grep -c "promoteToSharedSource(nodeId, externalInput.name)\\|promoteToSharedSource(nodeId, externalInput\\.name)" src/components/sidebar/BCsTabForm.tsx; grep -c "variant=\"ghost\"" src/components/sidebar/BCsTabForm.tsx; grep -c "+ New " src/components/sidebar/BCsTabForm.tsx; npx tsc --noEmit 2>&1 | tail -10; npx vitest run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "↗ Promote to shared source" gui/src/components/sidebar/BCsTabForm.tsx` returns 1.
    `grep -c "promoteToSharedSource(" gui/src/components/sidebar/BCsTabForm.tsx` returns 1 or more.
    `grep -c "variant=\"ghost\"" gui/src/components/sidebar/BCsTabForm.tsx` returns 1 or more (Promote button uses ghost variant).
    `grep -c "+ New " gui/src/components/sidebar/BCsTabForm.tsx` returns 0 (legacy + New ${sourceCompId} button removed; Promote replaces it).
    `cd gui && npx tsc --noEmit` exits 0.
    `cd gui && npx vitest run` reports zero new failures vs Plan 07 baseline.
  </acceptance_criteria>
  <done>Promote button live; correct visibility gating; legacy "+ New" button removed; tsc + vitest green modulo expected RED stubs.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1.08-01 | Tampering | promoteToSharedSource action | accept | externalInputName is constrained by the registry shape (TS type); consumerNodeId is the active selection — both come from in-process state. No external input flows here. |
| T-63.1.08-02 | DoS | repeated Promote clicks | accept | Visibility rule hides the button after the first successful promote (entry.mode becomes "source"); user cannot stack-spawn nodes accidentally |
</threat_model>

<verification>
- promoteToSharedSource action exists on the store, atomic + snapshot-disciplined.
- BCsTabForm renders the Promote button per UI-SPEC visibility rules with the exact ↗ Unicode label.
- Plan 01 promoteToSharedSource.test.ts GREEN.
- tsc + vitest green modulo Plan 10 RED stubs (CR-01/02/03/click-vs-drag).
</verification>

<success_criteria>
- D-07 + D-08 satisfied: the only Promote affordance is the explicit button in the BCs tab; canvas spawn position matches A6 default; new node n is seeded from consumer.
- The dominant flow (one channel / one BC / inline value) requires zero canvas-source thinking; the rare shared-source flow requires one click.
</success_criteria>

<output>
After completion, create `.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-08-SUMMARY.md`
</output>
