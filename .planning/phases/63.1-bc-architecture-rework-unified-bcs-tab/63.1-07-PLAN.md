---
phase: 63.1
plan: 07
type: execute
wave: 4
depends_on: ["63.1-03", "63.1-05"]
files_modified:
  - gui/src/components/sidebar/BCsTabForm.tsx
  - gui/src/components/sidebar/BCModePicker.tsx
autonomous: true
requirements:
  - D-11
  - D-12
requirements_addressed:
  - D-11
  - D-12
must_haves:
  truths:
    - "BC mode picker is a shadcn Select dropdown, not a 5-pill segmented row (D-11)"
    - "Required-unset state shows placeholder 'Select BC mode...' + inline destructive hint 'BC required — select a mode' verbatim"
    - "Symmetric toggle is a labeled SegmentedButtonGroup with two pills 'Symmetric' | 'Asymmetric' (D-12)"
    - "Custom SymmetricToggle component is deleted from BCsTabForm.tsx; BCModePicker.tsx file is deleted from the repo"
    - "Boolean store value is preserved at the boundary: 'sym' → true, 'asym' → false"
  artifacts:
    - path: "gui/src/components/sidebar/BCsTabForm.tsx"
      provides: "Inline Select dropdown for BC mode; SegmentedButtonGroup for Symmetric/Asymmetric; removed SymmetricToggle local component"
      contains: "Symmetric"
  key_links:
    - from: "BCsTabForm Mode Select onValueChange"
      to: "setBCMode store action"
      via: "existing onModeChange handler"
      pattern: "setBCMode"
    - from: "SegmentedButtonGroup onChange"
      to: "setBCSymmetric(nodeId, baseField, v === 'sym')"
      via: "string→boolean translation at the boundary"
      pattern: "setBCSymmetric"
---

<objective>
Wave 3 — UX rework of the BCs tab's mode picker (D-11) and symmetric toggle (D-12). Replace the 5-pill BCModePicker with an inline shadcn Select. Replace the custom L=R SymmetricToggle with a labeled SegmentedButtonGroup that says "Symmetric | Asymmetric". Delete the BCModePicker.tsx file once it has no remaining consumer.

Purpose: Mode change is rare, value edit is common — the dropdown shape gives the value editor full row width and reduces the visual weight of a switch the user rarely flips. The segmented control eliminates the "L=R" engineer-speak that confused users about which state the toggle is in.

Output: BCsTabForm.tsx with two new inline primitives; BCModePicker.tsx physically deleted; tests covering both controls.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-CONTEXT.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-05-SUMMARY.md
@gui/src/components/sidebar/BCsTabForm.tsx
@gui/src/components/sidebar/BCModePicker.tsx
@gui/src/components/sidebar/SegmentedButtonGroup.tsx
@gui/src/components/ui/select.tsx
@gui/src/lib/bcMode.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace BCModePicker (5-pill) with inline shadcn Select dropdown in BCsTabForm</name>
  <files>gui/src/components/sidebar/BCsTabForm.tsx, gui/src/components/sidebar/BCModePicker.tsx</files>
  <behavior>
    - In BCsTabForm.tsx, every FieldRow that currently renders `<BCModePicker active={entry?.mode} ... />` now renders an inline shadcn `<Select value={entry?.mode ?? ""} onValueChange={onModeChange}>` with the 5 mode options (Value/Profile/Function/Mark/Source)
    - When entry is undefined, the Select shows placeholder "Select BC mode..." and a destructive hint "BC required — select a mode" renders below
    - The Select consumes the full row width (`w-full` on SelectTrigger); spacing matches UI-SPEC §"Mode Dropdown — Layout"
    - BCModePicker.tsx is deleted from the repo (the inline Select replaces its sole consumer)
  </behavior>
  <read_first>
    gui/src/components/sidebar/BCsTabForm.tsx (find every import of BCModePicker — there should be one; find every JSX usage — every FieldRow; lines ~39-45 contain the existing shadcn Select usage pattern for the source-list dropdown — copy that import + JSX shape; lines 713-732 source-list Select as the imitable analog),
    gui/src/components/sidebar/BCModePicker.tsx (full file — to understand the required-unset hint copy at lines 50-54 and BC_MODE_OPTIONS at lines 28-34; both are migrated into the inline Select),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md (§"Mode Dropdown" full section incl. Options table, Layout diagram, Required-unset hint, Trigger states; §"Copywriting Contract" — `Select BC mode...`, `BC required — select a mode`),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Pattern 3" lines 359-394 — example JSX),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"BCsTabForm.tsx — mode picker pivot" lines 640-678),
    gui/src/components/ui/select.tsx (Select / SelectTrigger / SelectValue / SelectContent / SelectItem exports)
  </read_first>
  <action>Step A — imports: in BCsTabForm.tsx, ensure shadcn Select primitives are imported (`Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem` from `@/components/ui/select`); they are likely already imported because the file uses Select for the source-list dropdown. REMOVE `import BCModePicker from "./BCModePicker"`. Step B — replace BCModePicker JSX: locate the FieldRow body where `<BCModePicker active={...} onChange={...} />` is currently rendered. Replace with the JSX shape from RESEARCH lines 366-383: `<Select value={entry?.mode ?? ""} onValueChange={(m) => onModeChange(m as BCMode)}><SelectTrigger className="w-full"><SelectValue placeholder="Select BC mode..." /></SelectTrigger><SelectContent>` then 5 SelectItem children with values "value"/"profile"/"function"/"mark"/"source" and labels "Value"/"Profile"/"Function"/"Mark"/"Source" verbatim. Below the Select, conditionally render `{entry === undefined && <p className="text-xs text-destructive/80 mt-[6px]">BC required — select a mode</p>}` (copy verbatim from BCModePicker.tsx:51). Wrap the Select + hint in a `<div className="flex flex-col gap-[6px]">` so the hint aligns naturally below. Step C — delete BCModePicker.tsx: `rm gui/src/components/sidebar/BCModePicker.tsx`. Verify by `grep -rE "BCModePicker" gui/src` returning zero matches. If any test file imports BCModePicker directly (Plan 01 did NOT create one targeting BCModePicker as a component — the dropdown behavior is verified through BCsTabForm rendering tests in the next task; double-check), update it. Step D — preserve the Promote button slot: do NOT add Promote here — that is Plan 08. The new Select sits alone on its row for now; Plan 08 will place the Promote button next to it.</action>
  <verify>
    <automated>cd gui && test ! -f src/components/sidebar/BCModePicker.tsx && echo "BCModePicker deleted"; grep -rE "BCModePicker" src 2>/dev/null | head -5; grep -c "Select BC mode" src/components/sidebar/BCsTabForm.tsx; grep -c "BC required — select a mode" src/components/sidebar/BCsTabForm.tsx; grep -c "SelectItem value=\"value\"\\|SelectItem value=\"profile\"" src/components/sidebar/BCsTabForm.tsx; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    File `gui/src/components/sidebar/BCModePicker.tsx` does NOT exist.
    `grep -rE "BCModePicker" gui/src` returns 0 matches.
    `grep -c "Select BC mode" gui/src/components/sidebar/BCsTabForm.tsx` returns 1.
    `grep -c "BC required — select a mode" gui/src/components/sidebar/BCsTabForm.tsx` returns 1.
    `grep -c "SelectItem" gui/src/components/sidebar/BCsTabForm.tsx` returns 5 or more new occurrences (5 mode options) plus any pre-existing usage.
    `cd gui && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>BCModePicker file deleted, no orphaned imports, inline Select renders the 5 modes plus required-unset hint, tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace SymmetricToggle (custom switch) with SegmentedButtonGroup in BCsTabForm</name>
  <files>gui/src/components/sidebar/BCsTabForm.tsx</files>
  <behavior>
    - The custom SymmetricToggle local component (currently at lines 342-369) is deleted
    - In its place at the top of each paired GroupBlock body, a SegmentedButtonGroup renders with options [{value:"sym",label:"Symmetric"}, {value:"asym",label:"Asymmetric"}]
    - active = isSymmetric ? "sym" : "asym"
    - onChange = (v) => setBCSymmetric(nodeId, group.baseField, v === "sym")
    - Existing setBCSymmetric store action is unchanged (boolean store value preserved at the boundary)
    - Spacing: mb-[8px] above the first FieldRow (matches UI-SPEC §"Symmetric / Asymmetric Segmented Control" placement)
  </behavior>
  <read_first>
    gui/src/components/sidebar/BCsTabForm.tsx (lines 342-369 SymmetricToggle component to delete; line where the JSX `<SymmetricToggle ... />` is rendered — find by grep; lines 530-538 Profile preset SegmentedButtonGroup usage as analog),
    gui/src/components/sidebar/SegmentedButtonGroup.tsx (verify props: options, active, onChange, size),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md (§"Symmetric / Asymmetric Segmented Control" full section incl. exact JSX + copy lock-in),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Pattern 3 — Dropdown picker + segmented control" lines 357-394),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"Replace SymmetricToggle with SegmentedButtonGroup" lines 680-703)
  </read_first>
  <action>Step A — locate and delete the SymmetricToggle local component declaration at lines 342-369 (the `function SymmetricToggle({...}) {...}` block). Remove any helper type `SymmetricToggleProps`. Step B — locate the JSX call site (grep for `<SymmetricToggle`); replace with the SegmentedButtonGroup JSX shown in UI-SPEC §"Symmetric / Asymmetric Segmented Control" — `<SegmentedButtonGroup options={[{value:"sym",label:"Symmetric"},{value:"asym",label:"Asymmetric"}]} active={isSymmetric ? "sym" : "asym"} onChange={(v) => setBCSymmetric(nodeId, group.baseField, v === "sym")} size="sm" />`. Keep the surrounding container (likely the GroupBlock head) — only replace the toggle element. Add a wrapper `<div className="mb-[8px]">` if not already present so spacing matches the diagram in UI-SPEC §"Mode Dropdown — Layout". Step C — verify the SegmentedButtonGroup import is already present (file uses it for Profile preset and Function signature pickers per UI-SPEC); if missing, add `import SegmentedButtonGroup from "./SegmentedButtonGroup"`. Step D — copy lock-in: confirm by grep that the strings "Symmetric" and "Asymmetric" (full words, not Sym/Asym) appear together in this section per UI-SPEC §"Copy lock-in" line 297. Step E — preserve store boundary: ensure setBCSymmetric still receives a boolean as its third arg — no signature change.</action>
  <verify>
    <automated>cd gui && grep -c "function SymmetricToggle" src/components/sidebar/BCsTabForm.tsx; grep -c "SymmetricToggle" src/components/sidebar/BCsTabForm.tsx; grep -c "Symmetric\\b" src/components/sidebar/BCsTabForm.tsx; grep -c "Asymmetric\\b" src/components/sidebar/BCsTabForm.tsx; grep -c "v === \"sym\"" src/components/sidebar/BCsTabForm.tsx; npx tsc --noEmit 2>&1 | tail -10; npx vitest run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "function SymmetricToggle" gui/src/components/sidebar/BCsTabForm.tsx` returns 0.
    `grep -c "<SymmetricToggle" gui/src/components/sidebar/BCsTabForm.tsx` returns 0.
    `grep -c "Symmetric\\b" gui/src/components/sidebar/BCsTabForm.tsx` returns at least 1 (the SegmentedButtonGroup label).
    `grep -c "Asymmetric\\b" gui/src/components/sidebar/BCsTabForm.tsx` returns at least 1.
    `grep -c "v === \"sym\"" gui/src/components/sidebar/BCsTabForm.tsx` returns 1.
    `grep -c "setBCSymmetric" gui/src/components/sidebar/BCsTabForm.tsx` returns 1 or more (call site preserved).
    `cd gui && npx tsc --noEmit` exits 0.
    `cd gui && npx vitest run` reports zero new failures vs Plan 06 baseline.
  </acceptance_criteria>
  <done>SymmetricToggle component deleted, SegmentedButtonGroup with Symmetric/Asymmetric labels in place, store boundary unchanged, tsc + vitest green modulo expected RED stubs.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1.07-01 | N/A | UI primitive swap | N/A — same store actions called with same inputs; only the React rendering of the controls changes |
</threat_model>

<verification>
- BCModePicker.tsx file gone.
- BCsTabForm.tsx uses shadcn Select for BC mode and SegmentedButtonGroup for Symmetric/Asymmetric.
- All copy strings match UI-SPEC verbatim.
- tsc + vitest green modulo expected RED stubs.
</verification>

<success_criteria>
- D-11 + D-12 satisfied at the UI surface.
- No regression in setBCMode / setBCSymmetric store-action behavior (boundary preserved).
- BCsTabForm.tsx is the single home for both controls — no more freestanding picker component.
</success_criteria>

<output>
After completion, create `.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-07-SUMMARY.md`
</output>
