---
phase: 63.1-bc-architecture-rework-unified-bcs-tab
verified: 2026-05-13T23:55:00Z
status: passed
score: 23/23 decisions verified (D-01..D-23)
overrides_applied: 0
deferred:
  - truth: "SidebarPanel.anchors.test.tsx > Channel BCs tab body still renders the existing BCsTabForm content below Anchors"
    addressed_in: "Follow-up tsc/test cleanup sweep (see deferred-items.md)"
    evidence: "Pre-existing failure — Plan 07 deleted the legacy 'Symmetric (L = R)' label in BCsTabForm.tsx but the SidebarPanel.anchors.test.tsx fixture at line 124 still asserts that copy. Verified pre-existing via `git stash && vitest run` on Plan 08 merge commit. Not caused by Plan 10. Logged in deferred-items.md."
human_verification: []
---

# Phase 63.1: BC Architecture Rework — Unified BCs Tab — Verification Report

**Phase Goal:** Rework the BC architecture that shipped in Phase 63 to address three problem classes: (a) correctness bugs (CR-01/02/03, click-vs-drag stale Properties, n-mismatch ring stale lifecycle), (b) wrong default for the dominant case (Sources-as-canvas-nodes when one channel/one BC/one value suffices), (c) the 5-pill BCModePicker UX advertising the rarest action. Deliver a single coherent rework: unified BCs tab (Anchors + External Inputs sections) per component, anchors slice replacing `bcs: BCEntry[]`, selector-derived validator replacing stored `errorTagsByNodeId`, segmented `Symmetric|Asymmetric` control, mode dropdown, "Promote to shared source" hoist, and hard schema break (no migration).

**Verified:** 2026-05-13T23:55:00Z
**Status:** PASSED (with one deferred pre-existing test failure outside this phase's scope)
**Re-verification:** No — initial verification

---

## Coverage Matrix: D-01..D-23 → Evidence

| Decision | Topic | Evidence Pointer | Status |
|---|---|---|---|
| **D-01** | Pressure anchor is the only anchor kind in scope; gravity stays per-component; transient ICs deferred | `gui/src/lib/anchors.ts:17-21` (D-01 lock-in comment); `AnchorEntry.portField` typed as `"port_in.P" \| "port_out.P"` only (`anchors.ts:30-33`) | PASS |
| **D-02** | New `anchors: Record<nodeId, AnchorEntry>` slice; at-most-one per node; sentinel-by-absence | `gui/src/lib/anchors.ts:30-40`; `gui/src/store/useStore.ts:142` (AppState field), `:163` (CanvasSnapshot field), `:733` (initial `{}`), `:1216-1228` (setAnchor / clearAnchor bodies) | PASS |
| **D-03** | Delete legacy `bcs: BCEntry[]` slice + freestanding `BCPanel.tsx`; single rendering site | `BCPanel.tsx` / `BCRow.tsx` confirmed deleted (`ls` → ENOENT); `grep -nE "\\bbcs\\b" gui/src/store/useStore.ts gui/src/lib/codeGenerator.ts gui/src/lib/validation.ts gui/src/lib/projectIO.ts` → 0 live matches; sole rendering site is `SidebarPanel.tsx:418-433` (BCs tab body) | PASS |
| **D-04** | Anchors section only for components with a FlowPort | `AnchorsSection.tsx:41-44` (`hasFlowPort = component.ports.some(p => p.type === "FlowPort"); if (!hasFlowPort) return null`); `SidebarPanel.tsx:160` (host-level gate) | PASS |
| **D-05** | Codegen iterates `Object.entries(anchors)` and emits `${instanceName}.${portField} ~ ${value}` | `codeGenerator.ts:1232-1233` (`// Pressure anchors (D-05)` + `for (const [nodeId, entry] of Object.entries(anchorsState.anchors))`); 3rd arg signature is `anchorsState: CodegenAnchorsState` at `:688` | PASS |
| **D-06** | Sources toolbox category removed from default toolbox | `ToolboxPanel.tsx` — `grep "Sources"` returns 0 matches; only `Hydraulic` + `Thermal` categories present (`:20-21`). Registry entries kept in `components.json` for `promoteToSharedSource` (Plan 09 SUMMARY confirms) | PASS |
| **D-07** | "↗ Promote to shared source" trigger as button next to mode dropdown | `BCsTabForm.tsx:420-429` (`variant="ghost"`, label `↗ Promote to shared source` U+2197, onClick dispatches `promoteToSharedSource(nodeId, externalInput.name)`) | PASS |
| **D-08** | Promote spawns WT/HFS node + creates dashed BC edge + replaces inline value with reference | `useStore.ts:1335` (`promoteToSharedSource` action); 63.1-08-SUMMARY confirms spawn-offset `(consumer.x - 160, consumer.y - 40)`, n-seed before setBCMode, dashed bcEdge materialized via setBCMode's edge-materialization branch. Plan-01 RED `promoteToSharedSource.test.ts` flipped GREEN (1/1) | PASS |
| **D-09** | Two clearly-labeled sections inside BCs tab: Anchors (top) + External Inputs (below) | `SidebarPanel.tsx:418-433` (`{hasFlowPort && (<h3>Anchors</h3><AnchorsSection.../>{hasExternalInputs && <Separator/>}})` + `{hasExternalInputs && (<h3>External Inputs</h3>...)`)` | PASS |
| **D-10** | BCs tab stays in right sidebar next to Properties | `SidebarPanel.tsx:181-188` (BCs tab inside ComponentTabs strip alongside Properties); no relocation | PASS |
| **D-11** | Replace 5-pill BCModePicker with dropdown | `BCModePicker.tsx` deleted (`ls` → ENOENT). `BCsTabForm.tsx:402-409` — shadcn `Select` with placeholder `Select BC mode...` + 5 `SelectItem` (Value/Profile/Function/Mark/Source) | PASS |
| **D-12** | Replace "L=R" switch with labeled `Symmetric | Asymmetric` segmented control | `BCsTabForm.tsx:283-292` — `SegmentedButtonGroup` with options `[{value:"sym",label:"Symmetric"},{value:"asym",label:"Asymmetric"}]`; boolean translation at the store boundary (`v === "sym"`) | PASS |
| **D-13** | Canvas indicator on anchored ports | `StreamNode.tsx:123` (`FlowPortHandle` sub-component), `:159-160` (`data-testid="anchor-indicator"`, `aria-label="Pressure anchor"`); Plan-01 RED `StreamNode.anchor.test.tsx` flipped GREEN (Plan 09) | PASS |
| **D-14** | No migration path for Phase-63-era `.streamgui` files; no fallback UI | `projectIO.ts:205-218` (explicit no-fallback comment block; `parsed.anchors ?? {}` — legacy field silently dropped). `grep -nE "\\bbcs\\b" gui/src/lib/projectIO.ts` → 0 live matches | PASS |
| **D-15** | Delete stored `errorTagsByNodeId` slice; replace with memoized selector | `gui/src/lib/selectors/nodeErrors.ts` (95-line pure selector module); `useStore.ts` `grep -nE "errorTagsByNodeId"` → 7 matches, all D-15 historical comments (lines 134, 255, 579, 814, 2026, 2112). No live slice field; `_checkBCNMismatch` gone | PASS |
| **D-16** | Eliminate stored-derived-state bug class by construction | `selectNodeErrors` is a pure pull-based function (`nodeErrors.ts:55-95`); zero stored mismatch tags exist anywhere. Wired via `StreamNode.tsx:180` selector call (`useStore(useCallback((s) => selectNodeErrors(s as unknown as NodeErrorsInput, id).length > 0, [id]))`) | PASS |
| **D-17** | Memoize the selector (primitive boolean return per Pitfall 1) | `nodeErrors.ts:10-12` (header doc requires primitive-boolean consumption); `StreamNode.tsx:180` consumer wraps in `useCallback` + `.length > 0` returning primitive boolean — stable shallow-equality | PASS |
| **D-18** | All current consumers of `errorTagsByNodeId` migrated to selector | The sole production consumer was `StreamNode.tsx` for the red ring — now reads through `selectNodeErrors` (`StreamNode.tsx:180`). Test fixtures migrated by Plan 02 (5 files) + Plan 03 (17 files); `grep -lrE "bcs:\\s*\\[\\]" gui/src` → 0 files | PASS |
| **D-19** | Phase 71 framework extends same pattern (selector-as-validator) | Foundation set: `nodeErrors.ts` header explicitly documents the `(state, nodeId) => string[]` shape as the Phase 71 contract (`nodeErrors.ts:7-8`); reserved `anchors` field on `NodeErrorsInput` (`:32`) for future validators | PASS |
| **D-20** | CR-01 — addEdge BCPort branch writes `bcMode` entry | `useStore.ts:1137-1175` (addEdge BCPort branch): on canvas drag with `connection.targetHandle` truthy, derives `bcModeKey`, reads `bcSymmetric ?? true` (default ON), constructs `entry: BCModeEntry = {mode: "source", sourceNodeId: connection.source}`, seeds primary + sibling, calls `set({ edges, isDirty: true, bcMode: nextBCMode })`. Inline `CR-01 (D-20)` comment at `:1145`. RED test `useStore.addEdge.bcport.test.ts` 2/2 GREEN | PASS |
| **D-21** | CR-02 / CR-03 — setBCSymmetric edge reconciliation | `useStore.ts:1381-1441` (setBCSymmetric body): `if/else if` branching covers CR-02 (left undef + right defined → delete right + remove dangling edge via `_reconcileEdgesForBCMode`) at `:1406-1419` and CR-03 (left defined + entries differ → mirror left→right + add/remove right edge) at `:1420-1432`. Shared helper `_reconcileEdgesForBCMode` defined at `:602`. RED test `useStore.setBCSymmetric.test.ts` 2/2 GREEN | PASS |
| **D-22** | selectedNodeId synced from ReactFlow select events (click-vs-drag fix) | `useStore.ts:955-972` (onNodesChange select-sync loop runs BEFORE isContentless early-return at `:976`); handles `selected:true` → `selectNode(c.id)` and `selected:false` → `selectNode(null)`. Inline `D-22` comment at `:956`. RED test `useStore.onNodesChange.select.test.ts` 2/2 GREEN | PASS |
| **D-23** | N-mismatch ring auto-clears (subsumed by D-15) | Structural: selector recomputes on every render — no event-driven cleanup. `nodeErrors.ts:1` header explicitly references D-23. Empirically backed by `useStore.bc.test.ts` 20/20 GREEN (multiple n-mismatch appearance + disappearance cycles) | PASS |

**Score:** 23/23 decisions verified

---

## Additional Acceptance Checks

### Legacy slice / file removal (D-03, D-14)

| Check | Expected | Actual | Status |
|---|---|---|---|
| `gui/src/components/BCPanel.tsx` exists | NO | `ls` → ENOENT | PASS |
| `gui/src/components/BCRow.tsx` exists | NO | `ls` → ENOENT | PASS |
| `gui/src/components/sidebar/BCModePicker.tsx` exists | NO | `ls` → ENOENT | PASS |
| `gui/src/components/sidebar/__tests__/BCModePicker.test.tsx` exists | NO | `ls` → ENOENT | PASS |
| `bcs:` field declaration in useStore.ts | absent | 0 live matches; `bcs` token absent (per Plan 03's strict-comment rule using "boundary-conditions" prose) | PASS |
| `BCEntry` export from codeGenerator.ts | absent | `grep -cE "\\bBCEntry\\b" codeGenerator.ts` → 0 | PASS |
| `errorTagsByNodeId` slice declaration in useStore.ts | absent | All 7 matches are D-15 historical comments; no field declaration, no initializer, no setter | PASS |
| `_checkBCNMismatch` action body in useStore.ts | absent | `grep -cE "_checkBCNMismatch\\s*[(:]" useStore.ts` → 0 (2 doc-comment matches don't include call/decl syntax) | PASS |

### Plan 01 RED → GREEN flip-through

All 9 Plan-01 Wave-0 RED test files are now GREEN:

| Test file | Owning plan | Status |
|---|---|---|
| `useStore.anchors.test.ts` (5 tests) | Plan 03 | GREEN |
| `useStore.onNodesChange.select.test.ts` (2 tests) | Plan 10 | GREEN |
| `useStore.addEdge.bcport.test.ts` (2 tests) | Plan 10 | GREEN |
| `useStore.setBCSymmetric.test.ts` (2 tests) | Plan 10 | GREEN |
| `promoteToSharedSource.test.ts` (1 test) | Plan 08 | GREEN |
| `lib/selectors/__tests__/nodeErrors.test.ts` (4 tests) | Plan 02 | GREEN |
| `lib/__tests__/codeGenerator.anchors.test.ts` (3 tests) | Plan 04 | GREEN |
| `lib/__tests__/projectIO.anchors.test.ts` (3 tests) | Plan 03 | GREEN |
| `components/__tests__/StreamNode.anchor.test.tsx` (2 tests) | Plan 09 | GREEN |

### Full GUI vitest run

```
$ cd gui && npx vitest run
Test Files  1 failed | 47 passed (48)
Tests       1 failed | 550 passed | 9 todo (560)
```

The single failure is the documented deferred item (`SidebarPanel.anchors.test.tsx` line 124, asserting the legacy "Symmetric (L = R)" copy that Plan 07 retired). Verified pre-existing on Plan 08 merge commit `098676d` via stash. See `deferred-items.md`.

### TSC baseline

```
$ cd gui && npx tsc --noEmit | grep -cE "error TS"
11
```

11 pre-existing errors, all in documented categories (StreamNode Handle `data` prop x3, BCsTabForm.test.tsx StreamNodeData casts x3, SidebarRouter.test.tsx `peaking` field x2, validation.test.ts unused imports x3). Plan 02/03 baseline was 12; the difference of 1 reflects Plan 09's Rule-1 fix of the `ToolboxPanel.test.tsx` `activeLayer: "all"` typo (a net improvement, not a regression). Zero new errors caused by 63.1.

---

## Deferred Items

One pre-existing test failure remains, addressed outside this phase per the executor scope-boundary rule:

| # | Item | Addressed In | Evidence |
|---|---|---|---|
| 1 | `SidebarPanel.anchors.test.tsx > "Channel BCs tab body still renders the existing BCsTabForm content below Anchors"` | Follow-up tsc/test cleanup sweep | The test at line 124 still asserts `screen.getByText("Symmetric (L = R)")`. Plan 07 retired that copy in `BCsTabForm.tsx` (replaced with `Symmetric \| Asymmetric` segmented control) but missed the assertion in `SidebarPanel.anchors.test.tsx`. Verified pre-existing on Plan 08 merge via `git stash` (Plan 10 SUMMARY §"Out-of-Scope Discoveries"). Two-line fix: change assertion to `screen.getByRole("button", { name: "Symmetric" })` (matching Plan 07's earlier `SidebarPanel.test.tsx` fix). |

The deferred-items.md also records 4 pre-existing tsc-error categories (`SidebarRouter.test.tsx peaking`, `validation.test.ts` unused imports, `BCsTabForm.test.tsx` StreamNodeData casts) — none caused by Phase 63.1, all listed for a future tsc cleanup sweep.

---

## Anti-Patterns Scan

| File | Pattern | Severity | Impact |
|---|---|---|---|
| (none) | No TBD/FIXME/XXX/HACK markers in any 63.1-modified file (verified by `grep -nE "TBD\|FIXME\|XXX" gui/src/store/useStore.ts gui/src/lib/anchors.ts gui/src/lib/selectors/nodeErrors.ts gui/src/components/sidebar/AnchorsSection.tsx gui/src/components/sidebar/BCsTabForm.tsx`) | — | None |
| `errorTagsByNodeId` comment references in `useStore.ts` (7) and `StreamNode.tsx` (1) | Historical doc comment (D-15 deletion sites) | INFO | Intentional — documents the deleted slice for grep-traceability per Plan 02 SUMMARY §"Documentation choice". Plan 03 enforced a stricter rule for `bcs` ("no bare token in comments") yielding 0 matches there. |

No stub implementations introduced. Promote action, Anchors UI, anchor indicator, and all CR fixes exercise live store paths.

---

## Verification Notes

**Goal-backward verification methodology:** I started from the phase goal (unified BCs tab + selector-derived validator + CR-01/02/03 + click-vs-drag), enumerated the D-01..D-23 decisions as observable truths, and verified each in the codebase via direct file inspection + grep + the SUMMARY artifacts as cross-references (not as primary evidence). Every D-* decision has a file:line pointer or a passing vitest assertion.

**SUMMARY trust posture:** I cross-checked every SUMMARY claim against the actual codebase. Highlights:
- The "12 baseline tsc errors" claim shifted to 11 after Plan 09's Rule-1 ToolboxPanel.test.tsx fixture fix — a real net improvement.
- The 550-passing / 1-failing vitest result matches Plan 10's claim and the single failure matches `deferred-items.md` exactly.
- All physical deletions (BCPanel.tsx, BCRow.tsx, BCModePicker.tsx, BCModePicker.test.tsx) are confirmed via `ls` → ENOENT.
- All asserted code paths (CR-01 branch, CR-02/CR-03 reconciliation, D-22 select-sync loop, D-23 selector pattern) are present at the cited line numbers.

**Adversarial counter-points considered and dismissed:**
- **"D-14 might still have legacy fallback":** Inspected `projectIO.ts:205-218` — the `parsed.anchors ?? {}` line is the entire restore path; no `bcs` branch exists. Comment block explicitly references CLAUDE.md no-back-compat rule.
- **"errorTagsByNodeId comments could be live code":** Verified each of the 7 matches in `useStore.ts` — all begin with `//` and reference D-15 historical context. No `errorTagsByNodeId:` field declaration anywhere.
- **"selectNodeErrors might not actually wire through to canvas red ring":** Verified `StreamNode.tsx:180` reads through the selector and computes a primitive boolean for `hasBCError`; the ring rendering branch downstream consumes that boolean (per Plan 02 SUMMARY).

---

## Final Verdict

**COMPLETE** — All 23 D-* decisions in 63.1-CONTEXT.md have codebase evidence and verified behavior. The phase delivers the full BC architecture rework as scoped:

1. Anchors slice replaces legacy `bcs: BCEntry[]` end-to-end (store, projectIO, codegen, validation, sidebar UI, canvas indicator) with hard schema break per D-14.
2. Selector-derived validator (`selectNodeErrors`) replaces stored `errorTagsByNodeId` and eliminates the stale-derived-state bug class.
3. BCs-tab UX rebuilt: AnchorsSection + External Inputs sections; mode dropdown + segmented `Symmetric|Asymmetric`; inline "↗ Promote to shared source" button.
4. CR-01 (addEdge BCPort bcMode write), CR-02 (setBCSymmetric left-undef edge), CR-03 (setBCSymmetric mirror + edge sync), click-vs-drag (D-22) and n-mismatch auto-clear (D-23) all confirmed fixed.
5. Legacy surfaces (BCPanel/BCRow/BCModePicker/Sources toolbox category) physically removed.

The single deferred item (`SidebarPanel.anchors.test.tsx` line 124) is a pre-existing test-fixture oversight from Plan 07's surface swap — not a phase-blocking gap. It is documented in `deferred-items.md` for a follow-up cleanup sweep that should also pick up the 4 pre-existing tsc-error categories.

**Recommended next action:** Ready for `/gsd:ship`. The single deferred test fixture can be folded into the same cleanup PR as the pre-existing tsc baseline (`SidebarRouter.test.tsx peaking`, `validation.test.ts` unused imports, `BCsTabForm.test.tsx` StreamNodeData casts), since none of these block the phase goal. Human UAT against the Manual-Only Verifications table in 63.1-VALIDATION.md (BCs tab layout, canvas anchor indicator, Promote flow, Sources toolbox, n-mismatch auto-clear, click-vs-drag, FlowPort gate) is the next quality gate before milestone consolidation.

---

*Verified: 2026-05-13T23:55:00Z*
*Verifier: Claude (gsd-verifier)*
