---
phase: 70-presets-and-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gui/src-tauri/Cargo.toml
  - gui/src-tauri/capabilities/default.json
  - gui/components.json
  - gui/src/components/ui/textarea.tsx
  - gui/src/components/ui/radio-group.tsx
autonomous: false
requirements: []
# Traces CONTEXT.md decisions: D-04 (Library = appConfigDir), D-05 (file-watch),
# and the shadcn primitives needed for the Save modal (D-15).

must_haves:
  truths:
    - "Tauri JS-side `watch()` import from @tauri-apps/plugin-fs returns a working UnwatchFn at runtime (no IPC error)."
    - "Reads, writes, removes, mkdirs, and dir-listings of `$APPCONFIG/presets/**` and the already-allowed `$APPDATA` path succeed."
    - "shadcn `Textarea` and `RadioGroup` components are importable from `@/components/ui/textarea` and `@/components/ui/radio-group`."
  artifacts:
    - path: "gui/src-tauri/Cargo.toml"
      provides: "tauri-plugin-fs with watch feature flag"
      contains: 'tauri-plugin-fs = { version = "2.4.5", features = ["watch"] }'
    - path: "gui/src-tauri/capabilities/default.json"
      provides: "fs:allow-watch, fs:allow-unwatch, fs:scope-appconfig-recursive, $APPCONFIG/presets/** scope entry"
      contains: "fs:allow-watch"
    - path: "gui/src/components/ui/textarea.tsx"
      provides: "shadcn Textarea component export"
      exports: ["Textarea"]
    - path: "gui/src/components/ui/radio-group.tsx"
      provides: "shadcn RadioGroup + RadioGroupItem exports"
      exports: ["RadioGroup", "RadioGroupItem"]
  key_links:
    - from: "gui/src-tauri/Cargo.toml"
      to: "@tauri-apps/plugin-fs watch IPC commands"
      via: "cargo feature flag"
      pattern: 'features\s*=\s*\["watch"\]'
    - from: "gui/src-tauri/capabilities/default.json"
      to: "Tauri ACL"
      via: "permission identifiers"
      pattern: "fs:allow-watch"
---

<objective>
Land the platform substrate that everything else in Phase 70 depends on: enable the `watch` feature on `tauri-plugin-fs`, grant the four ACL permissions/scopes needed for file-system watching under `$APPCONFIG/presets/**`, and install the two shadcn primitives (`textarea`, `radio-group`) consumed by the Save-as-Preset modal.

Purpose: every downstream plan (presetIO, store slice, PresetsPanel, SavePresetModal, wiring) imports either the watch API, the appconfig FS scope, or one of the new shadcn primitives. Without these, nothing runs.

Output: 1 Cargo.toml line edit, ~4 lines added to `capabilities/default.json`, and two new shadcn files added by `npx shadcn add textarea radio-group`.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/70-presets-and-templates/70-CONTEXT.md
@.planning/phases/70-presets-and-templates/70-RESEARCH.md
@.planning/phases/70-presets-and-templates/70-PATTERNS.md
@.planning/phases/70-presets-and-templates/70-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Current Cargo dep (gui/src-tauri/Cargo.toml line 26):
       tauri-plugin-fs = "2.4.5"
     Target form (RESEARCH.md Q1):
       tauri-plugin-fs = { version = "2.4.5", features = ["watch"] }

     Current capabilities/default.json already grants fs:default, fs:allow-mkdir,
     fs:allow-write-text-file, fs:allow-read-text-file, fs:allow-read-dir,
     fs:allow-remove, fs:scope-home-recursive, fs:scope-appdata-recursive,
     and a fs:scope object for "$APPDATA/STREAM-Composer/autorecover/**".

     Additions required (RESEARCH.md Q1):
       "fs:allow-watch",
       "fs:allow-unwatch",
       "fs:scope-appconfig-recursive",
       {
         "identifier": "fs:scope",
         "allow": [ { "path": "$APPCONFIG/presets/**" } ]
       }

     $APPCONFIG resolves to ~/.config/com.stream.composer on Linux
     (identifier from tauri.conf.json is "com.stream.composer", NOT "stream-composer";
     never hardcode the segment — always join via appConfigDir()).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable tauri-plugin-fs `watch` feature in Cargo.toml</name>
  <files>gui/src-tauri/Cargo.toml</files>
  <read_first>
    - gui/src-tauri/Cargo.toml (full file — single line edit, ~30-line file)
    - .planning/phases/70-presets-and-templates/70-RESEARCH.md (Q1 "Cargo.toml change required" block, Pitfall 1)
  </read_first>
  <action>
    Change the existing `tauri-plugin-fs = "2.4.5"` dependency line to use the table form with the `watch` feature enabled, per RESEARCH.md Q1. Do not bump the version. Do not touch any other dependency. Per CLAUDE.md branching policy, do not switch branches.
  </action>
  <verify>
    <automated>cd gui/src-tauri && grep -E 'tauri-plugin-fs\s*=\s*\{\s*version\s*=\s*"2\.4\.5",\s*features\s*=\s*\["watch"\]\s*\}' Cargo.toml | grep -c watch</automated>
  </verify>
  <done>The single Cargo.toml line is replaced with the table form including `features = ["watch"]`; grep gate above returns 1.</done>
</task>

<task type="auto">
  <name>Task 2: Grant fs:allow-watch / fs:allow-unwatch / appconfig scope in capabilities/default.json</name>
  <files>gui/src-tauri/capabilities/default.json</files>
  <read_first>
    - gui/src-tauri/capabilities/default.json (full file — read entire 35-line JSON)
    - .planning/phases/70-presets-and-templates/70-RESEARCH.md (Q1 "capabilities/default.json additions required")
    - .planning/phases/70-presets-and-templates/70-PATTERNS.md (Modified File Pattern Notes — capabilities/default.json section)
  </read_first>
  <action>
    Add the three string-form permissions `fs:allow-watch`, `fs:allow-unwatch`, and `fs:scope-appconfig-recursive` to the `permissions` array, immediately after the existing `fs:allow-read-dir` entry. Then add a new object-form `fs:scope` entry with `allow: [{ "path": "$APPCONFIG/presets/**" }]`, inserted as a sibling of the existing `$APPDATA/STREAM-Composer/autorecover/**` scope object (i.e., a second `fs:scope` object — do NOT extend the existing one's allow array, since the two scopes serve different subsystems and keeping them separate documents intent). Preserve trailing comma syntax. Do not remove or reorder any existing permission. Output must remain valid JSON with the same `$schema`, `identifier`, `description`, and `windows` fields.

    Per `RESEARCH.md` Pitfall 1 and CLAUDE.md: this is a Tauri ACL change that requires a Tauri rebuild at integration time (Wave 4). Do not run `cargo build` from this task — that happens in the wiring/UAT plan.
  </action>
  <verify>
    <automated>cd gui/src-tauri && node -e "const c=JSON.parse(require('fs').readFileSync('capabilities/default.json','utf8'));const p=c.permissions;const has=s=>p.includes(s);const scope=p.find(x=>typeof x==='object'&&JSON.stringify(x.allow||[]).includes('APPCONFIG/presets'));if(!has('fs:allow-watch')||!has('fs:allow-unwatch')||!has('fs:scope-appconfig-recursive')||!scope){process.exit(1)}console.log('ok')"</automated>
  </verify>
  <done>All four entries present; JSON parses; existing entries untouched; the appconfig scope object lives separately from the existing autorecover scope object.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Install shadcn `textarea` and `radio-group` primitives (human-run command)</name>
  <files>
    gui/components.json
    gui/src/components/ui/textarea.tsx
    gui/src/components/ui/radio-group.tsx
  </files>
  <read_first>
    - gui/components.json (verify current registry + aliases)
    - .planning/phases/70-presets-and-templates/70-RESEARCH.md (Environment Availability table — `shadcn textarea` and `shadcn radio-group` rows; A-09 in UI-SPEC)
    - .planning/phases/70-presets-and-templates/70-UI-SPEC.md (Registry Safety section: both are official shadcn registry, no third-party vetting required)
  </read_first>
  <what-built>
    Shadcn primitives `textarea` and `radio-group` not yet installed in `gui/components.json`. The Save-as-Preset modal (Surface 5) consumes both: `Textarea` for the Description field, `RadioGroup`/`RadioGroupItem` for the Store selector (Library / Project).
  </what-built>
  <action>
    Halt automated execution. The user runs `cd gui && npx shadcn@latest add textarea radio-group` from the project root (the official shadcn registry; per UI-SPEC Registry Safety, no third-party vetting required). The executor does not write code in this task — it only waits for the resume signal after the user has verified the install per the steps in `<how-to-verify>` below. After resume the executor proceeds to plan 70-02.
  </action>
  <how-to-verify>
    From the `gui/` directory run:

    ```
    cd gui && npx shadcn@latest add textarea radio-group
    ```

    If prompted to overwrite existing components, decline (these should be net-new). Then verify:

    1. `ls gui/src/components/ui/textarea.tsx gui/src/components/ui/radio-group.tsx` lists both files.
    2. `grep -E '"(textarea|radio-group)"' gui/components.json` (if registry block tracks installed list) OR confirm the two files compile by running `cd gui && npx tsc --noEmit 2>&1 | grep -E "(textarea|radio-group)" | head -5` returns no NEW errors above the existing baseline (Phase 61 documented 7 pre-existing tsc errors; Phase 65 added a few more — track delta, not absolute count).
    3. Both files export the expected symbols: `grep -E '^export.*(Textarea|RadioGroup)' gui/src/components/ui/textarea.tsx gui/src/components/ui/radio-group.tsx`.

    Resume signal: "approved" once both files exist and the tsc delta is zero. If shadcn adds breaking dependencies (e.g., new @radix-ui packages mid-flight), report the diff before approving.
  </how-to-verify>
  <acceptance_criteria>
    - `gui/src/components/ui/textarea.tsx` exists and exports `Textarea`.
    - `gui/src/components/ui/radio-group.tsx` exists and exports `RadioGroup` and `RadioGroupItem`.
    - `cd gui && npm install` (if shadcn added Radix deps) completes without error.
    - No regressions in existing tsc error count.
  </acceptance_criteria>
  <verify>
    <automated>cd gui && ls src/components/ui/textarea.tsx src/components/ui/radio-group.tsx >/dev/null 2>&1 && grep -E "^export.*(Textarea)" src/components/ui/textarea.tsx >/dev/null && grep -E "^export.*(RadioGroup)" src/components/ui/radio-group.tsx >/dev/null && echo ok</automated>
  </verify>
  <done>Both shadcn primitive files exist with their expected exports and the human approved via "approved" resume signal.</done>
  <resume-signal>Type "approved" or report the diff if shadcn pulled unexpected deps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri webview → Tauri Rust core (ACL) | JS-side `watch()` calls cross into the Rust core via IPC; ACL gates which paths can be watched. |
| `npx shadcn` registry → local filesystem | Shadcn fetches component source from the official shadcn registry over HTTPS and writes TS files into `gui/src/components/ui/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Elevation of Privilege | tauri-plugin-fs `watch` capability | mitigate | Scope `fs:scope` object restricts watch/read to `$APPCONFIG/presets/**` only — no wildcard FS access granted. Existing `$APPDATA/STREAM-Composer/autorecover/**` scope is unaffected. |
| T-70-02 | Information Disclosure | `fs:scope-appconfig-recursive` | mitigate | The recursive scope is bounded to the per-app `appConfigDir()` which resolves to `~/.config/com.stream.composer/` on Linux — Tauri ACL guarantees the scope variable cannot escape this directory. |
| T-70-03 | Tampering | npm/shadcn install (Task 3) | mitigate | Human-verify checkpoint runs `npx shadcn@latest add ...` against the official shadcn registry. Both `textarea` and `radio-group` are first-party shadcn components (UI-SPEC Registry Safety table — A-09). Human inspects the diff before approving. |
| T-70-SC | Tampering | npm/cargo installs (this plan & downstream) | mitigate | Package Legitimacy Audit: `tauri-plugin-fs` and `@radix-ui/react-radio-group` (transitive of shadcn radio-group) are both established libraries used elsewhere in this codebase. No new third-party packages introduced here. |
</threat_model>

<verification>
- `grep -E 'features\s*=\s*\["watch"\]' gui/src-tauri/Cargo.toml` returns one match.
- `cd gui/src-tauri && node -e "JSON.parse(require('fs').readFileSync('capabilities/default.json','utf8'))"` succeeds (JSON valid).
- All four ACL identifiers / scope object present in the permissions array.
- Both shadcn files exist with their expected exports.
- No new tsc errors above existing baseline.
</verification>

<success_criteria>
Wave 1 substrate ready: `watch` feature compiled into the Rust plugin once Tauri is next built; ACL permits watching `$APPCONFIG/presets/**`; `Textarea` and `RadioGroup` importable from `@/components/ui/`. Downstream plans 70-02..70-06 can be authored against these primitives without further substrate work.
</success_criteria>

<output>
Create `.planning/phases/70-presets-and-templates/70-01-SUMMARY.md` when done. Note: Tauri rebuild deferred to Wave 4 (70-06), where the integration UAT first exercises the watcher and ACL.
</output>
