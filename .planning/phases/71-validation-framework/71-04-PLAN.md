---
phase: 71-validation-framework
plan: 04
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - gui/src/lib/validation/rules/portType.ts
  - gui/src/lib/validation/rules/requiredConnections.ts
  - gui/src/lib/validation/rules/danglingFlowPort.ts
  - gui/src/lib/validation/rules/__tests__/portType.test.ts
  - gui/src/lib/validation/rules/__tests__/requiredConnections.test.ts
  - gui/src/lib/validation/rules/__tests__/danglingFlowPort.test.ts
  - gui/src/lib/validation/index.ts
autonomous: true
requirements: [D-07, D-08, D-14, D-15, D-16, D-19]
tags: [validation, rules, structural, port-type, dangling, required-connections]

must_haves:
  truths:
    - "portType rule emits ValidationResult{severity:'error'} for any edge whose source/target port types disagree OR for any BCPort edge whose (sourceComponent, targetComponent) pair is not allowed by isAllowedBCConnection."
    - "portType emits edge + both endpoint targets symmetrically (D-14)."
    - "requiredConnections rule emits ValidationResult{severity:'error'} for any required port (Port.required === true OR component-doc convention) without an attached edge."
    - "danglingFlowPort rule emits ValidationResult{severity:'error'} for each unconnected FlowPort on each node (folds VALD-01 verbatim from validateTopology, D-16)."
    - "danglingFlowPort emits one ValidationResult per unconnected FlowPort with targets: [{kind:'port', nodeId, portName}] (D-14: no edge for dangling case)."
    - "All three rule files are pure functions; zero useStore imports, zero React imports."
    - "All three rules are registered in gui/src/lib/validation/index.ts via explicit import + array push."
  artifacts:
    - path: "gui/src/lib/validation/rules/portType.ts"
      provides: "portType: Validator covering port-type mismatch + BCPort allow-list (D-19's single source of truth)"
    - path: "gui/src/lib/validation/rules/requiredConnections.ts"
      provides: "requiredConnections: Validator (covers 'all required ports must be connected' per §3.9 rule 4)"
    - path: "gui/src/lib/validation/rules/danglingFlowPort.ts"
      provides: "danglingFlowPort: Validator (VALD-01 fold per D-16)"
  key_links:
    - from: "gui/src/lib/validation/index.ts"
      to: "gui/src/lib/validation/rules/portType.ts"
      via: "explicit registration"
      pattern: "import \\{ portType \\}"
    - from: "gui/src/components/CanvasPanel.tsx"
      to: "gui/src/lib/validation/rules/portType.ts"
      via: "Plan 13 reroutes isValidConnection through this rule per D-19"
      pattern: "portType\\.run\\("
---

<objective>
Ship three structural / port-graph validators per D-15 (rules 4, 5, 6 of the §3.9 set), folding VALD-01 (D-16) and providing the canonical portType rule that Plan 13 reroutes onConnect through (D-19).

Purpose: these three rules cover the "missing connection / wrong connection type" axis of structural validation. They share no algorithm (each is straightforward graph iteration) but share file/test conventions so they ship together.

Output: three pure-function Validator modules + three vitest test files + registry append in index.ts.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-validation-framework/71-CONTEXT.md
@.planning/phases/71-validation-framework/71-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: portType rule + tests (D-19 single source of truth)</name>
  <files>gui/src/lib/validation/rules/portType.ts, gui/src/lib/validation/rules/__tests__/portType.test.ts</files>
  <read_first>
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-11, D-14, D-19 (target shape + symmetric edge emission + single-source-of-truth contract)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Pattern 4: onConnect Hard-Block Reroute (D-19)" + §"Pitfall 2"
    - gui/src/lib/bcMode.ts (isAllowedBCConnection — current allow-list; the rule MUST consult it so connection-time semantics are preserved)
    - gui/src/components/CanvasPanel.tsx:263-288 (current isValidConnection — what Plan 13 will reroute)
    - gui/src/lib/validation/types.ts (Validator + ValidationResult + Target — Plan 01)
    - gui/src/lib/validation/snapshot.ts (ValidationSnapshot — Plan 01)
    - gui/src/registry/components.json (Port.type values: 'FlowPort' | 'ThermalPort' | 'BCPort')
  </read_first>
  <behavior>
    - Edge connecting a FlowPort handle to a ThermalPort handle → 1 ValidationResult with severity 'error', validatorId 'port_type', targets [{kind:'edge',edgeId}, {kind:'port', nodeId:srcId, portName:srcHandle}, {kind:'port', nodeId:tgtId, portName:tgtHandle}]; description names both component instance names and both port types.
    - Edge connecting two FlowPorts → no result.
    - Edge connecting two ThermalPorts → no result.
    - BCPort edge from WallTemperature → Channel.T_wall_left (allowed by isAllowedBCConnection) → no result.
    - BCPort edge from WallTemperature → HeatDiffusion (not in allow-list) → 1 ValidationResult with the same target shape.
    - Edge with missing source/target node or missing handle → no result (defensive; cannot validate).
  </behavior>
  <action>
    Create portType.ts exporting `export const portType: Validator = { id: 'port_type', severity: 'error', description: 'Incompatible port types', scope: ['edges'], run(snapshot) { ... } }`.

    Run logic:
      - Iterate snapshot.edges.
      - For each edge, resolve the source and target component definitions via snapshot.getComponentDef on the node's componentId (look up node by edge.source / edge.target from snapshot.nodes).
      - Resolve port objects by name match on def.ports against edge.sourceHandle and edge.targetHandle.
      - If both ports resolve:
          * If both are 'BCPort' → delegate to isAllowedBCConnection(srcCompId, tgtCompId); emit if false.
          * Else if port types differ → emit.
          * Else → no emit.
      - If a port is BCPort and the other is not → emit (mixed BCPort with non-BCPort is invalid).
      - Use a stable result id: `port_type::${edge.id}` (D-11 stable id for dedup).

    Targets: [{kind:'edge', edgeId: edge.id}, {kind:'port', nodeId: src, portName: srcHandle}, {kind:'port', nodeId: tgt, portName: tgtHandle}] — D-14 symmetric emission.

    Test file: write RED first per <behavior>. Use synthetic ComponentDefinition fixtures (Pump, Channel, WallTemperature, HeatDiffusion) with the ports each component actually exposes per gui/src/registry/components.json.

    Register in gui/src/lib/validation/index.ts: add `import { portType } from './rules/portType';` and push to the validators array.
  </action>
  <verify>
    <automated>cd gui && npm run test -- --run src/lib/validation/rules/__tests__/portType.test.ts</automated>
    <automated>cd gui && grep -c "portType" src/lib/validation/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - File gui/src/lib/validation/rules/portType.ts exists; exports `portType: Validator` with `id: 'port_type'`.
    - Test file has ≥6 `it(` blocks covering the cases above; all pass.
    - `gui/src/lib/validation/index.ts` imports portType and pushes it into the validators array.
    - `grep -c "useStore\|from \"react\"" gui/src/lib/validation/rules/portType.ts` returns 0.
  </acceptance_criteria>
  <done>portType rule is registered and behaviorally tested; ready for Plan 13's onConnect reroute consumer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: requiredConnections rule + tests</name>
  <files>gui/src/lib/validation/rules/requiredConnections.ts, gui/src/lib/validation/rules/__tests__/requiredConnections.test.ts</files>
  <read_first>
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-15 (rule 4: required-connections)
    - .planning/notes/gui-redesign-design-decisions.md §3.9 (rule definition — every required Port must have an attached edge)
    - gui/src/registry/components.json (Port.required boolean field — check whether the registry schema carries it; if not, use Port.required where present and fall back to type-based heuristic: FlowPort is required by default, ThermalPort is required only on ChannelAndContacts thermal_left/thermal_right pairs, BCPort is NEVER required)
    - gui/src/registry/types.ts (Port type definition — see if `required?: boolean` exists in the schema)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Architectural Responsibility Map"
  </read_first>
  <behavior>
    - Pump with port_in unconnected → 1 ValidationResult severity 'error' validatorId 'required_connections' targets [{kind:'port', nodeId, portName:'port_in'}, {kind:'node', nodeId}].
    - Pump with both ports connected → no result.
    - ChannelAndContacts with thermal_left[1] unconnected (and CAC.n=2) → 1 ValidationResult per missing thermal cell; description names the array index.
    - WallTemperature with BCPort unconnected → no result (BCPort is never required per the heuristic and §3.9).
    - HeatDiffusion with only some ThermalPort cells connected → 1 ValidationResult per missing cell.
  </behavior>
  <action>
    Create requiredConnections.ts exporting `requiredConnections: Validator` with id 'required_connections', severity 'error', scope ['nodes', 'edges'].

    Run logic: iterate snapshot.nodes; for each node resolve its ComponentDefinition; for each port determine if it is required (consult Port.required if the schema carries the field, else apply the heuristic: FlowPort always required, ThermalPort on ChannelAndContacts thermal_left/thermal_right arrays required, all others not). For array-shaped ports (e.g. thermal_left[1:n] on CAC) iterate from 1 to node.data.parameters.n and check each cell.

    For each unconnected required port, emit one ValidationResult with id `required_connections::${nodeId}::${portName}` (or `::${index}` for array cells), targets [{kind:'port', nodeId, portName}, {kind:'node', nodeId}] (D-14: emit both port + node so canvas red-ring AND port handle highlight).

    Description: "<instanceName>.<portName> requires a connection" (terse engineering voice; memory feedback_engineering_voice_copy).

    Test file: behavior block above, vitest, pure-function pattern from Plan 01.

    Register in index.ts.
  </action>
  <verify>
    <automated>cd gui && npm run test -- --run src/lib/validation/rules/__tests__/requiredConnections.test.ts</automated>
    <automated>cd gui && grep -c "requiredConnections" src/lib/validation/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - File gui/src/lib/validation/rules/requiredConnections.ts exists; exports `requiredConnections: Validator`.
    - Test file has ≥5 `it(` blocks covering the cases above; all pass.
    - Registry index.ts has 1 import + 1 push for requiredConnections.
    - Pure function: no store / React imports.
  </acceptance_criteria>
  <done>requiredConnections rule registered, tested, and ready for Wave-3 panel consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: danglingFlowPort rule + tests (VALD-01 fold per D-16)</name>
  <files>gui/src/lib/validation/rules/danglingFlowPort.ts, gui/src/lib/validation/rules/__tests__/danglingFlowPort.test.ts</files>
  <read_first>
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-16 (VALD-01 fold spec)
    - gui/src/lib/validation.ts:79-128 (validateTopology VALD-01 logic — fold this verbatim into the rule)
    - gui/src/lib/validation.test.ts (existing VALD-01 tests — copy the test cases for portability)
    - gui/src/lib/validation/rules/requiredConnections.ts (Task 2 — verify scope overlap: both rules check missing FlowPort connections. requiredConnections is the broader rule (covers ThermalPort cells too); danglingFlowPort is VALD-01's narrower, FlowPort-only check kept separate because §3.9 lists them as distinct rules. Description and id MUST differ to avoid dedup collision.)
  </read_first>
  <behavior>
    - Node with FlowPort port_in unconnected → 1 ValidationResult severity 'error' validatorId 'dangling_flow_port' targets [{kind:'port', nodeId, portName:'port_in'}].
    - Same node also flagged by requiredConnections — both results coexist (the panel renders both entries in v1 per D-11 "panel renders one entry per result"; cross-rule dedup is deferred per CONTEXT).
    - Node with all FlowPorts connected → no result.
    - ThermalPort or BCPort unconnected → no result (this rule is FlowPort-only).
  </behavior>
  <action>
    Create danglingFlowPort.ts exporting `danglingFlowPort: Validator` with id 'dangling_flow_port', severity 'error', scope ['nodes', 'edges']. The logic mirrors validateTopology lines 87-107 (existing nodeErrors loop): iterate snapshot.nodes, get the ComponentDefinition, filter ports by type==='FlowPort', for each port determine direction by name (isInput = port.name.includes('in')), check edges for a matching connection, emit if unconnected.

    Result shape: id `dangling_flow_port::${nodeId}::${portName}`, targets [{kind:'port', nodeId, portName}], description '<instanceName>.<portName> unconnected'.

    Note in the file's JSDoc header: "Folds VALD-01 from gui/src/lib/validation.ts:79-128 per D-16. Coexists with requiredConnections.ts (Task 2): this rule is FlowPort-specific (mirrors the legacy validateTopology behavior), requiredConnections is broader (covers ThermalPort cells too). Cross-rule dedup is deferred per CONTEXT.md."

    Test file: port the existing VALD-01 tests from gui/src/lib/validation.test.ts; adapt to the new ValidationSnapshot input + ValidationResult output shape. Add a test confirming ThermalPort/BCPort unconnected does NOT trigger this rule.

    Register in index.ts.
  </action>
  <verify>
    <automated>cd gui && npm run test -- --run src/lib/validation/rules/__tests__/danglingFlowPort.test.ts</automated>
    <automated>cd gui && grep -c "danglingFlowPort" src/lib/validation/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists; exports `danglingFlowPort: Validator`.
    - Test file has ≥4 `it(` blocks; all pass.
    - Registry index.ts has 1 import + 1 push for danglingFlowPort.
    - Pure function: no store / React imports.
    - The JSDoc header documents the VALD-01 fold + coexistence rationale.
  </acceptance_criteria>
  <done>danglingFlowPort rule registered as VALD-01's replacement; ready for Plan 13 to delete validation.ts.</done>
</task>

</tasks>

<verification>
- `cd gui && npm run test -- --run src/lib/validation/rules` passes (all three rule test files).
- `cd gui && grep -c "import.*from './rules" src/lib/validation/index.ts` returns 3.
- `cd gui && grep -rn "useStore\|from \"react\"" src/lib/validation/rules/portType.ts src/lib/validation/rules/requiredConnections.ts src/lib/validation/rules/danglingFlowPort.ts` returns nothing (D-06 invariant).
- `cd gui && npm run typecheck` produces no MORE errors than the Plan 01 baseline.
</verification>

<success_criteria>
- Three structural-graph rules ship and are registered in the explicit array (D-07).
- portType is the single source of truth that Plan 13 reroutes onConnect through (D-19).
- danglingFlowPort folds VALD-01 (D-16) so Plan 13 can delete validation.ts cleanly.
</success_criteria>

<output>
Create `.planning/phases/71-validation-framework/71-04-SUMMARY.md` when done. Include: per-rule test counts, line-count of each rule file, and confirmation that the registry array now lists 3 rules.
</output>
