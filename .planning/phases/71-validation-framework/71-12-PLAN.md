---
phase: 71-validation-framework
plan: 12
type: execute
wave: 7
depends_on: ["71-01", "71-03", "71-04", "71-05", "71-06", "71-07", "71-08", "71-09", "71-10", "71-11"]
files_modified:
  - gui/src/lib/exportCode.ts
  - gui/src/components/BottomPanel.tsx
  - gui/src/App.tsx
  - gui/src/components/ValidationDialog.tsx
autonomous: true
requirements: [D-15, D-17]
tags: [validation, export-gate, toast, dialog-deletion]

must_haves:
  truths:
    - "exportCode.ts no longer calls validateAndGate; instead it builds the snapshot, calls runValidators synchronously, and gates on error severity (D-17)."
    - "On error severity present: a sonner toast 'Export blocked: N validation errors. See Validation panel.' fires for ≤2s; bottomPanelOpen is set to true; activeBottomTab is set to 'validation'; statusbar chip pulses via the existing 0→N animation (Plan 10) because validationResults now contains errors."
    - "Export button in BottomPanel is disabled when hasNodes is false OR when at least one validationResults entry has severity 'error'; tooltip says 'Resolve N validation errors first' when error count > 0 (planner-baseline copy)."
    - "ValidationDialog.tsx is deleted; its import and mount are removed from App.tsx (D-17)."
    - "The legacy validateAndGate action remains on the store for now (Plan 13 deletes it together with validateTopology + clearValidation + validationResult slice) — but no caller in src/ should still call it after this plan."
  artifacts:
    - path: "gui/src/lib/exportCode.ts"
      provides: "Refactored exportCode using runValidators + sonner toast"
  key_links:
    - from: "gui/src/lib/exportCode.ts"
      to: "gui/src/lib/validation/runner.ts + gui/src/lib/validation/snapshot.ts"
      via: "synchronous validation gate"
      pattern: "runValidators\\("
    - from: "gui/src/lib/exportCode.ts"
      to: "gui/src/components/ui/sonner.tsx"
      via: "toast.error call"
      pattern: "toast\\.error"
---

<objective>
Rewire the export gate per D-17: synchronous runValidators call, sonner toast on error, auto-focus the Validation tab, disabled Export button when errors > 0, delete the legacy ValidationDialog modal.

Purpose: D-17 is the unification deliverable — the modal is replaced by the panel surface that Plans 09/10/11 just built. The export gate becomes the canonical pre-export hook (no modal interruption).

Output: rewritten exportCode.ts, disabled-Export-button gating in BottomPanel, App.tsx ValidationDialog cleanup, ValidationDialog.tsx deleted.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-validation-framework/71-CONTEXT.md
@.planning/phases/71-validation-framework/71-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite exportCode.ts to use runValidators + sonner toast</name>
  <files>gui/src/lib/exportCode.ts</files>
  <read_first>
    - gui/src/lib/exportCode.ts (full file — current validateAndGate call at lines 40-58)
    - gui/src/lib/validation/runner.ts (Plan 01 — runValidators contract)
    - gui/src/lib/validation/snapshot.ts (Plan 01 — buildValidationSnapshot helper)
    - gui/src/components/ui/sonner.tsx (Plan 03 — exports toast)
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-17 (exact gate copy + behavior)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Export Gate Replacement" (code example)
    - gui/src/registry/index.ts (getComponent — pass to buildValidationSnapshot)
  </read_first>
  <action>
    Rewrite exportCode.ts:

      ```
      import { save } from "@tauri-apps/plugin-dialog";
      import { writeTextFile } from "@tauri-apps/plugin-fs";
      import type { Node } from "@xyflow/react";
      import useStore from "../store/useStore";
      import { serializeSections, type CodeSection } from "./codeGenerator";
      import { buildValidationSnapshot } from "./validation/snapshot";
      import { runValidators } from "./validation/runner";
      import { toast } from "../components/ui/sonner";

      export interface ExportCodeOptions {
        sections: CodeSection[];
        nodes: Node[];
      }

      export async function exportCode(opts: ExportCodeOptions): Promise<boolean> {
        if (opts.nodes.length === 0) return false;

        const s = useStore.getState();
        const snapshot = buildValidationSnapshot(s);
        const results = runValidators(snapshot);
        const errorCount = results.filter(r => r.severity === 'error').length;

        // D-17: write the results to the store so the panel reflects the latest state
        // even when the debounced runner hasn't fired yet. errorNodeIds is derived
        // the same way initValidation does it (one-line repeat — extract to a helper
        // in Plan 01 follow-up if it shows up again).
        const errorNodeIds = new Set(
          results
            .filter(r => r.severity === 'error')
            .flatMap(r => r.targets)
            .filter(t => t.kind === 'node' || t.kind === 'port')
            .map(t => (t as { nodeId: string }).nodeId),
        );

        if (errorCount > 0) {
          toast.error(
            `Export blocked: ${errorCount} validation ${errorCount === 1 ? 'error' : 'errors'}. See Validation panel.`,
            { duration: 2000 },
          );
          useStore.setState({
            validationResults: results,
            errorNodeIds,
            bottomPanelOpen: true,
            activeBottomTab: 'validation',
          });
          return false;
        }

        // Even on success, propagate the fresh results so the panel matches the moment of export
        useStore.setState({ validationResults: results, errorNodeIds });

        const filePath = await save({
          defaultPath: "system.jl",
          filters: [{ name: "Julia files", extensions: ["jl"] }],
        });
        if (!filePath) return false;
        const code = serializeSections(opts.sections);
        await writeTextFile(filePath, code);
        return true;
      }
      ```

    Notes:
      - Do NOT call useStore.getState().validateAndGate() anywhere in this file. validateAndGate stays on the store until Plan 13 deletes it.
      - The two useStore.setState calls produce new Set/array references (Pitfall 1 — no in-place mutation).
      - errorNodeIds derivation is duplicated here once; if a third site appears, the executor extracts it to a helper in gui/src/lib/validation/snapshot.ts. For this plan, leave it inline.
  </action>
  <verify>
    <automated>cd gui && grep -c "validateAndGate" src/lib/exportCode.ts</automated>
    <automated>cd gui && grep -c "runValidators\|buildValidationSnapshot\|toast.error" src/lib/exportCode.ts</automated>
    <automated>cd gui && npm run test -- --run src/lib/exportCode 2>&1 | tail -20 || echo "no exportCode test (acceptable)"</automated>
    <automated>cd gui && npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "validateAndGate" gui/src/lib/exportCode.ts` returns 0.
    - `grep -c "runValidators" gui/src/lib/exportCode.ts` returns ≥ 1.
    - `grep -c "buildValidationSnapshot" gui/src/lib/exportCode.ts` returns ≥ 1.
    - `grep -c "toast.error" gui/src/lib/exportCode.ts` returns ≥ 1.
    - typecheck does not exceed baseline.
    - If gui/src/lib/__tests__/exportCode.test.ts (or similar) exists, it must pass with whatever test reorganization the contract change requires; if none exists, the typecheck gate is sufficient.
  </acceptance_criteria>
  <done>Export gate is the D-17 spec: synchronous run, toast on error, panel auto-focused, no modal.</done>
</task>

<task type="auto">
  <name>Task 2: Disable Export button when validation errors exist; tooltip; Toolbar parity</name>
  <files>gui/src/components/BottomPanel.tsx</files>
  <read_first>
    - gui/src/components/BottomPanel.tsx (full file — Export button at lines 153-162; current `disabled={!hasNodes}` predicate)
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-17 ("Export button in the chrome is disabled (with tooltip) when error count > 0")
    - gui/src/components/ui/tooltip.tsx (Tooltip wrapper — there's already a tooltip on the Copy button in BottomPanel, but Export currently has none; mirror the Tooltip wrap)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Finding 9"
  </read_first>
  <action>
    Edit BottomPanel.tsx:

      (a) Add a primitive-boolean subscription:
        ```
        const errorCount = useStore((s) => s.validationResults.filter(r => r.severity === 'error').length);
        ```
        (This is a derived primitive — re-renders only when the count crosses an integer boundary; Pitfall 1 doesn't apply because the closure runs filter().length each subscription, but the comparison is on the primitive.)

      (b) Change the Export <Button> disabled predicate from `disabled={!hasNodes}` to `disabled={!hasNodes || errorCount > 0}`.

      (c) Wrap the Export Button in a Tooltip (mirror the Copy button's Tooltip wrapping at lines 120-133 if necessary; otherwise add a fresh Tooltip). Tooltip content:
          - If errorCount > 0: `Resolve ${errorCount} validation ${errorCount === 1 ? 'error' : 'errors'} first`.
          - Else if !hasNodes: `Add components first`.
          - Else: omit the Tooltip wrapper OR set content to `Export generated Julia code to file` (existing aria-label is similar).

      Note: keep the existing aria-label attribute on the button.

    Do NOT touch the Copy button — Copy is allowed to succeed even with validation errors (it copies the code as-is; the user can take it elsewhere).
  </action>
  <verify>
    <automated>cd gui && grep -c "errorCount" src/components/BottomPanel.tsx</automated>
    <automated>cd gui && grep -c "Resolve.*validation" src/components/BottomPanel.tsx</automated>
    <automated>cd gui && npm run typecheck 2>&1 | tail -10</automated>
    <automated>cd gui && npm run test -- --run src/components 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "errorCount" gui/src/components/BottomPanel.tsx` returns ≥ 2 (selector + predicate).
    - `grep -c "Resolve" gui/src/components/BottomPanel.tsx` returns ≥ 1 (tooltip copy).
    - Export button uses `disabled={!hasNodes || errorCount > 0}`.
    - Component tests pass; typecheck no worse than baseline.
  </acceptance_criteria>
  <done>Export button is the visible signal; tooltip surfaces the count; D-17's "disabled-button is the primary signal" contract is satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Delete ValidationDialog.tsx and remove its App.tsx mount</name>
  <files>gui/src/components/ValidationDialog.tsx, gui/src/App.tsx</files>
  <read_first>
    - gui/src/App.tsx (full file — specifically line 15 import and line 587 mount)
    - gui/src/components/ValidationDialog.tsx (full file — to confirm no other consumers)
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-17 (delete, not deprecate)
  </read_first>
  <action>
    Step 1: Remove from App.tsx:
      - Delete line 15: `import ValidationDialog from "./components/ValidationDialog";`.
      - Delete line 587: `<ValidationDialog />`.

    Step 2: Delete the file gui/src/components/ValidationDialog.tsx entirely (do not leave a stub, do not add a deprecation shim — D-17 is explicit).

    Step 3: Verify nothing else imports it:
      ```
      grep -rn "ValidationDialog" gui/src/
      ```
      Should return zero results (or only references inside .planning/ comments). If any other file imports it, fix that file in this task as well — likely Toolbar.tsx had a reference (pre-Phase-65 Toolbar.tsx was deleted; sanity-check anyway).
  </action>
  <verify>
    <automated>cd gui && test ! -f src/components/ValidationDialog.tsx && echo "deleted"</automated>
    <automated>cd gui && grep -rn "ValidationDialog" src/ | tee /tmp/validation-dialog-refs.txt</automated>
    <automated>cd gui && npm run typecheck 2>&1 | tail -15</automated>
    <automated>cd gui && npm run test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File gui/src/components/ValidationDialog.tsx does NOT exist after this task.
    - `grep -rn "ValidationDialog" gui/src/` returns NOTHING.
    - npm run typecheck does not exceed baseline.
    - npm run test still passes (no test depended on ValidationDialog).
  </acceptance_criteria>
  <done>ValidationDialog is physically removed; the modal-based gate is dead code-wise.</done>
</task>

</tasks>

<verification>
- `cd gui && npm run test` passes overall.
- `cd gui && npm run typecheck` does not exceed Plan 01 baseline.
- `cd gui && grep -rn "ValidationDialog" src/` returns nothing.
- `cd gui && grep -rn "validateAndGate" src/ | grep -v "useStore.ts"` returns nothing (only the store's own action remains; no external caller).
- Manual sanity (executor optional): launch the dev server, click Export with validation errors → toast appears, BottomPanel switches to Validation tab, statusbar pulses, Export button is disabled. (Automated UAT is Plan 14/verify-work, not this plan.)
</verification>

<success_criteria>
- D-17 fully implemented: synchronous gate, toast, auto-open Validation tab, disabled Export button, ValidationDialog deleted.
- No back-compat shim — D-17's "delete, not deprecate" is honored.
</success_criteria>

<output>
Create `.planning/phases/71-validation-framework/71-12-SUMMARY.md` when done. Include: typecheck error count vs baseline, confirmation that grep finds no ValidationDialog references, and the exact tooltip strings used.
</output>
