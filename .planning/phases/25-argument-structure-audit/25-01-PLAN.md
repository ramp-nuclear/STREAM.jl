---
phase: 25-argument-structure-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/resistors.jl
  - src/components/misc.jl
  - src/physical_models/correlations.jl
  - src/examples.jl
  - test/test_resistors.jl
  - test/test_misc.jl
  - test/test_channel.jl
  - test/test_composition.jl
  - test/test_correlations.jl
  - test/test_flapper.jl
  - test/test_pump.jl
  - test/test_sign_safety.jl
  - test/test_heat_diffusion.jl
  - test/test_validation.jl
  - test/test_loss_of_flow.jl
  - examples/lof_transient.jl
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Resistor, Gravity, Inertia, HeatExchanger, ConstantTemperature accept their physics parameter as a positional argument"
    - "laminar_friction accepts aspect_ratio as a positional argument"
    - "name stays keyword-only on all components"
    - "Complex multi-arg constructors (Channel, ChannelAndContacts, etc.) remain keyword-only"
    - "Full test suite passes with zero failures"
    - "CLAUDE.md documents the two-tier positional/keyword rule"
  artifacts:
    - path: "src/components/resistors.jl"
      provides: "Resistor(R; name), Gravity(H; name) signatures"
      contains: "function Resistor(R; name)"
    - path: "src/components/misc.jl"
      provides: "Inertia(L_over_A; name), HeatExchanger(T_bc; name), ConstantTemperature(T; name) signatures"
      contains: "function Inertia(L_over_A; name)"
    - path: "src/physical_models/correlations.jl"
      provides: "laminar_friction(aspect_ratio::Real) positional signature"
      contains: "function laminar_friction(aspect_ratio::Real)"
    - path: "CLAUDE.md"
      provides: "Two-tier argument convention rule"
      contains: "Positional when"
  key_links:
    - from: "src/components/resistors.jl"
      to: "test/test_resistors.jl"
      via: "positional call sites"
      pattern: "Resistor\\(\\d"
    - from: "src/components/misc.jl"
      to: "src/examples.jl"
      via: "positional call sites in build_loop helpers"
      pattern: "HeatExchanger\\([A-Z_a-z]"
---

<objective>
Sweep all 6 targeted function signatures from keyword-only to positional, update every call site across src/ and test/, and update CLAUDE.md with the new two-tier convention rule.

Purpose: Align the API with idiomatic Julia conventions -- positional args for single-parameter components, keyword-only for complex multi-arg constructors. This is a pure refactoring pass with no behavior changes.
Output: Updated source signatures, migrated call sites, updated CLAUDE.md, green test suite.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-argument-structure-audit/25-CONTEXT.md
@.planning/phases/25-argument-structure-audit/25-RESEARCH.md

<interfaces>
<!-- Existing Pump pattern to replicate (from src/components/pump.jl) -->
```julia
# Target pattern: positional physics arg + keyword name
function Pump(dP_pump::Real; name)
    ...
end
```

<!-- @named macro usage -->
```julia
# @named injects name=:varname as keyword
@named r = Resistor(1.0e5)    # becomes Resistor(1.0e5; name=:r)

# Dynamic names in comprehensions pass name explicitly
ct = [ConstantTemperature(T_bc; name=Symbol(:ct_, i)) for i in 1:n]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change 6 signatures and update all call sites</name>
  <read_first>
    src/components/resistors.jl
    src/components/misc.jl
    src/physical_models/correlations.jl
    src/examples.jl
    test/test_resistors.jl
    test/test_misc.jl
    test/test_channel.jl
    test/test_composition.jl
    test/test_correlations.jl
    test/test_flapper.jl
    test/test_pump.jl
    test/test_sign_safety.jl
    test/test_heat_diffusion.jl
    test/test_validation.jl
    test/test_loss_of_flow.jl
    examples/lof_transient.jl
  </read_first>
  <files>
    src/components/resistors.jl
    src/components/misc.jl
    src/physical_models/correlations.jl
    src/examples.jl
    test/test_resistors.jl
    test/test_misc.jl
    test/test_channel.jl
    test/test_composition.jl
    test/test_correlations.jl
    test/test_flapper.jl
    test/test_pump.jl
    test/test_sign_safety.jl
    test/test_heat_diffusion.jl
    test/test_validation.jl
    test/test_loss_of_flow.jl
    examples/lof_transient.jl
  </files>
  <action>
Change 6 function signatures from keyword-only to positional, per D-01 and D-04. Then update every call site across src/ and test/.

**Signature changes (3 source files):**

1. `src/components/resistors.jl` -- change Resistor and Gravity:
   - `function Resistor(; name, R)` --> `function Resistor(R; name)`
   - `function Gravity(; name, H)` --> `function Gravity(H; name)`
   - Update docstring first line to match new signature on each

2. `src/components/misc.jl` -- change Inertia, HeatExchanger, ConstantTemperature:
   - `function Inertia(; name, L_over_A)` --> `function Inertia(L_over_A; name)`
   - `function HeatExchanger(; name, T_bc)` --> `function HeatExchanger(T_bc; name)`
   - `function ConstantTemperature(; name, T)` --> `function ConstantTemperature(T; name)`
   - Update docstring first line to match new signature on each

3. `src/physical_models/correlations.jl` -- change laminar_friction:
   - `function laminar_friction(; aspect_ratio::Real)` --> `function laminar_friction(aspect_ratio::Real)`
   - Update docstring first line to match

**Call site migrations (complete inventory from RESEARCH.md):**

4. `src/examples.jl`:
   - `HeatExchanger(T_bc = T_inlet)` --> `HeatExchanger(T_inlet)` (and variants with/without spaces around `=`)
   - `Gravity(H = H)` --> `Gravity(H)`
   - `Resistor(R=R)` --> `Resistor(R)` (9 occurrences in build_cube)
   - `Inertia(L_over_A=L_over_A)` --> `Inertia(L_over_A)`
   - `Resistor(R=R_ext)` --> `Resistor(R_ext)`

5. `test/test_resistors.jl`:
   - `Resistor(R=1.0e5)` --> `Resistor(1.0e5)` (2 occurrences)

6. `test/test_misc.jl`:
   - `Inertia(L_over_A=1e3)` --> `Inertia(1e3)` (2 occurrences)
   - `Inertia(L_over_A=L_over_A)` --> `Inertia(L_over_A)`
   - `Resistor(R=R_val)` --> `Resistor(R_val)`
   - `HeatExchanger(T_bc=313.15)` --> `HeatExchanger(313.15)` (2 occurrences)

7. `test/test_channel.jl`:
   - `Gravity(H=3.0)` --> `Gravity(3.0)`
   - `HeatExchanger(T_bc=T_inlet)` --> `HeatExchanger(T_inlet)` (4 occurrences)
   - `ConstantTemperature(name=Symbol(:ct_l, i), T=T_wall)` --> `ConstantTemperature(T_wall; name=Symbol(:ct_l, i))` (array comprehension pattern)
   - `ConstantTemperature(name=Symbol(:ct_r, i), T=T_wall)` --> `ConstantTemperature(T_wall; name=Symbol(:ct_r, i))`
   - `ConstantTemperature(name=Symbol(:ct2_, i), T=T_wall)` --> `ConstantTemperature(T_wall; name=Symbol(:ct2_, i))`
   - `ConstantTemperature(T=373.15)` --> `ConstantTemperature(373.15)` (@named form)

8. `test/test_composition.jl`:
   - `HeatExchanger(T_bc=T_inlet_qol)` --> `HeatExchanger(T_inlet_qol)` (~10 occurrences)
   - `ConstantTemperature(name=Symbol(...), T=T_wall_qol)` --> `ConstantTemperature(T_wall_qol; name=Symbol(...))` (array comprehensions)
   - `laminar_friction(aspect_ratio=0.0025/0.070)` --> `laminar_friction(0.0025/0.070)` (6 occurrences)

9. `test/test_correlations.jl`:
   - `laminar_friction(aspect_ratio=0.01814)` --> `laminar_friction(0.01814)` (2 occurrences)
   - `laminar_friction(aspect_ratio=geom.depth/geom.width)` --> `laminar_friction(geom.depth/geom.width)` (4 occurrences)
   - `HeatExchanger(T_bc=T_inlet)` --> `HeatExchanger(T_inlet)` (6 occurrences)
   - `ConstantTemperature(name=Symbol(...), T=T_wall)` --> `ConstantTemperature(T_wall; name=Symbol(...))` (6 array comprehensions)

10. `test/test_flapper.jl`:
    - `Resistor(R=1e5)` --> `Resistor(1e5)` (2 occurrences)
    - `Inertia(L_over_A=L_over_A)` --> `Inertia(L_over_A)`

11. `test/test_pump.jl`:
    - `HeatExchanger(T_bc=313.15)` --> `HeatExchanger(313.15)` (2 occurrences)
    - `Inertia(L_over_A=L_over_A)` --> `Inertia(L_over_A)`
    - `Resistor(R=R_val)` --> `Resistor(R_val)`

12. `test/test_sign_safety.jl`:
    - `HeatExchanger(T_bc=T_inlet_sign)` --> `HeatExchanger(T_inlet_sign)` (3 occurrences)
    - `ConstantTemperature(name=Symbol(:ct_l, i), T=T_wall_sign)` --> `ConstantTemperature(T_wall_sign; name=Symbol(:ct_l, i))`
    - `ConstantTemperature(name=Symbol(:ct_r, i), T=T_wall_sign)` --> `ConstantTemperature(T_wall_sign; name=Symbol(:ct_r, i))`

13. `test/test_heat_diffusion.jl`:
    - `ConstantTemperature(name=Symbol(:ct_l, i), T=T_bc)` --> `ConstantTemperature(T_bc; name=Symbol(:ct_l, i))`
    - `ConstantTemperature(name=Symbol(:ct_r, i), T=T_bc)` --> `ConstantTemperature(T_bc; name=Symbol(:ct_r, i))`
    - `ConstantTemperature(name=Symbol(:ct5_l, i), T=T_bc)` --> `ConstantTemperature(T_bc; name=Symbol(:ct5_l, i))`
    - `ConstantTemperature(name=Symbol(:ct12_l, i), T=T_bc)` --> `ConstantTemperature(T_bc; name=Symbol(:ct12_l, i))`
    - `ConstantTemperature(name=Symbol(:ct12_r, i), T=T_bc)` --> `ConstantTemperature(T_bc; name=Symbol(:ct12_r, i))`

14. `test/test_validation.jl`:
    - `HeatExchanger(T_bc=T_in_l)` --> `HeatExchanger(T_in_l)` (and T_in_r, T_in, T_in_v02 variants)
    - `ConstantTemperature(name=Symbol(:ct_l_, i), T=T_wall)` --> `ConstantTemperature(T_wall; name=Symbol(:ct_l_, i))`
    - `ConstantTemperature(name=Symbol(:ct_r_, i), T=T_wall)` --> `ConstantTemperature(T_wall; name=Symbol(:ct_r_, i))`

15. `test/test_loss_of_flow.jl`:
    - `HeatExchanger(T_bc=BYPASS_T_INLET)` --> `HeatExchanger(BYPASS_T_INLET)`

16. `examples/lof_transient.jl`:
    - Check for any keyword call sites for affected components and update to positional

**CRITICAL: After all edits, run a comprehensive grep to confirm zero remaining old-style call sites:**
```
grep -rn 'Resistor(R=' src/ test/ examples/
grep -rn 'Gravity(H=' src/ test/ examples/
grep -rn 'Inertia(L_over_A=' src/ test/ examples/
grep -rn 'HeatExchanger(T_bc=' src/ test/ examples/
grep -rn 'ConstantTemperature(.*T=' src/ test/ examples/
grep -rn 'laminar_friction(aspect_ratio=' src/ test/ examples/
```
All must return zero results. Also grep for `ConstantTemperature(name=` to confirm no remaining old-style comprehension patterns (valid new form is `ConstantTemperature(value; name=`).

Per D-03: No backward-compat shims. Old keyword form is deleted. MethodError forces migration.
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM && julia --project test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/resistors.jl` contains `function Resistor(R; name)` and `function Gravity(H; name)`
    - `src/components/misc.jl` contains `function Inertia(L_over_A; name)` and `function HeatExchanger(T_bc; name)` and `function ConstantTemperature(T; name)`
    - `src/physical_models/correlations.jl` contains `function laminar_friction(aspect_ratio::Real)`
    - `grep -rn 'Resistor(R=' src/ test/ examples/` returns zero results
    - `grep -rn 'Gravity(H=' src/ test/ examples/` returns zero results
    - `grep -rn 'Inertia(L_over_A=' src/ test/ examples/` returns zero results
    - `grep -rn 'HeatExchanger(T_bc=' src/ test/ examples/` returns zero results
    - `grep -rn 'laminar_friction(aspect_ratio=' src/ test/ examples/` returns zero results
    - `grep -rn 'ConstantTemperature(name=.*T=' src/ test/ examples/` returns zero results (old comprehension pattern gone)
    - `julia --project test/runtests.jl` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 6 signatures changed to positional. All ~60 call sites migrated. Full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with two-tier convention rule</name>
  <read_first>
    CLAUDE.md
  </read_first>
  <files>CLAUDE.md</files>
  <action>
Per D-15, replace the first two bullet points in the "## Component authoring conventions" section of CLAUDE.md.

**Replace this text (lines 61-64):**
```
- All component constructor arguments are **keyword-only** (no positional args). Matches MTK convention everywhere.
  *Why: Keyword-only prevents argument-order bugs and makes call sites self-documenting. MTK's own `@mtkmodel` macro generates keyword-only constructors.*
- Factory functions (`PipeGeometry_rectangular`, `PipeGeometry_circular`) are also keyword-only.
  *Why: Consistency with component constructors. Mixing positional and keyword styles across the API creates confusion.*
```

**With this text:**
```
- **Positional arguments** when: (a) the argument type determines behavior, enabling multiple dispatch (e.g., `Pump(dP::Real; name)` vs `Pump(dP::Any; name)`); OR (b) the constructor/function has 1 or fewer physics parameters and its role is unambiguous from the function name (e.g., `Resistor(R; name)`, `Gravity(H; name)`, `HeatExchanger(T_bc; name)`).
  *Why: Positional args with type annotations enable Julia's multiple dispatch and keep call sites concise when the meaning is obvious.*
- **Keyword arguments** when: multiple arguments of the same type where labeling prevents order bugs (e.g., `Channel(; name, L, Dh, ...)` has many Float64 params); OR complex constructors with many parameters where self-documentation outweighs brevity.
  *Why: Keyword-only prevents argument-order bugs for multi-parameter constructors where positional would be ambiguous.*
- Factory functions (`PipeGeometry_rectangular`, `PipeGeometry_circular`) use positional arguments (established in v0.4).
- The `name` kwarg is **always keyword-only** (provided by `@named` macro, never positional).
  *Why: The `@named` macro injects `name=:varname` as a keyword argument. Making it positional would break `@named`.*
```

Keep the remaining bullets (internal helpers with `_` prefix, docstring rule) unchanged.
  </action>
  <verify>
    <automated>grep -c "Positional arguments" /home/itay/projects/Julia-STREAM/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "Positional arguments" in the Component authoring conventions section
    - CLAUDE.md contains "Keyword arguments" in the Component authoring conventions section
    - CLAUDE.md contains "name kwarg is **always keyword-only**"
    - CLAUDE.md does NOT contain "All component constructor arguments are **keyword-only**"
    - CLAUDE.md does NOT contain "Factory functions (`PipeGeometry_rectangular`, `PipeGeometry_circular`) are also keyword-only"
  </acceptance_criteria>
  <done>CLAUDE.md updated with the two-tier positional/keyword convention rule per D-15.</done>
</task>

</tasks>

<verification>
1. All 6 function signatures are positional: `grep -n 'function Resistor\|function Gravity\|function Inertia\|function HeatExchanger\|function ConstantTemperature\|function laminar_friction' src/components/resistors.jl src/components/misc.jl src/physical_models/correlations.jl`
2. No old-style keyword call sites remain: `grep -rn 'Resistor(R=\|Gravity(H=\|Inertia(L_over_A=\|HeatExchanger(T_bc=\|laminar_friction(aspect_ratio=' src/ test/ examples/`
3. Full test suite green: `cd /home/itay/projects/Julia-STREAM && julia --project test/runtests.jl`
4. CLAUDE.md has new rule: `grep 'Positional arguments' CLAUDE.md`
</verification>

<success_criteria>
- Six function signatures changed from keyword-only to positional
- All ~60 call sites across 14+ files migrated
- Full test suite passes (zero failures, zero errors)
- CLAUDE.md documents the two-tier positional/keyword convention
- No backward-compatibility shims exist
</success_criteria>

<output>
After completion, create `.planning/phases/25-argument-structure-audit/25-01-SUMMARY.md`
</output>
