---
phase: 17-file-structure-reorganization
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/physical_models/correlations.jl
  - src/composition/helpers.jl
  - src/examples.jl
  - src/solvers.jl
  - src/STREAM.jl
  - src/correlations.jl
  - src/helpers.jl
autonomous: true
requirements:
  - STR-03
  - STR-04
  - STR-05

must_haves:
  truths:
    - "src/physical_models/correlations.jl exists; src/correlations.jl is deleted"
    - "src/composition/helpers.jl exists; src/helpers.jl is deleted"
    - "src/examples.jl contains build_loop, build_loop_vertical, build_loop_transient, build_cube"
    - "src/solvers.jl no longer contains build_loop, build_loop_vertical, build_loop_transient, build_cube"
    - "STREAM.jl uses the canonical 13-include list from CONTEXT.md"
    - "All 161 tests pass after all three moves"
  artifacts:
    - path: "src/physical_models/correlations.jl"
      provides: "dittus_boelter, blasius_friction, constant_Nusselt, laminar_friction, rectangular_laminar_correction, regime_dependent"
      contains: "dittus_boelter"
    - path: "src/composition/helpers.jl"
      provides: "symmetric_plate, plate, one_sided_connection, compose_systems, port, check_gravity_mismatch"
      contains: "symmetric_plate"
    - path: "src/examples.jl"
      provides: "build_loop, build_loop_vertical, build_loop_transient, build_cube"
      contains: "build_cube"
  key_links:
    - from: "src/STREAM.jl"
      to: "src/physical_models/correlations.jl"
      via: "include at position 4 in the final 13-include list"
      pattern: "include.*physical_models/correlations"
    - from: "src/STREAM.jl"
      to: "src/examples.jl"
      via: "include at position 13 (last) in the final 13-include list"
      pattern: "include.*examples"
    - from: "src/components/channel.jl"
      to: "dittus_boelter"
      via: "function call — correlations.jl is included at position 4, before channel.jl at position 5"
      pattern: "dittus_boelter"
---

<objective>
Move src/correlations.jl to src/physical_models/correlations.jl, move src/helpers.jl to src/composition/helpers.jl, and extract the four example-builder functions from src/solvers.jl into a new src/examples.jl. Update src/STREAM.jl to the final canonical 13-include list. Delete the old src/correlations.jl and src/helpers.jl.

Purpose: Completes the canonical file layout — correlations under physical_models/ (matching Python STREAM naming), helpers under composition/, and solver logic isolated from demo code in examples.jl.
Output: src/physical_models/correlations.jl, src/composition/helpers.jl, src/examples.jl created; src/correlations.jl and src/helpers.jl deleted; src/solvers.jl trimmed; src/STREAM.jl updated to final 13-include list.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-file-structure-reorganization/17-CONTEXT.md
@.planning/phases/17-file-structure-reorganization/17-RESEARCH.md
@.planning/phases/17-file-structure-reorganization/17-01-SUMMARY.md

<interfaces>
<!-- Current state after Plan 01 — src/STREAM.jl interim 11-include list: -->
```julia
include("fluids.jl")
include("connectors.jl")
include("geometry.jl")
include("correlations.jl")
include("components/channel.jl")
include("components/pump.jl")
include("components/resistors.jl")
include("components/misc.jl")
include("components/thermal_channel.jl")
include("components/heat_diffusion.jl")
include("helpers.jl")
include("solvers.jl")
```

<!-- Target src/STREAM.jl final 13-include list (per CONTEXT.md locked decision): -->
```julia
include("fluids.jl")
include("connectors.jl")
include("geometry.jl")
include("physical_models/correlations.jl")
include("components/channel.jl")
include("components/pump.jl")
include("components/resistors.jl")
include("components/misc.jl")
include("components/thermal_channel.jl")
include("components/heat_diffusion.jl")
include("composition/helpers.jl")
include("solvers.jl")
include("examples.jl")
```

<!-- Functions to STAY in src/solvers.jl (per CONTEXT.md locked decision): -->
<!--   steady_state_guess (lines 20–24) -->
<!--   solve_steady (lines 99–108) -->
<!--   solve_transient (lines 251–274) -->

<!-- Functions to MOVE from src/solvers.jl to src/examples.jl: -->
<!--   build_loop (lines 53–83 approx — read current file for exact boundaries) -->
<!--   build_loop_vertical (lines 134–178 approx) -->
<!--   build_loop_transient (lines 196–232 approx) -->
<!--   build_cube (lines 299–342 approx) -->

<!-- solvers.jl convention: Leave the `using ModelingToolkit`, `using DifferentialEquations`, -->
<!-- `using Sundials` statements at the top of solvers.jl UNTOUCHED — no cleanup in this phase. -->

<!-- examples.jl convention: NO `export`, NO `using`, NO `include` statements. -->
<!-- All referenced symbols (Pump, Channel, HeatExchanger, etc.) are in module scope. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move correlations.jl and helpers.jl (STR-03, STR-04)</name>
  <files>src/physical_models/correlations.jl, src/composition/helpers.jl, src/STREAM.jl, src/correlations.jl, src/helpers.jl</files>
  <read_first>
    - src/correlations.jl (full file — copy verbatim to new path)
    - src/helpers.jl (full file — copy verbatim to new path)
    - src/STREAM.jl (current interim 11-include list — to update two paths)
  </read_first>
  <action>
1. Create `src/physical_models/` directory.
2. Create `src/physical_models/correlations.jl` with the EXACT content of `src/correlations.jl` — no changes to the content whatsoever.
3. Create `src/composition/` directory.
4. Create `src/composition/helpers.jl` with the EXACT content of `src/helpers.jl` — no changes to the content whatsoever.
5. Delete `src/correlations.jl`.
6. Delete `src/helpers.jl`.
7. Update `src/STREAM.jl`: replace `include("correlations.jl")` with `include("physical_models/correlations.jl")` and replace `include("helpers.jl")` with `include("composition/helpers.jl")`. The include list should now be 12 includes (without examples.jl yet — that comes in Task 2).

After these changes, verify the module still loads:
```bash
cd /home/itay/projects/Julia-STREAM && julia --project=. -e 'using STREAM; @assert isdefined(STREAM, :dittus_boelter); @assert isdefined(STREAM, :symmetric_plate); println("correlations + helpers ok")'
```
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM && julia --project=. -e 'using STREAM; @assert isdefined(STREAM, :dittus_boelter); @assert isdefined(STREAM, :symmetric_plate); println("ok")'</automated>
  </verify>
  <acceptance_criteria>
    - `src/physical_models/correlations.jl` exists: `ls src/physical_models/correlations.jl` succeeds
    - `grep "dittus_boelter" src/physical_models/correlations.jl` returns a match
    - `src/correlations.jl` does NOT exist: `ls src/correlations.jl` returns error
    - `src/composition/helpers.jl` exists: `ls src/composition/helpers.jl` succeeds
    - `grep "symmetric_plate" src/composition/helpers.jl` returns a match
    - `src/helpers.jl` does NOT exist: `ls src/helpers.jl` returns error
    - `grep "include.*physical_models/correlations" src/STREAM.jl` returns a match
    - `grep "include.*composition/helpers" src/STREAM.jl` returns a match
    - `grep "include.*correlations.jl\"" src/STREAM.jl` returns NO match (old path gone)
    - `grep "include.*helpers.jl\"" src/STREAM.jl` returns NO match (old path gone)
    - `julia --project=. -e 'using STREAM; println("ok")'` exits 0
  </acceptance_criteria>
  <done>correlations.jl at new path under physical_models/; helpers.jl at new path under composition/; old files deleted; STREAM.jl updated; module loads</done>
</task>

<task type="auto">
  <name>Task 2: Extract example builders into src/examples.jl (STR-05)</name>
  <files>src/examples.jl, src/solvers.jl, src/STREAM.jl</files>
  <read_first>
    - src/solvers.jl (full file — read to identify exact line boundaries of the 4 functions to extract and the content to keep)
    - src/STREAM.jl (current 12-include list — to add examples.jl as the final include)
  </read_first>
  <action>
1. Read `src/solvers.jl` in full. Identify the four functions to move:
   - `build_loop` (with its comment block starting `# ---- build_loop ----` or similar — read the actual file for exact boundaries)
   - `build_loop_vertical` (with its comment block)
   - `build_loop_transient` (with its comment block)
   - `build_cube` (with its comment block)

   The research identified approximate line boundaries: build_loop ~lines 53–83, build_loop_vertical ~lines 134–178, build_loop_transient ~lines 196–232, build_cube ~lines 299–342. Read the actual file to confirm exact boundaries — each function starts with a `# ---` separator comment and ends at the next such separator or EOF.

2. Create `src/examples.jl` with a file header comment:
   ```julia
   # examples.jl — Example system builders for STREAM.jl
   # build_loop, build_loop_vertical, build_loop_transient, build_cube
   ```
   Then paste the four functions in their original order with their separator comment blocks intact.

   DO NOT add `export`, `using`, or `include` statements. All referenced symbols (Pump, Channel, HeatExchanger, Gravity, Resistor, PipeGeometry_circular, System, compose, mtkcompile, connect, @named, @parameters, t, cp_water, rho_water) are in module scope.

3. Remove the four functions and their separator comment blocks from `src/solvers.jl`. Leave the `using` statements at lines 8–11, the file header comment at lines 1–7, and the three remaining functions:
   - `steady_state_guess` — STAYS
   - `solve_steady` — STAYS
   - `solve_transient` — STAYS

   Do NOT remove the `using ModelingToolkit`, `using DifferentialEquations`, `using Sundials` lines — leave those untouched per locked decision.

4. Update `src/STREAM.jl` to add `include("examples.jl")` as the 13th and final include, after `include("solvers.jl")`. The complete final include list must be exactly:
   ```julia
   include("fluids.jl")
   include("connectors.jl")
   include("geometry.jl")
   include("physical_models/correlations.jl")
   include("components/channel.jl")
   include("components/pump.jl")
   include("components/resistors.jl")
   include("components/misc.jl")
   include("components/thermal_channel.jl")
   include("components/heat_diffusion.jl")
   include("composition/helpers.jl")
   include("solvers.jl")
   include("examples.jl")
   ```

5. Run full test suite to confirm zero regressions:
   ```bash
   cd /home/itay/projects/Julia-STREAM && julia --project=. -e 'using Pkg; Pkg.test()'
   ```
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM && julia --project=. -e 'using Pkg; Pkg.test()'</automated>
  </verify>
  <acceptance_criteria>
    - `src/examples.jl` exists and contains all four functions: `grep -c "function build_loop\b\|function build_loop_vertical\|function build_loop_transient\|function build_cube" src/examples.jl` returns 4
    - `grep "build_loop\|build_cube" src/solvers.jl` returns NO match (functions removed from solvers.jl)
    - `grep "steady_state_guess" src/solvers.jl` returns a match (stays in solvers.jl)
    - `grep "solve_steady\|solve_transient" src/solvers.jl` returns matches (stay in solvers.jl)
    - `grep "using ModelingToolkit" src/solvers.jl` returns a match (using statements untouched)
    - `src/examples.jl` does NOT contain `export`, `using`, or `include`
    - `grep "include.*examples" src/STREAM.jl` returns `include("examples.jl")`
    - `grep -c "include(" src/STREAM.jl` returns 13 (exactly 13 include calls)
    - `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0 with all tests passing
  </acceptance_criteria>
  <done>src/examples.jl created with all 4 builder functions; solvers.jl trimmed to solver-only logic; STREAM.jl has final 13-include canonical list; all tests pass</done>
</task>

</tasks>

<verification>
Final state check after both tasks:
```bash
# Canonical structure verification
ls /home/itay/projects/Julia-STREAM/src/
# Expected: STREAM.jl components/ composition/ connectors.jl examples.jl fluids.jl geometry.jl physical_models/ solvers.jl
# NOT present: components.jl correlations.jl helpers.jl

ls /home/itay/projects/Julia-STREAM/src/components/
# Expected: channel.jl heat_diffusion.jl misc.jl pump.jl resistors.jl thermal_channel.jl

ls /home/itay/projects/Julia-STREAM/src/physical_models/
# Expected: correlations.jl

ls /home/itay/projects/Julia-STREAM/src/composition/
# Expected: helpers.jl

# Include count
grep -c 'include(' /home/itay/projects/Julia-STREAM/src/STREAM.jl
# Expected: 13

# Full test suite
cd /home/itay/projects/Julia-STREAM && julia --project=. -e 'using Pkg; Pkg.test()'
# Expected: all tests pass, zero failures
```
</verification>

<success_criteria>
- `src/physical_models/correlations.jl` exists with correlation functions; `src/correlations.jl` deleted
- `src/composition/helpers.jl` exists with composition helpers; `src/helpers.jl` deleted
- `src/examples.jl` exists with build_loop, build_loop_vertical, build_loop_transient, build_cube
- `src/solvers.jl` contains only solve_steady, solve_transient, steady_state_guess (plus using statements)
- `src/STREAM.jl` has exactly 13 include calls matching the canonical order from CONTEXT.md
- All tests pass (zero regressions from v0.4 baseline)
- `ls src/` shows the canonical layout from CLAUDE.md with no old flat files remaining
</success_criteria>

<output>
After completion, create `.planning/phases/17-file-structure-reorganization/17-02-SUMMARY.md`
</output>
