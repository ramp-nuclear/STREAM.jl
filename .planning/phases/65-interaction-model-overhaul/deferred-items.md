# Deferred Items — Phase 65

Issues discovered during execution that are out of scope for the current task.

- **Plan 10 — pre-existing failure:** `gui/src/components/sidebar/__tests__/SidebarPanel.anchors.test.tsx > "Channel BCs tab body still renders the existing BCsTabForm content below Anchors"` fails on the Plan 10 baseline (before any Plan 10 edits). Out of scope per executor scope-boundary rule — not caused by Plan 10 changes.
