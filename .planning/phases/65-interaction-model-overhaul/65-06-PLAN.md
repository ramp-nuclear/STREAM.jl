---
phase: 65-interaction-model-overhaul
plan: 06
type: execute
wave: 4
depends_on: [65-05]
files_modified:
  - gui/src/store/useStore.ts
  - gui/src/lib/projectIO.ts
  - gui/src/components/CanvasPanel.tsx
  - gui/src/components/canvasMenus/SnapToGridButton.tsx
  - gui/src/components/__tests__/SnapToGridButton.test.tsx
  - gui/src/lib/__tests__/projectIO.snapToGrid.test.ts
autonomous: false
requirements: []
tags: [gui, snap-to-grid, layout, scp-persistence, phase-65]

must_haves:
  truths:
    - "Decisions addressed: D-07 (canvas-overlay button surface in v1.2, Settings dialog deferred), D-08 (16px fixed grid matching ReactFlow Background gap=16), D-09 (ReactFlow built-in snapToGrid + snapGrid={[16,16]} props, no custom snap), D-10 (default OFF + persisted per-project in .scp layout block)."
    - "A small button with a Grid icon appears in the top-right canvas overlay stack alongside the existing Controls / MiniMap."
    - "Clicking the button toggles its visual state (active vs inactive) and toggles ReactFlow's snapToGrid behavior live."
    - "When snap is ON, dragging a node end-position quantizes to a multiple of 16px on both axes."
    - "When snap is ON, dropping a toolbox component onto the canvas also lands on a 16px multiple."
    - "Snap state defaults to OFF on new projects."
    - "Toggling snap sets isDirty: true so the next Save persists it."
    - "The .scp file's layout block now contains snap_to_grid: boolean alongside active_left_tab and active_layer."
    - "Loading a project with snap_to_grid: true restores the toggle to ON; loading a project without the field defaults to OFF (RESEARCH Pitfall 3 — empty-state tolerance)."
  artifacts:
    - path: "gui/src/lib/projectIO.ts"
      provides: "StreamProject.layout.snap_to_grid: boolean field; serialize emits it; deserialize defaults to false when absent."
      contains: "snap_to_grid"
    - path: "gui/src/store/useStore.ts"
      provides: "snapToGrid: boolean state field; setSnapToGrid action; saveProject/saveProjectAs/loadProject wired."
      contains: "snapToGrid:"
    - path: "gui/src/components/canvasMenus/SnapToGridButton.tsx"
      provides: "Overlay button component reading/setting snapToGrid via the store."
      contains: "lucide-react"
    - path: "gui/src/components/CanvasPanel.tsx"
      provides: "ReactFlow snapToGrid + snapGrid={[16,16]} props bound to store; SnapToGridButton rendered in the top-right overlay."
      contains: "snapGrid={[16, 16]}"
  key_links:
    - from: "SnapToGridButton onClick"
      to: "useStore setSnapToGrid"
      via: "useStore((s) => s.setSnapToGrid)"
      pattern: "setSnapToGrid"
    - from: "CanvasPanel ReactFlow root"
      to: "useStore snapToGrid"
      via: "snapToGrid={snapEnabled} snapGrid={[16, 16]}"
      pattern: "snapGrid=\\{\\[16, 16\\]\\}"
    - from: "projectIO.ts serializeProject"
      to: ".scp file layout.snap_to_grid"
      via: "args.snapToGrid → project.layout.snap_to_grid"
      pattern: "snap_to_grid"
---

<objective>
Ship the snap-to-grid feature surface for v1.2: a canvas-overlay toggle
button (no Settings dialog — that's Phase 67/72), a 16px fixed grid, and
per-project persistence in the `.scp` layout block. ReactFlow's built-in
`snapToGrid` + `snapGrid` props deliver the snapping behavior; the only
new code is the toggle button, the store boolean, the projectIO field, and
their wiring.

Depends on Plan 03 (CanvasPanel must already be the post-Plan-03 shape
with the canvas-overlay surface intact) and Plan 05 (the canvasMenus
directory exists from Plan 05; SnapToGridButton lives there alongside the
context menus for cohesion).

Purpose: deliver D-07 through D-10 verbatim. Settings-dialog surfacing is
deferred to Phase 67/72; this plan owns only the canvas-overlay button +
persistence.

Output: a new SnapToGridButton.tsx; a store slice extension; a layout
block schema extension; vitest coverage for the persistence round-trip
and for the button's toggle behavior.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-interaction-model-overhaul/65-CONTEXT.md
@.planning/notes/gui-redesign-design-decisions.md
@gui/src/lib/projectIO.ts
@gui/src/store/useStore.ts

<interfaces>
<!-- Existing .scp v2.0 layout block (gui/src/lib/projectIO.ts lines 58-62, 81-83) -->
```typescript
layout: {
  active_left_tab: ActiveLeftTab;
  active_layer: LayerView;
};

function defaultLayout(): StreamProject["layout"] {
  return { active_left_tab: "Components", active_layer: "Both" };
}
```

<!-- Phase 65 extension -->
```typescript
layout: {
  active_left_tab: ActiveLeftTab;
  active_layer: LayerView;
  snap_to_grid: boolean;                // NEW — D-10
};

function defaultLayout(): StreamProject["layout"] {
  return { active_left_tab: "Components", active_layer: "Both", snap_to_grid: false };
}
```

<!-- Existing serialize args (lines 89-104) extend with snapToGrid: boolean -->

<!-- ReactFlow built-in props — already documented at D-09 -->
//   snapToGrid={true|false}
//   snapGrid={[16, 16]}
// No custom snapping math; ReactFlow handles drag + drop quantization.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: projectIO schema extension + serialize/deserialize round-trip tests</name>
  <files>gui/src/lib/projectIO.ts, gui/src/lib/__tests__/projectIO.snapToGrid.test.ts</files>
  <read_first>
    - gui/src/lib/projectIO.ts entire file (it's small — confirm the StreamProject interface, serializeProject signature, deserializeProject defaults)
    - .planning/phases/65-interaction-model-overhaul/65-CONTEXT.md D-10 ("Persisted per-project in the .scp layout block")
    - .planning/notes/gui-redesign-design-decisions.md §7 lines ~1393-1394
  </read_first>
  <behavior>
    Test cases:
    - serializeProject({snapToGrid: true, ...rest}) produces JSON whose `parsed.layout.snap_to_grid === true`.
    - serializeProject({snapToGrid: false, ...rest}) produces JSON whose `parsed.layout.snap_to_grid === false` (explicit, NOT absent).
    - deserializeProject of a v2.0 JSON with `layout.snap_to_grid: true` returns a project whose `layout.snap_to_grid === true`.
    - deserializeProject of a v2.0 JSON WITHOUT `layout.snap_to_grid` (legacy v2.0 file from Phase 62/63/63.1) returns `layout.snap_to_grid === false` (empty-state tolerance — RESEARCH Pitfall 3).
    - Round-trip: serialize → deserialize → snap_to_grid preserved.
  </behavior>
  <action>
    1. Add `snap_to_grid: boolean;` to the `StreamProject['layout']` interface (line 58-62 area).
    2. Update `defaultLayout()` to return `snap_to_grid: false`.
    3. Add `snapToGrid: boolean;` to `SerializeProjectArgs` interface (line 89-104 area).
    4. In `serializeProject` (line 121+), emit `snap_to_grid: args.snapToGrid` inside the `layout` object literal (line 139-142). Explicit false is fine — the spec requires the field to be written.
    5. In `deserializeProject` (line 170+), extend the rawLayout extraction (line 192-198) to include `snap_to_grid: (rawLayout.snap_to_grid as boolean) ?? false`.
    6. Create gui/src/lib/__tests__/projectIO.snapToGrid.test.ts covering the five behavior cases. Reuse fixtures from the existing projectIO tests if available, otherwise build minimal `SerializeProjectArgs` literals.
    7. The PROJECT_FORMAT_VERSION stays "2.0" — adding a new layout field is forward-compatible per the existing empty-state tolerance. Do NOT bump to "2.1" (no migration; old files load with snap_to_grid defaulted to false).
  </action>
  <verify>
    <automated>cd gui && npx vitest run src/lib/__tests__/projectIO.snapToGrid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'snap_to_grid' gui/src/lib/projectIO.ts | grep -c .` returns ≥ 5 (interface + default + serialize emit + deserialize parse + types).
    - `grep -nE 'snapToGrid:' gui/src/lib/projectIO.ts | grep -c .` returns 1 (SerializeProjectArgs field).
    - `cd gui && npx vitest run src/lib/__tests__/projectIO.snapToGrid.test.ts` exits 0 with 5 cases passing.
    - `cd gui && npx tsc --noEmit` reports no new errors.
    - PROJECT_FORMAT_VERSION remains the literal "2.0".
  </acceptance_criteria>
  <done>
    .scp schema carries snap_to_grid; round-trip is exercised; backwards compatibility with Phase 62/63 saved files is verified (loads default to false).
  </done>
</task>

<task type="auto">
  <name>Task 2: useStore snapToGrid slice + saveProject/loadProject wiring</name>
  <files>gui/src/store/useStore.ts</files>
  <read_first>
    - gui/src/store/useStore.ts lines 760-768 (initial state for layer view + isDirty + currentFilePath — the place where snapToGrid initial state lives)
    - gui/src/store/useStore.ts lines 1763-1870 (saveProject / saveProjectAs paths — find where the serializeProject args are constructed)
    - gui/src/store/useStore.ts lines 1906+ (loadProjectFromPath — find where activeLeftTab + activeLayer are restored from project.layout)
    - gui/src/store/useStore.ts lines 960-973 (setActiveLayer / cycleLayer — the precedent for "layout-block-persisted boolean with isDirty: true on change")
  </read_first>
  <action>
    1. Add `snapToGrid: boolean;` to the state interface near `isDirty` (line 181-200 area).
    2. Add `setSnapToGrid: (v: boolean) => void;` to the action interface.
    3. Initial state: `snapToGrid: false,` near line 766.
    4. Implement `setSnapToGrid: (v) => set({ snapToGrid: v, isDirty: true }),` near the setActiveLayer block (line 963). Snap toggle MUST set isDirty per D-10.
    5. In saveProject + saveProjectAs (lines 1763, 1820), where `serializeProject({...})` is called, add `snapToGrid: get().snapToGrid` to the args object. (Grep for the existing `activeLeftTab: state.activeLeftTab` line and follow the pattern.)
    6. In loadProjectFromPath (line 1906+), where project.layout.active_left_tab and project.layout.active_layer are restored, ALSO restore `snapToGrid: project.layout.snap_to_grid`. Add it to the `set({...})` call that hydrates the loaded project state.
    7. In newProject (line 2061+), include `snapToGrid: false` in the reset state — matches D-10 "OFF by default for new projects."

    NO new tests in this task — the round-trip is already covered by Plan 06 Task 1's projectIO tests, and the button behavior is covered by Task 3.
  </action>
  <verify>
    <automated>cd gui && npx tsc --noEmit && cd gui && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'snapToGrid:' gui/src/store/useStore.ts | grep -c .` returns ≥ 4 (interface + initial state + setSnapToGrid + save args + load restore — typically 5-7 matches).
    - `grep -nE 'setSnapToGrid' gui/src/store/useStore.ts | grep -c .` returns ≥ 2 (declaration + implementation).
    - `cd gui && npx tsc --noEmit` reports no new errors.
    - `cd gui && npx vitest run` no NEW failures.
  </acceptance_criteria>
  <done>
    snapToGrid is a first-class store field; toggling it dirties the project; saving and loading round-trip it through the .scp layout block.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SnapToGridButton component + CanvasPanel wiring</name>
  <files>gui/src/components/canvasMenus/SnapToGridButton.tsx, gui/src/components/CanvasPanel.tsx, gui/src/components/__tests__/SnapToGridButton.test.tsx</files>
  <read_first>
    - gui/src/components/CanvasPanel.tsx (post-Plan-03 and post-Plan-05 — has rcMenu wiring and menu rendering already)
    - gui/src/components/ThemeMenu.tsx (lucide Settings icon precedent — use the same import pattern for the Grid icon)
    - .planning/phases/65-interaction-model-overhaul/65-CONTEXT.md D-07, D-08, D-09 (canvas-overlay button, 16px, ReactFlow built-in snap props)
    - gui/src/components/ui/button.tsx (shadcn Button — likely the right base for the overlay button)
  </read_first>
  <behavior>
    Test cases (SnapToGridButton.test.tsx):
    - Renders a button with aria-label "Snap to grid" (or similar accessible name); pressed state reflects `snapToGrid` from the store.
    - When store snapToGrid === false, button is NOT in pressed state (data-state="off" or aria-pressed="false").
    - When store snapToGrid === true, button IS in pressed state.
    - Clicking the button calls `setSnapToGrid(!currentValue)` — verified by mocking the store and asserting the spy.
    - Button uses the Grid lucide icon (`<Grid />` from lucide-react).
  </behavior>
  <action>
    Create gui/src/components/canvasMenus/SnapToGridButton.tsx:
    1. Default-export `SnapToGridButton()`.
    2. Read `snapToGrid` and `setSnapToGrid` from `useStore` via two separate `useStore((s) => ...)` selectors (primitive selectors for stable equality).
    3. Render a `<button>` styled to match the existing ReactFlow Controls/MiniMap overlay aesthetic — a small icon button with a Grid icon from `lucide-react`. Use shadcn `Button` with `size="icon"` if it produces an appropriate small button, else a custom styled `<button>`.
    4. Active state visual: when snap is ON, give the button a colored background (e.g. `bg-primary text-primary-foreground` or a subtle outline ring); when OFF, neutral. Use `aria-pressed={snapToGrid}` and `data-state={snapToGrid ? 'on' : 'off'}` for accessibility + test selectors.
    5. onClick toggles: `setSnapToGrid(!snapToGrid)`.
    6. Tooltip via title attribute or shadcn Tooltip: "Snap to grid (16px)".

    Modify gui/src/components/CanvasPanel.tsx:
    7. Import the button: `import SnapToGridButton from './canvasMenus/SnapToGridButton';`.
    8. Read `snapToGrid` from the store: `const snapEnabled = useStore((s) => s.snapToGrid);`.
    9. Add two new props to the `<ReactFlow>` element (right next to the Plan 03 additions): `snapToGrid={snapEnabled}` and `snapGrid={[16, 16]}`. Both are ReactFlow built-ins from @xyflow/react.
    10. **Render host (W9 lock — absolutely-positioned div, NOT Panel):**
        - `gui/src/components/CanvasPanel.tsx` does NOT currently import `Panel` from `@xyflow/react` (confirmed by reading the imports block lines 2-15: only `ReactFlow, Controls, MiniMap, Background, BackgroundVariant, ConnectionLineType, useReactFlow` plus types are imported). Adding `Panel` would be a new import surface and an extra DOM layer for one button.
        - **Locked pattern:** render `<SnapToGridButton />` as a sibling absolutely-positioned div inside `containerRef`'s render output. Place it AFTER the closing `</ReactFlow>` tag and BEFORE `<WelcomeOverlay />` (which currently sits as the last child at line 230). The button wraps itself in `<div className="absolute top-2 right-2 z-10">` so it sits in the top-right overlay stack alongside ReactFlow's built-in Controls (bottom-left by default) and MiniMap (bottom-right by default). z-10 keeps it above the Background grid but below the WelcomeOverlay's modal-ish layer if it ever activates.
        - Do NOT import `Panel` from `@xyflow/react` even though it's available — keep the import surface minimal.

    Create gui/src/components/__tests__/SnapToGridButton.test.tsx:
    11. Use @testing-library/react. Render the button with a fresh useStore. Use `act()` to call setSnapToGrid via the store and assert the button's `data-state` flips. Click the button via `fireEvent.click` and assert the store's snapToGrid value flips.
  </action>
  <verify>
    <automated>cd gui && npx vitest run src/components/__tests__/SnapToGridButton.test.tsx && cd gui && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'snapGrid=\{\[16, 16\]\}' gui/src/components/CanvasPanel.tsx | grep -c .` returns 1.
    - `grep -nE 'snapToGrid=\{' gui/src/components/CanvasPanel.tsx | grep -c .` returns 1 (the ReactFlow prop).
    - `grep -nE 'SnapToGridButton' gui/src/components/CanvasPanel.tsx | grep -c .` returns ≥ 2 (import + JSX).
    - `grep -nE 'lucide-react' gui/src/components/canvasMenus/SnapToGridButton.tsx | grep -c .` returns 1.
    - `grep -nE 'aria-pressed' gui/src/components/canvasMenus/SnapToGridButton.tsx | grep -c .` returns 1.
    - `cd gui && npx vitest run src/components/__tests__/SnapToGridButton.test.tsx` exits 0.
    - `cd gui && npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>
    A canvas-overlay button toggles snap-to-grid; ReactFlow honors the 16px snap props live; the toggle persists through Save/Load.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual smoke — snap-to-grid end-to-end</name>
  <what-built>
    A new overlay button toggles ReactFlow's built-in snap-to-grid with a
    16px fixed grid; toggle state persists per-project in the .scp file.
  </what-built>
  <how-to-verify>
    1. `cd gui && npm run tauri dev`.
    2. **Button visible:** look at the top-right overlay area of the canvas. EXPECT: alongside the Controls + MiniMap, a small button with a grid icon. Hover: tooltip "Snap to grid (16px)" (or equivalent).
    3. **Default OFF:** on a fresh project (File → New, or initial empty canvas), the button should appear in the inactive visual state (neutral background, no fill).
    4. **Toggle ON:** click the button. EXPECT: button switches to active visual state (colored background or ring).
    5. **Drag snaps:** drag any node around. EXPECT: at drop, position snaps to a 16px multiple on both axes. With the dotted Background visible, the node visibly aligns with the dot grid.
    6. **Drop snaps:** drag a Pump from the toolbox onto the canvas. EXPECT: it lands on a 16px multiple.
    7. **Toggle OFF:** click the button again. Drag a node. EXPECT: free positioning (no snap).
    8. **Persistence — save and reload:** turn snap ON. File → Save As → my_snap_test.scp. Quit the app. Reopen, File → Open → my_snap_test.scp. EXPECT: button is active; nodes still snap.
    9. **Persistence — load legacy file:** open any pre-Phase-65 .scp file (e.g. gui/export_examples/abc.streamgui or any saved during Phase 64). EXPECT: snap is OFF by default (the file doesn't carry snap_to_grid; the deserializer defaults to false).
    10. **isDirty signal:** with the project unsaved, toggle snap. EXPECT: the title bar / save button reflects unsaved changes (dirty dot or equivalent).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any anomaly (e.g. "drop does not snap on toolbox drop", "button doesn't change visual state on toggle").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .scp file → projectIO.deserializeProject → store hydration | snap_to_grid is parsed from JSON. Untrusted file content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-08 | T (Tampering) | deserializeProject snap_to_grid parse | mitigate | Cast via `(rawLayout.snap_to_grid as boolean) ?? false`. JavaScript truthy-coercion is not enough — explicit boolean default protects against null / undefined / string "true" which would render as truthy. Acceptance criterion: passing `{layout:{snap_to_grid:"yes"}}` to deserialize yields a value that, when fed to ReactFlow's `snapToGrid` prop, is treated as truthy — that's acceptable noise; only the `false` default-on-missing case is hard-required. |

Low-risk UI state mutation, untrusted input is bounded to a single boolean field.
</threat_model>

<verification>
- 5 round-trip cases in projectIO.snapToGrid.test.ts.
- SnapToGridButton click test confirms store + visual state update.
- Manual smoke covers the four functional states (off / on / drag-snap / drop-snap) plus the two persistence states (save+reload / legacy-load).
</verification>

<success_criteria>
Source assertion: `snapGrid={[16, 16]}` and `snapToGrid={snapEnabled}` both present on the ReactFlow root.
Behavior assertion: dragging a node with snap ON quantizes the drop position to a 16px multiple.
Behavior assertion: saving a project with snap ON, closing, reopening — snap remains ON.
</success_criteria>

<output>
After completion, create `.planning/phases/65-interaction-model-overhaul/65-06-SUMMARY.md`.
</output>
