---
phase: 58-mtk-system-determinacy-repair
plan: 04
type: execute
wave: 4
depends_on: ["58-03"]
files_modified:
  - test/test_validation.jl
  - test/test_determinacy.jl
autonomous: true
requirements: []
scenarios_addressed:
  - VAL-02-TWOPLATE
  - VAL-02-TRANSIENT
  - DETERMINACY-PHASE58 (row 5 of 5)
tags: [mtk, determinacy, val-02, two-plate, transient, symbol-access, fix]

must_haves:
  truths:
    - "VAL-02 two-plate steady topology mtkcompiles with `fully_determined=true` and `solve_steady` returns `Success`"
    - "VAL-02 transient T_outlet step testset runs `solve_transient` to `Success` (no `variable sys does not exist` error)"
    - "_build_val02_twoplate helper in test_determinacy.jl flips from RED to GREEN — closing the last Phase-58 scenario row"
    - "All five Phase-58 scenarios in test/test_determinacy.jl are GREEN"
    - "test_validation.jl :204 (simple_loop testset) flipped to fully_determined=true (last bug-hiding audit site in this file)"
  artifacts:
    - path: "test/test_validation.jl"
      provides: "Two power pins; one symbol-access fix; two audit flips"
      contains: "hd1.power ~ power_per_plate"
    - path: "test/test_determinacy.jl"
      provides: "VAL-02 two-plate helper updated with both pins"
      contains: "hd1.power ~ power_per_plate"
  key_links:
    - from: "test/test_validation.jl conns_v02"
      to: "hd1.power(t) and hd2.power(t)"
      via: "two closing equations"
      pattern: "hd[12]\\.power ~ power_per_plate"
    - from: "test/test_validation.jl:317"
      to: "ssys.T_wall_callable"
      via: "direct namespace access (drop the `.sys.` segment)"
      pattern: "ssys\\.T_wall_callable"
---

<objective>
Last Phase-58 scenario fix plan. Three distinct edits in `test/test_validation.jl`, all small, all in the same file (RESEARCH OQ-2 — fold into one plan):

1. **VAL-02 two-plate steady (Δ=−2):** Two `HeatDiffusion` instances → two missing pins. Add `hd1.power ~ power_per_plate` AND `hd2.power ~ power_per_plate` to `conns_v02`. Flip `fully_determined=false` to `=true` at line 996.
2. **VAL-02 transient (symbol-access bug — NOT determinacy):** Replace `ssys.sys.T_wall_callable` with `ssys.T_wall_callable` at line 317 (RESEARCH §3 Scenario F live verification: direct path resolves; `.sys.` raises).
3. **simple_loop audit flip at line 204:** flips after the broader determinacy is closed; this site was `bug-hiding` per RESEARCH §5.

Sync the `_build_val02_twoplate` helper in `test/test_determinacy.jl` so the last RED row in "Determinacy: Phase 58 scenarios" goes GREEN.

Output: ~5 line-level edits in `test_validation.jl` and 2 helper edits in `test_determinacy.jl`.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/58-mtk-system-determinacy-repair/58-CONTEXT.md
@.planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md
@.planning/phases/58-mtk-system-determinacy-repair/58-01-SUMMARY.md
@.planning/phases/58-mtk-system-determinacy-repair/scratch/diag_table.md
@CLAUDE.md

<interfaces>
VAL-02 steady topology (test_validation.jl:935-996):
  @named hd1 = HeatDiffusion(...; power=power_per_plate)
  @named hd2 = HeatDiffusion(...; power=power_per_plate)
  power_per_plate = 1e4 (declared at line 939)
  Two HD instances → two missing pins (Δ=−2 per RESEARCH §3 Scenario E).
  Pin shape:
    hd1.power ~ power_per_plate
    hd2.power ~ power_per_plate

VAL-02 transient symbol-access (test_validation.jl:317, RESEARCH §3 Scenario F):
  Current (broken):  T_wall_sym = ssys.sys.T_wall_callable
  Fix:               T_wall_sym = ssys.T_wall_callable
  Use the direct named path — preserves the original "stable named access, immune to parameter
  reordering" intent of the inline comment.

simple_loop audit at line 204:
  The simple_loop testset at line 171 uses one CAC + ConstantTemperature arrays + connect, no HD.
  All eqs are structurally balanced today (verified by RESEARCH §5 — listed under bug-hiding
  alongside the other test_validation.jl :204 entries). Flip to fully_determined=true.
</interfaces>

<source_lines>
test/test_validation.jl:171-289   — `@testset "Python parity: simple loop"`
test/test_validation.jl:204       — simple_loop `mtkcompile(sys; fully_determined=false)` (audit flip target #1)

test/test_validation.jl:295-326   — `@testset "VAL-02: Transient T_outlet rises after T_wall step"`
test/test_validation.jl:317       — `T_wall_sym = ssys.sys.T_wall_callable` (symbol-access fix target)

test/test_validation.jl:935       — `@testset "VAL-02: Two-plate one-channel topology — both faces active"`
test/test_validation.jl:947-958   — `@named hd1 = HeatDiffusion(...; power=power_per_plate)`
test/test_validation.jl:959-970   — `@named hd2 = HeatDiffusion(...; power=power_per_plate)`
test/test_validation.jl:972-992   — `conns_v02 = [...]`
test/test_validation.jl:992       — closing `]` of conns_v02
test/test_validation.jl:996       — `ssys_v02 = mtkcompile(sys_v02; fully_determined=false)` (audit flip target #2)
</source_lines>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VAL-02 two-plate — two pins + audit flip; sync test_determinacy.jl helper</name>
  <files>
    test/test_validation.jl
    test/test_determinacy.jl
  </files>
  <read_first>
    test/test_validation.jl (lines 935-1018 — entire VAL-02 two-plate testset)
    test/test_determinacy.jl (the _build_val02_twoplate helper from Plan 58-01)
    .planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md (§3 Scenario E)
  </read_first>
  <action>
    Three edits.

    Edit 1: Add two pins to conns_v02 in test/test_validation.jl. Locate the closing `]` of `conns_v02` (line ~992). Insert these two lines immediately before the closing `]`, indented to match surrounding entries:

        hd1.power ~ power_per_plate,
        hd2.power ~ power_per_plate,

    The pins use the local variable `power_per_plate` (declared at line 939 = 1e4), which is also passed to both HD constructors at lines 957 and 969. Per RESEARCH §3 Scenario E live verification, these two pins close the Δ=−2 deficit (91/93 → 91/91).

    Edit 2: Flip the audit at line 996. Replace `ssys_v02 = mtkcompile(sys_v02; fully_determined=false)` with `ssys_v02 = mtkcompile(sys_v02; fully_determined=true)`.

    Edit 3: Sync the `_build_val02_twoplate` helper in `test/test_determinacy.jl`. The helper was created in Plan 58-01 Task 3 with NO pins. Add `hd1.power ~ power_per_plate,` and `hd2.power ~ power_per_plate,` as the last two elements before the closing `]` of its conns_v02 vector. Mirror Edit 1 exactly.

    Verification cycle:

        bin/jl test/test_determinacy.jl

    Expected: `_build_val02_twoplate` row in "Determinacy: Phase 58 scenarios" flips from RED to GREEN. After this task, all five Phase-58 scenario rows in test_determinacy.jl are GREEN.

    Run only this file as the per-task gate — saves the full test_validation.jl run for after Tasks 2 and 3 land.

    Forbidden actions: no `check_length=false`, no `src/` edits, no `Manifest.toml` bumps, no widening of energy-balance tolerances. Stay on `channels-redesign` (D-07).
  </action>
  <verify>
    <automated>grep -c 'hd1\.power ~ power_per_plate' test/test_validation.jl | awk '$1 == 1 {exit 0} {exit 1}' &amp;&amp; grep -c 'hd2\.power ~ power_per_plate' test/test_validation.jl | awk '$1 == 1 {exit 0} {exit 1}' &amp;&amp; grep -c 'hd1\.power ~ power_per_plate' test/test_determinacy.jl | awk '$1 == 1 {exit 0} {exit 1}' &amp;&amp; grep -c 'hd2\.power ~ power_per_plate' test/test_determinacy.jl | awk '$1 == 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'hd1.power ~ power_per_plate' test/test_validation.jl` returns exactly 1 match.
    - `grep -n 'hd2.power ~ power_per_plate' test/test_validation.jl` returns exactly 1 match.
    - The line at `test/test_validation.jl:996` reads `ssys_v02 = mtkcompile(sys_v02; fully_determined=true)`.
    - `grep -c 'hd1.power ~ power_per_plate' test/test_determinacy.jl` returns 1.
    - `grep -c 'hd2.power ~ power_per_plate' test/test_determinacy.jl` returns 1.
    - `bin/jl test/test_determinacy.jl` reports `_build_val02_twoplate` row PASSING; no regression on previous rows (MTR sym/asym/onesided GREEN, VAL-01 GREEN).
    - Working tree dirty in only `test/test_validation.jl` and `test/test_determinacy.jl`. No `src/` edits.
    - `git rev-parse --abbrev-ref HEAD` returns `channels-redesign`.
  </acceptance_criteria>
  <done>VAL-02 two-plate steady topology Δ=0 after two pins; helper synced; test_determinacy.jl shows all five Phase-58 scenario rows GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: VAL-02 transient symbol-access fix at line 317</name>
  <files>
    test/test_validation.jl
  </files>
  <read_first>
    test/test_validation.jl (lines 295-326 — VAL-02 transient testset)
    .planning/phases/58-mtk-system-determinacy-repair/scratch/diag_val02_transient.jl (live verification from Plan 58-01)
    .planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md (§3 Scenario F, lines 200-208; R-2 lines 463-469)
  </read_first>
  <action>
    Single one-line edit.

    The current line at test/test_validation.jl:317 reads:

        T_wall_sym = ssys.sys.T_wall_callable   # stable named access, immune to parameter reordering

    Replace with:

        T_wall_sym = ssys.T_wall_callable   # stable named access, immune to parameter reordering

    Drop the `.sys` namespace segment. Per RESEARCH §3 Scenario F live verification: `ssys.T_wall_callable` resolves; `ssys.sys.T_wall_callable` raises `ArgumentError: System sys: variable sys does not exist`.

    The trailing comment "stable named access, immune to parameter reordering" stays — that was the original motivation for using the namespaced access; the named-direct access has the same property.

    Do NOT use `last(parameters(ssys))` even though RESEARCH OQ-2 calls it valid. The named access is more readable and the test's existing comment already documents the reasoning.

    Verification cycle:

        bin/jl test/test_validation.jl

    Expected: the VAL-02 transient testset (test_validation.jl:295) runs to completion; `solve_transient(ssys, op_ic, t_arr)` returns Success; the assertions at lines 322 (`sol.retcode == ReturnCode.Success`), 324 (`!any(isnan, T_ts)`), and 325 (`T_ts[end] > T_ts[1]`) all pass.

    Forbidden: no `src/` edits. No `check_length=false`. No new branches.
  </action>
  <verify>
    <automated>grep -c 'ssys\.sys\.T_wall_callable' test/test_validation.jl | awk '$1 == 0 {exit 0} {exit 1}' &amp;&amp; grep -c 'T_wall_sym = ssys\.T_wall_callable' test/test_validation.jl | awk '$1 == 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'ssys.sys.T_wall_callable' test/test_validation.jl` returns ZERO matches.
    - `grep -n 'T_wall_sym = ssys.T_wall_callable' test/test_validation.jl` returns exactly 1 match (line 317).
    - The trailing comment "stable named access, immune to parameter reordering" is preserved on the same line.
    - Running `bin/jl test/test_validation.jl` no longer raises `ArgumentError: System sys: variable sys does not exist`. The VAL-02 transient testset passes its three `@test` assertions.
    - No regression in test_determinacy.jl (rerun: all five Phase-58 rows still GREEN).
    - Working tree dirty in only `test/test_validation.jl`.
  </acceptance_criteria>
  <done>VAL-02 transient symbol-access fixed; solve_transient succeeds; outlet-rise assertion holds.</done>
</task>

<task type="auto">
  <name>Task 3: Flip simple_loop audit site at test_validation.jl:204</name>
  <files>
    test/test_validation.jl
  </files>
  <read_first>
    test/test_validation.jl (lines 171-289 — simple_loop testset; line 204 in particular)
    .planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md (§5 audit table)
    .planning/phases/58-mtk-system-determinacy-repair/scratch/audit_table.md (Plan 58-01 audit)
  </read_first>
  <action>
    Single one-line edit.

    Replace at test/test_validation.jl:204:

        ssys = mtkcompile(sys; fully_determined=false)

    with:

        ssys = mtkcompile(sys; fully_determined=true)

    Justification (per RESEARCH §5 audit row for `:204`): the simple_loop scenario uses one `ChannelAndContacts` + `ConstantTemperature` arrays + standard hydraulic loop; no `HeatDiffusion` instance is involved, so there is no `power(t)` variable to close. The system has been structurally balanced under the new MTK API the entire time; the `=false` flag was carried over defensively. After Plans 58-02..04 close the genuine gaps, this site is bug-hiding by virtue of conveying false information about the system's structural state.

    After this edit, every `mtkcompile` call in `test/test_validation.jl` uses `fully_determined=true`. Confirm: `grep -n 'fully_determined=false' test/test_validation.jl` returns ZERO matches.

    Verification cycle (full Wave 4 gate):

        bin/jl test/test_determinacy.jl
        bin/jl test/test_validation.jl

    Expected: all 6 canonical-builder rows GREEN. All 5 Phase-58 scenario rows GREEN. The simple_loop testset runs to completion (no regression — it always was structurally fine; the flag flip just removes the bug-hiding flag). The 5 KEPT testsets in test_validation.jl (simple_loop + the four MTR/VAL groups) all reach `solve_*` Success, no `solver_error` sentinel rows emitted.

    Forbidden: no `src/` edits. No `check_length=false`. No new branches.
  </action>
  <verify>
    <automated>grep -c 'fully_determined=false' test/test_validation.jl | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fully_determined=false' test/test_validation.jl` returns ZERO matches (every site in this file is now `=true`).
    - `bin/jl test/test_validation.jl` runs the simple_loop testset to completion — all assertions pass.
    - `bin/jl test/test_determinacy.jl` shows all canonical-builder + Phase-58 scenario rows PASSING.
    - Working tree dirty in only `test/test_validation.jl` (Tasks 1, 2, 3 all touch this file plus Task 1 touched test_determinacy.jl).
    - `git rev-parse --abbrev-ref HEAD` returns `channels-redesign`.
  </acceptance_criteria>
  <done>Final test_validation.jl audit site flipped; every fully_determined call in the file is now =true; phase's primary scenarios all reach Success.</done>
</task>

</tasks>

<threat_model>
## Threat Model — N/A

Internal Julia library; no external attack surface. Plan 58-04 adds two closing equations, fixes one symbol-access bug, and flips two `fully_determined` flags; no security-relevant code paths added or removed.
</threat_model>

<verification>
End-of-plan integrated check:

    bin/jl test/test_determinacy.jl   # all 6 canon rows PASS, all 5 Phase-58 rows PASS
    bin/jl test/test_validation.jl    # all 7 in-scope testsets reach solver Success

After Plan 58-04, `grep -n 'fully_determined=false' test/test_validation.jl` returns ZERO matches; this is the file-level evidence that the bug-hiding audit sweep is complete for `test_validation.jl`.

The remaining bug-hiding audit site (test_heat_diffusion.jl:185) and the legitimate-structural inline-comment work are owned by Plan 58-05.
</verification>

<success_criteria>
- 2 power pins in conns_v02; 1 audit flip at :996.
- 1 symbol-access fix at :317.
- 1 simple_loop audit flip at :204.
- 2 helper updates in test_determinacy.jl (`_build_val02_twoplate` gets both pins).
- All 5 Phase-58 rows in test_determinacy.jl are GREEN.
- VAL-02 steady reaches `solve_steady` Success.
- VAL-02 transient reaches `solve_transient` Success.
- `grep 'fully_determined=false' test/test_validation.jl` returns nothing.
- No edits to `src/`. No `check_length=false`. No new branches.
</success_criteria>

<output>
After completion, create `.planning/phases/58-mtk-system-determinacy-repair/58-04-SUMMARY.md` documenting all five line-level edits with file:line + before/after, live `bin/jl test/test_determinacy.jl` output showing all 5 Phase-58 rows GREEN, and `bin/jl test/test_validation.jl` output showing VAL-02 steady + VAL-02 transient + simple_loop testsets all reach Success.
</output>
