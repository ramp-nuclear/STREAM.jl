---
phase: 58-mtk-system-determinacy-repair
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - test/test_validation.jl
  - test/test_determinacy.jl
autonomous: true
requirements: []
scenarios_addressed:
  - MTR-SYM
  - MTR-ASYM
  - MTR-ONESIDED
  - DETERMINACY-PHASE58 (rows 1, 2, 3 of 5)
tags: [mtk, determinacy, mtr, fix]

must_haves:
  truths:
    - "MTR symmetric topology reaches `solve_steady` with `ReturnCode.Success`"
    - "MTR asymmetric topology reaches `solve_steady` with `ReturnCode.Success`"
    - "MTR one-sided topology reaches `solve_steady` with `ReturnCode.Success`"
    - "All three MTR scenario testsets in `test/test_determinacy.jl` flip from RED to GREEN"
    - "All three `mtkcompile(...; fully_determined=false)` sites in MTR testsets flip to `=true` after the pin lands"
  artifacts:
    - path: "test/test_validation.jl"
      provides: "Three power pins added; three audit sites flipped"
      contains: "hd.power ~ 1e4"
    - path: "test/test_determinacy.jl"
      provides: "Three Phase-58 helper functions get `hd.power ~ 1e4` to mirror production fix"
      contains: "hd.power ~ 1e4"
  key_links:
    - from: "test/test_validation.jl conns vector"
      to: "hd.power(t) variable declared at heat_diffusion.jl:145"
      via: "closing equation `hd.power ~ 1e4`"
      pattern: "hd\\.power ~ 1\\.?e?4"
---

<objective>
Apply the mechanical fix from Plan 58-01's diagnostic table to MTR symmetric, asymmetric, and one-sided scenarios. The fix is identical for all three (RESEARCH §3 Scenarios A/B/C, OQ-1 — collapse to one plan). Each: add `hd.power ~ 1e4` to the `conns` vector, then flip the same testset's `mtkcompile(...; fully_determined=false)` to `=true` to lock the determinacy contract.

Same edits to `_build_mtr_sym`, `_build_mtr_asym`, `_build_mtr_onesided` in `test/test_determinacy.jl` so the helpers mirror the production fix and the RED rows go GREEN.

Purpose: Close 3 of 5 Phase-58 scenarios in one focused plan; identical fix shape across the three justifies the merge.

Output: 3 testset edits in `test_validation.jl` (one pin + one audit flip per testset = 6 line-level edits in three sites); 3 helper edits in `test_determinacy.jl` (one pin per helper).
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/58-mtk-system-determinacy-repair/58-CONTEXT.md
@.planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md
@.planning/phases/58-mtk-system-determinacy-repair/58-01-SUMMARY.md
@.planning/phases/58-mtk-system-determinacy-repair/scratch/diag_table.md
@.planning/phases/58-mtk-system-determinacy-repair/scratch/diag_mtr_power_pin.jl
@CLAUDE.md

<interfaces>
Closing equation pattern (RESEARCH §4 Pattern 1):

  src/components/heat_diffusion.jl:119 docstring:
    "power: total power into plate [W], MTK variable — must be constrained via a connection equation
     (e.g. `fuel.power ~ 1e4` for standalone use, or `rods.fuel.power ~ pk.P * scale` for PK-coupled use)"

  In-tree prior art (verbatim):
    src/examples.jl:499                heated.fuel.power ~ power_W,
    src/examples.jl:651                rods_fuel.power ~ pk.P * power_scale
    test/test_heat_diffusion.jl:182    [hd.power ~ pwr],

Live verification from Plan 58-01 diagnostic (scratch/diag_mtr_power_pin.jl):

  === MTR symmetric WITH hd.power pin ===
  mtkcompile(sys_mtr; fully_determined=true) -> SUCCESS
    n_eqs=92  n_unknowns=92
  solve_steady retcode: Success
</interfaces>

<source_lines>
test/test_validation.jl:357-361   — `@named hd = HeatDiffusion(...; power=1e4)` MTR sym
test/test_validation.jl:362-375   — MTR sym `conns = [...]` vector body
test/test_validation.jl:374       — last bracket-list-splat line `for i in 1:nz]...,`
test/test_validation.jl:375       — closing `]` of conns vector
test/test_validation.jl:379       — `ssys = mtkcompile(sys; fully_determined=false)`  (audit flip target #1)

test/test_validation.jl:527-531   — `@named hd = HeatDiffusion(...; power=1e4)` MTR asym
test/test_validation.jl:532-545   — MTR asym `conns = [...]` body
test/test_validation.jl:544       — last `for i in 1:nz]...,` line
test/test_validation.jl:545       — closing `]`
test/test_validation.jl:549       — `ssys = mtkcompile(sys; fully_determined=false)`  (audit flip target #2)

test/test_validation.jl:695-699   — `@named hd = HeatDiffusion(...; power=1e4)` MTR one-sided
test/test_validation.jl:700-707   — MTR one-sided `conns = [...]` body
test/test_validation.jl:706       — last `for i in 1:nz]...,` line
test/test_validation.jl:707       — closing `]`
test/test_validation.jl:709       — `ssys = mtkcompile(sys; fully_determined=false)`  (audit flip target #3)

test/test_determinacy.jl  — `_build_mtr_sym`, `_build_mtr_asym`, `_build_mtr_onesided` helpers
                            (created in Plan 58-01 Task 3 with NO pin; this plan adds it)
</source_lines>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `hd.power ~ 1e4` pin to MTR sym/asym/one-sided conns; flip three audit sites; sync test_determinacy.jl helpers</name>
  <files>
    test/test_validation.jl
    test/test_determinacy.jl
  </files>
  <read_first>
    test/test_validation.jl (lines 333-379, 504-549, 668-709)
    test/test_determinacy.jl (the _build_mtr_sym, _build_mtr_asym, _build_mtr_onesided helpers from Plan 58-01)
    .planning/phases/58-mtk-system-determinacy-repair/scratch/diag_mtr_power_pin.jl (the verified fix for MTR sym)
    src/examples.jl (line 499 — `heated.fuel.power ~ power_W` reference pattern)
    .planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md (§3 Scenarios A/B/C; §4 Pattern 1)
  </read_first>
  <action>
    Six line-level edits in `test/test_validation.jl` and three in `test/test_determinacy.jl`. Each edit is minimal and mechanical.

    ### Edit 1 — MTR symmetric pin (test/test_validation.jl)

    Locate the closing `]` of the MTR sym `conns` vector. The current state is:

    ```julia
    conns = [
        connect(pump_l.port_out, hx_l.port_in),
        connect(hx_l.port_out, cac_l.port_in),
        connect(cac_l.port_out, pump_l.port_in),
        pump_l.port_in.P ~ 1.0e5,
        connect(pump_r.port_out, hx_r.port_in),
        connect(hx_r.port_out, cac_r.port_in),
        connect(cac_r.port_out, pump_r.port_in),
        pump_r.port_in.P ~ 1.0e5,
        [connect(getproperty(hd, Symbol(:thermal_left, i)),
                 getproperty(cac_l, Symbol(:thermal_left, i))) for i in 1:nz]...,
        [connect(getproperty(hd, Symbol(:thermal_right, i)),
                 getproperty(cac_r, Symbol(:thermal_left, i))) for i in 1:nz]...,
    ]
    ```

    Insert `hd.power ~ 1e4,` as the LAST element before `]`. The post-edit state:

    ```julia
        [connect(getproperty(hd, Symbol(:thermal_right, i)),
                 getproperty(cac_r, Symbol(:thermal_left, i))) for i in 1:nz]...,
        hd.power ~ 1e4,
    ]
    ```

    Rationale: MTR sym uses `power=1e4` at the constructor (test_validation.jl:360). The pin must match. Per RESEARCH §3 Scenario A live verification: this pin alone closes the Δ=−1 deficit (92 eqs / 93 unknowns → 92/92).

    ### Edit 2 — MTR symmetric audit flip (test/test_validation.jl:379)

    Replace:
    ```julia
    ssys = mtkcompile(sys; fully_determined=false)
    ```
    with:
    ```julia
    ssys = mtkcompile(sys; fully_determined=true)
    ```

    ### Edit 3 — MTR asymmetric pin (test/test_validation.jl ~:545)

    Same insertion pattern as Edit 1, in the MTR asymmetric `conns` vector. Locate the closing `]` of the conns vector that's part of the `@testset "Python parity: MTR asymmetric"` block. The asym constructor uses `power=1e4` (test_validation.jl:530), so use the same `hd.power ~ 1e4,` line. Insert as the last element before `]`:

    ```julia
        [connect(getproperty(hd, Symbol(:thermal_right, i)),
                 getproperty(cac_r, Symbol(:thermal_left, i))) for i in 1:nz]...,
        hd.power ~ 1e4,
    ]
    ```

    ### Edit 4 — MTR asymmetric audit flip (test/test_validation.jl:549)

    Replace `ssys = mtkcompile(sys; fully_determined=false)` with `ssys = mtkcompile(sys; fully_determined=true)`.

    ### Edit 5 — MTR one-sided pin (test/test_validation.jl ~:707)

    Same insertion pattern, in the MTR one-sided `conns` vector. The one-sided constructor uses `power=1e4` (test_validation.jl:698), so use `hd.power ~ 1e4,`. Insert as the last element before `]`:

    ```julia
        [connect(getproperty(hd, Symbol(:thermal_left, i)),
                 getproperty(cac_l, Symbol(:thermal_left, i))) for i in 1:nz]...,
        hd.power ~ 1e4,
    ]
    ```

    ### Edit 6 — MTR one-sided audit flip (test/test_validation.jl:709)

    Replace `ssys = mtkcompile(sys; fully_determined=false)` with `ssys = mtkcompile(sys; fully_determined=true)`.

    ### Edits 7, 8, 9 — Sync `test/test_determinacy.jl` helpers

    Add `hd.power ~ 1e4,` as the last element before `]` in each of the three helper functions:
    - `_build_mtr_sym()` — created in Plan 58-01 Task 3 with the comment `# Plan 58-02 adds: hd.power ~ 1e4` at the bottom of the conns vector. REPLACE that comment with `hd.power ~ 1e4,`.
    - `_build_mtr_asym()` — same.
    - `_build_mtr_onesided()` — same.

    These three edits make the determinacy helpers mirror the post-fix production state.

    ### Verification cycle

    After all nine edits, run:
    ```
    bin/jl test/test_determinacy.jl
    ```

    Expected post-edit state: the three MTR rows in "Determinacy: Phase 58 scenarios" testset flip from RED to GREEN. The other two scenarios (VAL-01, VAL-02) remain RED — those are owned by Plans 58-03 and 58-04.

    Then run:
    ```
    bin/jl test/test_validation.jl
    ```
    Expected: the three MTR testsets ("Python parity: MTR symmetric/asymmetric/one-sided") MUST emit `s.retcode == ReturnCode.Success` rather than the `solver_error` sentinel row. The `parity_report.csv` should now contain real per-tier rows for the three MTR scenarios. Tier-FAIL rows are out of scope for this plan (tier verdicts are owned by Plan 56-06's resume).

    ### Branching invariant (D-07)

    Verify before commit: `git rev-parse --abbrev-ref HEAD` MUST return `channels-redesign`. Do NOT run `git switch`, `git checkout -b`, or `git branch <new>` at any point.

    ### Forbidden actions
    - Do NOT add `check_length=false` anywhere (D-05).
    - Do NOT touch `src/components/heat_diffusion.jl`. The closing-equation contract there (line 119) is correct.
    - Do NOT bump `Manifest.toml` MTK package versions.
    - Do NOT add or remove `connect(...)` lines beyond the three new pin equations.
    - Do NOT widen tolerances or add `try/catch` around the `solve_steady` calls — RESEARCH proved the fix lands clean.
  </action>
  <verify>
    <automated>grep -c 'hd\.power ~ 1e4' test/test_validation.jl | awk '$1 == 3 {exit 0} {exit 1}' &amp;&amp; grep -c 'fully_determined=true' test/test_validation.jl | awk '$1 &gt;= 3 {exit 0} {exit 1}' &amp;&amp; bin/jl test/test_determinacy.jl 2&gt;&amp;1 | grep -E 'MTR symmetric.*Pass|Pass.*MTR symmetric' &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'hd.power ~ 1e4' test/test_validation.jl` returns exactly 3 matches (one per MTR testset).
    - `grep -n 'fully_determined=false' test/test_validation.jl` no longer matches lines 379, 549, 709 — those three lines now read `fully_determined=true`.
    - `grep -c 'hd.power ~ 1e4' test/test_determinacy.jl` returns exactly 3 (one per MTR helper).
    - `bin/jl test/test_determinacy.jl` reports the three MTR testsets in "Determinacy: Phase 58 scenarios" as PASSING. The two non-MTR rows (VAL-01, VAL-02 twoplate) MAY still fail — that is expected.
    - `bin/jl test/test_validation.jl` runs the three MTR testsets to completion. The `solver_error` sentinel row is no longer emitted for `mtr_symmetric`, `mtr_asymmetric`, or `mtr_one_sided`.
    - `git rev-parse --abbrev-ref HEAD` returns `channels-redesign`.
    - No `git switch` / `git checkout -b` / `git branch` invoked anywhere in this task's execution.
    - Working tree contains exactly two modified files: `test/test_validation.jl` and `test/test_determinacy.jl`. No `src/` files modified.
  </acceptance_criteria>
  <done>Three MTR scenarios reach `solve_steady` Success; three audit sites flipped to `fully_determined=true`; `test_determinacy.jl` helpers updated; only `test/` files touched; on `channels-redesign` branch.</done>
</task>

</tasks>

<threat_model>
## Threat Model — N/A

Internal Julia library; no external attack surface (no network, no auth, no untrusted inputs). Plan 58-02 adds three closing equations to test scenarios and flips three `fully_determined` flags; no security-relevant code paths added or removed.
</threat_model>

<verification>
Per-task gate (≤30 s warm via daemon):
```
bin/jl test/test_determinacy.jl
```
Three MTR rows in "Determinacy: Phase 58 scenarios" PASS. Two non-MTR rows MAY remain RED until Plans 58-03/04.

Wave gate (≤5 min):
```
bin/jl test/test_determinacy.jl && bin/jl test/test_validation.jl
```
Plus the three MTR testsets in `test_validation.jl` reach `s.retcode == ReturnCode.Success`.
</verification>

<success_criteria>
- 3 `hd.power ~ 1e4` pins added in `test/test_validation.jl` (one per MTR testset).
- 3 `fully_determined=false` → `=true` flips at lines 379, 549, 709.
- 3 helper updates in `test/test_determinacy.jl`.
- All three MTR scenarios reach `ReturnCode.Success`.
- "Determinacy: Phase 58 scenarios" — three MTR rows are GREEN.
- No edits to `src/` files. No `check_length=false`. No new branches. Stay on `channels-redesign`.
</success_criteria>

<output>
After completion, create `.planning/phases/58-mtk-system-determinacy-repair/58-02-SUMMARY.md` documenting: which 9 lines were edited (with file:line and before/after), live `bin/jl test/test_determinacy.jl` output showing three MTR rows now GREEN, and `bin/jl test/test_validation.jl` output showing MTR testsets reach `Success`.
</output>
