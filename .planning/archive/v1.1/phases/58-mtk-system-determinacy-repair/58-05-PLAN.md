---
phase: 58-mtk-system-determinacy-repair
plan: 05
type: execute
wave: 5
depends_on: ["58-04"]
files_modified:
  - test/test_heat_diffusion.jl
  - test/test_misc.jl
  - test/test_pump.jl
  - test/test_resistors.jl
  - test/test_channels.jl
  - test/test_flapper.jl
  - src/components/flapper.jl
autonomous: true
requirements: []
scenarios_addressed:
  - PK-VAL (verification only — no code change expected)
  - AUDIT-FFD (close legitimate-structural sites with inline comments + flip last bug-hiding site)
tags: [mtk, determinacy, audit, phase-close, pk-verify]

must_haves:
  truths:
    - "test_heat_diffusion.jl:185 (HDIFF-04 with already-pinned hd.power) flips to fully_determined=true"
    - "Every legitimate-structural / isolated-component-test fully_determined=false site has an inline comment naming the structural reason"
    - "src/components/flapper.jl:38 docstring explicitly names the callback-set state as the structural reason a Flapper compiles only with fully_determined=false"
    - "PointKinetics validation testset reaches solve_* (steady or transient fallback) — verified by full bin/jl test/test_validation.jl run"
    - "bin/jl test/runtests.jl is GREEN (no NEW failures vs the v1.0 baseline)"
  artifacts:
    - path: "test/test_heat_diffusion.jl"
      provides: "HDIFF-04 audit flip at line 185"
      contains: "fully_determined=true"
    - path: "src/components/flapper.jl"
      provides: "Tightened docstring naming structural reason"
      contains: "callback"
  key_links:
    - from: "audit table at scratch/audit_table.md (Plan 58-01)"
      to: "every legitimate-structural / isolated-component-test site"
      via: "inline comment naming the structural reason"
      pattern: '#.*(no T equation|isolated|callback|value-source|under-determined|hypothesis A)'
---

<objective>
Phase-close audit + PK verification + remaining bug-hiding audit flip. After this plan:
- The last `bug-hiding` audit site (`test_heat_diffusion.jl:185`) is converted to `fully_determined=true`.
- Every `legitimate-structural` / `isolated-component-test` site preserved by Plan 58-01's audit gets a one-line inline comment explaining why it stays `=false` (so a future grep maintainer doesn't re-classify it as bug-hiding).
- The `flapper.jl:38` docstring is tightened to name the callback-set state as the structural reason.
- PointKinetics validation testset is verified to pass via the existing transient fallback path now that the upstream wrappers (Plan 58-04 closed `test_validation.jl:204`) no longer trip earlier.
- Full `bin/jl test/runtests.jl` is GREEN.

Purpose: Close Phase 58. The earlier plans fixed scenarios; this plan closes the audit sweep and confirms the regression test plus the seven scenarios stay green together.

Output: 1 audit flip in `test/test_heat_diffusion.jl`. ~22 inline comments scattered across `test_misc.jl`, `test_pump.jl`, `test_resistors.jl`, `test_channels.jl`, `test_flapper.jl`. 1 docstring tighten in `src/components/flapper.jl`. No source-code logic changes.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/58-mtk-system-determinacy-repair/58-CONTEXT.md
@.planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md
@.planning/phases/58-mtk-system-determinacy-repair/58-01-SUMMARY.md
@.planning/phases/58-mtk-system-determinacy-repair/scratch/audit_table.md
@CLAUDE.md

<interfaces>
Audit table site list (RESEARCH §5; Plan 58-01 Task 2 verifies and refines):

bug-hiding (the last one — flip in this plan):
  test/test_heat_diffusion.jl:185  HDIFF-04 already pins `[hd.power ~ pwr]` at :182, so structurally determined

isolated-component-test / legitimate-structural (preserve with inline comment):
  test/test_misc.jl:19, 37, 41, 48, 71, 131, 178
  test/test_pump.jl:18, 36, 68, 83, 99, 133
  test/test_resistors.jl:18
  test/test_heat_diffusion.jl:44
  test/test_channels.jl:16 (a comment line — leave as is per RESEARCH §5), 67, 70, 84, 94, 468, 675, 804, 1087
  test/test_flapper.jl:58, 110, 151

doc-only (tighten docstring):
  src/components/flapper.jl:38  add explicit "callback-set state(t)" language

doc-only (leave):
  src/components/channels.jl:207, 409  inline comments inside Channel/CHF describing Phase 55 D-08 Hypothesis-A pattern

PK testset path (RESEARCH §3 PK-VAL, R-4):
  build_loop_pk is structurally Δ=0 (already passes the new test_determinacy.jl Testset 1 since
  Plan 58-01 Task 3 built it). solve_steady returns Failure (KINSOL flag −7) for some VAL-PK-* cases;
  the existing transient fallback at test_validation.jl:1059-1064, :1118-1124, :1167-1175 handles it.
  Verification only — no new fix. If a PK testset fails post-Plan 58-04, document and treat as
  out-of-scope numerical convergence (R-4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the last bug-hiding audit site (test_heat_diffusion.jl:185); add inline comments to legitimate-structural sites</name>
  <files>
    test/test_heat_diffusion.jl
    test/test_misc.jl
    test/test_pump.jl
    test/test_resistors.jl
    test/test_channels.jl
    test/test_flapper.jl
  </files>
  <read_first>
    .planning/phases/58-mtk-system-determinacy-repair/scratch/audit_table.md (Plan 58-01 Task 2 output — authoritative audit list)
    .planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md (§5 audit table, lines 258-294)
    test/test_heat_diffusion.jl (lines 175-194 — HDIFF-04 with `[hd.power ~ pwr]` at line 182)
    test/test_misc.jl (lines 17-50 — RL circuit; 65-75 — HX; 125-135 — WT; 170-180 — HFS)
    test/test_pump.jl (lines 16-20, 30-40, 65-70, 80-85, 95-105, 130-135)
    test/test_resistors.jl (line 16-20)
    test/test_channels.jl (lines 65-95 — Hypothesis A spike; lines 460-475, 670-680, 800-810, 1080-1090)
    test/test_flapper.jl (lines 55-60, 105-115, 145-155)
  </read_first>
  <action>
    This task has TWO sub-actions: (a) flip the last bug-hiding audit site; (b) add inline comments to legitimate sites.

    Sub-action A — Flip test_heat_diffusion.jl:185

    Locate test/test_heat_diffusion.jl:185. The current line reads:

        ssys = mtkcompile(sys; fully_determined=false)

    Replace with:

        ssys = mtkcompile(sys; fully_determined=true)

    Justification: line 182 already pins `[hd.power ~ pwr]` (in scope at line 180 inside the `vcat`). Live verification from RESEARCH §5: the system IS structurally determined; the `=false` was a leftover. After this flip, the test continues to compile and pass — RESEARCH did not flag any side effect.

    Sub-action B — Add inline comments to legitimate-structural / isolated-component-test sites

    For each site below, append a one-line `# ` comment to the same line as the `mtkcompile(...; fully_determined=false)` (or `check_length=false`) call. The comment names the structural reason. If a comment already exists on the line, leave it (RESEARCH §5 marks several as "already-commented") — do NOT replace existing comments.

    Use these exact comment strings, chosen to make a future maintainer's grep self-explanatory:

    ## test/test_misc.jl

    Line 19 (Inertia compile in isolation):
        @test_nowarn mtkcompile(L; fully_determined=false)  # isolated component: dangling ports

    Line 37 — already commented, RESEARCH §5 says "tighten" the comment. Replace existing trailing comment with:
        ssys = mtkcompile(sys; fully_determined=false)  # legitimate-structural: pure RL circuit, no T equations exist by design

    Line 41 — current comment is fine (`check_length=false required because T unknowns are underdetermined ...`); leave as is.

    Line 48 — same pre-existing comment string is sufficient; leave as is.

    Line 71 (HeatExchanger compile):
        @test_nowarn mtkcompile(hx; fully_determined=false)  # isolated component: HX is value-source, no port closure needed

    Line 131 (WallTemperature compile):
        ssys = mtkcompile(wt; fully_determined=false)  # isolated component: value-source, only RHS-driven port equations

    Line 178 (HeatFluxSource compile):
        ssys = mtkcompile(hfs; fully_determined=false)  # isolated component: value-source, only RHS-driven port equations

    ## test/test_pump.jl

    Line 18:
        @test_nowarn mtkcompile(pump; fully_determined=false)  # isolated component: unconnected ports

    Line 36:
        ssys5 = mtkcompile(sys5; fully_determined=false)  # isolated network: no pressure anchor on rate-equation test

    Line 68:
        @test_nowarn mtkcompile(pump_s; fully_determined=false)  # isolated component: unconnected ports

    Line 83:
        ssys_r = mtkcompile(sys_r; fully_determined=false)  # isolated network: no pressure anchor (pump+resistor flipped sign)

    Line 99:
        @test_nowarn mtkcompile(pump_c; fully_determined=false)  # isolated component: callable pump in isolation

    Line 133:
        ssys = mtkcompile(sys; fully_determined=false)  # isolated network: no pressure anchor on pump+resistor test

    ## test/test_resistors.jl

    Line 18:
        @test_nowarn mtkcompile(r; fully_determined=false)  # isolated component: pure resistance, no anchor

    ## test/test_heat_diffusion.jl

    Line 44:
        @test_nowarn mtkcompile(hd; fully_determined=false)  # isolated component: dangling thermal ports + unset power(t) by design

    (Line 185 is flipped to =true in Sub-action A above — no comment needed.)

    ## test/test_channels.jl

    The five sites below are Hypothesis-A pattern from Phase 55 D-08 (per RESEARCH §5). One inline comment per site naming the spike origin is enough; do NOT add a multi-line comment block.

    Line 70:
        ssys = mtkcompile(ch; fully_determined=false)  # isolated component: Channel external-input vars (Phase 55 D-08 Hypothesis-A)

    Line 84:
        ssys = mtkcompile(chf; fully_determined=false)  # isolated component: ChannelHeatFlux external-input vars (Phase 55 D-08)

    Line 94:
        ssys = mtkcompile(cac; fully_determined=false)  # isolated component: CAC per-cell ports unconnected (Phase 55 D-08)

    Line 468:
        ssys = mtkcompile(sys; fully_determined=false)  # integration test: per-cell wall-T binding, ports intentionally underdetermined (Phase 55 D-08)

    Line 675:
        ssys = mtkcompile(sys; fully_determined=false)  # integration test: per-cell wall-T binding (Phase 55 D-08)

    Line 804:
        ssys_v1 = mtkcompile(sys_v1; fully_determined=false)  # integration test: per-cell wall-T binding (Phase 55 D-08)

    Line 1087:
        ssys_cac = mtkcompile(sys_cac; fully_determined=false)  # integration test: per-cell wall-T binding (Phase 55 D-08)

    Lines 16, 67 are existing free-standing comments inside files (not `mtkcompile` calls); leave them alone (RESEARCH §5 marks `:16, :67` as comment-line metadata, not call sites — verify by reading those lines and skipping them if they are not `mtkcompile` invocations).

    ## test/test_flapper.jl

    Line 58:
        ssys = mtkcompile(sys; fully_determined=false)  # legitimate-structural: Flapper state(t) is set by ContinuousCallback, not an MTK equation

    Line 110:
        ssys = mtkcompile(sys; fully_determined=false)  # legitimate-structural: Flapper state(t) set by callback (see flapper.jl:38)

    Line 151:
        ssys = mtkcompile(sys; fully_determined=false)  # legitimate-structural: Flapper state(t) set by callback (see flapper.jl:38)

    Verification cycle:

        bin/jl test/test_determinacy.jl
        bin/jl test/runtests.jl

    Expected: test_determinacy.jl is fully GREEN (Phase-58 testset still GREEN — no regression). Full suite is GREEN modulo pre-existing flaky cases (VAL-01 Fourier numerical, NET-03 Cube flow KINSOL convergence — both documented in STATE.md "Blockers/Concerns" as Phase-58-independent).

    Forbidden actions: no `src/` edits in this task (Sub-action B comments only test/ files). The `src/components/flapper.jl:38` docstring tightening is in Task 2, not here. No `check_length=false`. No new branches.
  </action>
  <verify>
    <automated>awk '/fully_determined=false/{n++} END{exit !(n==1)}' test/test_heat_diffusion.jl &amp;&amp; sed -n '185p' test/test_heat_diffusion.jl | grep -q 'fully_determined=true' &amp;&amp; bin/jl test/test_determinacy.jl 2&gt;&amp;1 | grep -E 'Test Summary|Test Passed' &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - The line at `test/test_heat_diffusion.jl:185` reads `ssys = mtkcompile(sys; fully_determined=true)` (the LAST bug-hiding flip).
    - `test/test_heat_diffusion.jl` has exactly ONE remaining `fully_determined=false` call (line 44, the isolated-HD compile in HDIFF-01).
    - Every `mtkcompile(...; fully_determined=false)` call site in `test/test_misc.jl`, `test/test_pump.jl`, `test/test_resistors.jl`, `test/test_channels.jl`, `test/test_flapper.jl`, and `test/test_heat_diffusion.jl:44` has a trailing inline comment containing one of the marker keywords: `isolated`, `legitimate-structural`, `callback`, `Phase 55 D-08`, `value-source`, `no T equations`, or `pressure anchor`. Verified by: `grep -n 'fully_determined=false' <each file>` returning every match with a `#`-marked rationale on the same line.
    - `bin/jl test/test_determinacy.jl` shows all 11 testsets (6 canonical + 5 Phase-58) PASSING.
    - `bin/jl test/runtests.jl` runs to completion. Any failures are pre-existing flakies documented in STATE.md "Blockers/Concerns" (VAL-01 Fourier numerical edge — not the structural one Plan 58-03 just fixed; NET-03 Cube KINSOL flakiness).
    - Working tree dirty across the listed test/ files only. No `src/` edits in this task.
    - `git rev-parse --abbrev-ref HEAD` returns `channels-redesign`.
  </acceptance_criteria>
  <done>Last bug-hiding audit site flipped; legitimate-structural sites carry rationale comments; full suite GREEN; test_determinacy.jl regression test complete.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten src/components/flapper.jl:38 docstring + verify PK testset; phase-close runtests pass</name>
  <files>
    src/components/flapper.jl
  </files>
  <read_first>
    src/components/flapper.jl (lines 30-50 — docstring around line 38)
    test/test_validation.jl (lines 1042-1213 — PK validation testsets and the existing transient-fallback paths at :1059-1064, :1118-1124, :1167-1175)
    .planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md (§3 Scenario PK-VAL, lines 211-213; R-4 lines 477-481)
  </read_first>
  <action>
    Two sub-actions:

    Sub-action A — Tighten src/components/flapper.jl:38 docstring

    Read the existing docstring around line 38. The current text (per RESEARCH §5) is:
        Uncompiled `System`. Call `mtkcompile(sys; fully_determined=false)` before solving a ...

    Tighten to explicitly name the callback-set state as the structural reason. The intent: a future maintainer reading the docstring should NOT have to grep tests to understand why `fully_determined=false` is required. Update the line(s) around 38 to say:

        Uncompiled `System`. The Flapper's `state(t)` is set by an external `ContinuousCallback`,
        not by an MTK equation, so the system is intentionally structurally underdetermined. Call
        `mtkcompile(sys; fully_determined=false)` before solving a ...

    Preserve all surrounding docstring text (Arguments, Returns, etc.). The tighten is purely additive — keep all existing factual content; the change is to make the structural reason explicit.

    Sub-action B — Verify PK testset

    Run:

        bin/jl test/test_validation.jl

    Locate the "PointKinetics validation" testset output (around test_validation.jl:1042). Confirm:

    - The four sub-testsets (VAL-PK-01, VAL-PK-02a, VAL-PK-02b, VAL-PK-03) all REPORT — i.e. the suite does not bail before reaching them. Plan 58-04's audit flip at :204 should have removed the upstream try/catch wrapper trip, allowing the PK section to execute.
    - Whether each VAL-PK-* PASSES or FAILS, the testset reaches its `@test` lines via either `solve_steady` Success OR the transient fallback at lines 1059-1064, 1118-1124, 1167-1175.
    - If ALL four sub-testsets pass, the phase gate "PK testset reaches solve_* (steady or transient fallback)" is satisfied automatically. Document in the phase-close summary.
    - If any VAL-PK-* FAILS the assertion (e.g. `@test abs(P_final) < 0.1`), do NOT attempt to fix it in Phase 58. Per RESEARCH §3 PK-VAL and R-4, that is a separate numerical-conditioning issue (KINSOL tolerances, IC scaling) that is OUT OF PHASE 58 SCOPE. Document the failing assertion in the phase-close SUMMARY (not a TODO; a "deferred follow-up" with the precise failing assertion line and observed vs expected value).
    - If the testset RAISES (does not reach the `@test` line), that is a Phase 58 escape. Treat as a regression and root-cause before declaring Plan 58-05 done.

    Sub-action C — Full-suite confirmation

    Run:

        bin/jl test/runtests.jl

    Required terminal state:
    - `Test Summary` block at the end shows the suite ran to completion.
    - The five formerly-broken Phase-58 scenarios (3 MTR + VAL-01 + VAL-02 steady + VAL-02 transient + simple_loop audit) all reach Success.
    - Pre-existing flakies (VAL-01 Fourier numerical edge, NET-03 Cube KINSOL convergence) MAY still fail — both are Phase-58-independent per STATE.md "Blockers/Concerns" lines 100-102.
    - No `ExtraVariablesSystemException` / `ExtraEquationsSystemException` / `ArgumentError: Equations ... different lengths` / `ArgumentError: System sys: variable sys does not exist` errors anywhere in the output.

    Forbidden: no edits to `src/components/heat_diffusion.jl`. No `check_length=false`. No `Manifest.toml` bump. No new branches.
  </action>
  <verify>
    <automated>grep -q 'ContinuousCallback' src/components/flapper.jl &amp;&amp; bin/jl test/test_determinacy.jl 2&gt;&amp;1 | grep -E 'Determinacy: Phase 58 scenarios' &gt; /dev/null &amp;&amp; OUT=$(mktemp) &amp;&amp; bin/jl test/runtests.jl &gt; "$OUT" 2&gt;&amp;1; RC=$?; ! grep -qE 'ExtraVariablesSystemException|ExtraEquationsSystemException|ArgumentError: System sys' "$OUT" &amp;&amp; grep -qE 'Pass: *[1-9][0-9]*' "$OUT" &amp;&amp; rm -f "$OUT" &amp;&amp; test $RC -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/flapper.jl` docstring around line 38 contains the substring `ContinuousCallback` and explicitly names the callback-set state as the reason for `fully_determined=false`.
    - `bin/jl test/test_validation.jl` runs the PointKinetics validation testset to completion. The four VAL-PK-* sub-testsets all REPORT (none of them is short-circuited by an upstream wrapper trip).
    - `bin/jl test/runtests.jl` runs to completion. Output contains no `ExtraVariablesSystemException`, no `ExtraEquationsSystemException`, no `ArgumentError: Equations ... different lengths`, no `ArgumentError: System sys: variable sys does not exist`.
    - The seven in-scope Phase-58 scenarios (MTR sym, MTR asym, MTR onesided, VAL-01 Fourier, VAL-02 two-plate steady, VAL-02 transient, PK validation) ALL reach a working solver call. Per-tier numerical assertions inside each scenario MAY still fail; those are downstream numerical work (out of Phase 58 scope per CONTEXT.md `<domain>` "Out of scope" and RESEARCH R-4).
    - Working tree dirty in only `src/components/flapper.jl` for this task.
    - `git rev-parse --abbrev-ref HEAD` returns `channels-redesign`.
  </acceptance_criteria>
  <done>flapper.jl docstring names the callback as the structural reason; PK testset reaches its assertions via existing fallback path; full runtests pass with no determinacy/symbol-access errors.</done>
</task>

</tasks>

<threat_model>
## Threat Model — N/A

Internal Julia library; no external attack surface. Plan 58-05 flips one `fully_determined` flag, adds inline comments, and tightens one docstring; no security-relevant code paths added or removed.
</threat_model>

<verification>
End-of-phase integrated check:

    bin/jl test/test_determinacy.jl   # all 11 testsets PASS (6 canonical builders + 5 Phase-58 scenarios)
    bin/jl test/runtests.jl           # full suite runs; no determinacy/symbol-access errors

Audit completeness:
    grep -rn 'fully_determined=false\|check_length=false' src/ test/ | grep -v ':#' | wc -l
should equal the original count MINUS 8 flips (the 7 bug-hiding sites in test_validation.jl already
flipped in Plans 58-02..04 + the 1 in test_heat_diffusion.jl:185 flipped in this plan = 8 total).
Every remaining `=false` site has a trailing `#` comment.
</verification>

<success_criteria>
- 1 audit flip (test_heat_diffusion.jl:185).
- ~22 inline comments on legitimate-structural / isolated-component-test sites.
- 1 docstring tighten (src/components/flapper.jl:38 names ContinuousCallback).
- PK validation testset reaches `solve_*` (steady or transient fallback).
- `bin/jl test/runtests.jl` runs to completion with no determinacy errors and no symbol-access errors.
- All 11 testsets in `test/test_determinacy.jl` PASS.
- No `src/` logic changes (only the flapper.jl docstring touched). No `check_length=false`. No new branches.
</success_criteria>

<output>
After completion, create `.planning/phases/58-mtk-system-determinacy-repair/58-05-SUMMARY.md` documenting: the audit flip at test_heat_diffusion.jl:185, the list of files updated with inline-comment additions, the flapper.jl docstring before/after, live `bin/jl test/test_determinacy.jl` output showing all 11 rows GREEN, live `bin/jl test/runtests.jl` summary line, and any deferred PK numerical follow-ups (if VAL-PK-* assertions fail post-fix).
</output>
