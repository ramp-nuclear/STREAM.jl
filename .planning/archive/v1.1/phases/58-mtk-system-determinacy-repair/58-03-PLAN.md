---
phase: 58-mtk-system-determinacy-repair
plan: 03
type: execute
wave: 3
depends_on: ["58-02"]
files_modified:
  - test/test_validation.jl
  - test/test_determinacy.jl
autonomous: true
requirements: []
scenarios_addressed:
  - VAL-01-FOURIER
  - DETERMINACY-PHASE58 (row 4 of 5)
tags: [mtk, determinacy, val-01, fourier, fix]

must_haves:
  truths:
    - "VAL-01 HD Fourier transient setup `mtkcompile`s with `fully_determined=true`"
    - "VAL-01 testset reaches `solve(ODEProblem)` Success and the four Fourier checkpoints pass"
    - "_build_val01_fourier helper in test_determinacy.jl flips from RED to GREEN"
  artifacts:
    - path: "test/test_validation.jl"
      provides: "One power pin (hd_v01.power ~ 0.0); one audit flip"
      contains: "hd_v01.power ~ 0.0"
    - path: "test/test_determinacy.jl"
      provides: "Helper updated with the same pin"
      contains: "hd_v01.power ~ 0.0"
  key_links:
    - from: "test/test_validation.jl conns_v01"
      to: "hd_v01.power(t)"
      via: "closing equation"
      pattern: "hd_v01\\.power ~ 0\\.0"
---

<objective>
Single HD instance, single closing equation, one audit flip. The VAL-01 Fourier validation builds an isolated plate (`hd_v01`) with both faces pinned to `ConstantTemperature(T_wall)` and `power=0.0` (pure diffusion relaxation toward T_wall). The fix shape from Plan 58-01 diagnostic: append `hd_v01.power ~ 0.0` to `conns_v01`, then flip `fully_determined=false` to `=true` at the same testset.

Sync the matching `_build_val01_fourier` helper in `test/test_determinacy.jl`.

Purpose: Close VAL-01 (4 of 5 Phase-58 scenarios). The Fourier-series numerical assertions (the testset's reason for existing) execute as soon as `mtkcompile` succeeds.

Output: 2 line-level edits in `test_validation.jl` (one pin + one flip); 1 helper edit in `test_determinacy.jl`.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/58-mtk-system-determinacy-repair/58-CONTEXT.md
@.planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md
@.planning/phases/58-mtk-system-determinacy-repair/58-01-SUMMARY.md
@.planning/phases/58-mtk-system-determinacy-repair/scratch/diag_table.md
@CLAUDE.md

<interfaces>
Constructor declaration in test_validation.jl:876:
  @named hd_v01 = HeatDiffusion(;
      nz=nz_v01, nx=nx_v01, Lz=Lz_v01, Lx=Lx_v01, y=y_v01,
      rho_s=rho_s_v01, cp_s=cp_s_v01, k_s=k_s_v01,
      power_shape=ps_v01,
      power=0.0,                  # ← drives the pin value below
  )

Pin shape (from RESEARCH §3 Scenario D, verified live: Δ goes −1 → 0 with this pin):
  hd_v01.power ~ 0.0
</interfaces>

<source_lines>
test/test_validation.jl:842       — VAL-01 testset opens (`@testset "VAL-01: HeatDiffusion transient — Fourier series validation"`)
test/test_validation.jl:874-887   — `@named hd_v01 = HeatDiffusion(...; power=0.0)`
test/test_validation.jl:888-889   — `ct_l`/`ct_r` ConstantTemperature arrays
test/test_validation.jl:890-899   — `conns_v01 = [...]` body
test/test_validation.jl:898       — last `for i in 1:nz_v01]...,` line
test/test_validation.jl:899       — closing `]`
test/test_validation.jl:900-902   — `@named sys_v01 = compose(...)`
test/test_validation.jl:903       — `ssys_v01 = mtkcompile(sys_v01; fully_determined=false)`  (audit flip target)
</source_lines>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `hd_v01.power ~ 0.0` to conns_v01; flip fully_determined; sync test_determinacy.jl helper</name>
  <files>
    test/test_validation.jl
    test/test_determinacy.jl
  </files>
  <read_first>
    test/test_validation.jl (lines 842-927 — entire VAL-01 testset)
    test/test_determinacy.jl (the _build_val01_fourier helper from Plan 58-01)
    src/components/heat_diffusion.jl (line 119 — docstring contract)
    .planning/phases/58-mtk-system-determinacy-repair/58-RESEARCH.md (§3 Scenario D, lines 163-170)
  </read_first>
  <action>
    Three edits total: 2 in `test/test_validation.jl`, 1 in `test/test_determinacy.jl`.

    ### Edit 1 — Pin `hd_v01.power ~ 0.0` (test/test_validation.jl ~:899)

    Locate the closing `]` of `conns_v01`. Current state:

    ```julia
    conns_v01 = [
        [
            connect(ct_l[i].thermal, getproperty(hd_v01, Symbol(:thermal_left, i))) for
            i in 1:nz_v01
        ]...,
        [
            connect(ct_r[i].thermal, getproperty(hd_v01, Symbol(:thermal_right, i))) for
            i in 1:nz_v01
        ]...,
    ]
    ```

    Insert `hd_v01.power ~ 0.0,` as the last element before `]`. Post-edit:

    ```julia
        [
            connect(ct_r[i].thermal, getproperty(hd_v01, Symbol(:thermal_right, i))) for
            i in 1:nz_v01
        ]...,
        hd_v01.power ~ 0.0,
    ]
    ```

    Pin value `0.0` matches the constructor `power=0.0` at line 887. The Fourier validation requires zero internal heat source — relaxation toward T_wall is pure diffusion (RESEARCH §3 Scenario D).

    ### Edit 2 — Flip audit at line 903

    Replace:
    ```julia
    ssys_v01 = mtkcompile(sys_v01; fully_determined=false)
    ```
    with:
    ```julia
    ssys_v01 = mtkcompile(sys_v01; fully_determined=true)
    ```

    ### Edit 3 — Sync `_build_val01_fourier` helper in test_determinacy.jl

    The helper was created in Plan 58-01 Task 3 with NO pin (mirroring the broken state). Add `hd_v01.power ~ 0.0,` as the last element before the closing `]` of its `conns_v01` vector. Mirror Edit 1 exactly.

    ### Verification cycle

    ```
    bin/jl test/test_determinacy.jl
    ```
    Expected: `_build_val01_fourier` row in "Determinacy: Phase 58 scenarios" testset flips from RED to GREEN. The three MTR rows already passed in Plan 58-02; the VAL-02 row remains RED (Plan 58-04).

    ```
    bin/jl test/test_validation.jl
    ```
    Expected: VAL-01 testset reaches the Fourier-series checkpoint loop and runs all four `@test isapprox(T_num, T_ref; rtol=0.01)` assertions (test_validation.jl:920-922) plus the final `@test isapprox(T_center_series[end], T_wall; rtol=0.01)` (line 926). Whether all four PASS is a numerical-correctness check — RESEARCH §3 Scenario D verified the Δ collapse to 0 but did NOT re-verify the analytical reference. If any of the four checkpoints fails post-fix, that is a separate numerical investigation (out of scope for Phase 58 — Phase 58's "must reach a working solver call" gate is satisfied as long as `solve` returns `Success`).

    ### Branching invariant
    Verify `git rev-parse --abbrev-ref HEAD` returns `channels-redesign`. Do NOT run any branch-creating or branch-switching git commands.

    ### Forbidden actions
    - Do NOT add `check_length=false` (D-05).
    - Do NOT edit `src/components/heat_diffusion.jl`.
    - Do NOT bump `Manifest.toml`.
    - Do NOT widen Fourier-checkpoint tolerances. The fix is structural; if numerics fail post-fix, that is its own investigation.
  </action>
  <verify>
    <automated>grep -c 'hd_v01\.power ~ 0\.0' test/test_validation.jl | awk '$1 == 1 {exit 0} {exit 1}' &amp;&amp; ! grep -n 'ssys_v01 = mtkcompile(sys_v01; fully_determined=false)' test/test_validation.jl &amp;&amp; grep -c 'hd_v01\.power ~ 0\.0' test/test_determinacy.jl | awk '$1 == 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'hd_v01.power ~ 0.0' test/test_validation.jl` returns exactly 1 match (the new pin).
    - The line at `test/test_validation.jl:903` reads `ssys_v01 = mtkcompile(sys_v01; fully_determined=true)`.
    - `grep -n 'hd_v01.power ~ 0.0' test/test_determinacy.jl` returns exactly 1 match (the helper sync).
    - `bin/jl test/test_determinacy.jl` reports `_build_val01_fourier` row in "Determinacy: Phase 58 scenarios" as PASSING. The three MTR rows still PASS (regression check). The VAL-02 row may still fail.
    - `bin/jl test/test_validation.jl` runs the VAL-01 testset to completion (does not raise `ArgumentError: Equations ... different lengths`); `solve(ODEProblem)` returns successfully.
    - `git rev-parse --abbrev-ref HEAD` returns `channels-redesign`.
    - Working tree contains exactly two modified files: `test/test_validation.jl` and `test/test_determinacy.jl`. No `src/` files modified.
  </acceptance_criteria>
  <done>VAL-01 Fourier closing equation added; audit site flipped; helper synced; `_build_val01_fourier` testset row GREEN.</done>
</task>

</tasks>

<threat_model>
## Threat Model — N/A

Internal Julia library; no external attack surface. Plan 58-03 adds one closing equation and flips one `fully_determined` flag; no security-relevant code paths added or removed.
</threat_model>

<verification>
Per-task gate:
```
bin/jl test/test_determinacy.jl
```
The `_build_val01_fourier` row PASSES. MTR rows remain GREEN (no regression).

Wave gate:
```
bin/jl test/test_determinacy.jl && bin/jl test/test_validation.jl
```
VAL-01 testset reaches the Fourier checkpoints; structural error gone.
</verification>

<success_criteria>
- 1 `hd_v01.power ~ 0.0` pin in `test/test_validation.jl` (after `conns_v01` sub-arrays).
- 1 audit flip at line 903.
- 1 `_build_val01_fourier` helper updated.
- VAL-01 testset reaches `solve(ODEProblem)` Success.
- "Determinacy: Phase 58 scenarios" — VAL-01 row is GREEN; MTR rows still GREEN.
- No edits to `src/`. No `check_length=false`. No new branches.
</success_criteria>

<output>
After completion, create `.planning/phases/58-mtk-system-determinacy-repair/58-03-SUMMARY.md` documenting the 3 line-level edits with file:line + before/after, live `bin/jl test/test_determinacy.jl` output showing VAL-01 row GREEN, and `bin/jl test/test_validation.jl` output showing VAL-01 reaches Fourier checkpoints.
</output>
