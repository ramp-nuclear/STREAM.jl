---
phase: 55
plan: 11
type: execute
wave: 7
depends_on: ["55-10"]
files_modified:
  - test/test_thresholds.jl
  - test/runtests.jl
  - CLAUDE.md
autonomous: true
worktree: false
requirements:
  - TEST-05
user_setup: []
nyquist_compliant: true

must_haves:
  truths:
    - "test/test_analysis.jl renamed to test/test_thresholds.jl via `git mv` (D-20). No content change — pure rename."
    - "test/runtests.jl updated: `include(\"test_analysis.jl\")` becomes `include(\"test_thresholds.jl\")`."
    - "CLAUDE.md File Structure Standard updated to reflect (a) the new src/components/sources.jl file (D-24) and (b) the post-Phase-55 14-file test layout."
    - "Full local test suite returns no NEW failures vs the v1.0 baseline. Pre-existing flakies (VAL-01 Fourier numerical, NET-03 cube KINSOL convergence) tolerated per CONTEXT.md D-22."
    - "TEST-05 close gate met: `bin/jl test/runtests.jl` runs end-to-end and any failures are confined to the v1.0 baseline known-flaky list."
  artifacts:
    - path: "test/test_thresholds.jl"
      provides: "Renamed from test/test_analysis.jl (D-20 — pure rename, no content change)"
      contains_no: "(none)"
    - path: "test/runtests.jl"
      provides: "Final 14-file test layout (Phase 55 close)"
      contains: "include(\"test_thresholds.jl\")"
      contains_no: "include(\"test_analysis.jl\")"
    - path: "CLAUDE.md"
      provides: "File Structure Standard updated for sources.jl + final test layout"
      contains: "sources.jl"
  key_links:
    - from: "test/runtests.jl"
      to: "every test_*.jl in the final 14-file layout"
      via: "ordered include() statements"
      pattern: "include\\(\"test_"
---

<plan_meta>
**Phase:** 55 — Composition Helpers, Examples & Test Suite
**Plan:** 11 — Phase 55 close gate: test_thresholds rename + runtests.jl final wiring + CLAUDE.md doc fix + full-suite TEST-05 verify
**Wave:** 7 — depends on plan 55-10 (test_integration.jl shipped, 4 absorbed files deleted)
**Estimated context:** ~25-30%
**Concurrency:** sequential (final wave; single plan)
**Worktree:** false — close-gate verification touches the full test suite which is the canonical location; running in a worktree would need to re-run the suite at merge time anyway. Run in the working tree directly to avoid double-work.
</plan_meta>

<objective>
Close Phase 55. Three remaining tasks:

1. **Rename `test/test_analysis.jl` → `test/test_thresholds.jl`** (D-20 — pure rename to match Python STREAM's `test_libraries/test_thresholds.py`).
2. **Final `test/runtests.jl` wiring** for the 14-file post-Phase-55 layout (D-22).
3. **Update `CLAUDE.md` File Structure Standard** to reflect the new `src/components/sources.jl` file + final test layout (D-24).
4. **Run the full test suite** and confirm no NEW failures vs the v1.0 baseline (TEST-05 close gate per D-22).

After this plan, Phase 55 is complete — TEST-01 (test_channels rewrite), TEST-02 (builders + examples), TEST-03 (composition helpers verified), TEST-05 (full suite green vs baseline) all met. TEST-04 (Python parity) is Phase 56's deliverable.

Purpose: TEST-05 fulfilled — Phase 55 close gate met.
Output: Renamed `test_thresholds.jl`, final `runtests.jl`, updated `CLAUDE.md`, signed-off TEST-05 verification report.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/55-composition-helpers-examples-test-suite/55-CONTEXT.md
@.planning/phases/55-composition-helpers-examples-test-suite/55-RESEARCH.md
@.planning/phases/55-composition-helpers-examples-test-suite/55-VALIDATION.md
@CLAUDE.md
@test/runtests.jl
@test/test_analysis.jl
</context>

<interfaces>
<!-- D-22 close gate criterion (CONTEXT.md) -->
<!--   `bin/jl test/runtests.jl` returns no NEW failures vs the v1.0 baseline. -->
<!--   Pre-existing flakies (VAL-01 Fourier numerical, NET-03 Cube flow KINSOL convergence) -->
<!--   remain tolerated per the v1.0 baseline. -->

<!-- v1.0 baseline source (revision 1 fix): -->
<!--   `git tag --list` confirms `v1.0` tag exists in the repository. The baseline -->
<!--   capture in Step 2 of Task 3 uses the tag directly, not a commit SHA. -->

<!-- Final 14-file test layout (CONTEXT.md test layout block, lines 165-229): -->
<!--   test_geometry.jl, test_pump.jl, test_resistors.jl, test_misc.jl, test_heat_diffusion.jl, -->
<!--   test_flapper.jl, test_channels.jl, test_connectors.jl, test_point_kinetics.jl, -->
<!--   test_fluids.jl, test_correlations.jl, test_thresholds.jl (RENAMED), -->
<!--   test_composition.jl, test_integration.jl (NEW), test_validation.jl -->
<!--   = 15 listed but D-22 says 14 — the discrepancy is because test_validation.jl -->
<!--   is conventionally separated from "regular" tests since Phase 56's deliverable. -->
<!--   The runtests.jl includes all 14 + test_validation.jl. -->
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename test_analysis.jl → test_thresholds.jl + update runtests.jl + finalize layout</name>
  <files>test/test_thresholds.jl, test/runtests.jl</files>
  <read_first>
    - test/test_analysis.jl (CURRENT — confirm content; D-20 says pure rename, no content change)
    - test/runtests.jl (CURRENT — after plans 55-05 + 55-10 edits; should now have only `test_analysis.jl` to rename)
    - .planning/phases/55-composition-helpers-examples-test-suite/55-CONTEXT.md test layout block (lines 165-229; D-20)
  </read_first>
  <action>
**Step 1 — Rename:**

```bash
git mv test/test_analysis.jl test/test_thresholds.jl
```

This is a pure rename — content unchanged. Confirm:

```bash
diff <(git show HEAD:test/test_analysis.jl) test/test_thresholds.jl
```

Expected: zero diff. If non-zero, the executor accidentally edited the file; revert.

**Step 2 — Update runtests.jl:**

Read the current `test/runtests.jl` and replace `include("test_analysis.jl")` with `include("test_thresholds.jl")`.

After the edit, the final 15-include layout (14 functional + test_validation.jl) should be:

```julia
# test/runtests.jl — thin orchestrator (Phase 55 close)
# All test logic lives in the individual test_*.jl files.
#
# 14-file Phase 55 final layout (per Python STREAM rules + 55-CONTEXT.md D-17..D-22):
#   - Per-component unit tests: geometry, pump, resistors, misc, heat_diffusion, flapper,
#     channels (consolidated Channel + CHF + CAC + _channel_core + sign-safety), connectors,
#     point_kinetics (component-only after Phase 55 trim).
#   - Library tests: fluids, correlations, thresholds (renamed from analysis).
#   - Composition: composition (rewritten under Phase 55 D-18).
#   - Integration: integration (NEW; absorbs examples/solvers/loss_of_flow/subcooled_boiling
#     + TF-06/07 from point_kinetics).
#   - External-reference validation: validation (untouched; Phase 56's deliverable).

include("test_geometry.jl")
include("test_connectors.jl")
include("test_fluids.jl")
include("test_channels.jl")
include("test_pump.jl")
include("test_flapper.jl")
include("test_resistors.jl")
include("test_misc.jl")
include("test_heat_diffusion.jl")
include("test_correlations.jl")
include("test_thresholds.jl")
include("test_composition.jl")
include("test_validation.jl")
include("test_integration.jl")
include("test_point_kinetics.jl")
```

**Step 3 — Smoke:**

```bash
bin/jl test/test_thresholds.jl
```

Expected: same testsets that passed in test_analysis.jl pre-rename now pass under the new filename.

```bash
bin/jl test/runtests.jl
```

Expected: every include is reachable. Test pass/fail counts go to Task 3 (TEST-05 close-gate verification).
  </action>
  <verify>
    <automated>test -f test/test_thresholds.jl && ! test -f test/test_analysis.jl && grep -q 'include("test_thresholds.jl")' test/runtests.jl && (grep -c 'include("test_analysis.jl")' test/runtests.jl | xargs -I {} test {} -eq 0)</automated>
  </verify>
  <done>
    test/test_analysis.jl renamed to test/test_thresholds.jl (zero content diff); test/runtests.jl includes test_thresholds.jl in place of test_analysis.jl; the 14-file final layout is wired; `bin/jl test/test_thresholds.jl` runs the same testsets that previously ran in test_analysis.jl.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update CLAUDE.md File Structure Standard for sources.jl + final test layout</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md "File Structure Standard" section (full)
    - .planning/phases/55-composition-helpers-examples-test-suite/55-CONTEXT.md D-24 (full prescriptive text)
  </read_first>
  <action>
Apply two CLAUDE.md edits:

**Edit 1: src/components/ tree (CLAUDE.md "File Structure Standard" → src/ section).**

Insert a new line for `sources.jl` between `misc.jl` and `channels.jl` to mirror the post-Wave-1 directory structure. The current shape:

```
  components/
    pump.jl                     # Pump (fixed-dP and fixed-mdot modes)
    resistors.jl                # Friction, Gravity, Resistor
    misc.jl                     # Inertia, HeatExchanger, ConstantTemperature
    channels.jl                 # Channel, ChannelHeatFlux, ChannelAndContacts + _channel_core (shared private core)
    heat_diffusion.jl           # HeatDiffusion + _diffusion_eqs (2D FD solid plate)
```

becomes:

```
  components/
    pump.jl                     # Pump (fixed-dP and fixed-mdot modes)
    resistors.jl                # Friction, Gravity, Resistor
    misc.jl                     # Inertia, HeatExchanger, ConstantTemperature
    sources.jl                  # WallTemperature, HeatFluxSource (value-source subsystems for channel external inputs)
    channels.jl                 # Channel, ChannelHeatFlux, ChannelAndContacts + _channel_core (shared private core)
    heat_diffusion.jl           # HeatDiffusion + _diffusion_eqs (2D FD solid plate)
```

**Edit 2: test/ tree.**

Update the `test/` block to reflect the post-Phase-55 14-file layout. Replace the current block with:

```
test/
  runtests.jl                 # Thin orchestrator: one include() per test file, nothing else
  test_geometry.jl            # PipeGeometry tests (PHY-01)
  test_connectors.jl          # FlowPort, ThermalPort (HeatFluxPort retired in Phase 55 D-06)
  test_fluids.jl              # Fluid property functions (FOUND-02)
  test_channels.jl            # Channel/CHF/CAC variants + _channel_core enthalpy-form physics
                              # + flow-reversal sign safety (Phase 55 D-17 unified file —
                              # absorbs legacy test_channel.jl, test_channel_core.jl,
                              # test_sign_safety.jl)
  test_pump.jl                # Pump tests (COMP-02, PHY-05)
  test_flapper.jl             # Flapper tests
  test_resistors.jl           # Friction, Gravity, Resistor, network tests (COMP-03/04, NET-*)
  test_misc.jl                # Inertia, HeatExchanger, ConstantTemperature, WallTemperature, HeatFluxSource
  test_heat_diffusion.jl      # HeatDiffusion (HDIFF-01..05)
  test_correlations.jl        # HTC + friction correlation function unit tests
  test_thresholds.jl          # CHF/OFI/OSV/ONB/twall + ChannelState (renamed from test_analysis.jl, Phase 55 D-20)
  test_composition.jl         # symmetric_plate, plate, one_sided_connection, compose_systems,
                              # port, check_gravity_mismatch, _infer_n, connect_temperature_feedback
                              # — heavy CAC↔HD coverage (Phase 55 D-18)
  test_validation.jl          # Quantitative cross-validation against Python STREAM (Phase 56)
  test_integration.jl         # NEW: single big integration file — builders, solvers,
                              # LOF transient, ISCB, PK loops, COMPAT (Phase 55 D-19)
  test_point_kinetics.jl      # PK component-unit tests only (TF-06/07 moved to test_integration.jl)
```

**Edit 3: "Test placement rule" sentence.**

The current rule "Test file mirrors source file" is preserved. Add a clarifying note for the new value-source family — `WallTemperature`/`HeatFluxSource` tests live in `test_misc.jl` per D-21 (planner judgment, documented).

After all three edits, do not run anything specific to CLAUDE.md (it's documentation), but confirm the file is syntactically valid markdown:

```bash
head -n 80 CLAUDE.md
```

Visual inspection: tree blocks render correctly; no orphaned indentation.
  </action>
  <verify>
    <automated>grep -q 'sources.jl' CLAUDE.md && grep -q 'test_integration.jl' CLAUDE.md && grep -q 'test_thresholds.jl' CLAUDE.md && grep -qE 'WallTemperature.*HeatFluxSource|sources\.jl.*WallTemperature' CLAUDE.md</automated>
  </verify>
  <done>
    CLAUDE.md File Structure Standard reflects the post-Phase-55 reality: `src/components/sources.jl` listed; `test/` block updated to the 14-file layout; the `test_misc.jl` line mentions WallTemperature/HeatFluxSource; the renamed `test_thresholds.jl` appears (not `test_analysis.jl`).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: TEST-05 close-gate run — full suite green vs v1.0 baseline (using v1.0 git tag)</name>
  <files>(none — verification + report)</files>
  <read_first>
    - test/runtests.jl (final form after Task 1)
    - .planning/phases/55-composition-helpers-examples-test-suite/55-CONTEXT.md D-22 (close-gate criterion: "no NEW failures vs v1.0 baseline; pre-existing flakies VAL-01 + NET-03 tolerated")
    - .planning/phases/55-composition-helpers-examples-test-suite/55-VALIDATION.md "Manual-Only Verifications" (the v1.0 baseline regression interpretation guidance)
    - .planning/STATE.md (current branch is `channels-redesign`)
    - `git tag --list` confirms the `v1.0` tag is the canonical baseline (revision 1 fix replaces the prior commit-SHA reference with the tag).
  </read_first>
  <action>
**Step 1 — Run the full test suite on the current branch:**

```bash
# Restart daemon (large session post-Phase-55; minimize Revise drift risk)
tmux kill-session -t stream-jl 2>/dev/null || true
bin/jl-up

# Capture timing + full stdout
runtime=$( { time bin/jl test/runtests.jl 2>&1 | tee /tmp/phase55_full_suite.log ; } 2>&1 | tail -n 3 )
echo "Wall time: $runtime"
```

Expected total runtime: ~90-180s warm. Failures from individual `@test` calls do NOT abort the suite — Julia's `Test` stdlib reports a per-testset error/fail count.

**Step 2 — Capture the v1.0 baseline failure list using the v1.0 git tag:**

The `v1.0` tag is the canonical baseline (verified via `git tag --list`). Use a temporary worktree pinned to that tag and run the test suite there with cold-start `julia --project=.` (NOT `bin/jl`) — cold-start avoids any cross-contamination from the main daemon's loaded module state.

```bash
# Skip the baseline capture if a previous log exists (it's expensive — ~2-3 min cold).
if [ ! -f /tmp/v10_baseline_failures.log ]; then
    git worktree add /tmp/v10_baseline_worktree v1.0
    cd /tmp/v10_baseline_worktree && julia --project=. test/runtests.jl 2>&1 | tee /tmp/v10_baseline_failures.log || true
    cd -
    git worktree remove /tmp/v10_baseline_worktree
fi
```

Why cold-start `julia --project=.` instead of `bin/jl`: the main daemon (tmux session `stream-jl`) is watching `/home/itay/projects/Julia-STREAM/src` for Revise hot-reloads, so submitting work that runs from a different worktree path would either trigger a Revise miss (silent stale module) or pollute the daemon's module state with the v1.0 source. A standalone `julia --project=.` in the worktree is hermetic.

**If running the v1.0 baseline is impractical** (too slow, or the worktree doesn't compile cleanly under v1.0), fall back to the documented expectation per CONTEXT.md D-22 + STATE.md:
- VAL-01 (Fourier series validation in test_integration.jl §LOF section) is a pre-existing flaky numerical test — UNCHANGED.
- NET-03 (Cube flow KINSOL convergence in test_integration.jl §Builders smokes section) is a pre-existing KINSOL convergence failure — UNCHANGED.
- Any other failure is NEW and must be investigated.

**Step 3 — Diff the failures:**

```bash
# Extract failure lines from both logs (look for "Test Failed" or "Error During Test")
grep -E 'Test (Failed|Error)' /tmp/phase55_full_suite.log > /tmp/phase55_fails.txt
grep -E 'Test (Failed|Error)' /tmp/v10_baseline_failures.log > /tmp/v10_fails.txt 2>/dev/null || true

# diff
diff /tmp/v10_fails.txt /tmp/phase55_fails.txt | tee /tmp/phase55_diff.txt
```

If `/tmp/v10_baseline_failures.log` doesn't exist (Step 2 skipped), use the manual triage criterion:
- For each failure in `/tmp/phase55_fails.txt`, check if it matches VAL-01 or NET-03 by name.
- Anything else is a NEW failure → BLOCKER.

**Step 4 — Decision:**

- **No new failures (only VAL-01 + NET-03 tolerated):** TEST-05 close gate met. Proceed to write the SUMMARY (next step). The phase ships.
- **New failures present:** STOP. List them in the SUMMARY as a `## BLOCKER` section. Do NOT close the phase. Surface to the orchestrator.

**Step 5 — Write the close-gate verification report:**

Append a section to `55-11-SUMMARY.md` (this plan's summary):

```markdown
## TEST-05 Close-Gate Verification

**Full-suite command:** `bin/jl test/runtests.jl`
**Baseline reference:** `v1.0` git tag (captured in /tmp/v10_baseline_worktree via cold-start `julia --project=.`)
**Total runtime:** {RUNTIME_MEASURED}
**Total testsets:** {N}
**Total tests:** {M}
**Failures:** {K}

### Failure breakdown

| Failure | Pre-existing in v1.0 baseline? | Status |
|---------|-------------------------------|--------|
| VAL-01: Fourier series validation | YES (CONTEXT.md D-22) | TOLERATED |
| NET-03: Cube flow KINSOL convergence | YES (STATE.md blockers) | TOLERATED |
| ... (any other) | YES/NO | TOLERATED / NEW (BLOCKER) |

### Verdict

{TEST-05 close gate met / TEST-05 close gate NOT met — N new failures}
```

**No git commit happens in this plan.** The orchestrator's `gsd-execute-phase` finalization (or `/gsd:ship`) handles the milestone-close commit.
  </action>
  <verify>
    <automated>bin/jl test/runtests.jl 2>&1 | tee /tmp/phase55_full_suite.log > /dev/null; grep -c 'Test Summary' /tmp/phase55_full_suite.log | xargs -I {} test {} -ge 1</automated>
  </verify>
  <done>
    `bin/jl test/runtests.jl` runs end-to-end and produces a complete Test Summary report; the v1.0 baseline was captured via `git worktree add /tmp/v10_baseline_worktree v1.0` + cold-start `julia --project=.` (NOT a commit SHA — the v1.0 tag is the canonical baseline reference); failures (if any) are categorized in the SUMMARY against the v1.0 baseline (VAL-01 / NET-03 tolerated); if any failure is NEW, the close gate is not met and a BLOCKER is surfaced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure in-process MTK simulation — no external attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-11 | (N/A) | doc edits + close-gate verification | accept | Pure in-process simulation library; no network, no auth, no untrusted input. ASVS L1 N/A. |
</threat_model>

<verification>
- `test -f test/test_thresholds.jl` returns 0; `! test -f test/test_analysis.jl`
- `grep -q 'include("test_thresholds.jl")' test/runtests.jl` succeeds
- CLAUDE.md mentions `sources.jl`, `test_integration.jl`, `test_thresholds.jl`
- `bin/jl test/runtests.jl` runs end-to-end with no NEW failures vs v1.0 baseline
- v1.0 baseline reference is the `v1.0` git tag (captured via `git worktree add /tmp/v10_baseline_worktree v1.0`), NOT a commit SHA
</verification>

<success_criteria>
1. test_analysis.jl renamed to test_thresholds.jl (pure rename per D-20).
2. test/runtests.jl reflects the final 14-file Phase 55 layout.
3. CLAUDE.md File Structure Standard updated for sources.jl + final test layout (D-24).
4. TEST-05 close gate met: full local test suite green vs v1.0 baseline (only pre-existing flakies VAL-01 + NET-03 tolerated per D-22).
5. v1.0 baseline capture uses the canonical `v1.0` git tag.
6. If new failures appear, plan halts with a BLOCKER. Otherwise Phase 55 closes.
</success_criteria>

<output>
After completion, create `.planning/phases/55-composition-helpers-examples-test-suite/55-11-SUMMARY.md` documenting:
- Rename outcome (test_analysis.jl → test_thresholds.jl, zero content diff)
- runtests.jl final form (14-file layout)
- CLAUDE.md File Structure Standard edits (sources.jl line + test layout block)
- Full-suite test runtime + total testset count + total test count + failure count
- Failure-by-failure triage against v1.0 baseline (captured from `v1.0` git tag)
- TEST-05 close gate verdict (PASS / FAIL)
- If PASS: Phase 55 ships. ROADMAP.md Phase 55 status flips to `[x]`. Plans 56's TEST-04 cross-validation is the next milestone gate.
- If FAIL: BLOCKER section listing the NEW failures + reproducer commands.
</output>
