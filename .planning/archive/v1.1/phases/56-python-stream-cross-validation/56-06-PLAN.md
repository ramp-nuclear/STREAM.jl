---
phase: 56-python-stream-cross-validation
plan: 06
type: execute
wave: 4
depends_on:
  - 05
files_modified:
  - .planning/MILESTONES.md
autonomous: false
requirements:
  - TEST-04
tags:
  - testing
  - validation
  - milestone-close
  - cleanup
truths:
  - "ROADMAP success criterion 4 grep cleanup verified — no `_channel_base_eqs`, `observed_mode`, `skip_htc` references in src/ or test/"
  - "Branch `channels-redesign` is the working branch (per CLAUDE.md branching policy)"
  - "Worst-drift GRAY row (scenario + quantity + rtol with sign) is captured in MILESTONES.md narrative entry per D-09"
  - "If h_tc tier or other parity-test FAIL surfaced in Plan 05, Plan 06 closes the loop: either documents in MILESTONES.md as accepted-FAIL with rationale (and re-runs with a documented widened hard_ceiling — D-04 planner discretion + D-11 known equivalence gap explicit documentation), or escalates to a future phase"
  - "Per D-12 milestone close gate: full test suite has no NEW failures vs Phase 55 baseline + drift report committed + MILESTONES.md narrative entry exists + cleanup grep returns empty"
must_haves:
  truths:
    - "`grep -rn '_channel_base_eqs\\|observed_mode\\|skip_htc' src/ test/` returns no matches"
    - "Current git branch is `channels-redesign` — verified before any commit"
    - ".planning/MILESTONES.md gains a v1.1-close entry naming the worst per-quantity drift in the gray zone with sign and magnitude (D-09)"
    - "If a hard_ceiling was widened to accommodate a documented gap, the widening is recorded in MILESTONES.md alongside the gap rationale (D-04 + D-11)"
    - "test/data/parity_report.csv is up-to-date with the final run committed alongside MILESTONES.md (D-12 gate item 2)"
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "v1.1 close narrative entry per D-09"
      contains: "v1.1"
  key_links:
    - from: ".planning/MILESTONES.md v1.1 entry"
      to: "test/data/parity_report.csv worst-drift row"
      via: "MILESTONES.md narrative cites the worst-drift quantity + rtol value"
      pattern: "drift"
---

<objective>
Close out Phase 56 by performing the milestone-close cleanup gate items per D-12, authoring the v1.1-close narrative entry in `.planning/MILESTONES.md` per D-09 (which also documents D-01's "rewrite, not diagnose" framing as the phase's organizing principle), and (if needed) deciding what to do with any FAIL-tier rows surfaced by Plan 05's parity report — accept with documented hard_ceiling widening per D-04 + D-11, or escalate as a real Julia bug for a future phase.

Purpose: Without this plan, Phase 56 produces a parity harness that may report FAIL-tier rows (e.g., h_tc per Gap #2) but no narrative is appended to MILESTONES.md, no documentation closes the loop on the documented gaps, and the milestone gate D-12 isn't formally satisfied. This plan is the final phase-close action: cleanup grep, branch verification, MILESTONES.md authorship, and (if applicable) hard_ceiling adjustment with rationale.

Output: One MODIFIED file (.planning/MILESTONES.md) with a v1.1-close entry. Possibly one MODIFIED file (test/test_validation.jl) if a hard_ceiling needs widening with rationale (this is the human's call in Task 2). Possibly one re-run of `bin/jl test/runtests.jl` to confirm the full suite is green or has only documented failures. Plan 06 is `autonomous: false` because the MILESTONES.md narrative wording (D-09 Claude's Discretion) and the accept-FAIL-vs-widen-vs-escalate decision (Open Question 2 in RESEARCH.md) both require human input.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-python-stream-cross-validation/56-CONTEXT.md
@.planning/phases/56-python-stream-cross-validation/56-RESEARCH.md
@.planning/phases/56-python-stream-cross-validation/56-VALIDATION.md
@.planning/phases/56-python-stream-cross-validation/56-05-SUMMARY.md
@.planning/MILESTONES.md
@CLAUDE.md
@test/test_validation.jl
@test/data/parity_report.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: ROADMAP success criterion 4 grep cleanup + branch verification (autonomous)</name>
  <read_first>
    - .planning/ROADMAP.md §"Phase 56: Python STREAM Cross-Validation" (success criterion 4)
    - .planning/phases/56-python-stream-cross-validation/56-CONTEXT.md (D-12 gate item 6)
    - CLAUDE.md §"Branching Policy"
  </read_first>
  <files></files>
  <action>
    Run the cleanup grep that ROADMAP success criterion 4 mandates:

    ```bash
    cd /home/itay/projects/Julia-STREAM
    if grep -rn '_channel_base_eqs\|observed_mode\|skip_htc' src/ test/ 2>/dev/null; then
        echo "FAIL: stale Phase 53 cruft detected — Phase 53 was supposed to remove these"
        exit 1
    else
        echo "[Plan 56-06 cleanup] grep clean — no _channel_base_eqs / observed_mode / skip_htc references"
    fi
    ```

    Expected: zero matches. Phase 53 (CORE-02, CORE-03, CORE-04) deleted these flag-driven helpers. If matches exist, that's a Phase 53 regression — surface as a real bug to fix, not a Phase 56 deliverable. Likely cause: a recent edit accidentally re-introduced a reference. Locate the file/line and fix.

    Verify the working branch is `channels-redesign`:

    ```bash
    BRANCH=$(git rev-parse --abbrev-ref HEAD)
    if [ "$BRANCH" != "channels-redesign" ]; then
        echo "FAIL: current branch '$BRANCH' is not 'channels-redesign'"
        echo "Per CLAUDE.md branching policy, GSD must NEVER auto-create branches."
        echo "User owns branch creation. Stop and ask before proceeding."
        exit 1
    else
        echo "[Plan 56-06 branch] OK — on channels-redesign per CLAUDE.md policy"
    fi
    ```

    Verify .planning/config.json has `git.branching_strategy: "none"`:

    ```bash
    grep -q '"branching_strategy": "none"' .planning/config.json || \
        { echo "FAIL: branching_strategy is not 'none' — fix per CLAUDE.md before proceeding"; exit 1; }
    echo "[Plan 56-06 config] git.branching_strategy = 'none' per CLAUDE.md"
    ```

    Run the full test suite to capture the post-Plan-05 baseline:

    ```bash
    bin/jl test/runtests.jl 2>&1 | tee /tmp/56-06_full_suite.log
    FULL_RC=${PIPESTATUS[0]}
    echo "[Plan 56-06 full-suite] exit code: $FULL_RC"
    grep -E '@testset|Test Summary:|FAIL|Error During Test' /tmp/56-06_full_suite.log | tail -50
    ```

    The full suite is expected to have either:
    - Pre-existing flakies tolerated per Phase 55 D-22 (NET-03 Cube flow, VAL-01 HD Fourier — neither caused by Phase 56)
    - Possibly NEW FAIL-tier rows from Plan 05's parity testsets driven by Gap #2 (h_tc) — these are surfaced honestly per RESEARCH.md Open Question 2

    Compare against Phase 55 baseline (recorded in `.planning/phases/55-composition-helpers-examples-test-suite/55-VERIFICATION.md`). Tally:
    - PRE-Phase-56 known-flaky failures: <list>
    - NEW failures introduced by Phase 56: <list>

    If NEW failures are non-Gap-#2-related (e.g., a regression in the harness machinery, an MTK API drift, a typo in test_validation.jl), STOP and return to Plan 56-05 for fixes. If NEW failures are exactly the Gap #2 h_tc rows surfaced by the parity harness — that's expected per RESEARCH.md Open Question 2 — Task 2 (human checkpoint) decides what to do with them.

    Record the tally in the SUMMARY for Task 2's human review.
  </action>
  <verify>
    <automated>! grep -rn '_channel_base_eqs\|observed_mode\|skip_htc' src/ test/ 2>/dev/null && [ $(git rev-parse --abbrev-ref HEAD) = "channels-redesign" ] && grep -q '"branching_strategy": "none"' .planning/config.json && test -f /tmp/56-06_full_suite.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '_channel_base_eqs\|observed_mode\|skip_htc' src/ test/` returns no matches (ROADMAP success criterion 4)
    - Current git branch is `channels-redesign` (CLAUDE.md branching policy)
    - .planning/config.json has `"branching_strategy": "none"`
    - /tmp/56-06_full_suite.log contains the captured output of `bin/jl test/runtests.jl`
    - The pre-existing flakies vs NEW failures tally is recorded in the Plan 06 SUMMARY for Task 2's human review
  </acceptance_criteria>
  <done>
    Cleanup grep is clean, branch is channels-redesign, full suite log is captured, and the failure tally is documented for the Task 2 human checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of parity report + decide on FAIL-tier row dispositions</name>
  <what-built>
    Task 1 ran the full test suite and captured /tmp/56-06_full_suite.log. The Plan 56-05 SUMMARY recorded the worst-drift GRAY row and the FAIL row list. The harness has ALREADY honestly surfaced what the Python↔Julia parity looks like under the new enthalpy-form energy balance (Phase 53 NRG-01..04).

    Three possible verdicts the developer can land on:

    **(a) FULL PASS** — All parity testsets are CLEAN/GRAY only; no FAIL rows. This is the best case: 1.75% mdot drift falls in GRAY (≤ 2% hard_ceiling) and is documented as "drift = 1.75% on mdot, candidate cause Gap #2 (HTC film-T vs bulk-T)" in MILESTONES.md per D-09. Milestone closes cleanly.

    **(b) DOCUMENTED-FAIL with widened hard_ceiling** — Some quantities (likely h_tc per Gap #2 — RESEARCH.md estimates ~5-20% h drift) FAIL at default 2%. Per D-04 + D-11 + RESEARCH.md Open Question 2, the developer can widen specific hard_ceiling values with rationale (e.g., `hard_ceiling=0.10` on h_tc with note "Gap #2 — HTC film-T eval; documented 5-20% expected magnitude"). The widening MUST be recorded in MILESTONES.md alongside the gap rationale per D-11. After widening, re-run Plan 56-05's harness to confirm the suite is green. This is the recommended path if Gap #2 is the sole FAIL driver.

    **(c) ESCALATE-AS-BUG** — Drifts that aren't explained by the 3 documented gaps are surfaced. This means a real Julia regression (e.g., the enthalpy-form face-averaged cp introduced numerical noise beyond the documented gaps). Phase 56 cannot ship under D-12 with unexplained FAIL — escalate to a new phase (out of v1.1; future maintenance milestone) and roll back any unexplained-FAIL-driving change in src/.

    Case (a) is the green path; case (b) is the realistic-with-Gap-#2 path; case (c) requires escalation.
  </what-built>
  <how-to-verify>
    Step 1 — Inspect the parity report:

    ```bash
    cd /home/itay/projects/Julia-STREAM
    head -1 test/data/parity_report.csv
    grep -c ',CLEAN,' test/data/parity_report.csv
    grep -c ',GRAY,'  test/data/parity_report.csv
    grep -c ',FAIL,'  test/data/parity_report.csv
    grep ',FAIL,' test/data/parity_report.csv | head -20   # see the actual FAIL rows
    grep ',GRAY,' test/data/parity_report.csv | sort -t, -k6 -gr | head -10  # worst-drift GRAY rows
    ```

    Step 2 — Inspect the test-suite output:

    ```bash
    grep -A3 'Python parity:' /tmp/56-06_full_suite.log | head -50
    grep -E 'Test Summary|@testset.*FAIL|@testset.*Pass|Error During Test' /tmp/56-06_full_suite.log
    ```

    Step 3 — Decide the verdict (a / b / c) above.

    Step 4 — If verdict is (a) FULL PASS:
    - Note the worst-drift GRAY row (scenario + quantity + signed rtol)
    - Skip directly to Task 3 to author MILESTONES.md narrative

    Step 5 — If verdict is (b) DOCUMENTED-FAIL with widened hard_ceiling:
    - Identify which quantities FAIL and which gap explains them (most likely Gap #2 → h_tc)
    - Decide a documented hard_ceiling per quantity that's loose enough to catch real regressions but tight enough to be meaningful (per RESEARCH.md Open Question 2: "Stay at 2%" is the recommendation, but if the actual drift is 8% the testset will FAIL until widened)
    - Edit `test/test_validation.jl`'s parity_check calls for the affected quantities to add `hard_ceiling=<value>` with `note="Gap #2 — HTC film-T eval; documented N% expected magnitude"`
    - Re-run `bin/jl test/test_validation.jl` to confirm the suite is now green
    - Re-run `bin/jl test/runtests.jl` to confirm the full suite has no NEW failures vs Phase 55 baseline beyond the documented widenings
    - Proceed to Task 3 — record the widenings in MILESTONES.md

    Step 6 — If verdict is (c) ESCALATE-AS-BUG:
    - Document the unexplained-FAIL rows
    - Phase 56 does NOT close; create a new phase scope (out of v1.1) for the investigation
    - Stop here; do NOT proceed to Task 3
  </how-to-verify>
  <resume-signal>
    Reply one of:
    - "approved: full pass" — verdict (a); proceed to Task 3 with worst-drift GRAY noted
    - "approved: widen X to Y on quantities Z..., gap rationale: ..." — verdict (b); developer made the widenings, re-ran the suite, confirmed green; proceed to Task 3
    - "escalate: <description of unexplained FAIL>" — verdict (c); Phase 56 does not close; escalate to a new milestone-maintenance phase
  </resume-signal>
  <files></files>
  <action>This task is a human-verification checkpoint. Operative content is in `<what-built>`, `<how-to-verify>`, and `<resume-signal>` above. The executor pauses here for the developer to perform the verification steps manually. No code edits in this task.</action>
  <verify>
    <automated>echo "checkpoint:human-verify — pause for developer review per <how-to-verify> above"</automated>
  </verify>
  <done>Developer replies via `<resume-signal>`; orchestrator records the response and unblocks the next task/plan.</done>
</task>

<task type="auto">
  <name>Task 3: Author the v1.1-close narrative entry in .planning/MILESTONES.md (autonomous; assumes Task 2 was approved with verdict (a) or (b))</name>
  <read_first>
    - .planning/MILESTONES.md (current contents — preserve format / style of any existing milestone entries)
    - test/data/parity_report.csv (final committed version)
    - .planning/phases/56-python-stream-cross-validation/56-05-SUMMARY.md (worst-drift GRAY row + any FAIL rows)
    - Task 2 resume-signal text (records developer's verdict + any widenings)
    - .planning/phases/56-python-stream-cross-validation/56-CONTEXT.md (D-09 narrative content; D-04 + D-11 widening rationale documentation)
    - .planning/phases/56-python-stream-cross-validation/56-RESEARCH.md §"Known Equivalence Gaps" — verbatim language for documenting the 3 gaps
  </read_first>
  <files>.planning/MILESTONES.md</files>
  <action>
    Read the current `.planning/MILESTONES.md` to understand the existing format. If the file does not exist, create it with a top-level `# STREAM.jl — Milestones` heading. If it exists, append a new entry at the appropriate position (likely the bottom or under a "v1.1" section if the file is per-milestone-organized).

    Compose the v1.1-close entry with this structure (per D-09 + D-11):

    ```markdown
    ## v1.1 — Final Channel-Family Redesign  (closed: 2026-05-XX, replace XX with actual date)

    **Branch:** `channels-redesign` (single PR into main)
    **Phases:** 52, 53, 54, 55, 56
    **Requirements:** 22/22 complete

    ### Cross-validation outcome (Phase 56 / TEST-04)

    Per Phase 56 Python-STREAM cross-validation harness (`test/test_validation.jl`,
    `test/parity_helpers.jl`, `test/data/python_parity_reference.jl`,
    `test/data/parity_report.csv`):

    - Total quantities compared: <N> (across 4 scenarios × 4 D-07 tiers)
    - CLEAN (rtol ≤ 1e-6): <N_clean> ({percent}%)
    - GRAY  (1e-6 < rtol < hard_ceiling): <N_gray> ({percent}%)
    - FAIL  (rtol ≥ hard_ceiling): <N_fail> ({percent}%)

    **Worst-drift GRAY:** <scenario>, <quantity> — rtol = <value> (sign: <julia> vs <python>).
    Candidate cause: <gap citation, e.g., Gap #2 HTC film-T vs bulk-T eval per Phase 56 RESEARCH.md>.

    ### Documented equivalence gaps (Phase 56 / D-11)

    Three gaps were identified during Phase 56 source review and disclosed in the parity testset
    header comments. The drift report's per-quantity rtol values are interpreted in the context
    of these gaps:

    1. **Gap #1: circular `heated_parts` partition.** Python `EffectivePipe.circular(L, D)`
       returns `heated_parts=(πD, 0)`; Julia `PipeGeometry_circular(L, D)` returns
       `(πD/2, πD/2)`. Total perimeter is identical; per-side partition differs.
       Mitigation in harness: simple-loop circular scenario compares total q_wall, not per-side.
       Magnitude after mitigation: 0% (mitigation makes it exact).

    2. **Gap #2: HTC fluid-property evaluation temperature.** Python's
       `wall_heat_transfer_coeff` evaluates fluid properties at film T = (T_cool+T_wall)/2;
       Julia's CAC evaluates at bulk T_cool. Real physics divergence (not manifest drift).
       Expected magnitude: ~1-2% on mdot, up to ~5-20% on h_tc per cell. Matches the
       observed 1.75% mdot drift carried over from Phase 55 VAL-01.
       Mitigation: NONE — Phase 56 reports the gap; future work (out of scope) would
       harmonize the evaluation point in either Python or Julia.
       <If verdict (b): hard_ceiling for h_tc widened from 0.02 to <value> with this gap as rationale.>

    3. **Gap #3: Solver tolerance asymmetry.** Julia uses Sundials `KINSOL` with
       `abstol=1e-8, reltol=1e-6`; Python uses scipy `optimize.root(method='hybr')` with
       default `xtol=1.49e-8`. Both converge their own residuals; sub-1e-6 rtol is
       aspirational, not guaranteed.
       Mitigation: NONE — documented as the floor on what the CLEAN tier can achieve.

    ### Hard-ceiling configuration (Phase 56 / D-04)

    Per-quantity hard-ceiling values (rtol thresholds for FAIL vs GRAY):

    | Quantity | Hard ceiling | Rationale |
    |----------|--------------|-----------|
    | scalars (T_out, mdot, dP)  | 0.02 (2%) | D-04 default |
    | per-cell coolant T[i]      | 0.02 (2%) | D-04 default |
    | per-cell h_tc              | <0.02 or widened — record actual value> | <D-04 default OR Gap #2 widening rationale> |
    | per-cell q_density         | 0.02 (2%) | D-04 default |
    | per-cell T_wall            | 0.02 (2%) | D-04 default |
    | one-sided T_out_l          | 0.05 (5%) | KNOWN GAP — Python `one_sided_connection` distributes heat to BOTH plate faces (Phase 56 RESEARCH.md Known-Different Master List) |
    | one-sided plate T(z, x)    | 0.20 (20%) | Same KNOWN GAP; Julia plate-T validated separately via analytical T_max formula |
    | plate T (sym/asym MTR)     | 0.02 (2%) | D-04 default |

    ### Phase 56 deliverables

    - `test/parity_helpers.jl` (NEW, ~100 lines per D-15) — ParityRow + parity_check + drift-report machinery + 5 equivalence-checklist asserts (D-10)
    - `test/data/python_parity_reference.jl` (NEW, ~580 lines per D-17) — hardcoded Python-STREAM reference constants for 4 scenarios × 4 D-07 tiers
    - `test/data/parity_report.csv` (NEW, committed per D-08/D-16) — long-format auditable drift report
    - `test/generate_reference.py` (REWRITTEN per D-17) — emits all D-07 tiers as ready-to-paste Julia const blocks
    - `test/generate_mtr_reference.py` (REWRITTEN per D-17) — emits all D-07 tiers including plate T(z,x) for 3 MTR variants
    - `test/test_validation.jl` (MODIFIED per D-13) — 5 testsets REPLACED with parity testsets + 12 harness self-tests added; 3 testsets KEPT verbatim
    - 12-self-test harness "test the tester" — guards against parity-machinery regressions per RESEARCH.md "Test the Tester"

    ### v1.1 milestone close gate (D-12)

    1. ✅ Hard-floor pass (per-quantity hard_ceiling) — all parity testsets green after the harness widening + documentation step
    2. ✅ Drift report committed at `test/data/parity_report.csv`
    3. ✅ MILESTONES.md narrative entry added (this document)
    4. ✅ Existing 3 KEPT testsets (D-13) still green
    5. ✅ Branch `channels-redesign` clean; full suite has no NEW failures vs Phase 55 baseline beyond documented Gap #2
    6. ✅ Cleanup grep clean (`! grep -rn '_channel_base_eqs\|observed_mode\|skip_htc' src/ test/`)
    ```

    Replace `<N>`, `<scenario>`, `<quantity>`, `<value>`, `<julia>`, `<python>`, etc. with the actual numbers from the Plan 56-05 SUMMARY + parity_report.csv. The hard-ceiling table reflects the actual values (default 2% if Task 2 verdict was (a) full pass; widened values if verdict was (b)).

    Append the entry to `.planning/MILESTONES.md`. If the file doesn't exist, create it with the standard header and this as the first entry.

    Use the Edit tool (or Write tool if creating) — do NOT use heredoc/cat per the project conventions.

    Verify with the orchestrator's commit step that test/data/parity_report.csv is in the commit alongside MILESTONES.md per D-12 gate item 2.
  </action>
  <verify>
    <automated>test -f .planning/MILESTONES.md && grep -q 'v1.1' .planning/MILESTONES.md && grep -q 'Phase 56' .planning/MILESTONES.md && grep -q 'parity' .planning/MILESTONES.md && grep -q 'Gap #2' .planning/MILESTONES.md && grep -q 'D-12' .planning/MILESTONES.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/MILESTONES.md` exists with a v1.1 close entry
    - Entry references "Phase 56" and "parity"
    - Entry documents the 3 known equivalence gaps (Gap #1, Gap #2, Gap #3) per D-11
    - Entry records the worst-drift GRAY row (scenario + quantity + rtol) per D-09
    - Entry tabulates the per-quantity hard-ceiling configuration per D-04, including any widenings + rationale
    - Entry checklist confirms all 6 D-12 milestone-close gate items
    - If Task 2 verdict (b) was reached, the widening values are reflected in both the table AND test/test_validation.jl (Plan 56-05's testset code)
  </acceptance_criteria>
  <done>
    .planning/MILESTONES.md gains the v1.1-close narrative entry per D-09 + D-11. The 6 D-12 gate items are formally documented. Phase 56 closes; v1.1 is ready for `/gsd:complete-milestone`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/MILESTONES.md write | Standard project-doc edit; not security-relevant |
| Test suite re-run for verdict (b) | Same surface as Plan 05; bounded by KINSOL convergence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-06-01 | T (Tampering) | Documented hard_ceiling widening (verdict b) | mitigate | The widening is recorded in BOTH test_validation.jl (the actual threshold) AND MILESTONES.md (the rationale); future PRs can audit either side. The cleanup grep + 3-KEPT regression checks make it impossible to silently widen without surfacing the gap rationale. |
| T-56-06-02 | I (Information disclosure) | MILESTONES.md narrative wording | accept | Narrative records solver-state drift values + gap explanations; no PII / credentials. Same surface as the parity_report.csv. |
| T-56-06-03 | E (Elevation via undocumented widening) | hard_ceiling kwargs in parity_check calls | mitigate | Each widening MUST include a `note=...` argument explaining the gap. RESEARCH.md Open Question 2 documents the rationale for keeping h_tc at 2% by default; deviations are surfaced in MILESTONES.md per the human checkpoint. |

Phase 56 is test-only. Plan 06 is documentation + cleanup; no new attack surface.
</threat_model>

<verification>
- `! grep -rn '_channel_base_eqs\|observed_mode\|skip_htc' src/ test/` returns 0 matches (Task 1)
- Current branch is `channels-redesign` (Task 1)
- `bin/jl test/runtests.jl` passes with no NEW failures vs Phase 55 baseline beyond documented Gap #2 effects (Task 1+2 combined)
- `.planning/MILESTONES.md` contains the v1.1-close narrative entry (Task 3)
- `test/data/parity_report.csv` is up-to-date with the FINAL committed run (Task 2 may have re-run after widening)
</verification>

<success_criteria>
- ROADMAP success criterion 4 (cleanup grep) — verified clean.
- D-12 gate items 1-6 all verified.
- D-09 narrative entry in MILESTONES.md authored with worst-drift GRAY + 3 documented gaps + per-quantity hard-ceiling table.
- D-04 + D-11: any hard_ceiling widenings beyond the 2% default are recorded with explicit rationale.
- Branch `channels-redesign` is clean and ready for the single PR into main (the user opens this PR per CLAUDE.md branching policy; Plan 06 does NOT open the PR).
- TEST-04 fully complete; v1.1 milestone is ready for `/gsd:ship` then `/gsd:complete-milestone`.
</success_criteria>

<output>
After completion, create `.planning/phases/56-python-stream-cross-validation/56-06-SUMMARY.md` documenting:
- Final cleanup grep result (zero matches)
- Final full-suite test result (PASS/FAIL counts; pre-existing flakies vs NEW)
- Verdict reached at Task 2 (a / b / c) and the resume-signal text from the user
- Any hard_ceiling widenings made (which quantities, from what to what, with what rationale)
- The exact MILESTONES.md entry's worst-drift GRAY row text (so the user can sanity-check)
- Confirmation that test/data/parity_report.csv is up-to-date and committed alongside MILESTONES.md per D-12 gate item 2
- The v1.1 single-PR readiness state (clean working tree, branch identifier, what the user does next)
</output>
