---
phase: 24.1-bypass-lof-topology
plan: 01
subsystem: simulation
tags: [modelingtoolkit, channel, flapper, loss-of-flow, bypass-topology, momentum-inertia]

# Dependency graph
requires:
  - phase: 24-loss-of-flow
    provides: build_loop_lof series topology, Flapper component, ChannelHeatFlux with g parameter
  - phase: 23-flapper-solver-events
    provides: Flapper component with SymbolicContinuousCallback and ref_mdot wiring
provides:
  - Channel momentum inertia term (L/A * Dt(mdot)) in _channel_base_eqs and Channel
  - build_loop_lof_bypass() 4-node parallel network topology with Flapper bypass
  - ChannelAndContacts and ChannelHeatFlux inherit inertia via _channel_base_eqs
affects:
  - 24.1-02 (transient LOF validation will use build_loop_lof_bypass)
  - future solver tests (channel now has ODE for mdot)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Channel momentum inertia: Dt(port_in.mdot) in dP equation; MTK auto-promotes mdot as differential state
    - 3-way junction wiring: connect(a.port_out, b.port_in, c.port_in) for parallel paths
    - Bypass topology: 4-node network with long path (ChannelHeatFlux+Channel) and short path (Flapper)

key-files:
  created: []
  modified:
    - src/components/channel.jl - Added Dt(port_in.mdot) inertia term to both Channel and _channel_base_eqs
    - src/examples.jl - Added build_loop_lof_bypass() with 4-node bypass topology
    - src/STREAM.jl - Exported build_loop_lof_bypass
    - test/test_channel.jl - Updated equation count test (5->6, accounts for momentum ODE)

key-decisions:
  - "Channel momentum inertia added inline in dP equation using existing L, A locals — no new function parameters needed"
  - "Dt = Differential(t) declared once per function scope (_channel_base_eqs and Channel), not per-equation"
  - "build_loop_lof_bypass uses g=-g_acc for ChannelHeatFlux (downward flow) and g=+g_acc for return Channel (upward) per gravity sign convention"
  - "Pump(0.0) + Inertia pattern used (FLAP-06: callable Pump incompatible with Flapper events)"
  - "mtkcompile(fully_determined=false) used: 28 eqs vs 27 unknowns is expected (same as build_loop_lof)"
  - "ret.thermal.T ~ T_inlet as wall BC for unheated return pipe"

patterns-established:
  - "Momentum inertia pattern: (L / A) * Dt(port_in.mdot) appended to channel dP equation"
  - "4-node bypass topology: pump series -> junction A -> parallel [channel path | flapper] -> junction C -> series resistor"

requirements-completed: [LOF-01, LOF-02, LOF-03]

# Metrics
duration: 12min
completed: 2026-03-21
---

# Phase 24.1 Plan 01: Channel Momentum Inertia and Bypass LOF Topology Summary

**Channel momentum inertia `(L/A)*Dt(mdot)` added to all channel variants; `build_loop_lof_bypass()` implements 4-node parallel-path network with Flapper bypass for physical LOF transient simulation.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-03-21
- **Completed:** 2026-03-21
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added `(L / A) * Dt(port_in.mdot)` momentum inertia to `Channel` dP equation and `_channel_base_eqs` (which propagates to ChannelAndContacts and ChannelHeatFlux automatically)
- Built `build_loop_lof_bypass()` with physically correct 4-node bypass topology: real 3-way junctions, parallel heated channel path vs Flapper shortcut, gravity signs matching flow direction
- Exported `build_loop_lof_bypass` from STREAM.jl; compiles to 28-eq/27-unknown ODESystem via mtkcompile

## Task Commits

Each task was committed atomically:

1. **Task 1: Add momentum inertia term to _channel_base_eqs and Channel** - `ce7a125` (feat)
2. **Task 2: Create build_loop_lof_bypass() and export it** - `0eeb5d6` (feat)

## Files Created/Modified
- `src/components/channel.jl` - Added `Dt = Differential(t)` in `_channel_base_eqs`; appended `(L/A)*Dt(port_in.mdot)` to dP equation in both Channel and _channel_base_eqs
- `src/examples.jl` - Added `build_loop_lof_bypass()` with 4-node parallel network topology and full docstring
- `src/STREAM.jl` - Added `build_loop_lof_bypass` to export line
- `test/test_channel.jl` - Updated COMP-01 equation count test from 5 to 6 (5 energy ODEs + 1 momentum inertia)

## Decisions Made
- Momentum inertia uses existing `L` and `A` locals in each function scope — no new parameters needed
- `Pump(0.0) + Inertia` locked by FLAP-06: callable Pump cannot coexist with Flapper's SymbolicContinuousCallback
- `g=-g_acc` for ChannelHeatFlux (downward A->B), `g=+g_acc` for return Channel (upward B->C) per RESEARCH.md gravity analysis
- `fully_determined=false` required at mtkcompile: MTK structural analysis flags differential states from promoted `Dt(port_in.mdot)`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated test_channel.jl equation count test**
- **Found during:** Task 1 (Add momentum inertia term)
- **Issue:** Test `COMP-01: Channel equation count` checked `length(energy_eqs) == 5` by filtering for equations containing "Differential". Adding `Dt(port_in.mdot)` to dP equation increases this count to 6.
- **Fix:** Updated assertion to `== 6` with comment: `5 energy balance ODEs + 1 momentum inertia in dP`
- **Files modified:** `test/test_channel.jl`
- **Verification:** `include("test/test_channel.jl")` passes all tests
- **Committed in:** `ce7a125` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug: stale equation count)
**Impact on plan:** Necessary update; the test was counting Differential occurrences and the new inertia term is a deliberate addition. No scope creep.

## Issues Encountered
None — both tasks executed as planned.

## Known Stubs
None — no placeholder data or hardcoded stubs.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Channel momentum inertia is live across all three channel variants (Channel, ChannelAndContacts, ChannelHeatFlux)
- `build_loop_lof_bypass()` compiles and is exported; ready for Phase 24.1 Plan 02 transient LOF validation
- No blockers

## Self-Check: PASSED

---
*Phase: 24.1-bypass-lof-topology*
*Completed: 2026-03-21*
