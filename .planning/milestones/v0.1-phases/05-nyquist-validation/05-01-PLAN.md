---
phase: 05-nyquist-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation/01-VALIDATION.md
  - .planning/phases/02-components/02-VALIDATION.md
  - .planning/phases/03-integration-and-validation/03-VALIDATION.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "Phase 01 VALIDATION.md has nyquist_compliant: true in its frontmatter"
    - "Phase 02 VALIDATION.md has nyquist_compliant: true in its frontmatter"
    - "Phase 03 VALIDATION.md has nyquist_compliant: true in its frontmatter"
    - "All 54 tests still pass after any gap-filling the auditor performs"
  artifacts:
    - path: ".planning/phases/01-foundation/01-VALIDATION.md"
      provides: "Phase 01 Nyquist compliance record"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/02-components/02-VALIDATION.md"
      provides: "Phase 02 Nyquist compliance record"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/03-integration-and-validation/03-VALIDATION.md"
      provides: "Phase 03 Nyquist compliance record"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "/gsd:validate-phase 1"
      to: ".planning/phases/01-foundation/01-VALIDATION.md"
      via: "validate-phase workflow commit-docs step"
      pattern: "nyquist_compliant: true"
    - from: "/gsd:validate-phase 2"
      to: ".planning/phases/02-components/02-VALIDATION.md"
      via: "validate-phase workflow commit-docs step"
      pattern: "nyquist_compliant: true"
    - from: "/gsd:validate-phase 3"
      to: ".planning/phases/03-integration-and-validation/03-VALIDATION.md"
      via: "validate-phase workflow commit-docs step"
      pattern: "nyquist_compliant: true"
---

<objective>
Run the GSD validate-phase workflow for phases 01, 02, and 03 to produce formal Nyquist compliance records for the v0.1 milestone.

Purpose: All three prior phases were executed and verified (54 tests green) but their VALIDATION.md files still have `nyquist_compliant: false` — the status set at creation time. The validate-phase workflow audits each phase's requirement-to-test mapping, closes any gaps, and flips the flag to `nyquist_compliant: true`. This is the final bookkeeping step before v0.1 is complete.

Output: Three updated VALIDATION.md files with `nyquist_compliant: true` committed to git.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Phase artifacts for executor reference -->
<!-- Phase 01 -->
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<!-- Phase 02 -->
@.planning/phases/02-components/02-VALIDATION.md
@.planning/phases/02-components/02-01-SUMMARY.md
@.planning/phases/02-components/02-02-SUMMARY.md
@.planning/phases/02-components/02-03-SUMMARY.md
@.planning/phases/02-components/02-04-SUMMARY.md

<!-- Phase 03 -->
@.planning/phases/03-integration-and-validation/03-VALIDATION.md
@.planning/phases/03-integration-and-validation/03-01-SUMMARY.md
@.planning/phases/03-integration-and-validation/03-02-SUMMARY.md
@.planning/phases/03-integration-and-validation/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Validate Phase 01 (Foundation)</name>
  <files>.planning/phases/01-foundation/01-VALIDATION.md</files>
  <action>
    Run `/gsd:validate-phase 1` to audit Phase 01 (Foundation).

    The workflow will:
    1. Read all Phase 01 PLAN and SUMMARY files to build a requirement-to-task map for FOUND-01, FOUND-02, CONN-01, CONN-02
    2. Cross-reference each requirement against existing tests in `test/runtests.jl` (and any targeted test files)
    3. Classify each as COVERED / PARTIAL / MISSING
    4. Present a gap table — select "Fix all gaps" (option 1) at the interactive prompt

    IMPORTANT: Do NOT attempt to automate the following Manual-Only item from Phase 01:
    - "@register_symbolic at module top-level" — compile-time constraint, cannot be automated

    If the gsd-nyquist-auditor is spawned to write tests for PARTIAL/MISSING items, allow it to complete. The 54-test green baseline means no implementation gaps exist — any auditor work will be minor test metadata fixes only.

    After the workflow completes, confirm `nyquist_compliant: true` is in the 01-VALIDATION.md frontmatter.
  </action>
  <what-built>Phase 01 VALIDATION.md with nyquist_compliant: true, all FOUND-01/FOUND-02/CONN-01/CONN-02 requirements COVERED</what-built>
  <how-to-verify>
    1. Run: `grep "nyquist_compliant" /home/itay/projects/Julia-STREAM/.planning/phases/01-foundation/01-VALIDATION.md`
    2. Expected output: `nyquist_compliant: true`
    3. If the auditor added any tests, run: `julia --project=/home/itay/projects/Julia-STREAM -e "using Pkg; Pkg.test()"` — must still pass (54 or more tests)
  </how-to-verify>
  <resume-signal>Type "approved" when 01-VALIDATION.md shows nyquist_compliant: true, or describe any issues</resume-signal>
  <verify>
    <automated>grep "nyquist_compliant: true" /home/itay/projects/Julia-STREAM/.planning/phases/01-foundation/01-VALIDATION.md</automated>
  </verify>
  <done>01-VALIDATION.md frontmatter shows `nyquist_compliant: true`; file is committed to git via commit-docs</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Validate Phase 02 (Components)</name>
  <files>.planning/phases/02-components/02-VALIDATION.md</files>
  <action>
    Run `/gsd:validate-phase 2` to audit Phase 02 (Components).

    The workflow will:
    1. Read all Phase 02 PLAN and SUMMARY files to build a requirement-to-task map for COMP-01, COMP-02, COMP-03, COMP-04
    2. Cross-reference each requirement against existing tests
    3. Classify each as COVERED / PARTIAL / MISSING
    4. Present a gap table — select "Fix all gaps" (option 1) at the interactive prompt

    IMPORTANT: Do NOT attempt to automate the following Manual-Only item from Phase 02:
    - "observed(compiled_sys) symbolic inspection" — requires post-mtkcompile symbolic API, cannot be automated into a standard test

    If the gsd-nyquist-auditor is spawned, allow it to complete. Trust its output — the 54-test baseline covers all COMP requirements.

    After the workflow completes, confirm `nyquist_compliant: true` is in the 02-VALIDATION.md frontmatter and the test suite still passes.
  </action>
  <what-built>Phase 02 VALIDATION.md with nyquist_compliant: true, all COMP-01/COMP-02/COMP-03/COMP-04 requirements COVERED</what-built>
  <how-to-verify>
    1. Run: `grep "nyquist_compliant" /home/itay/projects/Julia-STREAM/.planning/phases/02-components/02-VALIDATION.md`
    2. Expected output: `nyquist_compliant: true`
    3. If the auditor added any tests, run: `julia --project=/home/itay/projects/Julia-STREAM -e "using Pkg; Pkg.test()"` — must still pass (54 or more tests)
  </how-to-verify>
  <resume-signal>Type "approved" when 02-VALIDATION.md shows nyquist_compliant: true, or describe any issues</resume-signal>
  <verify>
    <automated>grep "nyquist_compliant: true" /home/itay/projects/Julia-STREAM/.planning/phases/02-components/02-VALIDATION.md</automated>
  </verify>
  <done>02-VALIDATION.md frontmatter shows `nyquist_compliant: true`; file is committed to git via commit-docs</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Validate Phase 03 (Integration and Validation)</name>
  <files>.planning/phases/03-integration-and-validation/03-VALIDATION.md</files>
  <action>
    Run `/gsd:validate-phase 3` to audit Phase 03 (Integration and Validation).

    The workflow will:
    1. Read all Phase 03 PLAN and SUMMARY files to build a requirement-to-task map for SYS-01, SYS-02, SOLV-01, SOLV-02, VAL-01, VAL-02, VAL-03
    2. Cross-reference each requirement against existing tests
    3. Classify each as COVERED / PARTIAL / MISSING
    4. Present a gap table — select "Fix all gaps" (option 1) at the interactive prompt

    IMPORTANT: Do NOT attempt to automate the following Manual-Only item from Phase 03:
    - "Run generate_reference.py for Python reference values" — requires Python STREAM installed in ~/projects/STREAM; this is the reference generation script, not a Julia test

    If the gsd-nyquist-auditor is spawned, allow it to complete. The VAL-01/VAL-02/VAL-03 requirements are covered by the Julia comparison tests in `test/runtests.jl` (T_outlet_ref=327.7894 K, mdot_ref=0.609289 kg/s hardcoded).

    After the workflow completes, confirm `nyquist_compliant: true` is in the 03-VALIDATION.md frontmatter and the test suite still passes.
  </action>
  <what-built>Phase 03 VALIDATION.md with nyquist_compliant: true, all SYS/SOLV/VAL requirements COVERED</what-built>
  <how-to-verify>
    1. Run: `grep "nyquist_compliant" /home/itay/projects/Julia-STREAM/.planning/phases/03-integration-and-validation/03-VALIDATION.md`
    2. Expected output: `nyquist_compliant: true`
    3. Final milestone check — run full suite: `julia --project=/home/itay/projects/Julia-STREAM -e "using Pkg; Pkg.test()"` — must pass (54 or more tests)
    4. Final gate check: `grep "nyquist_compliant" /home/itay/projects/Julia-STREAM/.planning/phases/0{1,2,3}-*/0{1,2,3}-VALIDATION.md` — all three must show `nyquist_compliant: true`
  </how-to-verify>
  <resume-signal>Type "approved" when all three VALIDATION.md files show nyquist_compliant: true and the test suite passes, or describe any issues</resume-signal>
  <verify>
    <automated>grep "nyquist_compliant: true" /home/itay/projects/Julia-STREAM/.planning/phases/03-integration-and-validation/03-VALIDATION.md</automated>
  </verify>
  <done>03-VALIDATION.md frontmatter shows `nyquist_compliant: true`; all three phases compliant; 54+ tests still pass; v0.1 milestone Nyquist-complete</done>
</task>

</tasks>

<verification>
All three phases are Nyquist-compliant when this command returns three matching lines:

```bash
grep "nyquist_compliant" \
  /home/itay/projects/Julia-STREAM/.planning/phases/01-foundation/01-VALIDATION.md \
  /home/itay/projects/Julia-STREAM/.planning/phases/02-components/02-VALIDATION.md \
  /home/itay/projects/Julia-STREAM/.planning/phases/03-integration-and-validation/03-VALIDATION.md
```

Expected output:
```
.planning/phases/01-foundation/01-VALIDATION.md:nyquist_compliant: true
.planning/phases/02-components/02-VALIDATION.md:nyquist_compliant: true
.planning/phases/03-integration-and-validation/03-VALIDATION.md:nyquist_compliant: true
```

Test suite must remain green:
```bash
julia --project=/home/itay/projects/Julia-STREAM -e "using Pkg; Pkg.test()"
```
</verification>

<success_criteria>
1. `grep "nyquist_compliant: true" .../01-VALIDATION.md` returns a match
2. `grep "nyquist_compliant: true" .../02-VALIDATION.md` returns a match
3. `grep "nyquist_compliant: true" .../03-VALIDATION.md` returns a match
4. `Pkg.test()` passes (54 or more tests — auditor may add tests but must not break existing ones)
5. All three VALIDATION.md files committed to git via the validate-phase commit-docs step
</success_criteria>

<output>
After completion, create `.planning/phases/05-nyquist-validation/05-01-SUMMARY.md`
</output>
