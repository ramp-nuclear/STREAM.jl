---
phase: 51-julia-startup-performance-reliable-sysimage
plan: 02
type: execute
wave: 2
depends_on:
  - "51-01"
files_modified:
  - stream.so
autonomous: false
requirements:
  - TTFX-01
  - TTFX-02
  - TTFX-03
  - TTFX-04

must_haves:
  truths:
    - "stream.so exists in the repo root after build_sysimage.sh completes"
    - "TTFX baseline numbers (without sysimage) are recorded"
    - "TTFX improvement numbers (with sysimage) are recorded and confirm speedup"
    - "Full test suite passes when run with julia --sysimage stream.so"
  artifacts:
    - path: "stream.so"
      provides: "Prebuilt Julia sysimage with baked MTK dispatch"
      min_lines: 0
    - path: ".planning/phases/51-julia-startup-performance-reliable-sysimage/51-02-SUMMARY.md"
      provides: "Recorded TTFX baseline and sysimage timing numbers"
      contains: "mtkcompile"
  key_links:
    - from: "build_sysimage.sh"
      to: "stream.so"
      via: "PackageCompiler.create_sysimage"
      pattern: "sysimage_path.*stream.so"
    - from: "stream.so"
      to: "test/runtests.jl"
      via: "julia --sysimage stream.so --project=. test/runtests.jl"
      pattern: "sysimage"
---

<objective>
Run the sysimage build and verify the phase hard gates from D-07, D-08, and D-09:
(1) build_sysimage.sh produces stream.so, (2) timing script confirms measurable TTFX
improvement, (3) full test suite passes with the sysimage.

Purpose: The code changes in Plan 01 are only valuable if the sysimage actually builds.
This plan is the end-to-end verification that the warmup code and build script work as
a system. The user must run the build in a separate terminal due to WSL2 crash risk.

Output: stream.so in repo root, recorded timing numbers, confirmed green test suite.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-julia-startup-performance-reliable-sysimage/51-CONTEXT.md
@.planning/phases/51-julia-startup-performance-reliable-sysimage/51-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure baseline TTFX (without sysimage)</name>
  <files>time_startup.jl</files>
  <read_first>
    - time_startup.jl (confirm it exists and has correct content from Plan 01)
  </read_first>
  <action>
Run the timing script WITHOUT the sysimage to establish the baseline. This satisfies D-08
(baseline numbers must be recorded).

Run:
```bash
julia --project=. time_startup.jl
```

Record the three numbers printed:
- "using STREAM (load): X.X s"
- "mtkcompile (minimal 2-node): X.X s"
- "Total: X.X s"

These will be compared against the sysimage numbers in Task 3. Expected baseline: 60-120s
total on a cold Julia session. If Julia is already warmed from a previous run in the same
session, numbers will be lower — this is expected (Julia caches compiled code within a session).

Note: This task does NOT produce a new file — it just records numbers. Record them in the
executor's notes for inclusion in the SUMMARY.md at plan end.
  </action>
  <verify>
    <automated>test -f /home/itay/projects/Julia-STREAM/time_startup.jl && echo "time_startup.jl exists" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=. time_startup.jl` completes without error (exit 0)
    - Output contains "using STREAM (load):" with a numeric value
    - Output contains "mtkcompile (minimal 2-node):" with a numeric value
    - Output contains "Total:" with a numeric value
    - Numbers are recorded for comparison in SUMMARY.md
  </acceptance_criteria>
  <done>
    Baseline TTFX numbers recorded. time_startup.jl runs without errors without sysimage.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Build sysimage in separate terminal</name>
  <read_first>
    - build_sysimage.sh (confirm pre-flight check is present from Plan 01)
    - CLAUDE.md Performance section (confirm WSL2 docs are present)
  </read_first>
  <what>
    Run `./build_sysimage.sh` in a SEPARATE terminal outside this chat session.

    **Why separate terminal:** WSL2 has crashed and terminated the chat session during previous
    sysimage build attempts. Running it outside protects the chat session.

    **Expected duration:** 5-15 minutes. The script will print:
    1. Pre-flight check result: "Pre-flight check: XXXX MB free — OK (threshold: 6144 MB)"
    2. Julia version
    3. RAM available
    4. "Building stream.so — this takes 5-15 minutes."
    5. PackageCompiler progress (may appear silent for several minutes during link step)
    6. "Done! sysimage written to stream.so"

    **If build crashes (WSL2 OOM):**
    - No stream.so will appear in the repo root
    - Follow the .wslconfig instructions in CLAUDE.md to increase WSL2 memory
    - Run `wsl --shutdown` and reopen terminal, then retry

    **After the build completes:**
    - Confirm stream.so exists: `ls -lh stream.so`
    - Report back with the file size (typically 200-500 MB)
  </what>
  <instructions>
    1. Open a new WSL2 terminal (outside this chat)
    2. `cd /home/itay/projects/Julia-STREAM`
    3. `./build_sysimage.sh`
    4. Wait for completion (5-15 min)
    5. Run `ls -lh stream.so` to confirm the file exists
    6. Return to this chat and type "build complete: XXXX MB" (with the actual file size)
    7. If it crashed, type "build crashed" and we will diagnose
  </instructions>
  <resume-signal>Type "build complete: [size]" or "build crashed"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify TTFX improvement and full test suite with sysimage</name>
  <files>(reads stream.so)</files>
  <read_first>
    - time_startup.jl (verify file is intact)
    - test/runtests.jl (confirm test suite structure)
  </read_first>
  <action>
Run in sequence. Only run after Task 2 checkpoint confirms stream.so exists.

**Step 1: Verify stream.so exists**
```bash
test -f stream.so && echo "stream.so present: $(ls -lh stream.so | awk '{print $5}')" || echo "MISSING"
```

**Step 2: Measure TTFX with sysimage (D-08)**
```bash
julia --sysimage stream.so --project=. time_startup.jl
```

Record the three numbers. Expected improvement: total should drop from ~90s to ~5-15s.
The "mtkcompile" line should be under 5s with sysimage (vs 30-90s without).

**Step 3: Run full test suite with sysimage (D-09)**
```bash
julia --sysimage stream.so --project=. test/runtests.jl
```

This must exit 0 with all tests passing. If any test fails that was passing without the
sysimage, this is a sysimage correctness bug — do not close the phase until resolved.

Note: VAL-01 (Fourier series) and NET-03 (cube flow KINSOL) are pre-existing flaky tests
documented in STATE.md — their failure status is unchanged from before this phase.

**Step 4: Confirm D-05 (package list unchanged)**
```bash
grep -A5 "create_sysimage" build_sysimage.jl
```
Confirm the package list is: STREAM, ModelingToolkit, OrdinaryDiffEq, Symbolics, QuadGK, Sundials
(no additions or removals).
  </action>
  <verify>
    <automated>test -f /home/itay/projects/Julia-STREAM/stream.so && echo "OK" || echo "MISSING"</automated>
    <automated>julia --sysimage /home/itay/projects/Julia-STREAM/stream.so --project=/home/itay/projects/Julia-STREAM -e 'using STREAM; println("sysimage load OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f stream.so` exits 0 (file exists)
    - `julia --sysimage stream.so --project=. time_startup.jl` exits 0 and prints timing numbers
    - Sysimage mtkcompile time is measurably less than baseline (target: under 15s, ideally under 5s)
    - `julia --sysimage stream.so --project=. test/runtests.jl` exits 0
    - No test that was passing before this phase now fails (check against STATE.md known flaky tests)
    - `grep "create_sysimage" build_sysimage.jl` followed by package list shows exactly the 6 packages from D-05
  </acceptance_criteria>
  <done>
    stream.so exists and loads correctly.
    Sysimage TTFX numbers recorded and show improvement over baseline.
    Full test suite passes with --sysimage flag.
    Phase D-07, D-08, D-09 hard gates all satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stream.so → julia runtime | Sysimage loaded at startup — must be built from trusted source (repo root build_sysimage.sh) |
| julia --sysimage → test suite | Tests run against baked sysimage; correctness regression is the key risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | Tampering | stream.so | accept | File is git-ignored and built locally; no external input; correctness verified by running full test suite against it |
| T-51-05 | Denial of Service | WSL2 OOM during build | mitigate | Pre-flight memory check in build_sysimage.sh exits 1 if free RAM < 6144 MB; user warned to run in separate terminal |
| T-51-06 | Repudiation | TTFX numbers | accept | Numbers recorded in SUMMARY.md for reference; no audit requirement for dev tooling |
</threat_model>

<verification>
Phase is complete when ALL of the following are true:
1. stream.so exists in repo root (`test -f stream.so` exits 0)
2. Baseline TTFX numbers recorded (from Task 1)
3. Sysimage TTFX numbers recorded and show improvement (from Task 3)
4. `julia --sysimage stream.so --project=. test/runtests.jl` exits 0
5. SUMMARY.md records all four timing numbers (load baseline, compile baseline, load+sysimage, compile+sysimage)
</verification>

<success_criteria>
- D-07 hard gate: stream.so exists (file size > 100 MB)
- D-08 hard gate: time_startup.jl prints measurably lower numbers with --sysimage vs without
- D-09 hard gate: julia --sysimage stream.so --project=. test/runtests.jl exits 0
- SUMMARY.md records: baseline total time, sysimage total time, speedup ratio
</success_criteria>

<output>
After completion, create `.planning/phases/51-julia-startup-performance-reliable-sysimage/51-02-SUMMARY.md`

The SUMMARY.md MUST include a table with columns: Metric | Baseline | With Sysimage | Improvement
Rows: using STREAM load, first mtkcompile, Total
</output>
