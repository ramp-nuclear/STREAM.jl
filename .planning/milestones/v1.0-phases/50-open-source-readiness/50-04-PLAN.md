---
phase: 50-open-source-readiness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-08

must_haves:
  truths:
    - "README leads with what STREAM.jl models (nuclear/TH physics), not MTK internals"
    - "README contains a runnable build_loop quick-start code block"
    - "README installation section does NOT present sysimage as a prerequisite"
    - "README contains a component catalog table with Channel, Pump, HeatDiffusion, PointKinetics, ChannelAndContacts, HeatExchanger"
    - "README contains validation summary mentioning 1% tolerance across steady-state, transient, and point kinetics"
    - "README contains a Relationship to Python STREAM section with a link"
  artifacts:
    - path: "README.md"
      provides: "Public-facing project documentation"
      contains: "## Quick Start"
  key_links:
    - from: "README.md CI badge"
      to: ".github/workflows/ci.yml"
      via: "GitHub Actions badge URL"
      pattern: "github.com/itaybnv/STREAM.jl/actions/workflows/ci.yml"
    - from: "README.md quick-start"
      to: "src/examples.jl build_loop"
      via: "code block uses build_loop"
      pattern: "build_loop"
---

<objective>
Write README.md for public GitHub discovery. Target audience: nuclear engineers and thermal-hydraulics physicists. Leads with physics modeling capability, includes runnable quick-start, component catalog, validation summary, and relationship to Python STREAM.

Purpose: The README is the first thing a potential user sees on GitHub. It must answer "what does this model?" before "how does it work?" — per D-01. No Documenter.jl site in this phase (D-08); docstrings cover API reference.
Output: Complete README.md at repo root.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-open-source-readiness/50-CONTEXT.md
@.planning/phases/50-open-source-readiness/50-RESEARCH.md

<interfaces>
<!-- What README must reference. From src/STREAM.jl exports and CONTEXT.md decisions. -->

Component catalog (D-04) — these 6 components and their one-line descriptions:
- Channel: Single-phase coolant channel with axial discretization; Dittus-Boelter HTC and Blasius friction
- Pump: Pressure-rise source with fixed-dP or fixed-mdot modes; supports callable dP(t) for transient pump trips
- HeatDiffusion: 2D finite-difference solid fuel plate with ThermalPort arrays for fluid coupling
- PointKinetics: Six-group delayed-neutron point kinetics with callable reactivity control and SCRAM events
- ChannelAndContacts: Channel with bilateral ThermalPort arrays for plate-to-coolant heat transfer (MTR geometry)
- HeatExchanger: Temperature boundary condition (constant-T heat sink); used to set coolant inlet temperature

build_loop quick-start (D-02) — the pattern to show:
```julia
using STREAM
using DifferentialEquations

ssys = build_loop(T_inlet=313.15, T_wall=373.15, dP_pump=3.0e4)
op   = [ssys.ch.inlet.mdot => 0.490]
sol  = solve_steady(ssys, op)
println("T_outlet = ", sol[ssys.ch.T_out] - 273.15, " °C")
```

CI badge URL pattern (from RESEARCH.md):
[![CI](https://github.com/itaybnv/STREAM.jl/actions/workflows/ci.yml/badge.svg)](https://github.com/itaybnv/STREAM.jl/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Repo URL (D-18): https://github.com/itaybnv/STREAM.jl
Python STREAM reference (D-06): ~/projects/STREAM (private; mention existence without linking to private path)
Sysimage caveat (D-03): optional performance optimization, currently unreliable on WSL2; NOT a prerequisite
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <read_first>
    - /home/itay/projects/Julia-STREAM/src/STREAM.jl (full exports list — for component catalog completeness)
    - /home/itay/projects/Julia-STREAM/Project.toml (version number for Installation section)
    - /home/itay/projects/Julia-STREAM/CLAUDE.md (sysimage section — to correctly describe the caveat per D-03)
    - Check if README.md already exists: `ls /home/itay/projects/Julia-STREAM/README.md 2>/dev/null`
  </read_first>
  <action>
Create `/home/itay/projects/Julia-STREAM/README.md` with all sections below. Follow the exact section order specified.

<!-- section separator -->

**Section 1: Badges (discretionary per Claude's Discretion)**
Two badges on one line:
- CI badge linking to https://github.com/itaybnv/STREAM.jl/actions/workflows/ci.yml
- MIT License badge linking to https://opensource.org/licenses/MIT

**Section 2: Title + Tagline**
`# STREAM.jl`
One-sentence tagline: "A Julia library for thermal-hydraulics simulation of research reactor cooling systems, built on ModelingToolkit.jl."

**Section 3: What STREAM.jl Models (D-01 — physics-first, not MTK-first)**
2-3 paragraph description explaining:
- Models pressurized water/light-water research reactor cooling loops (forced convection, natural convection, flow reversal)
- Steady-state and transient analysis: pump trip, loss-of-flow, SCRAM events
- MTR plate-fuel geometry: HeatDiffusion 2D solid plate coupled to ChannelAndContacts
- Point kinetics with delayed neutron groups, temperature feedback, callable reactivity control
- Do NOT lead with "ModelingToolkit" or "acausal" in this section — mention Julia/MTK at the end as implementation detail

**Section 4: Quick Start (D-02)**
Header: `## Quick Start`

Installation subsection:
```julia
# Install Julia 1.10+ from https://julialang.org/downloads/
# Clone and activate:
git clone https://github.com/itaybnv/STREAM.jl
cd STREAM.jl
julia --project=. -e 'using Pkg; Pkg.instantiate()'
```

Runnable example:
```julia
using STREAM
using DifferentialEquations

# Build a simple forced-convection loop
# Pump → HeatExchanger (T_inlet reset) → Channel → back to Pump
ssys = build_loop(
    T_inlet = 313.15,   # K (40 °C) coolant inlet
    T_wall  = 373.15,   # K (100 °C) wall temperature
    dP_pump = 3.0e4,    # Pa pump pressure rise
)

# Steady-state initial guess and solve
op  = [ssys.ch.inlet.mdot => 0.490]
sol = solve_steady(ssys, op)

println("T_outlet = ", round(sol[ssys.ch.T_out] - 273.15, digits=2), " °C")
println("mdot     = ", round(abs(sol[ssys.ch.inlet.mdot]), digits=4), " kg/s")
```

Expected output note: `# T_outlet ≈ 54.6 °C  (validated against Python STREAM)`

**Section 5: Component Catalog (D-04)**
Header: `## Component Catalog`

Markdown table with columns: Component | Models | Key Parameters

Rows for these exact 6 components:
| Component | Models | Key Parameters |
|-----------|--------|----------------|
| `Channel` | Single-phase coolant channel with axial FD discretization | `n` (cells), `geometry` (PipeGeometry), `g` (gravity) |
| `Pump` | Pressure-rise source; supports callable `dP(t)` for pump trips | `dP_pump` or `mdot_pump` |
| `HeatDiffusion` | 2D finite-difference solid fuel plate with lateral and axial conduction | `nz`, `nx`, `power_shape`, `k_s`, `rho_s`, `cp_s` |
| `PointKinetics` | Six-group delayed-neutron reactor kinetics with SCRAM support | `beta_k`, `lambda_k`, callable `rho_c_fn` |
| `ChannelAndContacts` | Coolant channel with bilateral `ThermalPort` arrays for plate coupling | `n`, `geometry` |
| `HeatExchanger` | Constant-temperature heat sink (sets coolant inlet temperature) | `T_bc` |

Add a note: "Full API reference is available via Julia's built-in help system: `?Channel`, `?build_loop`, etc."

**Section 6: Validation Summary (D-05)**
Header: `## Validation`

Content: "STREAM.jl results are validated within 1% of Python STREAM across three benchmark categories:"
- Bullet: "**Steady-state** — T_outlet and mass flow rate in forced-convection loops (see `test/test_validation.jl` VAL-01)"
- Bullet: "**Transient** — Outlet temperature response to wall temperature steps (VAL-02)"
- Bullet: "**Point kinetics** — Prompt-jump power response and reactivity insertion scenarios (Phase 47/48 validation)"
- Bullet: "**HeatDiffusion** — 1D Fourier series validation of axial plate temperature decay (VAL-01 Fourier)"

**Section 7: Installation (D-03)**
Header: `## Installation`

```julia
# From the Julia REPL:
using Pkg
Pkg.develop(path="/path/to/STREAM.jl")

# Or clone and use directly:
git clone https://github.com/itaybnv/STREAM.jl
cd STREAM.jl
julia --project=. -e 'using Pkg; Pkg.instantiate()'
```

Subsection: "### Performance (Optional Sysimage)"
Content: "A precompilation sysimage (`build_sysimage.sh`) is included to reduce Julia startup time from ~90 seconds to ~5 seconds. **The sysimage is an optional performance optimization and is currently unreliable on some platforms (notably WSL2).** STREAM.jl runs correctly without it — use `julia --project=.` directly."

Do NOT say "build_sysimage.sh works" or present it as a required step. Per D-03.

**Section 8: Examples**
Header: `## Examples`

Three bullet points:
- `examples/simple_loop.jl` — Minimal forced-convection loop: build, solve, plot T_out
- `examples/mtr_assembly.jl` — HeatDiffusion + ChannelAndContacts thermal coupling (MTR plate-fuel)
- `examples/lof_transient.jl` — Loss-of-flow transient: pump trip, flapper event, natural circulation establishment

Run with: `julia --project=. examples/simple_loop.jl`

**Section 9: Relationship to Python STREAM (D-06)**
Header: `## Relationship to Python STREAM`

Content explaining:
- STREAM.jl is a Julia reimplementation of the Python STREAM thermal-hydraulics library (internal research tool)
- Motivation: Julia's ModelingToolkit.jl enables symbolic-numeric DAE compilation, automatic Jacobian sparsity, and equation-level composition — enabling larger systems than Python's manual ODE formulation
- Physics models match: same Dittus-Boelter HTC, Blasius friction, MTR plate geometry, point kinetics parameters
- Validation shows < 1% deviation across all benchmark cases
- Python STREAM is an internal tool; Julia STREAM.jl is the open-source implementation

**Section 10: License footer**
`## License`
`MIT License — see [LICENSE](LICENSE) for details.`

<!-- section separator -->

Write all sections as a single README.md file. Use proper Markdown formatting (headers with ##, code blocks with triple backticks and julia/bash language hints, table with pipes).
  </action>
  <verify>
    <automated>
grep -q "## Quick Start" /home/itay/projects/Julia-STREAM/README.md &&
grep -q "build_loop" /home/itay/projects/Julia-STREAM/README.md &&
grep -q "Component Catalog" /home/itay/projects/Julia-STREAM/README.md &&
grep -q "HeatDiffusion" /home/itay/projects/Julia-STREAM/README.md &&
grep -q "PointKinetics" /home/itay/projects/Julia-STREAM/README.md &&
grep -q "Python STREAM" /home/itay/projects/Julia-STREAM/README.md &&
grep -q "sysimage" /home/itay/projects/Julia-STREAM/README.md &&
grep -q "MIT" /home/itay/projects/Julia-STREAM/README.md &&
echo "README.md structure OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `[![CI]` badge linking to github.com/itaybnv/STREAM.jl actions
    - README.md contains `## Quick Start` section
    - README.md contains `build_loop(` in a code block
    - README.md contains `## Component Catalog` section
    - README.md contains table rows for all 6 components: Channel, Pump, HeatDiffusion, PointKinetics, ChannelAndContacts, HeatExchanger
    - README.md contains `## Validation` section with "1%" mentioned
    - README.md contains `## Installation` section
    - README.md contains sysimage caveat with wording indicating it is optional and unreliable (NOT a prerequisite)
    - README.md contains `## Relationship to Python STREAM` section
    - README.md does NOT contain "Documenter" (deferred per D-08)
    - README.md does NOT present sysimage as Step 1 of installation
    - First section (after badges) leads with physics description, not "ModelingToolkit.jl acausal" terminology
  </acceptance_criteria>
  <done>README.md exists at repo root with all required sections per D-01 through D-06 and D-08. Content is accurate, physics-first, and matches the current codebase capabilities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → public GitHub | README is the primary discovery surface for external users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-10 | Information Disclosure | Python STREAM mention | mitigate | Python STREAM is described as "internal research tool" without linking to its private location; no internal paths or credentials referenced |
| T-50-11 | Tampering | CI badge URL | accept | Badge URL is static markdown; worst case badge shows "failing" if CI is red — informational only (D-14: no branch protection) |
| T-50-12 | Spoofing | Quick-start example output | accept | Expected output comment is approximate ("≈ 54.6 °C"); stated as "validated against Python STREAM" which is accurate per VAL-01 |
</threat_model>

<verification>
After the task:
1. `wc -l README.md` — should be > 80 lines (a real README, not a stub)
2. `grep -c "^##" README.md` — should return ≥ 8 (at least 8 top-level sections)
3. `grep "PointKinetics" README.md` — returns component catalog row
4. `grep -i "prerequisite\|required.*sysimage\|sysimage.*required" README.md` — returns nothing (sysimage not presented as required)
5. `grep "build_loop(" README.md` — returns quick-start code line
</verification>

<success_criteria>
- README.md has ≥ 8 second-level sections (## headers)
- Opens with physics description (what it models), not implementation details
- Contains all 6 components in catalog table
- Contains validation summary with "1%" tolerance claim
- Installation section includes sysimage caveat matching D-03 intent
- Contains Python STREAM relationship section
- Quick-start code block uses build_loop and is syntactically valid Julia
</success_criteria>

<output>
After completion, create `.planning/phases/50-open-source-readiness/50-04-SUMMARY.md`
</output>
