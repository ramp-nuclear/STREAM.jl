---
context: phase
phase: 48-scram-solver-integration
task: 0
total_tasks: 1
status: not_started
last_updated: 2026-04-05
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_None._

<current_state>
Phase 48 has NOT been started. No code has been written. The ROADMAP.md lists it as
`[ ] Phase 48: SCRAM Solver Integration (0/1 plans)`.

Last commit: `b4238a6` — fix(test-point-kinetics): update to new connect_temperature_feedback API
1380 tests passing on branch `gsd/v0.9-point-kinetics-reactor-control`.
</current_state>

<completed_work>
None — this phase has not started.

**Context from this session (2026-04-05):**
The following was designed and agreed upon in a planning discussion:

1. `change_state(ctrl, t, power, dPdt)` already has the right signature (from Phase 46).
2. `should_continue` and `abort_states` already exist on `ReactivityController`.
3. `solve_transient` already accepts `callbacks` kwarg.
4. The pattern follows Flapper: user constructs callback, passes it in.

**API decisions locked in:**
- `SCRAM_at_power(power_limit)` → returns `(state, t, power, dPdt) -> new_state`
- `scram_callback(ssys, ctrl; pk_sym=:pk)` → returns `DiscreteCallback`
  - Fires after every accepted step (condition = always true)
  - Extracts `integrator[pk_sys.P]`, calls `change_state(ctrl, t, power, 0.0)`
  - Calls `terminate!(integrator)` when `ctrl.state in ctrl.abort_states`
  - Handles standalone case: `nameof(ssys) == pk_sym ? ssys : getproperty(ssys, pk_sym)`
- dPdt hardcoded as 0.0 for now; future: `get_du(integrator)` with symbolic indexing
- Export both from `src/STREAM.jl`
- No changes to `solve_transient` — it stays generic
</completed_work>

<remaining_work>
**Plan 48-01 (the only plan):**

Task 1: Implementation
- Add `SCRAM_at_power(power_limit)` to `src/components/point_kinetics.jl`
- Add `scram_callback(ssys, ctrl; pk_sym=:pk)` to `src/components/point_kinetics.jl`
- Add `using DifferentialEquations: DiscreteCallback, terminate!` at top of file
- Export `SCRAM_at_power`, `scram_callback` in `src/STREAM.jl`

Task 2: Tests (in `test/test_point_kinetics.jl`)
- SCRAM-01: `SCRAM_at_power` state machine unit test (below/above/stays)
- SCRAM-02: `ReactivityController` + `change_state` + state log test
- SCRAM-03: `scram_callback` with standalone `PointKinetics` — verify `sol.t[end] < tspan[2]` (solver actually terminated early) and `ctrl.state == :SCRAM`

Task 3: Verify full suite still passes (1380 + new SCRAM tests)
</remaining_work>

<decisions_made>
- `scram_callback` is NOT part of `solve_transient` — stays generic, user passes callback via `callbacks` kwarg
- `dPdt=0.0` placeholder for now; add `get_du(integrator)` support when a state machine needs it
- `pk_sym` kwarg handles both standalone (`@named pk = PointKinetics(...)`) and composed (`ssys.pk`) cases
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading
1. `src/components/point_kinetics.jl` — ReactivityController, change_state, abort_states (lines ~296-412)
2. `src/STREAM.jl` — export list
3. `src/solvers.jl` — solve_transient callbacks kwarg pattern
4. `test/test_point_kinetics.jl` — find correct insertion point for new testsets

## Next Action
Execute Plan 48-01:
- Implement `SCRAM_at_power` + `scram_callback` in point_kinetics.jl
- Export both
- Write SCRAM-01/02/03 tests
- Verify 1380+ tests pass
- Commit and create 48-01-SUMMARY.md
