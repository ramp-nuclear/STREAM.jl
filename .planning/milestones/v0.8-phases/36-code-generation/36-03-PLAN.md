---
phase: 36-code-generation
plan: 03
type: execute
wave: 3
depends_on: ["36-02"]
files_modified: []
autonomous: false
requirements: [CODE-01, CODE-02, CODE-03, CODE-04, CODE-05, CODE-06, CODE-07]

must_haves:
  truths:
    - "Canvas with Pump-HeatExchanger-Channel-Pump loop generates valid STREAM.jl code"
    - "Pressing Export opens native file dialog; saved file contains generated code"
    - "BC panel allows adding pump.inlet.P ~ 1.0e5; generated code includes it"
    - "Bottom panel toggles open/closed via Code button"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete code generation feature: live code preview, BC editing, and file export.

Purpose: Confirm the entire Phase 36 feature works end-to-end from a user perspective.

Output: UAT confirmation or issue list.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-code-generation/36-CONTEXT.md
@.planning/phases/36-code-generation/36-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete code generation feature</name>
  <files>gui/src/components/Toolbar.tsx, gui/src/components/BottomPanel.tsx, gui/src/components/CodePreview.tsx, gui/src/components/BCPanel.tsx</files>
  <action>
    Human verification checkpoint. Phase 36 code generation: pure code generator function, live code preview in collapsible bottom panel, boundary condition editor, file export via native dialog.

    Start the app: `cd gui && npm run tauri dev`

    **Test 1 — Empty state:**
    1. Open the app with an empty canvas
    2. Click the "Code" button in the toolbar above the canvas
    3. Verify the bottom panel opens showing the Code tab
    4. Verify it displays: `# Add components to the canvas to generate Julia code.`
    5. Verify the Export button is disabled (grayed out)

    **Test 2 — Basic loop code generation:**
    1. Drag a Pump onto the canvas
    2. Drag a Channel onto the canvas
    3. Connect Pump.outlet to Channel.inlet and Channel.outlet to Pump.inlet
    4. Verify the code preview updates automatically with:
       - `using ModelingToolkit, STREAM`
       - `@named pump_1 = Pump(...)` with correct positional arg
       - `@named channel_1 = Channel(; n=..., geometry=...)` with keyword args
       - `connect(pump_1.outlet, channel_1.inlet)` and `connect(channel_1.outlet, pump_1.inlet)`
       - `ODESystem(eqs, t; systems=[pump_1, channel_1])`
       - `mtkcompile(sys)`

    **Test 3 — BC editing:**
    1. Click the "BCs" tab in the bottom panel
    2. Select pump_1 from the Component dropdown
    3. Select inlet.P from the Port dropdown
    4. Enter 1.0e5 in the value field
    5. Click Add
    6. Verify a BC row appears: `pump_1.inlet.P ~ 100000.0` (or similar)
    7. Switch to the Code tab and verify the BC appears in the eqs array
    8. Delete the BC row using the X button; verify it disappears from code

    **Test 4 — Parameter changes reflected:**
    1. Click the Channel node to open the sidebar
    2. Change n from default to 10
    3. Verify the Code preview updates to show `n=10`
    4. Change PipeGeometry dimensions; verify code updates

    **Test 5 — Export:**
    1. Click the Export button
    2. Verify a native OS file save dialog appears with "system.jl" as default
    3. Save the file to a known location
    4. Open the saved file in a text editor; verify it matches the code preview

    **Test 6 — Node deletion cleans up BCs:**
    1. Add a BC for pump_1 (any port/value)
    2. Delete the pump_1 node from the canvas
    3. Switch to BCs tab; verify the BC row for pump_1 is gone

    **Test 7 — Default elision:**
    1. Add a Channel with all default values (g=0, htc=dittus_boelter, friction=blasius_friction)
    2. Verify generated code does NOT include `g=0.0`, `htc_correlation=dittus_boelter`, or `friction_correlation=blasius_friction`
    3. Change g to 9.80665; verify it now appears in generated code
  </action>
  <verify>Manual: all 7 test scenarios pass visual/functional inspection. No console errors.</verify>
  <done>User confirms all code generation features work as specified. Type "approved" to confirm Phase 36 is complete, or describe any issues found.</done>
</task>

</tasks>

<verification>
All 7 manual test scenarios pass. No console errors.
</verification>

<success_criteria>
- User confirms all code generation features work as specified
- Canvas topology generates valid STREAM.jl code
- BC editing works end-to-end
- Export produces correct .jl file
</success_criteria>

<output>
After completion, create `.planning/phases/36-code-generation/36-03-SUMMARY.md`
</output>
