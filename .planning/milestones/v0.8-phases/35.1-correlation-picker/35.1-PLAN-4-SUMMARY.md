---
phase: 35.1-correlation-picker
plan: 4
subsystem: testing
tags: [vitest, testing-library, react, FunctionSelect, ParameterForm, InstanceNameField]

# Dependency graph
requires:
  - phase: 35.1-correlation-picker
    provides: FunctionSelect component, FactoryCorrelationValue type, info icons on NumericField and InstanceNameField

provides:
  - FunctionSelect.test.tsx with 9 tests covering all interaction paths
  - ParameterForm.test.tsx Function-type and activeMode filtering tests implemented
  - InstanceNameField.test.tsx label and info icon test added

affects: [35.1-correlation-picker, future GUI phases]

# Tech tracking
tech-stack:
  added: ["@testing-library/user-event ^14"]
  patterns: ["getByDisplayValue for inputMode=decimal inputs (not spinbutton)", "getAllByRole for multiple comboboxes in factory sub-field context"]

key-files:
  created:
    - gui/src/components/sidebar/__tests__/FunctionSelect.test.tsx
  modified:
    - gui/src/components/sidebar/__tests__/ParameterForm.test.tsx
    - gui/src/components/sidebar/__tests__/InstanceNameField.test.tsx
    - gui/package.json
    - gui/package-lock.json

key-decisions:
  - "getByDisplayValue('1') instead of getByRole('spinbutton'): NumericField uses inputMode=decimal not type=number so spinbutton role is not assigned"
  - "getAllByRole('combobox')[0] for top-level trigger: factory sub-field renders a second FunctionSelect so there are multiple comboboxes"
  - "Installed @testing-library/user-event: was not in devDeps but required by the plan's test spec"

patterns-established:
  - "FunctionSelect test pattern: use userEvent.click on combobox trigger then click option by label text"
  - "Factory sub-field numeric test pattern: use getByDisplayValue on the default value string"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-04-02
---

# Phase 35.1 Plan 4: Tests — FunctionSelect, ParameterForm Function Params, Info Icons Summary

**Vitest test suite for FunctionSelect (9 tests), ParameterForm mode filtering, and InstanceNameField info icon, with @testing-library/user-event installed**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-02T10:56:08Z
- **Completed:** 2026-04-02T10:58:30Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments
- Created FunctionSelect.test.tsx with 9 tests covering simple/factory emission, sub-field rendering, sub-field merging, and [object Object] guard
- Replaced two `it.todo` stubs in ParameterForm.test.tsx with working tests for Function-type params and activeMode filtering
- Added InstanceNameField Name label + SVG info icon test

## Task Commits

1. **Task 1: Create FunctionSelect.test.tsx** - `f727d1f` (test)
2. **Task 2: Fill ParameterForm.test.tsx stubs** - `8a1c93b` (test)
3. **Task 3: Add InstanceNameField label/icon test** - `2be96a1` (test)

## Files Created/Modified
- `gui/src/components/sidebar/__tests__/FunctionSelect.test.tsx` - 9 tests for FunctionSelect component
- `gui/src/components/sidebar/__tests__/ParameterForm.test.tsx` - 2 stubs replaced with working tests
- `gui/src/components/sidebar/__tests__/InstanceNameField.test.tsx` - 1 new test added
- `gui/package.json` - added @testing-library/user-event devDependency
- `gui/package-lock.json` - lockfile updated

## Decisions Made
- Used `getByDisplayValue("1")` to query the threshold NumericField input because `inputMode="decimal"` does not produce a `spinbutton` ARIA role (only `type="number"` does)
- Used `getAllByRole("combobox")[0]` in the [object Object] test because selecting a factory option also renders a sub-FunctionSelect dropdown (two comboboxes in DOM)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Installed missing @testing-library/user-event**
- **Found during:** Task 1 (FunctionSelect.test.tsx creation)
- **Issue:** Package not in devDependencies; import failed at test run time
- **Fix:** `npm install --save-dev @testing-library/user-event`
- **Files modified:** gui/package.json, gui/package-lock.json
- **Verification:** Import resolves, all userEvent.click calls succeed
- **Committed in:** f727d1f (Task 1 commit)

**2. [Rule 1 - Bug] Fixed two test queries incompatible with actual DOM**
- **Found during:** Task 1 verification run
- **Issue 1:** `getByRole("spinbutton")` fails — NumericField uses inputMode not type=number
- **Issue 2:** `getByRole("combobox")` fails with "multiple elements" — factory sub-field adds second combobox
- **Fix:** Changed to `getByDisplayValue("1")` and `getAllByRole("combobox")[0]` respectively
- **Files modified:** gui/src/components/sidebar/__tests__/FunctionSelect.test.tsx
- **Verification:** Both tests pass after fix
- **Committed in:** f727d1f (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both fixes necessary for tests to run. No scope creep.

## Issues Encountered
None beyond the two auto-fixed deviations above.

## Next Phase Readiness
- Phase 35.1 correlation-picker is complete (all 4 plans done)
- 77 tests pass, 17 todos remain (other component stubs not in scope of this phase)
- Ready to continue to next GUI phase

---
*Phase: 35.1-correlation-picker*
*Completed: 2026-04-02*
