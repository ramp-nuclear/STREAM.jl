---
phase: 68-layers-system-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gui/src/lib/layers.ts
  - gui/src/lib/__tests__/layers.test.ts
  - gui/src/components/ui/checkbox.tsx
  - gui/package.json
  - gui/package-lock.json
autonomous: true
requirements:
  - D-05
must_haves:
  truths:
    - "layers.ts exports a 4-key independent-layer API (LayerKey, ActiveLayers, ALL_LAYERS_ON, getComponentLayers, isNodeVisible, isEdgeDimmed)"
    - "The old LayerView type, isComponentVisibleInLayer, isNodeDimmed signatures are gone (D-05)"
    - "Layer membership is derived from ComponentDefinition.category, not port-type sniffing"
    - "shadcn Checkbox component file is installed at gui/src/components/ui/checkbox.tsx"
    - "All layers.ts tests pass with the new API"
  artifacts:
    - path: "gui/src/lib/layers.ts"
      provides: "Pure-function 4-layer API (no React/store/DOM deps)"
      contains: "export type LayerKey, export function isNodeVisible, export function isEdgeDimmed"
    - path: "gui/src/lib/__tests__/layers.test.ts"
      provides: "Unit tests for the new API surface"
    - path: "gui/src/components/ui/checkbox.tsx"
      provides: "shadcn Checkbox primitive"
  key_links:
    - from: "gui/src/lib/layers.ts"
      to: "gui/src/registry/types.ts"
      via: "import type { ComponentDefinition }"
      pattern: "ComponentDefinition.category"
---

<objective>
Rewrite `gui/src/lib/layers.ts` from the v0.8 three-mode API (`LayerView = "Hydraulic" | "Both" | "Thermal"`) to a four-layer independent-toggle API, and install the shadcn `Checkbox` primitive that downstream plans (LayersChip popover) require. This is the foundation everything else in Phase 68 imports from.

Purpose: Replaces the central typing surface so Wave 2 (store) and Wave 3 (UI) can compile against a stable contract; addresses D-05 (delete `LayerView`); decouples layer membership from port sniffing per the Claude's Discretion guidance in CONTEXT.md (category is the source of truth — handles Sources and Reactor Physics correctly which have no ports/non-canvas ports).

Output: New `layers.ts` source + test file with passing test suite; installed `checkbox.tsx` shadcn primitive.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-layers-system-overhaul/68-CONTEXT.md
@.planning/phases/68-layers-system-overhaul/68-RESEARCH.md
@.planning/phases/68-layers-system-overhaul/68-UI-SPEC.md

<interfaces>
From gui/src/registry/types.ts (ComponentDefinition.category values):
- "Hydraulic" | "Thermal" | "Sources" | "Resources" | "Reactor Physics"

Category → LayerKey mapping (per RESEARCH.md Pattern 2; "Resources" intentionally excluded — ReactivityController is not a canvas component):
- "Hydraulic"        → "Hydraulic"
- "Thermal"          → "Thermal"
- "Sources"          → "Sources"
- "Reactor Physics"  → "ReactorPhysics"
- "Resources"        → (no key — getComponentLayers returns [])

Current consumers of the old API (to be updated by later plans, do NOT touch in this plan):
- gui/src/store/useStore.ts                       (Plan 02)
- gui/src/components/CanvasPanel.tsx              (Plan 03)
- gui/src/components/StreamNode.tsx               (Plan 03)
- gui/src/components/ToolboxPanel.tsx             (Plan 03)
- gui/src/lib/projectIO.ts                        (Plan 02)
- gui/src/lib/__tests__/projectIO.snapToGrid.test.ts  (Plan 02)
- gui/src/store/__tests__/saveAndOpenErrors.test.ts   (Plan 02)
- gui/src/store/__tests__/useStore.test.ts        (Plan 02)
- gui/src/store/__tests__/useStore.codePanel.test.ts  (Plan 02)
- gui/src/components/__tests__/ToolboxPanel.test.tsx  (Plan 03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Checkbox primitive</name>
  <files>gui/src/components/ui/checkbox.tsx, gui/package.json, gui/package-lock.json</files>
  <read_first>
    - gui/components.json (shadcn preset: new-york / zinc / CSS variables)
    - gui/src/components/ui/toggle.tsx (existing shadcn primitive — confirm install location pattern)
    - .planning/phases/68-layers-system-overhaul/68-RESEARCH.md (Registry Safety section)
  </read_first>
  <action>From the `gui/` directory run `npx shadcn@latest add checkbox` (non-interactive — accept all defaults; shadcn writes to `gui/src/components/ui/checkbox.tsx` per the `gui/components.json` aliases). This is the official shadcn registry; no slopcheck required per UI-SPEC §"Registry Safety". After install, verify the file exports a `Checkbox` Radix-based component and that `@radix-ui/react-checkbox` was added to `gui/package.json` dependencies. Do NOT modify the generated checkbox.tsx — it is the canonical shadcn output.</action>
  <verify>
    <automated>cd gui && test -f src/components/ui/checkbox.tsx && grep -q 'export.*Checkbox' src/components/ui/checkbox.tsx && grep -q '@radix-ui/react-checkbox' package.json</automated>
  </verify>
  <done>`gui/src/components/ui/checkbox.tsx` exists with a `Checkbox` export; `@radix-ui/react-checkbox` is present in `gui/package.json` dependencies; `gui/package-lock.json` is updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite layers.ts with the 4-layer independent API + test it</name>
  <files>gui/src/lib/layers.ts, gui/src/lib/__tests__/layers.test.ts</files>
  <read_first>
    - gui/src/lib/layers.ts (the file being replaced — current API: LayerView, isNodeDimmed, isComponentVisibleInLayer, isEdgeDimmed, getComponentLayers)
    - gui/src/lib/__tests__/layers.test.ts (existing test scaffolding — to be replaced wholesale)
    - gui/src/registry/types.ts (ComponentDefinition shape — line ~213 for category field)
    - gui/src/registry/components.json (verify category values for fixture data: Channel="Hydraulic", ConstantTemperature="Thermal", WallTemperature="Sources", PointKinetics="Reactor Physics", ReactivityController="Resources")
    - .planning/phases/68-layers-system-overhaul/68-RESEARCH.md (Pattern 2 + "Code Examples / layers.ts New API Skeleton")
  </read_first>
  <behavior>
    Test cases (in `gui/src/lib/__tests__/layers.test.ts`, replacing the existing file entirely):

    `ALL_LAYERS_ON` constant:
    - Test 1: equals `{ Hydraulic: true, Thermal: true, Sources: true, ReactorPhysics: true }` exactly (all 4 keys, all true).

    `getComponentLayers(comp)`:
    - Test 2: comp with `category: "Hydraulic"` → returns `["Hydraulic"]`
    - Test 3: comp with `category: "Thermal"` → returns `["Thermal"]`
    - Test 4: comp with `category: "Sources"` → returns `["Sources"]`
    - Test 5: comp with `category: "Reactor Physics"` (with space) → returns `["ReactorPhysics"]` (no space)
    - Test 6: comp with `category: "Resources"` → returns `[]` (excluded from layer system)
    - Test 7: comp with an unknown category → returns `[]`

    `isNodeVisible(comp, activeLayers)`:
    - Test 8: Hydraulic comp with `{Hydraulic: true, ...}` → true
    - Test 9: Hydraulic comp with `{Hydraulic: false, Thermal: true, Sources: true, ReactorPhysics: true}` → false
    - Test 10: Thermal comp with `{Thermal: false, ...rest true}` → false
    - Test 11: Sources comp with `{Sources: false, ...rest true}` → false
    - Test 12: Reactor Physics comp with `{ReactorPhysics: false, ...rest true}` → false
    - Test 13: Resources comp (no layer) with all-off activeLayers → true (always visible — Resources have no canvas presence anyway, but the function must not hide them)
    - Test 14: Hydraulic comp with all layers off → false

    `isEdgeDimmed(edgeLayerKey, activeLayers)`:
    - Test 15: `isEdgeDimmed("Hydraulic", {Hydraulic: true, ...})` → false
    - Test 16: `isEdgeDimmed("Hydraulic", {Hydraulic: false, ...})` → true
    - Test 17: `isEdgeDimmed("Thermal", {Thermal: false, ...rest true})` → true
    - Test 18: `isEdgeDimmed(null, activeLayers)` → false (edges with no layer association are never dimmed)
  </behavior>
  <action>
    Replace `gui/src/lib/layers.ts` wholesale with the new API:

    1. Exports:
       - `export type LayerKey = "Hydraulic" | "Thermal" | "Sources" | "ReactorPhysics"` (no space in ReactorPhysics — TypeScript-friendly key form)
       - `export type ActiveLayers = Record<LayerKey, boolean>`
       - `export const ALL_LAYERS_ON: ActiveLayers = { Hydraulic: true, Thermal: true, Sources: true, ReactorPhysics: true }`
       - `export const LAYER_KEYS: readonly LayerKey[] = ["Hydraulic", "Thermal", "Sources", "ReactorPhysics"] as const` (ordering used by LayersChip popover row order per UI-SPEC §2)
       - `export function getComponentLayers(comp: ComponentDefinition): LayerKey[]`
       - `export function isNodeVisible(comp: ComponentDefinition, activeLayers: ActiveLayers): boolean`
       - `export function isEdgeDimmed(edgeLayerKey: LayerKey | null, activeLayers: ActiveLayers): boolean`

    2. Internal-only constant (not exported):
       - `const CATEGORY_TO_LAYER_KEY: Partial<Record<string, LayerKey>> = { "Hydraulic": "Hydraulic", "Thermal": "Thermal", "Sources": "Sources", "Reactor Physics": "ReactorPhysics" }` — note the space in `"Reactor Physics"` (matches registry category string exactly). "Resources" is intentionally omitted.

    3. Function bodies:
       - `getComponentLayers`: lookup `CATEGORY_TO_LAYER_KEY[comp.category]`; return `[key]` if defined, else `[]`.
       - `isNodeVisible`: `const layers = getComponentLayers(comp); if (layers.length === 0) return true; return layers.some(k => activeLayers[k]);`
       - `isEdgeDimmed`: `if (!edgeLayerKey) return false; return !activeLayers[edgeLayerKey];`

    4. DELETE entirely from the file: `LayerView` type, `isComponentVisibleInLayer` function, `isNodeDimmed` function, and the old port-sniffing `getComponentLayers` overload. Do NOT re-export them under aliases; this is a clean break (`feedback_no_back_compat_during_heavy_dev` — STREAM.jl + Composer GUI are in heavy dev).

    5. Imports: keep `import type { ComponentDefinition } from "../registry/types";`. The file has no other dependencies — it must remain pure-function (no React, no Zustand, no DOM) per `<execution_context>`.

    6. JSDoc on each exported symbol explaining purpose and the D-02 "visible if ANY layer is active" rule for `isNodeVisible`.

    Then rewrite `gui/src/lib/__tests__/layers.test.ts` from scratch (delete the existing test bodies; they reference deleted exports). Use the same `describe`/`it` Vitest pattern as the existing file. Fixture component shape: `{ id, name, category, ports: [], parameters: [] } as ComponentDefinition` — only `category` is read by the new API so the other fields can be minimal `as` casts. Cover all 18 test cases in `<behavior>`. Do NOT inline implementation in this action block.

    Verify TypeScript compilation of the layers module in isolation passes (the rest of `gui/src` still references deleted symbols and is expected to fail tsc until later plans land — see Plan 02/03; this plan only guarantees `layers.ts` + `layers.test.ts` compile and the test file passes via Vitest).
  </action>
  <verify>
    <automated>cd gui && npx vitest run src/lib/__tests__/layers.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `gui/src/lib/layers.ts` exports exactly: `LayerKey`, `ActiveLayers`, `ALL_LAYERS_ON`, `LAYER_KEYS`, `getComponentLayers`, `isNodeVisible`, `isEdgeDimmed`.
    - `gui/src/lib/layers.ts` does NOT export `LayerView`, `isComponentVisibleInLayer`, or `isNodeDimmed` (verify via `grep -E 'export.*(LayerView|isComponentVisibleInLayer|isNodeDimmed)' gui/src/lib/layers.ts` returns no matches).
    - `gui/src/lib/layers.ts` imports nothing from `react`, `zustand`, or `@xyflow/react` (verify via `grep -E "from ['\"](react|zustand|@xyflow)" gui/src/lib/layers.ts` returns no matches — pure function module).
    - All 18 test cases in `gui/src/lib/__tests__/layers.test.ts` pass under Vitest.
    - Test fixtures use `category` strings matching `gui/src/registry/components.json` values verbatim (`"Hydraulic"`, `"Thermal"`, `"Sources"`, `"Reactor Physics"` with the space, `"Resources"`).
  </acceptance_criteria>
  <done>New 4-layer API surface lives in `layers.ts`; `layers.test.ts` covers all 18 behaviors and passes; downstream plans can now import `LayerKey`/`ActiveLayers`/`isNodeVisible`/`isEdgeDimmed`/`LAYER_KEYS`/`ALL_LAYERS_ON`.</done>
</task>

</tasks>

<verification>
- `cd gui && npx vitest run src/lib/__tests__/layers.test.ts` → all tests pass.
- `cd gui && test -f src/components/ui/checkbox.tsx` → file exists.
- `grep -E 'export.*(LayerView|isComponentVisibleInLayer|isNodeDimmed)' gui/src/lib/layers.ts | grep -v '^#'` → no matches (old API gone).
- Note: full `cd gui && npx tsc --noEmit` is EXPECTED to fail in this plan because Plan 02/03 consumers still reference deleted symbols. That cleanup is owned by Wave 2 + Wave 3 plans; Wave 1's contract is solely the layers module + checkbox primitive.
</verification>

<success_criteria>
- Pure-function 4-layer API is live and tested in isolation.
- shadcn Checkbox primitive is available for the LayersChip popover (Plan 04).
- The `LayerKey` / `ActiveLayers` types are the canonical contract Wave 2's store rewrite will consume.
</success_criteria>

<output>
Create `.planning/phases/68-layers-system-overhaul/68-01-SUMMARY.md` per `templates/summary.md` when done. List the exported names from the new `layers.ts` so Plan 02/03/04 executors can grep the SUMMARY for the contract.
</output>
