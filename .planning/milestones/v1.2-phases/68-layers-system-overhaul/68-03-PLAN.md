---
phase: 68-layers-system-overhaul
plan: 03
type: execute
wave: 3
depends_on:
  - 68-02
files_modified:
  - gui/src/components/CanvasPanel.tsx
  - gui/src/components/StreamNode.tsx
  - gui/src/components/ToolboxPanel.tsx
  - gui/src/components/__tests__/ToolboxPanel.test.tsx
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04
  - D-06
  - D-11
must_haves:
  truths:
    - "Off-layer nodes are visually dimmed (opacity 0.2) AND non-interactive (selectable:false, draggable:false) in dim mode (D-02)"
    - "Off-layer nodes use ReactFlow hidden:true prop in hide mode (hideOffLayer === true) (D-02)"
    - "Off-layer edges dim to opacity 0.15 in dim mode and hidden:true in hide mode (D-04: edges follow their own layer)"
    - "Dual-layer nodes (CAC) stay visible when ANY of their layers is active; off-layer port handles dim to opacity 0.2 and are non-interactive (display:none in hide mode) (D-02 + D-03)"
    - "Completing a connection involving an off-layer node auto-enables that node's layer(s) via setLayerVisible — never blocks the connection"
    - "Tab key no longer cycles layers; the Tab handler block in CanvasPanel.tsx is removed entirely (D-06)"
    - "ToolboxPanel renders all draggable components regardless of activeLayers (D-11: toolbox does NOT filter by layer)"
    - "ToolboxPanel tests pass with no activeLayer fixture references"
  artifacts:
    - path: "gui/src/components/CanvasPanel.tsx"
      provides: "4-layer-aware node + edge enrichment; layer-aware onConnect; Tab handler removed"
    - path: "gui/src/components/StreamNode.tsx"
      provides: "Per-handle dim for dual-layer nodes driven by activeLayers (Thermal/Hydraulic) instead of LayerView"
    - path: "gui/src/components/ToolboxPanel.tsx"
      provides: "Always-show-all components (no layer-based filtering)"
  key_links:
    - from: "gui/src/components/CanvasPanel.tsx"
      to: "gui/src/lib/layers.ts"
      via: "isNodeVisible / isEdgeDimmed / getComponentLayers"
      pattern: "isNodeVisible\\(.*activeLayers\\)"
    - from: "gui/src/components/CanvasPanel.tsx onConnect"
      to: "useStore.setLayerVisible"
      via: "auto-enable on off-layer connect"
      pattern: "setLayerVisible\\("
    - from: "gui/src/components/StreamNode.tsx"
      to: "gui/src/store/useStore.ts"
      via: "activeLayers selector"
      pattern: "activeLayers\\.(Hydraulic|Thermal)"
---

<objective>
Migrate the three canvas-consumer files (CanvasPanel, StreamNode, ToolboxPanel) from the deleted three-mode API to the 4-layer independent-toggle API; implement the new off-layer locking, dual-layer port-handle dim, layer-aware connect, and Tab-shortcut removal contracts. Also remove ToolboxPanel's layer-based filter per D-11 (toolbox stays a stable drag palette).

Purpose: This is the canvas-side runtime behavior that makes layer toggling actually do something visible — the visual contract from UI-SPEC §3 ("Off-layer Visual State") lands here. Addresses D-02 (visible if any layer active), D-03 (off-layer port handle dim + lock), D-04 (edges follow their own layer), D-06 (Tab removed), D-11 (toolbox does not filter).

Output: Three updated component files + one updated test file that compile cleanly against the Plan 02 store and render correct visual + interactive behavior for the four layer states.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-layers-system-overhaul/68-CONTEXT.md
@.planning/phases/68-layers-system-overhaul/68-RESEARCH.md
@.planning/phases/68-layers-system-overhaul/68-UI-SPEC.md
@.planning/phases/68-layers-system-overhaul/68-01-SUMMARY.md
@.planning/phases/68-layers-system-overhaul/68-02-SUMMARY.md

<interfaces>
From gui/src/lib/layers.ts (Plan 01):
- export type LayerKey = "Hydraulic" | "Thermal" | "Sources" | "ReactorPhysics";
- export type ActiveLayers = Record<LayerKey, boolean>;
- export function getComponentLayers(comp: ComponentDefinition): LayerKey[];
- export function isNodeVisible(comp: ComponentDefinition, activeLayers: ActiveLayers): boolean;
- export function isEdgeDimmed(edgeLayerKey: LayerKey | null, activeLayers: ActiveLayers): boolean;

From gui/src/store/useStore.ts (Plan 02):
- state.activeLayers: ActiveLayers
- state.hideOffLayer: boolean
- state.setLayerVisible(key: LayerKey, visible: boolean): void
- state.toggleLayer / setAllLayersVisible / setHideOffLayer also available (Plan 04 consumer)

Existing CanvasPanel surface (per RESEARCH.md):
- enrichedNodes useMemo: lines ~96-131
- Tab shortcut block: lines ~274-286 inside the useEffect handleKeyDown
- onConnect callback: lines ~161-166
- onNodeClick dimming guard (Pitfall 1): line ~171
- Overlay button stack: line ~360 (`absolute top-2 right-2 z-10 flex flex-col gap-1`)

Existing StreamNode surface:
- Imports `type LayerView` at line ~15 (Pitfall 7)
- dimFlowHandles / dimThermalHandles logic at lines ~334, ~346-349

Existing ToolboxPanel:
- Imports `isComponentVisibleInLayer` from `../lib/layers` (Pitfall 6)
- Filtering logic at lines ~25-29
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite CanvasPanel enrichment + onConnect + remove Tab shortcut</name>
  <files>gui/src/components/CanvasPanel.tsx</files>
  <read_first>
    - gui/src/components/CanvasPanel.tsx (full file — node enrichment useMemo ~lines 96-131, onConnect ~lines 161-166, onNodeClick guard ~line 171, Tab handler ~lines 274-286, overlay cluster ~line 360)
    - gui/src/store/useStore.ts (confirm exported state shape: activeLayers, hideOffLayer, setLayerVisible — delivered by Plan 02)
    - gui/src/lib/layers.ts (confirm isNodeVisible / isEdgeDimmed / getComponentLayers signatures — delivered by Plan 01)
    - gui/src/registry/index.ts (the `getComponent(componentId)` helper used today by the enrichment pass — confirm signature stays the same)
    - gui/src/components/HydraulicEdge.tsx, gui/src/components/BCEdge.tsx (determine how to derive a per-edge LayerKey — by edge `type` field or by inspecting endpoint port types)
    - .planning/phases/68-layers-system-overhaul/68-UI-SPEC.md §3 ("Off-layer Visual State")
    - .planning/phases/68-layers-system-overhaul/68-RESEARCH.md (Pattern 1, Pattern 4, Pitfall 1, Pitfall 5)
  </read_first>
  <behavior>
    The CanvasPanel test surface is largely covered by ToolboxPanel.test.tsx (Task 3) and integration smoke; CanvasPanel itself has no dedicated unit test today. Behavioral contracts to satisfy (verified by tsc + downstream Wave-3 LayersChip plan's UAT):

    Node enrichment (in dim mode, hideOffLayer=false):
    - When `isNodeVisible(comp, activeLayers) === false`: enriched node carries `selectable: false`, `draggable: false`, and `style.opacity === 0.2` + `style.pointerEvents === "none"` + `style.transition === "opacity 150ms ease"`.
    - When `isNodeVisible === true`: enriched node passes through unchanged (no style override, default selectable/draggable).

    Node enrichment (in hide mode, hideOffLayer=true):
    - When `isNodeVisible === false`: enriched node carries `hidden: true`; no opacity transition applied (Pitfall: don't animate hidden nodes).
    - When `isNodeVisible === true`: enriched node passes through unchanged.

    Edge enrichment:
    - Each edge has a derivable `LayerKey | null` (Hydraulic for HydraulicEdge, Thermal for ThermalPort-typed edges, Sources for BCEdge / BCPort-connected edges; null if undetermined).
    - When edge's layer is off: opacity 0.15 + transition 150ms in dim mode; `hidden: true` in hide mode.
    - When BOTH endpoint nodes are hidden in hide mode: edge also gets `hidden: true` (Pitfall 5 — phantom-edge prevention).

    onConnect auto-enable (D-04 / Pattern 4):
    - After a new connection is created, iterate `[connection.source, connection.target]` → look up each node → get its `getComponentLayers(...)` → for each layer key that is currently `false` in `activeLayers`, call `setLayerVisible(key, true)`. Never block the connection.

    Tab handler removal (D-06):
    - The `if (e.key === "Tab")` block in the handleKeyDown useEffect is gone. Tab navigates focusable elements per browser default (the existing input-focus guard around the deleted block can also be removed if it has no other purpose, but verify first).

    onNodeClick guard removal (Pitfall 1):
    - The `if (isNodeDimmed(...)) return` guard is gone. ReactFlow's `selectable: false` enrichment now prevents click events for off-layer nodes — the guard becomes dead code referencing a deleted function.
  </behavior>
  <action>
    In `gui/src/components/CanvasPanel.tsx`:

    1. Imports:
       - Remove: `import { ..., LayerView, isNodeDimmed, isEdgeDimmed, isComponentVisibleInLayer, ... } from "../lib/layers"` (delete every old-API import).
       - Add: `import { type ActiveLayers, type LayerKey, getComponentLayers, isNodeVisible, isEdgeDimmed } from "../lib/layers";`
       - Store selectors: replace `const activeLayer = useStore(s => s.activeLayer)` with `const activeLayers = useStore(s => s.activeLayers); const hideOffLayer = useStore(s => s.hideOffLayer);`. The Plan-04 LayersChip will read these too — fine to use the same selector shape.
       - Remove any `cycleLayer` / `setActiveLayer` selector if present.

    2. `enrichedNodes` useMemo (rewrite the existing pass at ~lines 96-131):
       - Iterate `nodes.map(node => ...)`.
       - Get `nodeData = node.data as unknown as StreamNodeData; const comp = getComponent(nodeData.componentId)`. If `!comp`, return node unchanged.
       - `const visible = isNodeVisible(comp, activeLayers);` if `visible` is true, return node unchanged.
       - If `hideOffLayer === true`: return `{ ...node, hidden: true }`.
       - Else (dim mode): return `{ ...node, style: { ...node.style, opacity: 0.2, pointerEvents: "none" as const, transition: "opacity 150ms ease" }, selectable: false, draggable: false }`.
       - useMemo deps: `[nodes, activeLayers, hideOffLayer]`.

    3. `enrichedEdges` useMemo (rewrite the existing edge-dim pass):
       - For each edge, derive `edgeLayerKey: LayerKey | null` from edge metadata. Concrete derivation rule: look at `edge.type` (existing options today are "hydraulic" / "thermal" / "bc" or similar — confirm from the file). Map: `"hydraulic"`→`"Hydraulic"`, `"thermal"`→`"Thermal"`, `"bc"`→`"Sources"`, anything else→`null`. If the existing file uses a different mechanism (e.g. inspecting source/target port types via the node data lookup pattern from the current `edgeIsThermal` derivation at line ~122), keep that mechanism and feed its result into a `LayerKey | null` derivation instead of a boolean.
       - `const dimmed = isEdgeDimmed(edgeLayerKey, activeLayers);`
       - In hide mode: if `dimmed` OR (both endpoint nodes are hidden — check via `enrichedNodes`-derived hidden set), return `{ ...edge, hidden: true }`.
       - In dim mode: if `dimmed`, return `{ ...edge, style: { ...edge.style, opacity: 0.15, transition: "opacity 150ms ease" } }`.
       - Otherwise return edge unchanged.
       - useMemo deps: `[edges, activeLayers, hideOffLayer, enrichedNodes]` (the enrichedNodes dep is needed for the both-endpoints-hidden phantom-edge guard).

    4. `onConnect` callback (extend existing ~lines 161-166):
       - After the existing `addEdge(connection)` (or equivalent) call:
         ```
         const currentNodes = useStore.getState().nodes;
         const currentActive = useStore.getState().activeLayers;
         const setLayerVisible = useStore.getState().setLayerVisible;
         for (const nodeId of [connection.source, connection.target]) {
           if (!nodeId) continue;
           const node = currentNodes.find(n => n.id === nodeId);
           if (!node) continue;
           const comp = getComponent((node.data as unknown as StreamNodeData).componentId);
           if (!comp) continue;
           for (const key of getComponentLayers(comp)) {
             if (!currentActive[key]) setLayerVisible(key, true);
           }
         }
         ```
       - useCallback deps unchanged from current (just `[addEdge]` or whatever the existing deps are — the store is read via `getState()` to avoid re-binding on every layer change, matching the Phase 65 onConnect pattern).

    5. Tab handler removal:
       - Delete the entire `if (e.key === "Tab") { ... cycleLayer() ... }` block from the `handleKeyDown` useEffect at ~lines 274-286.
       - If the surrounding input-focus guard (`if (document.activeElement?.tagName === "INPUT" ...) return`) becomes orphaned (no other key bindings inside the useEffect care about it), leave it in place if other shortcuts also benefit; otherwise tidy it up. Prefer leaving the structure intact to avoid scope creep — only the Tab `if` block is the target.

    6. `onNodeClick` guard removal (Pitfall 1):
       - Delete the `if (isNodeDimmed(...)) return` guard at ~line 171. The `selectable: false` enrichment from step 2 takes over.

    7. Grep `CanvasPanel.tsx` for any remaining old-API references: `LayerView`, `isNodeDimmed`, `isComponentVisibleInLayer`, `cycleLayer`, `setActiveLayer`, `state.activeLayer` (singular). Verify zero matches (excluding `state.activeLayers` plural via `grep -E ... | grep -v activeLayers`).

    8. NOTE FOR PLAN 04 EXECUTOR: Do NOT add the `<LayersChip />` mount to the overlay cluster in this plan. The overlay stack at ~line 360 stays at the current children (ZoomIn/ZoomOut/FitView/InteractiveLock/SnapToGrid). Plan 04 owns the LayersChip component creation AND its mount into that stack. Keeping the mount in Plan 04 also keeps the wave boundaries clean — Plan 04 depends on Plan 03 because both touch CanvasPanel.tsx.

    Do NOT inline implementation in this action block; write the code in the file directly.
  </action>
  <verify>
    <automated>cd gui && npx tsc --noEmit -p . 2>&1 | grep -E 'CanvasPanel\.tsx' ; cd gui && npx vitest run src/store/__tests__/useStore.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `CanvasPanel.tsx` no longer references `LayerView`, `isNodeDimmed`, `isComponentVisibleInLayer`, `cycleLayer`, `setActiveLayer`. `grep -nE '\b(LayerView|isNodeDimmed|isComponentVisibleInLayer|cycleLayer|setActiveLayer)\b' gui/src/components/CanvasPanel.tsx | grep -v '^\s*//'` returns zero matches.
    - The `if (e.key === "Tab")` block is physically removed (grep for `e\.key.*Tab.*cycleLayer` and `cycleLayer\(\)` returns zero matches in this file).
    - `enrichedNodes` useMemo sets `selectable: false, draggable: false, style.opacity: 0.2` for off-layer nodes in dim mode, and `hidden: true` in hide mode (verifiable by reading the code; no isolated unit test exists for this component).
    - `enrichedEdges` useMemo sets opacity 0.15 in dim mode + `hidden: true` in hide mode + phantom-edge guard.
    - `onConnect` calls `setLayerVisible(key, true)` for any off layer of either endpoint after addEdge.
    - tsc reports zero errors specifically inside `CanvasPanel.tsx` (errors in OTHER files in the same tsc pass are still acceptable — Plan 04 + Plan 05 will close those).
  </acceptance_criteria>
  <done>CanvasPanel speaks the 4-layer API end-to-end: node enrichment, edge enrichment, layer-aware connect, no Tab cycling, no stale onNodeClick guard.</done>
</task>

<task type="auto">
  <name>Task 2: Update StreamNode dual-layer port-handle dim logic</name>
  <files>gui/src/components/StreamNode.tsx</files>
  <read_first>
    - gui/src/components/StreamNode.tsx (full file — `import type { LayerView }` at line ~15, dimFlowHandles / dimThermalHandles derivation at lines ~334 + ~346-349 per RESEARCH.md Pitfall 7)
    - gui/src/store/useStore.ts (confirm activeLayers selector available)
    - gui/src/lib/layers.ts (LayerKey, ActiveLayers types only — no functions needed here; this file does per-handle dim, not per-node visibility)
    - .planning/phases/68-layers-system-overhaul/68-CONTEXT.md (D-03: dual-layer node with one layer off → off-layer port handles dim to 0.2 + lock non-interactive)
    - .planning/phases/68-layers-system-overhaul/68-UI-SPEC.md §3 ("Dual-layer node with one layer off")
  </read_first>
  <action>
    In `gui/src/components/StreamNode.tsx`:

    1. Imports:
       - Remove `import type { LayerView } from "../lib/layers";` (LayerView is deleted per Plan 01).
       - Add `import { type ActiveLayers } from "../lib/layers";` if a type annotation is needed.

    2. Store selector:
       - Replace any `const activeLayer = useStore(s => s.activeLayer);` with `const activeLayers = useStore(s => s.activeLayers); const hideOffLayer = useStore(s => s.hideOffLayer);`.

    3. `dimFlowHandles` / `dimThermalHandles` derivation (existing logic at lines ~334 and ~346-349 today reads `activeLayer === "Thermal"` / `activeLayer === "Hydraulic"`):
       - Replace `activeLayer === "Thermal"` (which dims flow handles in the old API) with `activeLayers.Hydraulic === false`. (Reasoning: the flow handles belong to the Hydraulic layer; they dim when the Hydraulic layer is off.)
       - Replace `activeLayer === "Hydraulic"` (which dims thermal handles in the old API) with `activeLayers.Thermal === false`. (Thermal handles dim when the Thermal layer is off.)

    4. Handle-style application — keep the existing inline-style pattern but additionally apply the lock behavior per D-03:
       - When `dimFlowHandles === true`: each FlowPort handle gets `opacity: 0.2` AND `pointerEvents: "none"` in dim mode, OR `display: none` in hide mode (`hideOffLayer === true`). Wrap the existing handle style object accordingly.
       - When `dimThermalHandles === true`: same treatment for ThermalPort handles.
       - The node-level body remains fully visible when at least one of its layers is on (D-02 — handled by CanvasPanel enrichment in Task 1). StreamNode's job is solely the per-handle layer treatment.

    5. Grep `StreamNode.tsx` for any remaining `LayerView` reference. Zero matches required.

    Important: do NOT change the node body's overall visibility logic here — that lives in CanvasPanel's enrichment pass. StreamNode only handles the per-port-handle dim for dual-layer scenarios (the CAC case where node is visible but some handles belong to an off layer).
  </action>
  <verify>
    <automated>cd gui && out=$(npx tsc --noEmit -p . 2>&1); if echo "$out" | grep -qE 'StreamNode\.tsx'; then echo "FAIL: tsc errors in StreamNode.tsx:"; echo "$out" | grep -E 'StreamNode\.tsx'; exit 1; else echo "PASS: no tsc errors in StreamNode.tsx"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\b(LayerView)\b' gui/src/components/StreamNode.tsx | grep -v '^\s*//'` returns zero matches.
    - `dimFlowHandles` is derived from `activeLayers.Hydraulic === false`; `dimThermalHandles` from `activeLayers.Thermal === false`.
    - In dim mode, dimmed handles carry `opacity: 0.2` + `pointerEvents: "none"`.
    - In hide mode (`hideOffLayer === true`), dimmed handles carry `display: none`.
    - tsc reports zero errors specifically inside `StreamNode.tsx`.
  </acceptance_criteria>
  <done>Dual-layer (Hydraulic+Thermal) nodes like CAC display their off-layer port handles dim+locked per D-03 in dim mode and hidden in hide mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Strip ToolboxPanel layer filtering (D-11) + update its tests</name>
  <files>gui/src/components/ToolboxPanel.tsx, gui/src/components/__tests__/ToolboxPanel.test.tsx</files>
  <read_first>
    - gui/src/components/ToolboxPanel.tsx (full file — `isComponentVisibleInLayer` import + use at lines ~25-29 per RESEARCH.md Pitfall 6)
    - gui/src/components/__tests__/ToolboxPanel.test.tsx (full file — search for `activeLayer:` fixtures)
    - gui/src/store/useStore.ts (confirm Plan 02 store API — `activeLayer` no longer exists)
    - .planning/phases/68-layers-system-overhaul/68-CONTEXT.md (D-11 from post_research_design_decisions: ToolboxPanel does NOT filter by activeLayers)
    - .planning/phases/68-layers-system-overhaul/68-RESEARCH.md ("ToolboxPanel Layer Filtering Impact" section + recommendation)
  </read_first>
  <behavior>
    Test cases for `ToolboxPanel.test.tsx` (modify the existing file in place; do not add new test files):

    Toolbox does NOT filter by layer (D-11):
    - Test 1: When `activeLayers = { Hydraulic: false, Thermal: true, Sources: true, ReactorPhysics: true }` is in the store, ToolboxPanel still renders ALL Hydraulic components (Channel, Pump, Friction, etc.) in the toolbox.
    - Test 2: When `activeLayers = { Hydraulic: true, Thermal: false, Sources: true, ReactorPhysics: true }`, ToolboxPanel still renders ALL Thermal components (ConstantTemperature, HeatDiffusion).
    - Test 3: When `activeLayers = { Hydraulic: false, Thermal: false, Sources: false, ReactorPhysics: false }` (all off), ToolboxPanel still renders every component present in the registry.

    Existing toolbox structural tests (category rendering, drag handlers, etc.):
    - All existing structural tests in the file must continue to pass. The change is purely removing the visibility filter — the category headings and drag affordances are untouched.

    Fixture migration:
    - Any pre-existing test setup that calls `useStore.setState({ activeLayer: "Both" })` or passes `activeLayer` props must be updated to `useStore.setState({ activeLayers: { Hydraulic: true, Thermal: true, Sources: true, ReactorPhysics: true }, hideOffLayer: false })`.
  </behavior>
  <action>
    In `gui/src/components/ToolboxPanel.tsx`:

    1. Imports:
       - Remove `isComponentVisibleInLayer` from the `../lib/layers` import (function is deleted in Plan 01).
       - Remove any `LayerView` import.
       - Remove any selector that reads `state.activeLayer` (singular) since the toolbox no longer cares about layer state. Keep selectors for components/registry lookup unchanged.

    2. Filter removal:
       - Locate the existing `.filter(comp => isComponentVisibleInLayer(comp, activeLayer))` call (or equivalent — exact shape at lines ~25-29 per research) and DELETE that filter step entirely.
       - If a `visibleSources` bypass exists (e.g., Sources always shown via a special branch), remove it too — the simplified rule is "show all draggable components from the registry, grouped by category, always." Category-based grouping itself (the headings: Hydraulic / Thermal / Sources / Reactor Physics / Resources) is preserved — this is the rendering structure, not a visibility filter.

    3. Grep `ToolboxPanel.tsx` for any remaining `activeLayer` (singular), `LayerView`, `isComponentVisibleInLayer` references. Zero matches required.

    In `gui/src/components/__tests__/ToolboxPanel.test.tsx`:

    4. Search for any `activeLayer:` literal in `useStore.setState(...)` calls or in mock props. Replace with `activeLayers: { Hydraulic: true, Thermal: true, Sources: true, ReactorPhysics: true }, hideOffLayer: false`.

    5. Add a new `describe("layer filter (D-11)", ...)` block at the end of the file covering tests 1–3 from `<behavior>`. Use the existing test render pattern (look at how the file already calls render(`<ToolboxPanel />`) and asserts on rendered text — mirror that). Use `useStore.setState({ activeLayers: ... })` to set up each test's layer state before the render call.

    6. Verify the existing structural tests in this file still pass — do NOT rewrite them, only update fixtures.
  </action>
  <verify>
    <automated>cd gui && npx vitest run src/components/__tests__/ToolboxPanel.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `ToolboxPanel.tsx` has no remaining `isComponentVisibleInLayer` import or call. `grep -n isComponentVisibleInLayer gui/src/components/ToolboxPanel.tsx | grep -v '^\s*//'` returns zero matches.
    - `ToolboxPanel.tsx` has no `activeLayer` selector (singular). `grep -nE '\bactiveLayer\b' gui/src/components/ToolboxPanel.tsx | grep -v activeLayers | grep -v '^\s*//'` returns zero matches.
    - All three new D-11 tests pass: with any combination of `activeLayers`, the toolbox renders the same set of components.
    - All pre-existing tests in `ToolboxPanel.test.tsx` continue to pass.
  </acceptance_criteria>
  <done>Toolbox is a stable drag palette: every registry-listed draggable component appears regardless of layer state, per D-11.</done>
</task>

</tasks>

<verification>
- `cd gui && npx vitest run src/components/__tests__/ToolboxPanel.test.tsx src/store/__tests__/useStore.test.ts src/lib/__tests__/projectIO.snapToGrid.test.ts src/lib/__tests__/layers.test.ts` → all pass.
- `cd gui && npx tsc --noEmit -p . 2>&1 | grep -E '(CanvasPanel|StreamNode|ToolboxPanel)\.tsx'` → zero matches (these three files compile clean). Errors in `App.tsx`, `BottomPanel.tsx`, `FileMenu.tsx`, `ViewMenu.tsx`, `SecondaryToolbar.tsx` are still expected — owned by Plan 05.
- `grep -rnE '\b(LayerView|isNodeDimmed|isComponentVisibleInLayer)\b' gui/src --include='*.ts' --include='*.tsx' | grep -v __tests__ | grep -v '^\s*//'` → only matches inside files owned by Plan 04 or Plan 05 (LayersChip and SecondaryToolbar respectively, both of which will go away or be created fresh).
</verification>

<success_criteria>
- Canvas correctly renders off-layer items dimmed+locked (or hidden) per D-02/D-03/D-04.
- Layer-aware connect tool auto-enables hidden layers; never blocks.
- Tab key no longer cycles layers (D-06).
- Toolbox displays all components always (D-11).
- Three component files + one test file compile and pass.
</success_criteria>

<output>
Create `.planning/phases/68-layers-system-overhaul/68-03-SUMMARY.md` per `templates/summary.md` when done. Specifically document the edge-layer derivation rule used (edge.type mapping vs port-type inspection) so Plan 04's LayersChip visual smoke-test and any future verification phase has the exact contract.
</output>
