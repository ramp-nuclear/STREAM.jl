---
phase: 68-layers-system-overhaul
plan: 04
type: execute
wave: 4
depends_on:
  - 68-02
  - 68-03
files_modified:
  - gui/src/components/LayersChip.tsx
  - gui/src/components/__tests__/LayersChip.test.tsx
  - gui/src/components/CanvasPanel.tsx
autonomous: true
requirements:
  - D-01
must_haves:
  truths:
    - "A LayersChip component exists at gui/src/components/LayersChip.tsx with chip + popover + 4 color squares + Layers label, using the D-01 accent palette (Hydraulic #3b82f6 / Thermal #f59e0b / Sources #8b5cf6 / Reactor Physics #f43f5e)"
    - "The chip is mounted in CanvasPanel.tsx as the last child of the top-right absolute overlay stack"
    - "Each of the 4 color squares is at full opacity when its layer is active, opacity 0.35 when inactive"
    - "Clicking the chip opens a Popover (side=left, align=start, w-52) containing 4 Checkbox rows in order: Hydraulic, Thermal, Sources, Reactor Physics"
    - "Each Checkbox in the popover is bound to activeLayers[key] and onCheckedChange calls toggleLayer(key)"
    - "Popover contains a Dim/Hide Toggle pair bound to hideOffLayer (Dim pressed when hideOffLayer=false, Hide pressed when hideOffLayer=true) that calls setHideOffLayer"
    - "Chip background shifts to bg-secondary when any layer is off (visual cue that 'not showing everything')"
    - "LayersChip.test.tsx covers chip color states, popover open/close, checkbox toggle, and Dim/Hide toggle"
  artifacts:
    - path: "gui/src/components/LayersChip.tsx"
      provides: "Floating layer-control chip + popover"
    - path: "gui/src/components/__tests__/LayersChip.test.tsx"
      provides: "Vitest interaction tests for the chip"
    - path: "gui/src/components/CanvasPanel.tsx"
      provides: "Updated overlay stack mounting LayersChip"
  key_links:
    - from: "gui/src/components/LayersChip.tsx"
      to: "gui/src/store/useStore.ts"
      via: "activeLayers + hideOffLayer + toggleLayer + setHideOffLayer selectors"
      pattern: "useStore"
    - from: "gui/src/components/LayersChip.tsx"
      to: "gui/src/lib/layers.ts"
      via: "LayerKey + LAYER_KEYS imports"
      pattern: "LAYER_KEYS"
    - from: "gui/src/components/LayersChip.tsx"
      to: "gui/src/components/ui/checkbox.tsx"
      via: "shadcn Checkbox import"
      pattern: "Checkbox"
    - from: "gui/src/components/CanvasPanel.tsx"
      to: "gui/src/components/LayersChip.tsx"
      via: "JSX mount in absolute overlay stack"
      pattern: "<LayersChip"
---

<objective>
Create the new `LayersChip` component (floating chip with 4-color indicator + popover containing 4 checkboxes + Dim/Hide toggle pair) and mount it into the top-right canvas overlay stack alongside the existing Phase 65 buttons. This is the sole user-facing control surface for the new 4-layer state.

Purpose: Phase 68's locked goal per CONTEXT.md is a "floating Layers chip pinned top-right of the canvas" — this plan delivers that single visual focal point. Addresses D-01 (4-color accent palette) and surfaces the Plan 02 store actions (toggleLayer, setHideOffLayer) to the user. This plan rides Wave 4 because it depends on (a) Plan 02's store contract AND (b) Plan 03's CanvasPanel.tsx edits — putting Plan 04 in the same wave as Plan 03 would create a CanvasPanel.tsx file conflict.

Output: New `LayersChip.tsx` + companion test file, plus a single-line JSX mount in `CanvasPanel.tsx`.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-layers-system-overhaul/68-CONTEXT.md
@.planning/phases/68-layers-system-overhaul/68-RESEARCH.md
@.planning/phases/68-layers-system-overhaul/68-UI-SPEC.md
@.planning/phases/68-layers-system-overhaul/68-01-SUMMARY.md
@.planning/phases/68-layers-system-overhaul/68-02-SUMMARY.md
@.planning/phases/68-layers-system-overhaul/68-03-SUMMARY.md

<interfaces>
From gui/src/lib/layers.ts (Plan 01):
- export type LayerKey = "Hydraulic" | "Thermal" | "Sources" | "ReactorPhysics";
- export const LAYER_KEYS: readonly LayerKey[];   // ordered: Hydraulic, Thermal, Sources, ReactorPhysics

From gui/src/store/useStore.ts (Plan 02):
- activeLayers: Record<LayerKey, boolean>
- hideOffLayer: boolean
- toggleLayer(key: LayerKey): void
- setHideOffLayer(value: boolean): void

From shadcn primitives (already installed; Plan 01 added Checkbox):
- gui/src/components/ui/popover.tsx — Popover, PopoverTrigger, PopoverContent
- gui/src/components/ui/checkbox.tsx — Checkbox (installed by Plan 01)
- gui/src/components/ui/toggle.tsx — Toggle (variant outline, size sm)
- gui/src/components/ui/separator.tsx — Separator

From CanvasPanel.tsx (Plan 03 baseline):
- Overlay stack at ~line 360: `absolute top-2 right-2 z-10 flex flex-col gap-1` div containing ZoomIn/ZoomOut/FitView/InteractiveLock/SnapToGridButton

Color values from CONTEXT.md D-01 + UI-SPEC §"Layer accent palette":
- Hydraulic       blue-500   #3b82f6
- Thermal         amber-500  #f59e0b
- Sources         violet-500 #8b5cf6
- ReactorPhysics  rose-500   #f43f5e
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LayersChip component (chip + popover)</name>
  <files>gui/src/components/LayersChip.tsx, gui/src/components/__tests__/LayersChip.test.tsx</files>
  <read_first>
    - gui/src/components/canvasMenus/SnapToGridButton.tsx (reference pattern for canvas overlay button: className shape, store-selector style, active-state visual)
    - gui/src/components/ui/popover.tsx (Popover / PopoverTrigger / PopoverContent API; default side/align props)
    - gui/src/components/ui/checkbox.tsx (Checkbox prop API — installed by Plan 01: checked, onCheckedChange, id, etc.)
    - gui/src/components/ui/toggle.tsx (Toggle prop API: pressed, onPressedChange, variant, size)
    - gui/src/components/ui/separator.tsx (Separator default orientation + className)
    - gui/src/lib/layers.ts (import LayerKey, LAYER_KEYS — delivered by Plan 01)
    - gui/src/store/useStore.ts (confirm activeLayers + hideOffLayer + toggleLayer + setHideOffLayer exports — delivered by Plan 02)
    - .planning/phases/68-layers-system-overhaul/68-UI-SPEC.md §1 ("Layers Chip"), §2 ("Layers Popover"), §"Copywriting Contract", §"Interaction Contract", §"Accessibility"
    - .planning/phases/68-layers-system-overhaul/68-CONTEXT.md (D-01 color palette + Claude's Discretion popover side/width)
    - .planning/phases/68-layers-system-overhaul/68-RESEARCH.md (Pattern 5 stub + "LayersChip Color Squares Pattern" code example)
  </read_first>
  <behavior>
    Test cases for `gui/src/components/__tests__/LayersChip.test.tsx` (new file using Vitest + @testing-library/react following the pattern in `gui/src/components/__tests__/ToolboxPanel.test.tsx`):

    Chip rendering + color square states:
    - Test 1: With default `activeLayers = ALL_LAYERS_ON`, render `<LayersChip />`. The chip shows 4 color squares; each has full opacity (verify via inline style backgroundColor matches the expected hex per LayerKey AND opacity is 1).
    - Test 2: Set `activeLayers.Hydraulic = false` in the store. The Hydraulic square (first square) has opacity 0.35; the other three squares remain at opacity 1.
    - Test 3: Set `activeLayers.Thermal = false`. The Thermal square (second) has opacity 0.35.
    - Test 4: Set `activeLayers.Sources = false`. The Sources square (third) has opacity 0.35.
    - Test 5: Set `activeLayers.ReactorPhysics = false`. The Reactor Physics square (fourth) has opacity 0.35.
    - Test 6: With any layer off, the chip's outer Button element has `bg-secondary` class applied (verify via className contains 'bg-secondary'). With all layers on, the chip's outer Button does NOT have `bg-secondary`.

    Popover open/close:
    - Test 7: Initially the popover content is not in the DOM. Clicking the chip button opens the popover; assertions check for the popover header text `Layers` and the 4 checkbox row labels (`Hydraulic`, `Thermal`, `Sources`, `Reactor Physics`).
    - Test 8: After the popover is open, the checkbox row labels match the LAYER_KEYS ordering top-to-bottom: Hydraulic first, then Thermal, then Sources, then Reactor Physics.

    Checkbox toggle behavior:
    - Test 9: Click the Hydraulic checkbox in the popover → assert `useStore.getState().activeLayers.Hydraulic === false` and the action `toggleLayer` was effectively called (verify via state change, not via spy — Zustand pattern).
    - Test 10: Click the Hydraulic checkbox again → activeLayers.Hydraulic flips back to true.
    - Test 11: Each of the four checkboxes is bound to its own LayerKey (parameterize: for each key in LAYER_KEYS, click the checkbox and assert only that key flipped in `activeLayers`).

    Dim/Hide toggle pair:
    - Test 12: Default state: hideOffLayer=false → the `Dim` Toggle is pressed (pressed=true via aria-pressed or data-state), the `Hide` Toggle is not pressed.
    - Test 13: Click the `Hide` Toggle → `useStore.getState().hideOffLayer === true`; the `Hide` Toggle is now pressed; the `Dim` Toggle is not pressed.
    - Test 14: Click the `Dim` Toggle → `useStore.getState().hideOffLayer === false`.

    Accessibility:
    - Test 15: The chip button has `aria-label="Open layer visibility controls"`.
    - Test 16: The closed-popover state has `aria-expanded="false"` on the chip; opening flips it to true.

    Note: Tests setup must reset the store before each test using `useStore.setState({ activeLayers: { ...ALL_LAYERS_ON }, hideOffLayer: false, isDirty: false })` in a `beforeEach`. Import `ALL_LAYERS_ON` from `../../lib/layers`. For Radix Popover content to be queryable, use `userEvent.click(chipButton)` and then `findByText` / `findByRole` — Radix portals to document.body which @testing-library/react queries reach by default.
  </behavior>
  <action>
    Create `gui/src/components/LayersChip.tsx`:

    1. Imports:
       - React (useState for popover open state if not using the shadcn uncontrolled Popover).
       - From `../lib/layers`: `type LayerKey`, `LAYER_KEYS`.
       - From `../store/useStore`: the `useStore` hook (selectors for activeLayers, hideOffLayer, toggleLayer, setHideOffLayer).
       - From `./ui/popover`: `Popover`, `PopoverTrigger`, `PopoverContent`.
       - From `./ui/checkbox`: `Checkbox`.
       - From `./ui/toggle`: `Toggle`.
       - From `./ui/separator`: `Separator`.

    2. Constants (declared at module scope, not inside the component):
       - `const LAYER_COLORS: Record<LayerKey, string> = { Hydraulic: "#3b82f6", Thermal: "#f59e0b", Sources: "#8b5cf6", ReactorPhysics: "#f43f5e" };`
       - `const LAYER_LABELS: Record<LayerKey, string> = { Hydraulic: "Hydraulic", Thermal: "Thermal", Sources: "Sources", ReactorPhysics: "Reactor Physics" };` (note the space in the Reactor Physics label per UI-SPEC Copywriting Contract)

    3. Component shape — default export `LayersChip` function component:
       - Selectors: `const activeLayers = useStore(s => s.activeLayers); const hideOffLayer = useStore(s => s.hideOffLayer); const toggleLayer = useStore(s => s.toggleLayer); const setHideOffLayer = useStore(s => s.setHideOffLayer);`
       - Local state: `const [open, setOpen] = useState(false);` (so the chip button can also expose aria-expanded correctly even though Radix tracks open internally).
       - Derived: `const anyOff = LAYER_KEYS.some(k => !activeLayers[k]);` (drives the chip's bg-secondary highlight).
       - Render structure: `<Popover open={open} onOpenChange={setOpen}><PopoverTrigger asChild>...chip button...</PopoverTrigger><PopoverContent side="left" align="start" className="w-52 p-3">...popover body...</PopoverContent></Popover>`

    4. Chip button (PopoverTrigger child) — visual contract per UI-SPEC §1:
       - Use a `button` element (not the shadcn Button component — UI-SPEC describes a custom-classed div/button matching SnapToGridButton's pattern; mirror that file's exact className idiom).
       - className composition:
         - Base: `flex items-center justify-center gap-2 h-8 px-2 rounded border shadow-sm transition-colors text-foreground border-border`
         - Background: `bg-background hover:bg-accent hover:text-accent-foreground` when `anyOff === false`, OR `bg-secondary hover:bg-accent hover:text-accent-foreground` when `anyOff === true` (apply via conditional class concatenation — use the `cn()` util if SnapToGridButton uses one).
       - aria-label: `Open layer visibility controls`
       - aria-expanded: `{open}`
       - Inside the button: a row with 4 color squares + a `span` with className `text-[12px] font-semibold` containing the literal text `Layers`.
       - Color squares: iterate `LAYER_KEYS.map(key => <span key={key} aria-hidden="true" className="w-3 h-3 rounded-[2px] inline-block transition-opacity duration-150" style={{ backgroundColor: LAYER_COLORS[key], opacity: activeLayers[key] ? 1 : 0.35 }} />)`. Wrap the squares in a flex container with `gap-1` (4px) so they tile correctly per UI-SPEC §1.

    5. Popover content — visual contract per UI-SPEC §2:
       - PopoverContent props: `side="left"`, `align="start"`, className `w-52 p-3`.
       - Header: a div with className `text-[12px] font-semibold mb-2` containing literal text `Layers`.
       - Checkbox list: iterate `LAYER_KEYS.map(key => ...)`. Each row is a `<label className="flex items-center gap-2 py-1 cursor-pointer">` wrapping:
         - A color square `span` with className `w-3 h-3 rounded-[2px]` and inline `style={{ backgroundColor: LAYER_COLORS[key] }}` (full opacity — this is a legend swatch, NOT a state indicator).
         - A `<Checkbox checked={activeLayers[key]} onCheckedChange={() => toggleLayer(key)} id={"layer-checkbox-" + key} />`.
         - A label text `span` with className `text-[13px] font-normal text-foreground` containing `LAYER_LABELS[key]`.
       - Separator: `<Separator className="my-2" />` between the checkbox list and the Off-layer section.
       - Off-layer section heading: a div with className `text-[12px] font-semibold uppercase tracking-[0.08em] text-muted-foreground mb-2` containing literal text `OFF-LAYER`.
       - Toggle pair: a flex row `<div className="flex gap-2">` containing two `<Toggle>` elements:
         - Dim: `<Toggle variant="outline" size="sm" pressed={hideOffLayer === false} onPressedChange={() => setHideOffLayer(false)} className="flex-1 text-[12px]">Dim</Toggle>`
         - Hide: `<Toggle variant="outline" size="sm" pressed={hideOffLayer === true} onPressedChange={() => setHideOffLayer(true)} className="flex-1 text-[12px]">Hide</Toggle>`

    6. Tooltip on the chip is NOT required by UI-SPEC §1. Skip.

    Do NOT inline implementation code in this action block beyond the snippets necessary for unambiguous identifier names. The executor writes the file content directly using the imports and visual contracts above.

    Then create `gui/src/components/__tests__/LayersChip.test.tsx`:
    - Vitest + @testing-library/react + @testing-library/user-event (look at `ToolboxPanel.test.tsx` for the exact import pattern in this codebase).
    - beforeEach resets the store as described in `<behavior>`.
    - Implement all 16 test cases in `<behavior>`.
    - Use `userEvent.setup()` for click interactions (shadcn / Radix Popover requires real user-event clicks to open).
    - For tests asserting `useStore.getState().activeLayers.X === Y`, prefer reading the store state directly rather than mocking the actions — Zustand store state IS the spec.
  </action>
  <verify>
    <automated>cd gui && npx vitest run src/components/__tests__/LayersChip.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `gui/src/components/LayersChip.tsx` exists with a default export `LayersChip` and uses the exact color hex values from D-01 (`#3b82f6`, `#f59e0b`, `#8b5cf6`, `#f43f5e`).
    - The chip renders 4 color squares in the order specified by `LAYER_KEYS` from `../lib/layers`.
    - Squares' opacity is 1 when their layer is active and 0.35 when inactive.
    - Chip button carries `aria-label="Open layer visibility controls"`.
    - Chip background uses `bg-secondary` when any layer is off, `bg-background` otherwise.
    - Popover content renders 4 Checkbox rows in LAYER_KEYS order with labels `Hydraulic`, `Thermal`, `Sources`, `Reactor Physics`.
    - Popover footer renders a `Dim`/`Hide` Toggle pair bound to `hideOffLayer` via `setHideOffLayer`.
    - All 16 test cases in `LayersChip.test.tsx` pass under Vitest.
    - tsc reports zero errors in `LayersChip.tsx` or its test.
  </acceptance_criteria>
  <done>LayersChip is a self-contained, store-bound, accessible chip + popover with passing interaction tests; ready to be mounted into the canvas overlay stack.</done>
</task>

<task type="auto">
  <name>Task 2: Mount LayersChip into CanvasPanel top-right overlay stack</name>
  <files>gui/src/components/CanvasPanel.tsx</files>
  <read_first>
    - gui/src/components/CanvasPanel.tsx (post-Plan-03 state — overlay stack at ~line 360, `absolute top-2 right-2 z-10 flex flex-col gap-1` containing the existing buttons)
    - gui/src/components/LayersChip.tsx (the file produced by Task 1 — confirm its default export and the import path)
    - .planning/phases/68-layers-system-overhaul/68-UI-SPEC.md §1 ("Placement" paragraph — LayersChip is appended as the LAST child of the overlay stack, below SnapToGridButton)
  </read_first>
  <action>
    In `gui/src/components/CanvasPanel.tsx`:

    1. Add the import at the top of the file alongside the existing component imports: `import LayersChip from "./LayersChip";` (use a default import — Task 1 produces a default export). Place the import near the SnapToGridButton import or alphabetically — match the file's existing import ordering convention.

    2. Locate the existing overlay stack div at ~line 360 (the `absolute top-2 right-2 z-10 flex flex-col gap-1` container). Plan 03 left this stack untouched.

    3. Append `<LayersChip />` as the LAST child of that container (after SnapToGridButton or whichever is currently last — per UI-SPEC §1 "Rendered as the last child below SnapToGridButton"). No props are needed — LayersChip pulls everything from the store.

    4. Verify there are no other LayersChip mount sites in the codebase: `grep -rn LayersChip gui/src` should return matches only in `LayersChip.tsx`, its test file, and `CanvasPanel.tsx` after this edit.

    5. Do NOT modify the container's className, gap, or position — UI-SPEC §1 says explicitly: "Do NOT create a separate overlay cluster — consistency with Phase 65 pattern."

    6. Run a full tsc check after this mount — at this point in the wave sequence, the entire `gui/src` tree should compile clean (Plan 03 cleared CanvasPanel/StreamNode/ToolboxPanel; Plan 05 cleared App/FileMenu/ViewMenu/BottomPanel + deleted SecondaryToolbar; Plan 04 Task 1 created LayersChip; this task adds the final consumer). If any tsc errors remain in any file in `gui/src/**/*.{ts,tsx}` (excluding pre-existing errors documented in `.planning/phases/61-.../deferred-items.md`), fix them in scope — the goal is a green tsc baseline at the end of Phase 68.
  </action>
  <verify>
    <automated>cd gui && grep -q '<LayersChip' src/components/CanvasPanel.tsx && grep -q 'import LayersChip' src/components/CanvasPanel.tsx && npx tsc --noEmit -p . 2>&1 | grep -vE '(^Found |^$)' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `gui/src/components/CanvasPanel.tsx` imports `LayersChip` from `./LayersChip` and renders `<LayersChip />` as a child of the `absolute top-2 right-2 z-10 flex flex-col gap-1` overlay container, appended after the existing buttons.
    - The overlay container's className, position, and gap are unchanged.
    - `cd gui && npx tsc --noEmit -p .` produces no NEW errors beyond the pre-existing baseline documented in `.planning/phases/61-registry-audit-rewrite-for-v1-1/deferred-items.md` (the 11 pre-existing tsc errors Phase 71 owns).
  </acceptance_criteria>
  <done>LayersChip is live on the canvas; the user can toggle individual layers and pick Dim/Hide directly from the floating chip.</done>
</task>

</tasks>

<verification>
- `cd gui && npx vitest run src/components/__tests__/LayersChip.test.tsx src/lib/__tests__/layers.test.ts src/store/__tests__/useStore.test.ts src/lib/__tests__/projectIO.snapToGrid.test.ts src/components/__tests__/ToolboxPanel.test.tsx --reporter=basic` → all pass.
- `cd gui && grep -rn LayersChip src` → matches in `LayersChip.tsx`, `LayersChip.test.tsx`, `CanvasPanel.tsx` only.
- `cd gui && npx tsc --noEmit -p .` → only the pre-existing 11 errors documented in Phase 61 deferred-items.md remain; no new errors introduced by Phase 68.
- Manual UAT (executor performs visually if running the dev build): launch `cd gui && npm run tauri dev`; verify the Layers chip renders top-right of the canvas with 4 color squares + label; clicking opens a popover with 4 checkboxes and a Dim/Hide pair; toggling Hydraulic dims (or hides) hydraulic-only components on the canvas; the chip's background turns secondary-tinted when any layer is off.
</verification>

<success_criteria>
- LayersChip is the sole user-facing control for layer visibility per CONTEXT.md.
- The 4-color palette from D-01 is faithfully reproduced (hex values exact).
- All four layer toggles wire to the Plan 02 store action `toggleLayer`; Dim/Hide toggle wires to `setHideOffLayer`.
- LayersChip.test.tsx covers chip rendering, color states, popover open/close, checkbox toggles, Dim/Hide toggle, and accessibility — all 16 cases pass.
- Phase 68 leaves `gui/src` with a green tsc baseline (no NEW errors beyond the documented Phase 61 deferred items).
</success_criteria>

<output>
Create `.planning/phases/68-layers-system-overhaul/68-04-SUMMARY.md` per `templates/summary.md` when done. Note the final canvas overlay-stack child order so any future overlay-stack change has a clear baseline.
</output>
