---
phase: 68-layers-system-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 68-01
files_modified:
  - gui/src/store/useStore.ts
  - gui/src/store/__tests__/useStore.test.ts
  - gui/src/store/__tests__/useStore.codePanel.test.ts
  - gui/src/store/__tests__/saveAndOpenErrors.test.ts
  - gui/src/lib/projectIO.ts
  - gui/src/lib/__tests__/projectIO.snapToGrid.test.ts
autonomous: true
requirements:
  - D-05
must_haves:
  truths:
    - "Store exposes activeLayers (Record<LayerKey, boolean>, default ALL_LAYERS_ON) and hideOffLayer (boolean, default false) state slices"
    - "Store exposes toggleLayer, setLayerVisible, setAllLayersVisible action setters"
    - "Old activeLayer / setActiveLayer / cycleLayer / LayerView no longer live in the store (D-05)"
    - ".scp save writes layout.active_layers (object) and layout.hide_off_layer (boolean)"
    - ".scp load reads layout.active_layers + layout.hide_off_layer; legacy layout.active_layer string is auto-converted on read (Both → all true, Hydraulic → only Hydraulic, Thermal → only Thermal)"
    - "All updated store + projectIO test files pass under Vitest"
  artifacts:
    - path: "gui/src/store/useStore.ts"
      provides: "Zustand store with 4-layer state + setters; .scp serialize/deserialize call sites updated"
    - path: "gui/src/lib/projectIO.ts"
      provides: ".scp layout block read/write for active_layers + hide_off_layer with legacy compat shim"
  key_links:
    - from: "gui/src/store/useStore.ts"
      to: "gui/src/lib/layers.ts"
      via: "import { LayerKey, ActiveLayers, ALL_LAYERS_ON }"
      pattern: "ALL_LAYERS_ON"
    - from: "gui/src/lib/projectIO.ts"
      to: "gui/src/lib/layers.ts"
      via: "import { LayerKey, ActiveLayers, ALL_LAYERS_ON }"
      pattern: "active_layers"
---

<objective>
Replace the v0.8 three-mode layer state in the Zustand store with the 4-layer independent-toggle state shape, and extend `.scp` project file serialization to round-trip the new fields with a read-side backward-compatibility shim for legacy files.

Purpose: Wave 3 UI plans (CanvasPanel enrichment, LayersChip, BottomPanel) all subscribe to `activeLayers` and `hideOffLayer` via store selectors and call `toggleLayer` / `setLayerVisible`. This plan provides that contract and ensures saved `.scp` files round-trip the new state. Addresses D-05 (delete `cycleLayer`/`setActiveLayer`/`LayerView`) and the Claude's-Discretion state-shape + persistence spec from CONTEXT.md.

Output: Updated `useStore.ts` + `projectIO.ts` + four test files compiling and passing under Vitest.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-layers-system-overhaul/68-CONTEXT.md
@.planning/phases/68-layers-system-overhaul/68-RESEARCH.md
@.planning/phases/68-layers-system-overhaul/68-01-SUMMARY.md

<interfaces>
From gui/src/lib/layers.ts (delivered by Plan 01):
- export type LayerKey = "Hydraulic" | "Thermal" | "Sources" | "ReactorPhysics";
- export type ActiveLayers = Record<LayerKey, boolean>;
- export const ALL_LAYERS_ON: ActiveLayers;
- export const LAYER_KEYS: readonly LayerKey[];

Existing store call sites that touch the old activeLayer (from RESEARCH.md Pitfall 2):
- state declaration:           useStore.ts ~lines 213-215
- setActiveLayer / cycleLayer: useStore.ts ~lines 1028-1038
- serializeProject(...activeLayer:) in saveProject:   ~line 2161
- serializeProject(...activeLayer:) in saveProjectAs: ~line 2227
- loadProject deserialization:                        ~line 2373
- newProject reset:                                   ~line 2453

Existing projectIO.ts layout block:
- SerializeProjectArgs (writes layout.active_layer string today)
- DeserializeProjectResult (reads layout.active_layer string today)
- Layout block lines ~58-63, ~82-84, ~141-148, ~195-203 per RESEARCH.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace activeLayer slice + actions in useStore with 4-layer state</name>
  <files>gui/src/store/useStore.ts, gui/src/store/__tests__/useStore.test.ts, gui/src/store/__tests__/useStore.codePanel.test.ts</files>
  <read_first>
    - gui/src/store/useStore.ts (full file — the activeLayer state lives at ~lines 213-215, cycleLayer/setActiveLayer at ~lines 1028-1038, serialize call sites at ~lines 2161, 2227, 2373, 2453)
    - gui/src/lib/layers.ts (new API from Plan 01: import LayerKey, ActiveLayers, ALL_LAYERS_ON)
    - gui/src/store/__tests__/useStore.test.ts (existing `describe("activeLayer")` block at ~line 308 to be replaced)
    - gui/src/store/__tests__/useStore.codePanel.test.ts (any references to activeLayer must be migrated)
    - .planning/phases/68-layers-system-overhaul/68-CONTEXT.md (Claude's Discretion: state shape + setter names)
    - .planning/phases/68-layers-system-overhaul/68-RESEARCH.md (Pitfall 2, Pitfall 4)
  </read_first>
  <behavior>
    Test cases in `useStore.test.ts` (replace the existing `describe("activeLayer")` block at ~line 308 with `describe("activeLayers")`):

    Default state:
    - Test 1: `useStore.getState().activeLayers` equals `ALL_LAYERS_ON` (`{ Hydraulic: true, Thermal: true, Sources: true, ReactorPhysics: true }`)
    - Test 2: `useStore.getState().hideOffLayer` equals `false`

    `toggleLayer(key)`:
    - Test 3: `toggleLayer("Hydraulic")` flips `activeLayers.Hydraulic` from true to false; other keys unchanged.
    - Test 4: Calling `toggleLayer("Thermal")` twice returns Thermal to true.
    - Test 5: Calling `toggleLayer` marks the project dirty (assert via `useStore.getState().isDirty === true` after a `clearDirty`/equivalent reset is NOT required; instead assert that calling toggleLayer flips isDirty from initial state — follow the pattern used in the surrounding tests for snapToGrid/other layout setters).

    `setLayerVisible(key, visible)`:
    - Test 6: `setLayerVisible("Sources", false)` sets Sources to false explicitly; idempotent on second call.
    - Test 7: `setLayerVisible("Sources", true)` after Test 6 restores Sources to true.

    `setAllLayersVisible(visible)`:
    - Test 8: `setAllLayersVisible(false)` sets all four keys to false at once.
    - Test 9: `setAllLayersVisible(true)` sets all four keys to true.

    `setHideOffLayer(value)`:
    - Test 10: `setHideOffLayer(true)` flips hideOffLayer to true; marks dirty.

    Removal verification:
    - Test 11: `(useStore.getState() as any).cycleLayer` is `undefined` (action removed per D-05).
    - Test 12: `(useStore.getState() as any).setActiveLayer` is `undefined`.
    - Test 13: `(useStore.getState() as any).activeLayer` is `undefined`.
  </behavior>
  <action>
    In `gui/src/store/useStore.ts`:

    1. Add at the imports section (alongside existing layer-related imports): `import { type LayerKey, type ActiveLayers, ALL_LAYERS_ON } from "../lib/layers";`. Remove any existing `import { ... LayerView ... } from "../lib/layers"` (LayerView is gone per Plan 01).

    2. State slice (replaces `activeLayer: LayerView` at ~line 213-215):
       - Remove the `activeLayer: LayerView;` field from the store state TypeScript type.
       - Add `activeLayers: ActiveLayers;` and `hideOffLayer: boolean;` to the same state type.
       - Default values in the `create(...)` initializer: `activeLayers: { ...ALL_LAYERS_ON }` (shallow clone so the constant cannot be mutated) and `hideOffLayer: false`.

    3. Action setters (replaces `setActiveLayer` and `cycleLayer` at ~lines 1028-1038):
       - DELETE `setActiveLayer` and `cycleLayer` entirely. Remove their TypeScript signatures from the store actions type.
       - ADD `toggleLayer: (key: LayerKey) => void` — implementation: `set(state => ({ activeLayers: { ...state.activeLayers, [key]: !state.activeLayers[key] }, isDirty: true }))` (mirror the dirty-flag pattern used by the surrounding setters such as `setSnapToGrid`).
       - ADD `setLayerVisible: (key: LayerKey, visible: boolean) => void` — implementation: `set(state => ({ activeLayers: { ...state.activeLayers, [key]: visible }, isDirty: true }))`.
       - ADD `setAllLayersVisible: (visible: boolean) => void` — implementation: `set(() => ({ activeLayers: { Hydraulic: visible, Thermal: visible, Sources: visible, ReactorPhysics: visible }, isDirty: true }))`.
       - ADD `setHideOffLayer: (value: boolean) => void` — implementation: `set(() => ({ hideOffLayer: value, isDirty: true }))`.

    4. `serializeProject` call sites (saveProject ~line 2161, saveProjectAs ~line 2227): replace the `activeLayer: state.activeLayer` arg with `activeLayers: state.activeLayers, hideOffLayer: state.hideOffLayer`. Update the call site only — the SerializeProjectArgs type is updated by Task 2 in this plan.

    5. `loadProject` deserialization (~line 2373): replace any read of `result.activeLayer` with reads of `result.activeLayers` and `result.hideOffLayer`. Pass them into the state set call. If the deserialized result has not been updated yet because of test-data shape mismatch, that is Task 2's responsibility — this task only writes the call site.

    6. `newProject` reset (~line 2453): replace `activeLayer: "Both"` (or whatever current value) with `activeLayers: { ...ALL_LAYERS_ON }, hideOffLayer: false`.

    7. Grep the whole `useStore.ts` for any remaining references to `activeLayer` (singular), `setActiveLayer`, `cycleLayer`, or `LayerView` and remove them. After this task `grep -n -E '\b(activeLayer|setActiveLayer|cycleLayer|LayerView)\b' gui/src/store/useStore.ts | grep -v '^#' | grep -v activeLayers` must return zero matches (the `grep -v activeLayers` is needed because the new field is `activeLayers` plural and partial-match would false-positive).

    Then update the test files:

    `gui/src/store/__tests__/useStore.test.ts`:
    - Replace the entire existing `describe("activeLayer", ...)` block (~line 308) with the `describe("activeLayers", ...)` block covering tests 1–13 from `<behavior>`.
    - At the top of the new describe block, add a `beforeEach` that resets the store to defaults via the existing test reset pattern (look at how other describe blocks reset — typically calling `useStore.setState({ activeLayers: { ...ALL_LAYERS_ON }, hideOffLayer: false, isDirty: false })` or equivalent).
    - Import `ALL_LAYERS_ON` from `../../lib/layers`.

    `gui/src/store/__tests__/useStore.codePanel.test.ts`:
    - Search this file for any `activeLayer:` literal in fixture/setState calls. Replace with `activeLayers: { ...ALL_LAYERS_ON }, hideOffLayer: false`. If no references exist, leave the file untouched. Do NOT add new behavioral tests here — this is a fixture-update touch only.
  </action>
  <verify>
    <automated>cd gui && npx vitest run src/store/__tests__/useStore.test.ts src/store/__tests__/useStore.codePanel.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `useStore.ts` has no remaining references to `activeLayer` (singular), `setActiveLayer`, `cycleLayer`, or `LayerView`. Verify via `grep -n -E '\b(setActiveLayer|cycleLayer|LayerView)\b' gui/src/store/useStore.ts | grep -v '^//' | grep -v '^\s*\*'` returns zero matches.
    - `useStore.ts` exports state with `activeLayers: ActiveLayers` and `hideOffLayer: boolean` fields.
    - All 13 behaviors in `useStore.test.ts` `describe("activeLayers")` block pass.
    - `useStore.codePanel.test.ts` still passes after fixture updates.
  </acceptance_criteria>
  <done>Store contract is the 4-layer independent shape; old setters are gone; both updated store test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend projectIO .scp layout block + add legacy active_layer read shim</name>
  <files>gui/src/lib/projectIO.ts, gui/src/lib/__tests__/projectIO.snapToGrid.test.ts, gui/src/store/__tests__/saveAndOpenErrors.test.ts</files>
  <read_first>
    - gui/src/lib/projectIO.ts (full file — SerializeProjectArgs type, DeserializeProjectResult type, layout block at lines ~58-63 / ~82-84 / ~141-148 / ~195-203 per RESEARCH.md)
    - gui/src/lib/__tests__/projectIO.snapToGrid.test.ts (line 61 passes `activeLayer: "Both"` per RESEARCH.md Pitfall 3 — fixture must be updated)
    - gui/src/store/__tests__/saveAndOpenErrors.test.ts (line 74 passes `activeLayer: "Both"` per RESEARCH.md Pitfall 3)
    - gui/src/lib/layers.ts (import LayerKey, ActiveLayers, ALL_LAYERS_ON)
    - .planning/phases/68-layers-system-overhaul/68-RESEARCH.md (Pattern 3 — exact legacy shim mapping)
  </read_first>
  <behavior>
    Test cases — extend `projectIO.snapToGrid.test.ts` (do NOT create a new file; the existing file is the home for layout-block round-trip tests per the established Phase 65 pattern):

    Round-trip of new fields:
    - Test A: `serializeProject({...minimal args, activeLayers: { Hydraulic: false, Thermal: true, Sources: true, ReactorPhysics: false }, hideOffLayer: true})` produces a JSON whose `layout.active_layers` and `layout.hide_off_layer` round-trip via `deserializeProject` to the same values.
    - Test B: `serializeProject({...minimal args, activeLayers: ALL_LAYERS_ON, hideOffLayer: false})` produces a JSON with `layout.active_layers` set to `{Hydraulic: true, Thermal: true, Sources: true, ReactorPhysics: true}` and `layout.hide_off_layer: false`.

    Legacy `active_layer` read compat (the meat of Pitfall 4 / D-05 backward-read):
    - Test C: Given a hand-built JSON payload with `layout.active_layer: "Both"` and NO `active_layers` field, `deserializeProject` returns `activeLayers === { Hydraulic: true, Thermal: true, Sources: true, ReactorPhysics: true }` and `hideOffLayer === false`.
    - Test D: Hand-built JSON with `layout.active_layer: "Hydraulic"` → `activeLayers === { Hydraulic: true, Thermal: false, Sources: false, ReactorPhysics: false }`.
    - Test E: Hand-built JSON with `layout.active_layer: "Thermal"` → `activeLayers === { Hydraulic: false, Thermal: true, Sources: false, ReactorPhysics: false }`.
    - Test F: Hand-built JSON with NEITHER `active_layer` nor `active_layers` (very old file or fresh project) → `activeLayers === ALL_LAYERS_ON`, `hideOffLayer === false`.
    - Test G: Hand-built JSON with both `active_layers` (new) AND `active_layer` (legacy) → new `active_layers` wins (the new field is the source of truth when present).

    Write-side absence of `active_layer`:
    - Test H: `serializeProject(...)` output does NOT contain an `active_layer` string field anywhere in the `layout` block (verify with `expect(serialized.layout.active_layer).toBeUndefined()`).
  </behavior>
  <action>
    In `gui/src/lib/projectIO.ts`:

    1. Imports: add `import { type LayerKey, type ActiveLayers, ALL_LAYERS_ON } from "./layers";`.

    2. `SerializeProjectArgs` type (the args passed in from `useStore.ts`):
       - REMOVE the `activeLayer: LayerView` (or string) field.
       - ADD `activeLayers: ActiveLayers;` and `hideOffLayer: boolean;` fields.

    3. `serializeProject(args)` body — inside the layout block construction:
       - REMOVE the line that writes `active_layer: args.activeLayer`.
       - ADD `active_layers: args.activeLayers,` and `hide_off_layer: args.hideOffLayer,` into the same layout object literal (preserve adjacent keys like `snap_to_grid`, `active_left_tab`).

    4. `DeserializeProjectResult` type:
       - REMOVE the `activeLayer` field.
       - ADD `activeLayers: ActiveLayers;` and `hideOffLayer: boolean;`.

    5. `deserializeProject(...)` body — replace the read of `rawLayout.active_layer` with this exact shim sequence (mirrors RESEARCH.md Pattern 3):

       ```
       const rawActiveLayers = rawLayout?.active_layers as Partial<Record<LayerKey, boolean>> | undefined;
       const legacyLayer = rawLayout?.active_layer as string | undefined;
       let activeLayers: ActiveLayers;
       if (rawActiveLayers) {
         activeLayers = { ...ALL_LAYERS_ON, ...rawActiveLayers };  // new field wins; missing keys default to true
       } else if (legacyLayer === "Hydraulic") {
         activeLayers = { Hydraulic: true, Thermal: false, Sources: false, ReactorPhysics: false };
       } else if (legacyLayer === "Thermal") {
         activeLayers = { Hydraulic: false, Thermal: true, Sources: false, ReactorPhysics: false };
       } else {
         activeLayers = { ...ALL_LAYERS_ON };  // "Both" or missing/unknown
       }
       const hideOffLayer: boolean = (rawLayout?.hide_off_layer as boolean | undefined) ?? false;
       ```

       Then add `activeLayers, hideOffLayer` to the returned object alongside the other layout-derived fields.

    6. Grep `projectIO.ts` for any remaining references to `activeLayer` (singular) or `LayerView` — remove them. The shim's `legacyLayer` local variable is acceptable; only the typed fields must go.

    Then update test fixtures:

    `gui/src/lib/__tests__/projectIO.snapToGrid.test.ts`:
    - At line ~61 (per RESEARCH.md Pitfall 3): replace `activeLayer: "Both"` in the `serializeProject(...)` fixture call with `activeLayers: { Hydraulic: true, Thermal: true, Sources: true, ReactorPhysics: true }, hideOffLayer: false`.
    - APPEND a new `describe("active_layers round-trip + legacy compat", ...)` block at the end of the file covering tests A through H from `<behavior>`. Import `ALL_LAYERS_ON` from `../layers`.

    `gui/src/store/__tests__/saveAndOpenErrors.test.ts`:
    - At line ~74 (per RESEARCH.md Pitfall 3): replace `activeLayer: "Both"` with `activeLayers: { Hydraulic: true, Thermal: true, Sources: true, ReactorPhysics: true }, hideOffLayer: false`.
    - Do NOT add new behavioral tests in this file; fixture-update touch only.
  </action>
  <verify>
    <automated>cd gui && npx vitest run src/lib/__tests__/projectIO.snapToGrid.test.ts src/store/__tests__/saveAndOpenErrors.test.ts src/store/__tests__/useStore.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `projectIO.ts` `SerializeProjectArgs` and `DeserializeProjectResult` both contain `activeLayers: ActiveLayers` + `hideOffLayer: boolean` and NO `activeLayer` field.
    - `serializeProject` writes `layout.active_layers` + `layout.hide_off_layer` and does NOT write `layout.active_layer` (verified by Test H).
    - `deserializeProject` reads new fields first; falls back to the legacy `active_layer` shim mapping when new fields are absent; defaults to ALL_LAYERS_ON when both absent.
    - All test cases A–H pass in `projectIO.snapToGrid.test.ts`.
    - `saveAndOpenErrors.test.ts` still passes after fixture update.
    - `useStore.test.ts` `describe("activeLayers")` block from Task 1 continues to pass (regression check — Task 2's `serializeProject` args type change must be compatible with Task 1's serialize call sites).
  </acceptance_criteria>
  <done>`.scp` save/load round-trips the 4-layer state; legacy `active_layer` files load correctly with the documented one-shot shim per CONTEXT.md Claude's-Discretion.</done>
</task>

</tasks>

<verification>
- `cd gui && npx vitest run src/store/__tests__/useStore.test.ts src/store/__tests__/useStore.codePanel.test.ts src/lib/__tests__/projectIO.snapToGrid.test.ts src/store/__tests__/saveAndOpenErrors.test.ts` → all four test files pass.
- `grep -n -E '\b(setActiveLayer|cycleLayer|LayerView)\b' gui/src/store/useStore.ts gui/src/lib/projectIO.ts | grep -v '^\s*//'` → zero matches (old API physically removed).
- Note: `cd gui && npx tsc --noEmit` is still expected to fail because Wave 3 plans (03, 04, 05) own the CanvasPanel / StreamNode / ToolboxPanel / ViewMenu / SecondaryToolbar / App / BottomPanel updates. Wave 2 unblocks Wave 3 by providing the contract; full tsc clean happens after Plan 03 + Plan 05.
</verification>

<success_criteria>
- Store and `.scp` schema speak the 4-layer language; legacy files are read with a one-shot translation.
- Wave 3 plans can `import { useStore } from "../store/useStore"` and read `activeLayers`/`hideOffLayer` + call `toggleLayer`/`setLayerVisible`/`setAllLayersVisible`/`setHideOffLayer`.
</success_criteria>

<output>
Create `.planning/phases/68-layers-system-overhaul/68-02-SUMMARY.md` per `templates/summary.md` when done. Document the exact new fields on `SerializeProjectArgs`/`DeserializeProjectResult`, the new store action signatures, and the legacy shim mapping so Plan 03/04/05 executors do not need to re-derive.
</output>
