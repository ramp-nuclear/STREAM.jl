---
phase: 68-layers-system-overhaul
plan: 05
type: execute
wave: 3
depends_on:
  - 68-02
files_modified:
  - gui/src/components/SecondaryToolbar.tsx
  - gui/src/App.tsx
  - gui/src/components/FileMenu.tsx
  - gui/src/components/ViewMenu.tsx
  - gui/src/components/BottomPanel.tsx
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
  - D-10
  - D-12
  - D-13
must_haves:
  truths:
    - "gui/src/components/SecondaryToolbar.tsx file is deleted from the repo (D-08)"
    - "App.tsx no longer imports or renders SecondaryToolbar (D-08)"
    - "File menu contains an 'Export to Julia…' item that calls exportCode (D-09)"
    - "View menu contains a 'Toggle Code Preview' item with backtick keyboard shortcut hint that calls toggleBottomPanel (D-10)"
    - "App.tsx registers a Ctrl+backtick keyboard shortcut that calls toggleBottomPanel"
    - "BottomPanel header has a collapse button (ChevronDown, ghost variant) with aria-label and tooltip showing the keyboard shortcut"
    - "When bottomPanelOpen is false, BottomPanel renders a 20px stub strip showing 'Code' + ChevronUp; clicking the strip calls toggleBottomPanel"
    - "BottomPanel Export-to-Julia button is preserved (D-12 — two entry points by design)"
    - "View menu no longer has a Layer submenu (D-07 — already removed by Phase 67 UAT, verify still absent)"
  artifacts:
    - path: "gui/src/components/FileMenu.tsx"
      provides: "File menu with new 'Export to Julia…' item"
    - path: "gui/src/components/ViewMenu.tsx"
      provides: "View menu with Toggle Code Preview + Theme submenu only"
    - path: "gui/src/components/BottomPanel.tsx"
      provides: "Header collapse button + closed-state stub strip"
    - path: "gui/src/App.tsx"
      provides: "Ctrl+backtick shortcut registered; SecondaryToolbar import + render removed"
  key_links:
    - from: "gui/src/components/FileMenu.tsx"
      to: "gui/src/lib/exportCode.ts"
      via: "exportCode invocation"
      pattern: "exportCode"
    - from: "gui/src/App.tsx"
      to: "gui/src/store/useStore.ts toggleBottomPanel"
      via: "useStore.getState().toggleBottomPanel()"
      pattern: "toggleBottomPanel"
    - from: "gui/src/components/BottomPanel.tsx"
      to: "gui/src/store/useStore.ts toggleBottomPanel"
      via: "collapse button + stub onClick"
      pattern: "toggleBottomPanel"
---

<objective>
Execute the SecondaryToolbar-removal + control-migration half of Phase 68. Delete `SecondaryToolbar.tsx` entirely; relocate its Export button to the File menu (D-09); add Toggle Code Preview back to the View menu with a new Ctrl+backtick keyboard shortcut hub registration (D-10/D-13); redesign the BottomPanel with a header collapse button and persistent closed-state stub strip (D-10).

Purpose: SecondaryToolbar's Layer ToggleGroup is being killed by Plan 04's LayersChip, but the strip also held Export and Code Preview controls. This plan absorbs those two controls into their new homes so the entire SecondaryToolbar can vanish in a single coordinated cut. Addresses D-07 (View menu no Layer submenu), D-08 (SecondaryToolbar deleted), D-09 (Export → File menu), D-10 (Code Preview toggle → bottom panel header + stub + View menu entry), D-12 (BottomPanel Export button preserved), D-13 (Ctrl+backtick registered in App.tsx).

Output: Deleted SecondaryToolbar.tsx; updated App.tsx / FileMenu.tsx / ViewMenu.tsx / BottomPanel.tsx that compile clean and deliver the contracted UX. Plan 04 (LayersChip) and this plan (Plan 05) are wave-3 siblings — they touch disjoint files and can run in parallel.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/68-layers-system-overhaul/68-CONTEXT.md
@.planning/phases/68-layers-system-overhaul/68-RESEARCH.md
@.planning/phases/68-layers-system-overhaul/68-UI-SPEC.md
@.planning/phases/68-layers-system-overhaul/68-02-SUMMARY.md

<interfaces>
From Plan 02 (already shipped at wave 2):
- useStore selectors: bottomPanelOpen: boolean, toggleBottomPanel(): void (existing; verify in useStore.ts)

Existing surface:
- gui/src/lib/exportCode.ts — exportCode util (unchanged in Phase 68)
- gui/src/App.tsx — line 10 imports SecondaryToolbar; line 397 renders SecondaryToolbar; keyboard hub uses handleKeyDown useEffect with Ctrl+S, Ctrl+N, Ctrl+O patterns (per RESEARCH.md)
- gui/src/components/SecondaryToolbar.tsx — full file is the deletion target; its handleExport calls exportCode with the same args BottomPanel.tsx already uses
- gui/src/components/FileMenu.tsx — Menubar-based; existing items include Save As followed by a MenubarSeparator (per RESEARCH.md insertion target)
- gui/src/components/ViewMenu.tsx — after Phase 67 UAT round 2, currently contains ONLY the Theme submenu (per RESEARCH.md "View Menu State"). Toggle Code Preview was removed; this plan adds it back per D-10.
- gui/src/components/BottomPanel.tsx — line 72 has `if (!bottomPanelOpen) return null;` (RESEARCH.md Pattern 5 — to be replaced). Tabs row at ~line 82 inside a `mx-2 mt-1 flex items-center` div — collapse button goes here. Existing Export button at ~line 109 (D-12: keep it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete SecondaryToolbar; relocate Export to File menu; restore View menu Toggle Code Preview</name>
  <files>gui/src/components/SecondaryToolbar.tsx, gui/src/App.tsx, gui/src/components/FileMenu.tsx, gui/src/components/ViewMenu.tsx</files>
  <read_first>
    - gui/src/components/SecondaryToolbar.tsx (full file — note its handleExport implementation: imports generateCode + exportCode, calls them with current store state; same args as BottomPanel.tsx handleExport)
    - gui/src/App.tsx (full file — line 10 SecondaryToolbar import, line 397 render, the handleKeyDown useEffect block that registers Ctrl+S/N/O — this is the hub where Ctrl+backtick joins)
    - gui/src/components/FileMenu.tsx (full file — Menubar Item structure, position of Save As + MenubarSeparator, current onClick handler pattern)
    - gui/src/components/ViewMenu.tsx (full file — confirm current state: only Theme submenu; Toggle Code Preview is NOT present; line counts in RESEARCH.md state lines 27-31 document the round-2 trim)
    - gui/src/lib/exportCode.ts (signature: confirm what args exportCode + generateCode take — the FileMenu invocation must replicate the args SecondaryToolbar uses today)
    - gui/src/store/useStore.ts (confirm bottomPanelOpen + toggleBottomPanel action are exported by the store today; both exist per Phase 67)
    - .planning/phases/68-layers-system-overhaul/68-UI-SPEC.md §5 (File Menu Addition), §6 (View Menu Cleanup), §7 (Titlebar After SecondaryToolbar Deletion)
    - .planning/phases/68-layers-system-overhaul/68-RESEARCH.md (Blast Radius section + View Menu State + Ctrl+backtick shortcut registration)
  </read_first>
  <action>
    1. DELETE `gui/src/components/SecondaryToolbar.tsx` using the executor filesystem tooling (e.g. `rm gui/src/components/SecondaryToolbar.tsx`), NOT via Edit. Before deletion, copy out the handleExport implementation as the reference for FileMenu's new item (step 3 below).

    2. In `gui/src/App.tsx`:
       - Remove the SecondaryToolbar import line at ~line 10.
       - Remove the SecondaryToolbar JSX render at ~line 397. The parent flex container collapses naturally — the canvas reclaims the ~32px height per UI-SPEC §7.
       - In the existing keyboard handleKeyDown useEffect (the one that registers Ctrl+S, Ctrl+N, Ctrl+O), add a new branch per D-13 that matches `e.ctrlKey && e.key === "` + backtick + `"`. Inside the branch: replicate the input-focus guard pattern used by the surrounding Ctrl shortcut branches (check `document.activeElement` tagName against INPUT/TEXTAREA and isContentEditable; if guarded, `return` without preventing default). When not guarded, call `e.preventDefault()` then `useStore.getState().toggleBottomPanel()`. Place the branch alongside the other shortcut branches; match the exact guard pattern the surrounding code uses — do not invent new guard syntax.
       - Verify no other reference to SecondaryToolbar remains in App.tsx.

    3. In `gui/src/components/FileMenu.tsx` — add the "Export to Julia…" item per D-09 / UI-SPEC §5:
       - Identify the existing Save As MenubarItem and the MenubarSeparator directly after it.
       - Insert AFTER that separator (before any later items if present, before the final separator-and-Exit if those exist) a new MenubarItem labeled `Export to Julia…` (with the ellipsis character `…`, single Unicode codepoint) wired to a `handleExportToJulia` onClick handler. The MenubarItem className should mirror the existing items (`text-xs font-normal`).
       - Add a `handleExportToJulia` callback at the top of the component function. Implementation: REPLICATE the SecondaryToolbar handleExport body exactly — read the same store fields via `useStore.getState()` (whatever the SecondaryToolbar handleExport read: typically `nodes`, `edges`, `resources`, project name) and call `generateCode` then `exportCode` with the same arguments. Import `generateCode` from its actual path and `exportCode` from `../lib/exportCode` — match the imports SecondaryToolbar used verbatim.
       - Add `useStore` import if not already present in FileMenu.tsx.
       - Do NOT add a keyboard shortcut hint (no shortcut existed for the toolbar Export button — per UI-SPEC §5 "do not invent one").
       - Verify the BottomPanel Export button (D-12) is NOT touched by this plan — it stays as is.

    4. In `gui/src/components/ViewMenu.tsx` — add Toggle Code Preview back per D-10 / UI-SPEC §6:
       - Confirm the file currently contains ONLY the Theme submenu (per RESEARCH.md). If a Layer submenu somehow still exists, remove it. If `setActiveLayer` / `activeLayer` / `cycleLayer` references exist, remove them.
       - Add a new MenubarItem labeled `Toggle Code Preview` BEFORE the Theme submenu, with `onClick={handleToggleCodePreview}`. The item must show the keyboard-shortcut hint via the standard `MenubarShortcut` sibling element. Use the pattern already used elsewhere in the codebase (look at how FileMenu's Ctrl+S item renders its `MenubarShortcut` — replicate). The visible shortcut text is `Ctrl+` followed by a literal backtick character.
       - `handleToggleCodePreview` callback: a function that calls `useStore.getState().toggleBottomPanel()` — same store action App.tsx's Ctrl+backtick handler calls and the same one BottomPanel's collapse button will call. All three entry points target one action.
       - Resulting View menu structure (per UI-SPEC §6 final tree): top-level items in order are `Toggle Code Preview` (with Ctrl+backtick shortcut) then `Theme` submenu (containing Light / Dark / System).

    5. Verify the Plan-03 changes did not accidentally touch any of these files. If `ToolboxPanel.test.tsx` happens to import SecondaryToolbar (it should not — RESEARCH.md confirmed no tests reference it), that is a coordination bug — flag and fix.

    6. After all four edits, grep the entire `gui/src` tree for any remaining SecondaryToolbar reference: `grep -rn SecondaryToolbar gui/src` must return zero matches.
  </action>
  <verify>
    <automated>cd gui && test ! -f src/components/SecondaryToolbar.tsx && ! grep -rq SecondaryToolbar src && grep -q 'Export to Julia' src/components/FileMenu.tsx && grep -q 'Toggle Code Preview' src/components/ViewMenu.tsx && grep -q 'toggleBottomPanel' src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `gui/src/components/SecondaryToolbar.tsx` does NOT exist on disk.
    - `grep -rn SecondaryToolbar gui/src` returns zero matches.
    - `gui/src/App.tsx` registers a Ctrl+backtick handler in its handleKeyDown useEffect that calls `useStore.getState().toggleBottomPanel()` and respects the same input-focus guard as adjacent shortcuts.
    - `gui/src/components/FileMenu.tsx` contains a MenubarItem labeled `Export to Julia…` positioned after the Save As section that calls a `handleExportToJulia` function which invokes `generateCode` + `exportCode` with the same args SecondaryToolbar's handleExport used.
    - `gui/src/components/ViewMenu.tsx` contains a `Toggle Code Preview` MenubarItem with a `MenubarShortcut` showing `Ctrl+` followed by a backtick, placed before the Theme submenu.
    - `gui/src/components/ViewMenu.tsx` does NOT contain any Layer radio submenu (D-07 — verify via grep for `setActiveLayer`, `cycleLayer`, `LayerView`, or any of the strings `"Hydraulic"`, `"Both"`, `"Thermal"` returns zero non-comment matches).
    - tsc reports zero errors specifically in the four touched files.
  </acceptance_criteria>
  <done>SecondaryToolbar is gone, its Export and Code-Preview-toggle responsibilities are rehoused, and Ctrl+backtick is wired into the App.tsx keyboard hub.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign BottomPanel with header collapse button and closed-state stub strip (D-10)</name>
  <files>gui/src/components/BottomPanel.tsx</files>
  <read_first>
    - gui/src/components/BottomPanel.tsx (full file — early return `if (!bottomPanelOpen) return null;` at ~line 72; TabsList row at ~line 82 inside a `mx-2 mt-1 flex items-center` div; existing Export button at ~line 109 inside the right-aligned `ml-auto` group)
    - gui/src/store/useStore.ts (confirm bottomPanelOpen + toggleBottomPanel exports)
    - gui/src/components/ui/button.tsx (shadcn Button: variants include "ghost", sizes include "sm")
    - gui/src/components/ui/tooltip.tsx (existing Tooltip component for the hover hint)
    - .planning/phases/68-layers-system-overhaul/68-UI-SPEC.md §4 ("Bottom-Panel Header Redesign") — exact strip className, sizes, and copy
    - .planning/phases/68-layers-system-overhaul/68-RESEARCH.md (Pattern 5 — exact closed-state stub snippet)
    - .planning/phases/68-layers-system-overhaul/68-CONTEXT.md (D-10 + D-12 — keep existing Export button)
  </read_first>
  <action>
    In `gui/src/components/BottomPanel.tsx`:

    1. Closed-state stub strip — REPLACE the early return `if (!bottomPanelOpen) return null;` at ~line 72 with a conditional that returns a clickable stub element per UI-SPEC §4 + RESEARCH.md Pattern 5. The stub is a single `div` with these exact properties:
       - className: `h-5 border-t flex items-center justify-center gap-1 cursor-pointer bg-chrome hover:bg-accent transition-colors select-none`
       - `onClick={toggleBottomPanel}`
       - `role="button"`
       - `aria-label="Expand code panel"`
       - Children: a `span` with className `text-[12px] font-normal text-muted-foreground` containing the literal text `Code`, followed by a `ChevronUp` lucide icon with className `w-3 h-3 text-muted-foreground`.
       - Import `ChevronUp` from `lucide-react` at the top (add `ChevronDown` too if not already present — needed in step 2).
       - The store selectors for `toggleBottomPanel` and `bottomPanelOpen` must already be in scope at the top of the component. Use `useStore(s => s.toggleBottomPanel)` for the action and `useStore(s => s.bottomPanelOpen)` for the read — match the existing selector style in this file.
       - Height is exactly `h-5` (20px) — DO NOT round to a different value. The 20px is locked by UI-SPEC §4.

    2. Open-state collapse button — ADD inside the existing TabsList row at ~line 82 (the `mx-2 mt-1 flex items-center` div):
       - Locate the `ml-auto` right-aligned group (where Copy / Export buttons currently sit at ~line 109).
       - Insert the collapse button BEFORE that group (so it sits between the TabsList and the Copy/Export cluster). Wrap in a `Tooltip`. The Tooltip structure: `Tooltip` outer, `TooltipTrigger asChild` wrapping the Button, `TooltipContent` containing the label `Collapse (Ctrl+` + backtick + `)`.
       - The Button: shadcn `Button` with `variant="ghost"`, `size="sm"`, `onClick={toggleBottomPanel}`, `aria-label="Collapse code panel"`, `className="h-7 w-7 p-0"`, containing a `ChevronDown` icon with `className="h-4 w-4"`.
       - Import `Tooltip`, `TooltipTrigger`, `TooltipContent` from the existing `./ui/tooltip` path (check imports — if already imported for the Copy button tooltip, reuse).
       - Position the collapse button on the LEFT of the `ml-auto` cluster — visually the row reads: Tabs on left, then Collapse, then Copy, then Export. UI-SPEC §4 says "before the Copy/Export buttons area"; place it accordingly.

    3. Preserve the existing BottomPanel Export button per D-12 — do NOT remove it. The FileMenu `Export to Julia…` (Plan 05 Task 1) coexists with the BottomPanel Export button by design (two entry points, same `exportCode` util).

    4. Verify the Tooltip provider is in scope. The shadcn Tooltip requires a `TooltipProvider` ancestor; this is typically mounted once in App.tsx for the whole app. If the existing Copy button tooltip works, the provider is already in place — no change needed. Otherwise, do NOT add a provider here (that is App.tsx scope) — file an issue in the SUMMARY for follow-up.

    5. Closed-state click target: the entire 20px strip is the click target (the outer div onClick covers full width). The `cursor-pointer` + `role="button"` makes the affordance obvious.

    6. Do NOT add any opacity transitions to the closed/open switch — switching is instantaneous per UI-SPEC §4 "Hidden mode does NOT animate" and Interaction Contract "switching between 0.2 opacity and display:none does not animate".

    7. Grep this file for any stale references to SecondaryToolbar or to the deleted store selectors (`activeLayer`, `setActiveLayer`, `cycleLayer`). None expected; verify.
  </action>
  <verify>
    <automated>cd gui && npx tsc --noEmit -p . 2>&1 | grep -E 'BottomPanel\.tsx' ; cd gui && grep -q 'aria-label="Expand code panel"' src/components/BottomPanel.tsx && grep -q 'aria-label="Collapse code panel"' src/components/BottomPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - When `bottomPanelOpen === false`, `BottomPanel.tsx` renders a 20px-tall (`h-5`) clickable stub strip with the literal text `Code`, a `ChevronUp` icon, and `aria-label="Expand code panel"`; clicking anywhere on the strip calls `toggleBottomPanel`.
    - When `bottomPanelOpen === true`, the existing tabs row contains a ghost-variant Button with `ChevronDown` icon, `aria-label="Collapse code panel"`, wrapped in a Tooltip whose content reads `Collapse (Ctrl+` + backtick + `)`. Clicking the button calls `toggleBottomPanel`.
    - The pre-existing BottomPanel Export button is unchanged and still present (D-12).
    - tsc reports zero errors specifically inside `BottomPanel.tsx`.
    - No references to `SecondaryToolbar`, `activeLayer` (singular), `setActiveLayer`, or `cycleLayer` remain in `BottomPanel.tsx`.
  </acceptance_criteria>
  <done>Bottom panel has a header collapse button (with tooltip + shortcut hint) and a persistent 20px stub strip when closed; D-10 / D-12 / D-13 are all wired.</done>
</task>

</tasks>

<verification>
- `cd gui && test ! -f src/components/SecondaryToolbar.tsx` → file is gone.
- `cd gui && ! grep -rq SecondaryToolbar src` → no remaining references.
- `cd gui && npx tsc --noEmit -p . 2>&1 | grep -E '(App|FileMenu|ViewMenu|BottomPanel)\.tsx' | grep -v '^Found '` → zero matches (these four files compile clean). Errors in CanvasPanel/StreamNode/ToolboxPanel are owned by Plan 03; LayersChip/CanvasPanel mount errors are owned by Plan 04.
- Manual smoke (executor performs visually after running the dev build, optional): launch `cd gui && npm run tauri dev`; verify File menu shows `Export to Julia…`; verify View menu shows `Toggle Code Preview` + Theme; verify Ctrl+backtick toggles the bottom panel; verify clicking the 20px stub re-opens the panel; verify the panel header collapse button + tooltip work.
</verification>

<success_criteria>
- SecondaryToolbar is physically deleted; no stale references remain anywhere in `gui/src`.
- Export-to-Julia exists in the File menu (D-09); the BottomPanel Export button is preserved (D-12).
- Toggle Code Preview is in the View menu with Ctrl+backtick hint (D-10).
- Ctrl+backtick is registered in the App.tsx keyboard hub (D-13).
- BottomPanel has a header collapse button and a 20px closed-state stub strip; all three toggle paths (View menu / Ctrl+backtick / button / stub click) call the same `toggleBottomPanel` store action.
- View menu does NOT contain a Layer submenu (D-07 confirmed).
</success_criteria>

<output>
Create `.planning/phases/68-layers-system-overhaul/68-05-SUMMARY.md` per `templates/summary.md` when done. Document the exact handleExportToJulia args used (so Plan 04 executor and any future verifier can match the BottomPanel Export behavior). Flag any TooltipProvider concern as a follow-up if the dev build shows missing provider warnings.
</output>
