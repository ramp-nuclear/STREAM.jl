---
phase: 61-registry-audit-rewrite-for-v1-1
plan: 03
type: execute
wave: 3
depends_on: ["61-01", "61-02"]
files_modified:
  - gui/src/registry/components.json
  - gui/src/registry/icons.ts
autonomous: true
requirements: []
tags: [gui, registry, sources, point-kinetics, resources]
must_haves:
  truths:
    - "components.json has new top-level component entries: WallTemperature, HeatFluxSource, PointKinetics, ReactivityController"
    - "WallTemperature and HeatFluxSource each expose a single output port of type BCPort, marked array_size=n, default_axis=horizontal"
    - "PointKinetics has a polymorphic `rho` with type_union [Real, Function, ReactivityController] + input_modes [scalar, callable, controller]; visible_when wiring for temp_worth/ref_temp"
    - "ReactivityController is encoded as resource_kind=reactivity_controller and category=Resources, with no ports and constructorModes optional"
    - "icons.ts registers Lucide icons for all four new component IDs"
  artifacts:
    - path: "gui/src/registry/components.json"
      provides: "Four new component entries appended to .components[]"
      contains: '"WallTemperature"'
    - path: "gui/src/registry/icons.ts"
      provides: "Icon mappings for the four new components"
      contains: "WallTemperature"
  key_links:
    - from: "Channel.external_inputs[].source_component / ChannelHeatFlux.external_inputs[].source_component"
      to: "WallTemperature / HeatFluxSource component entries"
      via: "FK string match into .components[].id"
      pattern: '"id": "WallTemperature"'
---

<objective>
Add the four v1.1 components missing from the registry: `WallTemperature`, `HeatFluxSource`, `PointKinetics` (Reactor Physics), and `ReactivityController` (Resource, not draggable). This resolves the dangling FKs created by Plan 02's `Channel.external_inputs[].source_component = "WallTemperature"` and `ChannelHeatFlux.external_inputs[].source_component = "HeatFluxSource"`. Also extend `gui/src/registry/icons.ts` so the toolbox and canvas can render icons for the new entries.

Purpose: Without these four entries the registry is internally inconsistent (FKs dangle) and the toolbox cannot render Sources/Reactor Physics components.

Output: Four new entries in `components.json`; four new icon mappings in `icons.ts`.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-registry-audit-rewrite-for-v1-1/61-CONTEXT.md
@.planning/phases/61-registry-audit-rewrite-for-v1-1/61-01-SUMMARY.md
@.planning/phases/61-registry-audit-rewrite-for-v1-1/61-02-SUMMARY.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WallTemperature and HeatFluxSource entries</name>
  <files>gui/src/registry/components.json</files>
  <read_first>
    - gui/src/registry/components.json (the rewritten Channel and ChannelHeatFlux entries from Plan 02; their `external_inputs[].source_component`/`source_port` must align byte-for-byte with the new entries' `id` and output port `name`)
    - src/components/sources.jl (lines 1-110 — full file; authoritative `WallTemperature(; name, n::Int, T_wall::Union{Real, AbstractVector{<:Real}, Function})` and `HeatFluxSource(; name, n::Int, q::Union{Real, AbstractVector{<:Real}, Function})` signatures; the `T_wall_out[1:n]` / `q_out[1:n]` output @variables are at lines 34 / 86)
    - .planning/phases/61-registry-audit-rewrite-for-v1-1/61-CONTEXT.md sections D-10/D-14/D-16 and the <specifics> WallTemperature sketch
  </read_first>
  <action>
    Append two new entries to the `.components` array in `gui/src/registry/components.json`:

    Entry A — `WallTemperature`:
    - `id: "WallTemperature"`, `label: "Wall Temperature"`, `category: "Sources"`, `description` ("Value-source block. Drives external T_wall_left/T_wall_right BCs on a Channel via a dashed BCPort edge. Per-cell wall temperature; n must match the consumer Channel's n.").
    - `parameters`:
      1. `n` — Int, required, positional false, description "Number of cells; must match consumer Channel.n".
      2. `T_wall` — uses D-10 polymorphic shape: `type_union: ["Real","Vector","Function"]`, `input_modes: ["scalar","vector","callable"]`, unit "K", required true, positional false, description "Per-cell wall-temperature value, vector, or callable T_wall(t)".
    - `external_inputs`: OMIT (value-source has no inputs of its own — it IS the source).
    - `ports`: a SINGLE entry per D-16: `{name: "T_wall_out", type: "BCPort", array_size: "n", default_axis: "horizontal", side: "right"}`. Note: BCPort is a GUI-only port-type tag per D-14; `side` stays as a static hint for the single output handle.
    - `constructorModes`: single mode `{mode: "default", signature: "WallTemperature(; n, T_wall)", parameters: ["n","T_wall"]}`.

    Entry B — `HeatFluxSource`:
    - `id: "HeatFluxSource"`, `label: "Heat Flux Source"`, `category: "Sources"`, `description` (mirror WallTemperature's but for flux: "Value-source block. Drives external q_left/q_right BCs on a ChannelHeatFlux via a dashed BCPort edge. Per-cell heat flux.").
    - `parameters`:
      1. `n` — Int, required, positional false.
      2. `q` — `type_union: ["Real","Vector","Function"]`, `input_modes: ["scalar","vector","callable"]`, unit "W/m^2", required true, positional false.
    - `external_inputs`: OMIT.
    - `ports`: single entry `{name: "q_out", type: "BCPort", array_size: "n", default_axis: "horizontal", side: "right"}`.
    - `constructorModes`: `{mode: "default", signature: "HeatFluxSource(; n, q)", parameters: ["n","q"]}`.

    Cross-validation (must hold after this task): the FK strings written by Plan 02 (`"source_component": "WallTemperature"`, `"source_port": "T_wall_out"`, and the HeatFluxSource pair) must resolve to entries that now exist in `.components`.
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui &amp;&amp; jq -e '(.components | map(.id) | contains(["WallTemperature","HeatFluxSource"])) and ((.components[] | select(.id=="WallTemperature").parameters | map(.name)) == ["n","T_wall"]) and ((.components[] | select(.id=="WallTemperature").ports[0]) | (.name=="T_wall_out" and .type=="BCPort" and .array_size=="n" and .default_axis=="horizontal")) and ((.components[] | select(.id=="WallTemperature").parameters[] | select(.name=="T_wall").type_union) == ["Real","Vector","Function"]) and ((.components[] | select(.id=="HeatFluxSource").ports[0]) | (.name=="q_out" and .type=="BCPort"))' src/registry/components.json &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.components | map(.id) | index("WallTemperature")'` returns non-null.
    - `jq '.components | map(.id) | index("HeatFluxSource")'` returns non-null.
    - `jq '.components[] | select(.id=="WallTemperature") | .category'` returns `"Sources"`.
    - `jq '.components[] | select(.id=="WallTemperature") | .ports[0].type'` returns `"BCPort"`.
    - `jq '.components[] | select(.id=="WallTemperature") | .parameters[] | select(.name=="T_wall") | .input_modes'` returns `["scalar","vector","callable"]`.
    - `jq '.components[] | select(.id=="HeatFluxSource") | .ports[0].name'` returns `"q_out"`.
    - FK resolution: every `Channel`/`ChannelHeatFlux` `external_inputs[].source_component` exists in `.components[].id`. Use jq: `jq '[.components[] | select(.id=="Channel" or .id=="ChannelHeatFlux") | .external_inputs[].source_component] - (.components | map(.id)) | length'` returns `0`.
    - `jq '.components | length'` returns `14` (12 original + 2 new).
    - `npm --prefix gui run build` exits 0.
  </acceptance_criteria>
  <done>
    WallTemperature and HeatFluxSource registered with polymorphic value kwarg and single BCPort output; Plan 02's FKs resolve cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add PointKinetics and ReactivityController entries</name>
  <files>gui/src/registry/components.json</files>
  <read_first>
    - src/components/point_kinetics.jl (lines 78-220: the two PointKinetics constructors; lines 370-422: ReactivityController struct + constructor + worth/change_state methods)
    - .planning/phases/61-registry-audit-rewrite-for-v1-1/61-CONTEXT.md sections D-12/D-13 and the <specifics> PointKinetics + ReactivityController sketch
    - gui/src/registry/components.json (the just-added WallTemperature/HeatFluxSource entries from Task 1 — to mirror their formatting and indentation)
  </read_first>
  <action>
    Append two more entries to the `.components` array:

    Entry C — `PointKinetics`:
    - `id: "PointKinetics"`, `label: "Point Kinetics"`, `category: "Reactor Physics"`, `description` ("Lumped-parameter reactor power dynamics. Accepts scalar rho, callable rho(t), or a ReactivityController Resource reference.").
    - `parameters` (order matches the union of the two Julia constructor signatures at `point_kinetics.jl:78` and `:214`):
      1. `rho` — `type_union: ["Real","Function","ReactivityController"]`, `input_modes: ["scalar","callable","controller"]`, default 0.0, required false, positional false, description "Reactivity input (Δρ); scalar value, time-callable, or Resource reference".
      2. `Lambda` — Real, default "U235_LAMBDA", required false, positional false, unit "s", description "Prompt neutron generation time (default: U-235)".
      3. `beta_k` — `type: "Vector"`, default "U235_BETA_K", required false, positional false, description "Delayed neutron precursor fractions (default: U-235 6-group)".
      4. `lambda_k` — `type: "Vector"`, default "U235_LAMBDA_K", required false, positional false, unit "1/s", description "Precursor decay constants (default: U-235)".
      5. `temp_worth` — `type_union: ["Mark"]`, required false, positional false, visible_when: "rho.input_mode in ['callable','controller']", description "Mark-in-code: temperature feedback dict (per-component reactivity coefficients). Per CONTEXT D-12 — Dict keys are MTK Systems and cannot be selected in JSON; user fills the Dict literal in the generated .jl file.".
      6. `ref_temp` — `type_union: ["Mark"]`, required false, positional false, visible_when: "rho.input_mode in ['callable','controller']", description "Mark-in-code: reference-temperature dict.".
    - `external_inputs`: OMIT.
    - `ports`: empty array `[]` (PK has no MTK ports for canvas wiring; it is connected via `connect_temperature_feedback` in the generated code, which is a codegen-side concern).
    - `constructorModes`: TWO modes per D-12 — `{mode: "scalar", signature: "PointKinetics(; rho, Lambda, beta_k, lambda_k)", parameters: ["rho","Lambda","beta_k","lambda_k"]}` and `{mode: "callable", signature: "PointKinetics(rho_c_fn; rho_val, Lambda, beta_k, lambda_k, temp_worth, ref_temp)", parameters: ["rho","Lambda","beta_k","lambda_k","temp_worth","ref_temp"]}`. The GUI mode picker (D-12) chooses which constructor based on `rho.input_mode`.

    Entry D — `ReactivityController` (Resource, not draggable):
    - `id: "ReactivityController"`, `label: "Reactivity Controller"`, `category: "Resources"`, `resource_kind: "reactivity_controller"`, `description` ("Resource (not a canvas component). Encapsulates input-reactivity function + state machine + abort states. Lives in the navigator under Resources → Reactivity Controllers (navigator tree is Phase 62). Referenced from PointKinetics.rho when input_mode == 'controller'.").
    - `parameters` (per D-13 + `point_kinetics.jl:406-422` constructor signature):
      1. `input_reactivity` — `type_union: ["Mark"]`, required true, positional false, description "Mark-in-code: callable (state, t_state, t) → Δρ; user fills the closure in the generated .jl.".
      2. `state_machine` — `type_union: ["Mark"]`, required true, positional false, description "Mark-in-code: callable (state, t) → new_state.".
      3. `abort_states` — `type_union: ["Mark"]`, required false, positional false, description "Mark-in-code: Set{Symbol} of states that signal abort.".
      4. `initial_state` — `type: "Symbol"`, default `:NORMAL`, required false, positional false, description "Initial state symbol (free-text input; codegen wraps with `:` prefix).".
      5. `initial_time` — `type: "Real"`, default 0.0, required false, positional false, unit "s", description "Initial time for the state machine.".
    - `external_inputs`: OMIT.
    - `ports`: OMIT entirely (the field is optional under the relaxed schema from Plan 01). Resources have no canvas presence.
    - `constructorModes`: OMIT (the field is optional under the relaxed schema; Resources don't have constructor-mode selection in the property panel — D-13 lists the kwargs as plain Properties-tab fields).
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui &amp;&amp; jq -e '(.components | map(.id) | contains(["PointKinetics","ReactivityController"])) and ((.components[] | select(.id=="PointKinetics").parameters[] | select(.name=="rho").type_union) == ["Real","Function","ReactivityController"]) and ((.components[] | select(.id=="PointKinetics").parameters[] | select(.name=="rho").input_modes) == ["scalar","callable","controller"]) and ((.components[] | select(.id=="PointKinetics").constructorModes | length) == 2) and ((.components[] | select(.id=="PointKinetics").parameters[] | select(.name=="temp_worth").visible_when) | type == "string") and ((.components[] | select(.id=="ReactivityController").category) == "Resources") and ((.components[] | select(.id=="ReactivityController").resource_kind) == "reactivity_controller") and ((.components[] | select(.id=="ReactivityController").ports) == null or (.components[] | select(.id=="ReactivityController").ports) == [])' src/registry/components.json &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.components | length'` returns `16` (14 after Task 1 + 2 here).
    - `jq '.components[] | select(.id=="PointKinetics") | .category'` returns `"Reactor Physics"`.
    - `jq '.components[] | select(.id=="PointKinetics") | .parameters[] | select(.name=="rho") | .type_union'` returns `["Real","Function","ReactivityController"]`.
    - `jq '.components[] | select(.id=="PointKinetics") | .constructorModes | length'` returns `2`.
    - `jq '.components[] | select(.id=="PointKinetics") | .parameters[] | select(.name=="temp_worth") | .visible_when'` is a non-empty string.
    - `jq '.components[] | select(.id=="ReactivityController") | .category'` returns `"Resources"`.
    - `jq '.components[] | select(.id=="ReactivityController") | .resource_kind'` returns `"reactivity_controller"`.
    - `jq '.components[] | select(.id=="ReactivityController") | .parameters | map(.name)'` returns `["input_reactivity","state_machine","abort_states","initial_state","initial_time"]`.
    - `jq '.components[] | select(.id=="ReactivityController") | .parameters[] | select(.name=="initial_state") | .default'` returns `":NORMAL"`.
    - `npm --prefix gui run build` exits 0.
  </acceptance_criteria>
  <done>
    PointKinetics carries the D-12 polymorphic `rho` + two-mode constructor switching; ReactivityController is a category-Resources entry with `resource_kind` and no ports / no constructorModes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register icons for the four new components</name>
  <files>gui/src/registry/icons.ts</files>
  <read_first>
    - gui/src/registry/icons.ts (full file — 60 lines so far; the `COMPONENT_ICONS` map at lines 28-41 and `CATEGORY_BORDER_CLASSES` map at line ~60)
    - gui/src/registry/components.json (just-added entries from Tasks 1-2 — confirm exact `id` strings)
  </read_first>
  <action>
    Update `gui/src/registry/icons.ts`:

    1. Extend the `lucide-react` import list (around line 7-21) to include four additional icons. Choose semantically reasonable existing Lucide icons:
       - `WallTemperature` → `Thermometer` (already imported — reuse) OR a distinct icon if collision is undesirable. To keep `Thermometer` for `HeatExchanger`, use `ThermometerSnowflake` or `Activity` for WallTemperature; pick `Thermometer` again is acceptable since icons are not unique. Recommended: import `Waves` for value-sources to distinguish from MTK components.
       - `HeatFluxSource` → `Zap` (flux as "energy flow").
       - `PointKinetics` → `Atom` or `Radiation`.
       - `ReactivityController` → `SlidersHorizontal` or `Settings2` (controller knob iconography).
       Use any reasonable choice from the Lucide set; the visual polish phase (Phase 8/68) will revisit. Executor chooses; document in the SUMMARY.
    2. Add four entries to the `COMPONENT_ICONS` map: `WallTemperature: <icon>`, `HeatFluxSource: <icon>`, `PointKinetics: <icon>`, `ReactivityController: <icon>`.
    3. Read the `CATEGORY_BORDER_CLASSES` map and add Tailwind border classes for the new categories `"Sources"`, `"Resources"`, `"Reactor Physics"`. Use the existing pattern (full literal strings; never dynamically constructed). Example: `"Sources": "border-l-amber-500"`, `"Resources": "border-l-slate-500"`, `"Reactor Physics": "border-l-purple-500"`. Executor picks Tailwind utility colors that are distinct from existing categories and are already valid Tailwind classes in this project's content paths (Plan 04 / Plan 05 do not re-audit — the executor confirms with `grep -r "border-l-amber-500" gui/` or by reading the Tailwind config to verify the color is in the palette).

    Do NOT change behavior of `getComponentIcon` or `FALLBACK_ICON` — additive only.
  </action>
  <verify>
    <automated>cd /home/itay/projects/Julia-STREAM/gui &amp;&amp; grep -c "WallTemperature:" src/registry/icons.ts &amp;&amp; grep -c "HeatFluxSource:" src/registry/icons.ts &amp;&amp; grep -c "PointKinetics:" src/registry/icons.ts &amp;&amp; grep -c "ReactivityController:" src/registry/icons.ts &amp;&amp; grep -c '"Sources"' src/registry/icons.ts &amp;&amp; grep -c '"Resources"' src/registry/icons.ts &amp;&amp; grep -c '"Reactor Physics"' src/registry/icons.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WallTemperature:" gui/src/registry/icons.ts` ≥ 1.
    - `grep -c "HeatFluxSource:" gui/src/registry/icons.ts` ≥ 1.
    - `grep -c "PointKinetics:" gui/src/registry/icons.ts` ≥ 1.
    - `grep -c "ReactivityController:" gui/src/registry/icons.ts` ≥ 1.
    - `grep -c '"Sources"' gui/src/registry/icons.ts` ≥ 1; same for `"Resources"` and `"Reactor Physics"`.
    - `npm --prefix gui run build` exits 0 (this catches missing Lucide icon imports — tsc fails if an icon name doesn't exist in `lucide-react`).
    - `npm --prefix gui test -- src/registry/__tests__/icons.test.ts` exits 0 if existing icon tests are still satisfied (or shows expected failures the executor should note — Plan 05 updates those tests).
  </acceptance_criteria>
  <done>
    Four new component icons registered; three new category border classes registered; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry FK resolution | `Channel.external_inputs[].source_component → WallTemperature.id` etc. — string lookup at runtime; resolved by this plan. |
| Lucide icon names | `icons.ts` references icon names that must exist in `lucide-react`'s exports. Typos cause tsc failure (caught by build verify). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07 | Tampering | new component entries | mitigate | jq verifies exact shape; FK resolution verified by jq set-subtraction. |
| T-61-08 | Information Disclosure | Mark-in-code params | accept | Mark-fields emit `# TODO:` comments in generated Julia; no execution surface. |
| T-61-09 | Denial of Service | Tailwind class purge | mitigate | Per `icons.ts` header comment, classes must be full literal strings — executor reads existing CATEGORY_BORDER_CLASSES pattern and mirrors it. |
</threat_model>

<verification>
- `jq '.components | length' gui/src/registry/components.json` returns `16`.
- All four new IDs present.
- No dangling FK from Plan 02: `jq '[.components[] | select(.id=="Channel" or .id=="ChannelHeatFlux") | .external_inputs[].source_component] - (.components | map(.id)) | length'` returns `0`.
- `npm --prefix gui run build` exits 0 at the end of every task.
</verification>

<success_criteria>
- 4 new entries in components.json matching the source-of-truth Julia signatures.
- 4 new icons + 3 new category borders in icons.ts.
- Build clean.
- Registry internally consistent — no FK dangles.
</success_criteria>

<output>
After completion, create `.planning/phases/61-registry-audit-rewrite-for-v1-1/61-03-SUMMARY.md` listing: the four added entries (label, category, key kwargs), icon choices made for each (and rationale), and any source-vs-registry discrepancies found during the audit of `src/components/sources.jl` / `src/components/point_kinetics.jl`.
</output>
