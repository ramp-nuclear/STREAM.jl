---
phase: 71-validation-framework
plan: 08
type: execute
wave: 6
depends_on: ["71-01", "71-07"]
files_modified:
  - gui/src/lib/validation/rules/pressureBoundaryRequired.ts
  - gui/src/lib/validation/rules/drivingElementRequired.ts
  - gui/src/lib/validation/rules/__tests__/pressureBoundaryRequired.test.ts
  - gui/src/lib/validation/rules/__tests__/drivingElementRequired.test.ts
  - gui/src/lib/validation/index.ts
autonomous: true
requirements: [D-07, D-08, D-15, D-16]
tags: [validation, rules, system, pressure-boundary, driving-element, vald-02, vald-03]

must_haves:
  truths:
    - "pressureBoundaryRequired rule fires when snapshot.anchors is empty (VALD-02 fold per D-16)."
    - "drivingElementRequired rule fires when no Pump or Gravity component exists in snapshot.nodes (VALD-03 fold per D-16)."
    - "Both rules are system-level: targets:[] (empty — no specific node)."
    - "Both rules are pure functions and registered in the explicit array."
    - "After this plan, validateTopology() in gui/src/lib/validation.ts is fully covered by the new registry (VALD-01 via Plan 04 danglingFlowPort + VALD-02 via this plan + VALD-03 via this plan). Plan 13 can delete validation.ts."
  artifacts:
    - path: "gui/src/lib/validation/rules/pressureBoundaryRequired.ts"
      provides: "pressureBoundaryRequired: Validator (VALD-02 lift)"
    - path: "gui/src/lib/validation/rules/drivingElementRequired.ts"
      provides: "drivingElementRequired: Validator (VALD-03 lift)"
  key_links:
    - from: "gui/src/lib/validation/index.ts"
      to: "gui/src/lib/validation/rules/pressureBoundaryRequired.ts"
      via: "explicit registration"
      pattern: "pressureBoundaryRequired"
---

<objective>
Ship the two system-level validators per D-15 + D-16: VALD-02 (pressure boundary required) and VALD-03 (driving element required). These complete the rule set so Plan 13 can delete validation.ts cleanly.

Purpose: existing validateTopology() carries VALD-02 + VALD-03 as ad-hoc system checks. D-16 lifts them into the registry. With Plan 04's danglingFlowPort covering VALD-01, this plan finishes the legacy-fold story.

Output: two pure-function Validator modules + tests + registry append.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-validation-framework/71-CONTEXT.md
@.planning/phases/71-validation-framework/71-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pressureBoundaryRequired rule + tests (VALD-02 lift)</name>
  <files>gui/src/lib/validation/rules/pressureBoundaryRequired.ts, gui/src/lib/validation/rules/__tests__/pressureBoundaryRequired.test.ts, gui/src/lib/validation/index.ts</files>
  <read_first>
    - gui/src/lib/validation.ts:109-111 (existing VALD-02 logic — `Object.keys(anchors).length === 0` → systemError 'No pressure boundary condition')
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-16 (VALD-02 fold spec)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Code Examples → Minimal Validator Rule Shape" (this rule is the literal example shown in RESEARCH; lift it almost verbatim)
    - gui/src/lib/anchors.ts (AnchorEntry shape)
  </read_first>
  <behavior>
    - snapshot.anchors = {} → 1 ValidationResult severity 'error' validatorId 'pressure_boundary_required' targets:[] description 'No pressure boundary condition. Set a pressure anchor on a FlowPort.'
    - snapshot.anchors = { 'n1': { portField: 'port_in.P', value: 1e5 } } → no result.
  </behavior>
  <action>
    Create pressureBoundaryRequired.ts copying the shape from RESEARCH §"Code Examples → Minimal Validator Rule Shape" verbatim:
      - id: 'pressure_boundary_required'
      - severity: 'error'
      - description: 'No pressure boundary condition'
      - scope: ['anchors']
      - run: if Object.keys(snapshot.anchors).length > 0 return []; else return one ValidationResult with id 'pressure_boundary_required::system', empty targets array, and the description 'No pressure boundary condition. Set a pressure anchor on a FlowPort.'

    Test file: 2 tests — empty anchors emits one result; non-empty anchors emits nothing.

    Register in index.ts.
  </action>
  <verify>
    <automated>cd gui && npm run test -- --run src/lib/validation/rules/__tests__/pressureBoundaryRequired.test.ts</automated>
    <automated>cd gui && grep -c "pressureBoundaryRequired" src/lib/validation/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists; exports `pressureBoundaryRequired: Validator` with id 'pressure_boundary_required'.
    - Test file has ≥2 `it(` blocks; both pass.
    - Registry index.ts has 1 import + 1 push for pressureBoundaryRequired.
    - Pure function: no store / React imports.
  </acceptance_criteria>
  <done>VALD-02 lifted into the registry; tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: drivingElementRequired rule + tests (VALD-03 lift)</name>
  <files>gui/src/lib/validation/rules/drivingElementRequired.ts, gui/src/lib/validation/rules/__tests__/drivingElementRequired.test.ts, gui/src/lib/validation/index.ts</files>
  <read_first>
    - gui/src/lib/validation.ts:113-122 (existing VALD-03 logic — `nodes.some(n => cid === 'Pump' || cid === 'Gravity')` heuristic; systemError 'No driving element (add a Pump or Gravity component)')
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-16 (VALD-03 fold spec)
    - gui/src/lib/validation/rules/loopClosure.ts (Plan 07 — same Pump/Gravity heuristic; the two rules MUST agree on the definition of "driving element")
  </read_first>
  <behavior>
    - snapshot.nodes empty → 1 ValidationResult severity 'error' validatorId 'driving_element_required' targets:[] description 'No driving element (add a Pump or Gravity component)'.
    - snapshot.nodes contains a Pump → no result.
    - snapshot.nodes contains a Gravity → no result.
    - snapshot.nodes contains only a Channel and a HX (no driving element) → 1 ValidationResult.
  </behavior>
  <action>
    Create drivingElementRequired.ts following the same minimal-validator shape:
      - id: 'driving_element_required'
      - severity: 'error'
      - description: 'No driving element'
      - scope: ['nodes']
      - run: if snapshot.nodes.some(n => { const cid = (n.data as { componentId?: string }).componentId; return cid === 'Pump' || cid === 'Gravity'; }) return []; else return one ValidationResult with id 'driving_element_required::system', empty targets, description 'No driving element (add a Pump or Gravity component).'.

    Test file: 4 tests covering the cases above.

    Register in index.ts.
  </action>
  <verify>
    <automated>cd gui && npm run test -- --run src/lib/validation/rules/__tests__/drivingElementRequired.test.ts</automated>
    <automated>cd gui && grep -c "drivingElementRequired" src/lib/validation/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists; exports `drivingElementRequired: Validator` with id 'driving_element_required'.
    - Test file has ≥4 `it(` blocks; all pass.
    - Registry index.ts has 1 import + 1 push for drivingElementRequired.
    - Pure function: no store / React imports.
  </acceptance_criteria>
  <done>VALD-03 lifted into the registry; tested. validateTopology()'s full coverage is now mirrored across danglingFlowPort + pressureBoundaryRequired + drivingElementRequired — Plan 13 can delete validation.ts.</done>
</task>

</tasks>

<verification>
- `cd gui && npm run test -- --run src/lib/validation/rules` passes (all rule test files across Plans 04-08).
- `cd gui && grep -c "import.*from './rules" src/lib/validation/index.ts` returns exactly 11 (3 + 3 + 1 + 2 + 2 — covering all 11 validators per the §3.9 list + VALD-02/VALD-03 lifts).
- The full set of registered ids in index.ts: port_type, required_connections, dangling_flow_port, z_n_match, length_match, geometry_consistency, n_match, loop_closure, gravity_sum_per_loop, pressure_boundary_required, driving_element_required. Verifiable by `grep "id: '" gui/src/lib/validation/rules/*.ts | sort`.
- VALD-01/02/03 are now fully replaced: danglingFlowPort (Plan 04), pressureBoundaryRequired + drivingElementRequired (this plan).
</verification>

<success_criteria>
- VALD-02 and VALD-03 lifted into the registry per D-15 + D-16.
- Full §3.9 rule set + the two lifts is registered (11 validators total).
- Plan 13 has a clean path to delete gui/src/lib/validation.ts.
</success_criteria>

<output>
Create `.planning/phases/71-validation-framework/71-08-SUMMARY.md` when done. Include: per-rule test counts, the final registered-rule list extracted from index.ts, and confirmation that every rule's `scope` is correctly declared.
</output>
