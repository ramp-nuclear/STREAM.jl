---
phase: 71-validation-framework
plan: 13
type: execute
wave: 8
depends_on: ["71-01", "71-04", "71-06", "71-08", "71-11", "71-12"]
files_modified:
  - gui/src/components/CanvasPanel.tsx
  - gui/src/components/StreamNode.tsx
  - gui/src/store/useStore.ts
  - gui/src/lib/validation.ts
  - gui/src/lib/validation.test.ts
  - gui/src/lib/selectors/nodeErrors.ts
autonomous: true
requirements: [D-15, D-16, D-18, D-19, D-20]
tags: [validation, onConnect, reroute, cleanup, deletion, vald-fold]

must_haves:
  truths:
    - "CanvasPanel.isValidConnection routes the port-type / BCPort allow-list check through the portType validator (Plan 04) for the proposed edge (D-19). The validator's run() is invoked one-shot on a synthetic 1-edge snapshot — NOT via the debounced runner."
    - "isAllowedBCConnection branch inside isValidConnection is removed in favor of portType's BCPort logic — single source of truth."
    - "StreamNode.tsx no longer imports selectNodeErrors or subscribes via hasBCError (D-20). The red-ring lights up via the unchanged errorNodeIds subscription, which is now populated by nMatch via the runner (Plan 06)."
    - "useStore.ts no longer carries: validationResult slice, validateAndGate action, clearValidation action, the import of validateTopology + TopologyResult from ../lib/validation."
    - "gui/src/lib/validation.ts is DELETED (field helpers were moved to gui/src/lib/validation/fields.ts in Plan 01)."
    - "gui/src/lib/validation.test.ts is DELETED (per-rule tests under gui/src/lib/validation/rules/__tests__/ + gui/src/lib/validation/__tests__/fields.test.ts cover the full surface)."
    - "gui/src/lib/selectors/nodeErrors.ts is DELETED (D-20 supersession by nMatch validator); gui/src/lib/selectors/topologyHints.ts remains untouched (axis-collision hint, RESEARCH Finding 3 — out of scope)."
  artifacts: []
  key_links:
    - from: "gui/src/components/CanvasPanel.tsx"
      to: "gui/src/lib/validation/rules/portType.ts"
      via: "one-shot run() inside isValidConnection per D-19"
      pattern: "portType\\.run\\("
---

<objective>
Close out the migration: reroute the onConnect hard-block through the portType validator (D-19), remove the legacy selectNodeErrors path in StreamNode + delete the selector (D-20), delete validateAndGate / clearValidation / validationResult slice + validation.ts + validation.test.ts (D-16). This is the final cleanup — after this plan, Phase 71's old code is gone and the new framework is the sole source of validation state.

Purpose: D-16, D-18, D-19, D-20 all require deletions or rewires that depend on Wave 3 UI surfaces being live (Plans 09-11) AND on the new export gate (Plan 12). Bundling them in one cleanup plan keeps the diff atomic and the grep gates exhaustive.

Output: rewired isValidConnection, removed legacy subscriptions, deleted three files.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-validation-framework/71-CONTEXT.md
@.planning/phases/71-validation-framework/71-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reroute isValidConnection through the portType validator (D-19)</name>
  <files>gui/src/components/CanvasPanel.tsx</files>
  <read_first>
    - gui/src/components/CanvasPanel.tsx:263-288 (current isValidConnection — uses getPortType + isAllowedBCConnection inline)
    - gui/src/lib/validation/rules/portType.ts (Plan 04 — portType.run(snapshot) contract; the rule covers both port-type mismatch AND BCPort allow-list)
    - gui/src/lib/validation/snapshot.ts (Plan 01 — ValidationSnapshot shape)
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-19 (single-source-of-truth contract; one-shot, not debounced)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Pattern 4" + Pitfall 2 (do NOT run the full validator suite inside isValidConnection — only portType)
  </read_first>
  <action>
    Edit isValidConnection in CanvasPanel.tsx (lines 263-288):

      Replace the current body with:
        ```
        const isValidConnection = useCallback((connection: Edge | Connection) => {
          if (!connection.source || !connection.target || !connection.sourceHandle || !connection.targetHandle) {
            return false;
          }

          const s = useStore.getState();
          const srcNode = s.nodes.find(n => n.id === connection.source);
          const tgtNode = s.nodes.find(n => n.id === connection.target);
          if (!srcNode || !tgtNode) return false;

          // Build a synthetic single-edge snapshot for one-shot portType validation.
          // D-19: do NOT run the full validator suite here — only the portType rule.
          // (RESEARCH Pitfall 2: full runValidators would call loopTraversal on every hover tick.)
          const syntheticEdge: Edge = {
            id: '__pending__',
            source: connection.source,
            target: connection.target,
            sourceHandle: connection.sourceHandle,
            targetHandle: connection.targetHandle,
          };
          const snapshot: ValidationSnapshot = {
            nodes: [srcNode, tgtNode],
            edges: [syntheticEdge],
            anchors: {},  // not used by portType
            bcMode: s.bcMode,
            resources: s.resources,
            getComponentDef: getComponent,
          };
          const results = portType.run(snapshot);
          return results.length === 0;
        }, []);
        ```

      Imports to add at the top of CanvasPanel.tsx:
        - `import { portType } from "@/lib/validation/rules/portType";`
        - `import type { ValidationSnapshot } from "@/lib/validation/snapshot";`
        - `import { getComponent } from "@/registry";` (already imported per existing CanvasPanel use; verify)
        - `import type { Edge } from "@xyflow/react";` (already imported per existing use)

      Imports to REMOVE if no other use site exists:
        - `import { isAllowedBCConnection } from "@/lib/bcMode";` — only if no other consumer in CanvasPanel.tsx; the rule now subsumes its logic. Confirm by `grep -c "isAllowedBCConnection" gui/src/components/CanvasPanel.tsx` after edit: should be 0.

      The local `getPortType` helper (line 45) can stay if other code uses it; otherwise remove. Grep before deciding.
  </action>
  <verify>
    <automated>cd gui && grep -c "portType.run\|portType\\." src/components/CanvasPanel.tsx</automated>
    <automated>cd gui && grep -c "isAllowedBCConnection" src/components/CanvasPanel.tsx</automated>
    <automated>cd gui && npm run typecheck 2>&1 | tail -10</automated>
    <automated>cd gui && npm run test -- --run src/components/CanvasPanel 2>&1 | tail -20 || echo "no CanvasPanel test (acceptable)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "portType\\." gui/src/components/CanvasPanel.tsx` returns ≥ 1.
    - `grep -c "isAllowedBCConnection" gui/src/components/CanvasPanel.tsx` returns 0 (subsumed).
    - typecheck does not exceed baseline.
    - Component tests pass; existing onConnect behavior preserved (no port-type mismatch allowed; valid BCPort connections still allowed).
  </acceptance_criteria>
  <done>onConnect hard-block reroute complete (D-19). The portType rule is the sole authority for port-type compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Remove hasBCError subscription + selectNodeErrors usage in StreamNode (D-20)</name>
  <files>gui/src/components/StreamNode.tsx</files>
  <read_first>
    - gui/src/components/StreamNode.tsx:17 (import of selectNodeErrors)
    - gui/src/components/StreamNode.tsx:324-337 (hasError + hasBCError subscriptions)
    - gui/src/components/StreamNode.tsx:387 (`const hasAnyError = hasError || hasBCError;`)
    - gui/src/lib/validation/rules/nMatch.ts (Plan 06 — replacement; nMatch results flow into errorNodeIds via initValidation)
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-20 (the explicit cleanup spec)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Pitfall 5" + §"Finding 8"
  </read_first>
  <action>
    Edits to gui/src/components/StreamNode.tsx:

      (a) Remove the import at line 17: `import { selectNodeErrors, type NodeErrorsInput } from "@/lib/selectors/nodeErrors";`.
      (b) Remove the `hasBCError` subscription block at lines 332-337.
      (c) Change line 387 from `const hasAnyError = hasError || hasBCError;` to `const hasAnyError = hasError;` — keep the rename if other code consumes it.
      (d) Remove any comment block that explains the Phase 63.1 hasBCError path (mark the deletion with a brief one-line comment: `// nMatch validator results flow through errorNodeIds via initValidation (Phase 71 D-20).`).

    Do NOT touch the hover/pin ring subscriptions (isCodeHovered, isCodePinned) or the layer-dim logic. The contract for StreamNode is unchanged at the rendering layer per D-18.
  </action>
  <verify>
    <automated>cd gui && grep -c "selectNodeErrors\|hasBCError" src/components/StreamNode.tsx</automated>
    <automated>cd gui && npm run typecheck 2>&1 | tail -10</automated>
    <automated>cd gui && npm run test -- --run src/components/StreamNode 2>&1 | tail -20 || echo "no StreamNode test (acceptable)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "selectNodeErrors\|hasBCError" gui/src/components/StreamNode.tsx` returns 0.
    - typecheck does not exceed baseline.
    - Existing StreamNode tests pass.
    - Manual sanity (executor optional): the red ring still appears on nodes flagged by n-mismatch (now via nMatch → errorNodeIds path).
  </acceptance_criteria>
  <done>StreamNode subscribes to errorNodeIds only; the legacy hasBCError path is gone (D-20).</done>
</task>

<task type="auto">
  <name>Task 3: Delete legacy state from useStore.ts (validateAndGate, clearValidation, validationResult slice, validateTopology import)</name>
  <files>gui/src/store/useStore.ts</files>
  <read_first>
    - gui/src/store/useStore.ts:15 (import: `validateTopology, type TopologyResult`)
    - gui/src/store/useStore.ts:199-201 (interface decls: validationResult, validateAndGate, clearValidation)
    - gui/src/store/useStore.ts:851-858 (initial-state: validationResult: null)
    - gui/src/store/useStore.ts:1876-1890 (validateAndGate + clearValidation action bodies)
    - all `validationResult` (singular) reset blocks across reset paths (lines 1027-1028, 1068-1069, 2452-2453, 2536-2537, 2730-2731 per earlier grep)
    - gui/src/lib/exportCode.ts (Plan 12 — confirm no validateAndGate call remains; this task can proceed only if Plan 12 finished)
  </read_first>
  <action>
    Edits to gui/src/store/useStore.ts:

      (a) Remove the import at line 15: `import { validateTopology, type TopologyResult } from "../lib/validation";` — delete the whole import line.

      (b) Remove the AppState interface entries (~line 198-201):
        - `errorNodeIds: Set<string>;` — KEEP (this slice is now populated by initValidation; D-18 keeps it on the store).
        - `validationResult: TopologyResult | null;` — REMOVE.
        - `validateAndGate: () => TopologyResult;` — REMOVE.
        - `clearValidation: () => void;` — REMOVE.

      (c) Remove the initial-state defaults `validationResult: null` (line 858) in the main initial-state block AND in EVERY reset path (search-and-destroy: `grep -n "validationResult:" gui/src/store/useStore.ts` after edit should return 0).

      (d) Remove the action bodies for `validateAndGate` (lines 1876-1886) and `clearValidation` (lines 1888-1890) entirely. Adjacent code that references them — there should be NONE in src/ after Plan 12 — must be confirmed absent: `grep -rn "validateAndGate\|clearValidation" gui/src/` returns nothing after this task.

      (e) Confirm that `errorNodeIds: new Set<string>()` remains in every reset path (since the slice stays on the store, just populated by the subscription rather than ad-hoc actions).

      (f) Confirm `validationResults` (plural — added Plan 01) is still present in every reset path; no edit needed.
  </action>
  <verify>
    <automated>cd gui && grep -c "validationResult\b" src/store/useStore.ts</automated>
    <automated>cd gui && grep -c "validateAndGate\|clearValidation" src/store/useStore.ts</automated>
    <automated>cd gui && grep -c "validateTopology\|TopologyResult" src/store/useStore.ts</automated>
    <automated>cd gui && grep -rn "validateAndGate\|clearValidation" src/</automated>
    <automated>cd gui && npm run typecheck 2>&1 | tail -20</automated>
    <automated>cd gui && npm run test -- --run src/store 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "validationResult\b" gui/src/store/useStore.ts` returns 0 (singular slice is gone).
    - `grep -c "validateAndGate\|clearValidation" gui/src/store/useStore.ts` returns 0.
    - `grep -c "validateTopology\|TopologyResult" gui/src/store/useStore.ts` returns 0.
    - `grep -rn "validateAndGate\|clearValidation" gui/src/` returns nothing across the entire src tree.
    - Store tests pass; typecheck no worse than baseline (and likely BETTER — the deleted imports were already counted in the baseline).
  </acceptance_criteria>
  <done>Legacy slice + actions + import physically removed from the store.</done>
</task>

<task type="auto">
  <name>Task 4: Delete gui/src/lib/validation.ts + validation.test.ts + gui/src/lib/selectors/nodeErrors.ts</name>
  <files>gui/src/lib/validation.ts, gui/src/lib/validation.test.ts, gui/src/lib/selectors/nodeErrors.ts</files>
  <read_first>
    - gui/src/lib/validation.ts (full file — confirm nothing else has been added since Plan 01 Task 2 rewired its importers)
    - gui/src/lib/selectors/nodeErrors.ts (full file — confirm Task 2 above removed the only consumer)
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-16, D-20 (delete spec)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Files to Delete vs Touch"
    - gui/src/lib/selectors/topologyHints.ts — DO NOT DELETE (RESEARCH Finding 3: axis-collision hint, out of scope)
  </read_first>
  <action>
    Step 1: Verify no remaining importers of validation.ts beyond the file itself + its co-located test:
      ```
      grep -rn "from \"@/lib/validation\"\b\|from \"../lib/validation\"\b\|from \"./validation\"\b" gui/src/ | grep -v "validation.test.ts" | grep -v "validation.ts:" | grep -v "validation/"
      ```
      Must return nothing. If anything appears, fix it before deleting.

    Step 2: Verify no remaining importers of selectors/nodeErrors.ts beyond the file itself:
      ```
      grep -rn "from \"@/lib/selectors/nodeErrors\"\|from \"../selectors/nodeErrors\"\|from \"./nodeErrors\"" gui/src/
      ```
      Must return nothing.

    Step 3: Delete three files:
      - gui/src/lib/validation.ts
      - gui/src/lib/validation.test.ts
      - gui/src/lib/selectors/nodeErrors.ts

    Step 4: Final grep gate:
      ```
      grep -rn "selectNodeErrors\|validateTopology\|TopologyResult\b" gui/src/
      ```
      Must return nothing across the entire src tree.

    Do NOT touch gui/src/lib/selectors/topologyHints.ts — explicit out of scope per RESEARCH Finding 3 (axis-collision hint, not a validation rule).
  </action>
  <verify>
    <automated>cd gui && test ! -f src/lib/validation.ts && echo "validation.ts deleted"</automated>
    <automated>cd gui && test ! -f src/lib/validation.test.ts && echo "validation.test.ts deleted"</automated>
    <automated>cd gui && test ! -f src/lib/selectors/nodeErrors.ts && echo "nodeErrors.ts deleted"</automated>
    <automated>cd gui && test -f src/lib/selectors/topologyHints.ts && echo "topologyHints.ts preserved"</automated>
    <automated>cd gui && grep -rn "selectNodeErrors\|validateTopology\|TopologyResult\b" src/ | tee /tmp/leftover-legacy.txt</automated>
    <automated>cd gui && npm run typecheck 2>&1 | tail -20</automated>
    <automated>cd gui && npm run test 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - All three files do NOT exist after this task.
    - gui/src/lib/selectors/topologyHints.ts STILL exists.
    - `grep -rn "selectNodeErrors\|validateTopology\|TopologyResult\b" gui/src/` returns NOTHING.
    - Full `npm run test` passes.
    - Full `npm run typecheck` is no worse than the 11-error baseline (likely BETTER — the deletions clean up some of the pre-existing errors flagged by Phase 61's deferred-items.md per STATE.md outstanding-items).
  </acceptance_criteria>
  <done>The legacy validation surface is physically gone; the new gui/src/lib/validation/ + the registered rules are the sole source of validation state. Phase 71 deliverable D-16 + D-18 + D-19 + D-20 closed.</done>
</task>

</tasks>

<verification>
- `cd gui && npm run test` passes overall.
- `cd gui && npm run typecheck` produces ≤ 11 errors (the documented baseline; D-22 expects no NEW errors).
- `cd gui && grep -rn "ValidationDialog\|validateAndGate\|clearValidation\|validateTopology\|selectNodeErrors\|hasBCError\|TopologyResult\b" src/` returns NOTHING across the entire src tree.
- `cd gui && grep -rn "import.*from.*\"\\(\\.\\./\\|@/\\)lib/validation\"$" src/` returns NOTHING (everyone imports from the new directory or from fields.ts).
- `cd gui && ls src/lib/validation/rules/*.ts | wc -l` returns 11 (port_type, required_connections, dangling_flow_port, z_n_match, length_match, geometry_consistency, n_match, loop_closure, gravity_sum_per_loop, pressure_boundary_required, driving_element_required).
</verification>

<success_criteria>
- D-16 closed (validateTopology folded, validation.ts deleted, field helpers in fields.ts).
- D-18 closed (errorNodeIds populated exclusively by initValidation; StreamNode contract unchanged).
- D-19 closed (onConnect routes through portType validator one-shot).
- D-20 closed (selectNodeErrors deleted; hasBCError removed; nMatch is the n-mismatch source of truth).
- Phase 71 cleanup wave complete; no legacy code remains.
</success_criteria>

<output>
Create `.planning/phases/71-validation-framework/71-13-SUMMARY.md` when done. Include: final typecheck error count vs the 11-error baseline, final test-pass count, the deleted-files list, and confirmation that gui/src/lib/selectors/topologyHints.ts was preserved.
</output>
