---
phase: 71-validation-framework
plan: 10
type: execute
wave: 2
depends_on: ["71-01", "71-03"]
files_modified:
  - gui/src/components/ValidationStatusBar.tsx
  - gui/src/components/__tests__/ValidationStatusBar.test.tsx
  - gui/src/App.tsx
  - gui/src/index.css
autonomous: true
requirements: [D-02, D-03, D-05]
tags: [validation, statusbar, ui, app-mount, initValidation, toaster]

must_haves:
  truths:
    - "A new always-visible statusbar strip (~22-24px) is mounted in App.tsx as a sibling AFTER <BottomPanel /> (D-02)."
    - "The strip shows three count chips: ⓭ N errors / ⚠ N warnings / ⓘ N info — using lucide-react icons (planner's discretion on exact glyphs: CircleAlert, TriangleAlert, Info)."
    - "Clicking a chip opens BottomPanel + sets activeBottomTab='validation' + dispatches a 'stream:validation-filter' CustomEvent that Plan 09's ValidationPanel consumes to pre-filter the rendered list by that severity (D-05 — locked decision, not optional). The panel does NOT auto-open from validation count changes (D-03 — only the chip pulses)."
    - "Chip pulse: when the error count transitions from 0 to N, the error chip plays a single CSS animation (~600ms keyframes pulse-once). No persistent spinner. (D-03)"
    - "App.tsx mounts the sonner <Toaster /> inside <TooltipProvider> on first render (one mount, app lifetime)."
    - "App.tsx calls initValidation() exactly once (on the same useEffect that runs initAutoRecover) and tears it down on unmount."
    - "ValidationDialog import + mount in App.tsx is LEFT IN PLACE for this plan — Plan 12 deletes it after the export gate is rewired. (Otherwise the modal would still fire on validateAndGate but with no way to dismiss new results.)"
  artifacts:
    - path: "gui/src/components/ValidationStatusBar.tsx"
      provides: "<ValidationStatusBar /> — three chips + click handlers (open panel + dispatch severity filter) + pulse animation"
    - path: "gui/src/index.css"
      provides: "@keyframes pulse-once + .pulse-once class (single-shot CSS animation)"
  key_links:
    - from: "gui/src/App.tsx"
      to: "gui/src/components/ValidationStatusBar.tsx"
      via: "sibling mount after BottomPanel"
      pattern: "ValidationStatusBar"
    - from: "gui/src/App.tsx"
      to: "gui/src/store/useStore.ts (initValidation)"
      via: "useEffect mount"
      pattern: "initValidation\\("
    - from: "gui/src/components/ValidationStatusBar.tsx"
      to: "gui/src/store/useStore.ts (validationResults + bottomPanelOpen + activeBottomTab)"
      via: "click handlers"
      pattern: "setActiveBottomTab\\|bottomPanelOpen"
    - from: "gui/src/components/ValidationStatusBar.tsx"
      to: "gui/src/components/ValidationPanel.tsx (Plan 09)"
      via: "window CustomEvent 'stream:validation-filter'"
      pattern: "stream:validation-filter"
---

<objective>
Mount the always-visible statusbar strip per D-02, wire its chip click-handlers per D-05 (open panel + dispatch severity-filter event consumed by Plan 09), implement the 0→N pulse per D-03, and bootstrap the validation subscription + sonner Toaster in App.tsx.

Purpose: D-02's statusbar is the always-visible discoverability surface. D-03's pulse-on-rise is the unobtrusive nudge to look at the panel. App.tsx is where initValidation() and the Toaster need to be mounted exactly once. The D-05 severity-filter contract is enforced END-TO-END: this plan dispatches the event; Plan 09 (same wave, parallel) listens for it and applies the filter to the panel list.

Output: ValidationStatusBar.tsx component + pulse-once CSS keyframes in index.css + App.tsx mount/init wiring.

**Wave note:** This plan was originally tagged Wave 7. The dependency on Plan 03 (sonner install) is the only real ordering constraint — `<Toaster />` cannot mount without the package. App.tsx + ValidationStatusBar do NOT depend on rule files; they consume the empty `validationResults` array Plan 01 ships. Wave dropped to **2** to allow this UI surface to build in parallel with Plans 04-08 rule files. Plan 11 still serializes after Plan 09 + Plan 10 (it consumes both) and sits at Wave 3.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-validation-framework/71-CONTEXT.md
@.planning/phases/71-validation-framework/71-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ValidationStatusBar.tsx + pulse-once CSS + tests</name>
  <files>gui/src/components/ValidationStatusBar.tsx, gui/src/components/__tests__/ValidationStatusBar.test.tsx, gui/src/index.css</files>
  <read_first>
    - .planning/phases/71-validation-framework/71-CONTEXT.md D-02, D-03, D-05 (statusbar UX contract — pre-filter is a locked decision)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Finding 5" + Pitfall 4 (statusbar mount layout details; BottomPanel stub is 14px so total chrome is ~36-38px when collapsed)
    - gui/src/components/SnapToGridButton.tsx (existing always-on-canvas overlay button pattern — copy the button-as-chip styling)
    - gui/src/index.css (current CSS — find existing animation utilities; pulse-once must not conflict with existing keyframe names)
    - gui/src/lib/validation/types.ts (Plan 01 — ValidationResult shape; count = results.filter(r => r.severity === sev).length)
    - gui/src/components/ValidationPanel.tsx (Plan 09 — confirm the 'stream:validation-filter' event-listener contract; dispatched event detail shape MUST be { severity: 'error' | 'warning' | 'info' })
  </read_first>
  <action>
    Create gui/src/index.css addition (append to the existing file): a single `@keyframes pulse-once` set (0% → opacity 1, 50% → opacity 0.4 + transform scale(1.1), 100% → opacity 1 + scale(1)) + a `.pulse-once { animation: pulse-once 600ms ease-out 1; }` class. Keep it simple — Phase 72 owns visual polish.

    Create ValidationStatusBar.tsx as a functional component:

    Subscribe:
      - validationResults: ValidationResult[] via primitive selector — derive three counts inline via useMemo: errorCount, warningCount, infoCount.
      - bottomPanelOpen + activeBottomTab + setActiveBottomTab + toggleBottomPanel from useStore.

    Layout:
      - Outer div: fixed-height 22px (CONTEXT D-02 range 22-24px; pick 22), bg-chrome (or whatever the existing app chrome class is — read CustomTitlebar.tsx for the matching dark token), border-t, flex flex-row items-center justify-between px-2 text-[11px].
      - Left side: three chip buttons (use shadcn Button variant="ghost" size="sm" with h-5 w-auto px-1.5 gap-1). Each chip: lucide icon + count. ALWAYS render all three chips, even when count===0 (consistent visual rhythm; the chip dims to muted text when count===0 — opacity-60).
      - Right side: empty for now (Phase 72 may add positional indicators).

    **Chip click handler — REQUIRED behavior (D-05 locked, Plan 09 implements the receiver).** For each chip (error / warning / info), the onClick:
      ```
      onClick={() => {
        useStore.setState({ bottomPanelOpen: true, activeBottomTab: 'validation' });
        window.dispatchEvent(new CustomEvent('stream:validation-filter', { detail: { severity: '<error|warning|info>' } }));
      }}
      ```
      The CustomEvent dispatch is REQUIRED, not optional. Plan 09 wires the matching listener in ValidationPanel.tsx — both halves ship in the same wave so the end-to-end behavior is testable in Plan 12's UAT.

    Pulse logic: useRef previousErrorCount; useEffect that compares previous vs current and, on a 0→N transition (previous === 0 && current > 0), adds 'pulse-once' className to the error chip via a useState boolean that auto-clears after 700ms via a setTimeout. Update previous ref after each render.

    Test file:
      - 3 chips render with the correct counts given a fake validationResults.
      - Click on error chip triggers store.setState with bottomPanelOpen=true and activeBottomTab='validation' (mock via vi.spyOn).
      - Click on error chip dispatches `window.dispatchEvent` with a CustomEvent of type 'stream:validation-filter' and detail.severity === 'error' (assert via window.addEventListener spy or vi.spyOn(window, 'dispatchEvent')).
      - Click on warning chip dispatches the same event with detail.severity === 'warning'.
      - Click on info chip dispatches the same event with detail.severity === 'info'.
      - Going from 0 errors to 1 error in the store mock adds the pulse-once class to the error chip element for one tick.
  </action>
  <verify>
    <automated>cd gui && npm run test -- --run src/components/__tests__/ValidationStatusBar.test.tsx</automated>
    <automated>cd gui && grep -c "pulse-once" src/index.css</automated>
    <automated>cd gui && grep -c "@keyframes pulse-once" src/index.css</automated>
    <automated>cd gui && grep -c "stream:validation-filter" src/components/ValidationStatusBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File gui/src/components/ValidationStatusBar.tsx exists; default export is a React component.
    - Test file has ≥5 `it(` blocks (three-chip render, chip-click store side effect, chip-click event dispatch for all three severities, pulse on 0→N); all pass.
    - `grep -c "pulse-once" gui/src/index.css` returns ≥ 2 (keyframe + class).
    - `grep -c "stream:validation-filter" gui/src/components/ValidationStatusBar.tsx` returns ≥ 1 (the CustomEvent dispatch).
    - The strip is exactly one fixed-height element (22px); verifiable in source via the style/className.
    - Uses ghost-button styling (existing Button primitive); no new ad-hoc HTML button.
  </acceptance_criteria>
  <done>Statusbar strip component exists with all three chips, click handlers (store mutation + REQUIRED 'stream:validation-filter' dispatch), and the pulse-on-rise animation.</done>
</task>

<task type="auto">
  <name>Task 2: Mount ValidationStatusBar + Toaster + initValidation in App.tsx</name>
  <files>gui/src/App.tsx</files>
  <read_first>
    - gui/src/App.tsx (full file — especially line 580 (closing div above ValidationDialog), the existing initAutoRecover useEffect at lines 130-200, the TooltipProvider wrapping)
    - gui/src/components/ValidationStatusBar.tsx (Task 1)
    - gui/src/components/ui/sonner.tsx (Plan 03 — exports Toaster)
    - gui/src/store/useStore.ts (initValidation export — Plan 01 Task 3)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Finding 4" + §"Finding 5" (mount conventions: initValidation alongside initAutoRecover; statusbar after BottomPanel inside the root flex-col)
  </read_first>
  <action>
    Edits to gui/src/App.tsx:

    (a) Imports: add `import ValidationStatusBar from "./components/ValidationStatusBar";` near the other component imports, `import { Toaster } from "./components/ui/sonner";` near the TooltipProvider import, and `initValidation` to the existing `import { initializeRecentFiles, initAutoRecover } from "./store/useStore";` line.

    (b) In the useEffect that calls initAutoRecover (find the lines ~130-200 — there are 3 invocation sites: clean launch, after Recover, after Discard), add a parallel `initValidation()` call. The simpler approach: a single new useEffect with `useEffect(() => { const teardown = initValidation(); return teardown; }, []);` at component top level — it does not need to be sequenced with the autoRecover flow because initValidation's snapshot reads are read-only and its writes are to validationResults/errorNodeIds (independent of autoRecover state). Keep this useEffect separate from autoRecover wiring to avoid the conditional-init complexity. The teardown returned by initValidation() handles unsubscribe + clearTimeout.

    (c) Mount `<ValidationStatusBar />` inside the root `<div className="flex flex-col h-screen w-screen overflow-hidden">` as a sibling AFTER `<BottomPanel />` (the line at ~579). The full sequence becomes: `<CustomTitlebar /> → <div flex-1 main area> → <BottomPanel /> → <ValidationStatusBar />`. Per RESEARCH Finding 5, this is structurally trivial — h-screen flex-col absorbs the additional 22px row.

    (d) Mount `<Toaster />` inside `<TooltipProvider>` (which wraps the root return JSX). Single mount at app lifetime. Place it adjacent to the existing `<UnsavedChangesDialog />` / `<ValidationDialog />` / `<CommandPalette />` / `<SavePresetModal />` sibling dialog mounts.

    (e) DO NOT remove the `import ValidationDialog from "./components/ValidationDialog";` or the `<ValidationDialog />` mount. Plan 12 owns those deletions after the export gate is rewired. (Removing them now would leave validateAndGate writing validationResult with no UI surface, but worse — Plan 12 hasn't replaced the gate yet, so removing the dialog mid-stream would break the existing error UX.)
  </action>
  <verify>
    <automated>cd gui && grep -c "ValidationStatusBar" src/App.tsx</automated>
    <automated>cd gui && grep -c "initValidation" src/App.tsx</automated>
    <automated>cd gui && grep -c "<Toaster" src/App.tsx</automated>
    <automated>cd gui && grep -c "ValidationDialog" src/App.tsx</automated>
    <automated>cd gui && npm run typecheck 2>&1 | tail -10</automated>
    <automated>cd gui && npm run test -- --run src/App 2>&1 | tail -20 || echo "no App test (acceptable)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ValidationStatusBar" gui/src/App.tsx` returns ≥ 2 (import + mount).
    - `grep -c "initValidation" gui/src/App.tsx` returns ≥ 2 (import + call).
    - `grep -c "<Toaster" gui/src/App.tsx` returns 1 (single mount).
    - `grep -c "ValidationDialog" gui/src/App.tsx` returns 2 (import + mount — left in place per Plan 12 handoff).
    - npm run typecheck does not exceed Plan 01 baseline.
    - Existing App tests (if any) still pass.
  </acceptance_criteria>
  <done>Statusbar strip is mounted; Toaster is mounted; initValidation runs on App mount and tears down on unmount. ValidationDialog remains until Plan 12.</done>
</task>

</tasks>

<verification>
- `cd gui && npm run test -- --run src/components/__tests__/ValidationStatusBar.test.tsx` passes.
- `cd gui && npm run typecheck` does not exceed baseline.
- `cd gui && npm run test` overall pass count is no lower than before this plan.
- After this plan: launching the app shows the statusbar strip with three chips (all at "0") under BottomPanel; clicking the error chip opens BottomPanel to the Validation tab (which shows "No issues." per Plan 09 — or the filtered "No results match the active filter." once Plan 09's listener is wired in the same wave). Manual sanity only — automated E2E not required; Plan 12 UAT covers it end-to-end.
</verification>

<success_criteria>
- Always-visible statusbar strip mounted (D-02).
- 0→N pulse animation wired (D-03).
- Click handlers route to the panel AND dispatch the locked 'stream:validation-filter' event (D-05).
- initValidation() lives across App lifetime; teardown cleans up.
- Toaster mounted for Plan 12.
</success_criteria>

<output>
Create `.planning/phases/71-validation-framework/71-10-SUMMARY.md` when done. Include: ValidationStatusBar test count, the exact strip height chosen, the pulse keyframe duration, the final ordering of dialog mounts inside <TooltipProvider>, and confirmation that the three chips each dispatch the matching 'stream:validation-filter' CustomEvent.
</output>
