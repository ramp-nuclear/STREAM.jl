---
phase: 71-validation-framework
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - gui/package.json
  - gui/package-lock.json
  - gui/src/components/ui/sonner.tsx
autonomous: false
requirements: [D-17]
tags: [validation, toast, dependency, install, sonner]

must_haves:
  truths:
    - "sonner is installed as a runtime dependency in gui/package.json."
    - "A shadcn-style wrapper component gui/src/components/ui/sonner.tsx exports a `<Toaster />` element with engineering-tool defaults (bottom-right, theme-aware, short duration)."
    - "No Toaster mount yet — Plan 10 (App.tsx integration) mounts it; Plan 12 (export gate) calls toast.error() from sonner."
  artifacts:
    - path: "gui/package.json"
      provides: "sonner pinned dependency entry"
    - path: "gui/src/components/ui/sonner.tsx"
      provides: "<Toaster /> wrapper that reads useTheme and forwards to sonner's Toaster"
  key_links:
    - from: "gui/src/App.tsx"
      to: "gui/src/components/ui/sonner.tsx"
      via: "Plan 10 mounts Toaster"
      pattern: "<Toaster"
    - from: "gui/src/lib/exportCode.ts"
      to: "sonner package"
      via: "Plan 12 imports toast"
      pattern: "import \\{ toast \\} from \"sonner\""
---

<objective>
Install sonner and create the shadcn Toaster wrapper so Plan 10 can mount it and Plan 12 can call toast.error() from the export gate per D-17.

Purpose: D-17's export-gate UX requires a transient toast. RESEARCH §"Standard Stack" + §"Finding 1" confirm no toast library exists in the project; sonner is the chosen addition.

Output: package.json entry + ui/sonner.tsx component. No mount, no caller wiring (those land in Plans 10 + 12).
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-validation-framework/71-CONTEXT.md
@.planning/phases/71-validation-framework/71-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Pre-install legitimacy gate for sonner [ASSUMED]</name>
  <files>(no source files — verification-only checkpoint)</files>
  <read_first>
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Package Legitimacy Audit" (sonner row marked [ASSUMED] because slopcheck was unavailable)
    - .planning/phases/71-validation-framework/71-CONTEXT.md (Claude's Discretion section — sonner explicitly named as the toast candidate)
  </read_first>
  <what-built>
    RESEARCH.md §"Package Legitimacy Audit" tags sonner as [ASSUMED] (slopcheck unavailable). Sonner is a well-known shadcn-ecosystem toast package by Emil Kowalski, but per the legitimacy gate protocol any [ASSUMED] / [SUS] package install MUST be confirmed by the developer before npm runs. This is the human-verify gate.
  </what-built>
  <action>
    Halt automated execution. The user manually confirms sonner's legitimacy per the steps in `<how-to-verify>`. The executor does NOT run any package-install command in this task — Task 2 owns the install AFTER the human resume signal. If the user finds something off in the verification steps, the planner re-evaluates: the fallback per RESEARCH §"Environment Availability" is the shadcn Toast primitive (more code, no new dependency).
  </action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/sonner in a browser.
    2. Confirm:
       - Maintainer: emilkowalski (Emil Kowalski).
       - Weekly downloads: >100k (sanity check — actively used).
       - Repository link: https://github.com/emilkowalski/sonner.
       - Latest version is current (within ~1 year).
    3. Cross-check the version with shadcn/ui's recommended Toaster integration (https://ui.shadcn.com/docs/components/sonner) to make sure the wrapper shape this plan creates is compatible.
    4. If anything looks off (typosquatted name, dormant repo, suspicious maintainer change), STOP and surface the finding — the planner will swap in shadcn's own Toast primitive (more work but a known-good fallback per RESEARCH §"Environment Availability").
  </how-to-verify>
  <verify>
    <human-check>Sonner package on npmjs.com confirmed legitimate (maintainer, repo, downloads).</human-check>
  </verify>
  <acceptance_criteria>
    - Human responds "approved" (or equivalent affirmative resume signal) confirming legitimacy.
    - If the response describes a problem, Task 2 is NOT executed and the planner revisits the design.
  </acceptance_criteria>
  <resume-signal>Type "approved" if sonner checks out; otherwise describe the issue.</resume-signal>
  <done>Human has confirmed sonner is safe to install; Task 2 may run.</done>
</task>

<task type="auto">
  <name>Task 2: Install sonner and create ui/sonner.tsx wrapper</name>
  <files>gui/package.json, gui/package-lock.json, gui/src/components/ui/sonner.tsx</files>
  <read_first>
    - gui/package.json (current dependencies — confirm sonner not already present)
    - gui/src/components/ui/tooltip.tsx (shadcn wrapper pattern — Toaster wrapper mirrors this convention: re-export sonner's Toaster with project defaults baked in)
    - gui/src/hooks/useTheme.ts (THEMES export + resolved theme value — Toaster theme should mirror it)
    - .planning/phases/71-validation-framework/71-RESEARCH.md §"Finding 1" (Toaster mount location: inside <TooltipProvider> in App.tsx — Plan 10 owns the mount)
  </read_first>
  <action>
    From the gui/ directory: run `npm install sonner` (this is the only network-touching command in this plan; the prior checkpoint approved it).

    Create gui/src/components/ui/sonner.tsx that:
      - Imports `Toaster as SonnerToaster, type ToasterProps` from `"sonner"`.
      - Imports `useTheme` from `"@/hooks/useTheme"` (or the equivalent path consistent with other ui/ shims — read tooltip.tsx for the convention).
      - Exports `const Toaster = ({ ...props }: ToasterProps) => ( <SonnerToaster theme={resolvedTheme as ToasterProps['theme']} position="bottom-right" duration={2000} closeButton richColors={false} ... /> )`. Use closure-captured resolvedTheme from useTheme().
      - Re-exports `toast` from sonner so consumers do `import { toast } from "@/components/ui/sonner"` (this keeps the shadcn idiom: one wrapper module).

    Defaults:
      - position: "bottom-right" (engineering-tool convention; consistent with the new statusbar Plan 10 mounts in the same corner region).
      - duration: 2000ms (matches D-17: "Short toast (≤2s)").
      - closeButton: false (terse engineering-voice; per memory feedback_engineering_voice_copy).
      - richColors: false (Phase 72 owns the visual treatment; default sonner styling stays neutral).
      - className wrapper allowing Phase 72 to override.

    Do NOT mount <Toaster /> in App.tsx yet — Plan 10 wires the mount alongside the statusbar strip. Do NOT call toast() anywhere yet — Plan 12 wires the export-gate caller.
  </action>
  <verify>
    <automated>cd gui && grep -c "\"sonner\":" package.json</automated>
    <automated>cd gui && test -f src/components/ui/sonner.tsx && echo "exists"</automated>
    <automated>cd gui && grep -c "export.*Toaster\|export.*toast" src/components/ui/sonner.tsx</automated>
    <automated>cd gui && npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\"sonner\":" gui/package.json` returns 1 (under `dependencies`).
    - File gui/src/components/ui/sonner.tsx exists and exports both `Toaster` and `toast` (the second is a pass-through re-export).
    - The Toaster wrapper sets `position="bottom-right"` and `duration={2000}` literals (verifiable via grep).
    - `npm run typecheck` does not produce MORE errors than the Plan 01 baseline.
    - `grep -rn "<Toaster" gui/src/` returns ONLY the wrapper file definition — no mount sites yet (Plan 10 adds them).
    - `grep -rn "from \"sonner\"\|from 'sonner'" gui/src/` returns ONLY gui/src/components/ui/sonner.tsx — no other importer (Plan 12 will import from the wrapper).
  </acceptance_criteria>
  <done>sonner installed, ui/sonner.tsx wrapper exported, no callers yet.</done>
</task>

</tasks>

<verification>
- `cd gui && npm ls sonner` resolves a single version.
- `cd gui && npm run typecheck` passes (no new errors beyond baseline).
- `cd gui && npm run test` passes (no behavior change yet — wrapper is unmounted).
</verification>

<success_criteria>
- sonner present as a pinned dependency.
- Toaster wrapper exists with project-appropriate defaults (bottom-right, 2s, theme-aware).
- No mount or caller — Plan 10 mounts the Toaster, Plan 12 calls toast.error() from the export gate.
</success_criteria>

<output>
Create `.planning/phases/71-validation-framework/71-03-SUMMARY.md` when done. Include: installed sonner version, confirmation of legitimacy-gate outcome, and the resolved package-lock diff size.
</output>
