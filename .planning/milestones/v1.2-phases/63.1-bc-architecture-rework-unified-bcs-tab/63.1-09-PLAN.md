---
phase: 63.1
plan: 09
type: execute
wave: 5
depends_on: ["63.1-03"]
files_modified:
  - gui/src/components/ToolboxPanel.tsx
  - gui/src/components/StreamNode.tsx
autonomous: true
requirements:
  - D-06
  - D-13
requirements_addressed:
  - D-06
  - D-13
must_haves:
  truths:
    - "ToolboxPanel.tsx no longer renders the Sources category block by default (D-06)"
    - "Registry entries for WallTemperature / HeatFluxSource remain available for programmatic spawn from promoteToSharedSource (Pitfall 7)"
    - "StreamNode.tsx renders a small filled circle anchor indicator next to the FlowPort handle whose portField matches anchors[nodeId].portField (D-13)"
    - "Anchor indicator has data-testid='anchor-indicator' and aria-label='Pressure anchor' (UI-SPEC §Canvas Anchor Indicator)"
    - "Indicator selector returns a primitive boolean (Pitfall 1 — no fresh array/object returns)"
  artifacts:
    - path: "gui/src/components/ToolboxPanel.tsx"
      provides: "Toolbox panel without the Sources category header + items"
      contains: "Sources"
    - path: "gui/src/components/StreamNode.tsx"
      provides: "Anchor indicator dot next to anchored FlowPort handle"
      contains: "anchor-indicator"
  key_links:
    - from: "StreamNode.tsx FlowPort handle map"
      to: "useStore selector reading anchors[nodeId]?.portField"
      via: "primitive-boolean selector wrapped in useCallback"
      pattern: "anchors\\[id\\]"
---

<objective>
Wave 5 — canvas / toolbox surface changes. (a) Hide the Sources category from the default toolbox per D-06 — registry entries for WallTemperature / HeatFluxSource stay so promoteToSharedSource (Plan 08) can spawn them programmatically. (b) Add a small filled-circle anchor indicator on StreamNode.tsx next to whichever FlowPort handle is anchored, per D-13 + UI-SPEC §"Canvas Anchor Indicator".

Purpose: Sources canvas nodes are now opt-in; the default user flow never sees the category. The anchor indicator gives instant feedback that a component is anchored (important for the future global "exactly one anchor per loop" validator in Phase 71).

Output: ToolboxPanel.tsx with the Sources block deleted; StreamNode.tsx FlowPort handle map extended with the indicator sub-element.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-CONTEXT.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md
@gui/src/components/ToolboxPanel.tsx
@gui/src/components/StreamNode.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove Sources category block from ToolboxPanel.tsx (D-06)</name>
  <files>gui/src/components/ToolboxPanel.tsx</files>
  <behavior>
    - The Sources category header and its mapped ToolboxItem list no longer render
    - `getComponentsByCategory("Sources")` call is removed (no other consumer in this file)
    - Registry entries themselves are untouched — WT / HFS components remain in components.json and can be spawned programmatically via addNode (used by Plan 08's promoteToSharedSource)
  </behavior>
  <read_first>
    gui/src/components/ToolboxPanel.tsx (lines 14-15 `sourceComponents` declaration; lines 65-84 Sources block render),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Toolbox hide (D-06)" lines 747-756; Pitfall 7 lines 499-503 — registry entries remain),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"ToolboxPanel.tsx — delete Sources block" lines 822-843)
  </read_first>
  <action>Step A — DELETE the `const sourceComponents = getComponentsByCategory("Sources")` line at line 14. DELETE the `const visibleSources = sourceComponents` line at line 15. Step B — DELETE the Sources block rendering at lines 65-84 entirely: the `<div className="text-xs font-semibold uppercase ...">Sources</div>` header AND the `{visibleSources.length > 0 && ...}` conditional + map. Step C — leave all other category blocks (Hydraulic, Thermal, Reactor Physics, etc.) untouched. Step D — confirm no orphaned imports: if `getComponentsByCategory` is still used for other categories, leave the import; if not, remove it.</action>
  <verify>
    <automated>cd gui && grep -c "Sources" src/components/ToolboxPanel.tsx; grep -c "sourceComponents\\|visibleSources" src/components/ToolboxPanel.tsx; grep -c "getComponentsByCategory" src/components/ToolboxPanel.tsx; npx tsc --noEmit 2>&1 | tail -10; npx vitest run src/components/__tests__/ToolboxPanel.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "Sources" gui/src/components/ToolboxPanel.tsx` returns 0 (no header, no category-key reference).
    `grep -c "sourceComponents" gui/src/components/ToolboxPanel.tsx` returns 0.
    `grep -c "visibleSources" gui/src/components/ToolboxPanel.tsx` returns 0.
    `cd gui && npx tsc --noEmit` exits 0.
    The existing `ToolboxPanel.test.tsx` may have asserted on the Sources block presence — if so, update that test to assert its absence; otherwise leave untouched.
  </acceptance_criteria>
  <done>Sources block physically removed from the toolbox UI; registry entries unchanged; tsc + ToolboxPanel test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add anchor indicator to StreamNode.tsx FlowPort handle map (D-13)</name>
  <files>gui/src/components/StreamNode.tsx</files>
  <behavior>
    - For each FlowPort rendered by the existing `flowPorts.map(...)` block, an additional `<div>` with `data-testid="anchor-indicator"` and `aria-label="Pressure anchor"` renders next to the Handle when anchors[id]?.portField matches the handle's expected portField ("port_in.P" for an input handle, "port_out.P" for an output handle)
    - Indicator visual: w-1.5 h-1.5 (6×6 px), rounded-full, bg-foreground, absolutely positioned next to the handle
    - The selector returns a primitive boolean (Pitfall 1)
    - To avoid the React rules-of-hooks violation inside .map (Pitfall mentioned in RESEARCH §"FlowPort handle render block" pitfall note line 818), extract the handle + indicator into a small sub-component `<FlowPortHandle nodeId={id} port={port} dimFlowHandles={dimFlowHandles} />` so each instance can call useStore directly. This mirrors the project's existing pattern for per-row sub-components (RESEARCH line 818)
    - Plan 01's StreamNode.anchor.test.tsx flips from RED to GREEN
  </behavior>
  <read_first>
    gui/src/components/StreamNode.tsx (lines 17-23 inline color constants; lines 77-91 existing selector pattern with comment block — preserve; lines 180-196 FlowPort handle map block — this is what we extend),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md (§"Canvas Anchor Indicator" full section — Spec table, Render gate, Interaction with existing handle visuals),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Anchor indicator (D-13)" lines 758-786 — full JSX pattern; Pitfall 1 lines 449-457; rules-of-hooks note at line 818),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"StreamNode.tsx — anchor indicator" lines 758-820),
    gui/src/components/__tests__/StreamNode.anchor.test.tsx (Plan-01 RED test — this task flips it to GREEN; test asserts on `data-testid="anchor-indicator"` and `aria-label="Pressure anchor"`)
  </read_first>
  <action>Step A — extract a new sub-component above the StreamNode default export: `function FlowPortHandle({ nodeId, port, dimFlowHandles }: { nodeId: string; port: { name: string; type: string; side?: string }; dimFlowHandles: boolean }) {...}`. Body: derive `isInPort = port.name.includes("in")`; derive `portFieldKey = isInPort ? "port_in.P" : "port_out.P"`; `const hasAnchor = useStore(useCallback((s) => s.anchors[nodeId]?.portField === portFieldKey, [nodeId, portFieldKey]))` — primitive boolean per Pitfall 1. Render a React Fragment containing (a) the existing `<Handle>` with the same props the original map was using (extract style logic; key={port.name}; data={{portType: port.type}}; type={isInPort ? "target" : "source"}; position={sideToPosition[port.side!]}; background/border colors from the constants; opacity handling for dimFlowHandles), and (b) the conditional indicator: `{hasAnchor && <div data-testid="anchor-indicator" aria-label="Pressure anchor" className={`absolute w-1.5 h-1.5 rounded-full bg-foreground ${dimFlowHandles ? "opacity-20" : ""}`} style={anchorIndicatorStyleFor(port.side)} />}`. Implement `anchorIndicatorStyleFor(side)` as a small helper returning the absolute-position offset described in UI-SPEC §"Canvas Anchor Indicator — Position" — for left-side, `{ position: "absolute", left: -6, top: -6 }`; for right-side, `{ right: -6, top: -6 }`; for top/bottom, mirror appropriately. (Adjust to match the existing ReactFlow Handle positioning convention so the indicator visually sits just outside the 12-px Handle; refine if visual smoke shows clipping — the values in the UI-SPEC are the recommended starting point.) Step B — rewrite the existing `flowPorts.map(...)` at lines 180-196 to `flowPorts.map((port) => <FlowPortHandle key={port.name} nodeId={id} port={port} dimFlowHandles={dimFlowHandles} />)`. This eliminates the rules-of-hooks violation that would occur if we called useStore directly inside .map. Step C — confirm the `hasError` and `hasBCError` selectors at lines 77-90 are untouched; the Pitfall-1 comment block is preserved.</action>
  <verify>
    <automated>cd gui && grep -c "anchor-indicator" src/components/StreamNode.tsx; grep -c "Pressure anchor" src/components/StreamNode.tsx; grep -c "function FlowPortHandle\\|const FlowPortHandle" src/components/StreamNode.tsx; grep -c "s.anchors\\[nodeId\\]" src/components/StreamNode.tsx; npx vitest run src/components/__tests__/StreamNode.anchor.test.tsx src/components/__tests__/StreamNode.test.tsx 2>&1 | tail -25; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "anchor-indicator" gui/src/components/StreamNode.tsx` returns 1.
    `grep -c "Pressure anchor" gui/src/components/StreamNode.tsx` returns 1.
    `grep -c "FlowPortHandle" gui/src/components/StreamNode.tsx` returns 2 or more (sub-component declaration + JSX usage).
    `grep -c "s.anchors\\[" gui/src/components/StreamNode.tsx` returns 1 or more (selector reads the anchors slice).
    `cd gui && npx vitest run src/components/__tests__/StreamNode.anchor.test.tsx` reports all GREEN.
    `cd gui && npx vitest run src/components/__tests__/StreamNode.test.tsx` reports all GREEN (no regression to existing render tests).
    `cd gui && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Anchor indicator renders next to anchored FlowPort handles, accessible label set, Plan 01 anchor test GREEN, no regression to existing StreamNode rendering.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1.09-01 | N/A | toolbox category hide + indicator render | N/A — UI-only; no data flow, no new input handling |
</threat_model>

<verification>
- ToolboxPanel.tsx has no Sources block; registry entries unaffected.
- StreamNode.tsx renders the anchor indicator with correct test hooks and primitive-boolean selector.
- Plan 01 StreamNode.anchor.test.tsx GREEN.
- ToolboxPanel.test.tsx still passes (with any necessary "no Sources" assertion update).
- tsc + vitest green modulo Plan 10 RED stubs.
</verification>

<success_criteria>
- D-06 + D-13 implemented.
- The dominant flow (no Sources category in toolbox) is now the actual default.
- Anchored components are visually distinguishable on canvas.
</success_criteria>

<output>
After completion, create `.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-09-SUMMARY.md`
</output>
