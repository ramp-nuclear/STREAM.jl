---
phase: 63.1
plan: 05
type: execute
wave: 3
depends_on: ["63.1-03"]
files_modified:
  - gui/src/components/BCPanel.tsx
  - gui/src/components/BCRow.tsx
  - gui/src/components/BottomPanel.tsx
autonomous: true
requirements:
  - D-03
requirements_addressed:
  - D-03
must_haves:
  truths:
    - "gui/src/components/BCPanel.tsx no longer exists (deleted file)"
    - "gui/src/components/BCRow.tsx no longer exists (deleted file)"
    - "BottomPanel.tsx no longer renders a BCs tab; only the Code tab remains"
    - "No import of BCPanel or BCRow anywhere in gui/src"
    - "D-03: the freestanding BCPanel.tsx is physically deleted along with BCRow.tsx and the bottom-panel BCs tab — this plan completes the 'single rendering site' half of D-03 that Plan 03 began (slice deletion); after both, no legacy BC surface remains in gui/src"
  artifacts:
    - path: "gui/src/components/BottomPanel.tsx"
      provides: "Bottom panel with only the Code tab"
      contains: "CodePreview"
  key_links:
    - from: "BottomPanel.tsx"
      to: "CodePreview.tsx"
      via: "single TabsContent (or direct render)"
      pattern: "CodePreview"
---

<objective>
Wave 2 part C — delete the legacy bottom-panel BCs surface per D-03. Remove BCPanel.tsx and BCRow.tsx entirely. Update BottomPanel.tsx to drop the BCs tab (keep Code). This is purely deletion + one tab removal — runs parallel to Plan 04 because the file sets are disjoint.

Purpose: Single rendering site for anchors is the per-component BCs tab in the right sidebar (Plan 06 lands AnchorsSection there). Removing BCPanel here is the last step in collapsing the legacy two-surface architecture.

Output: Two deleted files; BottomPanel.tsx with one tab.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-CONTEXT.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-03-SUMMARY.md
@gui/src/components/BottomPanel.tsx
@gui/src/components/BCPanel.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete BCPanel.tsx, BCRow.tsx; remove BCs tab from BottomPanel.tsx</name>
  <files>
    gui/src/components/BCPanel.tsx,
    gui/src/components/BCRow.tsx,
    gui/src/components/BottomPanel.tsx
  </files>
  <read_first>
    gui/src/components/BCPanel.tsx (full file — confirm it is the only consumer of the "bcs" tab and that its only state dependency was the (now-deleted) bcs slice; this informs why deletion is safe),
    gui/src/components/BottomPanel.tsx (lines 5, 22-36 — TabsList + TabsContent for both Code and BCs),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"BottomPanel.tsx — drop BCs tab" lines 847-867; §"Delete-list" — D-03 enumerates BCPanel + BCRow + bottom-panel BCs tab; A7 in Assumptions Log lines 972-973 confirms BCPanel is the sole "bcs" tab consumer),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"BottomPanel.tsx — drop BCs tab" lines 847-867 — exact lines to delete)
  </read_first>
  <action>Step A — delete gui/src/components/BCPanel.tsx (rm). Step B — delete gui/src/components/BCRow.tsx (rm). Step C — BottomPanel.tsx edits: (i) REMOVE `import BCPanel from "./BCPanel"` at line 5. (ii) REMOVE the `<TabsTrigger value="bcs" ...>BCs</TabsTrigger>` line. (iii) REMOVE the entire `<TabsContent value="bcs" ...><BCPanel /></TabsContent>` block. Result: BottomPanel has a single `<TabsContent value="code">` body. Either keep the single-tab `<Tabs defaultValue="code">` wrapper for visual continuity (acceptable per RESEARCH lines 866-867) OR collapse to a plain `<div>` wrapper around `<CodePreview />`. Preference: keep the `<Tabs>` wrapper to minimize churn and to leave room for future bottom-panel tabs (Phase 71 may reuse this slot). Step D — repo-wide consumer sweep: `grep -rE "BCPanel|BCRow" gui/src` should return zero matches (the deletion is complete only when no orphaned import survives). If any test file still imports BCPanel (unlikely — only `BCPanel` was the consumer surface, no test imports it), delete those tests.</action>
  <verify>
    <automated>cd gui && test ! -f src/components/BCPanel.tsx && test ! -f src/components/BCRow.tsx && echo "files deleted ok"; grep -rE "BCPanel|BCRow" src 2>/dev/null | head -5; grep -c "value=\"bcs\"" src/components/BottomPanel.tsx; grep -c "CodePreview" src/components/BottomPanel.tsx; npx vitest run 2>&1 | tail -20; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    Files `gui/src/components/BCPanel.tsx` and `gui/src/components/BCRow.tsx` do NOT exist (`test -f` returns failure).
    `grep -rE "BCPanel|BCRow" gui/src` returns 0 matches.
    `grep -c "value=\"bcs\"" gui/src/components/BottomPanel.tsx` returns 0.
    `grep -c "CodePreview" gui/src/components/BottomPanel.tsx` returns 1 or more (Code tab still renders the preview).
    `cd gui && npx tsc --noEmit` exits 0.
    `cd gui && npx vitest run` reports zero new failures (only the documented Plan-01 stubs awaiting Plans 08/10 remain RED).
  </acceptance_criteria>
  <done>BCPanel + BCRow physically removed; BottomPanel slimmed to single Code tab; tsc + vitest green (modulo expected RED stubs).</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1.05-01 | N/A | file deletion | N/A — pure removal of UI surface, no new code paths, no input handling |
</threat_model>

<verification>
- BCPanel.tsx and BCRow.tsx deleted.
- BottomPanel.tsx has only the Code tab.
- No orphaned imports.
- tsc + vitest green.
</verification>

<success_criteria>
- D-03 fully satisfied: anchors no longer have a freestanding panel surface; the legacy bottom-panel BCs tab is gone.
- Bottom panel still renders the Code preview (no regression to that surface).
</success_criteria>

<output>
After completion, create `.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-05-SUMMARY.md`
</output>
