---
phase: 63.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - gui/src/lib/selectors/__tests__/nodeErrors.test.ts
  - gui/src/store/__tests__/useStore.anchors.test.ts
  - gui/src/lib/__tests__/codeGenerator.anchors.test.ts
  - gui/src/store/__tests__/useStore.onNodesChange.select.test.ts
  - gui/src/store/__tests__/useStore.addEdge.bcport.test.ts
  - gui/src/store/__tests__/useStore.setBCSymmetric.test.ts
  - gui/src/store/__tests__/promoteToSharedSource.test.ts
  - gui/src/lib/__tests__/projectIO.anchors.test.ts
  - gui/src/components/__tests__/StreamNode.anchor.test.tsx
autonomous: true
requirements:
  - D-02
  - D-05
  - D-13
  - D-15
  - D-20
  - D-21
  - D-22
  - D-14
requirements_addressed:
  - D-02
  - D-05
  - D-13
  - D-15
  - D-20
  - D-21
  - D-22
  - D-14
must_haves:
  truths:
    - "Wave-0 test stubs exist for every new behavior (selector, anchors slice, codegen, projectIO swap, click-vs-drag, CR-01, CR-02/03, Promote, StreamNode anchor glyph)"
    - "Stubs are failing-by-default (RED) until implementation lands in later waves"
    - "Each stub names the exact decision IDs it covers"
  artifacts:
    - path: "gui/src/lib/selectors/__tests__/nodeErrors.test.ts"
      provides: "selectNodeErrors RED tests (D-15 / D-23)"
      contains: "describe('selectNodeErrors'"
    - path: "gui/src/store/__tests__/useStore.anchors.test.ts"
      provides: "anchors slice setAnchor/clearAnchor RED tests (D-02)"
      contains: "describe('anchors slice'"
    - path: "gui/src/lib/__tests__/codeGenerator.anchors.test.ts"
      provides: "anchors-emit RED tests (D-05)"
      contains: "describe('codeGenerator anchors'"
    - path: "gui/src/store/__tests__/useStore.onNodesChange.select.test.ts"
      provides: "click-vs-drag RED test (D-22)"
      contains: "describe('onNodesChange select sync'"
    - path: "gui/src/store/__tests__/useStore.addEdge.bcport.test.ts"
      provides: "CR-01 RED test (D-20)"
      contains: "CR-01"
    - path: "gui/src/store/__tests__/useStore.setBCSymmetric.test.ts"
      provides: "CR-02 / CR-03 RED tests (D-21)"
      contains: "CR-02"
    - path: "gui/src/store/__tests__/promoteToSharedSource.test.ts"
      provides: "promote action RED test (D-07 / D-08)"
      contains: "promoteToSharedSource"
    - path: "gui/src/lib/__tests__/projectIO.anchors.test.ts"
      provides: "schema swap RED test (D-14)"
      contains: "anchors"
    - path: "gui/src/components/__tests__/StreamNode.anchor.test.tsx"
      provides: "anchor indicator RED test (D-13)"
      contains: "anchor-indicator"
  key_links:
    - from: "63.1-VALIDATION.md Wave 0 Requirements"
      to: "this plan's nine test files"
      via: "1:1 mapping per RESEARCH.md §Wave 0 Gaps"
      pattern: "test.ts|test.tsx"
---

<objective>
Materialize the Wave 0 test scaffolding listed in `63.1-VALIDATION.md` and `63.1-RESEARCH.md` §"Wave 0 Gaps". Every new behavior in this phase (selector, anchors slice, codegen, projectIO schema swap, CR-01, CR-02/CR-03, click-vs-drag, Promote, StreamNode anchor glyph) gets a failing test stub here so subsequent waves implement against red tests (Nyquist sampling discipline per `workflow.nyquist_validation`).

Purpose: Prove every decision (D-02, D-05, D-13, D-14, D-15, D-20, D-21, D-22, plus D-07/D-08) is measurable before implementation; downstream waves cannot ship without flipping these tests green.

Output: Nine new test files under `gui/src/{lib,store,components}/__tests__/` plus `gui/src/lib/selectors/__tests__/`. All RED.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-CONTEXT.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-VALIDATION.md
@CLAUDE.md
@gui/src/store/__tests__/useStore.bc.test.ts
@gui/src/lib/__tests__/codeGenerator.bc.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add five store-action RED test stubs (anchors, click-vs-drag, addEdge.bcport, setBCSymmetric, promoteToSharedSource)</name>
  <files>
    gui/src/store/__tests__/useStore.anchors.test.ts,
    gui/src/store/__tests__/useStore.onNodesChange.select.test.ts,
    gui/src/store/__tests__/useStore.addEdge.bcport.test.ts,
    gui/src/store/__tests__/useStore.setBCSymmetric.test.ts,
    gui/src/store/__tests__/promoteToSharedSource.test.ts
  </files>
  <read_first>
    gui/src/store/__tests__/useStore.bc.test.ts (lines 1-70 — imports + beforeEach reset boilerplate + makeChannelNode fixture; copy verbatim into each new file's header per PATTERNS.md §"Test files"),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Validation Architecture" Phase Requirements → Test Map; §"Wave 0 Gaps"; §"CR-01 fix" lines 624-654; §"CR-02/CR-03 fix" lines 658-714; §"Click-vs-drag selection fix" lines 716-744; §"Promote to shared source" lines 788-820),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"Test files" lines 903-945 for reset/fixture template),
    gui/src/store/useStore.ts (line ranges 1047-1062 for current addEdge BCPort branch, 886-903 for current onNodesChange, 1283-1303 for current setBCSymmetric — so tests can assert against the CURRENT state and fail meaningfully)
  </read_first>
  <action>Create each file with vitest `// @vitest-environment happy-dom` header, copy the `beforeEach` reset from useStore.bc.test.ts:1-31 but adapt for the NEW slice shape — drop `bcs: []` and `errorTagsByNodeId: {}`, add `anchors: {}`. Every test must `expect` the post-Wave-1/2/3/5/6 behavior so it FAILS today against the current useStore.ts. Stub contents per file: (1) `useStore.anchors.test.ts` — describe('anchors slice'): "setAnchor writes Record entry keyed by nodeId" (calls `setAnchor("n1",{portField:"port_in.P",value:1e5})` then expects `useStore.getState().anchors["n1"]` to equal entry), "clearAnchor deletes the entry" (set then clear, expects `Object.keys(anchors).length===0`), "setAnchor overwrites prior entry on same nodeId" (D-02 at-most-one semantics), "setAnchor pushes an undo snapshot" (expects `useStore.getState()._undoPast.length===1` per Pitfall 4); (2) `useStore.onNodesChange.select.test.ts` — describe('onNodesChange select sync (D-22)'): "drag-select NodeChange.select with selected:true updates selectedNodeId" (dispatch `onNodesChange([{type:"select",id:"n2",selected:true}])` then expects `selectedNodeId === "n2"`); (3) `useStore.addEdge.bcport.test.ts` — describe('CR-01: addEdge BCPort branch'): "writes bcMode entry on canvas-drag of BC edge" — set up a WallTemperature node + a Channel node, call `addEdge({source:"wt1", sourceHandle:"out", target:"ch1", targetHandle:"T_wall_left"})`, expects `useStore.getState().bcMode[bcModeKey("ch1","T_wall_left")]` to equal `{mode:"source",sourceNodeId:"wt1"}`; "writes sibling bcMode entry when symmetric (default true)" using `T_wall_left` → also writes `T_wall_right` entry; (4) `useStore.setBCSymmetric.test.ts` — describe('CR-02: symmetric ON with leftEntry undefined + rightEntry defined deletes right'): seed `bcMode[ch1::T_wall_right]={mode:"value",value:300}`, call `setBCSymmetric("ch1","T_wall",true)`, expects `bcMode[bcModeKey("ch1","T_wall_right")]` to be undefined; describe('CR-03: symmetric ON mirrors source-mode AND adds the BC edge for the right side'): seed `bcMode[ch1::T_wall_left]={mode:"source",sourceNodeId:"wt1"}`, no right entry, call `setBCSymmetric("ch1","T_wall",true)`, expects both `bcMode[ch1::T_wall_right]` exists AND `edges.some(e => e.target==="ch1" && e.targetHandle==="T_wall_right" && e.type==="bcEdge")`; (5) `promoteToSharedSource.test.ts` — describe('promoteToSharedSource (D-07/D-08)'): "spawns WT node, seeds n from consumer, writes source-mode bcMode entry, materializes BC edge" — seed a Channel with `n:4`, call `promoteToSharedSource("ch1","T_wall_left")`, expects (a) a new WallTemperature node exists in `nodes` with `parameters.n===4`, (b) its position is `{x: consumer.x - 160, y: consumer.y - 40}` per RESEARCH A6, (c) `bcMode[ch1::T_wall_left].mode==="source"`, (d) a `type:"bcEdge"` edge exists from the new node to ch1. Use `bcModeKey` import from `@/lib/bcMode`. Type-only references to `AnchorEntry` may use inline `{ portField, value }` shape until `gui/src/lib/anchors.ts` lands in Wave 2 — add `// @ts-expect-error: anchors slice lands in Plan 03` directly above the `useStore.setState({...anchors: {}})` line if tsc complains.</action>
  <verify>
    <automated>cd gui && npx vitest run src/store/__tests__/useStore.anchors.test.ts src/store/__tests__/useStore.onNodesChange.select.test.ts src/store/__tests__/useStore.addEdge.bcport.test.ts src/store/__tests__/useStore.setBCSymmetric.test.ts src/store/__tests__/promoteToSharedSource.test.ts 2>&1 | grep -E "FAIL|passed|failed" | head -30</automated>
  </verify>
  <acceptance_criteria>
    File `gui/src/store/__tests__/useStore.anchors.test.ts` exists and contains the literal string `describe('anchors slice'` (or `describe("anchors slice"`).
    File `gui/src/store/__tests__/useStore.onNodesChange.select.test.ts` exists and contains `selectedNodeId`.
    File `gui/src/store/__tests__/useStore.addEdge.bcport.test.ts` exists and contains `CR-01`.
    File `gui/src/store/__tests__/useStore.setBCSymmetric.test.ts` exists and contains BOTH `CR-02` and `CR-03`.
    File `gui/src/store/__tests__/promoteToSharedSource.test.ts` exists and contains `promoteToSharedSource`.
    `cd gui && npx vitest run src/store/__tests__/useStore.anchors.test.ts src/store/__tests__/useStore.onNodesChange.select.test.ts src/store/__tests__/useStore.addEdge.bcport.test.ts src/store/__tests__/useStore.setBCSymmetric.test.ts src/store/__tests__/promoteToSharedSource.test.ts` reports at least one FAIL (RED is correct — implementation lands in Wave 1/2/5/6).
  </acceptance_criteria>
  <done>All five files committed; vitest shows them as failing (red) tests for the unimplemented behaviors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add four RED test stubs for selector, codegen, projectIO, and StreamNode anchor indicator</name>
  <files>
    gui/src/lib/selectors/__tests__/nodeErrors.test.ts,
    gui/src/lib/__tests__/codeGenerator.anchors.test.ts,
    gui/src/lib/__tests__/projectIO.anchors.test.ts,
    gui/src/components/__tests__/StreamNode.anchor.test.tsx
  </files>
  <read_first>
    gui/src/lib/__tests__/codeGenerator.bc.test.ts (existing codegen test as fixture/import template),
    gui/src/lib/__tests__/projectIO.scp.test.ts (existing projectIO test as roundtrip template),
    gui/src/components/__tests__/StreamNode.test.tsx (existing render-test template; happy-dom + render + screen.getByTestId pattern),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Anchors slice shape" lines 511-525 for AnchorEntry; §"Codegen contract change" lines 569-622 for emission format; §"Anchor indicator (D-13)" lines 758-786; §"Selector-derived validator" lines 248-313 for selectNodeErrors signature),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md (§"Canvas Anchor Indicator" — `data-testid="anchor-indicator"`, `aria-label="Pressure anchor"`, w-1.5 h-1.5 size)
  </read_first>
  <action>Create the four test files with RED expectations: (1) `gui/src/lib/selectors/__tests__/nodeErrors.test.ts` — describe('selectNodeErrors (D-15)'): "returns ['bc-n-mismatch'] when consumer's n differs from source's n" — build a state object `{nodes:[Channel n=5, WallTemperature n=3], edges:[], bcMode:{['ch1::T_wall_left']:{mode:'source',sourceNodeId:'wt1'}}, bcSymmetric:{}, anchors:{}}`, call `selectNodeErrors(state,'ch1')`, expect array to include `'bc-n-mismatch'`; "returns [] when n matches"; "returns [] when consumer mode is 'value' (not 'source')"; "auto-clears when underlying n is corrected" (D-23) — same state but n flipped to equal, expect `[]`. Import `selectNodeErrors` from `@/lib/selectors/nodeErrors` (the module doesn't exist yet — that's the RED). (2) `gui/src/lib/__tests__/codeGenerator.anchors.test.ts` — describe('codeGenerator anchors emit (D-05)'): "emits `${instanceName}.${portField} ~ ${value}` for each anchors[nodeId] entry"  — call `generateCode(nodes,[],{anchors:{n1:{portField:"port_in.P",value:1e5}}}, getComponentDef, undefined, {bcMode:{},bcSymmetric:{}})`, expect the returned string to contain `pump1.port_in.P ~ 1.0e5` (use formatReal — see codeGenerator.ts:1228); "emits assembly-prefixed path inside a fuel_assembly" — set up a node tagged with assemblyName and expect `${assemblyName}.${instanceName}.port_out.P ~ value`; "emits no anchor lines when anchors is empty" — passes `{anchors:{}}`, expects no line containing `.port_in.P ~` or `.port_out.P ~`. The signature `generateCode(nodes, edges, CodegenAnchorsState, ...)` doesn't exist yet — that's the RED. (3) `gui/src/lib/__tests__/projectIO.anchors.test.ts` — describe('projectIO anchors schema (D-14)'): "serialize emits `anchors` Record field (not `bcs` array)" — call `serializeProject` with a state that has `anchors:{n1:{portField:"port_in.P",value:1e5}}`, parse the resulting JSON, expect `parsed.anchors` to equal the input AND `parsed.bcs` to be undefined; "deserialize reads `anchors` field, defaults to {} when absent"; "deserialize does NOT read legacy `bcs` field" (D-14 no-fallback) — feed JSON with both `bcs:[...]` and `anchors:{}`, expect resulting state's anchors to be `{}` (anchors wins; bcs ignored). (4) `gui/src/components/__tests__/StreamNode.anchor.test.tsx` — describe('StreamNode anchor indicator (D-13)'): "renders an element with data-testid='anchor-indicator' when anchors[id].portField matches a FlowPort handle" — seed store with a Pump node n1 and `anchors={n1:{portField:"port_in.P",value:1e5}}`, render `<StreamNode id="n1" data={{componentId:"Pump",instanceName:"pump1",parameters:{}}} selected={false}/>` inside `<ReactFlowProvider>`, expect `screen.getByTestId("anchor-indicator")` to be in the document AND `screen.getByLabelText("Pressure anchor")` to be in the document; "does NOT render the indicator when no anchor entry exists for this node" — anchors={}, expects `screen.queryByTestId("anchor-indicator")` to be null.</action>
  <verify>
    <automated>cd gui && npx vitest run src/lib/selectors/__tests__/nodeErrors.test.ts src/lib/__tests__/codeGenerator.anchors.test.ts src/lib/__tests__/projectIO.anchors.test.ts src/components/__tests__/StreamNode.anchor.test.tsx 2>&1 | grep -E "FAIL|passed|failed" | head -30</automated>
  </verify>
  <acceptance_criteria>
    File `gui/src/lib/selectors/__tests__/nodeErrors.test.ts` exists and contains `selectNodeErrors`.
    File `gui/src/lib/__tests__/codeGenerator.anchors.test.ts` exists and contains `port_in.P ~` (as expected emission substring).
    File `gui/src/lib/__tests__/projectIO.anchors.test.ts` exists and contains both `anchors` and the no-fallback assertion text matching `bcs`.
    File `gui/src/components/__tests__/StreamNode.anchor.test.tsx` exists and contains `anchor-indicator`.
    `cd gui && npx vitest run src/lib/selectors/__tests__/nodeErrors.test.ts src/lib/__tests__/codeGenerator.anchors.test.ts src/lib/__tests__/projectIO.anchors.test.ts src/components/__tests__/StreamNode.anchor.test.tsx` reports at least one FAIL (RED is correct).
  </acceptance_criteria>
  <done>All four files committed; vitest reports them red (selector module missing; codegen signature wrong; projectIO field swap not yet done; StreamNode indicator not yet rendered).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Local desktop Tauri app; this plan only adds test files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1.01-01 | N/A | test scaffolding | N/A | No new code paths; only test stubs that fail until implementation lands. No data flow, no input handling. |
</threat_model>

<verification>
- Nine new files exist under expected paths.
- All nine files import `vitest` describe/it/expect.
- `cd gui && npx vitest run` reports the new files as RED (one or more failures per file) — implementation lands in subsequent waves.
- `cd gui && npx tsc --noEmit` may produce errors on the new tests referencing yet-to-exist modules (`@/lib/anchors`, `@/lib/selectors/nodeErrors`, new useStore actions); that's acceptable for Wave 0 (the tests are explicit RED scaffolding). If tsc errors block development workflow, suppress per-file with `// @ts-nocheck` at the top of each new test file with a TODO comment naming the implementing plan.
</verification>

<success_criteria>
- Nine new test files exist at the paths in `files_modified`.
- Each file's `describe` block references the decision ID it covers (e.g., `// D-15`, `// CR-01 (D-20)`).
- Running the targeted vitest invocations from `<verify>` shows RED tests (failures are expected and required).
- No production source code modified (this is test-scaffolding only).
</success_criteria>

<output>
After completion, create `.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-01-SUMMARY.md`
</output>
