---
phase: 63.1
plan: 06
type: execute
wave: 4
depends_on: ["63.1-03", "63.1-04", "63.1-05"]
files_modified:
  - gui/src/components/sidebar/AnchorsSection.tsx
  - gui/src/components/sidebar/SidebarPanel.tsx
autonomous: true
requirements:
  - D-04
  - D-09
  - D-10
requirements_addressed:
  - D-04
  - D-09
  - D-10
must_haves:
  truths:
    - "Right-sidebar BCs tab renders an Anchors section above External Inputs (D-09)"
    - "Anchors section only renders when the selected component has a FlowPort (D-04)"
    - "BCs tab remains in the right sidebar next to Properties (D-10) — no relocation"
    - "Empty state shows 'No anchor set' + '+ Add anchor' button; populated state shows port-field Select + NumericField + Clear anchor button (UI-SPEC verbatim)"
    - "Anchor value editor uses the project's NumericField primitive (Pattern G) and calls setAnchor/clearAnchor from the anchors slice"
  artifacts:
    - path: "gui/src/components/sidebar/AnchorsSection.tsx"
      provides: "Per-component Anchors section UI"
      exports: ["default AnchorsSection"]
      min_lines: 60
    - path: "gui/src/components/sidebar/SidebarPanel.tsx"
      provides: "ComponentTabs BCs body extended with AnchorsSection + Separator; hasBCs gate generalized"
      contains: "AnchorsSection"
  key_links:
    - from: "AnchorsSection.tsx"
      to: "useStore.setAnchor / useStore.clearAnchor (anchors slice)"
      via: "useStore action dispatch"
      pattern: "setAnchor|clearAnchor"
    - from: "SidebarPanel.tsx ComponentTabs"
      to: "AnchorsSection rendered only when hasFlowPort is true"
      via: "registry ports[].type === 'FlowPort' check"
      pattern: "FlowPort"
---

<objective>
Wave 3 — BCs-tab layout. Add the new AnchorsSection.tsx component (per UI-SPEC §"Anchors Section — Row Anatomy") and wire it into SidebarPanel.tsx's ComponentTabs body so the right-sidebar BCs tab renders Anchors above External Inputs (D-09). Gate Anchors on FlowPort presence (D-04). Broaden the hasBCs tab visibility gate from `external_inputs.length > 0` to `hasFlowPort || hasExternalInputs`.

Purpose: Single visible surface for anchors. The Plan 03 anchors slice has no UI yet — this plan delivers it. UI-SPEC strings + spacing are load-bearing; this plan implements them verbatim.

Output: New AnchorsSection.tsx; SidebarPanel.tsx ComponentTabs body extended.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-CONTEXT.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md
@.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-05-SUMMARY.md
@gui/src/components/sidebar/SidebarPanel.tsx
@gui/src/components/sidebar/BCsTabForm.tsx
@gui/src/components/sidebar/NumericField.tsx
@gui/src/components/ui/select.tsx
@gui/src/components/ui/button.tsx
@gui/src/components/ui/separator.tsx
@gui/src/lib/anchors.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AnchorsSection.tsx (empty + populated states; gated on FlowPort)</name>
  <files>gui/src/components/sidebar/AnchorsSection.tsx</files>
  <behavior>
    - Renders nothing (null) when the selected component has no FlowPort (D-04)
    - Renders empty state when `useStore(s => s.anchors[nodeId])` is undefined: muted "No anchor set" + "+ Add anchor" outline button
    - Clicking "+ Add anchor" calls setAnchor(nodeId, {portField:"port_in.P", value:0}) to enter populated state
    - Renders populated state when anchors[nodeId] exists: Port label + Select (port_in.P / port_out.P) + Pressure label + NumericField + "Clear anchor" ghost button
    - Port Select onValueChange dispatches setAnchor with the new portField and current value
    - NumericField onChange dispatches setAnchor with the same portField and new value
    - Clear button dispatches clearAnchor(nodeId)
    - All copy strings verbatim from UI-SPEC §"Copywriting Contract"
  </behavior>
  <read_first>
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md (§"Anchors Section — Row Anatomy" full section incl. State A empty + State B populated diagrams; §"Copywriting Contract" — exact strings; §"Spacing Scale" — only existing 4/8/16 tokens),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (no new content needed; Pattern G NumericField use),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"AnchorsSection.tsx (NEW)" lines 579-635 — analog excerpts from BCPanel.tsx port-select + BCsTabForm.ValueModeEditor with NumericField),
    gui/src/components/sidebar/BCsTabForm.tsx (lines 170-174 empty-state copy idiom; lines 479-496 ValueModeEditor — NumericField wiring; lines 271-273 section header style),
    gui/src/components/sidebar/NumericField.tsx (props: {param: Parameter, value, onChange}),
    gui/src/components/ui/select.tsx (Select / SelectTrigger / SelectContent / SelectItem / SelectValue),
    gui/src/components/ui/button.tsx (variants "outline" / "ghost" / sizes "sm"),
    gui/src/lib/anchors.ts (AnchorEntry shape; the portField literal union),
    gui/src/store/useStore.ts (setAnchor, clearAnchor — Plan 03 exports)
  </read_first>
  <action>Create gui/src/components/sidebar/AnchorsSection.tsx with default export `AnchorsSection({ nodeId, component }: { nodeId: string; component: ComponentDefinition })`. Imports: useStore from `@/store/useStore`; AnchorEntry type from `@/lib/anchors`; Select/SelectTrigger/SelectContent/SelectItem/SelectValue from `@/components/ui/select`; Button from `@/components/ui/button`; NumericField from `./NumericField`; ComponentDefinition type from `@/registry/types`; Parameter type from `@/registry/types`. Body: Step 1 — `const hasFlowPort = component.ports.some(p => p.type === "FlowPort")`. If false, return null (D-04 gate). Step 2 — `const entry = useStore(useCallback((s) => s.anchors[nodeId], [nodeId]))` (primitive-ish; though entry is an object; this selector is acceptable because we want re-render on entry change — if Pitfall 1 fires, split into two primitive selectors: one for `s.anchors[nodeId]?.portField` and one for `s.anchors[nodeId]?.value`). Step 3 — read setAnchor + clearAnchor with `const setAnchor = useStore(s => s.setAnchor); const clearAnchor = useStore(s => s.clearAnchor)` (action selectors are stable — zustand pattern). Step 4 — empty-state branch (entry === undefined): render `<p className="text-xs text-muted-foreground mb-[8px]">No anchor set</p>` then `<Button variant="outline" size="sm" onClick={() => setAnchor(nodeId, { portField: "port_in.P", value: 0 })}>+ Add anchor</Button>`. Step 5 — populated branch: container `<div className="flex flex-col gap-[8px] min-w-0">`. Inside: (a) `<Label className="text-[12px] font-medium leading-[1.4]">Port</Label>` followed by a Select with two options (port_in.P, port_out.P) — onValueChange calls `setAnchor(nodeId, { portField: v as "port_in.P" | "port_out.P", value: entry.value })`. The SelectTrigger className is `w-full`. (b) NumericField wired via a synthetic Parameter object — copy the pattern from BCsTabForm.ValueModeEditor (lines 479-496) — `{ name: "Pressure", type: "Real", required: true, positional: false, default: entry.value, unit: "Pa" }`, value=entry.value, onChange=`(v) => setAnchor(nodeId, { portField: entry.portField, value: typeof v === "number" ? v : entry.value })`. (c) `<Button variant="ghost" size="sm" onClick={() => clearAnchor(nodeId)} className="self-start">Clear anchor</Button>`. Step 6 — accessibility / test hooks: add `data-testid="anchors-section"` on the outer container; add `data-testid="anchor-add"` on the + Add anchor button; `data-testid="anchor-clear"` on the Clear button. Step 7 — confirm all copy strings exactly match UI-SPEC §Copywriting Contract: "No anchor set", "+ Add anchor", "Port", "Pressure", "Clear anchor".</action>
  <verify>
    <automated>cd gui && grep -c "export default function AnchorsSection\|export default AnchorsSection" src/components/sidebar/AnchorsSection.tsx; grep -c "No anchor set" src/components/sidebar/AnchorsSection.tsx; grep -c "+ Add anchor" src/components/sidebar/AnchorsSection.tsx; grep -c "Clear anchor" src/components/sidebar/AnchorsSection.tsx; grep -c "FlowPort" src/components/sidebar/AnchorsSection.tsx; grep -c "setAnchor\|clearAnchor" src/components/sidebar/AnchorsSection.tsx; npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    File `gui/src/components/sidebar/AnchorsSection.tsx` exists, is at least 60 lines (Step 1-7 cannot fit smaller).
    `grep -c "No anchor set" gui/src/components/sidebar/AnchorsSection.tsx` returns 1.
    `grep -c "+ Add anchor" gui/src/components/sidebar/AnchorsSection.tsx` returns 1.
    `grep -c "Clear anchor" gui/src/components/sidebar/AnchorsSection.tsx` returns 1.
    `grep -c "FlowPort" gui/src/components/sidebar/AnchorsSection.tsx` returns 1 or more (D-04 gate).
    `grep -c "setAnchor" gui/src/components/sidebar/AnchorsSection.tsx` returns 1 or more.
    `grep -c "clearAnchor" gui/src/components/sidebar/AnchorsSection.tsx` returns 1 or more.
    `grep -c "NumericField" gui/src/components/sidebar/AnchorsSection.tsx` returns 1 or more (Pattern G).
    `cd gui && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>AnchorsSection.tsx component file exists; gates on FlowPort; renders both states; copy strings match UI-SPEC; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SidebarPanel.tsx — broaden hasBCs gate; extend ComponentTabs BCs body</name>
  <files>gui/src/components/sidebar/SidebarPanel.tsx</files>
  <behavior>
    - hasBCs is `hasFlowPort || hasExternalInputs` (where hasFlowPort = component.ports.some(p => p.type === "FlowPort"))
    - `<TabsContent value="bcs">` body renders an "Anchors" h3 + AnchorsSection (when hasFlowPort), then optionally a Separator, then an "External Inputs" h3 + BCsTabForm (when hasExternalInputs)
    - Section headers use the existing h3 className from SidebarPanel.tsx:323 verbatim
    - Defensive empty: if neither hasFlowPort nor hasExternalInputs (theoretically the tab itself shouldn't render but defensive copy: "This component has no boundary conditions.")
  </behavior>
  <read_first>
    gui/src/components/sidebar/SidebarPanel.tsx (line 46 Separator import; line 152 current `const hasBCs = ...`; line 323 PROPERTIES h3 className; lines 356-398 ComponentTabs body — TabsContent value="bcs"; UI-SPEC lines 117-159 for the layout diagram + gating rules),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-UI-SPEC.md (§"BCs-Tab Layout" — ASCII diagram + gating rules; §"Copywriting Contract" — section header strings),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-RESEARCH.md (§"Two-section BCs tab (D-09)" lines 327-355),
    .planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-PATTERNS.md (§"SidebarPanel.tsx" lines 716-754 — exact JSX shape; Pattern E section header)
  </read_first>
  <action>Step A — imports: add `import AnchorsSection from "./AnchorsSection"`. The Separator import already exists at line 46. Step B — hasBCs gate (~line 152): replace `const hasBCs = (component.external_inputs?.length ?? 0) > 0` with: `const hasFlowPort = component.ports.some(p => p.type === "FlowPort"); const hasExternalInputs = (component.external_inputs?.length ?? 0) > 0; const hasBCs = hasFlowPort || hasExternalInputs;`. Step C — TabsContent value="bcs" body (~lines 356-398): replace the existing `<BCsTabForm component={component} nodeId={selectedNodeId} />` with the structure shown in PATTERNS lines 736-753 — top branch (hasFlowPort): `<h3 className="text-xs font-semibold uppercase tracking-wide text-muted-foreground leading-[1.3] mb-[8px]">Anchors</h3>` then `<AnchorsSection nodeId={selectedNodeId} component={component} />` then conditional Separator `{hasExternalInputs && <Separator className="my-[16px]" />}`; bottom branch (hasExternalInputs): `<h3 className="...">External Inputs</h3>` then existing `<BCsTabForm component={component} nodeId={selectedNodeId} />`. Defensive: if !hasFlowPort && !hasExternalInputs, render a single `<p className="text-xs text-muted-foreground">This component has no boundary conditions.</p>` (UI-SPEC line 144). Step D — verify the existing Properties tab body (`<TabsContent value="properties">`) is untouched; verify the Tabs strip itself is untouched (D-10). Step E — confirm by visual inspection that the JSX nests correctly: the conditional fragments use `<>...</>` per RESEARCH lines 337-354.</action>
  <verify>
    <automated>cd gui && grep -c "hasFlowPort" src/components/sidebar/SidebarPanel.tsx; grep -c "hasExternalInputs" src/components/sidebar/SidebarPanel.tsx; grep -c "AnchorsSection" src/components/sidebar/SidebarPanel.tsx; grep -c "\"Anchors\"\\|>Anchors<" src/components/sidebar/SidebarPanel.tsx; grep -c "External Inputs" src/components/sidebar/SidebarPanel.tsx; grep -c "This component has no boundary conditions" src/components/sidebar/SidebarPanel.tsx; npx tsc --noEmit 2>&1 | tail -10; npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "hasFlowPort" gui/src/components/sidebar/SidebarPanel.tsx` returns 2 or more (declaration + use).
    `grep -c "hasExternalInputs" gui/src/components/sidebar/SidebarPanel.tsx` returns 2 or more.
    `grep -c "hasBCs = hasFlowPort || hasExternalInputs" gui/src/components/sidebar/SidebarPanel.tsx` returns 1.
    `grep -c "AnchorsSection" gui/src/components/sidebar/SidebarPanel.tsx` returns 2 or more (import + JSX).
    `grep -c "External Inputs" gui/src/components/sidebar/SidebarPanel.tsx` returns 1 (the h3 label).
    `grep -c ">Anchors<" gui/src/components/sidebar/SidebarPanel.tsx` returns 1.
    `cd gui && npx tsc --noEmit` exits 0.
    `cd gui && npx vitest run` reports zero new failures vs Plan 04 baseline.
  </acceptance_criteria>
  <done>SidebarPanel.tsx ComponentTabs body renders the two-section BCs body with correct gating; tsc + vitest green modulo expected RED stubs.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1.06-01 | Tampering | AnchorsSection NumericField input | mitigate | NumericField wraps validateReal (existing helper at gui/src/lib/validation.ts:19-24); invalid numeric strings show aria-invalid red border and do not propagate to setAnchor. ASVS V5 satisfied. |
</threat_model>

<verification>
- AnchorsSection.tsx renders empty + populated states with all UI-SPEC strings.
- AnchorsSection is gated on FlowPort presence.
- SidebarPanel.tsx ComponentTabs BCs body renders both sections with the correct gating.
- BCs tab remains in the right sidebar (D-10 not violated — no relocation).
- tsc + vitest green modulo documented Plan-01 RED stubs.
</verification>

<success_criteria>
- D-04 / D-09 / D-10 implemented in the right sidebar.
- The "two-section BCs tab" mental model is now real; a user selecting a Pump sees Anchors-only, selecting a Channel sees Anchors + External Inputs, selecting a WallTemperature sees External-Inputs-only.
</success_criteria>

<output>
After completion, create `.planning/phases/63.1-bc-architecture-rework-unified-bcs-tab/63.1-06-SUMMARY.md`
</output>
