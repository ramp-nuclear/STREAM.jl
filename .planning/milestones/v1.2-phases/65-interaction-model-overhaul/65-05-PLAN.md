---
phase: 65-interaction-model-overhaul
plan: 05
type: execute
wave: 3
depends_on: [65-03, 65-04]
files_modified:
  - gui/src/components/canvasMenus/NodeContextMenu.tsx
  - gui/src/components/canvasMenus/EdgeContextMenu.tsx
  - gui/src/components/canvasMenus/CanvasContextMenu.tsx
  - gui/src/components/canvasMenus/AddComponentSubmenu.tsx
  - gui/src/components/sidebar/InstanceNameField.tsx
  - gui/src/components/CanvasPanel.tsx
  - gui/src/components/canvasMenus/__tests__/contextMenus.test.tsx
autonomous: false
requirements: []
tags: [gui, context-menu, shadcn, canvas, phase-65]

must_haves:
  truths:
    - "Decisions addressed: D-11 (shadcn/Radix ContextMenu component reused; canvas-pane uses onPaneContextMenu + shadcn Popover) and D-14 (action scope per §3.5 — Show errors HIDDEN until Phase 71, Auto-Layout grayed-out '(future)' stub, Show generated Julia code falls back to opening Code panel)."
    - "Right-clicking a node opens a context menu containing: Delete, Duplicate, Rename, Show generated Julia code (stub — selects all in Code panel), Show errors (HIDDEN until Phase 71 — does not render)."
    - "Right-clicking a hydraulic edge or a BC edge opens a context menu with Delete (and Show errors HIDDEN per D-14)."
    - "Right-clicking on the empty canvas pane opens a menu with Paste, Auto-Layout (grayed-out, label 'Auto-Layout (future)'), and Add Component › submenu mirroring the toolbox categories."
    - "Selecting Delete on the node menu removes the node + dependent edges (single undo step)."
    - "Selecting Duplicate fires duplicateSelection (Plan 04 action) on the right-clicked node."
    - "Selecting Rename triggers an inline rename — either by setting selectedNodeId + focusing the existing instanceName field in the sidebar, OR by opening a small input popover. Either path is acceptable as long as the user can edit the name and Enter commits."
    - "Selecting Paste on the canvas menu fires pasteFromClipboard."
    - "Selecting Add Component › <category> › <component> drops a new instance at the right-click click position (same coordinates the menu opened at)."
    - "The menu closes on outside click, Esc, or any item activation (Radix default)."
  artifacts:
    - path: "gui/src/components/canvasMenus/NodeContextMenu.tsx"
      provides: "Stateless component receiving {nodeId, x, y, onClose} props; renders shadcn ContextMenuContent."
    - path: "gui/src/components/canvasMenus/EdgeContextMenu.tsx"
      provides: "Same shape for edges."
    - path: "gui/src/components/canvasMenus/CanvasContextMenu.tsx"
      provides: "Paste / Auto-Layout (disabled) / Add Component submenu wrapper."
    - path: "gui/src/components/canvasMenus/AddComponentSubmenu.tsx"
      provides: "Iterates registry components grouped by category — invokes addNode on selection."
    - path: "gui/src/components/CanvasPanel.tsx"
      provides: "Renders the three menu components positioned by rcMenu.state.screenX/Y inside a Popover anchored to the canvas container; consumes rcMenu.state.kind to switch which menu renders."
  key_links:
    - from: "CanvasPanel.tsx rcMenu.state.kind switch"
      to: "NodeContextMenu / EdgeContextMenu / CanvasContextMenu"
      via: "conditional render based on kind === 'node' | 'edge' | 'pane'"
      pattern: "rcMenu\\.state\\.kind"
    - from: "NodeContextMenu Delete item"
      to: "useStore.removeNode(nodeId)"
      via: "onSelect handler"
      pattern: "removeNode\\("
    - from: "CanvasContextMenu Paste item"
      to: "useStore.pasteFromClipboard()"
      via: "onSelect handler"
      pattern: "pasteFromClipboard\\("
---

<objective>
Wire the shadcn/Radix ContextMenu primitives to the three canvas surfaces
(node / edge / empty pane) so that the rcMenu state plumbed by Plan 03 +
the clipboard actions implemented in Plan 04 become discoverable through
right-click. Item inventories follow D-14 verbatim: hidden (not grayed)
"Show errors" until Phase 71; Auto-Layout grayed-out with explicit
"(future)" suffix; Add Component submenu mirrors the toolbox.

This plan is the FIRST surface where Plans 03 and 04 become user-visible.
It depends on both: Plan 03 supplies the right-click event + screenXY +
target ID; Plan 04 supplies pasteFromClipboard / duplicateSelection /
copySelection (cut not needed in menus — keyboard-only per §3.5).

Purpose: deliver the §3.5 context menu spec verbatim with a focus-trapped,
keyboard-navigable, dark-mode-styled Radix component (D-11 — reuse the
existing `gui/src/components/ui/context-menu.tsx` already proven by
ResourcesTreePanel + ResourceRow in Phase 62).

Output: three new ContextMenuContent components + a small Popover wrapper
in CanvasPanel.tsx that positions them at the right-click coordinates.
Vitest coverage for each menu's item set + click-through behavior.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-interaction-model-overhaul/65-CONTEXT.md
@.planning/phases/65-interaction-model-overhaul/65-03-PLAN.md
@.planning/phases/65-interaction-model-overhaul/65-04-PLAN.md
@.planning/notes/gui-redesign-design-decisions.md
@gui/src/components/ui/context-menu.tsx
@gui/src/components/resources/ResourceRow.tsx

<interfaces>
<!-- shadcn ContextMenu primitives — see gui/src/components/ui/context-menu.tsx -->
```typescript
import {
  ContextMenu,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuSub,
  ContextMenuSubTrigger,
  ContextMenuSubContent,
  ContextMenuTrigger,
  ContextMenuSeparator,
} from "@/components/ui/context-menu";
```

<!-- Phase 62 integration precedent — ResourceRow.tsx lines 292-304 wraps
     its row trigger with <ContextMenuTrigger asChild> + emits items
     including a destructive Delete. Copy the visual idiom (variant="destructive"
     on Delete; muted look on disabled items). -->

<!-- rcMenu state (from Plan 03 useRightClickContextMenu) -->
```typescript
interface ContextMenuState {
  kind: "pane" | "node" | "edge" | null;
  targetId: string | null;
  screenX: number;
  screenY: number;
}
```

<!-- Registry — for Add Component submenu -->
```typescript
import { getAllComponents } from "@/registry";   // existing — used by ToolboxPanel
// Returns ComponentDefinition[] with .id, .name, .category (per Phase 61 schema).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NodeContextMenu + EdgeContextMenu + their menu items</name>
  <files>gui/src/components/canvasMenus/NodeContextMenu.tsx, gui/src/components/canvasMenus/EdgeContextMenu.tsx</files>
  <read_first>
    - gui/src/components/ui/context-menu.tsx (exported primitives — confirm naming)
    - gui/src/components/resources/ResourceRow.tsx lines 280-310 (integration precedent — copy the className conventions and the destructive Delete idiom)
    - gui/src/store/useStore.ts lines 1106-1126 (removeNode shape) and the Plan 04 duplicateSelection action
    - .planning/notes/gui-redesign-design-decisions.md §3.5 lines 485-489 (menu inventories)
    - .planning/phases/65-interaction-model-overhaul/65-CONTEXT.md D-14 (action scope verbatim)
  </read_first>
  <action>
    Create gui/src/components/canvasMenus/NodeContextMenu.tsx:
    1. Default-export `NodeContextMenu({nodeId, onClose}: {nodeId: string; onClose: () => void})`. It renders ONLY a `<ContextMenuContent>` (the parent Popover handles positioning).
    2. Items in order:
       - **Rename (W7 lock)** — onSelect:
         - The rename target is the `InstanceNameField` component at `gui/src/components/sidebar/InstanceNameField.tsx` (default export, props `{value, onChange}` per line 13-18). It is consumed by `gui/src/components/sidebar/SidebarPanel.tsx` line 170 with the binding `value={data.instanceName}` and `onChange={(name) => updateNodeParams(selectedNodeId, { instanceName: name })}`. The component renders an `<Input>` from `@/components/ui/input`.
         - Behavior: `useStore.getState().selectNode(nodeId)` (so the sidebar opens to this node, exposing the field); then `window.dispatchEvent(new CustomEvent('stream:focus-instance-name', {detail: {nodeId}}))`; then `onClose()`.
         - The listener for that event lives in `InstanceNameField.tsx` itself (Task 2 of this plan) — NOT in `SidebarPanel.tsx`. This co-locates the focus behavior with the input it operates on. The input must carry `data-instance-name-input` on the underlying `<input>` element (Task 2 wires it).
       - **Duplicate** — onSelect: `useStore.getState().selectNode(nodeId)` (so duplicate has a selection), then `useStore.getState().duplicateSelection()`, then onClose.
       - **Show generated Julia code** — onSelect (W8 lock):
         - The bottom panel in v0.8 has ONLY ONE tab (CodePreview rendering generated Julia — see `gui/src/components/BottomPanel.tsx`). The relevant store action is `toggleBottomPanel` (declared at `gui/src/store/useStore.ts` line 207, implemented line 1730). It is a pure toggle — opens if closed, closes if open.
         - Behavior: if `useStore.getState().bottomPanelOpen === false`, call `useStore.getState().toggleBottomPanel()` (which sets it to `true`). If already open, leave it open (do NOT toggle off — that would be hostile UX for a "show me" action). Inline this guard rather than always calling toggle.
         - Also dispatch `window.dispatchEvent(new CustomEvent('stream:show-code-for', {detail: {nodeId}}))` for future scroll-to-section wiring (Phase 66 listener — add a `// TODO: Phase 66 — listen to stream:show-code-for and scroll the CodePreview to the matching section` comment). Phase 65 does NOT implement the scroll target.
         - Then call onClose.
       - **Show errors for this component** — HIDDEN per D-14. Do NOT render this item until a future Phase 71 validation surface exists. Add a code comment `// Phase 71: render Show errors item when validation state exists for nodeId`.
       - **Separator**.
       - **Delete** — `variant="destructive"`. onSelect: `useStore.getState().removeNode(nodeId)`, then onClose.
    3. Use the shadcn `ContextMenuItem` for each entry. No icons (visual polish is Phase 72).

    Create gui/src/components/canvasMenus/EdgeContextMenu.tsx:
    4. Default-export `EdgeContextMenu({edgeId, onClose}: {edgeId: string; onClose: () => void})`.
    5. Items:
       - **Delete** — destructive variant. onSelect: filter the edge out of state via the existing `removeEdge`-equivalent path. (Check store: there's a `setEdges` or use `onEdgesChange` with a removal change. The cleanest approach: `useStore.getState().onEdgesChange([{ id: edgeId, type: 'remove' }])`.) Then onClose.
       - **Show errors for this connection** — HIDDEN per D-14 (same comment as nodes).
    6. NO other items per §3.5 spec.

    Keep both files small (≤ 80 lines each). No own state — purely render the items wired to store actions + onClose.
  </action>
  <verify>
    <automated>cd gui && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'ContextMenuItem.*Delete|onSelect.*removeNode' gui/src/components/canvasMenus/NodeContextMenu.tsx | grep -c .` returns ≥ 1.
    - `grep -nE 'duplicateSelection' gui/src/components/canvasMenus/NodeContextMenu.tsx | grep -c .` returns 1.
    - `grep -nE 'stream:focus-instance-name' gui/src/components/canvasMenus/NodeContextMenu.tsx | grep -c .` returns 1.
    - `grep -nE 'Show errors' gui/src/components/canvasMenus/NodeContextMenu.tsx` returns no ACTIVE matches (only a TODO/comment if present).
    - `grep -nE 'onEdgesChange|setEdges' gui/src/components/canvasMenus/EdgeContextMenu.tsx | grep -c .` returns ≥ 1.
    - Neither file imports from `@/store/useStore` more than the actions needed.
    - `cd gui && npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>
    Two stateless menu-content components exist; each maps menu items to the store actions delivered by Plan 04 + the existing removeNode/onEdgesChange paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: CanvasContextMenu + AddComponentSubmenu + sidebar rename listener</name>
  <files>gui/src/components/canvasMenus/CanvasContextMenu.tsx, gui/src/components/canvasMenus/AddComponentSubmenu.tsx, gui/src/components/sidebar/InstanceNameField.tsx</files>
  <read_first>
    - gui/src/components/ToolboxPanel.tsx (entire — to see the registry iteration pattern + how categories are rendered)
    - gui/src/registry/ index.ts (to confirm getAllComponents export)
    - gui/src/components/sidebar/InstanceNameField.tsx (W7-locked rename target — extend this file's default-exported function component with the focus-event listener; the underlying shadcn Input is at line 22-32 area, default-exported from line 18)
    - gui/src/components/sidebar/SidebarPanel.tsx line 170 (confirmation that InstanceNameField is rendered when a node is selected — no edits to this file)
    - gui/src/store/useStore.ts addNode (line 1051) to know the signature `addNode(componentId, position)`
  </read_first>
  <action>
    Create gui/src/components/canvasMenus/CanvasContextMenu.tsx:
    1. Default-export `CanvasContextMenu({flowPosition, onClose}: {flowPosition: {x:number; y:number}; onClose: () => void})`.
    2. Items:
       - **Paste** — onSelect: `useStore.getState().pasteFromClipboard()`, then onClose. (Note: paste uses Plan 04's per-paste offset; it does NOT honor the right-click position — §3.5 doesn't require menu-paste to land at the click. If user feedback later wants menu-paste to land at the click, it's a follow-up. Add an inline comment: `// Phase 65 v1.2: paste lands at the offset Plan 04 computes, NOT at the right-click position. Revisit if user feedback demands click-anchored paste.`)
       - **Auto-Layout (future)** — `disabled` prop on ContextMenuItem; label string literal "Auto-Layout (future)". No onSelect handler.
       - **Separator**.
       - **Add Component** — a `<ContextMenuSub>` containing `<ContextMenuSubTrigger>Add Component</ContextMenuSubTrigger>` and `<ContextMenuSubContent><AddComponentSubmenu flowPosition={flowPosition} onClose={onClose} /></ContextMenuSubContent>`.

    Create gui/src/components/canvasMenus/AddComponentSubmenu.tsx:
    3. Default-export `AddComponentSubmenu({flowPosition, onClose}: {flowPosition: {x:number;y:number}; onClose: () => void})`.
    4. Import `getAllComponents` (or equivalent) from `@/registry`. Group by `category` (sort categories alphabetically, components alphabetically within each).
    5. For each category, render a `<ContextMenuSub>` with `<ContextMenuSubTrigger>{categoryName}</ContextMenuSubTrigger>` and `<ContextMenuSubContent>` containing one `<ContextMenuItem>` per component. Item label: the registry's display name (`component.name` or `component.id`, whichever ToolboxPanel uses).
    6. Each item's onSelect: `useStore.getState().addNode(component.id, flowPosition); onClose();` — `flowPosition` is the flow-coordinate position computed by the parent (CanvasPanel converts the menu's screen coords via `screenToFlowPosition` before passing them down).

    Modify gui/src/components/sidebar/InstanceNameField.tsx (W7 lock — confirmed at `gui/src/components/sidebar/InstanceNameField.tsx:18`):
    7. Add a `useRef<HTMLInputElement | null>(null)` ref. Attach `ref={inputRef}` to the existing `<Input>` element. Also add `data-instance-name-input` (used by tests/diagnostics; the ref is the focus mechanism).
    8. Add a `useEffect` that registers a window listener for `'stream:focus-instance-name'`. On event, call `inputRef.current?.focus()` and `inputRef.current?.select()`. The InstanceNameField is rendered only when there is a selected node (the SidebarPanel guard at line 170 wraps it conditionally), so we do not need to filter by `event.detail.nodeId` here — by the time the event fires, `selectNode(nodeId)` has already run from the menu's onSelect (W7 rename step) and re-mounted the SidebarPanel with the new selection. Cleanup on unmount.
    9. The `data-instance-name-input` attribute lands on the same `<input>` element rendered by the shadcn `<Input>` component (Input passes through HTML attributes via spread). NO modification to SidebarPanel.tsx — the focus wiring is internal to InstanceNameField.tsx.
    10. There is no fallback "no dedicated instanceName input" branch — the component is confirmed to exist at `gui/src/components/sidebar/InstanceNameField.tsx` and is rendered in SidebarPanel.tsx line 170 whenever a node is selected. If the executor finds the file missing, STOP and escalate — do not invent a fallback.

    NO new shadcn primitives needed — `ContextMenuSub` / `ContextMenuSubTrigger` / `ContextMenuSubContent` are already in `gui/src/components/ui/context-menu.tsx`. Confirm by grepping.
  </action>
  <verify>
    <automated>cd gui && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'pasteFromClipboard' gui/src/components/canvasMenus/CanvasContextMenu.tsx | grep -c .` returns 1.
    - `grep -nE 'Auto-Layout \(future\)' gui/src/components/canvasMenus/CanvasContextMenu.tsx | grep -c .` returns 1.
    - `grep -nE 'disabled' gui/src/components/canvasMenus/CanvasContextMenu.tsx | grep -c .` returns ≥ 1.
    - `grep -nE 'getAllComponents|getComponent' gui/src/components/canvasMenus/AddComponentSubmenu.tsx | grep -c .` returns ≥ 1.
    - `grep -nE 'addNode' gui/src/components/canvasMenus/AddComponentSubmenu.tsx | grep -c .` returns 1.
    - `grep -nE 'data-instance-name-input' gui/src/components/sidebar/InstanceNameField.tsx` returns ≥ 1.
    - `grep -nE "stream:focus-instance-name" gui/src/components/sidebar/InstanceNameField.tsx` returns 1.
    - `grep -cE 'data-instance-name-input|stream:focus-instance-name' gui/src/components/sidebar/SidebarPanel.tsx` returns 0 (the wiring is internal to InstanceNameField.tsx — SidebarPanel is unmodified).
    - `cd gui && npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>
    Canvas menu lists Paste + Auto-Layout (disabled, "future" suffix) + Add Component submenu mirroring the toolbox; the rename event flow exists end-to-end.
  </done>
</task>

<task type="auto">
  <name>Task 3: CanvasPanel renders the three menus via Popover wrapper</name>
  <files>gui/src/components/CanvasPanel.tsx, gui/src/components/canvasMenus/__tests__/contextMenus.test.tsx</files>
  <read_first>
    - gui/src/components/CanvasPanel.tsx (the version after Plan 03 — must have rcMenu wired)
    - gui/src/components/ui/popover.tsx (CONFIRMED PRESENT — exports Popover, PopoverTrigger, PopoverAnchor, PopoverPortal, PopoverContent — all backed by radix-ui's Popover.Root with controlled `open` support per the import at line 2)
    - gui/src/components/ui/context-menu.tsx (CONFIRMED — exports ContextMenu, ContextMenuTrigger, ContextMenuContent, ContextMenuItem, ContextMenuSub, ContextMenuSubTrigger, ContextMenuSubContent, ContextMenuSeparator. Radix ContextMenu does NOT support a controlled `open` prop at arbitrary screen coords — it anchors to whichever element receives the native right-click on its Trigger. We do NOT use ContextMenu's positioning; we re-use its STYLED ITEMS inside a Popover for the visual treatment.)
    - .planning/phases/65-interaction-model-overhaul/65-CONTEXT.md D-11 (ContextMenu + Popover pattern)
  </read_first>
  <behavior>
    Test cases (gui/src/components/canvasMenus/__tests__/contextMenus.test.tsx):
    - Render `<NodeContextMenu nodeId="n1" onClose={spy} />` inside a `<ContextMenu open><ContextMenuTrigger /></ContextMenu>` test harness. Assert items by text: "Rename", "Duplicate", "Show generated Julia code", "Delete". Assert "Show errors" is NOT in the DOM.
    - Click "Delete" → spy is called AND useStore.getState().nodes after the click filters out n1. (Use a fresh store with one pre-seeded node.)
    - Render EdgeContextMenu: items are "Delete" only.
    - Render CanvasContextMenu: items are "Paste", "Auto-Layout (future)" (disabled), "Add Component" (submenu trigger). Auto-Layout is rendered with aria-disabled.
  </behavior>
  <action>
    Modify gui/src/components/CanvasPanel.tsx:
    1. Import the three menu components: `import NodeContextMenu from './canvasMenus/NodeContextMenu';` etc.
    2. Import shadcn Popover primitives (or, if not in repo, render a `<div>` with `position: absolute; left: screenX; top: screenY; zIndex: 50;`).
    3. After the `</ReactFlow>` close tag (but inside the outer `<div ref={containerRef}>`), conditionally render the menu UI when `rcMenu.state.kind !== null`. Compute `flowPosition` for the pane case: `const flowPosition = rcMenu.state.kind === 'pane' ? screenToFlowPosition({x: rcMenu.state.screenX, y: rcMenu.state.screenY}) : null;`
    4. **Host pattern (W10 lock — Popover wrapper, NOT controlled ContextMenu):**
       - Radix `ContextMenu.Root` does NOT accept a controlled `open` prop at arbitrary screen coordinates — it auto-anchors to the right-clicked Trigger element. We therefore use `Popover` as the host (per D-11) and re-use shadcn `ContextMenuItem` / `ContextMenuSub*` / `ContextMenuSeparator` as the styled item primitives inside `PopoverContent`. The shadcn `ContextMenuContent` wrapper itself is replaced by `PopoverContent` (which carries the same `bg-popover p-1 rounded-md border shadow-md` aesthetic via the className in `gui/src/components/ui/popover.tsx` line 47-49).
       - Structure:
         ```tsx
         <Popover open={rcMenu.state.kind !== null} onOpenChange={(o) => { if (!o) rcMenu.close(); }}>
           <PopoverAnchor
             style={{
               position: 'fixed',
               left: rcMenu.state.screenX,
               top: rcMenu.state.screenY,
               width: 1,
               height: 1,
               pointerEvents: 'none',
             }}
             aria-hidden
           />
           <PopoverContent
             align="start"
             side="bottom"
             sideOffset={0}
             className="p-1 w-auto min-w-[8rem]"
             onEscapeKeyDown={() => rcMenu.close()}
             onPointerDownOutside={() => rcMenu.close()}
           >
             {rcMenu.state.kind === 'node' && <NodeContextMenu nodeId={rcMenu.state.targetId!} onClose={rcMenu.close} />}
             {rcMenu.state.kind === 'edge' && <EdgeContextMenu edgeId={rcMenu.state.targetId!} onClose={rcMenu.close} />}
             {rcMenu.state.kind === 'pane' && flowPosition && <CanvasContextMenu flowPosition={flowPosition} onClose={rcMenu.close} />}
           </PopoverContent>
         </Popover>
         ```
       - **Why this is correct:** PopoverAnchor is an invisible 1x1 fixed-positioned element at `(screenX, screenY)` — Radix's Floating UI integration anchors the PopoverContent's positioning to it via `side`/`sideOffset`/`align`. The Popover is controlled via `open` (D-11 compliance). Items inside use `<ContextMenuItem>` / `<ContextMenuSub>` from `@/components/ui/context-menu` for visual + keyboard-nav parity with ResourceRow's right-click menus.
    5. Inside each of the three sub-menu components (NodeContextMenu / EdgeContextMenu / CanvasContextMenu): use shadcn `<ContextMenuItem>` directly — they render as styled `<div role="menuitem">` elements that work fine inside a Popover. The Radix `ContextMenu.Root` parent context is NOT required for individual `ContextMenuItem`s to render (they don't reach into the Root context for styling). If a runtime error fires because `ContextMenuItem` requires a `ContextMenu.Root` ancestor (Radix sometimes enforces context), fall back to: wrap each menu component's content in `<ContextMenu><ContextMenuTrigger asChild><span hidden /></ContextMenuTrigger><ContextMenuContent forceMount>...</ContextMenuContent></ContextMenu>` and accept the dead Trigger as architectural cost. EXECUTOR: verify the simple `ContextMenuItem`-without-Root case works first (run a single test render); only fall back if it errors.
    6. The keyboard navigation (arrow keys, Enter to activate, Esc to close) is supplied by Popover + ContextMenuItem (both use Radix's roving focus / focus-trap when inside a Popover). Do NOT add a custom keyboard handler.

    Create gui/src/components/canvasMenus/__tests__/contextMenus.test.tsx with the cases enumerated in <behavior>.
  </action>
  <verify>
    <automated>cd gui && npx vitest run src/components/canvasMenus/__tests__/contextMenus.test.tsx && cd gui && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'NodeContextMenu|EdgeContextMenu|CanvasContextMenu' gui/src/components/CanvasPanel.tsx | grep -c .` returns ≥ 3.
    - `grep -nE 'rcMenu\.state\.kind' gui/src/components/CanvasPanel.tsx | grep -c .` returns ≥ 1.
    - `grep -nE 'rcMenu\.close' gui/src/components/CanvasPanel.tsx | grep -c .` returns ≥ 1.
    - `cd gui && npx vitest run src/components/canvasMenus/__tests__/contextMenus.test.tsx` exits 0 with ≥ 4 test cases passing.
    - `cd gui && npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>
    Right-clicking a node / edge / canvas opens the correct menu at the right-click coordinates; menu items dispatch the correct store actions; outside-click and Esc close the menu (Radix default).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual smoke — context menus end-to-end</name>
  <what-built>
    Three context menus (node, edge, canvas) wired through Radix +
    positioned at the right-click coordinates. Items dispatch the store
    actions delivered by Plans 03 and 04 + the existing removeNode /
    onEdgesChange paths.
  </what-built>
  <how-to-verify>
    1. `cd gui && npm run tauri dev`. Open or build a small project with at least 2 nodes + 1 edge.
    2. **Node menu — Delete:** right-click a node (quick click, no drag). EXPECT: menu appears with Rename / Duplicate / Show generated Julia code / Delete. "Show errors" is NOT present. Click Delete. EXPECT: node + its edges disappear. Ctrl+Z restores.
    3. **Node menu — Duplicate:** right-click another node, Duplicate. EXPECT: a new node at +20px offset (same lowest-free naming as Ctrl+D from Plan 04).
    4. **Node menu — Rename:** right-click a node, Rename. EXPECT: the sidebar focuses the instanceName field (cursor blinks in the field, contents selected). If the sidebar has no rename field yet, EXPECT at minimum: the node becomes selected and the sidebar opens to it (Phase 72 will add the inline rename UI).
    5. **Node menu — Show generated Julia code:** right-click, Show generated Julia code. EXPECT: no error; the bottom Code panel becomes the active tab OR a console log appears mentioning the nodeId (Phase 66 will deliver the full scroll-to-section behavior).
    6. **Edge menu — Delete:** right-click an edge (the path itself, not a node). EXPECT: menu appears with Delete only. Click Delete. EXPECT: edge disappears. Ctrl+Z restores.
    7. **Canvas menu — Paste:** Ctrl+C on a node first to populate clipboard. Then right-click on empty canvas. EXPECT: menu shows Paste / Auto-Layout (future) / Add Component ›. Auto-Layout is visibly grayed-out (cursor not-allowed style, no hover highlight). Click Paste. EXPECT: pasted node appears.
    8. **Canvas menu — Add Component:** right-click empty canvas. Hover Add Component. EXPECT: a submenu cascades showing toolbox categories (Sources, Hydraulic, Thermal, Resources, Reactor Physics or whatever the current registry exposes). Drill into one category. Click a component name. EXPECT: a new instance lands at the right-click position with a lowest-free name (e.g. pump_<lowest-free>).
    9. **Right-drag pan still works:** right-click + drag on empty canvas. EXPECT: pans the canvas; NO menu appears (Plan 03 disambiguation still active).
    10. **Esc closes menu:** open any menu, press Esc. EXPECT: menu closes.
    11. **Outside-click closes menu:** open any menu, click elsewhere. EXPECT: menu closes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any anomaly (e.g. "right-click on edge selects the wrong edge", "Add Component submenu has wrong categories").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — internal UI only) | Menus dispatch existing store actions; the only untrusted input is clipboard text on Paste, already mitigated in Plan 04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-07 | T (Tampering) | Add Component submenu — addNode call site | accept | componentId comes from registry iteration (trusted source); flowPosition is user-controlled but addNode already handles any position. No untrusted-input path. |

Low-risk UI state mutation, no untrusted input boundary.
</threat_model>

<verification>
- All three menu surfaces render the §3.5 item inventories verbatim — including the HIDDEN "Show errors" until Phase 71 and the grayed-out "Auto-Layout (future)" placeholder.
- Each item dispatches its store action; vitest covers click-through behavior.
- Manual smoke confirms positioning at right-click coords, outside-click close, Esc close, and that the right-drag pan from Plan 03 is unaffected.
</verification>

<success_criteria>
Behavior assertion: right-click on a node opens a 4-item menu (Rename / Duplicate / Show generated Julia code / Delete); right-click on an edge opens a 1-item menu (Delete); right-click on empty canvas opens a 3-item menu (Paste / Auto-Layout (future) / Add Component ›).
Source assertion: "Show errors" is NOT rendered as an active item anywhere (only TODO comments referencing Phase 71).
Source assertion: "Auto-Layout (future)" is rendered with `disabled` prop.
</success_criteria>

<output>
After completion, create `.planning/phases/65-interaction-model-overhaul/65-05-SUMMARY.md`.
</output>
