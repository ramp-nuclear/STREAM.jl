---
phase: 65-interaction-model-overhaul
plan: 04
type: execute
wave: 2
depends_on: [65-01, 65-03]
files_modified:
  - gui/src/lib/clipboard.ts
  - gui/src/lib/__tests__/clipboard.test.ts
  - gui/src/store/useStore.ts
  - gui/src/store/__tests__/clipboardActions.test.ts
  - gui/src/components/CanvasPanel.tsx
autonomous: true
requirements: []
tags: [gui, clipboard, copy-paste, keyboard, smart-name, phase-65]

must_haves:
  truths:
    - "Decisions addressed: D-15 (OS clipboard via navigator.clipboard.writeText/readText with JSON payload), D-16 (Ctrl+D separate in-memory path that does NOT touch OS clipboard), D-19 (paste verbatim — internal edges only, external edges silently dropped, resource refs preserved)."
    - "Selecting one or more nodes and pressing Ctrl+C writes a JSON payload to navigator.clipboard whose nodes have new component IDs preserved and resource references preserved verbatim."
    - "Ctrl+V parses the clipboard JSON, mints new UUIDs for every node and internal edge, places nodes at +20px/+20px offset from originals, and selects the freshly-pasted set."
    - "Multiple Ctrl+V in succession offset each successive paste so pasted nodes don't pile up: paste N lands at original + N*20 on both axes (B4 lock)."
    - "Ctrl+X copies + deletes selected nodes (with their incident edges removed exactly as removeNode does today)."
    - "Ctrl+D duplicates selection in-memory (no clipboard mutation) at a FIXED +20px offset with new UUIDs. Ctrl+D uses a separate code path that does NOT share state with paste — pressing Ctrl+D after multiple Ctrl+V still lands at +20 from the current selection (no +60 surprise — B4 lock)."
    - "Paste of [pump_1, channel_1] when those names exist yields [pump_2, channel_2] (lowest-free per name; if pump_3 also exists, paste yields pump_2 — lowest free)."
    - "Edges where ONLY ONE endpoint is in the selection are dropped silently from the clipboard payload (no toast, no error)."
    - "Pasting a node that references a Geometry resource UUID re-uses the SAME UUID — resources are never duplicated."
    - "Malformed clipboard text (not JSON, or JSON missing the expected shape) results in a silent no-op on Ctrl+V — no thrown exception, no toast."
  artifacts:
    - path: "gui/src/lib/clipboard.ts"
      provides: "ClipboardPayload type; smartParseAndIncrement(name, existingNames) lowest-free smart-name function; isClipboardPayload type guard."
      contains: "export function smartParseAndIncrement"
    - path: "gui/src/store/useStore.ts"
      provides: "Clipboard slice: copySelection, cutSelection, pasteFromClipboard, duplicateSelection actions."
      contains: "copySelection: () =>"
    - path: "gui/src/components/CanvasPanel.tsx"
      provides: "Ctrl+C/X/V/D keydown handlers wired into the existing useEffect keydown listener."
      contains: "e.key === \"c\""
  key_links:
    - from: "CanvasPanel.tsx keydown handler"
      to: "useStore copySelection / cutSelection / pasteFromClipboard / duplicateSelection"
      via: "useStore.getState().copySelection() etc."
      pattern: "copySelection\\(\\)|pasteFromClipboard\\(\\)"
    - from: "pasteFromClipboard"
      to: "smartParseAndIncrement"
      via: "name-by-name iteration over pasted nodes building a running existingNames set"
      pattern: "smartParseAndIncrement\\("
    - from: "smartParseAndIncrement"
      to: "nextInstanceName (Plan 01)"
      via: "shares lowest-free semantics — different prefix rule but same lowest-free guarantee"
      pattern: "lowest"
---

<objective>
**W6 decision:** the checker offered an optional split into 04a (clipboard.ts pure
module) + 04b (useStore wiring + CanvasPanel keys). Declined — the three tasks
here are tightly coupled (Task 1's smart-parse rule is consumed by Task 2's
paste-batch loop, and Task 3 is a 5-line keyboard wire that has no value
shipping separately). Total context cost estimate is ~35-40% (Task 1 ~10%,
Task 2 ~20%, Task 3 ~5%); within the 50% budget. Keeping cohesion.

Implement copy / cut / paste / duplicate with the smart-parse-and-increment
naming rule from §3.5, build the JSON clipboard payload shape per D-15, and
add the in-memory duplicate path that does NOT mutate the OS clipboard per
D-16. Wire Ctrl+C/X/V/D in the existing CanvasPanel keydown handler. Drop
external edges silently per D-19.

This plan depends on Plan 01 (naming retrofit) because paste names must use
the same lowest-free guarantee. The smart-parse function is a thin wrapper
around the same algorithm that handles the `<name>_<digits>` vs no-digit
distinction from §3.5 lines 505-533.

Purpose: deliver the four §3.5 keyboard shortcuts in the GUI; the deeper
"context menu reveals the same actions" wiring is Plan 05.

Output: a pure `gui/src/lib/clipboard.ts` module with smartParseAndIncrement
and the JSON payload type guard; four new actions on useStore; CanvasPanel
keydown wiring; vitest coverage for the naming edge cases and a
serialize/deserialize round-trip.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-interaction-model-overhaul/65-CONTEXT.md
@.planning/phases/65-interaction-model-overhaul/65-01-PLAN.md
@.planning/notes/gui-redesign-design-decisions.md
@.planning/phases/64-connection-routing/64-CONTEXT.md

<interfaces>
<!-- Clipboard payload shape -->
```typescript
export interface ClipboardPayload {
  __format: "stream-composer-clipboard";
  version: 1;
  nodes: Node[];           // ReactFlow nodes; data.instanceName + .data.parameters preserved
  edges: Edge[];           // internal edges only (both endpoints in nodes[])
}
```

<!-- Smart parse rule (§3.5 lines 505-533):
  Case 1: name ends in `_<digits>`        → increment digits, find next lowest-free with same base
          'pump_1' + {pump_1, pump_3}      → 'pump_2'    (lowest free with base 'pump')
          'top_pump_1' + {top_pump_1}      → 'top_pump_2'
  Case 2: name does NOT end in `_<digits>` → append `_2`, then `_3`, etc., lowest-free
          'pump' + {pump}                  → 'pump_2'
          'pump_v2' + {pump_v2}             → 'pump_v3'  (acceptable noise — v2 LOOKS like digits)
-->

<!-- From gui/src/store/useStore.ts: existing patterns
  - _pushSnapshot() before every mutating action (line 1052 etc.)
  - isDirty: true on every content mutation
  - addNode shape (line 1051-1077) — copy the data structure for new pasted nodes
  - removeNode (line 1106-1126) — cutSelection mirrors this for each node
-->

<!-- From gui/src/components/CanvasPanel.tsx line 171-201: the existing keydown handler.
     Ctrl+C/X/V/D additions belong INSIDE this same useEffect callback. Skip when a text input has focus (same guard at lines 184-194). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: clipboard.ts pure module + smart-parse-and-increment</name>
  <files>gui/src/lib/clipboard.ts, gui/src/lib/__tests__/clipboard.test.ts</files>
  <read_first>
    - .planning/notes/gui-redesign-design-decisions.md §3.5 lines 490-533 (copy/paste/cut/duplicate spec + smart-parse-and-increment)
    - .planning/phases/65-interaction-model-overhaul/65-CONTEXT.md §decisions D-15, D-16, D-17, D-18, D-19
    - gui/src/store/useStore.ts:317 (nextResourceName — algorithmic precedent)
    - .planning/phases/65-interaction-model-overhaul/65-01-PLAN.md (the new nextInstanceName function shape)
  </read_first>
  <behavior>
    Test cases for smartParseAndIncrement(originalName, existingNames):
    - ('pump_1', new Set(['pump_1']))                    → 'pump_2'
    - ('pump_1', new Set(['pump_1','pump_2']))           → 'pump_3'
    - ('pump_1', new Set(['pump_1','pump_3']))           → 'pump_2'   (lowest-free with base 'pump')
    - ('top_pump_1', new Set(['top_pump_1']))            → 'top_pump_2'
    - ('top_pump_1', new Set(['top_pump_1','top_pump_2'])) → 'top_pump_3'
    - ('pump', new Set(['pump']))                        → 'pump_2'
    - ('heated_channel', new Set(['heated_channel']))    → 'heated_channel_2'
    - ('pump_v2', new Set(['pump_v2']))                  → 'pump_v3'  (acceptable noise — rule treats trailing digits)
    - ('pump_1', new Set())                              → 'pump_1'   (no collision — keep original)
    - ('pump', new Set())                                → 'pump'     (no collision — keep original)

    Test cases for isClipboardPayload(value):
    - ({__format:'stream-composer-clipboard', version:1, nodes:[], edges:[]}) → true
    - ({}) → false
    - ('not json') → false
    - ({__format:'wrong', version:1, nodes:[], edges:[]}) → false
    - (null) → false
    - ({__format:'stream-composer-clipboard', version:2, nodes:[], edges:[]}) → false (version mismatch)
  </behavior>
  <action>
    Create gui/src/lib/clipboard.ts as a PURE module (no Tauri imports, no store imports, no React):
    1. Export `ClipboardPayload` interface per <interfaces> shape.
    2. Export `CLIPBOARD_FORMAT_TAG = 'stream-composer-clipboard' as const`.
    3. Export `CLIPBOARD_VERSION = 1 as const`.
    4. Export `isClipboardPayload(value: unknown): value is ClipboardPayload` — checks tag, version, and that nodes/edges are arrays. No deep validation (paste tolerates partial-shape failures downstream).
    5. Export `smartParseAndIncrement(originalName: string, existingNames: Set<string>): string`:
       - If `!existingNames.has(originalName)` return `originalName` unchanged.
       - Parse trailing `_<digits>` via regex `^(.+)_(\d+)$`. If match, `base = capture[1]`, else `base = originalName`.
       - Loop `i = 2..10_000`: if `!existingNames.has(`${base}_${i}`)` return that candidate. (Start at 2 because the base alone is already a "1-equivalent" — matches the spec examples: `pump` → `pump_2`, `pump_1` → `pump_2`.)
       - Wait — re-read §3.5: `pump_1` → `pump_2 (or pump_3 if _2 is taken)`. So we want to start at the digit-after-the-current. Refine: if `originalName` matched the regex with digits `n`, scan from `n+1` upward; if no match, scan from `2` upward. BUT honor §3.5's "lowest free" — actually re-read line 517-519: "If pump_1 and pump_3 exist but pump_2 doesn't, copying pump_1 produces pump_2 (lowest free, not next after highest)." So scanning ALWAYS starts at 2 regardless of the digit captured. The digit on the original name is informational only — the lowest-free integer wins.
       - Final algorithm: derive `base` (strip trailing `_<digits>` if present), then scan `i = 2..10_000` for `${base}_${i}` not in `existingNames`. Throw on exhaustion.
    6. No dependencies on store; no DOM; pure function under test.

    Create gui/src/lib/__tests__/clipboard.test.ts:
    7. Import `smartParseAndIncrement` and `isClipboardPayload` from `../clipboard`.
    8. Cover every test case enumerated in <behavior>.
    9. No mocks needed.
  </action>
  <verify>
    <automated>cd gui && npx vitest run src/lib/__tests__/clipboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'export function smartParseAndIncrement|export const smartParseAndIncrement' gui/src/lib/clipboard.ts | grep -c .` returns 1.
    - `grep -nE 'export function isClipboardPayload|export const isClipboardPayload' gui/src/lib/clipboard.ts | grep -c .` returns 1.
    - `grep -nE "CLIPBOARD_FORMAT_TAG *= *['\"]stream-composer-clipboard['\"]" gui/src/lib/clipboard.ts | grep -c .` returns 1.
    - smartParseAndIncrement('pump_1', new Set(['pump_1','pump_3'])) === 'pump_2' (lowest-free assertion, NOT pump_4).
    - `cd gui && npx vitest run src/lib/__tests__/clipboard.test.ts` exits 0.
    - No imports from `@/store/*` or `@tauri-apps/*` or `react` in clipboard.ts.
  </acceptance_criteria>
  <done>
    Pure clipboard.ts module shipped; smart-parse-and-increment passes the spec edge cases; type guard tolerates malformed input.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useStore clipboard slice — copy / cut / paste / duplicate actions</name>
  <files>gui/src/store/useStore.ts, gui/src/store/__tests__/clipboardActions.test.ts</files>
  <read_first>
    - gui/src/store/useStore.ts lines 1051-1230 (addNode, removeNode, addEdge as patterns to mirror)
    - gui/src/store/useStore.ts lines 287-294 (the saveProject/newProject action surface for the type-declaration pattern)
    - gui/src/store/useStore.ts lines 180-200 (isDirty / currentFilePath shape)
    - gui/src/lib/clipboard.ts (from Task 1)
    - .planning/phases/65-interaction-model-overhaul/65-CONTEXT.md §decisions D-15 (writeText), D-16 (duplicate bypasses clipboard), D-19 (silent drop of external edges)
    - .planning/phases/64-connection-routing/64-CONTEXT.md (note: pasted nodes trigger autoflip on render automatically — paste does not compute handle sides)
  </read_first>
  <behavior>
    Action contract:
    - **copySelection()** — get selected nodes from `get().nodes.filter(n => n.selected)`. If empty, no-op. Build edge filter: include edges where BOTH `edge.source` and `edge.target` are in the selected-node-id set. Construct `ClipboardPayload` shape via clipboard.ts. Call `navigator.clipboard.writeText(JSON.stringify(payload))` wrapped in try/catch (no-op on failure — Tauri webview may not have clipboard permission in some test envs). NOT dirty-mutating; do NOT push snapshot.
    - **cutSelection()** — `_pushSnapshot()`; call copySelection internally; then remove each selected node via the existing removeNode logic (or equivalent inline: filter nodes, filter dependent edges, clear anchors per node, set isDirty:true). Single snapshot for the whole cut.
    - **pasteFromClipboard()** — read `navigator.clipboard.readText()`. JSON.parse with try/catch. If parse fails OR `!isClipboardPayload(value)` → silent return (D-15 contract). Else `_pushSnapshot()`. For each node in payload:
        * mint new UUID via `crypto.randomUUID()`
        * compute new instanceName via `smartParseAndIncrement(node.data.instanceName, runningExistingNames)`; ADD the new name to `runningExistingNames` before processing the next pasted node (lowest-free must remain stable across the paste batch)
        * preserve `componentId`, `parameters`, `constructorMode` verbatim — including any resource UUID references (geometry, power_shape) — D-19's "Resource references preserved verbatim"
        * **Offset model (B4 lock): each paste N lands at `original + N*20` on both axes.** Implementation: module-level `let pasteOffsetIndex = 0;` — increment at the START of each `pasteFromClipboard` call BEFORE computing offset, then `offset = (pasteOffsetIndex * 20, pasteOffsetIndex * 20)`. So first paste lands at +20, second at +40, third at +60 from original. Reset to 0 inside `copySelection` (new clipboard contents start a fresh sequence).
        * collect oldId→newId map.
      For each edge in payload:
        * remap source/target to new node IDs; mint new edge UUID via crypto.randomUUID(); preserve all edge data (type, sourceHandle, targetHandle, data).
      Set `nodes: [...existing, ...newNodes], edges: [...existing, ...newEdges], isDirty: true`. Then clear selection on existing nodes and set `selected: true` on the new ones (Phase 64 autoflip handles handle-side recomputation on render).
    - **duplicateSelection()** — like pasteFromClipboard but reads from `get().nodes.filter(n => n.selected)` directly (**NO clipboard touch — D-16**); no JSON serialization; **fixed `+20px/+20px` offset on every call — does NOT accumulate; does NOT share `pasteOffsetIndex` with paste** (B4 lock). No module-level counter for duplicate — the offset is the literal `(20, 20)` every time. Same smartParseAndIncrement loop; new UUIDs everywhere. Pushes ONE snapshot.

    Test cases (clipboardActions.test.ts):
    - copySelection writes navigator.clipboard.writeText with a payload whose `__format === 'stream-composer-clipboard'` and `nodes.length === selectedCount`.
    - copySelection with a selection [pump_1, channel_1] connected by an edge: the payload includes the edge. With the same selection but the edge endpoints split across selected/unselected: the edge is excluded from the payload.
    - pasteFromClipboard with a payload of [pump_1] when pump_1 already exists in store: new node lands with instanceName 'pump_2'. With pump_1+pump_3 existing: lands as 'pump_2' (lowest free).
    - pasteFromClipboard increments `pasteOffsetIndex` on each call. Two consecutive pastes of the same payload yield positions `(+20,+20)` from original on paste 1 and `(+40,+40)` from original on paste 2 (NOT +40 from paste-1's position). Test by asserting `newNode.position.x === originalNode.position.x + 20 * N` where N is the paste call count.
    - pasteFromClipboard preserves resource UUIDs verbatim — paste a Channel node whose parameters.geometry === 'uuid-A' yields a new node whose parameters.geometry === 'uuid-A' (NOT a new resource).
    - pasteFromClipboard with malformed clipboard text ("not json") is a silent no-op — nodes count unchanged, no throw.
    - pasteFromClipboard with payload edges where source-or-target is NOT in payload.nodes: the edge is silently dropped (this is the receive-side counterpart of copy's drop rule; defensive in case a malformed payload sneaks through).
    - cutSelection removes the selected nodes AND the dependent edges in a single snapshot.
    - duplicateSelection produces new nodes at a FIXED `+20px/+20px` offset on EVERY call (no accumulation across successive Ctrl+D presses) WITHOUT touching navigator.clipboard.writeText (mock it; assert call count 0). Test by pressing duplicate twice — both new nodes land at exactly `original + 20` on each axis (the second duplicate uses the first duplicate as a copy-source if selection moved, but the offset is still +20 from whatever was selected when duplicate fired).
    - copySelection resets `pasteOffsetIndex` to 0. Test: paste twice (positions +20, +40), then copy a new selection, then paste — the new paste should land at +20 (not +60) from its original.
  </behavior>
  <action>
    1. Add four method signatures to the State interface around line 287-294:
       ```typescript
       copySelection: () => Promise<void>;
       cutSelection: () => Promise<void>;
       pasteFromClipboard: () => Promise<void>;
       duplicateSelection: () => void;
       ```
    2. Implement the four actions in the action slice. Place them together as a block under a comment header `// Phase 65 Plan 04: Clipboard actions (D-15, D-16, D-19)`. Reuse existing patterns: `_pushSnapshot()`, `crypto.randomUUID()`, `set({...})`. Import `smartParseAndIncrement` and `isClipboardPayload` and `CLIPBOARD_FORMAT_TAG` and `CLIPBOARD_VERSION` from `@/lib/clipboard`.
    3. **Counter model (B4 lock):**
       - Module-level: `let pasteOffsetIndex = 0;` near the top of `useStore.ts` (this is the ONE allowed module-level mutable left after Plan 01 deleted `instanceCounters`). Document with a comment: `// Reset on copySelection — purely for visual paste offset stacking, NOT identity. Independent of duplicateSelection which uses a fixed +20.`
       - `pasteFromClipboard`: at the top of the function body (after the parse-success guard, before the snapshot), `pasteOffsetIndex += 1;` then use `offset = { dx: pasteOffsetIndex * 20, dy: pasteOffsetIndex * 20 }` for every node in this paste batch.
       - `copySelection`: set `pasteOffsetIndex = 0;` as the LAST line of the function (after writeText resolves).
       - `duplicateSelection`: does NOT read or touch `pasteOffsetIndex`. Uses the literal `(20, 20)` offset every call. This is a deliberate independence — Ctrl+D should not interfere with Ctrl+V's stacking, and Ctrl+V should not interfere with Ctrl+D's predictability.
    4. The selection-mutation step at the end of paste/duplicate: instead of separately toggling node.selected, you can use the existing onNodesChange / applyNodeChanges path with a "select" change for new IDs and "deselect" for old IDs — pick the path that fits the file's existing patterns. Either is acceptable.
    5. Create gui/src/store/__tests__/clipboardActions.test.ts. Use a fresh store per test via `useStore.setState({...initialState})` or by importing the same test utilities the existing useStore tests use. Mock `navigator.clipboard` via `Object.defineProperty(globalThis, 'navigator', {value: {clipboard: {writeText: vi.fn(), readText: vi.fn()}}, ...})` or vi.stubGlobal.
    6. Implementation rule: cut MUST take exactly ONE snapshot (not two). Use a private helper `_copySelectionImpl(state)` that returns the payload without snapshotting, then cutSelection calls both the helper and the remove logic inside a single set() / single snapshot.
    7. The "external edges are dropped silently" rule is enforced BOTH on copy (don't include them in the payload) AND on paste (defensive — drop unresolvable source/target IDs).
  </action>
  <verify>
    <automated>cd gui && npx vitest run src/store/__tests__/clipboardActions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'copySelection:|cutSelection:|pasteFromClipboard:|duplicateSelection:' gui/src/store/useStore.ts | grep -c .` returns ≥ 4 (interface declarations + implementations may show as ≥ 8).
    - `grep -nE 'smartParseAndIncrement|isClipboardPayload' gui/src/store/useStore.ts | grep -c .` returns ≥ 2.
    - `cd gui && npx vitest run src/store/__tests__/clipboardActions.test.ts` exits 0 with the 9 behavior cases listed.
    - `cd gui && npx tsc --noEmit` reports no new errors.
    - `grep -nE 'let pasteOffsetIndex *= *0' gui/src/store/useStore.ts | grep -c .` returns 1 (single module-level counter for paste).
    - `grep -nE 'pasteOffsetIndex *\+= *1|pasteOffsetIndex *= *0' gui/src/store/useStore.ts | grep -c .` returns ≥ 2 (increment in paste, reset in copy).
    - duplicateSelection's implementation MUST NOT reference `pasteOffsetIndex` — `grep -A 30 'duplicateSelection' gui/src/store/useStore.ts | grep -c 'pasteOffsetIndex'` returns 0.
    - Comment present: `grep -n "Reset on copySelection" gui/src/store/useStore.ts | grep -c .` returns ≥ 1.
  </acceptance_criteria>
  <done>
    Four actions exist on the store; clipboard payload writes/reads cleanly via navigator.clipboard; smart-name iteration produces lowest-free names per-batch; duplicateSelection does not touch the OS clipboard; malformed paste input is a silent no-op.
  </done>
</task>

<task type="auto">
  <name>Task 3: CanvasPanel keyboard wiring — Ctrl+C/X/V/D</name>
  <files>gui/src/components/CanvasPanel.tsx</files>
  <read_first>
    - gui/src/components/CanvasPanel.tsx lines 171-201 (existing keydown handler — Ctrl+Z/Y/Tab + the text-input focus guard)
    - .planning/phases/65-interaction-model-overhaul/65-CONTEXT.md §canonical_refs and §decisions D-15, D-16
  </read_first>
  <action>
    Extend the existing useEffect keydown handler in CanvasPanel.tsx (the one that already wires Ctrl+Z, Ctrl+Y/Shift+Z, Tab, and Esc-from-Plan-03):
    1. Inside the same `handleKeyDown` callback, after the existing text-input focus guard, add four `if ((e.ctrlKey || e.metaKey) && e.key === 'c'/'x'/'v'/'d') { e.preventDefault(); useStore.getState().copySelection() / cutSelection() / pasteFromClipboard() / duplicateSelection(); }` blocks.
    2. Place them AFTER the text-input focus guard (lines 184-194) so that Ctrl+C inside a property text input still does the browser-native copy. The Tab handler at line 185 already returns early on text-input focus — copy that idiom.
    3. Do NOT re-guard the Ctrl+Z/Y handlers — they currently run before the text-input guard. Phase 65 preserves that order. Match it for the new shortcuts: place Ctrl+C/X/V/D BEFORE the Tab block (so they're after Esc/Z/Y but before Tab) — except the text-input guard MUST come first. Implementation:
       ```
       (early Ctrl+Z, Ctrl+Y guards — preserved)
       (text-input focus guard — preserved)
       (new Ctrl+C/X/V/D blocks here)
       (Tab block — preserved)
       (Esc block from Plan 03 — preserved)
       ```
    4. No other changes to CanvasPanel.tsx; the new keys reuse the existing useStore.getState() pattern.
    5. Add a single inline comment block above the Ctrl+C/X/V/D group: `// Phase 65 Plan 04: clipboard shortcuts (D-15, D-16). Skipped when text input has focus (above)`.
  </action>
  <verify>
    <automated>cd gui && npx tsc --noEmit && cd gui && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'copySelection\(\)|cutSelection\(\)|pasteFromClipboard\(\)|duplicateSelection\(\)' gui/src/components/CanvasPanel.tsx | grep -c .` returns 4.
    - `grep -nE "e\.key === ['\"]c['\"]" gui/src/components/CanvasPanel.tsx | grep -c .` returns 1.
    - `grep -nE "e\.key === ['\"]v['\"]" gui/src/components/CanvasPanel.tsx | grep -c .` returns 1.
    - Existing Ctrl+Z / Tab / Esc handlers still in place: `grep -nE "e\.key === ['\"]z['\"]|e\.key === ['\"]Tab['\"]|e\.key === ['\"]Escape['\"]" gui/src/components/CanvasPanel.tsx | grep -c .` returns ≥ 3.
    - `cd gui && npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>
    The four shortcuts dispatch the matching store actions; text-input focus guard correctly suppresses them when user is typing in a sidebar input.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual smoke — copy/paste/cut/duplicate</name>
  <what-built>
    Ctrl+C/X/V/D shortcuts and the underlying clipboard actions are wired.
    The canvas should support drawio-style copy-paste with smart-name
    increment and resource reference preservation.
  </what-built>
  <how-to-verify>
    1. `cd gui && npm run tauri dev`.
    2. Build a small loop: drag in 2 Pumps + 1 Channel, name them pump_1 / pump_2 / channel_1 automatically. Connect them.
    3. **Ctrl+C / Ctrl+V single node:** select pump_1 (left-click), press Ctrl+C, then Ctrl+V. EXPECT: a pump_3 appears at +20px/+20px offset (because pump_1 and pump_2 occupy the lowest free slots). The new pump is now selected; the original is deselected.
    4. **Ctrl+V multi-paste (B4 model):** press Ctrl+V again. EXPECT: pump_4 (or next lowest-free) appears at +40px from the ORIGINAL (NOT +40 from the first paste). Press Ctrl+V a third time: a pump at +60px from the ORIGINAL. Each paste lands at `original + N*20` where N = paste count since the last copy.
    5. **Ctrl+C / Ctrl+V multi-node + internal edge:** marquee-select pump_1 + channel_1 + the edge between them. Press Ctrl+C, then Ctrl+V. EXPECT: a NEW pump_3-or-higher (lowest-free) + a NEW channel_2 appear at offset, connected by a new edge of the same type. The original pump_1↔channel_1 edge is unchanged.
    6. **External edge dropped silently:** select ONLY pump_1 (leave channel_1 unselected), press Ctrl+C, then Ctrl+V. EXPECT: the paste creates ONLY a new pump (no orphan edge dangling). No error toast.
    7. **Resource preservation:** if channel_1 references a Geometry resource, paste the channel — EXPECT in the sidebar: the pasted channel's geometry picker shows the SAME geometry resource (same name). No new Geometry entry appears in the Resources tab.
    8. **Ctrl+X cut:** select a pump, Ctrl+X. EXPECT: the pump disappears from the canvas; Ctrl+V immediately afterwards pastes it back. Ctrl+Z reverses the cut as a single undo.
    9. **Ctrl+D duplicate (B4 fixed-offset lock):** select a pump, Ctrl+D. EXPECT: a new pump_<lowest-free> appears at exactly `+20px/+20px` offset. Press Ctrl+D again (the new duplicate is now selected). EXPECT: another pump at `+20px` from the second-most-recent — duplicate ALWAYS uses fixed +20, never accumulates. The OS clipboard is unaffected — Ctrl+V (without a prior Ctrl+C) should NOT paste this duplicate again (verify by pasting in a text editor — the previous clipboard content should still be there).
    10. **Malformed paste no-op:** in a separate text editor, copy a non-Composer string ("hello world"). In Composer, press Ctrl+V. EXPECT: nothing happens; no toast, no console error.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any observed issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS clipboard → `navigator.clipboard.readText` → `JSON.parse` → `isClipboardPayload` | UNTRUSTED clipboard text crosses into the app. Malformed input must not throw, must not partially mutate state, must not spam toasts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-04 | T (Tampering) | pasteFromClipboard | mitigate | `JSON.parse` wrapped in try/catch; `isClipboardPayload` type guard rejects anything not matching the exact `__format` tag + `version`; no partial state mutation on rejection (the snapshot push happens AFTER the guard check, not before). |
| T-65-05 | D (Denial of service) | pasteFromClipboard with crafted payload of 100k nodes | accept | crypto.randomUUID() loop is O(n) and bounded by the user pasting; a malicious payload of 100k nodes would hang the renderer but only the user's own clipboard is the input source — not a remote attacker. Acceptable per CLAUDE.md "no back-compat / no malicious-input hardening during heavy dev". |
| T-65-06 | I (Information disclosure) | copySelection writes payload to OS clipboard | accept | The payload is the same data the user could already export via Save — no new disclosure surface. |
</threat_model>

<verification>
- `smartParseAndIncrement` tested against the §3.5 spec cases including the `pump_v2 → pump_v3` "acceptable noise" case.
- `isClipboardPayload` rejects non-JSON, wrong tag, wrong version, null.
- Paste preserves resource UUIDs verbatim — vitest fixture covers it.
- Single-snapshot guarantee for cutSelection — vitest covers the snapshot count (mock _pushSnapshot, assert calls === 1).
- Manual smoke covers the full Ctrl+C/X/V/D matrix + external-edge silent-drop + resource preservation + malformed-paste no-op.
</verification>

<success_criteria>
Source assertion: smartParseAndIncrement('pump_1', new Set(['pump_1','pump_3'])) === 'pump_2'.
Behavior assertion: pasting [pump_1, channel_1] when both names exist yields [pump_2, channel_2] (or lowest-free per name).
Behavior assertion: Ctrl+V with malformed clipboard text is a silent no-op (no exception, no toast, no state change).
Test assertion: clipboard.test.ts + clipboardActions.test.ts both exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/65-interaction-model-overhaul/65-04-SUMMARY.md`.
</output>
