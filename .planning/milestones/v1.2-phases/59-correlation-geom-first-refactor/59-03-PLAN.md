---
phase: 59-correlation-geom-first-refactor
plan: 03
type: execute
wave: 3
depends_on: ["59-01", "59-02"]
files_modified:
  - src/examples.jl
  - test/test_composition.jl
  - test/test_integration.jl
autonomous: true
requirements: []

must_haves:
  truths:
    - "src/examples.jl call sites at lines ~438, 441, 443, 581 pass geom to each correlation factory; no old kwargs (Dh, b, L, aspect_ratio) remain."
    - "test/test_composition.jl line ~20 passes geom to laminar_friction."
    - "test/test_integration.jl TF-06 (line ~770) and TF-07 (line ~854) pass geom to laminar_friction; no scalar-ratio call shape remains in that file."
    - "Full test suite passes under bin/jl test/runtests.jl (or per-file fan-out if NET-03 is still skip-blocked per STATE.md)."
    - "test/test_validation.jl Python parity gate passes — no semantic drift from refactor; pre-refactor FAIL baseline preserved exactly."
  artifacts:
    - path: "src/examples.jl"
      provides: "build_loop_lof_bypass + build_loop_pk call sites converted to geom-first factories."
      contains: "regime_dependent(geom"
    - path: "test/test_composition.jl"
      provides: "Composition test laminar_friction call switched to geom form."
      contains: "laminar_friction(geom"
    - path: "test/test_integration.jl"
      provides: "TF-06 and TF-07 testsets switched to laminar_friction(geom) — covers uncovered call sites at lines ~770 and ~854."
      contains: "laminar_friction(geom"
  key_links:
    - from: "src/examples.jl build_loop_lof_bypass"
      to: "regime_dependent(geom; ...) + elenbaas_htc(geom; g) + laminar_friction(geom)"
      via: "geom = PipeGeometry_circular(L_ch, D_ch) constructed at the top of the builder"
      pattern: "regime_dependent\\(geom"
    - from: "src/examples.jl build_loop_pk"
      to: "laminar_friction(geom)"
      via: "geom = PipeGeometry_rectangular(0.6, 0.070, 0.0025, 0.070) at line ~575"
      pattern: "laminar_friction\\(geom\\)"
    - from: "test/test_validation.jl"
      to: "test/data/python_parity_reference.jl"
      via: "Python STREAM reference table; load-bearing semantic-drift gate"
      pattern: "python_parity_reference"
---

<objective>
Sweep remaining call sites of refactored factories in src/examples.jl (lines
~438, 441, 443, 581 per CONTEXT.md `<canonical_refs>`) and test/test_composition.jl
line 20 over to geom-first signatures. Then run the integrated test gate
including the Python parity harness (test/test_validation.jl) — the
load-bearing assertion that the refactor introduced no semantic drift in any
closure body, per §3.1 Test plan and CONTEXT.md `<canonical_refs>` Verification gates.

Purpose: Closes the loop on Plans 01+02. Until this plan ships, `bin/jl
test/runtests.jl` will fail on the examples-based builders (build_loop_lof_bypass,
build_loop_pk) which still call factories with the old kwargs. Runs in Wave 2.

Output: Updated examples.jl, updated test_composition.jl, full test suite
green including Python parity.
</objective>

<execution_context>
@/home/itay/projects/STREAM.jl/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/STREAM.jl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-correlation-geom-first-refactor/59-CONTEXT.md
@.planning/phases/59-correlation-geom-first-refactor/59-01-PLAN.md
@.planning/phases/59-correlation-geom-first-refactor/59-02-PLAN.md
@CLAUDE.md

<interfaces>
Final factory signatures (from Plans 01 and 02):
  - laminar_friction(geom::PipeGeometry) -> (Re) -> f_darcy
  - elenbaas_htc(geom::PipeGeometry; g=9.81) -> (Re, Pr, T_bulk, T_wall) -> Nu
  - fully_developed_laminar_h_spl(geom::PipeGeometry) -> (Re, Pr, T_bulk, T_wall) -> Nu
  - developing_laminar_h_spl(geom::PipeGeometry; develop_length) -> (Re, Pr, T_bulk, T_wall) -> Nu  (develop_length mandatory)
  - regime_dependent(geom::PipeGeometry; htc_laminar::HTCCorrelation, htc_turbulent::HTCCorrelation, friction_laminar::Function, friction_turbulent::Function, htc_natural::Union{HTCCorrelation,Nothing}=nothing, g=nothing, Re_transition=2300) -> (htc=fn, friction=fn)

src/examples.jl already constructs PipeGeometry locally in every builder; the
required edits are mechanical kwarg-removal + adding the geom positional. No
new geom variables need to be introduced.

Call-site map (verified by grep on current main):
  - src/examples.jl line 438: `rd_ch = regime_dependent(;` — kwarg-first form, Dh=D_ch on line 444, g=g_acc on line 445.
  - src/examples.jl line 441: `friction_laminar=laminar_friction(1.0),` — scalar 1.0 was already a workaround for circular geometry; switch to laminar_friction(geom).
  - src/examples.jl line 443: `htc_natural=elenbaas_htc(; b=D_ch, L=L_ch, Dh=D_ch, g=g_acc),` — kwarg-first form, all derivable from the local geom = PipeGeometry_circular(L_ch, D_ch).
  - src/examples.jl line 581: `friction_correlation=laminar_friction(0.0025 / 0.070),` — switch to laminar_friction(geom) (geom from line 575).
  - test/test_composition.jl line 20: `friction_correlation=laminar_friction(0.0025 / 0.070))` — additional call site not listed in CONTEXT.md but found by grep; same pattern, geom already constructed earlier in the test or constructed inline.

STATE.md known issues (unrelated to this phase, do not chase):
  - NET-03 Cube flow KINSOL flag −11 halts runtests.jl orchestrator. If this
    halts the full suite, run per-file: test_correlations.jl, test_composition.jl,
    test_channels.jl, test_integration.jl, test_validation.jl.
  - MTR L/R convention disagreement — 92 parity FAIL rows in
    test/data/parity_report.csv. These are pre-existing FAIL rows from Phase 56,
    not introduced by this phase. The validation gate's expectation is "no
    NEW failures vs the pre-refactor baseline." See verify step below.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update src/examples.jl call sites to geom-first factories</name>
  <files>src/examples.jl</files>
  <read_first>
    - src/examples.jl (current state — build_loop_lof_bypass at lines ~420-513 has the regime_dependent/elenbaas_htc/laminar_friction cluster at lines 435-446; build_loop_pk at lines ~561-682 has the laminar_friction call at line 581 and constructs geom at line 575)
    - src/physical_models/htc/correlations.jl (NEW state after Plan 02 — confirm new signatures)
    - src/physical_models/friction/correlations.jl (NEW state after Plan 01 — confirm new signature)
    - .planning/phases/59-correlation-geom-first-refactor/59-CONTEXT.md (`<canonical_refs>` Source files to edit lists exact line numbers)
    - CLAUDE.md (Branching policy — commits stay on gui-redesign; daemon dev loop bin/jl)
  </read_first>
  <action>
    Edit src/examples.jl at the four lines identified in CONTEXT.md
    `<canonical_refs>` Source files to edit:

    ─── build_loop_lof_bypass (lines ~435-446) ───
    Current block (verbatim, indentation preserved):
      ```
      geom = PipeGeometry_circular(L_ch, D_ch)

      # NC-enabled regime switching for heated channel (D-10)
      rd_ch = regime_dependent(;
          htc_laminar=constant_Nusselt(; Nu=8.235),
          htc_turbulent=dittus_boelter,
          friction_laminar=laminar_friction(1.0),
          friction_turbulent=blasius_friction,
          htc_natural=elenbaas_htc(; b=D_ch, L=L_ch, Dh=D_ch, g=g_acc),
          Dh=D_ch,
          g=g_acc,
      )
      ```

    Replace with (geom is already defined two lines above):
      - regime_dependent: switch from kwarg-first `regime_dependent(;` to positional-first `regime_dependent(geom;`.
      - Inside the regime_dependent kwargs: switch `friction_laminar=laminar_friction(1.0)` to `friction_laminar=laminar_friction(geom)`.
      - Switch `htc_natural=elenbaas_htc(; b=D_ch, L=L_ch, Dh=D_ch, g=g_acc)` to `htc_natural=elenbaas_htc(geom; g=g_acc)` — all three geom-related kwargs (b, L, Dh) are derived from geom internally per Plan 02.
      - DELETE the `Dh=D_ch,` line entirely (Dh is no longer a user-facing kwarg on regime_dependent).
      - Keep `g=g_acc,` (g is still a kwarg).
      - The comment `# NC-enabled regime switching for heated channel (D-10)` is retained; D-10 referenced an earlier phase decision, not this phase. Do not edit it.

    ─── build_loop_pk (line ~575-582) ───
    Current block:
      ```
      geom = PipeGeometry_rectangular(0.6, 0.070, 0.0025, 0.070)
      ps = fill(1.0 / (nz * nx), nz, nx)
      @named cac = ChannelAndContacts(;
          n=n,
          geometry=geom,
          htc_correlation=constant_Nusselt(; Nu=8.235),
          friction_correlation=laminar_friction(0.0025 / 0.070),
      )
      ```

    Switch `friction_correlation=laminar_friction(0.0025 / 0.070)` to
    `friction_correlation=laminar_friction(geom)`. Everything else (geom
    construction, ChannelAndContacts other kwargs) unchanged.

    Do NOT modify any other line in src/examples.jl. The other builders
    (build_loop, build_loop_vertical, build_loop_transient, build_cube) either
    don't use the refactored factories or use them via a path that is already
    geom-compatible — confirm with grep before committing:
    `grep -nE "laminar_friction\(|elenbaas_htc\(|fully_developed_laminar_h_spl\(|developing_laminar_h_spl\(|regime_dependent\(" src/examples.jl`
    should return only the four edited sites and no others.
  </action>
  <verify>
    <automated>bin/jl -e 'using STREAM; ssys = build_loop_lof_bypass(); @assert ssys !== nothing; (ssys2, ic) = build_loop_pk((t) -> 0.0); @assert ssys2 !== nothing && !isempty(ic)'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -E "regime_dependent\(geom;" src/examples.jl` returns exactly one match (build_loop_lof_bypass).
    - Source assertion: `grep -E "elenbaas_htc\(geom" src/examples.jl` returns exactly one match.
    - Source assertion: `grep -cE "laminar_friction\(geom\)" src/examples.jl` returns 2 (one in build_loop_lof_bypass inside regime_dependent kwargs, one in build_loop_pk).
    - Source negation: `grep -nE "laminar_friction\(1\.0\)|laminar_friction\(0\.0025" src/examples.jl` returns NO matches.
    - Source negation: `grep -nE "elenbaas_htc\(;\s*b=|elenbaas_htc\(b=" src/examples.jl` returns NO matches.
    - Source negation: `grep -nE "^\s*Dh=D_ch,?\s*$|Dh=D_ch," src/examples.jl | grep -v '^#'` returns NO matches (the standalone Dh=D_ch kwarg line on regime_dependent is gone).
    - Source negation: `grep -nE "regime_dependent\(;" src/examples.jl` returns NO matches (no kwarg-first regime_dependent calls remain).
    - Behavior: the verify command exits 0 — both builders compile against the new signatures.
  </acceptance_criteria>
  <done>
    build_loop_lof_bypass uses regime_dependent(geom; ...) with elenbaas_htc(geom; g=g_acc) and laminar_friction(geom). build_loop_pk uses laminar_friction(geom). No remaining old-kwarg call shape (Dh=, b=, ratio scalar) in src/examples.jl. Both builders compile and return non-empty systems.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test/test_composition.jl laminar_friction call site</name>
  <files>test/test_composition.jl</files>
  <read_first>
    - test/test_composition.jl (current state — line ~20 contains `friction_correlation=laminar_friction(0.0025 / 0.070)`)
    - src/physical_models/friction/correlations.jl (NEW state after Plan 01)
    - .planning/phases/59-correlation-geom-first-refactor/59-CONTEXT.md (`<canonical_refs>` Source files to edit — this file was missed in CONTEXT but caught by grep; treat as a Plan 03 sweep item)
  </read_first>
  <action>
    Edit test/test_composition.jl at line ~20 where `laminar_friction(0.0025 / 0.070)` appears inside a ChannelAndContacts construction. Identify the local geom variable being passed to that ChannelAndContacts (look for `geometry=...` on the same call); pass the same geom to laminar_friction:
      OLD: `friction_correlation=laminar_friction(0.0025 / 0.070)`
      NEW: `friction_correlation=laminar_friction(<same geom variable>)`

    If no local geom variable exists at that call site (i.e. the
    ChannelAndContacts call uses a literal `geometry=PipeGeometry_rectangular(...)` inline), introduce a local
    geom binding above the call:
      `geom = PipeGeometry_rectangular(0.6, 0.070, 0.0025, 0.070)`
    and pass `geometry=geom` + `friction_correlation=laminar_friction(geom)`.
    Reuse the same edges/length the existing call uses; do not invent new
    geometry values.

    Confirm no other refactored-factory calls appear in test/test_composition.jl by grepping:
    `grep -nE "elenbaas_htc|fully_developed_laminar_h_spl|developing_laminar_h_spl|regime_dependent\(" test/test_composition.jl`
    should return no other matches. If it does, sweep them in the same task with the same geom-first pattern.
  </action>
  <verify>
    <automated>bin/jl test/test_composition.jl</automated>
  </verify>
  <acceptance_criteria>
    - Source negation: `grep -nE "laminar_friction\(0\.0025" test/test_composition.jl` returns NO matches.
    - Source assertion: `grep -cE "laminar_friction\(geom" test/test_composition.jl` returns at least 1.
    - Behavior: `bin/jl test/test_composition.jl` exits 0.
  </acceptance_criteria>
  <done>
    test/test_composition.jl uses laminar_friction(geom) with a properly-bound
    geom variable; the file passes when executed standalone.
  </done>
</task>

<task type="auto">
  <name>Task 2b: Update test/test_integration.jl TF-06 and TF-07 to laminar_friction(geom)</name>
  <files>test/test_integration.jl</files>
  <read_first>
    - test/test_integration.jl lines 755-870 (TF-06 testset starts at ~line 758 with `geom_tf = PipeGeometry_rectangular(0.6, 0.070, 0.0025, 0.070)` at line 764; TF-07 testset starts at ~line 838 with `geom_tf7 = PipeGeometry_rectangular(0.6, 0.070, 0.0025, 0.070)` at line 848). Confirm both `friction_correlation=laminar_friction(0.0025/0.070)` call sites at lines ~770 and ~854 and the in-scope geom variable name at each site.
    - src/physical_models/friction/correlations.jl (NEW state after Plan 01 — confirm `laminar_friction(geom::PipeGeometry)` signature).
    - .planning/phases/59-correlation-geom-first-refactor/59-CONTEXT.md (D-00 geom-first convention; D-01 clean break).
  </read_first>
  <action>
    Two surgical edits in test/test_integration.jl. Do NOT touch any other
    testset in the file (LOF, ISCB, PK loops, COMPAT are out of scope).

    1. TF-06 testset, line 770 currently reads:
         `friction_correlation=laminar_friction(0.0025/0.070),`
       The enclosing ChannelAndContacts call (lines ~766-771) uses
       `geometry=geom_tf` (line 768). Replace the scalar form with
       `friction_correlation=laminar_friction(geom_tf),`.

    2. TF-07 testset, line 854 currently reads:
         `friction_correlation=laminar_friction(0.0025/0.070),`
       The enclosing ChannelAndContacts call (lines ~850-855) uses
       `geometry=geom_tf7` (line 852). Replace with
       `friction_correlation=laminar_friction(geom_tf7),`.

    Both geom variables are constructed two lines above each call site as
    `PipeGeometry_rectangular(0.6, 0.070, 0.0025, 0.070)`; numerically,
    `0.0025/0.070 == geom.depth/geom.width`, so the closure produced is
    identical pre- and post-edit. No expected-value changes.

    Confirm no other refactored-factory call sites remain in test/test_integration.jl by:
    `grep -nE "laminar_friction\(|elenbaas_htc\(|fully_developed_laminar_h_spl\(|developing_laminar_h_spl\(|regime_dependent\(" test/test_integration.jl`
    should return only the two edited sites and no other matches. If it does,
    sweep them in the same task with the same geom-first pattern.
  </action>
  <verify>
    <automated>bin/jl test/test_integration.jl</automated>
  </verify>
  <acceptance_criteria>
    - Source negation: `grep -nE "laminar_friction\(0\.0025/0\.070\)" test/test_integration.jl` returns ZERO matches.
    - Source assertion: `grep -cE "laminar_friction\(geom" test/test_integration.jl` returns >= 2 (the two new call sites: geom_tf and geom_tf7).
    - Source assertion: `grep -nE "friction_correlation=laminar_friction\(geom_tf\)" test/test_integration.jl` returns exactly 1 match (TF-06).
    - Source assertion: `grep -nE "friction_correlation=laminar_friction\(geom_tf7\)" test/test_integration.jl` returns exactly 1 match (TF-07).
    - Behavior: `bin/jl test/test_integration.jl` exits 0 (TF-06 and TF-07 still pass; other testsets unaffected).
  </acceptance_criteria>
  <done>
    test/test_integration.jl TF-06 line 770 and TF-07 line 854 both call
    `laminar_friction(geom_tf)` / `laminar_friction(geom_tf7)` respectively; no
    scalar-ratio form remains in the file; integration test file still passes.
  </done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Run full test suite including Python parity gate</name>
  <files></files>
  <read_first>
    - .planning/phases/59-correlation-geom-first-refactor/59-CONTEXT.md (`<canonical_refs>` Verification gates: test_correlations + test_validation + test_channels + test_integration)
    - .planning/STATE.md (Pending Todos / Blockers — NET-03 KINSOL halt is a known orchestrator failure unrelated to this phase; MTR L/R disagreement = 92 pre-existing parity FAILs, not introduced here)
    - test/data/python_parity_reference.jl (reference table consumed by test_validation.jl — read header comment to confirm tolerance / tier structure)
    - test/test_validation.jl (parity harness)
    - test/test_integration.jl (POST-Task-2b state — confirms TF-06/TF-07 already migrated before the integration gate runs)
  </read_first>
  <action>
    This is the integrated gate. It depends on Plans 01, 02, and Tasks 1, 2,
    and 2b of this plan having committed.

    ─── Step 0: Parity baseline snapshot (BEFORE the gate runs) ───
    Capture the pre-refactor parity-report FAIL set so the deterministic diff
    in the acceptance criteria below can assert "no NEW FAIL rows" without
    relying on remembered numbers. This step runs once at the start of the
    task, BEFORE any source edits land in the executor's worktree (i.e.
    before Plan 01/02/03 source changes are checked out).

    Preflight safety: only stash if the worktree is clean. If there is
    uncommitted work, do NOT auto-stash — abort the baseline step and report
    to user so the executor can resolve in-flight work first.

      if git diff --quiet && git diff --cached --quiet; then
          git stash --include-untracked
          bin/jl test/test_validation.jl || true   # harness exits non-zero on FAILs; we only care that parity_report.csv is regenerated
          cp test/data/parity_report.csv /tmp/parity_baseline_59.csv
          git stash pop
      else
          echo "Worktree dirty; capture parity_report.csv baseline manually before running this task and copy to /tmp/parity_baseline_59.csv. Aborting."
          exit 1
      fi

    Verify: `test -f /tmp/parity_baseline_59.csv` succeeds AND
    `test -s /tmp/parity_baseline_59.csv` succeeds (file is non-empty).

    Caveat: if `git stash` is unsafe in the executor's worktree (e.g. mixed
    in-flight work the executor does not own), gate this step on the
    `git diff --quiet` check above. The executor MUST capture the baseline
    before any Plan 01/02 source change lands; otherwise the deterministic
    diff in acceptance criteria becomes meaningless.

    ─── Step 1: Full test-file fan-out ───
    Execute, in order, capturing exit code for each:

    1. `bin/jl test/test_correlations.jl` — unit + integration tests for all
       refactored factories.
    2. `bin/jl test/test_composition.jl` — composition helpers covering
       symmetric_plate / plate / one_sided_connection with the new call
       shape.
    3. `bin/jl test/test_channels.jl` — Channel / CHF / CAC variants;
       indirect call sites through examples.jl builders.
    4. `bin/jl test/test_integration.jl` — single big integration file
       covering build_loop_* and build_loop_transient (this is where
       build_loop_lof_bypass and build_loop_pk get exercised end-to-end).
    5. `bin/jl test/test_validation.jl` — Python parity harness. THIS IS THE
       LOAD-BEARING ASSERTION. Acceptance is "no NEW failures vs the
       pre-refactor baseline." The baseline per STATE.md is 424 CLEAN / 78
       GRAY / 34 FAIL (last activity 2026-05-09). After this phase the count
       must be unchanged OR strictly improved (e.g. more CLEAN rows, same FAILs
       restricted to the pre-existing MTR L/R disagreement set).

    For step 5 specifically:
      - Run `bin/jl test/test_validation.jl` and capture full stdout/stderr.
      - If the script exits 0: parity holds. Recorded as "Python parity gate
        passed."
      - If the script exits non-zero AND `test/data/parity_report.csv` shows
        a strict superset of the pre-refactor FAIL set (i.e. new FAIL rows
        appearing on rows that were CLEAN or GRAY before): STOP. Do not
        proceed to Plan 04. Open the parity_report.csv, identify which
        factory's closure body drifted, and report back. The refactor is not
        correct yet.
      - If the script exits non-zero BUT the FAIL set is identical to the
        pre-refactor baseline (MTR L/R rows only, total still 34 FAILs):
        treat as ACCEPTABLE per STATE.md "v1.1 close blocker — MTR L/R
        convention disagreement" — record as "parity gate passed modulo
        pre-existing MTR FAILs" in the SUMMARY and proceed.

    Do NOT attempt to fix MTR L/R drift in this phase — that is a v1.1 close
    item out of scope per CONTEXT.md `<domain>`.

    NET-03 handling: if `bin/jl test/runtests.jl` (the orchestrator) is
    attempted and halts on NET-03 KINSOL flag −11 per STATE.md, fall back to
    the per-file fan-out above. This is documented behavior, not a phase
    regression. Mark NET-03 as @test_skip if encountered (matches STATE.md
    Pending Todos / NET-03 cleanup item).
  </action>
  <verify>
    <automated>bin/jl test/test_correlations.jl && bin/jl test/test_composition.jl && bin/jl test/test_channels.jl && bin/jl test/test_integration.jl && bin/jl test/test_validation.jl</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `bin/jl test/test_correlations.jl` exits 0.
    - Behavior: `bin/jl test/test_composition.jl` exits 0.
    - Behavior: `bin/jl test/test_channels.jl` exits 0.
    - Behavior: `bin/jl test/test_integration.jl` exits 0.
    - Behavior: `bin/jl test/test_validation.jl` exits 0 OR exits non-zero with FAIL set strictly equal to the pre-refactor MTR L/R baseline (34 FAILs from STATE.md last_activity 2026-05-09). The decision tree above governs which outcome is acceptable.
    - File assertion: `test/data/parity_report.csv` exists and is non-empty after running test_validation.jl (the harness regenerates it).
    - Diff assertion: comparing `parity_report.csv` before and after this plan must show no NEW FAIL rows on previously CLEAN/GRAY rows. The MTR L/R FAILs are exempt — they were FAILing on the baseline and are still FAILing now.
    - Deterministic diff: `diff <(awk -F, '$NF=="FAIL"' /tmp/parity_baseline_59.csv | sort) <(awk -F, '$NF=="FAIL"' test/data/parity_report.csv | sort)` produces empty output — the set of FAIL rows is unchanged (exact set equality on the last-field FAIL filter). If this diff is non-empty, identify which rows newly appeared (or disappeared) and report; new FAIL rows are a refactor regression.
  </acceptance_criteria>
  <done>
    All five test files run and pass (or fail in the documented-exempt MTR L/R
    way). The Python parity gate confirms no semantic drift from the
    refactor. Phase 59 is functionally complete; Plan 04 only needs to emit
    the handoff doc.
  </done>
</task>

</tasks>

<threat_model>
No exposed surface — pure numerical library refactor, no new attack surface
introduced. No auth, network, user-input parsing, DB, or external service.
Per CONTEXT.md branch policy this phase commits to gui-redesign only; no
release artifact ships from this plan.
</threat_model>

<verification>
Per Task 3 above — five per-file runs sequenced. The Python parity harness
(test_validation.jl) is the load-bearing assertion that the refactor
introduced no semantic drift. STATE.md baseline (424 CLEAN / 78 GRAY / 34
FAIL) defines acceptable post-refactor parity output; new failures on
previously-passing rows abort progression to Plan 04.
</verification>

<success_criteria>
- src/examples.jl four call sites converted; build_loop_lof_bypass and build_loop_pk compile and solve.
- test/test_composition.jl call site converted; file passes.
- All five named test files (correlations, composition, channels, integration, validation) exit 0 OR exit per the documented MTR-L/R-exempt rule.
- No new FAIL rows in parity_report.csv vs pre-refactor baseline.
- No new branches created. Commits land on gui-redesign per CLAUDE.md branching policy.
</success_criteria>

<output>
After completion, create `.planning/phases/59-correlation-geom-first-refactor/59-03-SUMMARY.md`
</output>
