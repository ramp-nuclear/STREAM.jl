---
phase: 59-correlation-geom-first-refactor
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - src/physical_models/htc/correlations.jl
  - test/test_correlations.jl
autonomous: true
requirements: []

must_haves:
  truths:
    - "elenbaas_htc takes geom::PipeGeometry as first positional argument; b/L/Dh user-facing kwargs are gone."
    - "fully_developed_laminar_h_spl takes geom::PipeGeometry; Dh/aspect_ratio user-facing kwargs are gone."
    - "developing_laminar_h_spl takes geom::PipeGeometry; develop_length stays a mandatory kwarg per D-04; Dh/aspect_ratio user-facing kwargs are gone."
    - "regime_dependent takes geom::PipeGeometry as first positional argument; Dh user-facing kwarg is gone; group validation collapses to (htc_natural, g)."
    - "HTC and regime_dependent testsets in test/test_correlations.jl pass on the new signatures."
    - "D-01: clean break, no deprecation shim — every old factory kwarg form (elenbaas_htc(; b, L, Dh, g), fully_developed_laminar_h_spl(; Dh, aspect_ratio), developing_laminar_h_spl(; Dh, develop_length, aspect_ratio), regime_dependent(; ..., Dh, g)) is removed in the same commit that lands the geom-first signatures. Task 1 negation greps enforce this."
    - "D-03: elenbaas_htc enforces the rectangular-plate domain via docstring only — no @assert, @warn, or ArgumentError on geometry kind. Task 1 docstring update explicitly states the parallel-vertical-plates expectation; Task 2 NATCONV-01 test carries the inline D-03 anti-pattern comment for future readers."
  artifacts:
    - path: "src/physical_models/htc/correlations.jl"
      provides: "Four refactored factories with geom-first signatures + updated docstrings."
      contains: "function elenbaas_htc(geom::PipeGeometry"
    - path: "test/test_correlations.jl"
      provides: "HTC-02, HTC-03, NATCONV-01, PHY-04 (regime_dependent) tests on new signatures."
  key_links:
    - from: "src/physical_models/htc/correlations.jl"
      to: "src/geometry.jl"
      via: "geom.depth, geom.width, geom.L, geom.Dh derivation inside each factory"
      pattern: "geom\\.(depth|width|L|Dh)"
    - from: "src/physical_models/htc/correlations.jl regime_dependent"
      to: "(htc_natural, g) ArgumentError validation"
      via: "construction-time group validation, Dh dropped from group"
      pattern: "ArgumentError.*htc_natural.*g"
---

<objective>
Refactor the four geometry-consuming factories in src/physical_models/htc/correlations.jl
to take geom::PipeGeometry as first positional argument per D-00 (CONTEXT.md):

  - elenbaas_htc(; b, L, Dh, g=9.81)                          → elenbaas_htc(geom; g=9.81)
  - fully_developed_laminar_h_spl(; Dh, aspect_ratio)         → fully_developed_laminar_h_spl(geom)
  - developing_laminar_h_spl(; Dh, develop_length, aspect_ratio) → developing_laminar_h_spl(geom; develop_length)
  - regime_dependent(; htc_natural, Dh, g, ...)               → regime_dependent(geom; htc_natural, g, ...)

Clean break per D-01 — old kwargs are removed in the same commit, no shim.
develop_length stays a mandatory kwarg with no default per D-04.

Update HTC-02, HTC-03, NATCONV-01, and PHY-04 regime_dependent testsets in
test/test_correlations.jl. (PHY-03 laminar_friction tests are owned by Plan 01;
this plan does not touch them.)

Purpose: Lands the bulk of the geom-first convention on the HTC factories.
Independent of friction file at factory level (Plan 01); runs in Wave 1.

Output: Refactored htc/correlations.jl, updated HTC + NATCONV + PHY-04
testsets, dead user-facing kwargs (Dh, b, L, aspect_ratio) gone from the four
factories listed above.
</objective>

<execution_context>
@/home/itay/projects/STREAM.jl/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/STREAM.jl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-correlation-geom-first-refactor/59-CONTEXT.md
@CLAUDE.md

<interfaces>
PipeGeometry fields (from src/geometry.jl): L, Dh, A, heated_perimeter,
wet_perimeter, heated_parts, width, depth. Per-factory field consumption:

- elenbaas_htc: reads geom.depth (as `b`, the plate gap), geom.L, geom.Dh.
- fully_developed_laminar_h_spl: reads geom.depth, geom.width → derives
  aspect_ratio = depth/width. (Dh was previously accepted but the docstring
  notes it was unused in the Nu calc — dropping it changes no behavior.)
- developing_laminar_h_spl: reads geom.Dh, geom.depth, geom.width → derives
  aspect_ratio.
- regime_dependent: reads geom.Dh for the Gr/Re^2 calculation in the
  NC-enabled path. geom.depth / geom.width / geom.L not used here.

Closure interface invariant: every refactored factory still returns
`(Re, Pr, T_bulk, T_wall) -> Nu` (HTC) or `(Re) -> f_darcy` (friction) with
`args...` swallowing for HTCs that don't read T_bulk/T_wall. _channel_core in
src/components/channels.jl consumes these closures; the call signature is
not changing in this phase.

Private helpers (NOT modified): rectangular_laminar_correction,
_two_sided_heating_nusselt, _nusselt_coefficient_developing,
_bergles_rohsenow_dT_ONB, elenbaas_nusselt, Marco_Han_Nusselt, dittus_boelter,
constant_Nusselt, maximal_htc. These keep their current signatures per
CONTEXT.md `<domain>` Out of scope and `<decisions>` Claude's Discretion.

MTK patterns preserved (per CLAUDE.md):
- ifelse() (not if/else) for regime switching inside closures.
- Float64(...) immediate coercion for scalars captured in closures (matches
  existing `Re_tr = Float64(Re_transition)` and `Dh_val = Float64(Dh)` pattern
  in regime_dependent).
- args... swallowing on HTC closure tails for 4-arg interface compatibility.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor four HTC factories to geom-first signatures</name>
  <files>src/physical_models/htc/correlations.jl</files>
  <read_first>
    - src/physical_models/htc/correlations.jl (current state — four factories to refactor at lines ~110-172 (regime_dependent), ~219-227 (elenbaas_htc), ~276-279 (fully_developed_laminar_h_spl), ~297-304 (developing_laminar_h_spl); stateless functions and private helpers stay verbatim)
    - src/geometry.jl (confirm PipeGeometry fields: L, Dh, depth, width)
    - .planning/phases/59-correlation-geom-first-refactor/59-CONTEXT.md (D-00, D-01, D-03, D-04; `<code_context>` Float64 coercion pattern; `<specifics>` canonical construction shape)
    - .planning/notes/gui-redesign-design-decisions.md (§3.1 lines 118-201 — locked convention; specifically lines 156-171 on regime_dependent group validation collapse to (htc_natural, g))
    - CLAUDE.md (MTK Patterns — ifelse(), @register_symbolic, Float64() coercion in closures; component authoring conventions)
  </read_first>
  <action>
    Refactor all four factories in src/physical_models/htc/correlations.jl. Do
    NOT touch: dittus_boelter, constant_Nusselt, elenbaas_nusselt,
    _two_sided_heating_nusselt, _nusselt_coefficient_developing,
    _bergles_rohsenow_dT_ONB, maximal_htc, Marco_Han_Nusselt. They stay
    verbatim per CONTEXT.md `<domain>` Out of scope.

    ─── Factory 1: elenbaas_htc ───
    OLD signature: `function elenbaas_htc(; b, L, Dh, g=9.81)`
    NEW signature: `function elenbaas_htc(geom::PipeGeometry; g=9.81)`
    Inside the function: derive `b = geom.depth`, `L_h = geom.L`, `Dh_v = geom.Dh`
    immediately as Float64 (use `Float64(geom.depth)` etc. to keep the
    captured-scalar pattern). The closure body is otherwise identical: compute
    beta, nu from T_bulk, then Gr_val = Gr(beta, g, T_wall - T_bulk, Dh_v, nu),
    Ra_val = Ra(Gr_val, Pr), return elenbaas_nusselt(Ra_val, b, L_h).
    Per D-03: update the docstring to state "This correlation is for
    parallel-vertical-plates natural convection. It expects a rectangular
    PipeGeometry where geom.depth is the plate gap. No runtime check is
    performed; passing a circular or non-plate geometry will yield an
    aspect-ratio-blind Nu that may not be physical." No @assert / @warn.

    ─── Factory 2: fully_developed_laminar_h_spl ───
    OLD signature: `function fully_developed_laminar_h_spl(; Dh, aspect_ratio)`
    NEW signature: `function fully_developed_laminar_h_spl(geom::PipeGeometry)`
    Inside: derive `aspect_ratio = geom.depth / geom.width`. Body unchanged:
    `nu = _two_sided_heating_nusselt(aspect_ratio); return (Re, Pr, args...) -> nu`.
    The old `Dh` kwarg was documented as "accepted for interface consistency,
    not used in Nu calculation" — drop it; geom.Dh is available but
    intentionally not read here (matches old behavior).

    ─── Factory 3: developing_laminar_h_spl ───
    OLD signature: `function developing_laminar_h_spl(; Dh, develop_length, aspect_ratio)`
    NEW signature: `function developing_laminar_h_spl(geom::PipeGeometry; develop_length)`
    Per D-04: develop_length is a MANDATORY kwarg with NO default. Use plain
    `develop_length` (no `=` default) so MethodError fires if omitted; do not
    write `develop_length=nothing` and then check — the explicit-kwarg-no-default
    form is Julia-idiomatic and matches the design intent.
    Inside: derive `aspect_ratio = geom.depth / geom.width`,
    `Dh_v = Float64(geom.Dh)`. Body unchanged after that:
    `correction = 6 - 5 * exp(-0.75 * aspect_ratio / 0.3257); return (Re, Pr, args...) -> begin x_star = develop_length / Dh_v / Re / Pr / correction; nudev = _nusselt_coefficient_developing(x_star); _two_sided_heating_nusselt(aspect_ratio, nudev) end`.

    ─── Factory 4: regime_dependent ───
    OLD signature: `function regime_dependent(; htc_laminar, htc_turbulent, friction_laminar, friction_turbulent, Re_transition=2300, htc_natural=nothing, Dh=nothing, g=nothing)`
    NEW signature: `function regime_dependent(geom::PipeGeometry; htc_laminar::HTCCorrelation, htc_turbulent::HTCCorrelation, friction_laminar::Function, friction_turbulent::Function, htc_natural::Union{HTCCorrelation,Nothing}=nothing, g=nothing, Re_transition=2300)`
    The type annotations on htc_laminar/htc_turbulent/htc_natural use the
    HTCCorrelation alias from Plan 01 (`const HTCCorrelation = Function`) for
    documentation effect — they will not reject anything that isn't a Function
    today, but communicate intent (§3.1 of design-decisions). Use them.
    Inside the body:
      - `Re_tr = Float64(Re_transition)` (unchanged).
      - The Dh-or-g-without-htc_natural @warn at the old line ~134 must be
        DELETED entirely — since Dh is no longer a user-facing kwarg, only
        `g` remains as a stray-without-htc_natural signal. Per the §3.1
        scope, drop the warn entirely (not load-bearing; the
        ArgumentError below covers the inverse direction).
      - The ArgumentError at the old line ~125 must change from "all three
        (htc_natural, Dh, g)" to "both htc_natural and g" — collapse the group
        from triple to pair. Trigger: `!isnothing(htc_natural) && isnothing(g)`.
        Message: `regime_dependent: htc_natural provided but g is missing — both (htc_natural, g) must be supplied together.`
      - NC-enabled path (when htc_natural !== nothing): replace `Dh_val =
        Float64(Dh)` with `Dh_val = Float64(geom.Dh)`. Keep `g_val =
        Float64(g)`. Closure bodies (htc_forced_fn, htc_fn, friction_fn)
        unchanged.
      - Forced-only path (htc_natural === nothing): unchanged.

    Update the regime_dependent docstring: replace the old `# Arguments` block
    that mentions `Dh: hydraulic diameter ... (required when htc_natural is provided)`
    with text reflecting the new `geom::PipeGeometry` positional + `g` kwarg
    requirement. Update Usage examples to match the canonical form in
    CONTEXT.md `<specifics>`:
      `rd = regime_dependent(geom; htc_laminar=..., htc_turbulent=dittus_boelter, friction_laminar=fric, friction_turbulent=blasius_friction, htc_natural=elenbaas_htc(geom; g=9.81), g=9.81)`

    Per D-01 (clean break, no deprecation shim): the OLD kwarg-only forms for
    all four factories must be removed entirely. No wrapper methods, no
    deprecation overloads, no kwarg `b=nothing` fallback. Single method per
    factory.

    Internal helper preservation: rectangular_laminar_correction lives in the
    friction file (Plan 01 scope; do not touch from here).

    Do NOT alter the file-header design-comment block at the top of the file
    beyond updating the bullet that lists factories so the names match the new
    signatures (cosmetic).
  </action>
  <verify>
    <automated>bin/jl -e 'using STREAM; geom = PipeGeometry_rectangular(0.6, 0.07, 0.0025, 0.07); @assert isa(elenbaas_htc(geom), Function); @assert isa(fully_developed_laminar_h_spl(geom), Function); @assert isa(developing_laminar_h_spl(geom; develop_length=0.3), Function); rd = regime_dependent(geom; htc_laminar=constant_Nusselt(), htc_turbulent=dittus_boelter, friction_laminar=(Re)->64.0/Re, friction_turbulent=blasius_friction); @assert isa(rd.htc, Function) && isa(rd.friction, Function)'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -E "^function elenbaas_htc\(geom::PipeGeometry" src/physical_models/htc/correlations.jl` returns exactly one match.
    - Source assertion: `grep -E "^function fully_developed_laminar_h_spl\(geom::PipeGeometry\)" src/physical_models/htc/correlations.jl` returns exactly one match.
    - Source assertion: `grep -E "^function developing_laminar_h_spl\(geom::PipeGeometry" src/physical_models/htc/correlations.jl` returns exactly one match (geom-first signature present; signature whitespace/format is not load-bearing).
    - Behavior: D-04 mandatory `develop_length` (no default) is verified behaviorally — `bin/jl -e 'using STREAM; geom = PipeGeometry_circular(1.0, 0.01); try; developing_laminar_h_spl(geom); error("expected UndefKeywordError"); catch e; @assert e isa UndefKeywordError "got $(typeof(e))"; end; println("develop_length is mandatory: OK")'` exits 0 and prints `develop_length is mandatory: OK`. This proves D-04 without depending on signature formatting.
    - Source assertion: `grep -E "^function regime_dependent\(geom::PipeGeometry;" src/physical_models/htc/correlations.jl` returns exactly one match.
    - Source negation: `grep -nE "^function elenbaas_htc\(;" src/physical_models/htc/correlations.jl` returns NO matches (old kwarg-only signature gone — D-01 clean break).
    - Source negation: `grep -nE "^function fully_developed_laminar_h_spl\(;" src/physical_models/htc/correlations.jl` returns NO matches.
    - Source negation: `grep -nE "^function developing_laminar_h_spl\(;" src/physical_models/htc/correlations.jl` returns NO matches.
    - Source negation: `grep -nE "^function regime_dependent\(;" src/physical_models/htc/correlations.jl` returns NO matches.
    - Source negation: `grep -nE "Dh=nothing|Dh::|;\s*Dh,|, Dh," src/physical_models/htc/correlations.jl | grep -v '^#' | grep -v 'Dh_v\|Dh_val'` returns NO matches (Dh is not a user-facing kwarg or positional arg anywhere; only used as a local-derived variable Dh_v / Dh_val).
    - Source assertion: `grep -E "htc_natural provided but g is missing" src/physical_models/htc/correlations.jl` returns at least one match (group validation collapsed).
    - Source assertion: `grep -E "HTCCorrelation" src/physical_models/htc/correlations.jl` returns at least one match (alias is used in regime_dependent signature for documentation effect).
    - Behavior: the verify block command above exits 0 — all four refactored factories construct against a PipeGeometry and return Function-typed closures.
    - Source assertion: `grep -E "^function dittus_boelter|^dittus_boelter\(" src/physical_models/htc/correlations.jl` returns at least one match (unrelated function untouched).
    - Source assertion: `grep -E "^function maximal_htc|^function constant_Nusselt|^function Marco_Han_Nusselt|^elenbaas_nusselt\(" src/physical_models/htc/correlations.jl | grep -v '^#' | wc -l` returns >= 4 (all out-of-scope functions still present).
  </acceptance_criteria>
  <done>
    All four geometry-consuming factories accept geom::PipeGeometry as first
    positional argument. develop_length is a mandatory kwarg with no default.
    regime_dependent uses HTCCorrelation type annotations on htc_* kwargs. Old
    kwarg-only signatures are gone. Closure interfaces (Re, Pr, T_bulk, T_wall)
    -> Nu and (Re) -> f_darcy are preserved. Out-of-scope functions
    (dittus_boelter, constant_Nusselt, maximal_htc, etc.) and private helpers
    are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update HTC and regime_dependent testsets to geom-first calls</name>
  <files>test/test_correlations.jl</files>
  <read_first>
    - test/test_correlations.jl (current state — HTC-02 unit ~lines 542-563, HTC-03 unit ~lines 566-617, NATCONV-01 elenbaas_htc factory test ~lines 371-380, NATCONV-01 regime_dependent NC ~lines 431-528, PHY-04 unit ~lines 73-99, PHY-04 integration ~lines 234-345, HTC-02/HTC-03 integration ~lines 621-720)
    - src/physical_models/htc/correlations.jl (NEW state after Task 1 — confirm refactored signatures)
    - src/geometry.jl (PipeGeometry_rectangular / PipeGeometry_circular constructors)
    - .planning/phases/59-correlation-geom-first-refactor/59-CONTEXT.md (D-04 mandatory develop_length; D-06 keep single test file)
  </read_first>
  <action>
    Update every HTC-factory test call and regime_dependent test call in
    test/test_correlations.jl. Do NOT modify the PHY-03 laminar_friction-only
    testsets — Plan 01 owns those. Where a testset mixes both (PHY-04
    regime_dependent integration tests already include
    `friction_laminar=laminar_friction(geom)` after Plan 01), this task updates
    only the regime_dependent constructor call and the HTC-factory kwargs in
    the same testset, accepting Plan 01's prior laminar_friction edits.

    Specific edits:

    ─── HTC-02 unit testset (~line 542-563) ───
    - `htc_fn = fully_developed_laminar_h_spl(Dh=0.005, aspect_ratio=0.0)` →
      construct `geom = PipeGeometry_rectangular(L_dummy, edge1, edge2, edge1)`
      with `(edge1=1.0, edge2=0.0)` producing depth=0.0, width=1.0 → aspect=0.0.
      (For aspect_ratio=0.0 tests use `edge2 = 0.0`; for aspect_ratio=0.2 use
      `edge1=1.0, edge2=0.2`; for aspect_ratio=1.0 use `edge1=edge2=0.01`.) Then
      call `htc_fn = fully_developed_laminar_h_spl(geom)`. Expected Nu values
      from `_two_sided_heating_nusselt` are unchanged because they depend on
      aspect_ratio only.
    - Repeat for `htc_fn_ar02 = fully_developed_laminar_h_spl(Dh=0.005, aspect_ratio=0.2)`
      and `htc_sq = fully_developed_laminar_h_spl(Dh=0.01, aspect_ratio=1.0)`.

    ─── HTC-03 unit testset (~line 566-617) ───
    - `htc_dev = developing_laminar_h_spl(Dh=0.005, develop_length=0.3, aspect_ratio=ar)` →
      construct geom with Dh=0.005 (`PipeGeometry_rectangular` parameters that
      yield Dh=0.005 — easiest: `PipeGeometry_circular(L_dummy, 0.005)` since
      circular gives `geom.Dh = D`; but circular has aspect_ratio=1.0, which
      breaks ar variation. Use `PipeGeometry_rectangular(1.0, edge1, edge2, edge1)`
      with `edge1` and `edge2` chosen so `4*edge1*edge2/(2*(edge1+edge2)) =
      Dh_target` AND `min/max = ar_target`. For (Dh=0.005, ar=ar): given
      `ar = depth/width` and `Dh = 4*depth*width/(2*(depth+width)) =
      2*depth*width/(depth+width)`, with `depth = ar*width`, solve:
      `Dh = 2*ar*width^2 / (ar*width + width) = 2*ar*width / (ar+1)` →
      `width = Dh*(ar+1)/(2*ar)`. So for Dh=0.005, ar=0.1: width = 0.0275,
      depth = 0.00275. Add a local helper `_geom_for(Dh, ar)` at the top of
      the HTC-03 testset (or as a private `let` block) to keep call sites
      readable: `_geom_for(Dh, ar) = PipeGeometry_rectangular(1.0, Dh*(ar+1)/(2*ar), ar*Dh*(ar+1)/(2*ar), 1.0)`. Use it for every ar variant in HTC-03.
    - Switch every `developing_laminar_h_spl(Dh=..., develop_length=L, aspect_ratio=ar)` to `developing_laminar_h_spl(_geom_for(Dh, ar); develop_length=L)`.
    - Same treatment for the paired `fully_developed_laminar_h_spl(Dh=..., aspect_ratio=ar)` calls in this testset (use `_geom_for(Dh, ar)`).
    - Verify that the testset already asserts `htc_dev ≈ htc_fd` in the limit
      `develop_length → large`; that property is geometry-derived and is
      preserved when both factories receive the same geom.

    ─── NATCONV-01 elenbaas_htc factory test (~line 371-380) ───
    - `htc_fn = elenbaas_htc(b=0.00254, L=0.6, Dh=0.00254)` → construct
      `geom = PipeGeometry_rectangular(0.6, edge_wide, 0.00254, edge_wide)`
      with `edge_wide` chosen so that `geom.Dh ≈ 0.00254`. For a long-thin
      rectangle (gap << width), Dh ≈ 2 * gap, so to get Dh = 0.00254 with gap
      = 0.00254 requires `edge_wide → ∞`. That is not viable; instead use
      `PipeGeometry_circular(0.6, 0.00254)` so geom.Dh = 0.00254 and
      geom.depth = 0.00254 (D-03 docstring already warns this is a non-plate
      geometry but the runtime is trust-the-user). Then call
      `htc_fn = elenbaas_htc(geom)` (g defaults to 9.81). The test's existing
      numeric expectations for Ra/Nu are computed from the formula and depend
      only on b=geom.depth, L=geom.L, Dh=geom.Dh, g — all preserved.
    - REQUIRED inline comment: immediately above the `geom = PipeGeometry_circular(0.6, 0.00254)` line in the NATCONV-01 elenbaas_htc testset, insert the following multi-line comment so future readers understand the deliberate D-03 anti-pattern exercise:
      ```julia
      # D-03 (CONTEXT.md): elenbaas_htc docstring warns against non-rectangular geom.
      # Test uses PipeGeometry_circular here to preserve numerical parity with the
      # pre-refactor (b=0.00254, L=0.6, Dh=0.00254) fixture — circular yields
      # width==depth==Dh, which is identical to the old kwarg inputs. The trust-the-user
      # posture is exercised deliberately. Do NOT change to rectangular without
      # re-deriving the expected Nu.
      ```
      This comment is load-bearing for future-reader intent; do not omit or paraphrase the D-03 reference and the elenbaas_htc / non-rectangular tokens (the acceptance grep keys on those words).

    ─── NATCONV-01 regime_dependent NC tests (~line 431-528) ───
    - Every `rd = regime_dependent(...)` call site must switch to
      `rd = regime_dependent(geom; ...)`. Construct geom once at the top of
      the testset using the existing Dh and L values from the current test
      setup (look for `Dh = ...` near the test header and reuse). Remove the
      `Dh=Dh` kwarg from every regime_dependent call. Keep `g=g` (now a sole
      mandatory companion to htc_natural).
    - For the `@test_throws ArgumentError regime_dependent(...)` block at
      ~line 478: the existing test exercises the (htc_natural present, Dh
      missing) case. After refactor this case can no longer be constructed
      (Dh is not a user-facing arg). Replace with a test that exercises the
      new collapsed group: `@test_throws ArgumentError regime_dependent(geom;
      htc_laminar=..., htc_turbulent=..., friction_laminar=..., friction_turbulent=...,
      htc_natural=elenbaas_htc(geom))` — i.e. htc_natural provided but g
      omitted. The ArgumentError text now reads "htc_natural provided but g
      is missing".
    - For the `@test_logs (:warn, r"NC regime will not be detected") regime_dependent(...)` block at ~line 488: the warn was removed entirely in Task 1
      (Dh is no longer user-facing; only `g` could be a stray-without-htc_natural
      signal and the design decision was to drop the warn). DELETE this
      `@test_logs` block entirely. Add a one-line comment in its place:
      `# Phase 59 D-01: regime_dependent stray-kwarg @warn dropped; Dh no longer user-facing, lone g without htc_natural is a permitted no-op.`

    ─── PHY-04 unit testset (~line 73-99) ───
    - `rd = regime_dependent(htc_laminar=constant_Nusselt(Nu=8.235), htc_turbulent=dittus_boelter, friction_laminar=laminar_friction(0.01814), friction_turbulent=blasius_friction, Re_transition=2300.0)`
      → construct geom (use the same edges scheme Plan 01 chose for PHY-03
      unit: `PipeGeometry_rectangular(0.6, 0.07, 0.0012698, 0.07)` → aspect_ratio
      ≈ 0.01814) and pass `regime_dependent(geom; htc_laminar=..., htc_turbulent=..., friction_laminar=laminar_friction(geom), friction_turbulent=..., Re_transition=2300.0)`.
      Note: Plan 01 (Wave 1) has already landed when this plan executes — its
      changes to `friction_laminar=laminar_friction(geom)` in the PHY-04 unit
      testset are visible. This plan's edits to that testset modify the
      `regime_dependent(...)` call only; the `friction_laminar` line is already
      in its target state.

    ─── PHY-04 integration testsets (~line 234-345) ───
    - The laminar and turbulent branch integration tests already construct
      `geom = PipeGeometry_rectangular(...)` near the top of each testset.
      Switch `rd = regime_dependent(htc_laminar=..., friction_laminar=laminar_friction(geom), ...)` to
      `rd = regime_dependent(geom; htc_laminar=..., friction_laminar=laminar_friction(geom), ...)`.
      Drop any `Dh=geom.Dh` if present. Keep all other kwargs as-is.

    ─── HTC-02 / HTC-03 integration testsets (~line 621-720) ───
    - The "compiles in Channel" integration tests at lines ~626 and ~677 use
      `htc_fn = fully_developed_laminar_h_spl(Dh=0.01, aspect_ratio=0.1)` and
      `htc_fn = developing_laminar_h_spl(Dh=0.01, develop_length=0.3, aspect_ratio=0.1)`.
      Each testset constructs a geom for the surrounding ChannelAndContacts
      build (look for `geometry=...` in the @named call). Switch to
      `htc_fn = fully_developed_laminar_h_spl(geom)` and
      `htc_fn = developing_laminar_h_spl(geom; develop_length=0.3)` reusing
      the same geom variable.
    - The `friction_correlation=laminar_friction(0.1)` calls at lines ~639 and
      ~690 are owned by Plan 01's call-site cleanup IF they fit Plan 01's
      pattern of scalar→geom; otherwise they fall to this task. Inspect: at
      Plan 01 commit time only PHY-03 testsets were edited; these
      HTC-integration testsets at ~639 and ~690 mix HTC + friction usage and
      are owned by this Plan 02 task. Switch them to
      `friction_correlation=laminar_friction(geom)` here.

    After all edits, run `bin/jl test/test_correlations.jl` once and ensure
    exit 0. If a remaining MethodError mentions `Dh`, `b`, `L`, or
    `aspect_ratio` as a kwarg, you have missed a call site — grep
    `test/test_correlations.jl` for those tokens to find it.
  </action>
  <verify>
    <automated>bin/jl test/test_correlations.jl</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `bin/jl test/test_correlations.jl` exits 0 (all HTC-02, HTC-03, NATCONV-01, NATCONV-02, PHY-04 testsets pass; PHY-03 testsets already migrated by Plan 01 still pass).
    - Source negation: `grep -nE "elenbaas_htc\(b=|elenbaas_htc\(;\s*b" test/test_correlations.jl` returns NO matches.
    - Source negation: `grep -nE "fully_developed_laminar_h_spl\(Dh=" test/test_correlations.jl` returns NO matches.
    - Source negation: `grep -nE "developing_laminar_h_spl\(Dh=" test/test_correlations.jl` returns NO matches.
    - Source negation: `grep -nE "regime_dependent\(\s*htc_laminar=" test/test_correlations.jl` returns NO matches (every regime_dependent call now starts with geom positional, no longer with htc_laminar as the first kwarg).
    - Source negation: `grep -nE "Dh=Dh\b" test/test_correlations.jl` returns NO matches (regime_dependent Dh=Dh paired-kwarg pattern fully removed).
    - Source assertion: `grep -cE "regime_dependent\(geom" test/test_correlations.jl` returns a count >= 4 (unit PHY-04, integration PHY-04 laminar branch, integration PHY-04 turbulent branch, NATCONV-01 NC + collapsed-group test).
    - Source assertion: `grep -E "htc_natural provided but g is missing" test/test_correlations.jl` returns at least one match (the new @test_throws message string).
    - Source negation: `grep -nE "@test_logs.*NC regime will not be detected" test/test_correlations.jl` returns NO matches (the stale @warn test was removed per Task 1's deletion of the @warn).
    - Source assertion: `grep -E "D-03.*elenbaas_htc.*non-rectangular" test/test_correlations.jl` returns at least 1 match (the inline D-03 anti-pattern comment is present above the NATCONV-01 elenbaas_htc PipeGeometry_circular line).
  </acceptance_criteria>
  <done>
    test/test_correlations.jl HTC-02, HTC-03, NATCONV-01 (elenbaas_htc factory
    test + regime_dependent NC tests), PHY-04 (unit + integration) all
    construct PipeGeometry locally and pass geom to each refactored factory.
    The @test_logs warn-stray-Dh assertion is removed. The @test_throws
    ArgumentError test exercises the new (htc_natural, g) collapsed group.
    Full test file exits 0 under `bin/jl test/test_correlations.jl`.
  </done>
</task>

</tasks>

<threat_model>
No exposed surface — pure numerical library refactor, no new attack surface
introduced. No auth, network, user-input parsing, DB, or external service.
Per CONTEXT.md branch policy this phase commits to gui-redesign only; no
release artifact ships from this plan.
</threat_model>

<verification>
- `bin/jl test/test_correlations.jl` — full file passes (assuming Plan 01 has
  shipped first, which the wave structure guarantees through Plan 03's gate;
  if executed in parallel with Plan 01 the file may transiently fail until
  both are merged — Plan 03 owns the integrated test gate).
- `bin/jl -e 'using STREAM; geom = PipeGeometry_rectangular(0.6, 0.07, 0.0025, 0.07); rd = regime_dependent(geom; htc_laminar=constant_Nusselt(), htc_turbulent=dittus_boelter, friction_laminar=(Re)->64/Re, friction_turbulent=blasius_friction, htc_natural=elenbaas_htc(geom), g=9.81)'` — full geom-first construction chain works end-to-end.
</verification>

<success_criteria>
- All four geometry-consuming HTC factories take geom::PipeGeometry first
  positional; old kwarg-only forms removed.
- regime_dependent group validation collapses from (htc_natural, Dh, g) to
  (htc_natural, g).
- HTCCorrelation type alias appears as annotation on regime_dependent's
  htc_* kwargs.
- All HTC and regime_dependent testsets in test/test_correlations.jl pass.
- Out-of-scope functions (dittus_boelter, constant_Nusselt, maximal_htc,
  elenbaas_nusselt, Marco_Han_Nusselt, _two_sided_heating_nusselt,
  _nusselt_coefficient_developing) unchanged.
- No new branches created. Commits land on gui-redesign per CLAUDE.md branching policy.
</success_criteria>

<output>
After completion, create `.planning/phases/59-correlation-geom-first-refactor/59-02-SUMMARY.md`
</output>
