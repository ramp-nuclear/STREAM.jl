---
phase: 59-correlation-geom-first-refactor
plan: 04
type: execute
wave: 4
depends_on: ["59-03"]
files_modified:
  - .planning/notes/correlation-geom-first-api.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "`.planning/notes/correlation-geom-first-api.md` exists and is the Phase 61 handoff artifact per D-05."
    - "The doc contains a table covering every refactored factory (laminar_friction, elenbaas_htc, fully_developed_laminar_h_spl, developing_laminar_h_spl, regime_dependent) with final signature, geom fields read, and remaining kwargs."
    - "The doc explicitly states which factories were NOT touched (dittus_boelter, blasius_friction, turbulent_friction, constant_Nusselt, maximal_htc, regime_dependent_q_scb) per the §3.1 scope and D-02."
  artifacts:
    - path: ".planning/notes/correlation-geom-first-api.md"
      provides: "Phase 61 (GUI registry rewrite) handoff doc — single source of truth for the new API surface."
      min_lines: 60
  key_links:
    - from: ".planning/notes/correlation-geom-first-api.md"
      to: "src/physical_models/htc/correlations.jl + src/physical_models/friction/correlations.jl + src/STREAM.jl (HTCCorrelation)"
      via: "Direct factory-by-factory cross-reference; Phase 61 reads this doc instead of re-deriving from source"
      pattern: "laminar_friction|elenbaas_htc|fully_developed_laminar_h_spl|developing_laminar_h_spl|regime_dependent"
---

<objective>
Emit `.planning/notes/correlation-geom-first-api.md` per D-05 (CONTEXT.md
`<decisions>`). This is the Phase 61 handoff artifact: when the GUI registry
rewrite phase reads correlation parameter shapes from the source, it consumes
this doc instead of re-deriving from source files.

Per D-05: the doc lives in .planning/notes/ alongside
gui-redesign-design-decisions.md. It does NOT replace docstrings — those
remain the in-source source of truth. The doc captures the post-refactor
API surface only.

Purpose: Concrete deliverable for Phase 61. Runs in Wave 3 because it only
makes sense to emit after Plans 01-03 have landed and the parity gate has
confirmed the refactor is correct.

Output: Single Markdown doc with one table covering all five refactored
factories plus a "Not touched" section.
</objective>

<execution_context>
@/home/itay/projects/STREAM.jl/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/STREAM.jl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-correlation-geom-first-refactor/59-CONTEXT.md
@.planning/phases/59-correlation-geom-first-refactor/59-01-PLAN.md
@.planning/phases/59-correlation-geom-first-refactor/59-02-PLAN.md
@.planning/phases/59-correlation-geom-first-refactor/59-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author correlation-geom-first-api.md handoff doc</name>
  <files>.planning/notes/correlation-geom-first-api.md</files>
  <read_first>
    - .planning/phases/59-correlation-geom-first-refactor/59-CONTEXT.md (D-02 explicit scope guard for regime_dependent_q_scb; D-04 mandatory develop_length; D-05 doc structure suggestion in `<specifics>`; full `<domain>` In scope / Out of scope lists)
    - .planning/notes/gui-redesign-design-decisions.md (§3.1 lines 118-201 — locked convention; lines 1297 referencing this in Phase 61 dependencies)
    - src/physical_models/htc/correlations.jl (POST-PLAN-02 state — confirm final signatures verbatim before transcribing into the table)
    - src/physical_models/friction/correlations.jl (POST-PLAN-01 state — confirm final laminar_friction signature)
    - src/STREAM.jl (confirm HTCCorrelation alias and export are present)
  </read_first>
  <action>
    Create `.planning/notes/correlation-geom-first-api.md` with the following
    sections (in order). Write the file using the Write tool; do not use
    heredoc. Use Markdown headings, fenced ` ```julia ` code blocks for
    signatures, and pipe tables for the factory matrix.

    ─── Section 1: Header ───
    Top-line statement that this doc captures the post-Phase-59 correlation
    API surface, is the canonical handoff for Phase 61 (GUI registry audit),
    and lives alongside gui-redesign-design-decisions.md. Cite Phase 59
    CONTEXT.md D-05 as the source decision.

    ─── Section 2: Type alias ───
    Document `const HTCCorrelation = Function` exported from src/STREAM.jl.
    State its role: documentation-only annotation for closure-arg positions
    (e.g. regime_dependent's htc_laminar/htc_turbulent/htc_natural kwargs);
    not runtime-enforcing. Cite §3.1 of design-decisions.

    ─── Section 3: Refactored factories table ───
    A single Markdown pipe table with header:
    `| Factory | File | Final signature | geom fields read | Remaining kwargs | Notes |`

    One row per refactored factory:

      1. laminar_friction
         File: src/physical_models/friction/correlations.jl
         Final signature: `laminar_friction(geom::PipeGeometry) -> (Re) -> f_darcy`
         geom fields read: depth, width (derives aspect_ratio = depth/width)
         Remaining kwargs: none
         Notes: For circular geometry (depth == width), k_R ≈ 1.1246 giving f ≈ 56.9/Re. Use raw lambda (Re) -> 64/Re for strict-circular friction.

      2. elenbaas_htc
         File: src/physical_models/htc/correlations.jl
         Final signature: `elenbaas_htc(geom::PipeGeometry; g=9.81) -> (Re, Pr, T_bulk, T_wall) -> Nu`
         geom fields read: depth (as plate gap b), L, Dh
         Remaining kwargs: g (default 9.81 m/s²)
         Notes: Trust-the-user posture per D-03. Documented as parallel-vertical-plates NC; no runtime check on geometry kind.

      3. fully_developed_laminar_h_spl
         File: src/physical_models/htc/correlations.jl
         Final signature: `fully_developed_laminar_h_spl(geom::PipeGeometry) -> (Re, Pr, T_bulk, T_wall) -> Nu`
         geom fields read: depth, width (derives aspect_ratio)
         Remaining kwargs: none
         Notes: Dh is available from geom but intentionally not consumed (matches pre-refactor behavior — Nu depends on aspect_ratio only).

      4. developing_laminar_h_spl
         File: src/physical_models/htc/correlations.jl
         Final signature: `developing_laminar_h_spl(geom::PipeGeometry; develop_length) -> (Re, Pr, T_bulk, T_wall) -> Nu`
         geom fields read: depth, width, Dh
         Remaining kwargs: develop_length (MANDATORY, no default per D-04)
         Notes: develop_length is the distance from channel entrance at which Nu is evaluated. GUI registry should encode this as a required field.

      5. regime_dependent
         File: src/physical_models/htc/correlations.jl
         Final signature: `regime_dependent(geom::PipeGeometry; htc_laminar::HTCCorrelation, htc_turbulent::HTCCorrelation, friction_laminar::Function, friction_turbulent::Function, htc_natural::Union{HTCCorrelation,Nothing}=nothing, g=nothing, Re_transition=2300) -> (htc=fn, friction=fn)`
         geom fields read: Dh (only used in NC-enabled path for Gr formula)
         Remaining kwargs: htc_laminar, htc_turbulent, friction_laminar, friction_turbulent (all four required); htc_natural (optional, default nothing); g (paired with htc_natural — ArgumentError if htc_natural provided without g); Re_transition (default 2300)
         Notes: Group validation collapsed from (htc_natural, Dh, g) to (htc_natural, g) in Phase 59 since Dh is no longer a user-facing kwarg. The stray-kwarg @warn for Dh-without-htc_natural was removed.

    ─── Section 4: Not touched (explicit non-modifications) ───
    Bulleted list, each with one-line "why" anchored to a CONTEXT.md decision:

      - dittus_boelter (src/physical_models/htc/correlations.jl): stateless direct function, no geom needed. §3.1 "Stateless direct functions stay direct functions."
      - blasius_friction (src/physical_models/friction/correlations.jl): same — stateless.
      - turbulent_friction (src/physical_models/friction/correlations.jl): same — stateless. epsilon stays a kwarg per §3.1 "Pure tuning kwargs stay kwargs."
      - constant_Nusselt (src/physical_models/htc/correlations.jl): no geom-dependent state. Nu kwarg is a tuning kwarg per §3.1.
      - maximal_htc (src/physical_models/htc/correlations.jl): combinator over already-built HTC closures; no geom of its own.
      - elenbaas_nusselt (src/physical_models/htc/correlations.jl): standalone formula, not a factory.
      - Marco_Han_Nusselt (src/physical_models/htc/correlations.jl): standalone formula.
      - viscosity_correction (src/physical_models/friction/correlations.jl): standalone formula.
      - rectangular_laminar_correction (src/physical_models/friction/correlations.jl): private helper consumed inside laminar_friction.
      - regime_dependent_q_scb (src/physical_models/subcooled_boiling.jl): same factory pattern but reads no geom fields today. Touching would be churn. Per D-02, deferred until a future correction inside q_scb needs Dh.
      - Private helpers (_two_sided_heating_nusselt, _nusselt_coefficient_developing, _bergles_rohsenow_dT_ONB): not factories; not user-facing.

    ─── Section 5: GUI registry implications ───
    Short paragraph (3-5 bullets) summarizing what Phase 61 should do with
    this doc:
      - For each refactored factory, the GUI registry's parameter tree
        collapses: the Dh / L / b / aspect_ratio user-facing fields are
        replaced by a single "geom reference" pointing to the parent channel's
        PipeGeometry resource.
      - Remaining kwargs become the only editable fields per factory in the
        GUI: g (default 9.81), develop_length (mandatory, no default),
        Re_transition (default 2300), Nu (for constant_Nusselt, default
        8.235), epsilon (for turbulent_friction, default 0).
      - The HTCCorrelation alias has no GUI-visible effect; it is source-only
        documentation.
      - Per §3.10 / §3.11, htc_correlation / friction_correlation are
        Properties-tab fields on ChannelAndContacts (constructor kwargs); they
        accept factory-call expressions whose first argument is the channel's
        own geom.

    ─── Section 6: Validation / parity status ───
    Single sentence pointing to the parity gate result captured in
    Plan 03's SUMMARY: "Python parity gate (test/test_validation.jl) confirmed
    no semantic drift from this refactor; see .planning/phases/59-correlation-geom-first-refactor/59-03-SUMMARY.md."

    The doc must be self-contained — Phase 61 reads only this doc and
    docstrings, not Phase 59 plan artifacts.
  </action>
  <verify>
    <automated>test -f .planning/notes/correlation-geom-first-api.md && grep -cE "laminar_friction|elenbaas_htc|fully_developed_laminar_h_spl|developing_laminar_h_spl|regime_dependent" .planning/notes/correlation-geom-first-api.md | awk '{ exit ($1 < 10) ? 1 : 0 }'</automated>
  </verify>
  <acceptance_criteria>
    - File existence: `test -f .planning/notes/correlation-geom-first-api.md` returns 0.
    - File non-empty: `wc -l .planning/notes/correlation-geom-first-api.md` returns >= 60 lines.
    - Source assertion: `grep -E "^## " .planning/notes/correlation-geom-first-api.md | wc -l` returns >= 5 (sections 1-6, where Section 1 may be `#` and the rest `##`).
    - Source assertion: `grep -cE "laminar_friction\(geom::PipeGeometry\)" .planning/notes/correlation-geom-first-api.md` returns at least 1.
    - Source assertion: `grep -cE "elenbaas_htc\(geom::PipeGeometry" .planning/notes/correlation-geom-first-api.md` returns at least 1.
    - Source assertion: `grep -cE "fully_developed_laminar_h_spl\(geom::PipeGeometry\)" .planning/notes/correlation-geom-first-api.md` returns at least 1.
    - Source assertion: `grep -cE "developing_laminar_h_spl\(geom::PipeGeometry;\s*develop_length\)" .planning/notes/correlation-geom-first-api.md` returns at least 1 (mandatory-no-default form preserved in the doc).
    - Source assertion: `grep -cE "regime_dependent\(geom::PipeGeometry;" .planning/notes/correlation-geom-first-api.md` returns at least 1.
    - Source assertion: `grep -E "HTCCorrelation\s*=\s*Function" .planning/notes/correlation-geom-first-api.md` returns at least 1 (the type-alias declaration is documented).
    - Source assertion: `grep -E "regime_dependent_q_scb" .planning/notes/correlation-geom-first-api.md` returns at least 1 (D-02 deferred item is explicitly listed).
    - Source assertion: `grep -E "dittus_boelter|blasius_friction|turbulent_friction|constant_Nusselt|maximal_htc" .planning/notes/correlation-geom-first-api.md | wc -l` returns >= 5 (all out-of-scope stateless functions explicitly listed in Section 4).
    - Source assertion: `grep -E "Phase 61" .planning/notes/correlation-geom-first-api.md` returns at least 1 (handoff target identified by name).
  </acceptance_criteria>
  <done>
    `.planning/notes/correlation-geom-first-api.md` exists, is ≥ 60 lines, contains
    one row per refactored factory in the API table, lists every untouched
    correlation function in Section 4, names Phase 61 as the consumer, and
    documents the HTCCorrelation alias. Phase 61 has a single self-contained
    artifact to read.
  </done>
</task>

</tasks>

<threat_model>
No exposed surface — pure docs artifact in .planning/notes/. No new attack
surface. Commits land on gui-redesign per CLAUDE.md branching policy.
</threat_model>

<verification>
- File exists, line count >= 60.
- All five refactored factory signatures appear verbatim in the table.
- All untouched correlation functions listed in Section 4 with cited rationale.
- HTCCorrelation alias documented in Section 2.
- Phase 61 named as consumer in Section 5.
</verification>

<success_criteria>
- `.planning/notes/correlation-geom-first-api.md` exists and satisfies every grep acceptance criterion in Task 1.
- D-05 is closed: Phase 61 has a single canonical handoff artifact.
- No source files modified by this plan (docs-only).
- No new branches created. Commit lands on gui-redesign.
</success_criteria>

<output>
After completion, create `.planning/phases/59-correlation-geom-first-refactor/59-04-SUMMARY.md`
</output>
