---
phase: 60-fuel-assembly-composition-helper
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - test/test_composition.jl
  - .planning/notes/fuel-assembly-api.md
autonomous: true
requirements:
  - "D-06: four-variant parity tests + ArgumentError test coverage + smoke test"
  - "D-07: Phase 61 handoff note at .planning/notes/fuel-assembly-api.md"

must_haves:
  truths:
    - "Variant 1 (channel-bookended, k=2) helper-built system has the same steady-state unknown vector as a hand-rolled connect() system within rtol=1e-10"
    - "Variant 2 (plate-bookended, k=2) helper-built system matches hand-rolled within rtol=1e-10"
    - "Variant 3 (mixed, k=2, start=:channel) helper-built system matches hand-rolled within rtol=1e-10"
    - "Variant 4 (closed annular, k=3) helper-built system matches hand-rolled within rtol=1e-10"
    - "All four documented ArgumentError paths fire (bookend-vs-length conflict; bookend=:mixed without start; start on non-mixed bookend; closed=true with unequal lengths)"
    - "Smoke test confirms helper returns an uncompiled ModelingToolkit.AbstractSystem (no premature mtkcompile inside the helper)"
    - "Phase 61 reads .planning/notes/fuel-assembly-api.md and gets enough spec to write the codeGenerator detection rule without rereading src/"
  artifacts:
    - path: test/test_composition.jl
      provides: "fuel_assembly testset block — four variant parity tests + four ArgumentError tests + smoke test"
      contains: "fuel_assembly"
    - path: .planning/notes/fuel-assembly-api.md
      provides: "Phase 60 to Phase 61 spec handoff for fuel_assembly"
      contains: "Topology-detection rule"
  key_links:
    - from: test/test_composition.jl
      to: src/composition/helpers.jl
      via: "calls STREAM.fuel_assembly() and compares against hand-rolled connect() chains"
      pattern: "fuel_assembly\\("
    - from: .planning/notes/fuel-assembly-api.md
      to: .planning/notes/correlation-geom-first-api.md
      via: "sibling-shape Phase 61 handoff note (D-07 mirrors Phase 59 D-05)"
      pattern: "Phase 61"
---

<objective>
Lock the `fuel_assembly` contract from plan 60-01 with three deliverables:

1. **Variant parity tests** in `test/test_composition.jl`: one `@testset` per
   variant (1/2/3/4) at the representative `k` from D-06, each comparing the
   helper-built system against a hand-rolled `connect()` chain pointwise at
   machine precision (`rtol=1e-10`).
2. **ArgumentError tests** in the same file: bookend-vs-length conflict;
   `bookend=:mixed` without `start`; `start` with non-mixed bookend;
   `closed=true` with unequal lengths.
3. **Phase 61 handoff note** at `.planning/notes/fuel-assembly-api.md`
   (D-07) — sibling to `correlation-geom-first-api.md`. Captures the final
   signature, the four variants in ascii-art, the topology-detection rule
   spec, and the endpoint/wraparound rules.

Purpose: D-06 parity is the load-bearing acceptance gate for the helper.
D-07 unblocks Phase 61's codeGenerator rewrite.

Output: extended `test/test_composition.jl` + new
`.planning/notes/fuel-assembly-api.md`. No `src/` changes.
</objective>

<execution_context>
@/home/itay/projects/STREAM.jl/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/STREAM.jl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/60-fuel-assembly-composition-helper/60-CONTEXT.md
@.planning/phases/60-fuel-assembly-composition-helper/60-PATTERNS.md
@.planning/notes/gui-redesign-design-decisions.md
@.planning/notes/correlation-geom-first-api.md
@CLAUDE.md
@src/composition/helpers.jl
@test/test_composition.jl

<interfaces>
Identifiers the tests will consume (post plan 60-01).

From src/composition/helpers.jl (added by plan 60-01):
- `fuel_assembly(channels::Vector{<:AbstractSystem}, plates::Vector{<:AbstractSystem}; bookend=:auto, start=nothing, closed=false, name) -> ODESystem`
- Raises `ArgumentError` for invalid kwargs / shape mismatches.

From test/test_composition.jl (existing — reuse without modifying):
- `_mtr_pair(; n=4, nz=4, nx=2)` fixture (lines 15–25). For tests needing multiple distinct CAC/HD instances per testset, each instance must have a unique `@named` Symbol — see "Naming caveat" in 60-PATTERNS.md File 3.
- Existing testset pattern (Sections 4–6) — `_mtr_pair` -> helper call -> `@test rods isa ModelingToolkit.AbstractSystem` -> add BCs via `compose_systems` -> `mtkcompile` -> solve -> `@test sol.retcode == ReturnCode.Success`.

From src/STREAM.jl exports (after plan 60-01):
- `fuel_assembly` plus all existing exports (`Pump`, `HeatExchanger`, `solve_steady`, etc.) — all callable as `STREAM.X` or unqualified inside `using STREAM` blocks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fuel_assembly variant parity + ArgumentError testsets to test/test_composition.jl</name>
  <files>test/test_composition.jl</files>
  <read_first>
    - test/test_composition.jl (full file — must understand existing `_mtr_pair` fixture lines 15–25, Section 4 symmetric_plate parity testset lines 126–151 for testset shape, Section 7 compose_systems test lines 313–341 for multi-CAC naming convention, and the existing `@test_throws ErrorException` line ~304–307 for the ArgumentError analogy)
    - .planning/phases/60-fuel-assembly-composition-helper/60-CONTEXT.md (D-06 test plan — locked representative k values: k=2 for variants 1/2/3, k=3 for variant 4; locked parity gate: `isapprox(unknown vectors; rtol=1e-10)` after `solve_steady` with `mtkcompile(...; build_initializeprob=false)`)
    - .planning/phases/60-fuel-assembly-composition-helper/60-PATTERNS.md (File 3 section — parity-test shape, Naming caveat for multiple CACs, section placement: append at file tail after the last existing testset; do NOT renumber existing sections)
    - .planning/phases/60-fuel-assembly-composition-helper/60-01-SUMMARY.md (the plan-01 summary — confirms the chosen exception type is `ArgumentError`, so tests use `@test_throws ArgumentError`. If the SUMMARY reports a different choice for any reason, surface the mismatch before writing tests.)
    - src/composition/helpers.jl (the `fuel_assembly` function added by plan 60-01 — read the new function block to confirm the exact ArgumentError messages so tests can target them by behavior, not by string)
  </read_first>
  <action>
    Append a new section header comment block to `test/test_composition.jl` (after the existing final testset, file end) — header comment "fuel_assembly — Phase 60" identifying the source plan, mirroring the section-header style of the existing sections.

    For multi-CAC testsets, use the inline `@named` pattern shown in the existing test/test_composition.jl Section 7 (lines 313–317): explicitly write `@named c1 = ChannelAndContacts(...); @named c2 = ChannelAndContacts(...); @named c3 = ChannelAndContacts(...)` and analogously for HDs. This is the **only** supported `@named` form — MTK's `@named` macro expands at parse time and requires a bare LHS identifier; an interpolated form like `@named $(Symbol(prefix, "_cac")) = ChannelAndContacts(...)` will NOT parse-expand correctly and is prohibited. 60-PATTERNS.md File 3 "Naming caveat" called this out. Reuse the existing `_mtr_pair` fixture for testsets that only need ONE pair; for testsets needing 2–3 distinct instances, inline the `@named` calls and pass the same `geometry`, `htc_correlation`, `friction_correlation`, `power_shape` arguments that `_mtr_pair` uses (factor the shared kwargs into a local `kw = (; geometry=..., htc_correlation=..., ...)` if the call sites get unwieldy). Do NOT introduce a `_mtr_pair_named` helper that uses metaprogramming or string-interpolated symbols.

    For each of the four variants, write one `@testset "fuel_assembly variant N (<descriptor>, k=<K>) parity"` block. The K values are fixed by D-06 (do NOT change them): K=2 for variants 1/2/3, K=3 for variant 4.

    Per-testset structure (this is the parity gate; the four testsets share this shape, only the alternation pattern + helper kwargs differ):
    1. Build component vectors using `_mtr_pair_named` with unique prefixes per instance. For variant 1 (channel-bookended, k=2): three CACs `c1, c2, c3` and two HDs `p1, p2`. Variant 2 (plate-bookended, k=2): two CACs, three HDs. Variant 3 (mixed, k=2, start=:channel): two CACs, two HDs. Variant 4 (closed, k=3): three CACs, three HDs. Each instance gets a distinct prefix so `@named` produces distinct Symbols.
    2. **Helper-built system:** Call `fuel_assembly` with the appropriate kwargs and `name=:asm_helper`. Then wrap in a pump-driven loop via `compose_systems`:
       - `@named pump = Pump(3.0e4)`, `@named bc = HeatExchanger(313.15)`.
       - Connect pump -> bc -> `asm_helper.<first-CAC>.port_in` -> ... -> `asm_helper.<last-CAC>.port_out` -> pump.port_in via a `Vector{Equation}` of `connect(...)` lines plus the pressure anchor `pump.port_in.P ~ 1.0e5`. The hydraulic series wiring goes through every CAC's port_in/port_out in alternation order — but ONLY the CACs (plates have no flow ports). For the closed annular variant (4) the hydraulic loop still serializes through all three CACs in sequence.
       - Bind every HD's power: `asm_helper.p<k>.power ~ 1.0e3`.
       - Combine into a top-level system: `full_helper = compose_systems(asm_helper, pump, bc; connections=conns, name=:full_helper)`.
       - `ssys_helper = mtkcompile(full_helper; build_initializeprob=false)`. Match 60-CONTEXT line 242 — `build_initializeprob=false` is mandatory for HD+CAC.
    3. **Hand-rolled system:** Repeat steps 1-2 BUT replace the `fuel_assembly(...)` call with an explicit `Vector{Equation}` of `connect(port(...), port(...))` pairs reproducing the exact wiring pattern from `src/composition/helpers.jl` `plate(...)` lines 217–230 — for each adjacent pair in the alternation, emit the two thermal-face comprehensions. For variant 4, also emit the wraparound pair (`connect(port(c1, :thermal_left, i), port(p3, :thermal_right, i))` style). Compose via `compose_systems` with the same name structure but `name=:asm_hand`. Same pump/bc/power BCs. `ssys_hand = mtkcompile(full_hand; build_initializeprob=false)`.
    4. **Parity comparison (D-06 gate):**
       - `@test ssys_helper isa ModelingToolkit.AbstractSystem` (smoke).
       - Build identical initial-condition vectors for both systems. Use the IC shape from the existing Section 4 fixture (lines 174–183): `Pair{Any,Any}[ [ssys.<asm>.<cac_k>.T[i] => 313.15 for i in 1:n]..., [ssys.<asm>.<plate_k>.T[i, j] => 313.15 for i in 1:nz for j in 1:nx]..., ssys.<asm>.<first_cac>.port_in.mdot => 0.2 ]`. Note the IC keys use `:asm_helper` vs `:asm_hand` — they are NOT identical strings, but they bind the same set of physical states.
       - Solve both: `sol_helper = solve_steady(ssys_helper, ic_helper)`, `sol_hand = solve_steady(ssys_hand, ic_hand)`.
       - `@test sol_helper.retcode == ReturnCode.Success && sol_hand.retcode == ReturnCode.Success`.
       - The unknown vectors `sol_helper.u` and `sol_hand.u` correspond to the same physical states up to compiler ordering. The robust comparison is to read back each state by symbolic key: build a sorted list of `(state_name, value)` pairs from both solutions using the symbolic accessors (`sol_helper[ssys_helper.asm_helper.<cac>.T[i]]` etc.), then `@test isapprox(values_helper, values_hand; rtol=1e-10)`. If the executor finds that the unknowns vector is order-stable across the two compilations (it typically is for identically-structured compose calls), `@test isapprox(sol_helper.u, sol_hand.u; rtol=1e-10)` is acceptable — but document the choice in a comment.

    Add four `@testset "fuel_assembly — ArgumentError on <case>"` blocks per D-06:
    1. **Bookend-vs-length conflict:** Build a variant-1-shaped pair (e.g., 3 CACs + 2 HDs) but pass `bookend=:plate`. `@test_throws ArgumentError fuel_assembly(channels, plates; bookend=:plate, name=:bad)`.
    2. **bookend=:mixed without start:** Build equal-length pair (2 CACs + 2 HDs). `@test_throws ArgumentError fuel_assembly(channels, plates; bookend=:mixed, name=:bad)` (no `start` kwarg).
    3. **start on non-mixed bookend:** Build a 3 CAC + 2 HD pair. `@test_throws ArgumentError fuel_assembly(channels, plates; start=:channel, name=:bad)` (bookend defaults to :auto, infers :channel, but `start` is set, contradiction).
    4. **closed=true with unequal lengths:** Build a 3 CAC + 2 HD pair. `@test_throws ArgumentError fuel_assembly(channels, plates; closed=true, name=:bad)`.

    Add a final smoke test, wrapped in `@testset "fuel_assembly — uncompiled ODESystem smoke" begin ... end` so the `^@testset.*fuel_assembly` regex matches it. Inside the testset: build variant 3 mixed k=2 the cheapest way, then `@test asm isa ModelingToolkit.AbstractSystem` plus `@test_nowarn mtkcompile(asm; build_initializeprob=false)` to confirm the helper returned an uncompiled system that compiles cleanly.

    **Section placement:** Append all new content to the end of `test/test_composition.jl` (do NOT renumber existing sections).

    Do NOT modify the existing `_mtr_pair`, any existing testset, or `test/runtests.jl`. `runtests.jl` already includes `test/test_composition.jl`.
  </action>
  <verify>
    <automated>cd /home/itay/projects/STREAM.jl &amp;&amp; grep -c "^@testset.*fuel_assembly" test/test_composition.jl &amp;&amp; bin/jl test/test_composition.jl</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^@testset.*fuel_assembly' test/test_composition.jl` returns at least 9 (4 variant parity + 4 ArgumentError + 1 smoke).
    - Each of the four variant testset blocks contains both a `fuel_assembly(` call AND a hand-rolled `connect(port(` chain (verifiable by grep within each block).
    - Each variant testset contains an `isapprox(...; rtol=1e-10)` assertion (this is the D-06 parity gate).
    - Each of the four ArgumentError testsets contains exactly one `@test_throws ArgumentError fuel_assembly(` call (grep `@test_throws ArgumentError fuel_assembly` returns 4).
    - `bin/jl test/test_composition.jl` exits 0 (status code) and the daemon log reports "done" with no `Test Failed` lines in the output. All nine new testsets pass.
    - `bin/jl test/runtests.jl` continues to pass for the previously-passing testsets (no regression in pre-existing Section 4–7 tests).
  </acceptance_criteria>
  <done>
    All four variants verified to machine precision against hand-rolled `connect()` chains; all four ArgumentError paths confirmed to fire; smoke test confirms helper returns an uncompiled `ODESystem`. The D-06 acceptance gate is closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 61 handoff note at .planning/notes/fuel-assembly-api.md</name>
  <files>.planning/notes/fuel-assembly-api.md</files>
  <read_first>
    - .planning/notes/correlation-geom-first-api.md (full file — this is the sibling-shape analog per D-07; mirror its header block, section structure, and footer)
    - .planning/phases/60-fuel-assembly-composition-helper/60-CONTEXT.md (D-07 specifies the four mandatory sections: signature, ascii-art variants, topology-detection rule spec, endpoint/wraparound rules; the `<specifics>` block lines 354–361 give the table seed)
    - .planning/phases/60-fuel-assembly-composition-helper/60-PATTERNS.md (File 4 section — skeleton)
    - .planning/notes/gui-redesign-design-decisions.md §3.12 (the ascii-art diagrams of the four variants live here — copy them verbatim into the handoff note)
    - src/composition/helpers.jl (the fuel_assembly function block — for the canonical signature line and docstring synopsis; the handoff note must round-trip the signature exactly)
    - .planning/phases/60-fuel-assembly-composition-helper/60-01-SUMMARY.md (confirms ArgumentError choice and any internal-helper structure that affects the public surface)
  </read_first>
  <action>
    Create new file `.planning/notes/fuel-assembly-api.md`. Mirror the structure of `.planning/notes/correlation-geom-first-api.md` (same header metadata block, same heading depth, same footer).

    Mandatory sections (D-07):

    1. **Header block** — title `# Fuel-assembly composition helper — Post-Phase-60 surface`, status line, source-decision reference (Phase 60 CONTEXT D-07), companion document reference (`.planning/notes/gui-redesign-design-decisions.md` §3.12), scope sentence ("Captures the post-Phase-60 helper API + GUI topology-detection rule. Does NOT replace docstrings."), and a "Phase 60 landed in 2 plans on the `gui-redesign` working branch: [commit SHAs filled at archive time]." line. Match the shape of `correlation-geom-first-api.md`'s header block 1:1.

    2. **`## Helper signature`** — verbatim final signature from plan 60-01 (`fuel_assembly(channels::Vector{<:AbstractSystem}, plates::Vector{<:AbstractSystem}; bookend::Symbol=:auto, start::Union{Symbol,Nothing}=nothing, closed::Bool=false, name::Symbol)`) followed by a short kwargs table or bullet list giving each kwarg's semantics (D-02 for bookend, D-03 for start, D-04 for closed). Include the closed-loop note: `start` defaults to `:channel` when `closed=true && start===nothing` (the helper's documented canonical orientation).

    3. **`## The four variants`** — Copy the four ascii-art diagrams verbatim from `.planning/notes/gui-redesign-design-decisions.md` §3.12. Each diagram gets a short heading: `### Variant 1 — channel-bookended`, `### Variant 2 — plate-bookended`, `### Variant 3 — mixed`, `### Variant 4 — closed annular`. Below each diagram, give the canonical call shape (from 60-CONTEXT.md `<specifics>` lines 339–351).

    4. **`## Topology-detection rule (for Phase 61)`** — The table seed from 60-CONTEXT `<specifics>` lines 354–361, expanded with two extra columns: "Input signature (graph view)" and "Emitted call shape (Julia)". The "Input signature" column describes what the GUI codeGenerator looks at — count of CAC nodes, count of HD nodes, presence of a closing edge, endpoint-kind. The "Emitted call shape" column gives the exact Julia source the codeGenerator should produce for each variant (`fuel_assembly([c1, c2, c3], [p1, p2]; name=:asm)` for variant 1, etc.). End the section with one bullet: "If the topology does NOT match any of variants 1–4, the codeGenerator falls through to direct `connect(...)` emission per the existing v0.8 behavior — `fuel_assembly` is opt-in detection, not a forced rewrite."

    5. **`## Endpoint / wraparound rules`** — Concrete pseudo-code (NOT Julia source, NOT JS source — pseudo-code in numbered steps) for the Phase 61 codeGenerator emitter:
       a. Walk the alternation sequence in order; for each adjacent pair (X, Y) emit `connect(port(X, :thermal_right, i), port(Y, :thermal_left, i))` if X is the left neighbour spatially (per the spatial-absolute L/R convention `helpers.jl` uses — flag the convention disagreement called out in STATE.md "Blockers/Concerns" as a known issue Phase 61 inherits, not creates).
       b. Endpoint adiabatic faces: variant 1's outer-CAC `thermal_left[1]` (on the first CAC) and `thermal_right[end]` (on the last CAC); variant 2's outer-HD faces; variant 3's two distinct endpoint faces; variant 4 has NO unconnected endpoints. The codeGenerator should NOT emit explicit adiabatic boundary equations — MTK's default handles unconnected faces (per `one_sided_connection`'s precedent, called out in 60-CONTEXT `<decisions>` "Adiabatic endpoint handling" bullet).
       c. Wraparound pair (variant 4 only): emit `connect(port(channels[1], :thermal_left, i), port(plates[end], :thermal_right, i))` for `i in 1:n`. This mirrors the per-cell pattern with `start=:channel` semantics (60-CONTEXT `<decisions>` last bullet, Claude's Discretion item).

    6. **`## GUI registry implications (Phase 61 guidance)`** — Parallel to the `correlation-geom-first-api.md` section of the same name. Bullet points:
       - `fuel_assembly` is a composition helper, not a component — it should NOT appear in `components.json` as a draggable node. It is a CODEGEN-time emission, not a USER-edit-time entity.
       - The topology detector lives in `gui/src/lib/codeGenerator.ts` (Phase 61 owns this file).
       - No new `scope` field is needed for `fuel_assembly` — it has no user-tunable parameters beyond the component vectors (which already come from the existing component nodes' `@named` declarations).
       - When the detector matches a topology, the emitted call replaces the per-pair `connect(...)` lines that would otherwise appear in the Composition section of the generated Julia code.

    7. **Footer** — `*Phase 60 handoff artifact. Last updated 2026-05-11.*` matching `correlation-geom-first-api.md`'s footer.

    Place the file at exactly `.planning/notes/fuel-assembly-api.md` (NOT under `.planning/phases/60-.../`). Per 60-PATTERNS.md File 4, the handoff note is a Phase 60 deliverable that survives phase archival.
  </action>
  <verify>
    <automated>cd /home/itay/projects/STREAM.jl &amp;&amp; test -f .planning/notes/fuel-assembly-api.md &amp;&amp; grep -c "^## " .planning/notes/fuel-assembly-api.md &amp;&amp; grep -c "^### Variant" .planning/notes/fuel-assembly-api.md &amp;&amp; grep "Topology-detection rule" .planning/notes/fuel-assembly-api.md &amp;&amp; grep "fuel_assembly(channels::Vector" .planning/notes/fuel-assembly-api.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/notes/fuel-assembly-api.md` exists.
    - File contains at least 6 `^## ` top-level section headings (Helper signature, The four variants, Topology-detection rule, Endpoint / wraparound rules, GUI registry implications, plus the header block heading).
    - File contains exactly 4 `^### Variant ` subsection headings.
    - File contains the literal string `Topology-detection rule` in a section heading.
    - File contains the verbatim signature line `fuel_assembly(channels::Vector{<:AbstractSystem}, plates::Vector{<:AbstractSystem}; bookend::Symbol=:auto, start::Union{Symbol,Nothing}=nothing, closed::Bool=false, name::Symbol)`.
    - File path is exactly `.planning/notes/fuel-assembly-api.md` (sibling to `correlation-geom-first-api.md`), NOT under `.planning/phases/60-.../`.
  </acceptance_criteria>
  <done>
    Phase 61 has a self-contained spec at `.planning/notes/fuel-assembly-api.md` covering signature, the four variants in ascii-art, the topology-detection rule with input signature and emitted call shape for each variant, the endpoint/wraparound rules, and GUI registry implications. The note can be read alongside `correlation-geom-first-api.md` and gives the Phase 61 codeGenerator implementer everything needed without re-reading `src/`.
  </done>
</task>

</tasks>

<threat_model>
No external attack surface — test code + planning markdown. No I/O, no auth, no untrusted input, no network surface. The handoff note is a developer-facing reference document.
</threat_model>

<verification>
1. Test gate: `bin/jl test/test_composition.jl` exits 0 with all nine new testsets passing.
2. Regression gate: `bin/jl test/runtests.jl` exits 0 (the new testsets do not break the existing suite).
3. Handoff-note existence: `test -f .planning/notes/fuel-assembly-api.md && wc -l .planning/notes/fuel-assembly-api.md` returns a positive line count and the file is readable.
4. Handoff-note structure: `grep -c '^## ' .planning/notes/fuel-assembly-api.md` returns at least 5 and `grep -c '^### Variant' .planning/notes/fuel-assembly-api.md` returns 4.
</verification>

<success_criteria>
- Four `@testset` parity blocks (one per variant) pass at `rtol=1e-10`.
- Four ArgumentError `@test_throws` blocks pass.
- One smoke testset confirms helper returns an uncompiled `ODESystem`.
- `bin/jl test/runtests.jl` passes (no regression).
- `.planning/notes/fuel-assembly-api.md` exists at the correct path with all D-07 sections present.
- The handoff note's signature line matches the implementation in `src/composition/helpers.jl` character-for-character.
</success_criteria>

<output>
After completion, create `.planning/phases/60-fuel-assembly-composition-helper/60-02-SUMMARY.md`
covering: testset names added, per-variant parity result (max abs delta from `isapprox`), the four
ArgumentError cases confirmed firing, the handoff note's commit SHA, and any divergence from the
plan (especially the IC-key comparison strategy: did the executor use `sol.u` direct comparison or
the symbolic-accessor read-back path).
</output>
