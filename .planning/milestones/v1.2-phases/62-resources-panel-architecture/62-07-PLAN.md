---
phase: 62
plan: 07
type: execute
wave: 2
depends_on: [62-02, 62-05]
files_modified:
  - gui/src/components/project/ModelOptionsPanel.tsx
  - gui/src/App.tsx
  - gui/src/components/project/__tests__/ModelOptionsPanel.test.tsx
autonomous: true
requirements_addressed:
  - D-04
  - CD-04
tags: [gui, project-tab, model-options, form]

must_haves:
  truths:
    - "Working branch is gui-redesign; no new branches created."
    - "Project tab body IS the Model Options form — no inner selection step (D-04)."
    - "Right Properties panel is unused while editing Project: it shows the no-selection state with the variant copy from UI-SPEC."
    - "Solver defaults exposed are exactly {abstol, reltol, dtmax} per CD-04 — no extra fields."
  artifacts:
    - path: "gui/src/components/project/ModelOptionsPanel.tsx"
      provides: "Name + Description + Default fluid (read-only) + Default g + Solver defaults (abstol, reltol, dtmax) form bound to modelOptions store slice"
  key_links:
    - from: "ModelOptionsPanel onBlur for each field"
      to: "useStore.setModelOptions"
      via: "shallow-merge patch"
      pattern: "setModelOptions\\("
  references:
    - "D-04: Project tab body IS the Model Options form (Name / Description / Default fluid / Default g / Solver defaults)."
    - "CD-04: Solver defaults expose abstol, reltol, dtmax."
---

<objective>
Build the Model Options form per D-04 + CD-04 + UI-SPEC §"Project tab body". Mount it inside `<TabsContent value="Project">` in `App.tsx` replacing the stub from 62-05. The form is the entire tab body — no inner selection step.

Fields: Name (Input), Description (Textarea), Default fluid (read-only display `water` with disabled tooltip), Default g (NumericField default 9.80665 m/s^2), Solver defaults (abstol default 1e-8, reltol default 1e-6, dtmax default null/blank). On-blur per field commits via `setModelOptions(patch)` (shallow merge per 62-02). The right Properties panel renders the no-selection state with the variant copy `Select a resource on the left to edit it.` — but per UI-SPEC §"Right Properties panel — no-selection body", that variant is only shown when Resources tab was the LAST active tab, so the no-selection behavior in the right panel itself lives in 62-09 (the router). This plan ONLY handles the Project tab body.

Output: one new file under `gui/src/components/project/`, an App.tsx update to mount it, one vitest covering field render + on-blur commit + read-only fluid + solver field set.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-resources-panel-architecture/62-CONTEXT.md
@.planning/phases/62-resources-panel-architecture/62-UI-SPEC.md
@.planning/phases/62-resources-panel-architecture/62-VALIDATION.md
@gui/src/components/sidebar/NumericField.tsx
@gui/src/components/sidebar/InstanceNameField.tsx
@gui/src/components/ui/input.tsx
@gui/src/components/ui/label.tsx
@gui/src/components/ui/tooltip.tsx
@gui/src/store/useStore.ts

<interfaces>
After 62-02:
- `useStore` exposes `modelOptions: { name, description, default_fluid, g_default, solver: { abstol, reltol, dtmax } }`
- Action `setModelOptions(patch)` shallow-merges + pushes snapshot + sets isDirty
- `_pushSnapshot()` is called BEFORE the mutation (already wired in 62-02)

Existing field primitives:
- `NumericField` at `gui/src/components/sidebar/NumericField.tsx` — accepts `{ label, value, onChange, onBlur, unit?, description? }` and handles validation; reuse for `g_default`, `abstol`, `reltol`, `dtmax`
- `InstanceNameField` at `gui/src/components/sidebar/InstanceNameField.tsx` — accepts `{ label, value, onChange, onBlur }`; reuse for `name`
- No existing Textarea primitive — UI-SPEC §"Project tab body — Fields" says: `Description (Textarea — needs new textarea.tsx shim, or reuse Input with as="textarea" if available — executor picks)`. Decision for this plan: create a minimal local `<textarea>` element styled to match `<Input>` (no new shadcn shim file needed; the description field is a single use, doesn't justify a reusable component yet). If a future plan needs a real shadcn `textarea.tsx`, that's a follow-up.

UI-SPEC §"Project tab body — Save behavior":
- Form-level — values written on field blur (`onBlur`)
- `_pushSnapshot()` called before each mutation (already wired inside `setModelOptions` per 62-02 — this plan does NOT need to call snapshot directly)
- `isDirty` flips true (already handled by `setModelOptions`)

UI-SPEC §"Project tab body — Solver defaults exposure":
- Three fields: `abstol` (default 1e-8), `reltol` (default 1e-6), `dtmax` (default `nothing` / blank — interpreted as no cap)
- Each is a NumericField with help-icon tooltips citing `solve_steady` / `solve_transient` semantics
- The `dtmax` field accepts an empty string as "no cap" — store value becomes `null`

UI-SPEC §"Project tab body — Default fluid":
- Read-only display `water` (the bare lowercase string; this is the value, NOT a UI label)
- Disabled tooltip same as Fluids `+` button: `Multi-fluid support is planned for a future release.`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ModelOptionsPanel.tsx with Name + Description + Default fluid + Default g + Solver defaults fields; mount in App.tsx</name>
  <files>gui/src/components/project/ModelOptionsPanel.tsx, gui/src/App.tsx</files>
  <read_first>
    - .planning/phases/62-resources-panel-architecture/62-UI-SPEC.md (§"Project tab body" — every field + style locked)
    - .planning/phases/62-resources-panel-architecture/62-CONTEXT.md (D-04, CD-04)
    - gui/src/components/sidebar/NumericField.tsx (signature + reuse pattern)
    - gui/src/components/sidebar/InstanceNameField.tsx (signature for the Name field)
    - gui/src/components/ui/tooltip.tsx (for the read-only Default fluid disabled tooltip)
    - gui/src/store/useStore.ts (modelOptions slice + setModelOptions action)
    - gui/src/App.tsx (current shell — Tabs structure from 62-05)
  </read_first>
  <action>
    Create `gui/src/components/project/ModelOptionsPanel.tsx`:

    Structure (top-level container `<div className="p-[16px] pt-[32px] overflow-y-auto h-full">`):
    1. **Header:** `<h2 className="text-[16px] font-semibold leading-[1.3] mb-[24px]">Project Options</h2>` (matches UI-SPEC §Typography "Section heading" + §Spacing `lg` between header and form)
    2. **Name:** `<InstanceNameField label="Name" value={modelOptions.name} onChange={(v) => setLocalName(v)} onBlur={() => setModelOptions({ name: localName })} />` — or directly bind to the store on every keystroke (pick one and document; on-blur is the UI-SPEC contract, but a local mirror for editing-state is cleaner). Use local React state for the in-progress edit, commit on blur.
    3. **Description:** rendered as a plain `<textarea>` element styled with the same Tailwind classes that `<Input>` uses for visual consistency (read `gui/src/components/ui/input.tsx` once to extract the className spine). Local state mirror; commit on blur.
    4. **Default fluid:** `<Label>Default fluid</Label><TooltipProvider><Tooltip><TooltipTrigger asChild><Input value="water" readOnly disabled className="opacity-75" /></TooltipTrigger><TooltipContent>Multi-fluid support is planned for a future release.</TooltipContent></Tooltip></TooltipProvider>` — exact literal `Multi-fluid support is planned for a future release.` per UI-SPEC.
    5. **Default g:** `<NumericField label="Default g" value={modelOptions.g_default} onChange={(v) => setLocalG(v)} onBlur={() => setModelOptions({ g_default: Number(localG) })} unit="m/s^2" description="Gravitational acceleration applied to channels with non-zero g." />`. Default placeholder when empty: `9.80665`.
    6. **Solver defaults section:** Add a subsection separator (`<Separator className="my-[24px]" />`) and a sub-header `<h3 className="text-xs font-semibold uppercase tracking-wide text-muted-foreground mb-[16px]">Solver Defaults</h3>` (matches Hydraulic/Thermal group header treatment per UI-SPEC §Typography). Three NumericField rows:
       - `<NumericField label="abstol" value={modelOptions.solver.abstol} ... description="Absolute tolerance for solve_steady / solve_transient. Default 1e-8." onBlur={() => setModelOptions({ solver: { ...modelOptions.solver, abstol: Number(localAbstol) } })} />`
       - same for `reltol` (default 1e-6, description references `solve_steady` / `solve_transient`)
       - `<NumericField label="dtmax" value={modelOptions.solver.dtmax ?? ""} ... description="Maximum solver timestep (transient only). Leave blank for no cap." />`. Special handling: on blur, if the local string is empty, set `dtmax: null`; otherwise `dtmax: Number(local)`.
    7. **No Save button:** per UI-SPEC §"Save behavior — Form-level — values are written to modelOptions store slice on field blur". The form is fire-and-forget per field.

    Then edit `gui/src/App.tsx`: import `ModelOptionsPanel` and replace the `<TabsContent value="Project"><div>Project panel — coming in plan 62-07</div></TabsContent>` stub from 62-05 with `<TabsContent value="Project"><ModelOptionsPanel /></TabsContent>`.

    DO NOT add solver fields beyond `abstol`, `reltol`, `dtmax` (CD-04 explicit minimum; adding more is scope creep per scope_reduction_prohibition rule). DO NOT make `default_fluid` editable; the locked behavior is read-only `water`. DO NOT add a Save button (UI-SPEC explicit).
  </action>
  <verify>
    <automated>cd gui &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "src/components/project/.*error TS|src/App\\.tsx.*error TS" | tee /tmp/62-07-task1-tsc.log; [ ! -s /tmp/62-07-task1-tsc.log ]</automated>
  </verify>
  <acceptance_criteria>
    - `gui/src/components/project/ModelOptionsPanel.tsx` exists
    - `grep -c "Project Options" gui/src/components/project/ModelOptionsPanel.tsx` >= 1
    - `grep -c "abstol\|reltol\|dtmax" gui/src/components/project/ModelOptionsPanel.tsx` >= 3
    - `grep -c "setModelOptions" gui/src/components/project/ModelOptionsPanel.tsx` >= 4 (one per editable field: name, description, g_default, solver.abstol; reltol/dtmax may share the same shape)
    - `grep -c "Multi-fluid support is planned" gui/src/components/project/ModelOptionsPanel.tsx` >= 1 (verbatim disabled tooltip)
    - `grep -c "readOnly\|disabled" gui/src/components/project/ModelOptionsPanel.tsx` >= 1 (the Default fluid field is non-editable)
    - `grep -c "ModelOptionsPanel" gui/src/App.tsx` >= 1 (mounted)
    - TypeScript compiles for both edited files
    - Implements D-04 (Project tab body IS the form), CD-04 (solver fields = abstol/reltol/dtmax only)
  </acceptance_criteria>
  <done>ModelOptionsPanel.tsx present; App.tsx mounts it; form fields wired to setModelOptions on blur.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest ModelOptionsPanel.test.tsx — render shape + on-blur commit + read-only fluid + solver field set</name>
  <files>gui/src/components/project/__tests__/ModelOptionsPanel.test.tsx</files>
  <read_first>
    - gui/src/components/project/ModelOptionsPanel.tsx (Task 1)
    - .planning/phases/62-resources-panel-architecture/62-VALIDATION.md (test list for D-04)
    - .planning/phases/62-resources-panel-architecture/62-UI-SPEC.md (locked copy + behavior)
    - gui/src/store/__tests__/modelOptions.test.ts (62-02 Task 3 — share `beforeEach` reset pattern)
  </read_first>
  <behavior>
    - **D-04 shape:** mount; assert `screen.getByText(/project options/i)` (the section heading) is present; assert labels `Name`, `Description`, `Default fluid`, `Default g` all present; assert sub-section `Solver Defaults` present
    - **D-04 default values:** assert the Default g field shows `9.80665`; assert abstol shows `1e-8` (or `0.00000001` depending on NumericField's display format — match what NumericField does); assert reltol shows `1e-6`; assert dtmax shows empty string or `(blank)` placeholder
    - **CD-04 solver field set:** exactly three solver inputs `abstol`, `reltol`, `dtmax` are present; no `alg`, no `progress_callback`, no other extras
    - **Default fluid read-only:** the input has `readOnly` or `disabled` attribute; typing into it does not change the value (skip the Tooltip assertion if happy-dom struggles with Radix Tooltip)
    - **On-blur commit (Name):** type `demo` into the Name input; blur the input; assert `useStore.getState().modelOptions.name === "demo"`
    - **On-blur commit (g_default):** type `9.81` into the g input; blur; assert `useStore.getState().modelOptions.g_default === 9.81`
    - **On-blur commit (solver.abstol):** type `1e-10` into abstol; blur; assert `useStore.getState().modelOptions.solver.abstol === 1e-10`; assert reltol and dtmax are UNCHANGED (shallow merge for the solver subobject correctness)
    - **dtmax blank means null:** clear the dtmax input; blur; assert `useStore.getState().modelOptions.solver.dtmax === null`
    - **Snapshot pushed on commit:** before any edit, capture `useStore.getState()._undoPast.length`; commit a Name change; assert `_undoPast.length` increased by 1
  </behavior>
  <action>
    Create `gui/src/components/project/__tests__/ModelOptionsPanel.test.tsx`. Use vitest + @testing-library/react. Reset the store in `beforeEach` (mirror the pattern from 62-02 Task 3 modelOptions.test.ts).

    For NumericField interactions: read `NumericField.tsx` once to understand its `onChange`/`onBlur` shape (it may use a string-typed local state and convert to Number only on blur). Use `fireEvent.change` to type and `fireEvent.blur` to commit; the field component handles the string-to-number coercion.

    Each `it()` title references D-04 or CD-04.

    DO NOT test the Description textarea's multi-line behavior in depth — assert it renders, accepts input, and commits on blur. Multi-line newline rendering is a textarea native concern.
  </action>
  <verify>
    <automated>cd gui &amp;&amp; npx vitest run src/components/project/__tests__/ModelOptionsPanel.test.tsx 2>&amp;1 | tee /tmp/62-07-task2.log; grep -qE "Tests +[0-9]+ passed" /tmp/62-07-task2.log &amp;&amp; ! grep -qE "[0-9]+ failed" /tmp/62-07-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - `gui/src/components/project/__tests__/ModelOptionsPanel.test.tsx` exists; vitest exits 0
    - `grep -c "abstol\|reltol\|dtmax" gui/src/components/project/__tests__/ModelOptionsPanel.test.tsx` >= 3
    - `grep -cE "D-04|CD-04" gui/src/components/project/__tests__/ModelOptionsPanel.test.tsx` >= 2
    - At least 6 `it()` blocks
    - Implements D-04 (Project tab body shape + on-blur commit), CD-04 (solver field set)
  </acceptance_criteria>
  <done>ModelOptionsPanel test green; on-blur commits + solver-field-set verified.</done>
</task>

</tasks>

<verification>
- `cd gui && npx vitest run src/components/project/` — green
- `cd gui && npx tsc --noEmit` — no new errors above baseline
- Visual: `npm run dev`; click Project tab; observe form with all fields; edit Name + blur; see undo enabled (Ctrl+Z available)
</verification>

<success_criteria>
- ModelOptionsPanel.tsx renders the locked field set
- All fields commit on blur via setModelOptions; snapshot pushed
- Default fluid is read-only with the locked disabled tooltip copy
- Solver field set is exactly {abstol, reltol, dtmax}
- Implements D-04, CD-04
</success_criteria>

<output>
Create `.planning/phases/62-resources-panel-architecture/62-07-SUMMARY.md` capturing:
- Decision on Description rendering (plain styled `<textarea>` vs new shadcn shim — and why)
- dtmax-blank-to-null conversion semantics
- Any UX gap surfaced (e.g., undo of a model-options change must be visible somewhere; the tab body itself doesn't surface it — that's expected)
</output>
