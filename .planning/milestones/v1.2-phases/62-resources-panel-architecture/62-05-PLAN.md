---
phase: 62
plan: 05
type: execute
wave: 2
depends_on: [62-02, 62-03]
files_modified:
  - gui/src/App.tsx
  - gui/src/components/ToolboxPanel.tsx
  - gui/src/components/__tests__/AppShell.test.tsx
  - gui/src/components/__tests__/ToolboxPanel.test.tsx
autonomous: true
requirements_addressed:
  - D-01
  - D-02
  - D-07
  - D-30
tags: [gui, app-shell, tabs, sources-header]

must_haves:
  truths:
    - "Working branch is gui-redesign; no new branches created."
    - "Left panel shows tab strip [Components] [Resources] [Project]; Components is the default on cold start."
    - "Ctrl+1/2/3 switch tabs without colliding with browser shortcuts (preventDefault on the three accelerators only)."
    - "Sources category header is visible in the Components tab between THERMAL and (future) REACTOR PHYSICS; body is empty in Phase 62."
  artifacts:
    - path: "gui/src/App.tsx"
      provides: "Tabs shell wrapping left panel; routes active tab to body component; Ctrl+1/2/3 keyboard binding"
    - path: "gui/src/components/ToolboxPanel.tsx"
      provides: "Components tab body unchanged + new SOURCES group header (D-30)"
  key_links:
    - from: "gui/src/App.tsx"
      to: "Tabs from gui/src/components/ui/tabs.tsx"
      via: "TabsList + TabsTrigger + TabsContent"
      pattern: 'from "@/components/ui/tabs"'
    - from: "gui/src/App.tsx Ctrl+1/2/3 handler"
      to: "useStore.setActiveLeftTab"
      via: "keydown listener with e.preventDefault()"
      pattern: "setActiveLeftTab"
  references:
    - "D-01: Left panel gains tab strip [Components][Resources][Project]; Components is the cold-start default."
    - "D-02: Components tab body reuses today's ToolboxPanel.tsx unchanged."
    - "D-07: Keyboard: Ctrl+1/2/3 switch left tabs; Ctrl+Tab NOT used (browser collision)."
    - "D-30: Sources category header in Components tab; visibly empty until Phase 63."
---

<objective>
Wrap the existing left-panel region in `gui/src/App.tsx` with the Radix Tabs primitive (already available via `@/components/ui/tabs`) producing the locked tab strip `[Components] [Resources] [Project]` per D-01 + UI-SPEC §"Tab strip". Bind `Ctrl+1 / Ctrl+2 / Ctrl+3` accelerators via a keydown listener that calls `setActiveLeftTab(...)` with `preventDefault()` (D-07). Make the Components tab body render the **unchanged** `ToolboxPanel` (D-02). Extend `ToolboxPanel.tsx` to add the `SOURCES` group header per D-30 — header only, no rows, no drag handlers (Phase 63 wires the entries).

The Resources and Project tab bodies are stub-routed in this plan to placeholder components that 62-06 and 62-07 will replace. Use lightweight inline stubs (e.g., `<div>Resources panel — coming in plan 62-06</div>`) so this plan can ship independently of those two.

Output: App.tsx + ToolboxPanel.tsx edits + two new vitest files covering the Ctrl+1/2/3 contract (INV-12) and the SOURCES header render (D-30).
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-resources-panel-architecture/62-CONTEXT.md
@.planning/phases/62-resources-panel-architecture/62-UI-SPEC.md
@.planning/phases/62-resources-panel-architecture/62-RESEARCH.md
@.planning/phases/62-resources-panel-architecture/62-VALIDATION.md
@gui/src/App.tsx
@gui/src/components/ToolboxPanel.tsx
@gui/src/components/ui/tabs.tsx
@gui/src/store/useStore.ts

<interfaces>
After 62-02:
- `useStore` exposes `activeLeftTab: "Components" | "Resources" | "Project"` and `setActiveLeftTab(tab)`
- The store's `setActiveLeftTab` action sets `isDirty: true` and does NOT push a snapshot (UI state)

`gui/src/components/ui/tabs.tsx` is the shadcn-wrapped Radix Tabs primitive; named exports are `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` (verified by reading the file).

`ToolboxPanel.tsx` currently fetches Hydraulic + Thermal via `getComponentsByCategory`. Phase 62 adds a third call to `getComponentsByCategory("Sources")` for the count check (always render the header per D-30, even if zero visible Sources after layer filtering).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap left panel in Tabs in App.tsx; route active tab body; bind Ctrl+1/2/3</name>
  <files>gui/src/App.tsx</files>
  <read_first>
    - gui/src/App.tsx (current shell layout — where ToolboxPanel is mounted today)
    - gui/src/components/ui/tabs.tsx (Tabs/TabsList/TabsTrigger/TabsContent named exports)
    - .planning/phases/62-resources-panel-architecture/62-UI-SPEC.md (§"Tab strip" — 36px height, px-[12px] triggers, 2px bottom border --primary on active, text-muted-foreground inactive, no icons, no animation)
    - .planning/phases/62-resources-panel-architecture/62-CONTEXT.md (D-01, D-02, D-07)
    - gui/src/store/useStore.ts (activeLeftTab state + setActiveLeftTab action after 62-02)
  </read_first>
  <behavior>
    - Cold-start `activeLeftTab` is `"Components"` (per 62-02 initial state)
    - Three triggers labeled exactly `Components`, `Resources`, `Project` (verbatim per UI-SPEC Tab strip labels)
    - Clicking a trigger calls `setActiveLeftTab` with the matching string
    - `Ctrl+1` keydown fires `setActiveLeftTab("Components")` AND `e.preventDefault()`
    - `Ctrl+2` keydown fires `setActiveLeftTab("Resources")` AND `e.preventDefault()`
    - `Ctrl+3` keydown fires `setActiveLeftTab("Project")` AND `e.preventDefault()`
    - The Ctrl+1/2/3 binding is global on the document (listener on `window` or `document`); other Ctrl+N combinations are NOT intercepted (so Ctrl+S still triggers save, Ctrl+Z still triggers undo, etc.)
    - Active tab body routes: `"Components"` → existing `ToolboxPanel` (unchanged behavior); `"Resources"` → stub `<div className="p-[16px] text-[14px] text-muted-foreground">Resources panel — coming in plan 62-06</div>`; `"Project"` → stub `<div className="p-[16px] text-[14px] text-muted-foreground">Project panel — coming in plan 62-07</div>`
    - Visual: tab strip is 36px high, sits above the body; triggers use `px-[12px]` horizontal padding, text-only, no icons; active trigger has `border-b-2 border-primary text-foreground`, inactive `border-b-2 border-transparent text-muted-foreground`; hover on inactive adds `border-b-muted` (UI-SPEC §Tab strip interaction contracts)
  </behavior>
  <action>
    Edit `gui/src/App.tsx` to wrap the left-panel region with `<Tabs value={activeLeftTab} onValueChange={setActiveLeftTab}>` from `@/components/ui/tabs`. The existing `<ToolboxPanel ... />` mount becomes the `<TabsContent value="Components">` child. Add stub TabsContent children for `"Resources"` and `"Project"` (one-line `<div>` each — see Behavior).

    For the keyboard binding, add a `useEffect(() => { ... }, [setActiveLeftTab])` that registers a `keydown` listener on `window`. Inside the handler, check `e.ctrlKey && !e.shiftKey && !e.altKey && !e.metaKey` and `e.key === "1"` (or `"2"` / `"3"`) — three explicit branches. Call `e.preventDefault()` followed by the appropriate `setActiveLeftTab` call. Return a cleanup `removeEventListener`. Cite UI-SPEC §"Tab strip" `preventDefault on accelerator` as the source.

    DO NOT intercept Ctrl+Tab (per D-07 + UI-SPEC §"Disabled state" — Ctrl+Tab is explicitly rejected for browser-collision reasons). DO NOT use the older `e.keyCode` API (deprecated). DO NOT add icons to the tab triggers (UI-SPEC §"Tab strip" — text-only).

    TabsList styling: `className="h-[36px] px-0 ..."` matching UI-SPEC §Spacing tab-strip-height. Trigger styling: `className="px-[12px] data-[state=active]:border-primary data-[state=inactive]:border-transparent ..."`. Keep tailwind utility composition consistent with the rest of `gui/src/components/ui/tabs.tsx` defaults — only override what UI-SPEC mandates.

    Per UI-SPEC §"Focus management on tab switch", focus moves to the tab trigger on Ctrl+1/2/3 (Radix Tabs default — no custom override needed). Per "Tab switch focus behavior", focus on the trigger after the accelerator is acceptable (we do NOT auto-focus into the tab body).
  </action>
  <verify>
    <automated>cd gui &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "src/App\\.tsx.*error TS" | tee /tmp/62-05-task1-tsc.log; [ ! -s /tmp/62-05-task1-tsc.log ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "@/components/ui/tabs"' gui/src/App.tsx` returns 1
    - `grep -c "TabsList\|TabsTrigger\|TabsContent" gui/src/App.tsx` returns >= 3
    - `grep -cE 'TabsTrigger.*value="Components"' gui/src/App.tsx` returns 1
    - `grep -cE 'TabsTrigger.*value="Resources"' gui/src/App.tsx` returns 1
    - `grep -cE 'TabsTrigger.*value="Project"' gui/src/App.tsx` returns 1
    - `grep -c "setActiveLeftTab" gui/src/App.tsx` returns >= 4 (onValueChange + three keyboard branches OR a switch)
    - `grep -c "ctrlKey" gui/src/App.tsx` returns >= 1
    - `grep -c "preventDefault" gui/src/App.tsx` returns >= 1
    - TypeScript compiles for App.tsx (no new errors)
    - Implements D-01 (tab strip), D-02 (Components body unchanged via existing ToolboxPanel mount), D-07 (Ctrl+1/2/3 keyboard binding)
  </acceptance_criteria>
  <done>App.tsx wraps left panel with Tabs; keyboard accelerators wired; routes by active tab.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SOURCES category header to ToolboxPanel.tsx (D-30)</name>
  <files>gui/src/components/ToolboxPanel.tsx</files>
  <read_first>
    - gui/src/components/ToolboxPanel.tsx (current Hydraulic + Thermal pattern lines 11-21, 33-65)
    - .planning/phases/62-resources-panel-architecture/62-CONTEXT.md (D-30 — header only, no rows, no drag handlers)
    - .planning/phases/62-resources-panel-architecture/62-UI-SPEC.md (§Copywriting "Sources toolbox category header"; §Interaction "Sources toolbox category header")
    - gui/src/registry/index.ts (getComponentsByCategory shape — does it return [] cleanly for "Sources"?)
  </read_first>
  <behavior>
    - The SOURCES header is ALWAYS rendered when the Components tab is visible, regardless of which Components are returned by `getComponentsByCategory("Sources")`
    - Phase 61 registry has `WallTemperature` and `HeatFluxSource` tagged as Sources, but Phase 62 does NOT iterate them into draggable rows — only the header renders
    - Header text: `SOURCES` (uppercase, matches the existing `HYDRAULIC` / `THERMAL` treatment)
    - Header styling: `text-xs font-semibold uppercase tracking-wide text-muted-foreground mb-2 mt-4` (verbatim from existing Hydraulic header in ToolboxPanel.tsx)
    - No tooltip on the SOURCES header (UI-SPEC: "no inert affordances; do not invite the user to click")
    - The header renders BETWEEN the THERMAL group and where future REACTOR PHYSICS would land (i.e., after the Thermal block in the current layout)
    - No empty-body placeholder text (UI-SPEC: do NOT render "(none yet — click +)" under SOURCES — that placeholder is for the Resources tree groups, not the Components toolbox)
  </behavior>
  <action>
    Edit `gui/src/components/ToolboxPanel.tsx`. Add the SOURCES header block after the existing Thermal `{visibleThermal.length > 0 && ...}` block. The pattern mirrors the existing groups but with no conditional check (header always renders):

    ```
    <div className="text-xs font-semibold uppercase tracking-wide text-muted-foreground mb-2 mt-4">
      Sources
    </div>
    ```

    Wait — UI-SPEC §Copywriting locks the literal as `SOURCES` (all-caps). Re-read the existing `Hydraulic` rendering: it's lowercase `Hydraulic` in JSX (`<div>Hydraulic</div>`) with Tailwind `uppercase` class doing the visual upcasing. To match that convention, render `<div>Sources</div>` with the same `uppercase tracking-wide` classes, OR render the literal `SOURCES`. Either produces the same visual; pick the form that matches the existing two headers in the file (read once to confirm). The acceptance criterion below greps for the rendered uppercase form, which works either way because of the `uppercase` class.

    Do NOT call `getComponentsByCategory("Sources")` at all in this task — UI-SPEC §Interaction "`getComponentsByCategory("Sources")` ... BUT Phase 62 does not iterate these into the visible body." If you fetch but ignore, that's also fine; the simpler choice is to skip the fetch entirely.

    Update the existing `<h2 className="text-lg font-semibold mb-4">Components</h2>` heading: leave it unchanged. The tab trigger label `Components` (from 62-05 Task 1) is now the primary heading; the inline `<h2>` becomes redundant. Decision: KEEP the `<h2>` for now — UI-SPEC §"Tab strip labels" notes the tab label "Mirrors existing `ToolboxPanel.tsx:33` `<h2>Components</h2>` heading". Mirroring means coexisting, not removing. Phase 72 (design contract) may consolidate later.

    DO NOT wire drag handlers for Sources. DO NOT render `WallTemperature` or `HeatFluxSource` as draggable items.
  </action>
  <verify>
    <automated>cd gui &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "src/components/ToolboxPanel\\.tsx.*error TS" | tee /tmp/62-05-task2-tsc.log; [ ! -s /tmp/62-05-task2-tsc.log ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "Sources" gui/src/components/ToolboxPanel.tsx` returns >= 1 (the new header literal)
    - `grep -c "text-xs font-semibold uppercase tracking-wide text-muted-foreground" gui/src/components/ToolboxPanel.tsx` returns >= 3 (HYDRAULIC + THERMAL + new SOURCES use the same treatment)
    - `grep -c "WallTemperature\|HeatFluxSource" gui/src/components/ToolboxPanel.tsx` returns 0 (no rows wired in Phase 62)
    - TypeScript compiles for this file
    - Implements D-30 (Sources category header, empty body)
  </acceptance_criteria>
  <done>SOURCES header renders in Components tab between THERMAL and the bottom; no rows; no tooltip; no drag handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest AppShell.test.tsx (Ctrl+1/2/3 contract — INV-12) and ToolboxPanel.test.tsx (Sources header — D-30)</name>
  <files>gui/src/components/__tests__/AppShell.test.tsx, gui/src/components/__tests__/ToolboxPanel.test.tsx</files>
  <read_first>
    - gui/src/App.tsx (Task 1 — Tab strip + keyboard binding)
    - gui/src/components/ToolboxPanel.tsx (Task 2 — Sources header)
    - .planning/phases/62-resources-panel-architecture/62-VALIDATION.md (INV-12 + D-30 test requirements)
    - .planning/phases/62-resources-panel-architecture/62-UI-SPEC.md (Tab strip preventDefault contract; SOURCES header copy)
  </read_first>
  <behavior>
    **AppShell.test.tsx:**
    - Default render: `screen.getByRole("tab", { name: /Components/i })` has `aria-selected="true"`; the other two have `aria-selected="false"`
    - Click Resources tab: aria-selected flips; `useStore.getState().activeLeftTab === "Resources"`
    - `keyDown(window, { key: "2", ctrlKey: true })`: activeLeftTab becomes "Resources"; the event.preventDefault was called (assert via spy or via observing that no browser default fired; use `Event.defaultPrevented` after a dispatched event)
    - `keyDown(window, { key: "1", ctrlKey: true })`: activeLeftTab becomes "Components"
    - `keyDown(window, { key: "3", ctrlKey: true })`: activeLeftTab becomes "Project"
    - `keyDown(window, { key: "Tab", ctrlKey: true })`: activeLeftTab does NOT change (Ctrl+Tab is intentionally NOT intercepted; UI-SPEC explicit)
    - `keyDown(window, { key: "1", ctrlKey: false })`: activeLeftTab does NOT change (no modifier — just a "1" keypress shouldn't switch tabs)

    **ToolboxPanel.test.tsx:**
    - Renders; `screen.getByText(/sources/i)` is present (case-insensitive because the visual is uppercase via Tailwind class but the literal in JSX may be lowercase)
    - The SOURCES header is rendered AFTER the THERMAL header in DOM order (assert via comparing `compareDocumentPosition` or by querying both and checking order in the `parentNode.children` array)
    - No WallTemperature or HeatFluxSource elements rendered (the body under SOURCES is empty per D-30): `screen.queryByText(/WallTemperature/i)` is null; `screen.queryByText(/HeatFluxSource/i)` is null
    - The SOURCES header does NOT have a tooltip (UI-SPEC "no inert affordances"): check that the element does NOT have an `aria-describedby` and is not wrapped by a TooltipTrigger
  </behavior>
  <action>
    Create both files using vitest + @testing-library/react + happy-dom (existing project test stack per RESEARCH §Validation Architecture). Use `fireEvent.keyDown(window, ...)` for the keyboard tests.

    Reset the store between tests (`beforeEach`) so `activeLeftTab` starts at "Components". Use the same store-reset pattern as `gui/src/store/__tests__/resources.slice.test.ts` (from 62-02 Task 2).

    Each `it()` test title references the invariant or decision ID (INV-12 / D-30).

    DO NOT test the visual styling (border-color of active tab, hover bg) — those are CSS contract concerns; the locked Tailwind classes are the contract per UI-SPEC §Color and are not amenable to deterministic assertion in happy-dom.
  </action>
  <verify>
    <automated>cd gui &amp;&amp; npx vitest run src/components/__tests__/AppShell.test.tsx src/components/__tests__/ToolboxPanel.test.tsx 2>&amp;1 | tee /tmp/62-05-task3.log; grep -qE "Tests +[0-9]+ passed" /tmp/62-05-task3.log &amp;&amp; ! grep -qE "[0-9]+ failed" /tmp/62-05-task3.log</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; `npx vitest run` exits 0
    - `grep -c "ctrlKey" gui/src/components/__tests__/AppShell.test.tsx` returns >= 3 (one per accelerator)
    - `grep -c "INV-12\|D-07" gui/src/components/__tests__/AppShell.test.tsx` returns >= 1
    - `grep -ci "sources" gui/src/components/__tests__/ToolboxPanel.test.tsx` returns >= 1
    - `grep -c "D-30" gui/src/components/__tests__/ToolboxPanel.test.tsx` returns >= 1
    - Implements INV-12, D-30
  </acceptance_criteria>
  <done>AppShell + ToolboxPanel tests green; Ctrl+1/2/3 contract verified; SOURCES header presence verified.</done>
</task>

</tasks>

<verification>
- `cd gui && npx vitest run src/components/__tests__/AppShell.test.tsx src/components/__tests__/ToolboxPanel.test.tsx` — green
- `cd gui && npx tsc --noEmit` — no new errors above the 7-error baseline
- Visual: `npm run dev` shows the tab strip; clicking Resources / Project shows the stub bodies; Ctrl+1/2/3 cycles tabs
- `grep -ci "sources" gui/src/components/ToolboxPanel.tsx` >= 1
</verification>

<success_criteria>
- Tab strip rendered with three triggers; Ctrl+1/2/3 keyboard accelerators wired with preventDefault
- ToolboxPanel still shows Hydraulic + Thermal as before, plus the new SOURCES header (empty body)
- Stub bodies for Resources/Project tabs are clearly labeled and ready for 62-06 / 62-07 replacement
- Implements D-01, D-02, D-07, D-30 + INV-12
</success_criteria>

<output>
After completion, create `.planning/phases/62-resources-panel-architecture/62-05-SUMMARY.md` capturing:
- Final layout of the tab strip (any deviation from UI-SPEC 36px / px-[12px])
- Notes on Ctrl-key listener placement (window vs document; cleanup correctness)
- Confirmation that Ctrl+Tab is intentionally NOT intercepted
</output>
