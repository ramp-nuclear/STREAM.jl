---
phase: 62
plan: 15
type: execute
wave: 5
depends_on: [62-12, 62-13, 62-14]
files_modified:
  - gui/src/components/sidebar/ResourceReferencePicker.tsx
  - gui/src/components/sidebar/GeometryResourceEditor.tsx
  - gui/src/components/sidebar/PowerShapeResourceEditor.tsx
  - gui/src/components/sidebar/SidebarPanel.tsx
  - gui/src/components/resources/ResourcesTreePanel.tsx
  - gui/src/components/resources/ResourceRow.tsx
  - gui/src/components/project/ModelOptionsPanel.tsx
  - gui/src/store/useStore.ts
  - gui/src/components/sidebar/__tests__/ResourceReferencePicker.test.tsx
  - gui/src/components/sidebar/__tests__/GeometryResourceEditor.test.tsx
  - gui/src/components/sidebar/__tests__/PowerShapeResourceEditor.test.tsx
  - gui/src/components/resources/__tests__/ResourcesTreePanel.test.tsx
  - gui/src/components/project/__tests__/ModelOptionsPanel.test.tsx
autonomous: true
gap_closure: true
gap_source: 62-VERIFICATION.md
gap_step: 7
root_plan: cross-cutting
requirements: []
tags: [gui, copy, voice, professional, cross-cutting, gap-closure]

must_haves:
  truths:
    - "Every user-facing string in the Phase 62 substitution table below is rewritten to engineering-tool voice — terse, declarative, no AI-isms."
    - "Every string in the substitution table is pinned by at least one vitest assertion (existing or new) so future regressions get caught."
    - "Em-dashes (U+2014) only appear where a period would not work (parenthetical asides, range indicators); no em-dash decoration purely for tone."
    - "Tooltips on disabled controls state the rule, not an apology — `Select a resource to edit it.` style."
    - "Error dialog copy follows the pattern `<Fact>. <Action.>` — fact first, action second, both terminated."
    - "Sentinel label changes from `(leave unset — fill in code)` to `(leave unset — set in code)` per the substitution table."
    - "All 14 Phase 62 vitest files still green after the rewrites."
  artifacts:
    - path: ".planning/phases/62-resources-panel-architecture/62-15-COPY-AUDIT.md"
      provides: "The substitution table — current strings, proposed strings, file:line, rationale tag — committed alongside the code changes for auditability"
    - path: "gui/src/components/sidebar/ResourceReferencePicker.tsx"
      provides: "Updated empty-state copy + disabled-Edit tooltip per substitution table"
    - path: "gui/src/components/sidebar/GeometryResourceEditor.tsx"
      provides: "Updated identifier-validation copy per substitution table"
    - path: "gui/src/components/sidebar/PowerShapeResourceEditor.tsx"
      provides: "Updated identifier-validation + dim-error copy per substitution table"
    - path: "gui/src/components/sidebar/SidebarPanel.tsx"
      provides: "Updated no-selection panel copy per substitution table"
    - path: "gui/src/components/resources/ResourcesTreePanel.tsx"
      provides: "Updated search placeholder + Fluids disabled tooltip per substitution table"
    - path: "gui/src/store/useStore.ts"
      provides: "Updated save/open error dialog copy + missing-file warning copy per substitution table; sentinel power-shape name update"
  key_links:
    - from: "Every substituted string"
      to: "At least one vitest assertion (or snapshot fragment) pinning the new exact text"
      via: "explicit toBe / toMatch / toContain over the new string"
      pattern: "(toBe|toMatch|toContain)\\(.*\".*\""
---

<objective>
Rewrite Phase 62's user-facing strings to engineering-tool voice. The
audit below identifies 17 strings introduced in Plans 62-05 through
62-10 (plus the sentinel constant in 62-02 and the save/open error
dialogs from 62-04) and proposes verbatim replacements. The plan
executes the substitutions, then updates every test that pins the OLD
string to pin the NEW string instead. Acceptance criterion: every NEW
string is asserted by at least one test — no "reads more professional"
hand-waving.

**Supersession note:** Substitution rows #1 and #4 change the sentinel
string from `(leave unset — fill in code)` (locked in 62-CONTEXT.md
D-26) to `(leave unset — set in code)`. This supersedes D-26 under the
implicit authorization of user feedback in 62-VERIFICATION.md Critical
Gap #4 ("Need to rethink wording in this phase ... professional voice").
Documentation-only flag; the new value is the active locked value going
forward.

Purpose: Close VERIFICATION.md Critical Gap #4 ("Professional copy pass
— user-facing strings read as 'AI-ish'").

This plan runs AFTER 62-12, 62-13, 62-14 because:
- 62-12 may add new assertions over the picker layout that we DO NOT
  want to interleave-edit.
- 62-13 changes usage detection in ResourceRow and adds AlertDialog
  assertions; we will also touch ResourceRow.tsx if the AlertDialog
  description string is in the substitution table (it is — the kind
  label "power shape" should become "power-shape" for consistency).
- 62-14 modifies the saveProjectAs error dialog — we want the copy
  rewrite landing on top of the working fix, not the other way around.

Output: 7 source-file edits + 5 test-file edits + 1 new audit doc
(`62-15-COPY-AUDIT.md`).
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-resources-panel-architecture/62-VERIFICATION.md
@.planning/phases/62-resources-panel-architecture/62-UI-SPEC.md
@.planning/phases/62-resources-panel-architecture/62-06-SUMMARY.md
@.planning/phases/62-resources-panel-architecture/62-08-SUMMARY.md
@gui/src/components/sidebar/ResourceReferencePicker.tsx
@gui/src/components/sidebar/GeometryResourceEditor.tsx
@gui/src/components/sidebar/PowerShapeResourceEditor.tsx
@gui/src/components/sidebar/SidebarPanel.tsx
@gui/src/components/resources/ResourcesTreePanel.tsx
@gui/src/components/resources/ResourceRow.tsx
@gui/src/components/project/ModelOptionsPanel.tsx
@gui/src/store/useStore.ts

<voice_rules>
1. **Terse, declarative.** Strip filler words ("Please", "just", "simply",
   "currently", "for now").
2. **Engineering-tool voice.** State the rule or fact; do not apologize
   or reassure.
3. **No em-dash decoration.** Em-dashes are fine for parenthetical asides
   (`(leave unset — set in code)`, `Power Shapes — z_cosine kind`) but
   not where a period works.
4. **Tooltips ≤ 60 chars.** Single line. State the rule.
5. **Error dialogs: `<Fact>. <Action.>`** Two short clauses. Both
   terminated.
6. **No future-tense excuses.** Replace "is planned for a future release"
   with "Not yet supported." Replace "coming in plan 62-XX" with the
   actual behavior (or the placeholder copy if the surface is still
   intentionally stub).
7. **Lowercase resource kinds in body text.** Use `geometry`, `power shape`,
   `fluid` — not `Geometry`, `Power Shape`, `Fluid` — when they appear in
   sentence body (`Delete geometry mtr_ch?` not `Delete Geometry mtr_ch?`).
   Headers / section labels stay Title Case.
8. **Preserve the sentinel U+2014 in `(leave unset — set in code)`.** The
   parenthetical aside is the legitimate em-dash use case.
</voice_rules>

<substitution_table>
**NOTE on rows #1 and #4:** The sentinel-string change from
`(leave unset — fill in code)` → `(leave unset — set in code)`
supersedes locked decision D-26 in 62-CONTEXT.md, per user feedback in
62-VERIFICATION.md Critical Gap #4 ("Need to rethink wording in this
phase ... professional voice"). This documents the supersession for
future audit; the value itself is the new locked value going forward.

| # | File | Line(s) | Current string | New string | Rationale |
|---|------|---------|----------------|------------|-----------|
| 1 | `gui/src/store/useStore.ts` | 58 | `(leave unset — fill in code)` | `(leave unset — set in code)` | Rule 1: "fill in" is filler; "set in" is precise. Supersedes D-26 per user feedback in VERIFICATION Gap #4. |
| 2 | `gui/src/components/sidebar/ResourceReferencePicker.tsx` | 67 | `No geometries yet — click + New… or open the Resources tab.` | `No geometries. Use + New or the Resources tab.` | Rule 1, 4. Two short clauses; no "yet"; no nested punctuation. |
| 3 | `gui/src/components/sidebar/ResourceReferencePicker.tsx` | 68 | `No power shapes yet — click + New… or open the Resources tab.` | `No power shapes. Use + New or the Resources tab.` | Rule 1, 4. |
| 4 | `gui/src/components/sidebar/ResourceReferencePicker.tsx` | 146 | `(leave unset — fill in code)` | `(leave unset — set in code)` | Rule 1. Matches sentinel rename. Supersedes D-26 per user feedback in VERIFICATION Gap #4. |
| 5 | `gui/src/components/sidebar/ResourceReferencePicker.tsx` | 176 | `Select a resource to edit it.` | `Pick a resource first.` | Rule 1, 4. 22 chars vs 30; "pick" implies the select control directly. |
| 6 | `gui/src/components/sidebar/GeometryResourceEditor.tsx` | 131 | `Use ASCII letters, digits, and underscores; must not start with a digit.` | `Letters, digits, underscores. Cannot start with a digit.` | Rule 1, 5. "ASCII" is jargon — the keyboard already produces ASCII letters. Two clauses. |
| 7 | `gui/src/components/sidebar/PowerShapeResourceEditor.tsx` | 150 | `Use ASCII letters, digits, and underscores; must not start with a digit.` | `Letters, digits, underscores. Cannot start with a digit.` | Rule 1, 5. Same as #6. |
| 8 | `gui/src/components/sidebar/PowerShapeResourceEditor.tsx` | 224 | `Amplitude must be a finite number.` | `Amplitude must be finite.` | Rule 1. "a finite number" is six syllables for what "finite" says alone. |
| 9 | `gui/src/components/sidebar/PowerShapeResourceEditor.tsx` | 230 | `Please pick a CSV file via Browse.` | `Pick a CSV file via Browse.` | Rule 2. Drop "Please". |
| 10 | `gui/src/components/sidebar/SidebarPanel.tsx` | 271 | `Select a resource on the left to edit it.` | `Select a resource to edit it.` | Rule 1. "on the left" — the left panel is where Resources live; the user already sees them; redundant. |
| 11 | `gui/src/components/sidebar/SidebarPanel.tsx` | 272 | `Select a component on the canvas to view its properties.` | `Select a component to view its properties.` | Rule 1. "on the canvas" — the canvas is the only place components live; redundant. |
| 12 | `gui/src/components/resources/ResourcesTreePanel.tsx` | 72 | `Search resources…` | `Search resources…` | NO CHANGE — already terse and uses the proper ellipsis glyph. Listed here for audit completeness. |
| 13 | `gui/src/components/resources/ResourcesTreePanel.tsx` | 146 | `Multi-fluid support is planned for a future release.` | `Multiple fluids not yet supported.` | Rule 6, 4. 32 chars vs 50; states the rule, not the roadmap. |
| 14 | `gui/src/components/resources/ResourceRow.tsx` | 338-340 (description) | `Delete ${kindLabel(kind)} ${resource.name}? It is used by ${usages.length} component(s).` | `Delete ${kindLabel(kind)} ${resource.name}? Used by ${usages.length} component(s).` | Rule 1. "It is" is filler. |
| 15 | `gui/src/components/resources/ResourceRow.tsx` | 359 (kindLabel) | returns `"power shape"` | returns `"power shape"` | NO CHANGE in word; verify it stays lowercase per Rule 7. Listed for audit. |
| 16 | `gui/src/store/useStore.ts` | 1128, 1186 | `Couldn't save project. Check that the file isn't read-only and there is enough disk space, then try again.` | `Save failed. Check the file is writable and there is disk space.` | Rule 5, 1. Two clauses; both terminated; drop "Couldn't" colloquial. |
| 17 | `gui/src/store/useStore.ts` | 1214, 1362 | `Couldn't open this project. The file may be missing, corrupted, or not a valid .scp file.` | `Open failed. The file may be missing, corrupted, or not a valid .scp file.` | Rule 5, 1. Same pattern. |
| 18 | `gui/src/store/useStore.ts` | ~1340 (missing power-shape message) | `N power shape file(s) could not be found. Open the Resources tab to relocate them.` | `N power-shape file(s) not found. Open the Resources tab to relocate.` | Rule 1, 7. "could not be" is filler; "to relocate them" — "them" is implied. |
| 19 | `gui/src/store/useStore.ts` | 1351 | `Missing Power Shape file` (dialog title) | `Missing power-shape file` | Rule 7 — sentence-case in body / dialog title, hyphenated. |

NOTES:
- **U+2014 (em-dash) policy:** Substitution #1 / #4 keep the em-dash because it
  separates the literal value (`leave unset`) from the explanation
  (`set in code`). Substitution #2 / #3 REMOVE the em-dash because the
  rewrite splits into two sentences where a period works.
- **Punctuation glyphs:** The substitution table uses straight ASCII
  punctuation throughout EXCEPT for the U+2014 em-dash in `(leave unset
  — set in code)`. The horizontal-ellipsis glyph U+2026 (`…`) IS kept
  in the `+ New…`, `Edit…`, `Browse…` button labels and in the
  search-resources placeholder — those are existing canonical
  Phase-62 button conventions and changing them is out of scope.
- **kindLabel return values** (ResourceRow.tsx:357-361): the function
  already returns `"power shape"` lowercase. Verify and keep.
- **Editor headers** (`New Geometry` / `Edit Geometry` /
  `New Power Shape` / `Edit Power Shape`): unchanged. These are section
  titles, Rule 7 keeps them Title Case. Listed implicitly in audit
  completeness check.
- **ModelOptionsPanel descriptions** (lines 299, 324, 341, 358): these
  are info-tooltip help-text strings, not error/disabled tooltips. They
  read fine — declarative, terse, end with periods. Audit completeness
  check verifies no change needed.
</substitution_table>

<test_pinning_strategy>
Each NEW string in the substitution table must be pinned by an
assertion. Existing tests that asserted the OLD string MUST be updated
to assert the NEW string — search-and-replace the literal across the
test files in `<files_modified>`.

For strings that have NO existing test (e.g. error dialog copy in
useStore.ts), add a minimal new test asserting the literal appears in
the code path. Use a vitest spy on `message` and `save` mocks to
capture the dialog arguments.

The test-pinning is what makes this plan robust to future
regression — without it, an LLM-driven re-flow tomorrow could silently
revert the rewrites.
</test_pinning_strategy>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author 62-15-COPY-AUDIT.md and apply substitutions to source files</name>
  <files>.planning/phases/62-resources-panel-architecture/62-15-COPY-AUDIT.md, gui/src/components/sidebar/ResourceReferencePicker.tsx, gui/src/components/sidebar/GeometryResourceEditor.tsx, gui/src/components/sidebar/PowerShapeResourceEditor.tsx, gui/src/components/sidebar/SidebarPanel.tsx, gui/src/components/resources/ResourcesTreePanel.tsx, gui/src/components/resources/ResourceRow.tsx, gui/src/store/useStore.ts</files>
  <read_first>
    - All seven source files listed in <files> above (full read)
    - .planning/phases/62-resources-panel-architecture/62-VERIFICATION.md
    - .planning/phases/62-resources-panel-architecture/62-UI-SPEC.md (skim for any copy explicitly locked verbatim in the spec — preserve those even if they appear "casual")
  </read_first>
  <behavior>
    This is a string-substitution task; the behavior is "every string in
    the substitution table is replaced exactly as specified, no
    over-reach into adjacent strings."
    No new behavior. Verify by grep:
    - Every NEW string from the table appears in the codebase exactly once (or as many times as listed in the Lines column).
    - Every OLD string from the table appears ZERO times in the source files (test files are updated in Task 2).
  </behavior>
  <action>
    **Step A: Create `62-15-COPY-AUDIT.md`** in the phase directory.
    Content: title, one-paragraph rationale, the full substitution table
    from this plan reproduced verbatim, and a "Verification commands"
    section listing the grep gates from `<done>` below. The file is
    the auditable record for the user / reviewer; it lives alongside
    the phase artifacts.

    **Step B: Apply each substitution.** For each row in the table:
    1. Open the file.
    2. Use the Edit tool (NOT sed) to replace the exact OLD string with
       the exact NEW string. Match character-for-character including
       em-dash / ellipsis / quotes.
    3. If the table row is "NO CHANGE", verify the current string
       matches expectations and move on.
    4. Where the file has the same OLD string in multiple lines (e.g.
       useStore.ts lines 1128 AND 1186 both carry the
       "Couldn't save project" copy), perform the substitution at
       BOTH line locations. Edit tool with `replace_all: true` is
       allowed only when the OLD string is unambiguous (no risk of
       false-positive matches inside an unrelated context).
    5. For row #1 (sentinel name in useStore.ts:58) AND row #4
       (in-line copy in ResourceReferencePicker.tsx:146) AND any other
       place the literal `(leave unset — fill in code)` appears,
       update ALL occurrences. Use the verification grep below to
       catch leftovers.

    **Step C: Audit completeness check.** After applying:
    - `grep -rn "fill in code\|to be defined\|Please pick\|read-only\|is planned for a future release\|It is used by\|on the canvas\|on the left to edit" gui/src/` returns 0 matches in source files (test files OK — Task 2 handles those).
    - `grep -rn "set in code\|Pick a resource first\|Not yet supported\|Used by\|Save failed\|Open failed" gui/src/` returns at least 1 match in each NEW-string's owning file.

    Do NOT touch the UI-SPEC.md verbatim-locked phrases that are
    OUTSIDE the substitution table. Do NOT add new copy. Do NOT
    rephrase strings not in the table.
  </action>
  <verify>
    <automated>cd gui && grep -rEn "(fill in code|to be defined|Please pick|on the canvas|on the left to edit|is planned for a future release|It is used by) " src/ | grep -v "__tests__\|.test." | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/62-resources-panel-architecture/62-15-COPY-AUDIT.md` exists (`test -f .planning/phases/62-resources-panel-architecture/62-15-COPY-AUDIT.md && echo OK` prints `OK`).
    - `cd gui && grep -rE "fill in code" src/ | grep -v "__tests__\\|\\.test\\." | wc -l` returns `0`.
    - `cd gui && grep -rE "Please pick" src/ | grep -v "__tests__\\|\\.test\\." | wc -l` returns `0`.
    - `cd gui && grep -rE "on the canvas|on the left to edit" src/ | grep -v "__tests__\\|\\.test\\." | wc -l` returns `0`.
    - `cd gui && grep -rE "is planned for a future release" src/ | grep -v "__tests__\\|\\.test\\." | wc -l` returns `0`.
    - `cd gui && grep -rE "It is used by" src/ | grep -v "__tests__\\|\\.test\\." | wc -l` returns `0`.
    - `cd gui && grep -rE "Couldn't save project|Couldn't open this project" src/ | grep -v "__tests__\\|\\.test\\." | wc -l` returns `0`.
    - `cd gui && grep -c "set in code" src/store/useStore.ts` returns ≥ 1.
    - `cd gui && grep -c "set in code" src/components/sidebar/ResourceReferencePicker.tsx` returns ≥ 1.
    - `cd gui && grep -c "Pick a resource first" src/components/sidebar/ResourceReferencePicker.tsx` returns ≥ 1.
    - `cd gui && grep -c "Save failed" src/store/useStore.ts` returns ≥ 1.
    - `cd gui && grep -c "Open failed" src/store/useStore.ts` returns ≥ 1.
    - `cd gui && grep -c "Not yet supported" src/components/resources/ResourcesTreePanel.tsx` returns ≥ 1.
    - `cd gui && grep -cE "Used by \\$\\{usages\\.length\\}" src/components/resources/ResourceRow.tsx` returns ≥ 1.
    - `cd gui && grep -c "Amplitude must be finite" src/components/sidebar/PowerShapeResourceEditor.tsx` returns ≥ 1.
    - `cd gui && npx tsc --noEmit 2>&1 | grep -cE "^error TS"` returns the same count (or fewer) than the pre-change baseline of 6.
  </acceptance_criteria>
  <done>
    - `62-15-COPY-AUDIT.md` exists in the phase directory.
    - For every row in the substitution table marked "NO CHANGE": current string verified present.
    - For every row marked with a rewrite: OLD string returns 0 hits via `grep -rn "<OLD>" gui/src/components gui/src/store` (excluding `__tests__`); NEW string returns at least 1 hit in the owning file.
    - `cd gui && npx tsc --noEmit` produces no NEW errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Phase 62 vitest assertions to pin the new strings</name>
  <files>gui/src/components/sidebar/__tests__/ResourceReferencePicker.test.tsx, gui/src/components/sidebar/__tests__/GeometryResourceEditor.test.tsx, gui/src/components/sidebar/__tests__/PowerShapeResourceEditor.test.tsx, gui/src/components/resources/__tests__/ResourcesTreePanel.test.tsx, gui/src/components/project/__tests__/ModelOptionsPanel.test.tsx</files>
  <read_first>
    - All five test files listed in <files> above (full read)
    - The 62-15-COPY-AUDIT.md created in Task 1 (the canonical substitution list)
  </read_first>
  <behavior>
    Every existing assertion against an OLD string from the substitution
    table is rewritten to assert the NEW string. New assertions are
    added where no existing test pinned a substituted string (so every
    NEW string in the table is covered by at least one test).

    Acceptance: every test file listed in <files> still passes after
    edits. Test counts may increase (new pinning assertions) but must
    never decrease.
  </behavior>
  <action>
    For each test file:
    1. Read the file in full.
    2. Search for each OLD string from the substitution table that
       could plausibly appear in that file. Replace with the NEW
       string. Use the Edit tool.
    3. For any NEW string that does NOT have an existing test
       assertion in any of the 5 test files (cross-check after all
       OLD→NEW rewrites), add a fresh assertion. Place new
       assertions at the END of the relevant describe block in the
       file that owns the production code:
       - "Pick a resource first." → ResourceReferencePicker.test.tsx (Edit-disabled tooltip assertion)
       - "Letters, digits, underscores. Cannot start with a digit." → GeometryResourceEditor.test.tsx AND PowerShapeResourceEditor.test.tsx (one per file)
       - "Amplitude must be finite." → PowerShapeResourceEditor.test.tsx
       - "Pick a CSV file via Browse." → PowerShapeResourceEditor.test.tsx
       - "Select a resource to edit it." (SidebarPanel revised) — if no SidebarPanel test file currently exists, ADD A NEW FILE `gui/src/components/sidebar/__tests__/SidebarPanel.test.tsx` with two minimal tests:
         (a) when selectionKind="resource" and no selection, panel shows "Select a resource to edit it.";
         (b) when selectionKind="component" and no selection, panel shows "Select a component to view its properties."
         (Skip if a SidebarPanel test file already exists — extend it instead.)
       - "Multiple fluids not yet supported." → ResourcesTreePanel.test.tsx (Fluids row tooltip assertion — extend existing fluid-row test)
       - "Used by N component(s)." → ResourcesTreePanel.test.tsx (the existing AlertDialog test already has a regex assertion; update its regex)
       - The sentinel "(leave unset — set in code)" rename — update any test that asserted the old string; the existing ResourceReferencePicker.test.tsx and PowerShapeResourceEditor.test.tsx both assert on the sentinel label.
       - Save/open error dialog NEW strings ("Save failed. ...", "Open failed. ...", "Multiple power-shape...", "Missing power-shape file") → if no save/open dialog test currently exists in `gui/src/store/__tests__/`, add tests inside the same file 62-14 creates (`gui/src/store/__tests__/saveProjectAs.test.ts`) OR create `gui/src/store/__tests__/saveAndOpenErrors.test.ts`. Mock `message` from `@tauri-apps/plugin-dialog` and force the catch branch (e.g. `writeTextFile` rejects). Assert the message arg matches the NEW string.

    Use `screen.getByText("<exact new string>")` or
    `screen.findByText(...)` rather than regex matchers where the
    string is a stable literal. Where the test must remain a regex
    (e.g. dynamic interpolation like the Delete description), update
    the regex to match the NEW pattern: from `/Delete geometry g_used\? It is used by 1 component\(s\)\./` to `/Delete geometry g_used\? Used by 1 component\(s\)\./`.

    Do NOT delete existing tests. Do NOT change test scaffolding or
    setup hooks. Only update the asserted strings AND add new
    pinning assertions for previously unpinned NEW strings.
  </action>
  <verify>
    <automated>cd gui && npm run test -- --run src/components/sidebar/__tests__/ src/components/resources/__tests__/ src/components/project/__tests__/ src/store/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - Vitest command `cd gui && npm run test -- --run src/components/sidebar/__tests__/ src/components/resources/__tests__/ src/components/project/__tests__/ src/store/__tests__/` exits 0 with `0 failed` reported in the final summary line.
    - Full GUI vitest suite: `cd gui && npm run test -- --run` exits 0 with `0 failed`; total test count is ≥ 406 (baseline) and never decreases vs the pre-Task-2 count.
    - `grep -rc "Pick a resource first" gui/src/components/sidebar/__tests__/` returns ≥ 1.
    - `grep -rc "Letters, digits, underscores" gui/src/components/sidebar/__tests__/` returns ≥ 2 (Geometry + PowerShape editor tests).
    - `grep -rc "Amplitude must be finite" gui/src/components/sidebar/__tests__/PowerShapeResourceEditor.test.tsx` returns ≥ 1.
    - `grep -rc "Pick a CSV file via Browse" gui/src/components/sidebar/__tests__/PowerShapeResourceEditor.test.tsx` returns ≥ 1.
    - `grep -rc "Multiple fluids not yet supported" gui/src/components/resources/__tests__/ResourcesTreePanel.test.tsx` returns ≥ 1.
    - `grep -rcE "Used by \\\\\\$\\{|Used by [0-9]| Used by " gui/src/components/resources/__tests__/ResourcesTreePanel.test.tsx` returns ≥ 1.
    - `grep -rc "set in code" gui/src/components/sidebar/__tests__/` returns ≥ 1 (sentinel rename pinned).
    - `grep -rc "fill in code" gui/src/components/` returns 0 (no leftover OLD-string assertions).
    - `grep -rc "It is used by" gui/src/components/` returns 0 (no leftover OLD AlertDialog assertions).
    - `grep -rcE "Save failed|Open failed" gui/src/store/__tests__/` returns ≥ 1 (error-dialog NEW strings pinned).
    - `cd gui && npx tsc --noEmit 2>&1 | grep -cE "^error TS"` returns the same count (or fewer) than the pre-change baseline of 6.
  </acceptance_criteria>
  <done>
    - All five existing Phase 62 test files green; no test count decreases.
    - Every NEW string in the substitution table is asserted by at least one test (verify by `grep -rnF "<NEW string>" gui/src/**/__tests__/` returning at least 1 hit per row).
    - `cd gui && npx tsc --noEmit` produces no NEW errors.
    - Full GUI vitest suite still green: `cd gui && npm run test -- --run` (no regressions in untouched files).
  </done>
</task>

</tasks>

<verification>
- Per-file scoped vitest runs (sidebar, resources, project, store): all green.
- Full GUI vitest: `cd gui && npm run test -- --run` green (≥ 406 tests as
  per VERIFICATION baseline, may be higher after 62-12/13/14/15 add tests).
- Type check: 0 new errors over baseline 6.
- Audit doc `62-15-COPY-AUDIT.md` exists and matches in-code state.
- OLD-string grep gates return 0 matches in non-test source.
- NEW-string grep gates return ≥ 1 match per row in the owning file.
</verification>

<success_criteria>
- Closes VERIFICATION.md Critical Gap #4.
- Every user-facing string introduced in Phase 62 reads as
  engineering-tool voice.
- Every new string is locked by a test, so future copy regressions get
  caught at CI time.
- The substitution decisions are auditable via `62-15-COPY-AUDIT.md`,
  not buried in code diffs.
</success_criteria>

<output>
After completion, create `.planning/phases/62-resources-panel-architecture/62-15-SUMMARY.md`
documenting: total string substitutions applied, total tests touched
(updated + added), any deviations from the substitution table (e.g.
strings that turned out to be locked verbatim by UI-SPEC and were left
unchanged with rationale), and the grep-gate evidence.
</output>
