---
phase: 62
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utilities.jl
  - src/STREAM.jl
  - test/test_utilities.jl
  - test/runtests.jl
autonomous: true
requirements_addressed:
  - D-22
  - D-25
tags:
  - julia
  - source-helper
  - rebin
  - power-shape

must_haves:
  truths:
    - "Working branch is gui-redesign; no new branches created (CLAUDE.md branching policy)."
    - "All Julia identifiers ASCII; no Unicode in code or auto-suggested resource names."
    - "rebin_extensive lands as a public source helper; runs at script runtime, not at GUI codegen time."
    - "rebin_extensive does NOT validate or normalize input (caller-trust per feedback_power_shape_trust_caller.md)."
  artifacts:
    - path: "src/utilities.jl"
      provides: "rebin_extensive + cosine_power_shape + internal _rebin_1d kernel"
      contains: "function rebin_extensive"
    - path: "test/test_utilities.jl"
      provides: "CONS-01..04 conservation and shape parity tests"
      contains: "@testset"
    - path: "src/STREAM.jl"
      provides: "rebin_extensive and cosine_power_shape exported"
      contains: "export rebin_extensive"
  key_links:
    - from: "src/STREAM.jl"
      to: "src/utilities.jl"
      via: "include and export"
      pattern: 'include\("utilities\.jl"\)'
    - from: "test/runtests.jl"
      to: "test/test_utilities.jl"
      via: "include line"
      pattern: 'include\("test_utilities\.jl"\)'
  references:
    - "D-22: Power Shape Resources have four kinds (uniform / z_cosine / file_loaded / unset); codegen target for cosine_power_shape helper."
    - "D-25: rebin_extensive helper in src/utilities.jl; conservative area-weighted regrid; sum-preserving."
---

<objective>
Add Phase 62's Julia source helpers — `rebin_extensive` and `cosine_power_shape` — to a new `src/utilities.jl`, export them from `STREAM.jl`, and create `test/test_utilities.jl` covering conservation invariants CONS-01..04. These helpers are what codegen will emit for `file_loaded` and `z_cosine` Power Shape recipes (D-22). They run at the user's `julia script.jl` invocation, NOT in the GUI process.

Purpose: deliver the Julia foundation so Wave 3 codegen (62-10) can emit `rebin_extensive(readdlm(...), (nz, nx))` and `cosine_power_shape(nz, nx; amplitude=...)` confidently.

Output: A new source file, a new test file, two new exports, one new include line in `runtests.jl`. No GUI changes in this plan.
</objective>

<execution_context>
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/workflows/execute-plan.md
@/home/itay/projects/Julia-STREAM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/62-resources-panel-architecture/62-CONTEXT.md
@.planning/phases/62-resources-panel-architecture/62-RESEARCH.md
@.planning/phases/62-resources-panel-architecture/62-VALIDATION.md
@src/STREAM.jl
@src/composition/helpers.jl
@src/components/heat_diffusion.jl
@CLAUDE.md

<interfaces>
<!-- Existing STREAM.jl include + export block (extracted; do not re-explore) -->

src/STREAM.jl already declares:
- `const HTCCorrelation = Function` (line 9, declared BEFORE includes)
- Existing includes at lines 11-30: fluids, connectors, geometry, physical_models/*, components/*, composition/helpers, solvers, analysis, examples
- Existing export blocks at lines 32-102

Insertion point for Phase 62:
- Add `include("utilities.jl")` AFTER existing includes block (before `end  # module STREAM` at line 104). The natural location is right after `include("examples.jl")` at line 30, or grouped with `include("composition/helpers.jl")`. Pick one and document.
- Add `export rebin_extensive, cosine_power_shape` to a new top-level export line (Phase 62 utilities), grouped near the existing `export symmetric_plate, plate, one_sided_connection, compose_systems, fuel_assembly` line (98).

Existing component-authoring conventions (CLAUDE.md):
- Public functions: docstrings with `# Arguments` and `# Returns`
- Internal helpers prefixed with `_` and not exported (`_rebin_1d` is internal)
- ASCII-only Julia identifiers (memory `feedback_ascii_variable_names.md`)
- New src file -> new mirror test file (`test/test_utilities.jl`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/utilities.jl with rebin_extensive + cosine_power_shape + internal _rebin_1d kernel</name>
  <files>src/utilities.jl, src/STREAM.jl</files>
  <read_first>
    - .planning/phases/62-resources-panel-architecture/62-CONTEXT.md (D-22, D-25 — algorithm requirements, caller-trust posture)
    - .planning/phases/62-resources-panel-architecture/62-RESEARCH.md (Code Examples 1, 2, 3 — algorithm sketches; Pitfall 6 — separable order)
    - src/STREAM.jl (current include order + export blocks; insertion point)
    - src/composition/helpers.jl (sibling file style reference — docstring + function structure conventions)
    - src/components/heat_diffusion.jl (consumer reference — how `power_shape[i,j]` is used; caller-trust invariant)
    - CLAUDE.md (Component authoring conventions section — docstring rules; ASCII-only)
  </read_first>
  <behavior>
    Test contract (verified in Task 2):
    - CONS-02: `rebin_extensive(M, size(M))` returns a Matrix{Float64} equal to M (identity)
    - CONS-01: `sum(rebin_extensive(M, target_shape))` equals `sum(M)` to relative tolerance 1e-12 across identity, integer upsampling (3x3 -> 9x9), integer downsampling (9x9 -> 3x3), and non-integer ratios (5x5 -> 7x3, 4x6 -> 7x5)
    - CONS-03: `rebin_extensive(ones(a, b), (c, d))` equals `(a*b/(c*d)) * ones(c, d)` elementwise (uniform-in maps to uniform-out scaled by area ratio)
    - CONS-04: `cosine_power_shape(nz, nx; amplitude=1.0)` returns Matrix{Float64} of size `(nz, nx)` where every column is identical (uniform along x) and the column has a cosine profile evaluated at cell centers
    - No-validation contract: `rebin_extensive` does NOT assert positivity, normalization, or shape constraints; passes negative values and zeros through unchanged in sum
    - Type-stability: outputs are always `Matrix{Float64}` regardless of input element type
  </behavior>
  <action>
    Create new file `src/utilities.jl` exporting NOTHING directly (exports live in STREAM.jl per CLAUDE.md). Implement three functions:

    1. **Internal helper `_rebin_1d(v::AbstractVector{<:Real}, n_out::Integer)`** — area-weighted conservative 1D rebin per RESEARCH.md Example 1. Source cell `i` occupies interval `[(i-1)/n_in, i/n_in]`; target cell `j` occupies `[(j-1)/n_out, j/n_out]`; overlap area-weighted; identity path when `n_in == n_out` uses `copyto!`. Underscore prefix marks it internal. NO docstring at function-level needed beyond a short description; NOT exported.

    2. **Public `rebin_extensive(M::AbstractMatrix{<:Real}, target_shape::Tuple{Int,Int})`** — separable z-then-x: first rebin each column along axis 1 producing `intermediate` Matrix{Float64} of size `(nz_out, nx_in)`, then rebin each row along axis 2 producing `out` Matrix{Float64} of size `(nz_out, nx_out)`. Per RESEARCH Pitfall 6, the z-then-x order is canonical and locked; document this in the docstring. Docstring MUST cite the caller-trust posture explicitly (no validation, no normalization). Include `# Arguments` and `# Returns` sections per CLAUDE.md.

    3. **Public `cosine_power_shape(nz::Integer, nx::Integer; amplitude::Real=1.0)`** — return a Matrix{Float64} of size `(nz, nx)`. Build the axial vector `zaxis[i] = cos(pi * (i - 0.5) / nz - pi/2)^2` for `i in 1:nz` (cell-centered cosine-squared per RESEARCH Example 3), scale by `amplitude`, broadcast uniformly across `nx` columns via `repeat(col, 1, nx)`. ASCII-only: use `pi`, not the Unicode π character. Docstring per CLAUDE.md with `# Arguments` + `# Returns`. Note in docstring that the formula is `[ASSUMED]` parity with Python STREAM `uniform_x_power_shape` (lines 297-335 of `/home/itay/projects/STREAM/stream/composition/mtr_geometry.py`) — Task 2 includes a parity sanity check.

    Then modify `src/STREAM.jl`:
    - Add `include("utilities.jl")` after `include("examples.jl")` (line 30, grouped at the bottom of the include block since utilities is general-purpose not domain-specific)
    - Add a new export line near line 98 reading: `export rebin_extensive, cosine_power_shape` (per D-25, both are public)

    NO module declaration inside `utilities.jl` — it is included into the `STREAM` module like every other source file. NO Unicode identifiers anywhere. NO export statement inside `utilities.jl` (per CLAUDE.md "Never add `export` statements inside component files").
  </action>
  <verify>
    <automated>bin/jl -e 'using STREAM; println(rebin_extensive(ones(3,3), (5,5)))' 2>&amp;1 | tee /tmp/62-01-task1.log; grep -q "5×5 Matrix" /tmp/62-01-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - `src/utilities.jl` exists and contains `function rebin_extensive(M::AbstractMatrix{<:Real}, target_shape::Tuple{Int,Int})` and `function cosine_power_shape(nz::Integer, nx::Integer; amplitude::Real=1.0)` and internal `function _rebin_1d(v::AbstractVector{<:Real}, n_out::Integer)`
    - `grep -c '^include("utilities.jl")' src/STREAM.jl` returns 1
    - `grep -cE '^export.*rebin_extensive' src/STREAM.jl` returns 1
    - `grep -cE '^export.*cosine_power_shape' src/STREAM.jl` returns 1
    - `grep -c "π\|ξ\|β\|ρ" src/utilities.jl` returns 0 (ASCII-only invariant; no Unicode identifiers)
    - `grep -c "assert\|@assert\|throw(ArgumentError\|isnan(" src/utilities.jl` returns 0 (caller-trust per D-25 + feedback_power_shape_trust_caller.md)
    - Loading the module succeeds: `bin/jl -e 'using STREAM; @assert isdefined(STREAM, :rebin_extensive); @assert isdefined(STREAM, :cosine_power_shape)'` exits 0
    - Smoke: `rebin_extensive(ones(3,3), (5,5))` produces a `5×5 Matrix{Float64}` (verified via the automated check above)
    - Implements D-22 (cosine_power_shape helper, file_loaded codegen target) and D-25 (rebin_extensive)
  </acceptance_criteria>
  <done>src/utilities.jl present with three functions; STREAM.jl includes + exports them; module loads cleanly via `bin/jl`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test/test_utilities.jl covering CONS-01..04; append to runtests.jl</name>
  <files>test/test_utilities.jl, test/runtests.jl</files>
  <read_first>
    - .planning/phases/62-resources-panel-architecture/62-VALIDATION.md (CONS-01..04 definitions; Wave 0 Julia side list)
    - .planning/phases/62-resources-panel-architecture/62-RESEARCH.md (Pitfall 6 — test asserts only sum-conservation, NOT order-equivalence of matrix values)
    - test/runtests.jl (existing include pattern — mirror that style)
    - src/utilities.jl (the implementation under test, written in Task 1)
    - CLAUDE.md (test placement rule: test file mirrors src file)
  </read_first>
  <behavior>
    Test file MUST contain `@testset` blocks covering:
    - **CONS-02 identity:** `rebin_extensive(M, size(M)) == M` (strict equality) for at least one random 4x6 Float64 matrix
    - **CONS-01 sum-conservation:** `sum(rebin_extensive(M, target_shape)) ≈ sum(M)` rtol=1e-12 across:
      (a) identity 4x4 -> 4x4
      (b) integer up 3x3 -> 9x9
      (c) integer down 9x9 -> 3x3
      (d) non-integer up 5x5 -> 7x3
      (e) non-integer down 4x6 -> 7x5
      (f) 1xN row degenerate: 1x8 -> 1x3 and 1x8 -> 5x8
      (g) Nx1 column degenerate: 8x1 -> 3x1 and 8x1 -> 8x5
      (h) all-zeros: zeros(4,6) -> 7x5
      (i) all-ones: ones(4,6) -> 7x5
    - **CONS-03 uniform scale:** `rebin_extensive(ones(3,4), (7,5)) ≈ (12/35) * ones(7,5)` elementwise rtol=1e-12
    - **CONS-04 cosine shape:** `M = cosine_power_shape(10, 5; amplitude=1.0)`; assert `size(M) == (10, 5)`; assert `all(M[:, j] == M[:, 1] for j in 1:5)` (uniform along x); assert `M[5, 1] > M[1, 1]` and `M[5, 1] > M[10, 1]` (peak near axial center); assert returned eltype is `Float64`
  </behavior>
  <action>
    Create `test/test_utilities.jl` using the standard pattern (`using Test`, `using STREAM`, one or more `@testset` blocks). Reference RESEARCH Example 1/2/3 line numbers in comments only if helpful. Cite the relevant invariant ID (CONS-01..04) in each `@testset` header string so the verifier can correlate.

    DO NOT write a "rebin (10x5)->(3x7) along z-then-x equals rebin along x-then-z" test (per RESEARCH Pitfall 6 — only sum-conservation is invariant; the actual matrix values may differ at ULP between orders, which is fine).

    DO NOT add validation/assertion tests on negative inputs — the caller-trust contract (D-25) means passing negative or NaN values is the caller's problem, not a function-level check. Document this in a comment near the no-validation testset (which itself MUST NOT exist).

    Then modify `test/runtests.jl`: append the single line `include("test_utilities.jl")` at the appropriate position (alphabetical or topical — match the surrounding pattern). The orchestrator file is described in CLAUDE.md as "Thin orchestrator: one include() per test file, nothing else" — keep it that way.
  </action>
  <verify>
    <automated>bin/jl test/test_utilities.jl 2>&amp;1 | tail -20 | tee /tmp/62-01-task2.log; grep -qE "(Test Summary|All tests passed)" /tmp/62-01-task2.log &amp;&amp; ! grep -qE "(Error|FAIL)" /tmp/62-01-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - `test/test_utilities.jl` exists; running `bin/jl test/test_utilities.jl` exits 0 with all testsets passing
    - `grep -c '^include("test_utilities.jl")' test/runtests.jl` returns 1
    - At least 4 `@testset` blocks present (one per CONS-NN) — grep `grep -c '@testset' test/test_utilities.jl` returns >= 4
    - Test names reference invariant IDs: `grep -cE "CONS-0[1-4]" test/test_utilities.jl` returns >= 4
    - No order-equivalence test: `grep -c "x.then.z\|x_then_z\|both orders" test/test_utilities.jl` returns 0 (per RESEARCH Pitfall 6)
    - Full Julia suite still passes: `bin/jl test/runtests.jl` exits 0 (smoke; the v1.1 baseline is 239 tests passing per STATE.md — Phase 62 should add tests but not break any)
    - Implements CONS-01, CONS-02, CONS-03, CONS-04 (validation contract for D-25)
  </acceptance_criteria>
  <done>test_utilities.jl present, included in runtests.jl orchestrator, full Julia test suite green via daemon.</done>
</task>

</tasks>

<verification>
- `bin/jl test/test_utilities.jl` — green, 4+ testsets, no FAIL, no Error
- `bin/jl test/runtests.jl` — full Julia suite green (no regressions vs 239-test baseline)
- `grep -rE "rebin_extensive\b" src/STREAM.jl` returns the export line
- `git diff --name-only` shows exactly: `src/utilities.jl` (new), `src/STREAM.jl` (modified), `test/test_utilities.jl` (new), `test/runtests.jl` (modified)
</verification>

<success_criteria>
- `rebin_extensive` and `cosine_power_shape` are public, exported from STREAM.jl, ASCII-only, no validation/normalization (caller-trust per D-25)
- Conservation invariants CONS-01..04 hold per `test/test_utilities.jl`
- No regression on the full Julia test suite (`bin/jl test/runtests.jl`)
- File structure mirror rule honored: src/utilities.jl <-> test/test_utilities.jl
</success_criteria>

<output>
After completion, create `.planning/phases/62-resources-panel-architecture/62-01-SUMMARY.md` capturing:
- Final algorithm choice for `_rebin_1d` (any deviation from RESEARCH Example 1)
- Cosine formula confirmation (cell-centered cosine-squared) — note any Python parity spike result
- Exact insertion line numbers in STREAM.jl for the new include + export
- Any test that flagged a numerical surprise (ULP behavior, edge cases)
</output>
