---
phase: 12.1-pipegeometry
verified: 2026-03-14T12:45:00Z
status: passed
score: 9/9 must-haves verified
re_verification: false
---

# Phase 12.1: PipeGeometry — Verification Report

**Phase Goal:** Introduce PipeGeometry struct, fix 4.46x geometry mismatch between Julia and Python STREAM for MTR rectangular channels, and restore quantitative 1% VAL-01/02/03 validation against Python STREAM reference.

**Verified:** 2026-03-14T12:45:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | `PipeGeometry` struct is defined and exported; circular and rectangular outer constructors are callable | VERIFIED | `struct PipeGeometry` at `src/components.jl` line 31; single outer constructor with `D=nothing`/`y=nothing` sentinels at line 44; exported from `src/STREAM.jl` line 14 |
| 2  | `Channel`, `ChannelHeatFlux`, `ChannelAndContacts` each accept `geometry::PipeGeometry` and extract `L`/`Dh`/`A` from it | VERIFIED | `function Channel(; name, n::Int, geometry::PipeGeometry, g = 0.0)` at line 63; `function ChannelAndContacts(; name, n::Int, geometry::PipeGeometry, g = 0.0)` at line 301; `function ChannelHeatFlux(; name, n::Int, geometry::PipeGeometry, g = 0.0, T_wall)` at line 375 |
| 3  | `ChannelAndContacts` uses `geometry.heated_parts[1]`/`[2]` per face; `Channel`/`ChannelHeatFlux` use `sum(geometry.heated_parts)` | VERIFIED | `sum(geometry.heated_parts)` in Channel energy balance (line 101) and ChannelHeatFlux energy balance (line 416); `geometry.heated_parts[1]` and `geometry.heated_parts[2]` in ChannelAndContacts per-face equations (lines 345-352); no `π * Dh / 2` or `π * Dh` as heated perimeter remains anywhere |
| 4  | All existing test call sites updated to `PipeGeometry` API; no old-style `(L=..., D=..., A=...)` flat kwargs remain; `solvers.jl` builder functions updated | VERIFIED | 17 `PipeGeometry(` occurrences in `test/runtests.jl` (lines 123-1003); 3 occurrences in `src/solvers.jl` (lines 63, 148, 206); grep for old-style `Channel(.*L=.*D=.*A=` returns only PipeGeometry-wrapped calls |
| 5  | `generate_mtr_reference.py` `pipe_ch` uses rectangular MTR geometry with `heated_parts=(Y_LEN, Y_LEN)` | VERIFIED | Line 82: `heated_parts=(Y_LEN, Y_LEN)` with `Y_LEN = 0.07` (line 39); `heated_perimeter=2 * Y_LEN` at line 79 |
| 6  | Python reference script ran and produced numeric constants for all three VAL scenarios | VERIFIED | Constants hardcoded in `test/runtests.jl`: VAL-01 constants at lines 895-899 (315.1463 K, 0.599315 kg/s, 318.4815 K); VAL-02 at line 976 (344.3571 K); VAL-03 at lines 1036-1037 (315.1463 K, 0.599315 kg/s); no `XXXX.XXXX` placeholders present |
| 7  | VAL-01 has 5 `isapprox(...; rtol=0.01)` quantitative assertions (T_outlet_l/r, mdot_l/r, T_plate_center) | VERIFIED | Lines 900-904: `@test isapprox(T_out_l, val01_T_outlet_l_ref; rtol=0.01)` through `@test isapprox(T_center, val01_T_plate_center; rtol=0.01)` — all 5 assertions present; physics-only guards also retained at lines 906-916 |
| 8  | VAL-02 has quantitative `isapprox` assertion for `T_plate_center`; VAL-03 has T_outlet and mdot quantitative assertions | VERIFIED | VAL-02 line 977: `@test isapprox(T_center_02, val02_T_plate_center; rtol=0.01)`; VAL-03 lines 1038-1039: `@test isapprox(T_out_l_03, val03_T_outlet_ref; rtol=0.01)` and `@test isapprox(mdot_l_03, val03_mdot_ref; rtol=0.01)`; VAL-03 T_plate_center omitted with documented rationale (Python one_sided_connection gives physically inconsistent value) |
| 9  | STATE.md stale entries updated: `π*Dh/2` references replaced; "Python STREAM refs incompatible" entry removed | VERIFIED | STATE.md line 79 updated to `geometry.heated_parts[i]` wording; line 84 updated to `h_tc*geometry.heated_parts[i]*dz*deltaT`; "Python STREAM refs incompatible" row absent from key decisions table; new entry at line 96 documents VAL-03 omission decision; `grep "Python STREAM refs incompatible" STATE.md` returns no matches in the key decisions section |

**Score: 9/9 truths verified**

---

## Required Artifacts

### Plan 01 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/components.jl` | `PipeGeometry` struct + outer constructors + refactored Channel/CAC/CHF | VERIFIED | `struct PipeGeometry` at line 31; single outer constructor at line 44; all three channel constructors accept `geometry::PipeGeometry`; `heated_parts` used correctly in all energy balance equations |
| `src/STREAM.jl` | `PipeGeometry` in export list | VERIFIED | Line 14: `export ..., PipeGeometry` |
| `test/runtests.jl` | All call sites updated to `PipeGeometry` API | VERIFIED | 17 `PipeGeometry(` occurrences; `PipeGeometry` in `import STREAM:` at line 6; no old-style flat `L=..., D=..., A=...` channel constructor calls remain |
| `test/generate_mtr_reference.py` | `heated_parts=(Y_LEN, Y_LEN)` with `Y_LEN=0.07` | VERIFIED | Line 39: `Y_LEN = 0.07`; line 82: `heated_parts=(Y_LEN, Y_LEN)` |

### Plan 02 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `test/runtests.jl` | VAL-01/02/03 with hardcoded reference constants and `isapprox(...; rtol=0.01)` assertions | VERIFIED | 8 total `isapprox` assertions across VAL-01 (5), VAL-02 (1), VAL-03 (2); reference constant variables follow `val0N_*_ref` naming convention; no placeholder values |
| `.planning/STATE.md` | Updated key decisions — no stale `π*Dh/2` references; "Python STREAM refs incompatible" entry removed | VERIFIED | Key decisions table at lines 79 and 84 use `geometry.heated_parts[i]` wording; "Python STREAM refs incompatible" row not present; new `PipeGeometry kwarg dispatch` entry at line 93 |

---

## Key Link Verification

### Plan 01 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `Channel` constructor | `sum(geometry.heated_parts)` | energy balance loop | WIRED | `sum(geometry.heated_parts)` at line 101 of `components.jl` |
| `ChannelAndContacts` constructor | `geometry.heated_parts[1]` / `[2]` | per-face energy balance | WIRED | Lines 345-352 of `components.jl`: left face uses `[1]`, right face uses `[2]` in both `Dt(T[i])` and `Q_flow` equations |
| `src/STREAM.jl` | `PipeGeometry` | export list | WIRED | Line 14: `export ..., PipeGeometry` confirmed |

### Plan 02 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `generate_mtr_reference.py` output | `runtests.jl` VAL-01/02/03 hardcoded constants | manual checkpoint | WIRED | Constants pasted at lines 895-899 (VAL-01), 976 (VAL-02), 1036-1037 (VAL-03); no `XXXX.XXXX` placeholders; commit `492a703` |
| VAL-01/02/03 tests | `isapprox` assertions | `rtol=0.01` tolerance | WIRED | 8 `isapprox(...; rtol=0.01)` lines in runtests.jl across all three VAL tests |

---

## Requirements Coverage

No formal requirement IDs were assigned to Phase 12.1 (per VALIDATION.md). The phase addresses the physics correctness gap from Phase 12. The inherited requirements addressed are:

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|-------------|--------|----------|
| VAL-01 (from Phase 12) | Phase 12 gap | Symmetric MTR: T_outlet, mdot, T_plate center match Python STREAM within 1% | SATISFIED | 5 `isapprox` assertions at rtol=0.01 against hardcoded Python STREAM rectangular MTR constants (lines 900-904) |
| VAL-02 (from Phase 12) | Phase 12 gap | Asymmetric MTR: correct non-symmetric temperature profile | SATISFIED | Quantitative `isapprox(T_center_02, 344.3571; rtol=0.01)` at line 977 plus physics directionality assertions |
| VAL-03 (from Phase 12) | Phase 12 gap | One-sided coupling: T_outlet and mdot within 1%; adiabatic right face confirmed | SATISFIED | 2 `isapprox` assertions (T_outlet, mdot) at lines 1038-1039; T_plate_center omitted with documented rationale (Python value physically inconsistent); adiabatic `Q_flow=0` check at lines 1053-1057 |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `test/runtests.jl` | 1040-1043 | Comment explaining VAL-03 T_plate_center omission | Info | Intentional documentation; no assertion placeholder; the omission is justified and documented |

No TODO/FIXME/placeholder/stub patterns found in any modified source file. No empty implementations. No `return null` or static-return stubs. All `isapprox` assertions use actual numeric constants (not `XXXX.XXXX` placeholders).

The one notable deviation from Plan 02: VAL-03 has 2 quantitative assertions instead of 3 (T_plate_center omitted). This is documented in `12.1-02-SUMMARY.md` and in `STATE.md` line 96. The omission is correct — the Python `one_sided_connection` returns 318.48 K for one-sided geometry (identical to symmetric, which is physically inconsistent); Julia returns 323.64 K (correct). The physics assertion `T_center_03 > T_out_l_03` provides qualitative coverage.

---

## Human Verification Required

None. All assertions are automated `@test` / `isapprox` calls. The manual checkpoint (running `generate_mtr_reference.py`) was completed during execution and its output is hardcoded in `test/runtests.jl`. No visual, real-time, or external service behavior requires human review.

---

## Commit Verification

| Commit | Description | Verified |
|--------|-------------|---------|
| `5cb3319` | feat(12.1-01): PipeGeometry struct + channel constructor refactor | Present in git log |
| `83d0506` | feat(12.1-01): test call sites + Python reference script update | Present in git log |
| `8960c9f` | fix(12.1-02): VAL-02 initial guess fix in generate_mtr_reference.py | Present in git log |
| `492a703` | feat(12.1-02): hardcoded Python STREAM reference constants + 1% assertions | Present in git log |
| `8bef22e` | chore(12.1-02): STATE.md cleanup | Present in git log |

---

## Summary

Phase 12.1 fully achieves its stated goal. The 4.46x geometry mismatch (circular `π*Dh/2` vs. rectangular `y=0.07 m` per face) is eliminated by the `PipeGeometry` struct. All three channel constructors accept `geometry::PipeGeometry`. Every call site in `test/runtests.jl` and `src/solvers.jl` was migrated. The Python reference script uses the correct rectangular MTR geometry. VAL-01 has 5 quantitative 1% assertions; VAL-02 has 1; VAL-03 has 2 (T_plate_center deferred with documented rationale). STATE.md stale entries are cleaned up. The Phase 12 gap ("Python STREAM refs incompatible with Julia MTR geometry") is closed.

---

_Verified: 2026-03-14T12:45:00Z_
_Verifier: Claude (gsd-verifier)_
