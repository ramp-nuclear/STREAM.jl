// Stub - replaced in Task 2
import type { Parameter } from "@/registry/types";

interface MatrixBadgeProps {
  param: Parameter;
}

export default function MatrixBadge({ param: _param }: MatrixBadgeProps) {
  return <div>MatrixBadge placeholder</div>;
}
